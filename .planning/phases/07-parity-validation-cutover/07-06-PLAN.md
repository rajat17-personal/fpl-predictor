---
phase: 07-parity-validation-cutover
plan: 06
type: execute
wave: 6
depends_on: ["07-05"]
files_modified:
  - api/main.py
  - tests/test_react_seam.py
  - tests/test_fixture_mode.py
  - scripts/smoke_test.sh
  - .github/workflows/ci.yml
  - frontend/scripts/check-tokens.mjs
  - .planning/phases/02-data-layer-non-pitch-pages/PARITY-DEVIATIONS.md
  - .planning/phases/07-parity-validation-cutover/PARITY-REPORT.md
  - README.md
autonomous: false
requirements: [CUT-01]

estimate:
  tokens: 68000
  raw_tokens: 68000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "The cutover is approved by the user in person, against the completed PARITY-REPORT.md, and is never taken automatically (D-15)"
    - "After the flip, a plain `uvicorn api.main:app` with no environment variables set serves the React build at / and the live web/data JSON at /data (D-09)"
    - "FPL_FRONTEND=vanilla puts the vanilla site back with no code change, and that rollback path is asserted inside the built container image in CI (D-09, D-10)"
    - "The fixture-mode branch still wins over both, so the E2E suite's topology is unchanged by the flip"
    - "PARITY-DEVIATIONS.md carries a closing entry marking cutover and is a historical record from that point, no longer a gate (D-11)"
    - "The vanilla site assets remain in the repository, frozen and unmaintained, available as the one-env-var rollback for the agreed window (D-09, D-10)"
    - statement: "The Docker image still contains both frontends after the flip — only the serving default changed, not the image contents (D-12)"
      verification: backstop
  prohibitions:
    - requirement_id: CUT-01
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT change, pause, or reschedule the live weekly recommendation cycle — scripts/daily.sh, scripts/weekly.sh, their cron lines, and the web/data export contract are read-only for the whole of this phase"
    - requirement_id: CUT-01
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT approve or take the cutover on evidence that is not in PARITY-REPORT.md — no verdict from memory, and no delta treated as explained without a ledger entry number, a fixing commit SHA, or a named cron-green citation"
    - requirement_id: CUT-01
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT delete the vanilla site assets, remove the vanilla serving branch, or otherwise close the rollback path in this phase — retirement here means freezing and un-defaulting vanilla, never removing it"
    - requirement_id: CUT-01
      category: privacy
      status: resolved
      verification: judgment
      statement: "MUST NOT commit the user's personal FPL identity into PARITY-REPORT.md, the closing ledger entry, or any commit message — the numeric entry id 6980093 and the team name only"
  artifacts:
    - path: "api/main.py"
      provides: "React as the serving default with a vanilla rollback branch"
      contains: "FPL_FRONTEND"
    - path: "frontend/scripts/check-tokens.mjs"
      provides: "OKLCH chroma-budget gate against the React palette only, with the cross-site palette comparison removed"
      contains: "index.css"
    - path: ".planning/phases/02-data-layer-non-pitch-pages/PARITY-DEVIATIONS.md"
      provides: "The closed deviation ledger with a cutover entry"
      contains: "cutover"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "scripts/smoke_test.sh"
      via: "post-flip smoke invocation asserting the rollback env var restores vanilla in the container"
      pattern: "SMOKE_VANILLA_MODE"
    - from: "api/main.py"
      to: "tests/test_fixture_mode.py"
      via: "the unset-env mount assertion that moves when the default flips"
      pattern: "Mount"
---

<objective>
Retire the vanilla site as the default, on the user's explicit approval, without closing the door
behind us.

Purpose: CUT-01's final clause. The React site has completed a full gameweek cycle side by side
with verified parity, so it becomes what a plain `uvicorn api.main:app` serves. Vanilla's assets
stay in the repository, frozen and one environment variable away, for the agreed rollback window.
The deviation ledger closes and becomes history.

Output: React as the serving default with a proven vanilla rollback, the cross-site palette
lockstep assertions removed, a closing ledger entry, and README updated to describe what a plain
uvicorn now serves.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-parity-validation-cutover/07-CONTEXT.md
@.planning/phases/07-parity-validation-cutover/07-05-SUMMARY.md
@.planning/phases/07-parity-validation-cutover/PARITY-REPORT.md
@.planning/phases/02-data-layer-non-pitch-pages/PARITY-DEVIATIONS.md
</context>

## Artifacts this phase produces

See the table in `07-01-PLAN.md` under "Artifacts this phase produces" — the single authoritative
list of symbols created by Phase 7. This plan introduces one further new symbol,
`SMOKE_VANILLA_MODE` (an env var on `scripts/smoke_test.sh`, replacing `SMOKE_REACT_MODE` once
react is the default), and retires `SMOKE_REACT_MODE`. Neither exists in the codebase at planning
time; do not treat them as drift against existing source.

## Flagged Assumptions (spec-less probe fallback)

`CUT-01` remains the single `unclassified` / `unresolved` row from the deterministic edge probe;
the full statement is in `07-01-PLAN.md` under "Flagged Assumptions". This plan is where that
assumption bites hardest: the sequencing constraint ("stays live until … only then is it retired")
is enforced by wave ordering plus the Task 1 human gate, not by any machine-checkable predicate.
Executing this plan out of wave order, or approving its gate without reading the report, would
satisfy every `must_have` here while violating CUT-01 itself. Task 1 exists precisely to make that
impossible in practice; nothing in `must_haves` can make it impossible in principle.

## Decisions implemented here

| ID | Decision | Task |
|---|---|---|
| D-09 | Retirement = flip the seam's default; vanilla stays servable | T2 |
| D-10 | Rollback policy: data-wrong only, one-gameweek window | T1 (stated at the gate), T2 (rollback proven in CI) |
| D-11 | Lockstep and the ledger both close at the flip | T3 |
| D-12 | Docker image keeps both frontends this phase | T2 (asserted, Dockerfile untouched) |
| D-15 | Cutover is a human gate; never auto-flip | T1 |

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <decision>Approve the cutover: make the React site what a plain uvicorn serves, and freeze vanilla as the rollback</decision>
  <context>
    The validation gameweek is complete. PARITY-REPORT.md holds three stage tables — pre-deadline,
    mid-gameweek and post-finish — covering all eight pages, plus the defect-closure table, the
    manual eyeball verdict, the entry-6980093 same-session solver verdict, and the cron evidence
    for the whole window. Its cutover-readiness block states the remaining unexplained-deltas
    figure and maps onto the three ROADMAP success criteria.

    This is the decision CUT-01 gates on and D-15 requires you to take personally. Nothing here
    is auto-approvable.

    What approving changes: `uvicorn api.main:app` with no environment variables set starts
    serving the React build instead of `web/`. What it does not change: the vanilla assets stay
    in the repository, `FPL_FRONTEND=vanilla` puts them straight back, the Docker image keeps
    both frontends, and the `web/data` export contract is untouched. Per D-10 the rollback is
    considered live for one further gameweek and is used only if the React site shows wrong data
    or blocks a weekly action — cosmetic bugs are fixed forward.

    Read PARITY-REPORT.md's cutover-readiness block before answering. If any manual verdict is
    still unfilled, or the remaining unexplained-deltas figure is not zero, hold.
  </context>
  <options>
    <option id="approve">
      <name>Approve the flip</name>
      <pros>The cycle passed with zero unexplained deltas; the React site has carried a real FPL week end to end; rollback is one environment variable away and is proven in CI</pros>
      <cons>The daily driver changes; a defect not caught in one gameweek's data states would now be on the site you actually use</cons>
    </option>
    <option id="hold">
      <name>Hold — extend validation into the next gameweek</name>
      <pros>A page carried forward, an unfilled manual verdict, or a non-zero remaining-deltas figure gets a real second data state before the flip; costs only calendar time</pros>
      <cons>The phase stays open another week and the two-process session discipline continues</cons>
    </option>
    <option id="abort">
      <name>Do not flip — reassess the cutover approach</name>
      <pros>Appropriate if the week surfaced something structural rather than a list of closable deltas</pros>
      <cons>CUT-01 stays open and the next milestone inherits an unproven flip</cons>
    </option>
  </options>
  <resume-signal>Reply: approve, hold, or abort</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Flip the serving default to React and prove the vanilla rollback in the container</name>
  <reversibility rating="reversible">Rollback is the same environment seam, not a code change: `FPL_FRONTEND=vanilla` restores the vanilla site, the branch and the assets both stay in the repository, and CI asserts that path on every build.</reversibility>
  <precondition>PARITY-REPORT.md's cutover-readiness block reports zero remaining unexplained deltas, and the Task 1 decision checkpoint was answered "approve"</precondition>
  <files>api/main.py, tests/test_react_seam.py, tests/test_fixture_mode.py, scripts/smoke_test.sh, .github/workflows/ci.yml</files>
  <read_first>
    - api/main.py — the three-branch mount split written in 07-01 and the `_REACT_MODE` declaration beside `_FIXTURE_ROOT`. This task rearranges those branches; it does not add a fourth mechanism.
    - tests/test_react_seam.py — the four serving-mode tests, whose expectations invert for the unset-env case once the default flips.
    - tests/test_fixture_mode.py — `test_unset_env_leaves_a_single_root_mount`, whose docstring and assertion both describe the pre-flip default and must be rewritten to describe the post-flip one. Read the whole file so the reload/teardown discipline is preserved.
    - scripts/smoke_test.sh — the react-mode branch added in 07-01, including its env knob, its distinct default port and its index-marker assertion, all of which invert here.
    - .github/workflows/ci.yml — the `image:` job's two smoke invocations and the env key on the second one.
    - frontend/index.html and web/index.html — the two index markers that distinguish which frontend a response came from.
    - Dockerfile — confirm it COPYs both `web/` and `frontend/dist`; it must stay unchanged (D-12).
  </read_first>
  <action>
Make React the default and make the rollback a tested path rather than a claim.

**`api/main.py`.** Keep `_FIXTURE_ROOT` as the first branch, unchanged. Invert the remaining two:
the middle branch now activates on the literal env value `vanilla` and mounts the vanilla
directory at `/` exactly as today's default does; the final `else` branch becomes the React
serving path, mounting `/data` from the live export directory and then `/` from the built React
bundle, in that registration order. Update the `_REACT_MODE` declaration and its announcement
line to reflect the inversion — announce when the rollback is active rather than when React is,
since React is now the silent default. Replace the branch comments so they describe the new
reality: React is what a plain uvicorn serves; the vanilla branch is the D-10 rollback, live for
one further gameweek; and the fixture branch still wins over both.

**`tests/test_react_seam.py`.** Invert the two default-related tests: with no environment
variables set, assert two mounts named `data` then `site` with `site` resolving to the built
React bundle; with the rollback value set, assert a single mount named `site` resolving to the
vanilla directory. Leave the fixture-precedence test asserting the same thing it does today —
that is the guard that the E2E suite's topology did not move.

**`tests/test_fixture_mode.py`.** Rewrite `test_unset_env_leaves_a_single_root_mount` to describe
and assert the post-flip default: two mounts, `data` then `site`, React. Rename it so its name
states what it now proves. Change nothing else in that file — the fixture-mode tests and the
production-pool restore regression guard are unrelated to the flip.

**`scripts/smoke_test.sh`.** Retire the react-mode env knob and introduce a vanilla-mode one in
its place, documented in the usage header. With it set, the container is started with the
rollback env value and the root response is asserted to carry vanilla's markup marker and not the
React mount-point marker. With it unset, the existing fixture-mode invocation is unchanged. The
point of the assertion has inverted: before the flip CI proved React could be served; after it,
CI proves the rollback still works, which is the escape hatch D-10 depends on.

**`.github/workflows/ci.yml`.** Update the second smoke step's env key to the new name and its
comment to say it proves the D-10 rollback path inside the published image. Add no new action and
no new SHA pin. Keep both smoke invocations before the Trivy scan.

Do not touch the Dockerfile — D-12 keeps both frontends in the image this phase. Do not delete
any vanilla asset — that is a later milestone's cleanup, listed in CONTEXT.md's deferred ideas.

Docker is not installed on this host, so verify the container assertion statically here and
record in the SUMMARY that the CI `image:` job is the real gate.
  </action>
  <verify>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import importlib, api.main as m; from starlette.routing import Mount; r=importlib.reload(m); print([x.name for x in r.app.routes if isinstance(x,Mount)])"</automated>
    <fails_when>non-zero exit, or the printed list is not exactly ['data', 'site'] — with no environment variables set the default must now be the two-mount React topology</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import os,importlib,api.main as m; from starlette.routing import Mount; os.environ['FPL_FRONTEND']='vanilla'; r=importlib.reload(m); ms=[x for x in r.app.routes if isinstance(x,Mount)]; print([x.name for x in ms], [str(x.app.directory) for x in ms])"</automated>
    <fails_when>non-zero exit, or the printed name list is not exactly ['site'], or the printed directory does not end in the vanilla site directory — the rollback path is broken</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m pytest -q</automated>
    <fails_when>non-zero exit, or the summary line reports any failed or errored test</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/ruff check .</automated>
    <fails_when>non-zero exit, or any diagnostic line is printed</fails_when>
    <automated>npm --prefix e2e run test</automated>
    <fails_when>non-zero exit, or the Playwright summary line reports any failed spec — the fixture branch must still win over the new default</fails_when>
    <automated>bash -n scripts/smoke_test.sh</automated>
    <fails_when>non-zero exit, or any parse error is printed</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import sys,yaml; d=yaml.safe_load(open('.github/workflows/ci.yml')); s=[str(x) for x in d['jobs']['image']['steps']]; n=sum('SMOKE_VANILLA_MODE' in x for x in s); print('rollback smoke steps:', n); sys.exit(0 if n==1 else 1)"</automated>
    <fails_when>non-zero exit, or the printed count is not exactly 1 (0 means the step was never renamed; more than 1 means it was duplicated)</fails_when>
    <automated>git diff --quiet -- Dockerfile web/assets web/index.html && echo IMAGE AND VANILLA ASSETS UNCHANGED</automated>
    <fails_when>non-zero exit, or the line is not printed — D-12 keeps both frontends in the image and the rollback needs the vanilla assets intact</fails_when>
  </verify>
  <acceptance_criteria>
    - With no environment variables set, `api.main.app` has exactly two `Mount` routes named `data` then `site`, and `site`'s directory resolves to the built React bundle.
    - With the rollback env value set, `api.main.app` has exactly one `Mount` named `site` resolving to the vanilla site directory.
    - With `FPL_FIXTURE_DIR` set, the fixture branch still wins regardless of the frontend env var, and `npm --prefix e2e run test` is green.
    - `tests/test_fixture_mode.py` has exactly one changed test — the renamed unset-env mount assertion — and `git diff -- tests/test_fixture_mode.py` shows no other test body modified.
    - `grep -v '^#' scripts/smoke_test.sh | grep -c 'SMOKE_REACT_MODE'` returns 0 and the same filtered grep for the rollback knob returns at least 2.
    - The yaml probe reports exactly one rollback smoke step, and both smoke invocations still precede the Trivy scan step.
    - `git diff --quiet -- Dockerfile web/assets web/*.html scripts/daily.sh scripts/weekly.sh` succeeds — the image, the vanilla assets and the pipeline are all untouched.
    - `git ls-files web/index.html web/assets/style.css web/assets/app.js` lists all three — nothing was deleted.
  </acceptance_criteria>
  <done>A plain `uvicorn api.main:app` serves the React build with the live `/data` mount registered first; `FPL_FRONTEND=vanilla` restores the vanilla site and CI asserts that rollback inside the built image; the fixture branch still wins and the E2E suite is green; the Dockerfile and every vanilla asset are untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Close the deviation ledger and retire the cross-site palette lockstep</name>
  <reversibility rating="costly">Resurrecting the lockstep discipline after post-cutover React-only palette changes would mean re-porting every one of them to vanilla by hand; the D-10 rollback window is why vanilla stays frozen rather than maintained.</reversibility>
  <precondition>api.main serves the React build by default — the Task 2 flip is committed</precondition>
  <files>frontend/scripts/check-tokens.mjs, .planning/phases/02-data-layer-non-pitch-pages/PARITY-DEVIATIONS.md, .planning/phases/07-parity-validation-cutover/PARITY-REPORT.md</files>
  <read_first>
    - frontend/scripts/check-tokens.mjs — the whole file. Three regions matter: the module docstring at the top, whose closing clause describes the cross-site palette invariant; the `vanillaCssUrl` constant declared beside `reactCssUrl` near the imports; and the section headed with the lockstep invariant comment near the end, containing the `existsSync` skip branch, the `VANILLA_TO_REACT` map and the four per-token `record` calls. Those three regions are what this task removes; the OKLCH chroma-budget, hue-lock, lightness, `color-scheme` and body-paint assertions against `frontend/src/index.css` all stay.
    - .planning/phases/02-data-layer-non-pitch-pages/PARITY-DEVIATIONS.md — the whole file: the opening paragraph describing the ledger as Phase 7's gate, the eight-plus numbered rows, the palette-lockstep section whose final sentence points at the assertions being removed, and the "Appending an entry" discipline.
    - .planning/phases/07-parity-validation-cutover/PARITY-REPORT.md — the cutover-readiness block, which the closing ledger entry cites.
    - frontend/package.json — the `test` script chaining the token gate ahead of Vitest, which must keep working.
  </read_first>
  <action>
Close the two things D-11 says close at the flip.

**The token gate.** Remove the three regions named in `read_first`: the cross-site stylesheet URL
constant, the entire final invariant section that compares the four dark neutral hexes across the
two stylesheets, and the trailing clause of the module docstring describing that comparison. Every
assertion against `frontend/src/index.css` stays exactly as it is — the chroma budget, the hue
lock, the lightness hold, the `color-scheme` restoration and the body paint. If `existsSync`
becomes unused after the removal, drop it from the import; if it is still used elsewhere, leave
it. Replace the removed docstring clause with one sentence recording that the cross-site
comparison retired at cutover and naming the closing ledger entry, so a future reader finds the
reason rather than an unexplained absence.

**The ledger.** Append a closing entry to `PARITY-DEVIATIONS.md` — not a numbered deviation row,
but a dated closing section in the same voice as its existing palette section. It states: that
the React site completed the validation gameweek cycle and became the serving default on this
date; the validation gameweek number and a pointer to `PARITY-REPORT.md`; that this file is a
historical record from this point and no longer a gate on any phase; that the cross-site palette
assertions were removed in the same commit and why; and that vanilla is frozen and unmaintained
but still servable via the rollback env var for the agreed window. Amend the file's opening
paragraph, which currently describes the ledger as the thing Phase 7 compares against, so it
reads as closed rather than active.

**The report.** Add a closing line to `PARITY-REPORT.md` recording the flip: the date, the commit
SHA, the approval that Task 1 captured, and the date on which the rollback window ends.

Do not delete any vanilla asset and do not remove the vanilla serving branch — the rollback stays
live for the agreed window and deleting the assets is explicitly a later milestone's cleanup.
  </action>
  <verify>
    <automated>npm --prefix frontend run check:tokens</automated>
    <fails_when>non-zero exit, or the output contains any line reporting a failed assertion, or the final verdict line is absent — the remaining React-palette assertions must still run and still pass</fails_when>
    <automated>npm --prefix frontend run check:tokens | grep -c 'lockstep'</automated>
    <fails_when>any count above 0 — the cross-site comparison still runs</fails_when>
    <automated>npm --prefix frontend test</automated>
    <fails_when>non-zero exit, or the Vitest summary reports any failed test — the token gate is chained ahead of Vitest in this script and a broken script fails here first</fails_when>
    <automated>npm --prefix frontend run build</automated>
    <fails_when>non-zero exit, or a TypeScript diagnostic is printed</fails_when>
    <automated>grep -c 'style.css' frontend/scripts/check-tokens.mjs</automated>
    <fails_when>any count above 0 — the cross-site stylesheet is still being read</fails_when>
    <automated>grep -ci 'cutover' .planning/phases/02-data-layer-non-pitch-pages/PARITY-DEVIATIONS.md</automated>
    <fails_when>a count of 0 — the closing entry was never appended</fails_when>
    <automated>git ls-files web/assets/style.css web/assets/app.js web/index.html | wc -l</automated>
    <fails_when>a count other than 3 — a vanilla asset was deleted, which closes the rollback path this phase must keep open</fails_when>
  </verify>
  <acceptance_criteria>
    - `npm --prefix frontend run check:tokens` passes and its output no longer mentions the cross-site comparison.
    - The chroma-budget, hue-lock, lightness, `color-scheme` and body-paint assertions against `frontend/src/index.css` all still appear in the gate's output.
    - `frontend/scripts/check-tokens.mjs` no longer reads any file outside `frontend/`, and its module docstring records why the cross-site comparison was retired and where the reason is written down.
    - `PARITY-DEVIATIONS.md` carries a dated closing section naming the validation gameweek, pointing at `PARITY-REPORT.md`, stating that the file is now historical, and stating that vanilla is frozen but still servable.
    - `PARITY-DEVIATIONS.md`'s opening paragraph no longer describes the file as an active gate on this phase.
    - The numbered deviation rows are unchanged in content and count — closing the ledger does not rewrite its history.
    - `PARITY-REPORT.md` records the flip date, the flip commit SHA, the captured approval and the rollback-window end date.
    - `git ls-files` still lists all vanilla assets, and `grep -c 'vanilla' api/main.py` returns at least 1 — the rollback branch survives.
  </acceptance_criteria>
  <done>The token gate checks only the React palette and passes, its docstring explains the retirement, `PARITY-DEVIATIONS.md` is closed with a dated cutover entry and reads as historical, the report records the flip, and every vanilla asset and the rollback branch remain in place.</done>
</task>

<task type="auto">
  <name>Task 4: Update the developer-facing docs to describe what a plain uvicorn now serves</name>
  <precondition>api.main serves the React build by default and `FPL_FRONTEND=vanilla` restores vanilla — the Task 2 flip is committed</precondition>
  <files>README.md</files>
  <read_first>
    - README.md — the run instructions around the `uvicorn api.main:app --port 8000` line and the adjacent note about serving the site and the API, which describe the pre-flip behaviour.
    - api/main.py — the post-flip branch comments, so the README's wording matches the code's own description rather than drifting from it.
    - scripts/dual_site.sh — the side-by-side runner, which remains useful after the flip for comparing against the frozen vanilla site during the rollback window.
    - .planning/phases/07-parity-validation-cutover/07-CONTEXT.md — the Claude's-discretion note covering whether the flip commit updates docs; this task exercises that discretion in favour of updating them.
  </read_first>
  <action>
Bring the developer-facing run instructions in line with the flip, so the next person who follows
them is not surprised.

Update the README's run section to state that a plain `uvicorn api.main:app --port 8000` now
serves the React build at the root with the live export mounted at `/data`, and that the vanilla
site is available for the rollback window by setting the frontend env var to its rollback value on
the same command. Keep the existing `--port 8001` API-only note working; adjust its wording only
where the flip changed what the other process serves.

Add a short note pointing at `scripts/dual_site.sh` as the way to run both frontends at once
during the rollback window, and at `PARITY-REPORT.md` as the record of why the flip happened.

Say plainly that the vanilla assets are frozen and unmaintained and that their deletion is
deferred to a later milestone — so a reader does not take their presence as an invitation to edit
them.

Change only documentation in this task. No code, no configuration, no workflow file.
  </action>
  <verify>
    <automated>grep -c 'FPL_FRONTEND' README.md</automated>
    <fails_when>a count of 0 — the rollback env var is not documented, so a reader has no way to find the escape hatch</fails_when>
    <automated>grep -c 'dual_site' README.md</automated>
    <fails_when>a count of 0 — the side-by-side runner is undocumented</fails_when>
    <automated>git diff --name-only HEAD~1 -- . ':!README.md' | wc -l</automated>
    <fails_when>a count above 0 — this task's commit touched something other than the README</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m pytest -q</automated>
    <fails_when>non-zero exit, or the summary line reports any failed or errored test</fails_when>
  </verify>
  <acceptance_criteria>
    - The README states that a plain `uvicorn api.main:app` serves the React build and the live export at `/data`.
    - The README names the frontend env var and its rollback value as the way to serve vanilla, and states that the rollback window is one further gameweek.
    - The README points at `scripts/dual_site.sh` for running both frontends and at `PARITY-REPORT.md` for the cutover record.
    - The README states that the vanilla assets are frozen, unmaintained, and deleted in a later milestone.
    - The task's commit touches `README.md` and nothing else.
  </acceptance_criteria>
  <done>The README describes the post-flip default, the rollback env var and window, the side-by-side runner and the cutover record, and the commit touches documentation only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human approval → irreversible-feeling change | the flip changes what the user's own daily site serves |
| process env → serving branch | after the flip the *absence* of an env var selects React, inverting which state is the silent default |
| build-time gate → shipped palette | removing the cross-site palette assertions removes a check that has been running on every build |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-06-01 | Repudiation | the cutover decision | high | mitigate | Task 1 is a `checkpoint:decision` carrying `gate="blocking-human"`, so it stops for a human in every mode including autonomous runs; the answer is recorded verbatim in the SUMMARY and in `PARITY-REPORT.md` |
| T-07-06-02 | Denial of Service | the flipped serving default | high | mitigate | The rollback is an env var, not a revert: the vanilla branch and every vanilla asset stay in the repository, an acceptance criterion asserts `git ls-files` still lists them, and CI asserts the rollback serves vanilla inside the built image on every build |
| T-07-06-03 | Tampering | removing the cross-site palette assertions | medium | accept | The assertions existed to keep two live sites in lockstep; after the flip vanilla is frozen and non-authoritative, so they protect nothing. The React-side chroma-budget, hue-lock, lightness, `color-scheme` and body-paint assertions all remain and are asserted still present. Rationale is recorded in the closing ledger entry and in the gate's own docstring |
| T-07-06-04 | Spoofing | fixture mode reachable in the new default | high | mitigate | `_FIXTURE_ROOT` stays the first branch; the fixture-precedence test is left asserting exactly what it asserts today and `npm --prefix e2e run test` must stay green, so the flip cannot make frozen data servable in a production configuration |
| T-07-06-05 | Denial of Service | the weekly recommendation pipeline | high | mitigate | `scripts/daily.sh`, `scripts/weekly.sh`, `web/data` and the Dockerfile are all untouched, asserted by `git diff --quiet` in Task 2 |
</threat_model>

<verification>
- With no env vars set, `api.main` mounts `data` then `site` and `site` resolves to the built React bundle
- With the rollback env value set, `api.main` mounts a single `site` on the vanilla directory
- `python -m pytest -q`, `ruff check .`, `npm --prefix frontend test`, `npm --prefix frontend run build` and `npm --prefix e2e run test` are all green
- The CI `image:` job carries exactly one rollback smoke step, before the scanner
- `git ls-files` still lists every vanilla asset; the Dockerfile, cron scripts and `web/data` are untouched
</verification>

<success_criteria>
- The cutover was approved by the user in person against the completed report, never automatically
- A plain `uvicorn api.main:app` serves the React build with the live export at `/data`
- `FPL_FRONTEND=vanilla` restores the vanilla site, and CI proves that path inside the built image
- The fixture branch still wins over both and the E2E suite is unchanged
- `PARITY-DEVIATIONS.md` is closed with a dated cutover entry and the cross-site palette assertions are retired
- Every vanilla asset and the vanilla serving branch remain in the repository for the rollback window
- The README describes the new default, the rollback and the record of why the flip happened
</success_criteria>

<output>
Create `.planning/phases/07-parity-validation-cutover/07-06-SUMMARY.md` when done
</output>
