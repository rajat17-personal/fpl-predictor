---
phase: 07-parity-validation-cutover
plan: 04
type: execute
wave: 4
depends_on: ["07-03"]
files_modified:
  - .planning/phases/07-parity-validation-cutover/PARITY-REPORT.md
  - .planning/phases/02-data-layer-non-pitch-pages/PARITY-DEVIATIONS.md
  - frontend/src
autonomous: false
requirements: [CUT-01]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "All eight pages have a recorded mid-gameweek verdict, taken while matches are live or settling — a different data state from the pre-deadline pass (D-07)"
    - "The pass runs against the same validation gameweek the pre-deadline pass fixed; a pass against a different gameweek is not part of this cycle (D-13)"
    - "Any page carried forward from the pre-deadline stage is re-compared here and its carry-forward closed (D-14)"
    - "The mid-gameweek stage entry records the daily cron state since the previous pass, or records verbatim that the cron log is absent (D-16)"
    - statement: "The user has continued using the React site as the daily driver through the live gameweek, with vanilla standing by, and any dogfooding finding is recorded as a defect row (D-02)"
      verification: backstop
  prohibitions:
    - requirement_id: CUT-01
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT change, pause, or reschedule the live weekly recommendation cycle — scripts/daily.sh, scripts/weekly.sh, their cron lines, and the web/data export contract are read-only for the whole of this phase"
    - requirement_id: CUT-01
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT run a side-by-side parity comparison against fixture-mode or otherwise frozen data — both origins must be reading the same live web/data export at the same instant, or the comparison is not evidence"
    - requirement_id: CUT-01
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT record a parity verdict in PARITY-REPORT.md that is not traceable to a run artifact — a PARITY-DEVIATIONS.md entry number, a fixing commit SHA, or a named cron-green citation. No verdict from memory, and no delta reclassified as explained without one of those three"
    - requirement_id: CUT-01
      category: privacy
      status: resolved
      verification: judgment
      statement: "MUST NOT commit the user's personal FPL identity into PARITY-REPORT.md, PARITY-CHECKLIST.md, or any parity artifact — the numeric entry id 6980093 and the team name only, never the manager's first or last name returned by the live API"
  artifacts:
    - path: ".planning/phases/07-parity-validation-cutover/PARITY-REPORT.md"
      provides: "A completed mid-gameweek stage table with eight page verdicts and the cron citation"
      contains: "mid-gameweek"
  key_links:
    - from: "e2e/parity/parity-diff.mjs"
      to: ".planning/phases/07-parity-validation-cutover/PARITY-REPORT.md"
      via: "--stage mid-gameweek --out writes the second stage fragment"
      pattern: "mid-gameweek"
---

<objective>
Run the second of the three cycle-stage passes: the in-flight state, with the deadline passed and
matches live or settling.

Purpose: this stage exercises data states the pre-deadline pass cannot reach — the banner's
deadline-passed rendering, in-flight standings and leaders, and a squad page whose players have
started. Ledger entry 4 already records the banner's deadline-passed copy as an intended
difference, so this is the pass that actually exercises it.

Output: a completed mid-gameweek stage table in `PARITY-REPORT.md` with zero unclosed defects.

**Calendar-gated.** Task 1 carries a `<precondition>` that only becomes true once the validation
gameweek's deadline has passed and its scoreboard has not yet run. If it is not yet true, the
executor halts with a blocking-human checkpoint; resume when the gameweek is in flight.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-parity-validation-cutover/07-CONTEXT.md
@.planning/phases/07-parity-validation-cutover/07-03-SUMMARY.md
@.planning/phases/07-parity-validation-cutover/PARITY-CHECKLIST.md
@.planning/phases/02-data-layer-non-pitch-pages/PARITY-DEVIATIONS.md
</context>

## Artifacts this phase produces

See the table in `07-01-PLAN.md` under "Artifacts this phase produces" — the single authoritative
list of symbols created by Phase 7. This plan creates no new symbols; it fills the second stage
of `PARITY-REPORT.md`.

## Flagged Assumptions (spec-less probe fallback)

`CUT-01` remains the single `unclassified` / `unresolved` row from the deterministic edge probe;
the full statement is in `07-01-PLAN.md` under "Flagged Assumptions". Repeated here as a pointer,
not re-resolved.

Plan-specific flagged assumption: **"mid-gameweek" has no machine-readable marker in the export.**
`web/data/meta.json` carries no in-flight flag, so the stage is inferred from two facts that are
checkable — the deadline has passed, and the scoreboard has not yet scored this gameweek. A
gameweek whose fixtures are spread unusually (a Monday-night finish, a postponed match) can leave
that window open longer than the calendar suggests. The pass is still valid; the assumption is
only that "deadline passed and not yet scored" is a faithful proxy for "matches live or settling".

## Decisions implemented here

| ID | Decision | Task |
|---|---|---|
| D-02 | React is the daily driver during the validation week | T1 (handover confirmation) |
| D-05 | Hybrid comparison — script pass plus eyeball pass | T1 |
| D-06 | `PARITY-REPORT.md` is the evidence artifact | T1, T2 |
| D-07 | Pass 2 of 3: while matches are live or settling | T1 |
| D-14 | Defects fix forward, never restart the cycle | T2 |
| D-16 | Criterion-3 evidence is cron-green citations | T1 |

<tasks>

<task type="auto">
  <name>Task 1: Mid-gameweek scripted pass across all eight pages</name>
  <precondition>web/data/meta.json's gw equals the validation gameweek recorded in PARITY-REPORT.md's pre-deadline run header, its deadline_utc is now in the past, and web/data/scoreboard.json either does not exist or holds no entry for that gameweek — the gameweek is in flight, not finished</precondition>
  <files>.planning/phases/07-parity-validation-cutover/PARITY-REPORT.md</files>
  <read_first>
    - .planning/phases/07-parity-validation-cutover/PARITY-REPORT.md — the pre-deadline run header (for the validation gameweek number this pass must match), the empty mid-gameweek stage table, and any page carried forward from stage one.
    - web/data/meta.json — the live export's gw, deadline_utc and generated_utc for this run's header.
    - .planning/phases/07-parity-validation-cutover/PARITY-CHECKLIST.md — the cron-green step and the per-page eyeball list.
    - e2e/parity/parity-diff.mjs — the `--all --stage --out` interface and the verdict-line format.
    - .planning/phases/02-data-layer-non-pitch-pages/PARITY-DEVIATIONS.md — entries 3 and 4 in particular, the banner freshness line and the deadline-passed copy, which this stage is the first to exercise.
    - predict/scoreboard.py — how and when `web/data/scoreboard.json` gains an entry, which is the discriminator between this stage and the next.
  </read_first>
  <action>
Record the in-flight stage of the validation gameweek.

Confirm first that the export still names the same gameweek the pre-deadline run header fixed. If
the gameweek number has moved on, this is not the same cycle — stop and report rather than
recording a pass against a different gameweek; D-13 forbids joining a cycle mid-week.

Write the mid-gameweek run header into `PARITY-REPORT.md`: gameweek number, deadline timestamp,
export `generated_utc`, this run's UTC timestamp, and the verbatim command line used. Note the
observed data state — deadline passed, scoreboard not yet run for this gameweek.

Boot both sites with `scripts/dual_site.sh start`, run the eight-page diff with the mid-gameweek
stage name and the report as output target, then stop both servers. The servers are on-demand
(D-03) and must not be left competing with the cron host outside a session.

Pay explicit attention to the two pages this stage exercises for the first time: the gameweek
banner's deadline-passed rendering on every page that carries it, and the league and squad pages
now that players have started. A delta on the banner's deadline-passed copy is expected and is
explained by ledger entry 4; a delta anywhere else on those pages is a defect.

Assemble the cron citation (D-16) covering the runs since the pre-deadline pass: the cron log
file if present on this host, the alerts JSON Lines file's records since the previous pass, and
the git history of `web/data/`. If the cron log file does not exist, write that fact verbatim
rather than describing an unobserved run as green.

If any page was carried forward from the pre-deadline stage under D-14's single exception, close
its carry-forward here and update its pre-deadline row to point at this stage's re-comparison.

Do not resolve defects in this task; record them as the tool classified them and let Task 2 close
them.
  </action>
  <verify>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import json,os,datetime as dt; m=json.load(open('web/data/meta.json')); d=dt.datetime.fromisoformat(m['deadline_utc'].replace('Z','+00:00')); now=dt.datetime.now(dt.timezone.utc); sb=os.path.exists('web/data/scoreboard.json'); print('gw',m['gw'],'deadline_passed',d<now,'scoreboard_file',sb)"</automated>
    <fails_when>non-zero exit, or the output prints `deadline_passed False` — the gameweek has not started and this is still the pre-deadline state, not the mid-gameweek one</fails_when>
    <automated>bash scripts/dual_site.sh start</automated>
    <fails_when>non-zero exit, or the printed status block is missing either the `vanilla http://127.0.0.1:8000 up` line or the `react http://127.0.0.1:8001 up` line</fails_when>
    <automated>node e2e/parity/parity-diff.mjs --all --stage mid-gameweek --out .planning/phases/07-parity-validation-cutover/PARITY-REPORT.md; echo "exit=$?"</automated>
    <fails_when>the output contains no `TOTAL:` line, or that line reports fewer than 8 pages or `0 fields compared` — a vacuous run. A non-zero exit here means defects were found, which is Task 2's input, not a failure of this command</fails_when>
    <automated>bash scripts/dual_site.sh stop</automated>
    <fails_when>non-zero exit, or the output contains `still listening`</fails_when>
    <human-check>
      Working through PARITY-CHECKLIST.md's eyeball pass on both running sites while the gameweek is live, confirm across all eight pages that layout, spacing, theme and copy read the same, and that the deadline-passed banner state renders sensibly on both. Values and sort order are the script's job; do not re-check them by eye. Report "matched" or describe what differed.
    </human-check>
  </verify>
  <acceptance_criteria>
    - The mid-gameweek run header names the same gameweek number as the pre-deadline run header.
    - The mid-gameweek stage table has exactly eight page rows, one per route in `e2e/parity/extract.mjs`'s `PAGES`.
    - No verdict cell is blank; each is a clean verdict, an explained-delta verdict citing a ledger number, or a defect verdict carrying the explicit unfilled marker.
    - The run header records the observed data state — deadline passed, scoreboard not yet run for this gameweek.
    - The cron citation names a concrete evidence source that exists, or states verbatim that the cron log file is absent on this host.
    - Any page carried forward from the pre-deadline stage now has a closing re-comparison recorded, and its pre-deadline row points at it.
    - `git diff --quiet -- scripts/daily.sh scripts/weekly.sh web/data && echo PIPELINE UNTOUCHED` prints that line.
    - `bash scripts/dual_site.sh status` reports both ports down at the end of the scripted half.
  </acceptance_criteria>
  <done>The mid-gameweek stage table is filled with eight page verdicts against the same validation gameweek, the in-flight data state and the cron citation are recorded, any pre-deadline carry-forward is closed, and both servers are stopped.</done>
</task>

<task type="auto">
  <name>Task 2: Close every mid-gameweek defect by fixing forward and re-comparing</name>
  <precondition>PARITY-REPORT.md's mid-gameweek stage table is populated with eight page rows from Task 1</precondition>
  <files>.planning/phases/07-parity-validation-cutover/PARITY-REPORT.md, .planning/phases/02-data-layer-non-pitch-pages/PARITY-DEVIATIONS.md, frontend/src</files>
  <read_first>
    - .planning/phases/07-parity-validation-cutover/PARITY-REPORT.md — the mid-gameweek table's defect rows, which are this task's input, and the "Defects found and how they were closed" table.
    - .planning/phases/02-data-layer-non-pitch-pages/PARITY-DEVIATIONS.md — the existing rows and the "Appending an entry" discipline governing any new one.
    - .planning/phases/07-parity-validation-cutover/07-03-SUMMARY.md — how the pre-deadline defects were dispositioned, so the same reasoning is applied consistently rather than re-litigated.
    - The specific `frontend/src/routes/*.tsx` or `frontend/src/components/*.tsx` file behind each reported defect, and its vanilla counterpart in `web/` — read both before changing either.
    - e2e/parity/extract.mjs — the field whose comparison produced the defect.
  </read_first>
  <action>
Apply the same closure discipline the pre-deadline stage used. D-14 governs: fix forward,
re-compare the affected pages, continue — never restart the cycle.

For each defect, take exactly one of two dispositions. Fix the React code so the rendered value,
ordering or copy matches vanilla and commit — vanilla stays authoritative until the flip and is
never edited to match React. Or append a numbered row to `PARITY-DEVIATIONS.md` recording the
React behaviour as a deliberate improvement, following that file's own append discipline, and add
the new ledger number to the producing field's `knownDeviations` list in `e2e/parity/extract.mjs`
in the same commit.

There is no third option. A defect is never closed by narrowing the field map, deleting a
comparison, or relaxing a selector.

After each disposition, re-run the diff restricted to the affected pages against a fresh dual
boot, then update that page's mid-gameweek row with the disposition, the fixing commit SHA or the
new ledger number, and the re-comparison timestamp. Mirror the same information into the
"Defects found and how they were closed" table.

A fix that lands too late for the in-flight data state to be observed again is recorded as
carried into the post-finish stage, per D-14's single exception, naming the stage that will close
it. Do not fabricate a re-comparison against a data state that has already gone.

If the mid-gameweek pass reported zero defects, record that stated zero explicitly and note in the
SUMMARY that no code changed.
  </action>
  <verify>
    <automated>bash scripts/dual_site.sh start</automated>
    <fails_when>non-zero exit, or the printed status block is missing either the `vanilla http://127.0.0.1:8000 up` line or the `react http://127.0.0.1:8001 up` line</fails_when>
    <automated>node e2e/parity/parity-diff.mjs --all</automated>
    <fails_when>non-zero exit, or the `TOTAL:` line reports a non-zero defect count, or it reports fewer than 8 pages or `0 fields compared`</fails_when>
    <automated>bash scripts/dual_site.sh stop</automated>
    <fails_when>non-zero exit, or the output contains `still listening`</fails_when>
    <automated>npm --prefix frontend run build</automated>
    <fails_when>non-zero exit, or a TypeScript diagnostic is printed — `tsc -b` build mode is the real typecheck in this repo</fails_when>
    <automated>npm --prefix frontend test</automated>
    <fails_when>non-zero exit, or the Vitest summary reports any failed test, or `check-tokens.mjs` reports a failed assertion</fails_when>
    <automated>npm --prefix e2e run test</automated>
    <fails_when>non-zero exit, or the Playwright summary line reports any failed spec</fails_when>
  </verify>
  <acceptance_criteria>
    - Every defect row in the mid-gameweek stage table carries a fixing commit SHA or a ledger entry number; no unfilled markers remain except on pages explicitly recorded as carried into the post-finish stage.
    - Any ledger row appended to `PARITY-DEVIATIONS.md` is numbered sequentially after the highest existing number, carries all four columns, and its number appears in a `knownDeviations` list in `e2e/parity/extract.mjs`.
    - `git log --oneline -- web/assets web/*.html` shows no new commit from this task.
    - `git diff --quiet -- scripts/daily.sh scripts/weekly.sh web/data && echo PIPELINE UNTOUCHED` prints that line.
    - `git diff --quiet -- e2e/fixtures && echo FROZEN V1 FIXTURES UNTOUCHED` prints that line.
    - For each fix, the code change, the report update and any ledger row are in the same commit.
    - `node -e "import('./e2e/parity/extract.mjs').then(m=>{const n=m.PAGES.reduce((a,p)=>a+p.fields.length,0); console.log('fields:',n)})"` reports a count greater than or equal to the count recorded in `07-03-SUMMARY.md`.
  </acceptance_criteria>
  <done>The full eight-page diff exits clean with zero defects, every mid-gameweek closure is traceable to a commit SHA or a ledger number, the frontend build, unit tests and the frozen-fixture E2E suite are green, and vanilla is unedited.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live pipeline host → validation servers | the two uvicorns share the host with the daily cron during a live gameweek |
| live page content → committed evidence file | in-flight page text is written into a repository document |
| React code change → live vanilla site | a mid-gameweek fix must not reach the site the user is depending on while matches run |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-04-01 | Denial of Service | the two uvicorns on the cron host during a live gameweek | medium | mitigate | Both tasks stop the servers immediately after their runs and assert both ports are down; no session is left running unattended |
| T-07-04-02 | Spoofing | comparing against a different gameweek | high | mitigate | Task 1 asserts the export's gw equals the gameweek fixed in the pre-deadline run header and stops rather than recording a pass for a different cycle |
| T-07-04-03 | Information Disclosure | `PARITY-REPORT.md` content | high | mitigate | Verdict cells hold only field values from the extractor, whose field map has no manager-name field; the entry is referred to by numeric id only |
| T-07-04-04 | Tampering | fix-forward commits reaching the authoritative site | high | mitigate | Fixes change React only; an acceptance criterion asserts `git log` shows no new commit touching `web/assets` or `web/*.html` |
| T-07-04-05 | Denial of Service | the weekly recommendation pipeline | high | mitigate | `scripts/daily.sh`, `scripts/weekly.sh` and `web/data` are read-only for the whole phase, asserted by a `git diff --quiet` criterion in both tasks |
</threat_model>

<verification>
- `node e2e/parity/parity-diff.mjs --all` exits clean with zero defects against a live dual boot
- `PARITY-REPORT.md`'s mid-gameweek stage has eight page rows against the same validation gameweek, none blank
- `npm --prefix frontend run build`, `npm --prefix frontend test` and `npm --prefix e2e run test` are all green
- `git diff --quiet -- scripts/daily.sh scripts/weekly.sh web/data e2e/fixtures` — pipeline and frozen fixtures untouched
</verification>

<success_criteria>
- All eight pages carry a mid-gameweek verdict taken while the gameweek was in flight, against the same validation gameweek as stage one
- The deadline-passed banner state has actually been exercised and its ledger-explained delta recorded as explained
- Every defect is closed by fixing React forward or by adding a ledger row, then re-compared
- Any pre-deadline carry-forward is closed here
- The cron citation names evidence that exists, or states verbatim that the cron log is absent
</success_criteria>

<output>
Create `.planning/phases/07-parity-validation-cutover/07-04-SUMMARY.md` when done
</output>
