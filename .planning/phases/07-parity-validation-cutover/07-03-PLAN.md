---
phase: 07-parity-validation-cutover
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - .planning/phases/07-parity-validation-cutover/PARITY-REPORT.md
  - .planning/phases/02-data-layer-non-pitch-pages/PARITY-DEVIATIONS.md
  - frontend/src
autonomous: false
requirements: [CUT-01]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The validation gameweek is identified from the live export and recorded in PARITY-REPORT.md, and the cycle starts at its pre-deadline export rather than joining mid-week (D-13)"
    - "All eight pages have a recorded pre-deadline verdict citing either a ledger number, a fixing commit, or an explicit unfilled marker (D-06, D-07)"
    - "The pre-deadline stage entry records the state of the daily and weekly cron runs since the tooling was built, or records verbatim that the cron log is absent (D-16)"
    - "Every defect found is fixed forward and its affected pages re-compared in the same stage — the cycle is never restarted (D-14)"
    - statement: "The user has run their real FPL week on the React site with vanilla standing by as the reference, and the same-session solver comparison for entry 6980093 is either recorded as matching or recorded as a defect (D-02, D-08)"
      verification: backstop
  prohibitions:
    - requirement_id: CUT-01
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT change, pause, or reschedule the live weekly recommendation cycle — scripts/daily.sh, scripts/weekly.sh, their cron lines, and the web/data export contract are read-only for the whole of this phase"
    - requirement_id: CUT-01
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT run a side-by-side parity comparison against fixture-mode or otherwise frozen data — both origins must be reading the same live web/data export at the same instant, or the comparison is not evidence"
    - requirement_id: CUT-01
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT record a parity verdict in PARITY-REPORT.md that is not traceable to a run artifact — a PARITY-DEVIATIONS.md entry number, a fixing commit SHA, or a named cron-green citation. No verdict from memory, and no delta reclassified as explained without one of those three"
    - requirement_id: CUT-01
      category: privacy
      status: resolved
      verification: judgment
      statement: "MUST NOT commit the user's personal FPL identity into PARITY-REPORT.md, PARITY-CHECKLIST.md, or any parity artifact — the numeric entry id 6980093 and the team name only, never the manager's first or last name returned by the live API"
  artifacts:
    - path: ".planning/phases/07-parity-validation-cutover/PARITY-REPORT.md"
      provides: "A completed pre-deadline stage table with eight page verdicts and the cron-green citation"
      contains: "pre-deadline"
  key_links:
    - from: ".planning/phases/07-parity-validation-cutover/PARITY-REPORT.md"
      to: ".planning/phases/02-data-layer-non-pitch-pages/PARITY-DEVIATIONS.md"
      via: "each explained delta cell cites a ledger entry number"
      pattern: "ledger"
    - from: "e2e/parity/parity-diff.mjs"
      to: ".planning/phases/07-parity-validation-cutover/PARITY-REPORT.md"
      via: "--stage pre-deadline --out writes the stage fragment"
      pattern: "--stage"
---

<objective>
Run the first of the three cycle-stage passes: the pre-deadline state, with the gameweek banner
counting down and the fresh Friday export in place.

Purpose: this pass is where the validation gameweek is chosen and the cycle formally starts
(D-13). It is also where the largest number of deltas will surface, because it is the first time
all eight pages are compared against live data rather than frozen fixtures. Everything found here
is fixed forward and re-compared within this same stage (D-14).

Output: a completed pre-deadline stage table in `PARITY-REPORT.md`, any fixing commits with
matching ledger rows, and the handover for the user's own real-usage week on the React site.

**Calendar-gated.** Task 1 carries a `<precondition>` that only becomes true once the validation
gameweek's pre-deadline weekly export has landed. If it is not yet true, the executor halts with
a blocking-human checkpoint; that is the correct outcome, not a failure — resume this plan when
the export lands.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-parity-validation-cutover/07-CONTEXT.md
@.planning/phases/07-parity-validation-cutover/07-02-SUMMARY.md
@.planning/phases/07-parity-validation-cutover/PARITY-CHECKLIST.md
@.planning/phases/02-data-layer-non-pitch-pages/PARITY-DEVIATIONS.md
</context>

## Artifacts this phase produces

See the table in `07-01-PLAN.md` under "Artifacts this phase produces" — the single authoritative
list of symbols created by Phase 7. This plan creates no new symbols; it fills
`PARITY-REPORT.md`, which 07-02 created.

## Flagged Assumptions (spec-less probe fallback)

`CUT-01` remains the single `unclassified` / `unresolved` row from the deterministic edge probe;
the full statement is in `07-01-PLAN.md` under "Flagged Assumptions". Repeated here as a
pointer, not re-resolved.

Plan-specific flagged assumption: **the validation gameweek is not known at planning time.** At
planning the live export reads gameweek 4 with a deadline of 2026-09-12T12:30Z; D-13 fixes the
cycle as the first full gameweek whose pre-deadline Friday export lands *after* the build work is
done, which may be that gameweek or the next one. The plan therefore reads the gameweek from the
live export at run time and records it, rather than hardcoding a number. A build slip moves the
cycle; it does not fail the phase.

## Decisions implemented here

| ID | Decision | Task |
|---|---|---|
| D-02 | React is the daily driver during the validation week | T3 |
| D-05 | Hybrid comparison — script pass (T1) and eyeball pass (T3) | T1, T3 |
| D-06 | `PARITY-REPORT.md` is the evidence artifact | T1, T2, T3 |
| D-07 | Pass 1 of 3: after the pre-deadline export | T1 |
| D-08 | Same-session interactive comparison, entry 6980093 | T3 |
| D-13 | The cycle starts at its export; no joining mid-week | T1 precondition |
| D-14 | Defects fix forward, never restart the cycle | T2 |
| D-16 | Criterion-3 evidence is cron-green citations | T1 |

<tasks>

<task type="auto">
  <name>Task 1: Pre-deadline scripted pass across all eight pages</name>
  <precondition>web/data/meta.json exists, its deadline_utc is still in the future, and its generated_utc is later than the commit date of the most recent commit touching e2e/parity/ — i.e. the validation gameweek's pre-deadline weekly export has landed since the comparison tooling was built (D-13)</precondition>
  <files>.planning/phases/07-parity-validation-cutover/PARITY-REPORT.md</files>
  <read_first>
    - web/data/meta.json — the live export's gw, deadline_utc and generated_utc; these identify the validation gameweek and go into the run header.
    - .planning/phases/07-parity-validation-cutover/PARITY-REPORT.md — the empty pre-deadline stage table this task fills, and the run-header block's field list.
    - .planning/phases/07-parity-validation-cutover/PARITY-CHECKLIST.md — the cron-green step, which names exactly which evidence to cite.
    - e2e/parity/parity-diff.mjs — the `--all --stage --out` interface and the exact verdict-line format.
    - scripts/dual_site.sh — the start/stop/status contract and the ports it binds.
    - scripts/daily.sh and scripts/weekly.sh — the `run_step` accounting and the `ops.notify` alert path whose output is the cron-green citation.
  </read_first>
  <action>
Start the validation cycle and record its first stage.

Read `web/data/meta.json` and record the validation gameweek number, its `deadline_utc` and the
export's `generated_utc` into `PARITY-REPORT.md`'s run-header block for the pre-deadline stage,
together with the UTC timestamp of this run and the exact `parity-diff` command line used. State
in the header that this is the cycle's start (D-13) and that no later pass may be run against a
different gameweek.

Boot both sites with `scripts/dual_site.sh start`, then run the eight-page diff with the
pre-deadline stage name and the report as the output target, so the emitted fragment lands in the
pre-deadline stage table. Stop both servers afterwards — the servers are on-demand (D-03) and
must not be left running against the cron host.

Assemble the cron-green citation (D-16) from what actually exists on this host, in this order of
preference: the cron log file if present, naming the last daily and weekly run lines it contains;
the alerts JSON Lines file, naming the absence (or presence) of records dated since the tooling
commit; and the git history of `web/data/` showing the export commits that landed. If the cron
log file does not exist on this host, write that fact into the citation cell verbatim. An absent
log is never written up as a green run — this is Phase 6's alerting spine being cited, not a new
claim being manufactured.

Do not resolve any defect in this task. Record every reported delta exactly as the tool
classified it: explained deltas cite their ledger number, defects keep the explicit unfilled
marker. Task 2 closes them.

Touch no pipeline file. `scripts/daily.sh`, `scripts/weekly.sh`, their cron lines and the
`web/data` export contract are read-only for this whole phase.
  </action>
  <verify>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import json,datetime; m=json.load(open('web/data/meta.json')); print('gw', m['gw'], 'deadline', m['deadline_utc'], 'generated', m['generated_utc'])"</automated>
    <fails_when>non-zero exit, or the printed deadline is earlier than the printed generated timestamp (that combination means the gameweek has already started and this is not the pre-deadline state)</fails_when>
    <automated>bash scripts/dual_site.sh start</automated>
    <fails_when>non-zero exit, or the printed status block is missing either the `vanilla http://127.0.0.1:8000 up` line or the `react http://127.0.0.1:8001 up` line</fails_when>
    <automated>node e2e/parity/parity-diff.mjs --all --stage pre-deadline --out .planning/phases/07-parity-validation-cutover/PARITY-REPORT.md; echo "exit=$?"</automated>
    <fails_when>the output contains no `TOTAL:` line, or that line reports fewer than 8 pages or `0 fields compared` — a vacuous run. A non-zero exit here means defects were found, which is expected input for Task 2, not a failure of this command</fails_when>
    <automated>bash scripts/dual_site.sh stop</automated>
    <fails_when>non-zero exit, or the output contains `still listening`</fails_when>
    <automated>grep -c '^| ' .planning/phases/07-parity-validation-cutover/PARITY-REPORT.md</automated>
    <fails_when>the count did not increase by at least 8 relative to the same command run before this task — the eight page rows were not appended</fails_when>
  </verify>
  <acceptance_criteria>
    - `PARITY-REPORT.md`'s pre-deadline stage section carries a run header naming the validation gameweek number, the deadline timestamp, the export's generated timestamp, the run's own UTC timestamp, and the verbatim command line used.
    - The pre-deadline stage table has exactly eight page rows, one per route in `e2e/parity/extract.mjs`'s `PAGES`.
    - Every row's Verdict cell is one of a clean verdict, an explained-delta verdict citing a ledger number, or a defect verdict carrying the explicit unfilled marker — no blank verdict cells.
    - The Cron-green citation cell names a concrete evidence source that exists, or states verbatim that the cron log file is absent on this host.
    - `git diff --quiet -- scripts/daily.sh scripts/weekly.sh web/data && echo PIPELINE UNTOUCHED` prints that line.
    - `git diff --quiet -- e2e/fixtures && echo FROZEN V1 FIXTURES UNTOUCHED` prints that line.
    - No uvicorn process remains: `bash scripts/dual_site.sh status` reports both ports down.
    - Neither the report nor the commit message contains the manager's first or last name.
  </acceptance_criteria>
  <done>The pre-deadline stage table is filled with eight page verdicts and a real cron-green citation, the validation gameweek is fixed and recorded, and both servers are stopped.</done>
</task>

<task type="auto">
  <name>Task 2: Close every pre-deadline defect by fixing forward and re-comparing</name>
  <precondition>PARITY-REPORT.md's pre-deadline stage table is populated with eight page rows from Task 1</precondition>
  <files>.planning/phases/07-parity-validation-cutover/PARITY-REPORT.md, .planning/phases/02-data-layer-non-pitch-pages/PARITY-DEVIATIONS.md, frontend/src</files>
  <read_first>
    - .planning/phases/07-parity-validation-cutover/PARITY-REPORT.md — Task 1's pre-deadline table; every defect row is this task's input.
    - .planning/phases/02-data-layer-non-pitch-pages/PARITY-DEVIATIONS.md — the eight existing rows and the "Appending an entry" discipline that governs adding a ninth.
    - The specific `frontend/src/routes/*.tsx` or `frontend/src/components/*.tsx` file behind each reported defect, plus its vanilla counterpart in `web/` — read both before changing either.
    - e2e/parity/extract.mjs — the field whose comparison produced the defect, so the fix targets the real rendered value rather than a guess.
  </read_first>
  <action>
Turn every defect row into a closed row. D-14 governs: fix forward, re-compare the affected
pages, continue — never restart the cycle.

For each defect the pre-deadline pass reported, decide between exactly two dispositions and take
one:

**Fix forward.** The React side is wrong relative to vanilla. Change the React code so the
rendered value, ordering or copy matches vanilla, and commit. The vanilla site stays authoritative
until the flip (CUT-01), so vanilla is not edited to match React.

**New ledger row.** The React behaviour is a deliberate improvement not yet recorded. Append a
numbered row to `PARITY-DEVIATIONS.md` following its own append discipline — deviation, reason,
introducing plan id — and, so the tooling agrees with the document, add that ledger number to the
producing field's `knownDeviations` list in `e2e/parity/extract.mjs` in the same commit. A
behavioural difference shipped without a matching row is a parity defect, not a deviation.

There is no third option. A defect is never closed by narrowing the field map, by deleting a
comparison, or by relaxing a selector — that hides the difference rather than explaining it.

After each disposition, re-run the diff restricted to the affected page or pages against a fresh
dual boot, and update that page's row in the pre-deadline stage table: the disposition, the
fixing commit SHA or the new ledger number, and the re-comparison timestamp. Record the same
information in the report's "Defects found and how they were closed" table.

If a page's fix lands too late for its pre-deadline data state to be observed again — the
deadline has passed by the time the fix is ready — do not fabricate a re-comparison. Record that
page as carried into the next stage, per D-14's single exception, and note which stage will close
it.

If the pre-deadline pass reported zero defects, this task's work is to record that outcome
explicitly in the defect table (a stated zero, not an absent section) and to state in the SUMMARY
that no code changed.
  </action>
  <verify>
    <automated>bash scripts/dual_site.sh start</automated>
    <fails_when>non-zero exit, or the printed status block is missing either the `vanilla http://127.0.0.1:8000 up` line or the `react http://127.0.0.1:8001 up` line</fails_when>
    <automated>node e2e/parity/parity-diff.mjs --all</automated>
    <fails_when>non-zero exit, or the `TOTAL:` line reports a non-zero defect count, or it reports fewer than 8 pages or `0 fields compared`</fails_when>
    <automated>bash scripts/dual_site.sh stop</automated>
    <fails_when>non-zero exit, or the output contains `still listening`</fails_when>
    <automated>npm --prefix frontend run build</automated>
    <fails_when>non-zero exit, or a TypeScript diagnostic is printed — `tsc -b` build mode is the real typecheck in this repo and catches narrowing losses that `tsc --noEmit` misses</fails_when>
    <automated>npm --prefix frontend test</automated>
    <fails_when>non-zero exit, or the Vitest summary reports any failed test, or `check-tokens.mjs` reports a failed assertion</fails_when>
    <automated>npm --prefix e2e run test</automated>
    <fails_when>non-zero exit, or the Playwright summary line reports any failed spec — a parity fix that breaks the frozen-fixture E2E suite has changed behaviour the suite already pinned</fails_when>
  </verify>
  <acceptance_criteria>
    - Every defect row in the pre-deadline stage table now carries either a fixing commit SHA or a ledger entry number; no unfilled markers remain except on pages explicitly recorded as carried into the next stage.
    - The "Defects found and how they were closed" table has one row per defect, including a stated zero if none were found.
    - Any ledger row appended to `PARITY-DEVIATIONS.md` is numbered sequentially after 8, carries all four columns, and its number appears in a `knownDeviations` list in `e2e/parity/extract.mjs`.
    - `git log --oneline -- web/assets web/*.html` shows no new commit from this task — vanilla is not edited to match React.
    - `git diff --quiet -- scripts/daily.sh scripts/weekly.sh web/data && echo PIPELINE UNTOUCHED` prints that line.
    - `git diff --quiet -- e2e/fixtures && echo FROZEN V1 FIXTURES UNTOUCHED` prints that line.
    - For each fix, the code change, the report update and any ledger row are in the same commit.
    - No field was removed from `extract.mjs` and no selector was broadened: `node -e "import('./e2e/parity/extract.mjs').then(m=>{const n=m.PAGES.reduce((a,p)=>a+p.fields.length,0); console.log('fields:',n)})"` reports a count greater than or equal to the count recorded in `07-02-SUMMARY.md`.
  </acceptance_criteria>
  <done>The full eight-page diff exits clean with zero defects (explained deltas are permitted), every closure is traceable to a commit SHA or a ledger number, the frontend build, unit tests and the frozen-fixture E2E suite are all green, and vanilla is unedited.</done>
</task>

<task type="auto">
  <name>Task 3: Hand over the manual pass and the same-session solver comparison</name>
  <precondition>PARITY-REPORT.md's pre-deadline stage table shows zero unclosed defects from Task 2, and web/data/meta.json's deadline_utc is still in the future</precondition>
  <files>.planning/phases/07-parity-validation-cutover/PARITY-REPORT.md</files>
  <read_first>
    - .planning/phases/07-parity-validation-cutover/PARITY-CHECKLIST.md — the per-page eyeball list and the three D-08 interactive flows; this task drives it, it does not rewrite it.
    - .planning/phases/07-parity-validation-cutover/PARITY-REPORT.md — the pre-deadline stage table and the rows this task marks as awaiting the manual verdict.
    - scripts/dual_site.sh — the start contract, so both sites are already up before the user is asked to look at anything.
    - frontend/src/routes/Team.tsx — the rate, solve and multi-gameweek plan controls the D-08 session drives on the React side.
    - web/team.html — vanilla's equivalent controls, so each flow can be performed identically on both.
  </read_first>
  <action>
Set up the environment completely, then hand over the two halves of the pass that only a human
can perform — the eyeball comparison (D-05's manual half) and the same-session solver comparison
(D-08). Do not script either: D-05 explicitly rejected full scripted flow-diffing and D-08
explicitly specifies a manual same-input session.

Boot both sites with `scripts/dual_site.sh start` and confirm both answer before presenting
anything. Leave them running for the duration of the session — this is the one point in the plan
where the servers stay up, because the user is about to use them.

In `PARITY-REPORT.md`'s pre-deadline stage, add two rows beneath the page table: one for the
manual eyeball pass and one for the D-08 same-session solver comparison. Both start carrying the
explicit unfilled marker, naming what will replace it — the user's verdict, plus a defect row if
anything mismatched.

Present the checklist to the user as the handover, with the two origins' URLs, the entry id
6980093 to load on both, and the three flows to perform identically on each: rate my team; one
solve with an identical set of locks; one two-gameweek plan. State the reasoning that makes a
mismatch meaningful — both processes share the same model artifact and the same pool, so a
same-session cross-site difference is a defect rather than noise.

Also hand over D-02: for the rest of this gameweek the React site on port 8001 is the daily
driver for the user's real FPL week, with vanilla on 8000 standing by as the reference whenever
something looks off. Note in the report that dogfooding findings are recorded the same way as
scripted findings — a defect row with a fixing commit or a ledger number.

Record the user's verdicts into the two rows when they report back. Until they do, the rows keep
their unfilled markers, and 07-06's cutover gate will surface them as a reason to hold.

Do not stop the servers at the end of this task.
  </action>
  <verify>
    <automated>bash scripts/dual_site.sh start</automated>
    <fails_when>non-zero exit, or the printed status block is missing either the `vanilla http://127.0.0.1:8000 up` line or the `react http://127.0.0.1:8001 up` line</fails_when>
    <automated>bash scripts/dual_site.sh status</automated>
    <fails_when>non-zero exit, or either port is reported down — the user must never be pointed at a URL that does not answer</fails_when>
    <human-check>
      Both sites are running: vanilla at http://127.0.0.1:8000 and React at http://127.0.0.1:8001, serving the same live export for the validation gameweek recorded in PARITY-REPORT.md.

      Working through PARITY-CHECKLIST.md, confirm:
      1. Eyeball pass, all eight pages on both sites — layout, spacing, dark and light theme, and whether any element overflows or wraps badly. Values and sort order are the script's job; do not re-check them by eye.
      2. Same-session solver comparison, entry 6980093 loaded on both sites in one session:
         a. Rate my team — the rating tiles and their figures match across sites.
         b. One solve with an identical set of locks — the suggested transfers and resulting XI match.
         c. One two-gameweek plan — the per-week moves match.
      3. For the rest of this gameweek, use the React site on 8001 for your real FPL week, with vanilla on 8000 as the reference.

      Report either "matched" for each item, or a description of what differed so it becomes a defect row.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `PARITY-REPORT.md`'s pre-deadline stage carries a manual-eyeball row and a D-08 same-session row, each holding either a recorded verdict or the explicit unfilled marker naming what will replace it.
    - The D-08 row names entry 6980093 by its numeric id and the three flows performed; it contains no manager first or last name.
    - `bash scripts/dual_site.sh status` reports both ports up at the end of this task.
    - If any mismatch was reported, it appears as a row in the "Defects found and how they were closed" table with a disposition, and Task 2's closure discipline is applied to it before the stage is considered complete.
    - The SUMMARY records the handover verbatim, including which items are still awaiting a verdict.
  </acceptance_criteria>
  <done>Both sites are up and handed over, the checklist has been presented with concrete URLs and the entry id, and the report carries a manual-eyeball row and a D-08 row that either hold a verdict or visibly announce that they do not.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live pipeline host → validation servers | two uvicorns share CPU, memory and ports with the cron jobs that produce the weekly recommendations |
| live page content → committed evidence file | text read from running sites is written into a repository document |
| React code change → live vanilla site | a fix-forward commit must not reach the site the user actually depends on this week |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-03-01 | Denial of Service | the two long-running uvicorns on the cron host | medium | mitigate | D-03 on-demand sessions: Tasks 1 and 2 stop both servers immediately after their runs, and their acceptance criteria assert both ports are down; only Task 3 leaves them up, and only for the handover session |
| T-07-03-02 | Information Disclosure | `PARITY-REPORT.md` content | high | mitigate | Verdict cells carry only field values extracted by the ledger-aware fragment emitter, whose field map has no manager-name field; acceptance criteria require the D-08 row to name the entry numerically and forbid a manager name in the report or the commit message |
| T-07-03-03 | Tampering | fix-forward commits reaching the authoritative site | high | mitigate | Fixes change React only; an acceptance criterion asserts `git log` shows no new commit touching `web/assets` or `web/*.html` from this plan, so the site the user depends on this week cannot move under them |
| T-07-03-04 | Repudiation | a defect closed without evidence | high | mitigate | Every closure carries a commit SHA or a ledger number; closing by narrowing the field map is explicitly forbidden and an acceptance criterion asserts the total field count did not fall |
| T-07-03-05 | Denial of Service | the weekly recommendation pipeline | high | mitigate | `scripts/daily.sh`, `scripts/weekly.sh` and `web/data` are read-only for the whole phase; a `git diff --quiet` acceptance criterion on all three runs in both code-touching tasks |
</threat_model>

<verification>
- `node e2e/parity/parity-diff.mjs --all` exits clean with zero defects against a live dual boot
- `PARITY-REPORT.md`'s pre-deadline stage has eight page rows, a manual-eyeball row and a D-08 row, none of them blank
- `npm --prefix frontend run build`, `npm --prefix frontend test` and `npm --prefix e2e run test` are all green after any fix-forward commit
- `git diff --quiet -- scripts/daily.sh scripts/weekly.sh web/data e2e/fixtures` — the pipeline and the frozen fixtures are untouched
</verification>

<success_criteria>
- The validation gameweek is identified from the live export and recorded, and the cycle starts at its pre-deadline export
- All eight pages carry a pre-deadline verdict traceable to a ledger number, a fixing commit, or an explicit unfilled marker
- Every defect is closed by fixing React forward or by adding a ledger row, then re-compared in the same stage
- The cron-green citation names evidence that exists, or states verbatim that the cron log is absent
- The user has both sites in front of them, the checklist, and the entry id for the same-session comparison
</success_criteria>

<output>
Create `.planning/phases/07-parity-validation-cutover/07-03-SUMMARY.md` when done
</output>
