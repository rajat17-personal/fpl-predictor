---
phase: 05-container-build-ci-pipeline
plan: 04
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - .github/workflows/daily.yml
  - .github/workflows/weekly.yml
  - .gitignore
  - google-chrome-stable_current_amd64.deb
autonomous: true
requirements: [SEC-04]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The 140MB Chrome installer is gone from the working tree, and git history confirms it was never committed, so its removal required no history rewrite"
    - "Both scheduler workflows run on Python 3.14, install from the hashed runtime lock with --require-hashes, and commit under the github-actions bot identity rather than any personal identity"
    - "Neither scheduler workflow carries a schedule trigger, so nothing in this repository competes with the local cron that owns the irreplaceable daily price snapshot"
    - "The weekly workflow no longer claims the trained model file lives inside the repository"
    - "No tracked file contains a personal email address or a credential-shaped token literal, proven by a scan that reports how many tracked files it read"
    - "The daily price-snapshot history remains tracked in git and byte-identical to HEAD"
  prohibitions:
    - statement: "MUST NOT disturb or destroy the irreplaceable daily price-snapshot history — no workflow in this repository may carry an armed schedule trigger while the local cron owns that job, and no ignore-rule or deletion may drop data/snapshots from version control; that history cannot be backfilled"
      category: safety
      status: resolved
      verification: test
    - statement: "MUST NOT rewrite git history to purge any file — repo hygiene here is a working-tree deletion plus ignore-rule coverage only, and a rewrite would invalidate every existing commit reference to remove a file that was never committed in the first place"
      category: safety
      status: resolved
      verification: test
  artifacts:
    - path: ".github/workflows/daily.yml"
      provides: "Dispatch-only daily snapshot/price/scoreboard workflow, modernized to py3.14, hashed installs and bot identity"
      contains: "workflow_dispatch"
    - path: ".github/workflows/weekly.yml"
      provides: "Dispatch-only weekly export/digest workflow, same modernization, false model-location claim removed"
      contains: "workflow_dispatch"
    - path: ".gitignore"
      provides: "Complete exclusion coverage with the snapshot-history re-inclusion intact"
      contains: "!data/snapshots/"
  key_links:
    - from: ".github/workflows/daily.yml"
      to: "requirements.txt"
      via: "installs the hashed runtime lock produced in plan 05-01"
      pattern: "--require-hashes"
    - from: ".github/workflows/weekly.yml"
      to: "requirements.txt"
      via: "installs the hashed runtime lock produced in plan 05-01"
      pattern: "--require-hashes"
---

<objective>
Make the repository safe to push: modernize the two dormant scheduler workflows onto Python 3.14 and
hash-verified installs under a bot commit identity with their schedules disarmed, delete the 140MB
Chrome installer from the working tree, reconcile the uncommitted `.gitignore` edits, and prove no
tracked file carries a personal email or a credential-shaped literal.

Purpose: SEC-04, plus D-11, D-12 and D-16. This is the last thing that happens before a human pushes
this repository to GitHub for the first time, so anything wrong here becomes permanently visible the
moment plan 05-05's checkpoint completes.

Output: modernized `.github/workflows/daily.yml` and `.github/workflows/weekly.yml`, a reconciled
`.gitignore`, and a deleted installer.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-container-build-ci-pipeline/05-CONTEXT.md
@.planning/phases/05-container-build-ci-pipeline/05-RESEARCH.md
@.planning/phases/05-container-build-ci-pipeline/05-PATTERNS.md
@.planning/phases/05-container-build-ci-pipeline/05-01-SUMMARY.md
</context>

## Artifacts this phase produces

See `05-01-PLAN.md` § "Artifacts this phase produces" for the full phase-wide table. This plan creates
no new files; it edits three existing tracked files and deletes one untracked one.

## Flagged assumptions (unresolved probe edges)

The spec-less edge probe classified this plan's SEC-04 row as `unclassified` and it stays
**unresolved** — surfaced here rather than silently dropped:

- **SEC-04 / unclassified** — "review manually". Assumed reading: "proper `.gitignore`" means complete
  coverage of every generated, secret-bearing, and installer-shaped path currently present in the
  working tree, with the snapshot-history re-inclusion preserved. It does not mean an audit of paths
  that might appear in future, and it does not extend to `.planning/`, which is GSD's own record and is
  deliberately tracked under this project's `commit_docs` setting.

## Working-tree state the executor must reconcile, not clobber

At planning time `git status` shows uncommitted edits to two of this plan's files. Both are regressions
relative to `HEAD` and both must be resolved deliberately:

| File | What HEAD has | What the working tree has | Correct resolution |
|------|---------------|---------------------------|--------------------|
| `.github/workflows/weekly.yml` | a bot-shaped git identity on the two `git config` lines | a hardcoded personal name and a personal email address on those same lines | Modernize to the github-actions bot identity (Task 1). The working-tree edit is discarded by that change, not preserved. |
| `.gitignore` | section headers in the `# --- Name ---` style, file ends with a newline | headers flattened to plain `# Name`, two headers carrying trailing whitespace, final newline removed | Restore the `# --- Name ---` header style and the trailing newline; keep every rule and both explanatory blocks unchanged (Task 2). |

Neither file may be reverted with a bulk `git checkout` — the substantive rules and the explanatory
comment blocks at the end of `.gitignore` are correct in both versions and must survive.

<tasks>

<task type="auto">
  <name>Modernize the daily and weekly scheduler workflows</name>
  <files>.github/workflows/daily.yml, .github/workflows/weekly.yml</files>
  <read_first>
    - `.github/workflows/daily.yml` — read in full; this is an in-place edit and the commit-if-changed idiom must survive verbatim
    - `.github/workflows/weekly.yml` — read in full, including the uncommitted working-tree identity change described in the table above
    - `.planning/phases/05-container-build-ci-pipeline/05-PATTERNS.md` § daily.yml and weekly.yml — the seven required modernization diffs, item by item
    - `.planning/phases/05-container-build-ci-pipeline/05-RESEARCH.md` § Assumptions Log row A5 — the bot identity convention and its confidence level
    - `.planning/phases/05-container-build-ci-pipeline/05-CONTEXT.md` — D-11 (what modernizes) and D-12 (what stays off, and why)
    - `requirements.txt` — the hashed lock produced in plan 05-01 that both workflows now install
    - `.planning/phases/05-container-build-ci-pipeline/05-03-PLAN.md` § "Resolving action SHAs" — the SHA resolution command these files also need
  </read_first>
  <action>
    Apply the same modernization to both workflows, editing in place and leaving every other line
    untouched.

    Remove the `schedule:` block from each `on:` map, leaving `workflow_dispatch:` as the sole trigger.
    Replace the removed block with a comment recording why: the local cron on the developer's machine is
    the one production scheduler for the daily price snapshot, that history cannot be backfilled if a day
    is missed, and arming these workflows is next milestone's work once model delivery and hosting are
    settled. Do not leave the cron expressions behind as commented-out lines — a commented cron reads as
    "temporarily disabled", while this decision is deliberate and belongs in prose.

    Change `python-version` from `"3.12"` to `"3.14"` in both files.

    Change `pip install -r requirements.txt` to `pip install --require-hashes -r requirements.txt` in
    both, per D-04's hash enforcement everywhere. These workflows install the runtime lock only, never
    the dev lock (D-02) — they run the pipeline, not the test suite.

    SHA-pin both `uses:` lines in each file using the resolution procedure documented in
    `05-03-PLAN.md`: `actions/checkout` at v7.0.1 and `actions/setup-python` at v7.0.0, each written as
    the 40-character commit SHA with the version as a trailing comment. Both files currently carry
    mutable major tags, which is exactly what CI-04 forbids, and that requirement covers all three
    workflows in this repository rather than only the new one.

    Replace the git identity block in both `Commit outputs` steps with the github-actions bot identity:
    user name `github-actions[bot]` and email
    `41898282+github-actions[bot]@users.noreply.github.com`. In `weekly.yml` this discards the
    uncommitted working-tree edit that put a personal name and personal email address there — that
    discard is the intended outcome, not an accident to guard against.

    In `weekly.yml`, delete the sentence in the header comment claiming the workflow requires the trained
    model file to be present in the repository at 7MB. That claim is false: the model output directory is
    ignored by git and no checkout has ever contained it, and D-05 makes "the model arrives at run time"
    the standing posture for this milestone. Replace it with an accurate sentence saying the model is
    supplied at run time, and that this is one of the reasons the workflow stays dispatch-only.

    In `daily.yml`, add a one-line comment above the `git add` line noting that the price-model output
    path listed there sits under an ignored directory today, so that element of the add is a no-op —
    recorded as inherited work for Phase 6's cron-reliability requirement rather than changed here.

    Everything else stays exactly as it is: `runs-on: ubuntu-latest`, the scoped
    `permissions: contents: write`, `env: PYTHONPATH: .` on the pipeline steps, the ordered
    `python -m ...` command lists, and the commit-if-changed shell idiom. That idiom is the established
    pattern for these two workflows and is deliberately outside the failure-swallowing rule that governs
    `ci.yml` — these are pipeline jobs, not verification jobs, and the same shell construct means
    something different in each context.
  </action>
  <verify>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "
import yaml
for f in ('.github/workflows/daily.yml','.github/workflows/weekly.yml'):
    t = (lambda d: d.get('on', d.get(True)))(yaml.safe_load(open(f)))
    assert 'schedule' not in t, f + ' still carries a schedule trigger'
    assert 'workflow_dispatch' in t, f + ' lost its workflow_dispatch trigger'
print('SCHEDULERS DISARMED BUT STILL MANUALLY DISPATCHABLE')
"</automated>
    <fails_when>non-zero exit with an AssertionError naming which workflow failed which check, or "SCHEDULERS DISARMED BUT STILL MANUALLY DISPATCHABLE" absent from stdout</fails_when>
    <automated>for f in .github/workflows/daily.yml .github/workflows/weekly.yml; do grep -q 'python-version: "3.14"' "$f" || { echo "FAILED: $f is not on Python 3.14"; exit 1; }; grep -q -- '--require-hashes' "$f" || { echo "FAILED: $f installs without hash verification"; exit 1; }; grep -q 'user.name "github-actions\[bot\]"' "$f" || { echo "FAILED: $f lacks the bot user name"; exit 1; }; grep -q '41898282+github-actions\[bot\]@users.noreply.github.com' "$f" || { echo "FAILED: $f lacks the bot email"; exit 1; }; done; echo "BOTH SCHEDULERS: PY314 + HASHED INSTALL + BOT IDENTITY"</automated>
    <fails_when>non-zero exit with a "FAILED:" line naming the file and the missing property, or "BOTH SCHEDULERS: PY314 + HASHED INSTALL + BOT IDENTITY" absent from stdout</fails_when>
    <automated>! grep -qiE 'xp_model|cron:' .github/workflows/weekly.yml .github/workflows/daily.yml && echo "NO FALSE MODEL-LOCATION CLAIM AND NO CRON EXPRESSION SURVIVES"</automated>
    <fails_when>non-zero exit, or "NO FALSE MODEL-LOCATION CLAIM AND NO CRON EXPRESSION SURVIVES" absent from stdout — either the stale header sentence is still there or a cron expression was left commented out</fails_when>
    <automated>T=$(grep -hE '^[[:space:]]*-?[[:space:]]*uses:' .github/workflows/daily.yml .github/workflows/weekly.yml | wc -l); P=$(grep -hE '^[[:space:]]*-?[[:space:]]*uses:[[:space:]]*[^[:space:]]+@[0-9a-f]{40}([[:space:]]|$)' .github/workflows/daily.yml .github/workflows/weekly.yml | wc -l); echo "uses=$T sha_pinned=$P"; test "$T" -gt 0 && test "$T" = "$P"</automated>
    <fails_when>non-zero exit, or the echoed `uses=` and `sha_pinned=` counts differ — a scheduler workflow still pins an action to a mutable tag</fails_when>
    <automated>git diff HEAD --stat -- .github/workflows/daily.yml .github/workflows/weekly.yml; /home/sraja/miniconda3/envs/python314/bin/python -c "
import yaml
for f in ('.github/workflows/daily.yml','.github/workflows/weekly.yml'):
    j = list(yaml.safe_load(open(f))['jobs'].values())[0]
    assert j['runs-on'] == 'ubuntu-latest', f
    assert j['permissions'] == {'contents': 'write'}, (f, j['permissions'])
    assert any('PYTHONPATH' in str(s.get('env','')) for s in j['steps']), f + ' lost its PYTHONPATH step env'
    assert any('git diff --cached --quiet' in str(s.get('run','')) for s in j['steps']), f + ' lost the commit-if-changed idiom'
print('PRESERVED STRUCTURE INTACT IN BOTH SCHEDULERS')
"</automated>
    <fails_when>non-zero exit with an AssertionError naming the workflow and the lost property, or "PRESERVED STRUCTURE INTACT IN BOTH SCHEDULERS" absent from stdout</fails_when>
  </verify>
  <acceptance_criteria>
    - Neither scheduler workflow's trigger map contains a `schedule` key; both still contain `workflow_dispatch`
    - No cron expression survives in either file, commented or otherwise
    - Both files set `python-version: "3.14"` and install with `--require-hashes -r requirements.txt`
    - Both `Commit outputs` steps configure user name `github-actions[bot]` and email `41898282+github-actions[bot]@users.noreply.github.com`
    - `weekly.yml`'s header no longer names the trained model file as something the repository must contain
    - Every `uses:` line in both files carries a 40-character SHA with a trailing version comment
    - Both jobs still declare `runs-on: ubuntu-latest`, `permissions: {contents: write}`, a step carrying `PYTHONPATH`, and the commit-if-changed shell idiom
  </acceptance_criteria>
  <done>Both schedulers are modernized, SHA-pinned, hash-installing, bot-identified, and deliberately disarmed, with every other structural element preserved.</done>
</task>

<task type="auto">
  <name>Repo hygiene: remove the installer, reconcile .gitignore, prove the tree is push-clean</name>
  <files>.gitignore, google-chrome-stable_current_amd64.deb</files>
  <read_first>
    - `.gitignore` — read in full, including the uncommitted working-tree edits described in the table above
    - `.planning/phases/05-container-build-ci-pipeline/05-PATTERNS.md` § `.dockerignore` — quotes the full current `.gitignore` and identifies which parts are load-bearing
    - `.planning/phases/05-container-build-ci-pipeline/05-CONTEXT.md` § domain — the repo-scout facts, in particular that the installer was never committed and that `.git` is only 10MB
    - `.planning/STATE.md` § Blockers/Concerns — the standing note that the daily snapshot history cannot be backfilled
    - `.planning/codebase/CONCERNS.md` — the SEC-04 slice of the concerns audit this task closes
  </read_first>
  <action>
    Confirm first, then delete. Run `git log --all --oneline -- google-chrome-stable_current_amd64.deb`
    and confirm it returns nothing before removing anything. That fact is what makes this removal a
    working-tree deletion rather than a history problem, and it must be verified rather than assumed. If
    it returns any commit, stop and surface it: a history rewrite is explicitly forbidden by D-16 and
    would need a fresh decision, not an improvised one. With the check clean, delete the 140MB file with
    a plain `rm`. Also remove the `downloaded_files/` scratch directory if and only if it is empty; leave
    it alone if it holds anything.

    Reconcile `.gitignore`. Restore the `# --- Name ---` section-header comment style that `HEAD` carries
    and the working tree flattened, strip the trailing whitespace the working-tree edit left on two
    headers, and restore the final newline. Keep every existing rule and both explanatory comment blocks
    at the end of the file exactly as they are — in particular the two-line block re-including the daily
    price-snapshot history, and the "deliberately NOT ignored" block that documents why the export
    contract, the workflow definitions and the frozen E2E fixture set stay tracked. Add three rules the
    current file lacks and the working tree now needs, each under the appropriate existing section with
    an inline reason: the GSD runtime directory `.gsd/`, local virtual environments (`.venv/` and
    `venv/`), and Docker build metadata (`.docker/`). Add nothing covering `.planning/` — that directory
    is deliberately tracked.

    Then prove the tree is safe for its first push to a hosted remote. Scan every tracked file for a
    personal email address and for credential-shaped literals, and make the scan self-evidencing by
    reporting the number of tracked files it read alongside the result — a scan that silently matched
    nothing because it read nothing is worse than no scan. If there is a hit, print it and stop; do not
    redact and continue, because a literal in a tracked file is a decision for the developer, not a
    cleanup for the executor. The one hit that exists at planning time is the personal email in
    `weekly.yml`, which the previous task removes; a hit anywhere else is a finding for the SUMMARY.

    Finally confirm nothing in this plan touched the irreplaceable data: the daily snapshot history must
    still be tracked in git and byte-identical to `HEAD`.
  </action>
  <verify>
    <automated>test ! -e google-chrome-stable_current_amd64.deb && test -z "$(git log --all --oneline -- google-chrome-stable_current_amd64.deb)" && echo "INSTALLER GONE FROM THE TREE, ABSENT FROM HISTORY, NO REWRITE PERFORMED"</automated>
    <fails_when>non-zero exit, or the confirmation line is absent from stdout — either the file is still on disk, or it turns out to be in history and the removal strategy needs a fresh decision</fails_when>
    <automated>N=$(git ls-files | wc -l); test "$N" -gt 0 || { echo "FAILED: git ls-files returned nothing, the scan would be vacuous"; exit 1; }; HITS=$(git ls-files -z | xargs -0 grep -InE '[A-Za-z0-9._%+-]+@(gmail|yahoo|hotmail|outlook|icloud|protonmail)\.(com|me)|ghp_[A-Za-z0-9]{30,}|github_pat_[A-Za-z0-9_]{50,}|sk-[A-Za-z0-9]{32,}|AKIA[0-9A-Z]{16}' || true); test -z "$HITS" || { echo "FAILED: credential-shaped or personal-email literal in a tracked file:"; printf '%s\n' "$HITS"; exit 1; }; echo "SCANNED $N TRACKED FILES - NO PERSONAL EMAIL OR CREDENTIAL LITERAL"</automated>
    <fails_when>non-zero exit; a "FAILED:" line naming the offending file and line; a scanned-file count of 0; or the "SCANNED ... TRACKED FILES" line absent from stdout</fails_when>
    <automated>test "$(git ls-files data/snapshots | wc -l)" -gt 0 && git diff --quiet HEAD -- data/snapshots && echo "SNAPSHOT HISTORY STILL TRACKED AND UNCHANGED"</automated>
    <fails_when>non-zero exit, or "SNAPSHOT HISTORY STILL TRACKED AND UNCHANGED" absent from stdout — the irreplaceable price history was dropped from tracking or modified</fails_when>
    <automated>for p in '.gsd/' '.venv/' 'venv/' '.env' '*.deb' 'models/artifacts/' 'data/raw/' 'data/processed/' 'frontend/dist/' 'node_modules/'; do grep -qxF "$p" .gitignore || { echo "MISSING FROM .gitignore: $p"; exit 1; }; done; grep -qxF '!data/snapshots/' .gitignore && grep -qxF '!data/snapshots/**' .gitignore && tail -c1 .gitignore | od -c | grep -q '\\n' && echo "GITIGNORE COMPLETE, SNAPSHOT RE-INCLUSION INTACT, TRAILING NEWLINE RESTORED"</automated>
    <fails_when>non-zero exit with a "MISSING FROM .gitignore:" line naming the absent rule, or the summary line absent from stdout — a required rule is missing, the snapshot re-inclusion was dropped, or the final newline is still missing</fails_when>
    <automated>! grep -nE ' +$' .gitignore && test "$(grep -c '^# --- ' .gitignore)" -ge 6 && echo "HEADER STYLE RESTORED, NO TRAILING WHITESPACE"</automated>
    <fails_when>non-zero exit, or "HEADER STYLE RESTORED, NO TRAILING WHITESPACE" absent from stdout — the flattened header style or the trailing whitespace from the uncommitted edit survived</fails_when>
    <automated>git status --porcelain -- google-chrome-stable_current_amd64.deb; test -z "$(git status --porcelain -- google-chrome-stable_current_amd64.deb)" && echo "INSTALLER LEAVES NO TRACE IN GIT STATUS"</automated>
    <fails_when>non-zero exit, output showing a staged deletion or an untracked entry for the installer, or the confirmation line absent from stdout — a deletion appearing here would mean the file was tracked after all, contradicting the history check</fails_when>
  </verify>
  <acceptance_criteria>
    - `google-chrome-stable_current_amd64.deb` no longer exists on disk, and `git log --all -- <that path>` returns nothing
    - `git status --porcelain` shows no entry of any kind for that path
    - No history-rewriting command (`filter-branch`, `filter-repo`, `rebase --root`, `reflog expire --expire=now`) was run; the SUMMARY states this explicitly
    - `.gitignore` contains `.gsd/`, `.venv/`, `venv/`, `.docker/`, and every rule `HEAD` already had
    - `.gitignore` still contains both `!data/snapshots/` and `!data/snapshots/**`, and both explanatory comment blocks at the end of the file
    - `.gitignore` uses the `# --- Name ---` section-header style, has no trailing whitespace on any line, and ends with a newline
    - `.gitignore` contains no rule covering `.planning/`
    - A scan of every tracked file reports the count of files read and finds no personal email address and no credential-shaped literal
    - `git ls-files data/snapshots` is non-empty and `git diff HEAD -- data/snapshots` is empty
  </acceptance_criteria>
  <done>The installer is gone without a history rewrite, `.gitignore` is reconciled and complete, and a self-evidencing scan proves no tracked file carries a personal email or credential literal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working tree → a hosted git remote | Everything tracked becomes permanently visible to anyone with repository access the moment plan 05-05's push completes |
| scheduler workflow → the repository's own contents | Both workflows hold `contents: write` and commit back to the default branch |
| this repository's schedulers → the local production cron | Two independent schedulers running the same idempotent-per-day snapshot job would contend for the same irreplaceable data |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-04-01 | Information Disclosure | tracked files at first push | high | mitigate | A tracked-file scan for personal email addresses and credential-shaped literals that reports its own file count, so a vacuous pass is visible; a hit stops the task rather than being auto-redacted |
| T-05-04-02 | Repudiation | commits authored by the scheduler workflows | medium | mitigate | Both workflows commit as `github-actions[bot]` with the corresponding no-reply address, so automated commits are attributable to automation rather than to a person who did not make them |
| T-05-04-03 | Tampering | `uses:` lines in the scheduler workflows | high | mitigate | Both actions SHA-pinned and asserted by a counting gate; CI-04's pinning requirement is applied to all three workflows, not only the new one |
| T-05-04-04 | Denial of Service | the daily price-snapshot job | high | mitigate | Every `schedule:` trigger removed and asserted absent; the local cron remains the sole scheduler, so no second runner can contend for a job whose history cannot be backfilled |
| T-05-04-05 | Tampering | git history | high | mitigate | The installer's absence from history is verified before deletion, which removes the only stated reason anyone would reach for a rewrite; no rewriting command is used and the SUMMARY records that |
| T-05-04-06 | Information Disclosure | `.env` and generated artifacts | high | mitigate | `.gitignore` coverage for `.env`, `.env.*`, `*.deb`, `models/artifacts/`, `data/raw/`, `data/processed/` and `.gsd/` asserted line by line by a gate |
</threat_model>

<verification>
- Both schedulers: no `schedule` trigger, `workflow_dispatch` retained, Python 3.14, `--require-hashes`, bot identity, SHA-pinned actions, preserved job structure
- `weekly.yml`'s false model-location claim removed; no cron expression survives in either file
- The installer is deleted, confirmed never committed, and leaves no `git status` entry
- `.gitignore` is complete, correctly styled, newline-terminated, and still re-includes the snapshot history
- A file-count-reporting scan finds no personal email or credential literal in any tracked file
- `data/snapshots` remains tracked and byte-identical to `HEAD`
</verification>

<success_criteria>
SEC-04 is closed: the installer is gone, `.gitignore` covers every generated and secret-bearing path,
and all three workflow definitions are tracked, SHA-pinned and modernized. The repository is in a state
where the first push in plan 05-05 exposes nothing the developer would want back.
</success_criteria>

<output>
Create `.planning/phases/05-container-build-ci-pipeline/05-04-SUMMARY.md` when done
</output>
