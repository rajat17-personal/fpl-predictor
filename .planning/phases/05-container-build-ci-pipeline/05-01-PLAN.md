---
phase: 05-container-build-ci-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.in
  - requirements-dev.in
  - requirements.txt
  - requirements-dev.txt
  - ruff.toml
autonomous: false
requirements: [SEC-02, CI-01]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "requirements.txt pins every runtime dependency to an exact version and carries at least one --hash=sha256: line per pin"
    - "requirements-dev.txt exists as a separate hashed lock and agrees with requirements.txt on every shared package version"
    - "A venv created fresh from the Python 3.14 interpreter installs both locks with pip install --require-hashes and no source compilation"
    - "The full existing pytest suite passes from that clean hash-locked venv (77 tests as of 2026-09-04)"
    - "Neither compiled lock contains the browser-automation transitive chain that the floating understatapi constraint used to drag in"
    - "ruff check . exits 0 across every tracked Python module with no rule silenced in ruff.toml's ignore list"
  artifacts:
    - path: "requirements.in"
      provides: "Human-readable runtime dependency source (D-02 split, test-only deps excluded)"
      contains: "understatapi=="
    - path: "requirements-dev.in"
      provides: "Dev/CI dependency source layered on the runtime source"
      contains: "-r requirements.in"
    - path: "requirements.txt"
      provides: "Fully-pinned, hash-verified runtime lock consumed by CI and the Dockerfile"
      contains: "--hash=sha256:"
    - path: "requirements-dev.txt"
      provides: "Fully-pinned, hash-verified dev/CI lock (pytest, responses, ruff)"
      contains: "--hash=sha256:"
    - path: "ruff.toml"
      provides: "Python lint configuration for CI-01's lint step"
      contains: "select"
  key_links:
    - from: "requirements-dev.in"
      to: "requirements.in"
      via: "pip requirement-file include, so both locks resolve against one dependency graph"
      pattern: "-r requirements\\.in"
    - from: "requirements.txt"
      to: "requirements.in"
      via: "uv pip compile --generate-hashes output header records the source file"
      pattern: "requirements\\.in"
---

<objective>
Replace the repository's floating `>=` dependency constraints with two compiled, fully-pinned,
hash-verified lockfiles, and prove end to end that a clean Python 3.14 environment built only from
those locks runs the entire existing test suite green. Add the Python lint configuration that CI-01's
lint step will invoke.

Purpose: SEC-02 — every later layer of this phase (the Docker builder stage, the CI test job, the
daily/weekly schedulers) installs from these files. If the lock is wrong, every downstream layer
inherits the fault. Proving the install path locally, before any CI or container work exists, is what
makes the rest of the phase a transcription of a working command sequence rather than a guess.

Output: `requirements.in`, `requirements-dev.in`, hashed `requirements.txt`, hashed
`requirements-dev.txt`, `ruff.toml`, and a clean Python source tree that passes `ruff check .`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-container-build-ci-pipeline/05-CONTEXT.md
@.planning/phases/05-container-build-ci-pipeline/05-RESEARCH.md
@.planning/phases/05-container-build-ci-pipeline/05-PATTERNS.md
</context>

## Artifacts this phase produces

Symbols, files, and configuration keys created by Phase 5 (none of these exist in the codebase
today — do not treat them as drift against existing source):

| Artifact | Kind | Created in |
|----------|------|------------|
| `requirements.in` | new file | 05-01 |
| `requirements-dev.in` | new file | 05-01 |
| `requirements-dev.txt` | new file | 05-01 |
| `ruff.toml` | new file | 05-01 |
| `Dockerfile` | new file | 05-02 |
| `.dockerignore` | new file | 05-02 |
| `scripts/smoke_test.sh` | new file | 05-02 |
| `SMOKE_DOCKER` / `SMOKE_PORT` env seams | new env vars read by `scripts/smoke_test.sh` | 05-02 |
| `.github/workflows/ci.yml` | new file | 05-03 |
| `lint-build` / `test` / `e2e` / `image` / `publish` | new CI job names in `ci.yml` | 05-03 |
| `scripts/preflight.sh` | new file | 05-05 |

`requirements.txt` already exists but is fully rewritten (loose `>=` constraints → compiled hashed
lock). `.gitignore`, `.github/workflows/daily.yml`, and `.github/workflows/weekly.yml` already exist
and are edited in place by 05-04.

## Flagged assumptions (unresolved probe edges)

The spec-less edge probe classified this plan's SEC-02 row as `unclassified` and it stays
**unresolved** — it is surfaced here as an explicit assumption rather than silently dropped:

- **SEC-02 / unclassified** — "review manually". Assumed reading: SEC-02's "verified installable in a
  fresh environment" means a venv created from the Python 3.14 interpreter with no inherited
  site-packages, installing with `--require-hashes`, on this Linux/x86_64 host only. Installability on
  other platforms/architectures is out of scope for this phase and is not asserted anywhere.

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <decision>Approve installing the two new `[SUS]`-verdict developer tools this phase introduces: `uv` (lockfile compiler) and `ruff` (Python linter)</decision>
  <context>
    `05-RESEARCH.md` § Package Legitimacy Audit ran the legitimacy gate against both packages and both
    came back `[SUS]`. Neither package name was sourced from official documentation, so both carry an
    `[ASSUMED]` name marker as well. Under this project's established protocol (STATE.md records the
    same gate for Phase 1's 13-package set, Phase 2's four fonts/markdown packages, and Phase 4's
    `@playwright/test`), no `[SUS]`/`[ASSUMED]` package is installed without an explicit human
    approval — and this gate is never auto-approved, in any mode.

    What the audit found:

    | Package | Registry | Project age / stars | Source repo | Gate verdict | Why the gate fired |
    |---------|----------|---------------------|-------------|--------------|--------------------|
    | `uv` 0.12.9 | PyPI | repo created 2023-10-02, 89,430 stars | github.com/astral-sh/uv | `[SUS]` | `too-new` + `unknown-downloads` — the gate measures the *latest release* date, not project age |
    | `ruff` 0.16.6 | PyPI | repo created 2022-08-09, 49,471 stars | github.com/astral-sh/ruff | `[SUS]` | same signal pattern |

    Researcher's note, which is context for you and not a substitute for this gate: both are
    long-established Astral tools, and the `too-new` signal is a known false-positive pattern for
    fast-release-cadence CLI tools whose newest release is days old.

    Scope of the install if you approve: both go into the conda dev environment
    (`/home/sraja/miniconda3/envs/python314`) only. `ruff` additionally becomes a pinned line in
    `requirements-dev.in` (CI runs it). `uv` is deliberately absent from both lockfiles — D-01 makes it
    a lockfile compiler that never ships in CI or the image.

    No other new package is introduced by this phase. Every ML/API dependency being re-pinned
    (`pandas`, `numpy`, `pyarrow`, `requests`, `scikit-learn`, `lightgbm`, `understatapi`, `pulp`,
    `scipy`, `fastapi`, `uvicorn`, `httpx`, `joblib`, `responses`) is already an approved, in-use
    dependency of this repository.
  </context>
  <options>
    <option id="approve-both">
      <name>Approve both `uv` and `ruff`</name>
      <pros>Unblocks the whole phase. uv's `--generate-hashes` is what SEC-02's hash-verified lock requires; ruff is CI-01's lint step. Both installs are dev/CI-side only and never enter the shipped image.</pros>
      <cons>Two tools whose latest releases are recent enough to trip the gate's `too-new` signal.</cons>
    </option>
    <option id="approve-uv-only">
      <name>Approve `uv` only, drop the Python lint step</name>
      <pros>Smaller new-tool surface; SEC-02 still lands in full.</pros>
      <cons>CI-01 explicitly requires "lint" on every push/PR. Dropping it would leave a roadmap requirement unmet, so this needs a follow-up decision about what CI-01's lint step becomes.</cons>
    </option>
    <option id="reject">
      <name>Reject both</name>
      <pros>No new tooling.</pros>
      <cons>SEC-02 cannot be delivered — nothing else in the environment produces hash-generating locks (`pip freeze` is explicitly forbidden by D-02). The phase would need re-planning from the roadmap down.</cons>
    </option>
  </options>
  <resume-signal>Reply "approve-both", "approve-uv-only", or "reject". If approving, note any version you want pinned differently from 0.12.9 / 0.16.6.</resume-signal>
</task>

<task type="tracer">
  <name>End-to-end: hash-locked clean environment runs the whole existing test suite</name>
  <precondition>Network access to pypi.org is available and the interpreter at /home/sraja/miniconda3/envs/python314/bin/python reports Python 3.14.x</precondition>
  <reversibility rating="reversible">Both `.in` files and both locks are regenerable from one command; changing a pin is a recompile, not a migration.</reversibility>
  <files>requirements.in, requirements-dev.in, requirements.txt, requirements-dev.txt</files>
  <read_first>
    - `requirements.txt` — the current loose-constraint file; it is the source material for the D-02 split and the file being replaced
    - `.planning/phases/05-container-build-ci-pipeline/05-PATTERNS.md` § `requirements.in` / `requirements-dev.in` — the split pattern and the grouped-comment convention to preserve
    - `.planning/phases/05-container-build-ci-pipeline/05-RESEARCH.md` § Standard Stack (Runtime stack table), § Common Pitfalls 1 and 3, § Code Examples (uv lockfile compile)
    - `pytest.ini` — the plugin disables (`-p no:playwright -p no:seleniumbase`) and `testpaths` the verification run inherits
    - `tests/test_fixture_mode.py` — the SPA-fallback test that needs a built `frontend/dist/` before pytest runs
  </read_first>
  <action>
    Install the tools approved in Task 1 into the conda dev environment only, at the exact pins the
    checkpoint approved, re-verifying each pin against the live PyPI registry immediately before
    install (the project's established zero-drift discipline):
    `/home/sraja/miniconda3/envs/python314/bin/python -m pip install uv==APPROVED_UV_PIN ruff==APPROVED_RUFF_PIN`,
    substituting the two exact version strings the Task 1 checkpoint approved for the two placeholder
    tokens.

    Create `requirements.in` from `requirements.txt`, carrying over every non-test line and preserving
    the existing grouped section comments (`# Phase 0-1 (ingestion/build) ...`, `# Phase 2-3
    (features/model) ...`, `# Phase 4 (optimization)`, `# Product layer (site export, scoreboard,
    solver API)`) and the inline rationale comments. Two constraints change from the current file, each
    with a one-line inline comment naming the reason:
    - `understatapi` moves from a floating `>=0.5` to the exact pin `understatapi==0.7.1` — the floating
      constraint resolves to an old release whose declared dependencies drag in a browser-automation
      package chain the runtime API never calls; 0.7.1 declares only certifi, charset-normalizer, idna,
      requests and urllib3 (05-RESEARCH.md Pitfall 1).
    - `pulp` moves from `>=2.8` to `pulp>=3.3,<4.0` — PuLP 4.x removes the bundled CBC binary that
      `pulp.PULP_CBC_CMD` uses at all three call sites (`optimize/squad_ilp.py`,
      `optimize/transfers.py`, `optimize/multi_period.py`); the upper bound stops an unattended
      recompile from silently landing on it (05-RESEARCH.md Pitfall 3). The repo already emits PuLP's
      own 4.0 deprecation warning on every solve, so this is an active, not theoretical, boundary.
    The `# Test-only` block (`responses>=0.25,<0.27`) does NOT go in `requirements.in`.

    Create `requirements-dev.in` whose first line is `-r requirements.in`, followed by
    `responses>=0.25,<0.27` (carrying its existing inline comment), `pytest>=8`, and `ruff==` followed
    by the exact pin the Task 1 checkpoint approved. Do NOT add `uv` to either `.in` file — per D-01 it is a dev-time lockfile compiler and must
    never appear in a file that CI or the Docker builder installs from.

    Both locks are compiled with hashes because D-04 makes `--require-hashes` the install form
    everywhere downstream — CI jobs, the Docker builder stage, and both scheduler workflows — so a lock
    without hash lines would silently disarm every one of those consumers.

    Compile both locks with uv, runtime first:
    `uv pip compile requirements.in --generate-hashes --python-version 3.14 -o requirements.txt`
    then
    `uv pip compile requirements-dev.in --generate-hashes --python-version 3.14 -c requirements.txt -o requirements-dev.txt`.
    The `-c requirements.txt` constraint is what keeps every package shared between the two locks at
    one identical version, so `pip install --require-hashes -r requirements.txt -r requirements-dev.txt`
    (the invocation D-02 specifies for CI) never hits a conflicting-requirement error. If this uv build
    rejects `-c` alongside `--generate-hashes`, drop the flag and rely on same-session resolution — the
    shared-pin parity gate below is the arbiter either way, and a failing parity gate means the `-c`
    form must be made to work rather than accepted as drift.

    Then prove the whole path end to end, which is the point of this task: create a venv from the
    conda 3.14 interpreter with no inherited site-packages, install BOTH locks into it with
    `--require-hashes`, build `frontend/dist` so the SPA-fallback test has a shell to serve, and run
    the entire existing pytest suite from that venv using the `python -m pytest` form (the `-m` form is
    load-bearing: it puts the repository root on `sys.path`, which is how `import config` and
    `import api.main` resolve — a bare `pytest` binary does not). Every one of the 77 currently-passing
    tests must pass. If any dependency needs a source compile during the install, record which package
    and which wheel tag was missing in the SUMMARY — that is D-03's pre-authorized builder-stage
    fallback trigger for 05-02, and it must not be silently absorbed.

    Delete the venv when the run finishes; it is scratch, never committed, and lives outside the
    repository tree.
  </action>
  <verify>
    <automated>test "$(grep -c -- '--hash=sha256:' requirements.txt)" -gt 0 && test "$(grep -c -- '--hash=sha256:' requirements-dev.txt)" -gt 0 && echo "HASHES PRESENT IN BOTH LOCKS"</automated>
    <fails_when>non-zero exit, or "HASHES PRESENT IN BOTH LOCKS" is absent from stdout</fails_when>
    <automated>! grep -vE '^[[:space:]]*(#|$|--hash|-r |-c )' requirements.txt | grep -vE '==' | grep -q . && echo "EVERY REQUIREMENT LINE PINNED"</automated>
    <fails_when>non-zero exit, or "EVERY REQUIREMENT LINE PINNED" is absent from stdout (an unpinned or range-constrained line survived the compile)</fails_when>
    <automated>! grep -Eqi 'selenium|trio|wsproto|websocket-client' requirements.txt requirements-dev.txt && echo "NO BROWSER-AUTOMATION CHAIN IN LOCKS"</automated>
    <fails_when>non-zero exit, or "NO BROWSER-AUTOMATION CHAIN IN LOCKS" is absent from stdout (the understatapi pin regressed — 05-RESEARCH.md Pitfall 1)</fails_when>
    <automated>A=$(grep -E '^pandas==' requirements.txt) && B=$(grep -E '^pandas==' requirements-dev.txt) && test "$A" = "$B" && echo "SHARED PIN PARITY OK: $A"</automated>
    <fails_when>non-zero exit (either lock has no `pandas==` line at all, or the two pins differ), or "SHARED PIN PARITY OK" is absent from stdout</fails_when>
    <automated>V=$(mktemp -d) && /home/sraja/miniconda3/envs/python314/bin/python -m venv "$V" && "$V/bin/pip" install --require-hashes -r requirements.txt -r requirements-dev.txt && npm --prefix frontend run build && "$V/bin/python" -m pytest -q; rc=$?; rm -rf "$V"; exit $rc</automated>
    <fails_when>non-zero exit; pip prints "THESE PACKAGES DO NOT MATCH THE HASHES FROM THE REQUIREMENTS FILE"; or the pytest tail line reports "failed" or "error" instead of a bare "N passed"</fails_when>
  </verify>
  <acceptance_criteria>
    - `requirements.in` exists, contains `understatapi==0.7.1` and a `pulp` line carrying an upper bound below 4.0, and lists only runtime dependencies — the test-only HTTP-mocking library line lives in `requirements-dev.in` instead
    - `requirements-dev.in` line 1 is `-r requirements.in`; the file additionally lists the HTTP-mocking library, the test runner, and a `ruff==` pin
    - The lockfile compiler itself is absent from both `.in` files (it is a dev-workstation tool only, per D-01)
    - `requirements.txt` and `requirements-dev.txt` each contain at least one `--hash=sha256:` line and no unpinned requirement line
    - `pip install --require-hashes -r requirements.txt -r requirements-dev.txt` completes in a venv built with `python -m venv` from the 3.14 interpreter, with no `--no-binary` fallback and no reported conflicting requirement
    - `python -m pytest -q` from that venv reports every test passing (77 as of 2026-09-04); no test is skipped that was not already skipped on the conda env
    - The compiled locks contain no `selenium`, `trio`, `wsproto`, or `websocket-client` entry
  </acceptance_criteria>
  <done>Both `.in` sources and both hashed locks are committed, and a from-scratch hash-verified install of them runs the full existing test suite green.</done>
</task>

<task type="auto">
  <name>Add the Python lint configuration and drive `ruff check .` to zero findings</name>
  <files>ruff.toml, plus any tracked Python module ruff reports a finding in (fix-only, no behaviour change)</files>
  <read_first>
    - `.planning/phases/05-container-build-ci-pipeline/05-RESEARCH.md` § Wave 0 Gaps — the recommended exclude set, and § Standard Stack (Supporting) for the ruff pin
    - `.claude/CLAUDE.md` § Conventions — the repo's naming, import-organization, and comment conventions any fix must respect
    - `api/main.py` — the largest module and the most likely source of findings; read before editing anything in it
    - `predict/live.py` — the second entry point in the API import chain
  </read_first>
  <action>
    Create `ruff.toml` at the repository root (not a `pyproject.toml` — this repo has no packaging
    metadata and adding one would change how the project is interpreted by tooling for no benefit).
    Set `target-version` to the highest value this ruff build accepts up to `py314` (fall back to
    `py313` and record the fallback in the SUMMARY if `py314` is rejected), `line-length = 100` to match
    the repo's observed ~90–100 character style, and an `exclude` list covering the generated and
    vendored trees: `data`, `models/artifacts`, `frontend`, `e2e`, `web`, `downloaded_files`,
    `.planning`, `.gsd`. Add a `[lint]` table whose `select` list is ruff's default rule set —
    `E4`, `E7`, `E9`, `F` — stated explicitly so the gate cannot silently change meaning when ruff's
    own default moves. Open the file with the short "what this does and why" comment block that every
    other config and script in this repo carries.

    Then run the linter and fix every finding at the source. Unused imports get deleted, undefined
    names get resolved (an `F821` is a real bug, not a style nit), comparison and except-clause findings
    get corrected. Two things are forbidden: adding any rule code to `ruff.toml`'s `ignore` list to make
    a finding disappear, and adding a suppression comment with no rule code and no reason. If a finding
    is a genuine deliberate pattern, suppress it narrowly with the specific rule code plus a trailing
    reason, and list every such suppression in the SUMMARY. If a fix would change runtime behaviour
    rather than remove dead or invalid code, stop and record it in the SUMMARY instead of guessing —
    the pytest suite from Task 2 is the arbiter that no fix changed behaviour.

    Finally, add `ruff` to nothing new — it is already pinned in `requirements-dev.in` from Task 2.
  </action>
  <verify>
    <automated>/home/sraja/miniconda3/envs/python314/bin/ruff check .</automated>
    <fails_when>non-zero exit, or the output contains a line matching "Found [0-9]+ error"</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import tomllib,sys; c=tomllib.load(open('ruff.toml','rb')); ig=c.get('lint',{}).get('ignore',[]) or c.get('ignore',[]); assert not ig, ig; sel=c.get('lint',{}).get('select',[]); assert sel, 'no select list'; print('RUFF CONFIG CLEAN, select=',sel)"</automated>
    <fails_when>non-zero exit (an AssertionError traceback showing a non-empty ignore list, or a missing select list), or "RUFF CONFIG CLEAN" is absent from stdout</fails_when>
    <automated>! git ls-files '*.py' | xargs grep -n -- 'noqa$' && echo "NO BARE SUPPRESSIONS"</automated>
    <fails_when>non-zero exit, or "NO BARE SUPPRESSIONS" is absent from stdout (a suppression comment was added with no rule code)</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m pytest -q</automated>
    <fails_when>non-zero exit, or the tail line reports "failed"/"error" instead of a bare "N passed" — a lint fix changed behaviour</fails_when>
  </verify>
  <acceptance_criteria>
    - `ruff.toml` exists at the repository root, contains a `select` list, and its `ignore` list is empty or absent
    - `ruff check .` exits 0 with no findings reported
    - `git ls-files '*.py'` produces no file containing a suppression comment that ends without a rule code
    - `python -m pytest -q` still reports the same passing test count as before the lint fixes
    - Every source fix is a deletion of dead code or a correction of an invalid construct; no fix alters a function's inputs, outputs, or control flow
  </acceptance_criteria>
  <done>`ruff check .` is green across the tracked Python tree with no rule silenced, and the test suite is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI → this repository | Third-party package artifacts cross into the dependency graph that CI and the container image will install |
| developer workstation → committed lockfile | A pin resolved once locally becomes the contract every later environment installs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01-SC | Tampering | `pip install` of `uv`, `ruff`, and the compiled lock set | high | mitigate | package-legitimacy gate + blocking human checkpoint (Task 1) for the two `[SUS]`/`[ASSUMED]` packages; pins re-verified against the live registry immediately before install |
| T-05-01-01 | Tampering | `requirements.txt` / `requirements-dev.txt` | high | mitigate | Every pin carries `--hash=sha256:`; every consumer (CI job, Docker builder, daily/weekly workflows) installs with `--require-hashes`, so a repointed or republished PyPI artifact is rejected at install time rather than executed |
| T-05-01-02 | Tampering | floating `understatapi` / `pulp` constraints | medium | mitigate | Both pinned exactly in `requirements.in`; a lock gate asserts the browser-automation transitive chain is absent, so a silent re-expansion of the dependency graph fails the build |
| T-05-01-03 | Elevation of Privilege | `uv` as a build-time tool | medium | mitigate | `uv` is installed into the dev conda environment only and deliberately excluded from both `.in` files, so it never enters CI's or the container's execution environment |
| T-05-01-04 | Information Disclosure | the scratch verification venv | low | accept | Created under `mktemp -d` outside the repository tree and removed in the same command; it holds no credentials and is never committed |
</threat_model>

<verification>
- Both `.in` sources and both compiled locks exist and are committed
- A from-scratch venv installs both locks with `--require-hashes` and runs the full pytest suite green
- Shared packages carry byte-identical pins across the two locks
- `ruff check .` exits 0 with an empty `ignore` list
- The SUMMARY records: the approved `uv`/`ruff` pins, any package that required a source compile (D-03 trigger for 05-02), and every narrow lint suppression added
</verification>

<success_criteria>
SEC-02's "dependencies pinned/locked, verified installable in a fresh environment with cp314 wheels"
is satisfied by demonstration, not assertion: the exact `pip install --require-hashes` command the
Dockerfile and every CI job will run has been executed against these files in a clean 3.14 environment,
and the application's own test suite passed inside it. CI-01's lint tooling exists and is green.
</success_criteria>

<output>
Create `.planning/phases/05-container-build-ci-pipeline/05-01-SUMMARY.md` when done
</output>
