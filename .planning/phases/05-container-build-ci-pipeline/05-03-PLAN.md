---
phase: 05-container-build-ci-pipeline
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements: [CI-01, CI-02, CI-04, CI-05]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A single commit produces exactly one full ci.yml verification run: push covers branch commits, pull_request is narrowed to the events a push does not already cover, and a concurrency group with cancel-in-progress cancels a superseded in-flight run for the same ref or pull request"
    - "The test job refuses to run pytest against a missing or empty built frontend bundle: after downloading the artifact it asserts index.html and 404.html both exist and exits with a named message if either is absent"
    - "ci.yml's jobs form one linear needs chain (lint-build to test to e2e to image to publish), so the job producing the built bundle always completes before any job consuming it, regardless of runner scheduling"
    - "Every uses: line in ci.yml is pinned to a 40-character commit SHA, and every SHA was re-resolved from the GitHub API during execution rather than copied from a document"
    - "The publish job is the only job granted packages: write, and it never runs off the default branch"
    - "Trivy runs on every build and never fails it: no exit-code is set, the SARIF is uploaded unconditionally as a build artifact, and the Security-tab upload is allowed to error without reddening the job"
    - statement: "A ci.yml run that is cancelled or fails at any stage never pushes an image to GHCR, because the publish job requires the full needs chain and is gated on the default branch"
      verification: backstop
  prohibitions:
    - statement: "MUST NOT report a green ci.yml build while verifying nothing — no step may swallow a non-zero exit or invoke a test selector that matches zero tests; the sole permitted continue-on-error is the Security-tab SARIF upload, which is a report sink and not a verification step"
      category: transparency
      status: resolved
      verification: test
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "The chained lint/typecheck, backend+frontend test, Playwright E2E, image build+smoke+scan, and GHCR publish job graph"
      contains: "concurrency"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "e2e/playwright.config.ts"
      via: "the e2e job invokes the existing npm test script and lets the config own build + uvicorn boot; CI reimplements none of it"
      pattern: "npm --prefix e2e"
    - from: ".github/workflows/ci.yml"
      to: "scripts/smoke_test.sh"
      via: "the image job runs the smoke script against the locally-loaded image tag before any push"
      pattern: "smoke_test\\.sh"
    - from: ".github/workflows/ci.yml"
      to: "requirements.txt"
      via: "backend jobs install the hashed lock with --require-hashes"
      pattern: "--require-hashes"
---

<objective>
Write the single chained CI workflow that verifies every commit and produces the scanned, publishable
image: lint and typecheck, then backend and frontend tests against the built bundle, then the existing
Playwright suite, then the container build with its real-solve smoke test and Trivy scan, then a
main-only GHCR publish with a stubbed deploy step.

Purpose: CI-01, CI-02, CI-04 and CI-05 all land in this one file, and D-08 fixes its shape. The
load-bearing detail carried forward from Phase 4's review is that the backend test suite cannot run
before the frontend has been built — the SPA-fallback test serves a 500 against a missing bundle
directory — so the built bundle crosses jobs as an artifact rather than being rebuilt or assumed.

Output: `.github/workflows/ci.yml`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-container-build-ci-pipeline/05-CONTEXT.md
@.planning/phases/05-container-build-ci-pipeline/05-RESEARCH.md
@.planning/phases/05-container-build-ci-pipeline/05-PATTERNS.md
@.planning/phases/05-container-build-ci-pipeline/05-01-SUMMARY.md
@.planning/phases/05-container-build-ci-pipeline/05-02-SUMMARY.md
</context>

<!-- planner-discipline-allow: \|\| true -->

## Artifacts this phase produces

See `05-01-PLAN.md` § "Artifacts this phase produces" for the full phase-wide table. This plan
contributes `.github/workflows/ci.yml` and the five new job names it defines: `lint-build`, `test`,
`e2e`, `image`, `publish`.

## Flagged assumptions (unresolved probe edges)

The spec-less edge probe classified this plan's CI-02, CI-04 and CI-05 rows as `unclassified`; all
three stay **unresolved** and are surfaced here rather than silently dropped:

- **CI-02 / unclassified** — "review manually". Assumed reading: "cached browsers" means the Chromium
  download is restored from an `actions/cache` entry keyed on the E2E lockfile hash; a cache miss is a
  slow run, not a failure. The suite run is the existing full three-variant run, not a reduced subset.
- **CI-04 / unclassified** — "review manually". Assumed reading: "deploy step stubbed" means a named,
  no-op step in the publish job that records where a real deployment will attach next milestone; it must
  not attempt, simulate, or partially perform a deployment.
- **CI-05 / unclassified** — "review manually". Assumed reading: "reports on the image on every build"
  is satisfied by the SARIF being produced and retrievable for every run — via the build artifact
  unconditionally, and via the Security tab when the account's plan permits it. The plan deliberately
  does not depend on which of GitHub's two documented private-repo behaviours is real.

## Resolving action SHAs (applies to every task in this plan)

Every `uses:` line must carry a 40-character commit SHA with the human-readable version as a trailing
comment. `05-RESEARCH.md` § Standard Stack (Supporting) already resolved six of them, but SHAs age and
a tag can be repointed — which is the exact attack SHA-pinning defends against — so **re-resolve every
one during execution**, including the six already published, with:

```
/home/sraja/miniconda3/envs/python314/bin/python -c "import json,urllib.request as u,sys; d=json.load(u.urlopen('https://api.github.com/repos/'+sys.argv[1]+'/git/ref/tags/'+sys.argv[2])); o=d['object']; print(o['sha'] if o['type']=='commit' else json.load(u.urlopen(o['url']))['object']['sha'])" actions/checkout v7.0.1
```

If a re-resolved SHA differs from the one `05-RESEARCH.md` published for the same tag, use the
re-resolved value and record the delta in the SUMMARY — a moved tag is a finding, not a nuisance.

Actions and versions this plan needs: `actions/checkout` v7.0.1, `actions/setup-python` v7.0.0,
`actions/setup-node` v7.0.0, `actions/cache` v6.1.0, `actions/upload-artifact` v7.0.1,
`actions/download-artifact` v8.0.1, `docker/setup-buildx-action` v4.3.0, `docker/login-action` v4.6.0,
`docker/metadata-action` v6.2.0, `docker/build-push-action` v7.3.0, `aquasecurity/trivy-action` v0.36.0,
`github/codeql-action/upload-sarif` v4.37.9.

<tasks>

<task type="auto">
  <name>ci.yml skeleton plus the lint/typecheck and backend+frontend test jobs</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - `.github/workflows/daily.yml` — the repo's established job/step shape: `runs-on: ubuntu-latest`, scoped `permissions:`, `env: PYTHONPATH: .` on `python -m` steps
    - `.planning/phases/05-container-build-ci-pipeline/05-PATTERNS.md` § `.github/workflows/ci.yml` — the reusable conventions, the pytest invocation, and why the frontend build (not the typecheck script) is the real gate
    - `.planning/phases/05-container-build-ci-pipeline/05-RESEARCH.md` § Architecture Patterns (job graph), § Common Pitfalls 5
    - `frontend/package.json` — the `build`, `postbuild` and `test` scripts, and the deliberately-inert `typecheck` script
    - `pytest.ini` — the plugin disables the CI pytest step inherits automatically from the repo root
    - `tests/test_fixture_mode.py` — the SPA-fallback test that 500s when the built bundle is absent
    - `ruff.toml` — the lint configuration created in plan 05-01
  </read_first>
  <action>
    Create `.github/workflows/ci.yml` opening with a comment block stating what the workflow verifies
    and why the jobs are chained rather than parallel, matching the header-comment convention every
    other file in this repo uses.

    Workflow-level configuration:
    - `name: ci`.
    - Triggers: `push:` with no branch filter, plus `pull_request:` narrowed with
      `types: [opened, reopened, ready_for_review]`. A push to a branch that already has an open pull
      request otherwise fires both events for the same commit and burns two full runs out of the
      2000-minute monthly budget; narrowing the pull-request types to the ones a push does not already
      cover gives exactly one verification run per commit while still verifying a freshly-opened pull
      request that has no new push behind it. Put that reasoning in a comment above the trigger block.
    - Workflow-level `permissions: contents: read` — the least-privilege default. Individual jobs widen
      it only where they must.
    - A `concurrency:` block with `group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}`
      and `cancel-in-progress: true`, so a superseded push or pull-request update cancels its in-flight
      predecessor instead of racing it.

    Job `lint-build` (`runs-on: ubuntu-latest`):
    checkout; `actions/setup-python` with `python-version: "3.14"` and `cache: pip`; install both locks
    with `pip install --require-hashes -r requirements.txt -r requirements-dev.txt` (D-04 makes hash
    enforcement the install form in every job, and D-02 makes CI the consumer of both locks); run
    `ruff check .`; `actions/setup-node` with `node-version: "24"`, `cache: npm` and a
    `cache-dependency-path` listing both `frontend/package-lock.json` and `e2e/package-lock.json`; run
    `npm --prefix frontend ci`; run `npm --prefix frontend run build`. That build command is the real
    typecheck — it runs `tsc -b`, which expands the project references; the repository's `typecheck`
    script runs `tsc --noEmit` with no `-b` against a root config whose `files` array is empty, so it
    type-checks nothing and exits 0 regardless of real errors. Never use it as a gate; add a comment
    saying so. Finish the job by uploading `frontend/dist` with `actions/upload-artifact` under the name
    `frontend-dist`.

    Job `test` (`needs: lint-build`): checkout; setup-python 3.14 with pip cache; install both hashed
    locks; `actions/download-artifact` for `frontend-dist` into `frontend/dist`; then a named guard step
    that asserts `frontend/dist/index.html` and `frontend/dist/404.html` both exist and exits 1 with a
    message naming the missing file otherwise — without a built bundle the fixture-mode SPA-fallback
    test serves a 500 and the failure output points nowhere useful. Then run `pytest` from the
    repository root (bare, so it picks up `pytest.ini`'s plugin disables and `testpaths` automatically —
    add no extra flags). Then `actions/setup-node`, `npm --prefix frontend ci`, and
    `npm --prefix frontend test` for the Vitest suite.

    No step in either job may swallow a non-zero exit with a shell short-circuit, and no step may carry
    `continue-on-error`. A verification job that cannot go red verifies nothing.
  </action>
  <verify>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import yaml; d=yaml.safe_load(open('.github/workflows/ci.yml')); t=d.get('on', d.get(True)); assert 'push' in t and 'pull_request' in t, t; assert d['concurrency']['cancel-in-progress'] is True; assert d['permissions']['contents']=='read'; j=d['jobs']; assert 'lint-build' in j and 'test' in j, sorted(j); assert 'lint-build' in str(j['test']['needs']); print('CI SKELETON OK: triggers, concurrency, least-privilege default, needs chain')"</automated>
    <fails_when>non-zero exit (an AssertionError traceback, or a YAML parse error), or "CI SKELETON OK" is absent from stdout</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import yaml; s=yaml.safe_load(open('.github/workflows/ci.yml'))['jobs']['lint-build']['steps']; body=' '.join(str(x.get('run',''))+str(x.get('uses',''))+str(x.get('with','')) for x in s); assert 'ruff check' in body; assert 'npm --prefix frontend run build' in body; assert 'run typecheck' not in body and 'run:typecheck' not in body; assert 'frontend-dist' in body; print('LINT-BUILD JOB OK: ruff, real typecheck via build, dist artifact uploaded')"</automated>
    <fails_when>non-zero exit (an AssertionError traceback), or "LINT-BUILD JOB OK" is absent from stdout — most likely the inert typecheck script was used as the gate, or the artifact upload is missing</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import yaml; s=yaml.safe_load(open('.github/workflows/ci.yml'))['jobs']['test']['steps']; body=' '.join(str(x.get('run',''))+str(x.get('uses',''))+str(x.get('with','')) for x in s); assert 'download-artifact' in body; assert 'frontend/dist/index.html' in body and 'frontend/dist/404.html' in body, 'no bundle guard'; assert '--require-hashes' in body; assert 'pytest' in body; assert 'npm --prefix frontend test' in body; print('TEST JOB OK: artifact downloaded, bundle guarded, hashed install, pytest + vitest')"</automated>
    <fails_when>non-zero exit (an AssertionError traceback naming which assertion failed), or "TEST JOB OK" is absent from stdout</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import yaml; d=yaml.safe_load(open('.github/workflows/ci.yml')); bad=[(jn,st.get('name') or st.get('uses')) for jn,j in d['jobs'].items() for st in j.get('steps',[]) if st.get('continue-on-error') is True and 'upload-sarif' not in str(st.get('uses',''))]; assert not bad, bad; print('NO VERIFICATION STEP MAY CONTINUE-ON-ERROR')"</automated>
    <fails_when>non-zero exit (an AssertionError listing the offending job and step), or "NO VERIFICATION STEP MAY CONTINUE-ON-ERROR" is absent from stdout</fails_when>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` parses as YAML and defines the jobs `lint-build` and `test`
    - The trigger block contains both `push` and `pull_request`, with `pull_request` carrying a `types` list
    - A workflow-level `concurrency` block exists with `cancel-in-progress: true`
    - Workflow-level `permissions` grants `contents: read` and nothing else
    - `lint-build` runs `ruff check .`, runs `npm --prefix frontend run build`, and uploads `frontend/dist` as the artifact `frontend-dist`
    - No step anywhere in the file invokes the frontend `typecheck` script
    - `test` declares `needs: lint-build`, downloads `frontend-dist`, asserts both `frontend/dist/index.html` and `frontend/dist/404.html` exist before running tests, installs with `--require-hashes`, then runs `pytest` and `npm --prefix frontend test`
    - No step in either job carries `continue-on-error`
  </acceptance_criteria>
  <done>The workflow skeleton and CI-01's lint/typecheck/test chain exist, with the built-bundle handoff that Phase 4's review flagged as the blocker.</done>
</task>

<task type="auto">
  <name>Playwright E2E job against fixture-fed uvicorn</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - `e2e/playwright.config.ts` — read in full; it owns the entire server lifecycle (frontend build chained ahead of three fixture-fed uvicorn instances) and CI must reimplement none of it
    - `e2e/package.json` — the exact `test` and `install:browser` scripts to invoke
    - `.planning/phases/04-e2e-regression-suite/04-CONTEXT.md` — D-01 to D-05: why local and CI runs boot the identical topology
    - `.github/workflows/ci.yml` — the current state of the file after the previous task
  </read_first>
  <action>
    Add job `e2e` with `needs: test`.

    Checkout; `actions/setup-python` with `python-version: "3.14"` and `cache: pip`; install the runtime
    lock with `pip install --require-hashes -r requirements.txt`. The Playwright config resolves its
    interpreter from the `E2E_PYTHON` environment variable and falls back to plain `python` on PATH;
    CI deliberately leaves `E2E_PYTHON` unset so the interpreter that `setup-python` put on PATH is the
    one that boots uvicorn. Add a comment recording that, because it is the one CI-only branch in an
    otherwise identical local/CI topology.

    `actions/setup-node` with `node-version: "24"`, `cache: npm`, and the same two-entry
    `cache-dependency-path`. Then `npm --prefix frontend ci` and `npm --prefix e2e ci` — the config's
    `webServer` runs `npm --prefix frontend run build` itself, so the frontend's dependencies must be
    present in this job even though the built bundle is not downloaded here. Do not download the
    `frontend-dist` artifact into this job: the config rebuilds the bundle as the first link of its
    `webServer` command chain precisely so a stale or missing bundle cannot be served, and dropping a
    downloaded copy on top of that would be dead weight at best and a stale-file hazard at worst.
    Comment that choice so nobody "fixes" it later.

    Restore the browser download from cache with `actions/cache` keyed on the runner OS plus the hash
    of `e2e/package-lock.json`, with `~/.cache/ms-playwright` as the path, then run
    `npm --prefix e2e run install:browser` — the script already carries the `--with-deps chromium`
    arguments, so pass none. A cache miss is a slower run, never a failure.

    Run the suite with `npm --prefix e2e run test`. Pass no variant or project filters: the full
    three-server run (normal, blank, double gameweek) is what the suite means by default, and narrowing
    it in CI would leave the two variant fixture sets unexercised on every push. GitHub Actions sets
    `CI=true` automatically, which the config reads to disable server reuse and enable one retry.

    Finally, upload the Playwright HTML report with `actions/upload-artifact` under `if: always()` so a
    failed run leaves diagnosable output. The report upload is the only step in this job permitted to
    run on failure, and it still must not carry `continue-on-error`.
  </action>
  <verify>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import yaml; j=yaml.safe_load(open('.github/workflows/ci.yml'))['jobs']['e2e']; assert 'test' in str(j['needs']); s=j['steps']; body=' '.join(str(x.get('run',''))+str(x.get('uses',''))+str(x.get('with','')) for x in s); assert 'npm --prefix e2e run install:browser' in body; assert 'npm --prefix e2e run test' in body; assert 'npm --prefix frontend ci' in body and 'npm --prefix e2e ci' in body; assert 'ms-playwright' in body, 'no browser cache'; assert '--require-hashes' in body; print('E2E JOB OK: needs test, both installs, browser cache, config-owned run')"</automated>
    <fails_when>non-zero exit (an AssertionError traceback naming the failed assertion), or "E2E JOB OK" is absent from stdout</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import yaml; j=yaml.safe_load(open('.github/workflows/ci.yml'))['jobs']['e2e']; body=' '.join(str(x.get('run',''))+str(x.get('env','')) for x in j['steps'])+str(j.get('env','')); assert 'E2E_PYTHON' not in body, 'E2E_PYTHON must stay unset in CI'; assert 'E2E_VARIANTS' not in body, 'the full three-server run must not be narrowed in CI'; assert 'uvicorn' not in body, 'the playwright config owns server boot; CI must not reimplement it'; print('E2E JOB DELEGATES SERVER LIFECYCLE TO THE CONFIG')"</automated>
    <fails_when>non-zero exit (an AssertionError naming which forbidden variable or command appeared), or "E2E JOB DELEGATES SERVER LIFECYCLE TO THE CONFIG" is absent from stdout</fails_when>
    <automated>cd e2e && npx tsc --noEmit -p tsconfig.json && echo "E2E TYPES STILL OK"</automated>
    <fails_when>non-zero exit, a TypeScript diagnostic line matching "error TS", or "E2E TYPES STILL OK" absent from stdout — this proves the suite this job invokes is still intact and was not disturbed</fails_when>
  </verify>
  <acceptance_criteria>
    - `ci.yml` defines job `e2e` with `needs: test`
    - The job installs the runtime lock with `--require-hashes`, runs `npm --prefix frontend ci` and `npm --prefix e2e ci`
    - The job caches `~/.cache/ms-playwright` keyed on the E2E lockfile hash and runs `npm --prefix e2e run install:browser`
    - The job runs `npm --prefix e2e run test` with no project or variant filter appended
    - Neither `E2E_PYTHON` nor `E2E_VARIANTS` is set anywhere in the job
    - No step in the job starts uvicorn or builds the frontend directly — the Playwright config owns both
    - The Playwright HTML report is uploaded with `if: always()`
    - `npx tsc --noEmit -p tsconfig.json` run from inside `e2e/` still exits 0
  </acceptance_criteria>
  <done>CI-02's job exists and consumes Phase 4's suite verbatim, with cached browsers and no reimplementation of the server topology.</done>
</task>

<task type="auto">
  <name>Image build with smoke test and Trivy scan, then the main-only GHCR publish</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - `.planning/phases/05-container-build-ci-pipeline/05-RESEARCH.md` § Code Examples (GHCR publish block and the Trivy report-only block, with pre-resolved SHAs), § Common Pitfalls 4, § Security Domain
    - `Dockerfile` and `.dockerignore` — created in plan 05-02; the build this job runs
    - `scripts/smoke_test.sh` — created in plan 05-02; its argument contract and its `SMOKE_DOCKER`/`SMOKE_PORT` seams
    - `.planning/phases/05-container-build-ci-pipeline/05-CONTEXT.md` — D-09 (publish policy), D-10 (Trivy report-only), D-13 (private repo, 500MB GHCR budget)
    - `.github/workflows/ci.yml` — the current state of the file after the previous two tasks
  </read_first>
  <action>
    Add job `image` with `needs: e2e` and a job-scoped
    `permissions: contents: read, security-events: write`. It must NOT be granted `packages: write` —
    this job never pushes.

    Steps: checkout; `docker/setup-buildx-action`; `actions/download-artifact` for `frontend-dist` into
    `frontend/dist` (D-06 requires the built bundle inside the image, and `.dockerignore` deliberately
    keeps that directory in the build context); then `docker/build-push-action` with `context: .`,
    `push: false`, `load: true`, `tags: local/fpl:ci`, `cache-from: type=gha` and
    `cache-to: type=gha,mode=max`. Loading rather than pushing is what makes the image available to the
    smoke test and the scanner before anything reaches a registry.

    Then run `bash scripts/smoke_test.sh local/fpl:ci`. This is the CI-03 gate: it boots the loaded
    image against the frozen fixture set and asserts a real solve completes through the solver binary
    inside the container. It must run before the scanner and before any publish, and its failure must
    fail the job.

    Then `aquasecurity/trivy-action` against `local/fpl:ci` with `format: sarif` and
    `output: trivy-results.sarif`. Set no `exit-code` input at all — the action's default of 0 is what
    makes the scan report-only, and adding a non-zero value would turn a report into a gate this
    milestone deliberately does not want. Then, in this order: `actions/upload-artifact` with
    `if: always()` publishing `trivy-results.sarif` as a build artifact, and only then
    `github/codeql-action/upload-sarif` with `if: always()` and `continue-on-error: true`. The ordering
    and the two sinks are deliberate — GitHub's own documentation is internally inconsistent about
    whether a non-CodeQL SARIF upload succeeds for a private repository on this plan, so the artifact is
    the sink that always works and the Security-tab upload is best-effort. Put that reasoning in a
    comment. This is the only step in the entire workflow allowed to carry `continue-on-error`, because
    it is a report sink and not a verification step.

    Add job `publish` with `needs: image`, `if: github.ref == 'refs/heads/main'`, and job-scoped
    `permissions: contents: read, packages: write`. Steps: checkout; `docker/setup-buildx-action`;
    `actions/download-artifact` for `frontend-dist` into `frontend/dist`; `docker/login-action` against
    `ghcr.io` using `${{ github.actor }}` and `${{ secrets.GITHUB_TOKEN }}` — the workflow's own scoped
    token, never a personal access token; `docker/metadata-action` with
    `images: ghcr.io/${{ github.repository }}` and a `tags` list of `type=sha` plus
    `type=raw,value=latest,enable={{is_default_branch}}`; then `docker/build-push-action` with
    `context: .`, `push: true`, the metadata action's `tags` and `labels` outputs, and
    `cache-from: type=gha` so the layers the image job already built are reused rather than rebuilt.

    Close the publish job with a step named for what it is — a deploy placeholder — that echoes the
    published image reference and states that live deployment lands next milestone alongside hosting.
    It must perform no deployment, contact no host, and hold no credentials. CI-04 asks for the deploy
    step to be stubbed, and a stub that half-deploys is worse than none.

    Every `uses:` line added in this task must be SHA-pinned per the resolution procedure at the top of
    this plan.
  </action>
  <verify>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import yaml; d=yaml.safe_load(open('.github/workflows/ci.yml')); j=d['jobs']; assert 'e2e' in str(j['image']['needs']); assert 'image' in str(j['publish']['needs']); assert j['image']['permissions'].get('packages') is None, 'image job must not get packages: write'; assert j['image']['permissions'].get('security-events')=='write'; assert j['publish']['permissions'].get('packages')=='write'; assert 'refs/heads/main' in str(j['publish'].get('if','')), j['publish'].get('if'); print('IMAGE/PUBLISH SCOPING OK')"</automated>
    <fails_when>non-zero exit (an AssertionError traceback naming the failed scoping assertion), or "IMAGE/PUBLISH SCOPING OK" is absent from stdout</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import yaml; s=yaml.safe_load(open('.github/workflows/ci.yml'))['jobs']['image']['steps']; t=[x for x in s if 'trivy-action' in str(x.get('uses',''))]; assert len(t)==1, len(t); assert 'exit-code' not in (t[0].get('with') or {}), t[0].get('with'); a=[i for i,x in enumerate(s) if 'upload-artifact' in str(x.get('uses','')) and 'sarif' in str(x.get('with',''))]; u=[i for i,x in enumerate(s) if 'upload-sarif' in str(x.get('uses',''))]; assert a and u and a[0]<u[0], (a,u); assert s[u[0]].get('continue-on-error') is True; sm=[x for x in s if 'smoke_test.sh' in str(x.get('run',''))]; assert sm, 'no smoke test step'; assert s.index(sm[0])<s.index(t[0]), 'smoke test must precede the scan'; print('TRIVY REPORT-ONLY + ARTIFACT-FIRST + SMOKE BEFORE SCAN OK')"</automated>
    <fails_when>non-zero exit (an AssertionError traceback), or "TRIVY REPORT-ONLY + ARTIFACT-FIRST + SMOKE BEFORE SCAN OK" is absent from stdout</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import yaml; s=yaml.safe_load(open('.github/workflows/ci.yml'))['jobs']['image']['steps']; b=[x for x in s if 'build-push-action' in str(x.get('uses',''))]; assert len(b)==1; w=b[0]['with']; assert w.get('push') in (False,'false'), w.get('push'); assert w.get('load') in (True,'true'); print('IMAGE JOB BUILDS AND LOADS BUT NEVER PUSHES')"</automated>
    <fails_when>non-zero exit (an AssertionError showing the push/load inputs), or "IMAGE JOB BUILDS AND LOADS BUT NEVER PUSHES" is absent from stdout — a PR or branch build would publish to GHCR, violating D-09</fails_when>
    <automated>T=$(grep -cE '^[[:space:]]*-?[[:space:]]*uses:' .github/workflows/ci.yml); P=$(grep -cE '^[[:space:]]*-?[[:space:]]*uses:[[:space:]]*[^[:space:]]+@[0-9a-f]{40}([[:space:]]|$)' .github/workflows/ci.yml); echo "uses=$T sha_pinned=$P"; test "$T" -gt 0 && test "$T" = "$P"</automated>
    <fails_when>non-zero exit, or the echoed `uses=` and `sha_pinned=` counts differ — at least one action is pinned to a mutable tag instead of a commit SHA</fails_when>
    <automated>! grep -nE '\|\| true' .github/workflows/ci.yml && echo "NO FAILURE-SWALLOWING SHORT-CIRCUIT IN CI"</automated>
    <fails_when>non-zero exit, or "NO FAILURE-SWALLOWING SHORT-CIRCUIT IN CI" is absent from stdout — a run step short-circuits its own failure and the job would go green without verifying</fails_when>
  </verify>
  <acceptance_criteria>
    - `ci.yml` defines jobs `image` (`needs: e2e`) and `publish` (`needs: image`)
    - `image` has `security-events: write` and no `packages` permission; `publish` has `packages: write`
    - `publish` carries an `if:` condition naming `refs/heads/main`
    - The `image` job's `build-push-action` step has `push: false` and `load: true`
    - `bash scripts/smoke_test.sh local/fpl:ci` runs in `image`, positioned before the Trivy step
    - The Trivy step passes no `exit-code` input
    - The SARIF artifact upload appears before the Security-tab upload; only the Security-tab upload carries `continue-on-error: true`
    - `publish` logs in to `ghcr.io` with `secrets.GITHUB_TOKEN`, derives tags from `metadata-action` with `type=sha` and a default-branch-gated `latest`, and pushes
    - `publish` ends with a named no-op deploy placeholder step that performs no deployment
    - Every `uses:` line in the file is pinned to a 40-character SHA with a trailing version comment
    - No line in the file swallows a non-zero exit with a shell short-circuit
  </acceptance_criteria>
  <done>CI-03's smoke gate, CI-05's report-only scan and CI-04's scoped-token GHCR publish are wired into the chain, with the publish path unreachable from a branch or pull-request build.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream GitHub Action → this workflow's runner | Third-party code executes with the workflow's token and full access to the checkout |
| workflow run → GHCR | A run can publish an artifact that later becomes a deployment input |
| pull request from any ref → CI runner | Untrusted branch content is built and executed |
| Trivy findings → GitHub Security tab | Vulnerability data crosses into a surface whose availability depends on the account plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03-01 | Tampering | every `uses:` line | high | mitigate | All actions pinned to 40-character commit SHAs, re-resolved from the GitHub API at execution time; a counting gate fails the plan if any `uses:` line carries a mutable tag |
| T-05-03-02 | Elevation of Privilege | `GITHUB_TOKEN` scope | high | mitigate | Workflow default is `contents: read`; `packages: write` exists only on `publish`, `security-events: write` only on `image`; asserted by a parse gate on both jobs |
| T-05-03-03 | Tampering | GHCR `latest` tag | high | mitigate | `publish` requires the full `needs` chain and is gated on the default branch; the `image` job builds with `push: false` so no branch or pull-request run can reach the registry |
| T-05-03-04 | Spoofing | registry authentication | medium | mitigate | `docker/login-action` uses the run's own scoped `GITHUB_TOKEN`, never a long-lived personal access token stored as a secret |
| T-05-03-05 | Repudiation | a green build that verified nothing | high | mitigate | No step may carry `continue-on-error` except the Security-tab SARIF upload, and no run step may short-circuit its own failure; both asserted by gates. The smoke test is positioned before the scan so a broken image cannot be scanned-and-forgotten |
| T-05-03-06 | Information Disclosure | Trivy SARIF | low | accept | Vulnerability findings for a private image are uploaded to a private repository's own artifact storage and Security tab; no third-party sink is involved |
| T-05-03-07 | Denial of Service | duplicate runs exhausting the Actions minute budget | low | mitigate | Narrowed `pull_request` trigger types plus a `cancel-in-progress` concurrency group keyed on the pull-request number or ref |
</threat_model>

<verification>
- `ci.yml` parses and defines exactly the five chained jobs in one linear `needs` order
- Trigger, concurrency, and least-privilege permission defaults all assert green
- The built-bundle artifact crosses from `lint-build` into `test` and `image`, with an existence guard before pytest
- The E2E job delegates the entire server lifecycle to the existing Playwright config
- The image job builds-and-loads, smoke-tests, then scans; the publish job is the only path to GHCR and only from the default branch
- Every action is SHA-pinned; nothing but the SARIF Security-tab upload may continue on error
</verification>

<success_criteria>
CI-01, CI-02, CI-04 and CI-05 exist as one reviewable workflow whose every structural claim is asserted
by a gate that can run without a GitHub runner. The claims that only a real run can settle — that the
chain goes green end to end and that an image appears in GHCR — are carried to the D-14 human checkpoint
in plan 05-05, where they are the phase's authoritative gate.
</success_criteria>

<output>
Create `.planning/phases/05-container-build-ci-pipeline/05-03-SUMMARY.md` when done
</output>
