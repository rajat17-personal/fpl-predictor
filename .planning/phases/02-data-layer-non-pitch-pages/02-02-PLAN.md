---
phase: 02-data-layer-non-pitch-pages
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - .planning/phases/02-data-layer-non-pitch-pages/PARITY-DEVIATIONS.md
autonomous: false
requirements: [UI-02, UI-05]

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`frontend/package.json` lists `react-markdown@10.1.0`, `@fontsource/archivo@5.3.0`, `@fontsource/ibm-plex-sans@5.3.0` and `@fontsource/ibm-plex-mono@5.3.0` at exact versions with no caret or tilde range."
    - "No package is installed before a human has answered the package-legitimacy checkpoint in this plan — the checkpoint is blocking and is not auto-approvable."
    - "`.planning/phases/02-data-layer-non-pitch-pages/PARITY-DEVIATIONS.md` exists and carries the eight deviations the UI-SPEC already identified, each as a one-line entry naming the deviation and its reason (D-04)."
    - "The ledger states the rule that any further intentional difference from the vanilla site discovered during execution is appended as a new numbered line before that difference is committed."
    - "`npm --prefix frontend run test`, `npm --prefix frontend run typecheck` and `bash scripts/verify_frontend_build.sh` still pass after the installs — the four new packages do not disturb the Phase 1 toolchain."
  artifacts:
    - .planning/phases/02-data-layer-non-pitch-pages/PARITY-DEVIATIONS.md
    - frontend/package.json
    - frontend/package-lock.json
  key_links:
    - "package-legitimacy checkpoint → the single install of all four packages (one install, so no later plan re-edits package.json and no wave-2 file collision)"
    - "`react-markdown` → plan 02-06's Methodology renderer"
    - "`@fontsource/*` → plan 02-03's `index.css` font imports replacing the Phase 1 CDN stylesheet link"
    - "PARITY-DEVIATIONS.md → Phase 7 (CUT-01) side-by-side comparison, which treats this file as the complete list of explained deltas"
  prohibitions:
    - "The vanilla site stays live and authoritative until CUT-01: nothing in this phase edits, moves, or regenerates any file under `web/` or `web/data/`."
    - "The React build never bundles pipeline data: `web/data/*.json` is fetched at runtime, and no export JSON is copied into `frontend/public/` or `frontend/src/`."
---

<objective>
Clear the package-legitimacy gate for this phase's four new npm dependencies, install them once,
and create the parity deviation ledger that Phase 7's cutover comparison depends on.

Purpose: three of this phase's plans need packages that are not on Phase 1's human-approved list.
This project's established discipline (STATE.md, Phase 01) is that a new dependency set goes
through a blocking human approval before any install. Doing all four installs in one plan also
keeps `frontend/package.json` owned by exactly one plan, so the four wave-2 plans can run in
parallel without colliding on it. The ledger (D-04) is costly to reverse: entries skipped mid-phase
cannot be reconstructed later, and Phase 7 reads this file as the definitive list of intended
differences.

Output: four exact-pinned packages in `frontend/package.json`, and
`.planning/phases/02-data-layer-non-pitch-pages/PARITY-DEVIATIONS.md` seeded with eight entries.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-data-layer-non-pitch-pages/02-CONTEXT.md
@.planning/phases/02-data-layer-non-pitch-pages/02-RESEARCH.md
@.planning/phases/02-data-layer-non-pitch-pages/02-UI-SPEC.md
</context>

## Artifacts this phase produces

See the "Artifacts this phase produces" table in `02-01-PLAN.md` — it is the phase-wide list. This
plan contributes `PARITY-DEVIATIONS.md` and the four new package entries in
`frontend/package.json`.

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package-legitimacy approval for this phase's four new npm dependencies</name>
  <files>none — approval gate only</files>
  <what-built>
Nothing is built by this task. It is the blocking approval gate that stands in front of every
install in this phase. Four packages are proposed: `react-markdown@10.1.0` (renders the methodology
page's markdown without a raw-HTML sink, D-13) and `@fontsource/archivo@5.3.0`,
`@fontsource/ibm-plex-sans@5.3.0`, `@fontsource/ibm-plex-mono@5.3.0` (self-hosted fonts replacing
Phase 1's CDN stylesheet, D-12). All four returned an OK legitimacy verdict and were cross-checked
against the live npm registry on 2026-09-01; `react-markdown` has no postinstall script. They are
routed through this gate because research discovered them rather than Phase 1 pre-approving them.
  </what-built>
  <how-to-verify>
Spot-check any package you want to at `npmjs.com/package/<name>` — confirm the publish date,
weekly download count and linked source repository match the figures quoted in the action below,
and that the repository is the one you would expect for that package name. Then answer with an
explicit approve or reject. Rejecting `react-markdown` blocks plan 02-06's Methodology page;
rejecting the font packages blocks plan 02-03's font task and forces D-12 back to manually hosted
woff2 files.
  </how-to-verify>
  <resume-signal>
The user replies with an explicit approval or rejection for the four-package set. Record the answer
verbatim in the SUMMARY, then continue to Task 2 on approval, or halt the plan and report the
blocked downstream plan on rejection. This gate is never auto-approvable — `workflow.auto_advance`
does not apply to a package-legitimacy checkpoint.
  </resume-signal>
  <on-rejection>
Halt the plan without installing anything. Report which downstream plan each rejected package
blocks and what the fallback would cost: `react-markdown` has no in-repo substitute that avoids a
raw-HTML sink, and dropping the font packages means hand-subsetting and hand-licensing three woff2
families.
  </on-rejection>
  <read_first>
    - `.planning/phases/02-data-layer-non-pitch-pages/02-RESEARCH.md` — the `## Package Legitimacy Audit` table (all four verdicts OK, with publish dates, weekly download counts and source repos) and the note that a clean automated verdict still routes through the same human gate Phase 1 used.
    - `.planning/STATE.md` — the Phase 01 precedent line recording the human-approved 13-package install set, which is the discipline being followed here.
    - `frontend/package.json` — the current dependency set, so the reviewer can see exactly what is being added.
  </read_first>
  <action>
STOP. Do not run any install command until a human has answered.

Present the audit results and ask for an explicit approve or reject:

- `react-markdown@10.1.0` — npm, published 2025-03-07, ~33.7M weekly downloads,
  github.com/remarkjs/react-markdown, MIT, no postinstall script, verdict OK. Purpose: render the
  methodology page's markdown (D-13) without a raw-HTML injection sink.
- `@fontsource/archivo@5.3.0` — npm, published 2026-07-19, ~115.9k weekly downloads,
  github.com/fontsource/font-files, verdict OK. Purpose: self-hosted Archivo 700 (D-12).
- `@fontsource/ibm-plex-sans@5.3.0` — npm, published 2026-07-19, ~540.9k weekly downloads, same
  repo, verdict OK. Purpose: self-hosted IBM Plex Sans 400 (D-12).
- `@fontsource/ibm-plex-mono@5.3.0` — npm, published 2026-07-19, ~1.31M weekly downloads, same
  repo, verdict OK. Purpose: self-hosted IBM Plex Mono 400 (D-12).

State plainly that no package here carries a slop or suspicious verdict, that all four were
cross-checked against the live npm registry on 2026-09-01, and that they are being routed through
this gate because research discovered them rather than Phase 1 pre-approving them.

Record the user's verbatim answer in the plan SUMMARY. If the user rejects any package, halt the
plan and report which downstream plan is blocked: `react-markdown` blocks 02-06's Methodology page,
and the three font packages block 02-03's font task and would force D-12 back to manually hosted
woff2 files.

This checkpoint is never auto-approvable — `workflow.auto_advance` does not apply to a
package-legitimacy gate.
  </action>
  <verify>
    <automated>MISSING — human answer required; no runnable check exists for this gate</automated>
  </verify>
  <acceptance_criteria>
    - The user's verbatim approve/reject answer is recorded in the plan SUMMARY.
    - No install has run at the point this task completes — `git diff --stat frontend/package.json` reports no change yet.
    - If any package was rejected, the SUMMARY names the blocked downstream plan and the plan halts rather than proceeding to Task 2.
  </acceptance_criteria>
  <done>A human has explicitly approved or rejected the four-package set, and the answer is recorded verbatim.</done>
</task>

<task type="auto">
  <name>Task 2: Install the approved packages at exact pinned versions</name>
  <files>frontend/package.json, frontend/package-lock.json</files>
  <precondition>The npm registry is reachable from this machine (the Phase 01 frontend installs succeeded against it) and Task 1's human approval was granted.</precondition>
  <read_first>
    - `frontend/package.json` — the current pinned dependency set and the exact-version convention every Phase 1 entry follows (no caret on any pinned package).
    - `.planning/phases/02-data-layer-non-pitch-pages/02-RESEARCH.md` — the "Installation" block and the note that versions are re-verified against the live registry immediately before install, matching Phase 1's practice.
    - `.planning/STATE.md` — the Phase 01 note about removing a package that was not on the approved list before the first install; the same rule applies here.
  </read_first>
  <action>
Re-verify each version against the live registry before installing, exactly as Phase 1 did: run
`npm view react-markdown version`, `npm view @fontsource/archivo version`,
`npm view @fontsource/ibm-plex-sans version` and `npm view @fontsource/ibm-plex-mono version`, and
record the results in the SUMMARY. If a version has drifted from the audited 10.1.0 / 5.3.0 /
5.3.0 / 5.3.0, stop and report the drift rather than silently installing a version nobody audited.

Then run one install from the repo root:

`npm --prefix frontend install --save-exact react-markdown@10.1.0 @fontsource/archivo@5.3.0 @fontsource/ibm-plex-sans@5.3.0 @fontsource/ibm-plex-mono@5.3.0`

All four belong under `dependencies`, not `devDependencies`: `react-markdown` ships in the bundle
and the three font packages have their CSS imported by the app stylesheet.

If npm writes any package into `package.json` that was not in the approved list, remove it before
committing and note the removal in the SUMMARY — Phase 1 set that precedent when `create-vite`
introduced an unapproved default devDependency.

Confirm the Phase 1 toolchain is undisturbed afterwards: the existing Vitest suite, the typecheck,
and the build-purity script must all still pass.
  </action>
  <verify>
    <automated>node --input-type=module -e "import fs from 'node:fs'; const p = JSON.parse(fs.readFileSync('/home/sraja/fpl/frontend/package.json','utf8')); const want = {'react-markdown':'10.1.0','@fontsource/archivo':'5.3.0','@fontsource/ibm-plex-sans':'5.3.0','@fontsource/ibm-plex-mono':'5.3.0'}; let bad = 0; for (const k of Object.keys(want)) { const got = (p.dependencies ?? {})[k]; if (got !== want[k]) { console.error('BAD PIN', k, got); bad++; } } if (bad) process.exit(1); console.log('EXACT PINS OK');"</automated>
    <fails_when>non-zero exit, any line beginning "BAD PIN", or the absence of the line "EXACT PINS OK"</fails_when>
    <automated>npm --prefix frontend run test &amp;&amp; npm --prefix frontend run typecheck</automated>
    <fails_when>non-zero exit, a non-zero "failed" count in the vitest summary line, or any line matching "error TS"</fails_when>
    <automated>bash scripts/verify_frontend_build.sh</automated>
    <fails_when>non-zero exit, or the absence of the line "BUILD PURITY OK"</fails_when>
  </verify>
  <acceptance_criteria>
    - `frontend/package.json` `dependencies` contains the four packages at the literal versions `10.1.0`, `5.3.0`, `5.3.0`, `5.3.0` with no range prefix character.
    - `frontend/package-lock.json` is updated in the same commit as `frontend/package.json`.
    - The SUMMARY records the four `npm view` outputs and states whether any drifted from the audited versions.
    - No package outside the approved four was added to `dependencies` or `devDependencies`; if npm added one, the SUMMARY records its name and removal.
    - The full Vitest suite, the typecheck and `scripts/verify_frontend_build.sh` all pass after the install.
  </acceptance_criteria>
  <done>The four approved packages are installed at exact versions, the lockfile is updated, and the Phase 1 toolchain still passes end to end.</done>
</task>

<task type="auto">
  <name>Task 3: Create the parity deviation ledger</name>
  <files>.planning/phases/02-data-layer-non-pitch-pages/PARITY-DEVIATIONS.md</files>
  <reversibility rating="costly">Phase 7's CUT-01 comparison treats this ledger as the complete list of explained deltas; entries not written while the deviation is fresh cannot be reconstructed from the diff later.</reversibility>
  <read_first>
    - `.planning/phases/02-data-layer-non-pitch-pages/02-UI-SPEC.md` — the `## Deviation Ledger Seeds` table, which holds the eight already-identified entries verbatim with their reasons.
    - `.planning/phases/02-data-layer-non-pitch-pages/02-CONTEXT.md` — D-01 (behavioural parity, not visual cloning), D-03 (copy ports verbatim), D-04 (the ledger rule and its exact path), D-08, D-09, D-20, D-21.
    - `.planning/ROADMAP.md` — the Phase 7 entry, so the ledger's stated audience is accurate.
  </read_first>
  <action>
Create `.planning/phases/02-data-layer-non-pitch-pages/PARITY-DEVIATIONS.md` as the phase's
deviation ledger (D-04).

Open with a short purpose paragraph stating: this file lists every intentional difference between
the React rebuild and the live vanilla site; Phase 7 (CUT-01) compares the two sites and treats
every entry here as an explained delta and every unexplained difference as a defect; the parity
model is behavioural, not pixel-level (D-01), and all page copy ports verbatim (D-03).

Then a numbered table with columns `#`, `Deviation`, `Reason`, `Introduced by` carrying the eight
UI-SPEC seed entries, transcribed with their reasons intact:

1. Rich ErrorState/EmptyState with a Retry affordance instead of vanilla's silent blanks on a
   data-fetch failure — D-08, deliberate improvement.
2. Accessible tap/click/keyboard status-flag tooltip instead of vanilla's desktop-only `title`
   attribute — D-09, deliberate improvement plus Phase 3 mobile-table preparation.
3. The gameweek banner gains a "generated …" freshness line from `meta.json`'s `generated_utc` —
   D-20, deliberate trust-builder.
4. The banner's deadline-passed state reads `GW{n} deadline passed` instead of vanilla's
   `· passed` suffix — D-21, clarity improvement.
5. Price watchlist rows gain rise/fall trend icons vanilla does not have — UI-04 and D-17,
   deliberate small enhancement reusing existing dark-safe tokens.
6. PageShell's single unified footer replaces vanilla's per-page footer variants; the "How the
   model works" link is now sitewide and methodology's data-source credit line moves from the
   footer into the page body — Phase 1 shell simplification, logged retroactively now the ledger
   exists.
7. Table header eyebrow text and other sub-14px vanilla chrome renders at the 14px Label token
   instead of vanilla's ~11px — legibility improvement, respects the four-size typography contract.
8. Scoreboard tile values render at the 28px Display token at weight 700 instead of vanilla's 24px
   weight 500 — the two-weight typography contract has no 500.

Fill `Introduced by` with the plan that lands each one: 1 and 2 → 02-01, 3 and 4 → 02-03, 5 →
02-04, 6 → 01-06 (Phase 1, retroactive), 7 → whichever plan first renders that chrome, 8 → 02-05.

Close with an "Appending an entry" section stating the working rule: while executing any plan in
this phase, if you find yourself about to change vanilla behaviour rather than port it — including
"fixing" the counter-intuitive sort-arrow polarity, unifying the two different `maxHi` formulas, or
adding a `–` fallback to a column vanilla leaves un-guarded — append a new numbered row here in the
same commit as the change. A behavioural difference without a row is a parity defect, not a
deviation.
  </action>
  <verify>
    <automated>test -f .planning/phases/02-data-layer-non-pitch-pages/PARITY-DEVIATIONS.md &amp;&amp; grep -v '^ *#' .planning/phases/02-data-layer-non-pitch-pages/PARITY-DEVIATIONS.md | grep -cE '^\| *[1-8] *\|' | grep -qx 8 &amp;&amp; echo "LEDGER SEEDED"</automated>
    <fails_when>non-zero exit, or the absence of the line "LEDGER SEEDED" (the file is missing or does not carry exactly eight numbered table rows)</fails_when>
    <automated>grep -q 'Appending an entry' .planning/phases/02-data-layer-non-pitch-pages/PARITY-DEVIATIONS.md &amp;&amp; echo "APPEND RULE PRESENT"</automated>
    <fails_when>the absence of the line "APPEND RULE PRESENT"</fails_when>
  </verify>
  <acceptance_criteria>
    - The ledger exists at exactly `.planning/phases/02-data-layer-non-pitch-pages/PARITY-DEVIATIONS.md` — the path D-04 names and Phase 7 will look for.
    - It contains eight numbered rows whose deviation text matches the UI-SPEC seed table's wording.
    - Every row names both a reason and the plan that introduces it.
    - The closing section states that a behavioural difference shipped without a matching row is a parity defect.
    - The file mentions Phase 7 / CUT-01 explicitly as its consumer.
  </acceptance_criteria>
  <done>The ledger exists with all eight seed entries, per-entry attribution, and the append rule that governs the rest of the phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build | Four new third-party packages enter the frontend dependency tree and ship in the production bundle |
| planner/executor → repo | An automated agent adds dependencies without a human in the loop |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-SC | Tampering | npm installs (`react-markdown`, three `@fontsource` packages) | high | mitigate | Package-legitimacy audit already run (all four verdicts OK, no postinstall script on `react-markdown`) plus the blocking human checkpoint in Task 1; versions re-verified against the live registry immediately before install and pinned exactly, so the lockfile records precisely what was reviewed |
| T-02-04 | Repudiation | Undocumented parity deviations across the phase | medium | mitigate | The ledger created in Task 3 records every intentional difference with its reason and originating plan, so Phase 7 can distinguish an approved change from a regression instead of relying on memory |
</threat_model>

<verification>
1. The human approval answer is recorded verbatim in the SUMMARY.
2. `frontend/package.json` pins all four packages exactly; the lockfile is committed alongside it.
3. `npm --prefix frontend run test` and `npm --prefix frontend run typecheck` pass post-install.
4. `bash scripts/verify_frontend_build.sh` prints `BUILD PURITY OK`.
5. `PARITY-DEVIATIONS.md` exists with eight seed rows and the append rule.
</verification>

<success_criteria>
- No package was installed before the human gate was answered.
- The four packages are present at exact versions in `dependencies`, with an updated lockfile.
- The Phase 1 toolchain (tests, typecheck, build purity) is unaffected by the installs.
- The parity deviation ledger exists at the D-04 path with all eight seed entries and a stated append rule.
- Nothing under `web/` or `web/data/` was touched.
</success_criteria>

<output>
Create `.planning/phases/02-data-layer-non-pitch-pages/02-02-SUMMARY.md` when done.
</output>
