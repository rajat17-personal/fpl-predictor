---
phase: 02-data-layer-non-pitch-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/api.ts
  - frontend/src/lib/format.ts
  - frontend/src/lib/format.test.ts
  - frontend/src/lib/sortable.ts
  - frontend/src/lib/sortable.test.ts
  - frontend/src/lib/bandCell.ts
  - frontend/src/lib/bandCell.test.ts
  - frontend/src/lib/statusFlag.tsx
  - frontend/src/lib/statusFlag.test.tsx
  - frontend/src/lib/usePageMeta.ts
  - frontend/src/lib/usePageMeta.test.tsx
  - frontend/src/components/BandCell.tsx
  - frontend/src/routes/XpTable.tsx
  - frontend/src/routes/XpTable.test.tsx
  - frontend/src/test/fixtures/xp_table.json
  - frontend/src/test/fixtures/captains.json
autonomous: true
requirements: [UI-02]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    # --- Goal-backward observable truths (UI-02, D-01/D-02/D-05/D-06) ---
    - "Visiting `/` renders real rows from `/data/xp_table.json` with columns Pos, Player, Team, £m, Own %, xP (10–90% band) and Captain xP."
    - "Clicking a sortable column header once reorders the visible rows ASCENDING with nulls at the top and shows the ▼ glyph; clicking the same header again reorders DESCENDING with nulls at the bottom and shows ▲ (verified vanilla polarity, RESEARCH Pitfall 1 / rules R1–R3)."
    - "The position chip group (ALL/GK/DEF/MID/FWD, single-select) and the search box filter only the already-sliced top-50 rows — searching never reaches rows 51+ (R12)."
    - "Search matches case-insensitively against `name`, `team` (the full club name, which is not displayed) and `team_short`, after `.trim().toLowerCase()` (R12, R13)."
    - "The Captain picks sub-table renders the first 5 rows of `/data/captains.json` with no sort and no filter (R17)."
    - "A row whose `status` is not `\"a\"` renders a flag control after the player name; a row with `status === \"a\"` renders no flag element at all (R4)."
    # --- Edge-probe lift (EDGE_ABSENT fallback, UI-02) ---
    - "Boundary: a 51-row source renders exactly the first 50 rows, a 50-row source renders 50, a 49-row source renders 49 — the cap is a slice, never a pad (R10)."
    - "Adjacency/ordering: rows whose active sort-key values are exactly equal keep their pre-sort relative order (Array.prototype.sort is stable); no secondary/tie-break sort key exists anywhere in this phase."
    - "Empty: a zero-row source renders the shared EmptyState; a one-row source renders one row whose `maxHi` equals that row's `p90 ?? xp`; a row with null `p10`/`p90` collapses its band to a point at `xp` (R7)."
    - "Precision: every displayed number goes through `Number.prototype.toFixed` at the vanilla digit count (1 for £m and Own %, 2 for xP and Captain xP) — no custom rounding helper, no Intl.NumberFormat substitution, no banker's-rounding variant (R14–R16)."
    # --- UI-SPEC "UI Considerations" lift: E1 (xP table) ---
    - "E1 empty: two distinct zero conditions are handled — zero total rows renders the generic EmptyState, while a filter/search yielding zero renders the heading 'No players match'."
    - "E1 partial: per-row null `ownership` and `xp_capt` render `–`; null `p10`/`p90` fall back to `xp` inside the band."
    - "E1 overflow: the xP table renders inside a horizontally scrollable wrapper so its full column set is reachable at narrow widths."
    - "E1 long-text: player and team names wrap inside their cell — no truncation or ellipsis rule is introduced."
    - statement: "E1 loading — the in-flight `/data/xp_table.json` fetch renders the Phase 1 <Spinner/>."
      verification: backstop
    - statement: "E1 error — a failed `/data/xp_table.json` fetch renders the Phase 1 <ErrorState/> with a Retry affordance."
      verification: backstop
    - statement: "E1 populated — sort, filter and format correctness against the enumerated vanilla rules R1–R19 is carried by the per-rule Vitest suite (D-05)."
      verification: backstop
    # --- UI-SPEC "UI Considerations" lift: E10 (status-flag tooltip) ---
    - "E10 empty: an available player (`status === \"a\"`) has no flag element in the DOM at all."
    - "E10 populated: status in [i,s,u,n] renders ✕ in the destructive token with aria-label 'unavailable'; any other non-'a' status renders ▲ in the warn token with aria-label 'doubtful' (R5)."
    - "E10 partial: a row with empty/missing `news` falls back to the label word (`news || label`) as the tooltip body (R6)."
    - "E10 overflow: the tooltip popover is capped at 240px max-width and wraps within it rather than clipping."
    - "E10 long-text: variable-length FPL editorial `news` strings wrap inside the 240px popover and are never truncated."
  artifacts:
    - frontend/src/lib/sortable.ts
    - frontend/src/lib/bandCell.ts
    - frontend/src/lib/format.ts
    - frontend/src/lib/statusFlag.tsx
    - frontend/src/lib/usePageMeta.ts
    - frontend/src/components/BandCell.tsx
    - frontend/src/routes/XpTable.tsx
    - frontend/src/test/fixtures/xp_table.json
    - frontend/src/test/fixtures/captains.json
  key_links:
    - "`/data/xp_table.json` (root-relative, never `data/…`) → fetchJson → XpRow[] → slice(0,50) → filter → sortRows → BandCell DOM"
    - "`frontend/src/lib/api.ts` interface set → consumed unchanged by plans 02-04, 02-05, 02-06 (interface-first: no later plan re-edits api.ts)"
    - "sortRows dir polarity → the ▼/▲ header glyph (derived from `dir`, never from the resulting row order)"
    - "statusFlag glyph/aria mapping → reused verbatim by Differentials in plan 02-06"
  prohibitions:
    - "The top-50 free-preview cap and its Pro-tier teaser copy are not widened, removed, or made configurable — the full table stays behind the future paid tier (D-02)."
    - "No vanilla quirk is silently corrected: any behavioural difference from `web/assets/app.js` ships only alongside a matching line in PARITY-DEVIATIONS.md (D-04)."
---

<objective>
Port the flagship xP table page (`/`) to verified behavioural parity with the vanilla site, and
land the shared parity utilities every other page in this phase builds on.

Purpose: this is the phase's tracer. The single largest risk in Phase 2 is porting a subtly-
behaving vanilla algorithm wrong (RESEARCH Pitfall 1: the sort direction is inverted from what its
own arrow glyph implies). Proving one path end to end — fixture JSON → typed row → ported
comparator → band cell → rendered route → Vitest assertion — on the flagship page (D-06) settles
the port architecture before six more pages depend on it.

Output: `lib/sortable.ts`, `lib/bandCell.ts`, `lib/format.ts`, `lib/statusFlag.tsx`,
`lib/usePageMeta.ts`, `components/BandCell.tsx`, a fully-ported `routes/XpTable.tsx`, the complete
TypeScript interface set for the `web/data/*.json` contract in `lib/api.ts`, and the first slice of
the D-05 per-rule regression suite.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-data-layer-non-pitch-pages/02-CONTEXT.md
@.planning/phases/02-data-layer-non-pitch-pages/02-RESEARCH.md
@.planning/phases/02-data-layer-non-pitch-pages/02-PATTERNS.md
@.planning/phases/02-data-layer-non-pitch-pages/02-UI-SPEC.md
</context>

## Artifacts this phase produces

Symbols and files created by Phase 02 (none of these exist yet — do not treat them as drift):

| Artifact | Created in |
|----------|-----------|
| `sortRows()`, `SortState`, `useSortable()` — `frontend/src/lib/sortable.ts` | 02-01 |
| `bandGeometry()`, `bandTooltip()` — `frontend/src/lib/bandCell.ts` | 02-01 |
| `fixed1()`, `fixed2()`, `orDash()`, `localeInt()` — `frontend/src/lib/format.ts` | 02-01 |
| `StatusFlag` — `frontend/src/lib/statusFlag.tsx` | 02-01 |
| `usePageMeta()`, `PAGE_META` — `frontend/src/lib/usePageMeta.ts` | 02-01 |
| `BandCell` — `frontend/src/components/BandCell.tsx` | 02-01 |
| `XpRow`, `CaptainRow`, `FixtureTickerTeam`, `TickerGw`, `TickerFixture`, `Watchlist`, `WatchlistRow`, `StandingsRow`, `LeaderEntry`, `LeadersBoards`, `ScoreboardResponse`, `ScoreboardEntry`, `ScoreboardSummary` — added to `frontend/src/lib/api.ts` | 02-01 |
| `frontend/src/test/fixtures/*.json` (hand-authored parity fixtures) | 02-01, 02-03, 02-04, 02-05 |
| `PARITY-DEVIATIONS.md` (deviation ledger) | 02-02 |
| `useTheme()`, `ThemeChoice`, `resolveTheme()` — `frontend/src/lib/theme.ts` | 02-03 |
| `ThemeToggle` — `frontend/src/components/ThemeToggle.tsx` | 02-03 |
| `fmtAbs()`, `fmtRel()`, `fmtFreshness()` — `frontend/src/lib/deadline.ts` | 02-03 |
| `GwBanner` — `frontend/src/components/GwBanner.tsx` | 02-03 |
| `FdrCell` — `frontend/src/components/FdrCell.tsx` | 02-04 |
| `fetchScoreboard()` — `frontend/src/routes/Scoreboard.tsx` | 02-05 |
| `frontend/src/content/methodology.md` | 02-06 |

## Flagged planner assumptions (unresolved edge-probe rows)

The spec-less edge probe returned `unclassified` for UI-03, UI-04, UI-05 and UI-06 and they are
recorded here as explicit unresolved assumptions rather than silently dropped. The planner's
working assumption for all four is that their edge behaviour is fully constrained by the
enumerated vanilla Parity Rule Inventory (R20–R41) plus the UI-SPEC's per-page state tables, and
that no additional boundary/precision contract exists beyond those. If a reviewer disagrees, the
gap is in those inventories, not in these plans.

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "an xP table row renders from JSON with a working sortable header" — one path only</name>
  <files>frontend/src/lib/api.ts, frontend/src/lib/format.ts, frontend/src/lib/format.test.ts, frontend/src/lib/sortable.ts, frontend/src/lib/sortable.test.ts, frontend/src/lib/bandCell.ts, frontend/src/lib/bandCell.test.ts, frontend/src/components/BandCell.tsx, frontend/src/routes/XpTable.tsx, frontend/src/routes/XpTable.test.tsx, frontend/src/test/fixtures/xp_table.json</files>
  <read_first>
    - `web/assets/app.js` — the port source of truth. `makeSortable` (lines 57–79) and `bandCell` (lines 90–102) are the exact algorithms being ported; read the executed code, not any prose description of it.
    - `.planning/phases/02-data-layer-non-pitch-pages/02-RESEARCH.md` — Pitfall 1 (verified sort polarity truth table), Pitfall 4 (calc() width), Pitfall 5 (root-relative fetch paths), Pitfall 6 (pre-sorted slice), and the Parity Rule Inventory rows R1–R3, R7–R11, R14–R16.
    - `.planning/phases/02-data-layer-non-pitch-pages/02-PATTERNS.md` — the `sortable.ts` and `bandCell.ts` pattern assignments, which quote the vanilla source verbatim.
    - `web/index.html` — the xP table page; all page logic is the inline `<script type="module">` (lines 83–131) and the column set / copy is in the markup above it.
    - `frontend/src/lib/api.ts` — the existing `fetchJson`/`fetchApi`/`MetaResponse` module this task extends (do not rewrite the header comment or the relative-path discipline it documents).
    - `frontend/src/routes/XpTable.tsx` — the current Phase 1 tracer implementation being replaced; its `isPending`/`isError`/`!data` branch order is the pattern to keep.
    - `frontend/src/components/{Spinner,ErrorState,EmptyState}.tsx` — the shared state components to reuse rather than re-invent.
    - `frontend/src/routes/routeIsolation.test.tsx` and `frontend/src/test/harness.test.tsx` — the established Vitest + Testing Library idiom in this repo (jsdom, `afterEach(cleanup)`, jest-dom matchers already registered by `src/test/setup.ts`).
    - `web/data/xp_table.json` — read the first two rows only, to confirm the live field names the fixture must mirror.
  </read_first>
  <action>
Wire ONE path from the JSON contract to a rendered, sortable xP table row, through every layer this
phase touches. No other page, no filters, no captains table — those are Tasks 2 and 3.

Interface layer — extend `frontend/src/lib/api.ts` with the complete TypeScript interface set for
the whole `web/data/*.json` contract now, so no later plan has to re-edit this file (later plans in
this phase run in parallel and would collide). Declare, mirroring the live JSON exactly:
`XpRow` (`player_code`, `player_id`, `name`, `team`, `team_short`, `position`, `price_m`, `xp`,
`xp_capt`, `p10`, `p90`, `ownership`, `status`, `news`; `xp_capt`/`p10`/`p90`/`ownership` nullable),
`CaptainRow` (`name`, `team`, `team_short`, `position`, `price_m`, `xp`, `xp_capt`, `ownership`),
`TickerFixture` (`opp`, `home`, `fdr`), `TickerGw` (`gw`, `fixtures`), `FixtureTickerTeam` (`team`,
`short`, `gws`, `ease`, `xg_next`, `xgc_next`; the two xG fields nullable), `WatchlistRow` (`name`,
`team`, `position`, `price_m`, `ownership`, `net_transfers`, `progress`, `prob`, `proj_tonight`,
`status`), `Watchlist` (`mode`, `date`, `risers`, `fallers`, plus optional `locked_players`, `note`,
`trained_utc`, `val_moved_hit`), `StandingsRow` (`team`, `short`, `played`, `won`, `drawn`, `lost`,
`gf`, `ga`, `gd`, `points`), `LeaderEntry` (`name`, `team`, `position`, `value`), `LeadersBoards`
(keys `points`, `goals`, `assists`, `clean_sheets`, `cards`), and `ScoreboardEntry` /
`ScoreboardSummary` / `ScoreboardResponse` per `predict/scoreboard.py`'s emitted shape
(`entries[]` with `gw`, `n_players`, `generated_utc`, `mae_model`, `spearman_model`, optional
`mae_fpl`/`spearman_fpl`, `captain{name,team,points}`, `top5[]`, `best_player{name,points}`;
`summary` with `gameweeks`, `mae_model`, `spearman_model`, `captain_avg_points`, optional
`mae_fpl`/`spearman_fpl`). Keep `fetchJson`/`fetchApi`/`MetaResponse` unchanged.

Format layer — create `frontend/src/lib/format.ts` exporting four PRIMITIVES only: `fixed1(v)`,
`fixed2(v)`, `orDash(s)` (returns the en-dash `–` for null/undefined) and `localeInt(v)`
(`Number.prototype.toLocaleString`). Do NOT create per-field helpers such as a single ownership
formatter: RESEARCH Pitfall 3 documents that the vanilla pages deliberately disagree about which
fields get a `–` fallback, and a unified per-field formatter would erase that. Each call site
composes the primitives the way its own vanilla source did.

Sort layer — create `frontend/src/lib/sortable.ts` exporting
`sortRows<T>(rows: T[], key: keyof T, dir: 1 | -1, numeric: boolean): T[]` and a
`SortState<K> = { key: K | null; dir: 1 | -1 }` type, plus a `useSortable` hook holding that state
as ephemeral React state (D-07 — no URL params, no localStorage). Port the comparator exactly:
numeric compares `dir * ((y ?? -1e9) - (x ?? -1e9))` where `x` is the FIRST argument's value and
`y` the second; string compares `dir * String(y ?? "").localeCompare(String(x ?? ""))`. A fresh
click on a new key sets `dir = -1`; clicking the already-active key flips to `-dir`. The header
glyph is `dir < 0 ? "▼" : "▲"` and is derived from `dir` alone. Do not "fix" the fact that ▼
accompanies an ascending result — that is the live production behaviour and any change requires a
PARITY-DEVIATIONS.md line (D-04). `sortRows` must not mutate its input (copy before sorting).

Band layer — create `frontend/src/lib/bandCell.ts` exporting `bandGeometry(row, maxHi)` returning
`{ lo, hi, leftPct, hiPct, xpPct }` as percentage STRINGS from `Math.min(100, 100 * v / maxHi)`,
and `bandTooltip(row)` returning the verbatim copy
`xP {xp.toFixed(2)} — actual score lands between {lo.toFixed(1)} and {hi.toFixed(1)} in 8 gameweeks
out of 10` with `lo = p10 ?? xp`, `hi = p90 ?? xp` (R7–R9, D-03). Create
`frontend/src/components/BandCell.tsx` consuming it: the value `xp.toFixed(2)`, an `aria-hidden`
track wrapper, the track positioned with inline style `left: leftPct` and
`width: calc(<hiPct> - <leftPct>)` as a CSS calc expression string — NOT a pre-subtracted JavaScript
number (RESEARCH Pitfall 4) — the point at `calc(<xpPct> - 4px)`, and the range label
`{lo.toFixed(1)}–{hi.toFixed(1)}` using the en-dash. Colour via the existing `--color-band` and
`--color-band-pt` tokens through Tailwind token class names; no hex literal in the component.

Route layer — replace `frontend/src/routes/XpTable.tsx`. Fetch `/data/xp_table.json` with
TanStack Query (`queryKey: ["xp_table"]`, `staleTime: 60_000` per RESEARCH assumption A1) using the
ROOT-RELATIVE path — a bare `data/xp_table.json` resolves against the current route under client-
side routing and 404s (Pitfall 5). Slice the first 50 rows with NO re-sort (R10 — the pipeline
pre-sorts descending by `xp`; porting the assumption unchanged is the parity requirement). Compute
`maxHi = Math.max(...top.map(r => r.p90 ?? r.xp))` with no floor (R11 — the Differentials page uses
a floor of 1 and the two must not be unified). Render a table whose sortable headers carry the keys
`position`, `name`, `team_short`, `price_m`, `ownership`, `xp`, `xp_capt`, each header a
`<button type="button">` containing the label plus the direction glyph span, wired to
`useSortable`; numeric compare for every key except `position`, `name` and `team_short`. Cells:
`position` as-is, `name` as-is (the flag lands in Task 3), `team_short` as-is,
`price_m` via `fixed1` with no fallback (R14), `ownership` via `fixed1` + `orDash` (R15), `xp` via
`<BandCell>`, `xp_capt` via `fixed2` + `orDash` (R16). Wrap the table in a horizontally scrollable
container (E1 overflow). Keep the Phase 1 branch order: `isPending` → `<Spinner/>`, `isError` →
`console.error(error)` then `<ErrorState resource="the xP table" onRetry={refetch} />` (D-08; the
thrown error's message and body never reach the DOM), zero rows → `<EmptyState/>`.

Fixture layer — create `frontend/src/test/fixtures/xp_table.json`: a hand-authored 6-row array
(not a copy of the 154KB live file) mirroring the live field names exactly, deliberately containing
one row with `ownership: null`, one with `xp_capt: null`, one with `p10: null, p90: null`, one with
`status: "i"` and a non-empty `news`, one with `status: "d"` and empty `news`, and at least two rows
sharing an identical `xp` value so the stable-tie assertion has something to bite on. Order the
rows `xp`-descending so the untested pipeline assumption (Pitfall 6) is representable.

Tests — `sortable.test.ts` asserts the Node-verified truth table verbatim: fresh click `dir=-1`
sorts numeric ascending; second click `dir=+1` sorts descending; `dir=-1` puts null at the TOP and
`dir=+1` at the BOTTOM; `dir=-1` sorts strings ascending; equal keys preserve input order; the
input array is not mutated. `bandCell.test.ts` asserts the geometry percentages, the `p10`/`p90`
null fallback to `xp`, the 100% clamp, and the tooltip string character-for-character.
`format.test.ts` asserts `fixed1`/`fixed2` digit counts and that `orDash` returns the en-dash for
null and undefined only. `XpTable.test.tsx` is the END-TO-END check: stub `global.fetch` to return
the fixture, render the route inside a `QueryClientProvider`, and assert (a) rendered cell text for
a known row including the `–` fallbacks, (b) the band tooltip text is present, (c) clicking the
`£m` header once produces ascending row order with the ▼ glyph and clicking it again produces
descending with ▲, (d) a 51-row stubbed response renders exactly 50 rows, (e) a zero-row response
renders the EmptyState heading. Use `screen`/`within` queries, not snapshot files.
  </action>
  <verify>
    <automated>npm --prefix frontend run test -- src/routes/XpTable.test.tsx src/lib/sortable.test.ts src/lib/bandCell.test.ts src/lib/format.test.ts</automated>
    <fails_when>non-zero exit, or the vitest summary line reports any non-zero "failed" count, or it reports "No test files found"</fails_when>
    <automated>npm --prefix frontend run typecheck</automated>
    <fails_when>non-zero exit, or any line matching "error TS" in the output</fails_when>
    <automated>! grep -rqE '\bfetch\(["'"'"'`]data/' frontend/src && echo "ROOT-RELATIVE FETCH OK"</automated>
    <fails_when>the line "ROOT-RELATIVE FETCH OK" is absent from the output (a route-relative data fetch was introduced)</fails_when>
  </verify>
  <acceptance_criteria>
    - `npm --prefix frontend run test -- src/lib/sortable.test.ts` passes with an assertion that `sortRows([{v:3},{v:1},{v:5},{v:2}], "v", -1, true)` yields values `[1,2,3,5]` and that `sortRows([{v:3},{v:null},{v:5}], "v", -1, true)` yields `[null,3,5]`.
    - `npm --prefix frontend run test -- src/lib/bandCell.test.ts` passes with an assertion that `bandTooltip` returns exactly `xP 3.63 — actual score lands between 2.4 and 10.5 in 8 gameweeks out of 10` for `{xp: 3.63, p10: 2.41, p90: 10.47}`.
    - `grep -q 'calc(' frontend/src/components/BandCell.tsx` succeeds — the band width is a CSS calc expression, not a pre-subtracted number.
    - `grep -q 'slice(0, 50)' frontend/src/routes/XpTable.tsx` succeeds, and a test stubbing a deliberately mis-ordered source asserts the rendered row order equals the source order before any header is clicked — proving the fetched array is not re-sorted before slicing.
    - `frontend/src/lib/api.ts` exports all thirteen new interfaces named in the action; `npm --prefix frontend run typecheck` exits 0.
    - `! grep -rqE '#[0-9a-fA-F]{6}' frontend/src/components/BandCell.tsx` — colour goes through design tokens only.
    - `frontend/src/test/fixtures/xp_table.json` parses as an array of 6 objects and contains at least one row each with a null `ownership`, a null `xp_capt`, and null `p10`/`p90`.
  </acceptance_criteria>
  <done>Visiting `/` against the real dev proxy shows the top-50 xP table with band cells and working sortable headers; the four Vitest files above are green; `npm --prefix frontend run typecheck` exits 0; the slice is committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: xP table filters, verbatim page copy, and the per-route page-meta helper</name>
  <files>frontend/src/lib/usePageMeta.ts, frontend/src/lib/usePageMeta.test.tsx, frontend/src/routes/XpTable.tsx, frontend/src/routes/XpTable.test.tsx</files>
  <read_first>
    - `web/index.html` — the position-chip and search-filter logic (lines 95–121) and the verbatim page copy: h1 and sub-paragraph (lines 30–34), the top-50 free-preview note (lines 59–60), and the reading-the-numbers explainer (lines 61–66).
    - `.planning/phases/02-data-layer-non-pitch-pages/02-UI-SPEC.md` — the `/` Page Contract, the "Per-page title / meta description" table (all 8 routes), and the Copywriting Contract rows for "xP table — no filter matches".
    - `.planning/phases/02-data-layer-non-pitch-pages/02-PATTERNS.md` — the "Vanilla page chrome → PageShell equivalence table", which carries the same 8-route title/description values.
    - `frontend/src/routes/XpTable.tsx` — the Task 1 implementation being extended.
    - `frontend/src/routes/XpTable.test.tsx` — the Task 1 test file being extended.
    - `frontend/src/components/PageShell.tsx` — confirms the shell owns no per-page title today, so the hook is the only mechanism.
  </read_first>
  <behavior>
    - Selecting the DEF chip leaves only rows whose `position` is `DEF`; selecting ALL restores every sliced row.
    - Only one chip is active at a time and the active chip carries `aria-pressed="true"`.
    - Typing a lowercase substring of a player's name filters to that player; typing a substring of the full club name (`team`, which is not rendered in any cell) filters the same way; typing a substring of `team_short` filters the same way.
    - Leading/trailing whitespace in the search box is trimmed before matching, and matching is case-insensitive.
    - A position chip and a search term compose with AND, not OR.
    - A filter combination that matches zero of the sliced rows renders the heading "No players match" — not the generic EmptyState.
    - Filters apply strictly on top of the already-sliced top 50: a player at source index 60 is unreachable by any search term.
    - Navigating away and back resets both filters to ALL / empty (ephemeral state, D-07).
    - `usePageMeta("/")` sets `document.title` to `FPL ML — expected points, honestly measured` and sets the `content` of `meta[name="description"]` to the matching row, creating the meta element if the document has none.
  </behavior>
  <action>
Create `frontend/src/lib/usePageMeta.ts` (D-11): export a frozen `PAGE_META` record keyed by the
eight route paths `/`, `/team`, `/fixtures`, `/prices`, `/league`, `/scoreboard`, `/differentials`,
`/methodology`, each holding `{ title, description }` copied VERBATIM from the UI-SPEC's per-page
title/description table (D-03), and a `usePageMeta(path)` hook that on mount and on path change
assigns `document.title` and upserts `document.querySelector('meta[name="description"]')`'s
`content`. No helmet-style dependency, no new package. `/team`'s row is included now for table
completeness even though Phase 3 fills that page.

Extend `frontend/src/routes/XpTable.tsx`:

Position chips — a single-select toggle group over `ALL`, `GK`, `DEF`, `MID`, `FWD` rendered as
`<button type="button">` elements carrying `aria-pressed`, held in ephemeral React state defaulting
to `ALL`. Search — a controlled `<input type="search">` whose value is normalised with
`.trim().toLowerCase()` before matching (R13). The filter predicate is exactly vanilla's (R12):
`(posFilter === "ALL" || r.position === posFilter) && (!query || r.name.toLowerCase().includes(query)
|| r.team.toLowerCase().includes(query) || r.team_short.toLowerCase().includes(query))` — note
`r.team` is the full club name and is NOT rendered in any cell, yet it is searchable; that is
deliberate vanilla behaviour, keep it. Both filters run against the already-sliced top 50, never
against the full fetched array.

When the filter result is empty but the source had rows, render the heading `No players match`
with the body `Try a different position filter or search term.` (UI-SPEC Copywriting), distinct
from the shared `<EmptyState/>` used for a zero-row source.

Port the verbatim page copy (D-03) from `web/index.html`: the h1, the sub-paragraph including its
inline link to `/scoreboard` (use React Router's `<Link>`, not an anchor with a `.html` href), the
top-50 free-preview note, the reading-the-numbers explainer paragraph, and the `Captain picks` h2
placeholder heading that Task 3 fills. Call `usePageMeta("/")` at the top of the component.

Write the tests before the implementation of each behaviour above, extending
`XpTable.test.tsx` with the chip/search/AND-composition/no-match/unreachable-row cases and adding
`usePageMeta.test.tsx` for the title and description upsert (both the "meta element already
present" and "no meta element in the document" paths).
  </action>
  <verify>
    <automated>npm --prefix frontend run test -- src/routes/XpTable.test.tsx src/lib/usePageMeta.test.tsx</automated>
    <fails_when>non-zero exit, or the vitest summary line reports any non-zero "failed" count, or it reports "No test files found"</fails_when>
    <automated>npm --prefix frontend run typecheck</automated>
    <fails_when>non-zero exit, or any line matching "error TS" in the output</fails_when>
    <automated>grep -v "^ *[/*]" frontend/src/lib/usePageMeta.ts | grep -c "description:" | grep -qx 8 &amp;&amp; echo "PAGE META TABLE COMPLETE"</automated>
    <fails_when>the line "PAGE META TABLE COMPLETE" is absent (the route table does not carry exactly 8 description entries outside comments)</fails_when>
  </verify>
  <acceptance_criteria>
    - A test asserts that with the DEF chip active and the search box holding `  MUN  `, only rows that are both DEF and match `mun` remain — proving AND composition and trimming together.
    - A test stubs a 60-row response and asserts that searching for a token unique to source row 60 renders the "No players match" heading, proving filters never reach past the slice.
    - A test asserts `document.title` equals `FPL ML — expected points, honestly measured` after rendering the route.
    - `grep -q 'Showing the top 50 by xP' frontend/src/routes/XpTable.tsx` succeeds — the free-preview note is present verbatim.
    - `grep -q 'aria-pressed' frontend/src/routes/XpTable.tsx` succeeds — the chips are a real toggle group.
    - `! grep -rq '\.html"' frontend/src/routes/XpTable.tsx` — no vanilla `.html` href survived the port.
  </acceptance_criteria>
  <done>Position chips and search filter the table with vanilla's exact predicate, the zero-match copy is distinct from the generic empty state, the page's verbatim copy and per-route title/description are in place, and both test files are green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Accessible status flag and the Captain picks sub-table</name>
  <files>frontend/src/lib/statusFlag.tsx, frontend/src/lib/statusFlag.test.tsx, frontend/src/test/fixtures/captains.json, frontend/src/routes/XpTable.tsx, frontend/src/routes/XpTable.test.tsx</files>
  <read_first>
    - `web/assets/app.js` — `statusFlag` (lines 81–88): the exact trigger condition, the `["i","s","u","n"]` membership test, the glyph/class/aria mapping and the `news || label` tooltip source.
    - `web/index.html` — the Captain picks sub-table (lines 124–129), whose fallback rules deliberately differ from the main table's.
    - `.planning/phases/02-data-layer-non-pitch-pages/02-RESEARCH.md` — Pitfall 3 (per-page `??` inconsistency for `ownership`) and rules R4–R6, R17–R19.
    - `.planning/phases/02-data-layer-non-pitch-pages/02-UI-SPEC.md` — the "Status-flag tooltip (D-09)" section (button semantics, `aria-describedby`, hover/focus/click-toggle, Escape and click-outside, 240px cap) and the E10 consideration table.
    - `frontend/src/routes/XpTable.tsx` — the Task 2 implementation being extended.
    - `frontend/src/lib/format.ts` — the primitives whose composition differs between the two tables.
    - `web/data/captains.json` — the live shape the fixture must mirror.
  </read_first>
  <behavior>
    - A row with `status: "a"` renders no flag element — `queryByRole("button", { name: /unavailable|doubtful/ })` finds nothing.
    - A row with `status: "i"` renders a button whose accessible name is `unavailable` and whose visible glyph is `✕`.
    - A row with `status: "d"` renders a button whose accessible name is `doubtful` and whose visible glyph is `▲`.
    - Focusing the flag button reveals a `role="tooltip"` element containing the row's `news` text.
    - Clicking the flag button toggles the tooltip and flips `aria-expanded`; pressing Escape closes it; clicking elsewhere in the document closes it.
    - A row whose `news` is an empty string shows the label word (`doubtful` / `unavailable`) as the tooltip body instead.
    - The Captain picks table renders exactly 5 rows from a 7-row captains source.
    - A captains row with a null `ownership` renders the literal text produced by vanilla's un-guarded `?.toFixed(1)` — the en-dash fallback used by the main table is NOT applied here.
  </behavior>
  <action>
Create `frontend/src/lib/statusFlag.tsx` exporting a `StatusFlag` component taking
`{ status, news }`. It returns `null` when `status === "a"` (R4). Otherwise compute
`out = ["i","s","u","n"].includes(status)`, `label = out ? "unavailable" : "doubtful"`, glyph
`out ? "✕" : "▲"` (R5), and tooltip body `news || label` (R6). Render a `<button type="button">`
with `aria-label={label}`, `aria-expanded` reflecting the open state and `aria-describedby` pointing
at a `<span role="tooltip" id=…>` holding the body. Give the button padding to reach a 44px touch
target while the glyph stays visually small. Colour: destructive token when `out`, warn token
otherwise — token class names only, no hex literal. Open the tooltip on hover, on `focus-visible`,
and on click (D-09 — vanilla's `title` attribute is desktop-only and is deliberately replaced,
ledger entry 2); close it on `Escape` and on a click outside. Cap the tooltip at 240px max-width
with normal wrapping. Generate the tooltip id with React's `useId` so multiple flags on one page
never collide.

Mount `<StatusFlag>` in the xP table's Player cell, immediately after the name (Task 1 left the
cell name-only).

Create `frontend/src/test/fixtures/captains.json`: a hand-authored 7-row array mirroring the live
`web/data/captains.json` field names, with one row carrying a null `ownership` so the divergent
fallback is testable.

Extend `frontend/src/routes/XpTable.tsx` with the Captain picks sub-table under the existing h2:
fetch `/data/captains.json` as a second TanStack Query (`queryKey: ["captains"]`), take
`slice(0, 5)` with no sort and no filter (R17), and render columns Player / Team / £m / Own % /
Captain xP where Team uses `r.team` — the FULL club name, not `team_short`, unlike the main table —
`price_m` uses `fixed1`, `ownership` uses `fixed1` WITHOUT the en-dash fallback (R18, Pitfall 3 —
this divergence from the main table is vanilla behaviour and must be preserved, not unified), and
`xp_capt` uses `fixed2` with no optional chaining (R19). The captains fetch failing must not take
the main xP table down: render the sub-table's own inline failure quietly and keep the main table
mounted.

Write each assertion in the behaviour list before implementing the behaviour it covers.
  </action>
  <verify>
    <automated>npm --prefix frontend run test -- src/lib/statusFlag.test.tsx src/routes/XpTable.test.tsx</automated>
    <fails_when>non-zero exit, or the vitest summary line reports any non-zero "failed" count, or it reports "No test files found"</fails_when>
    <automated>npm --prefix frontend run test</automated>
    <fails_when>non-zero exit, or the vitest summary line reports any non-zero "failed" count</fails_when>
    <automated>npm --prefix frontend run typecheck &amp;&amp; bash scripts/verify_frontend_build.sh</automated>
    <fails_when>non-zero exit, any line matching "error TS", or the absence of the line "BUILD PURITY OK" in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - A test asserts `render(<StatusFlag status="a" news="x" />)` produces an empty container.
    - A test asserts the accessible name is `unavailable` and the glyph `✕` for each of `i`, `s`, `u`, `n`, and `doubtful` / `▲` for `d`.
    - A test asserts that clicking the flag button sets `aria-expanded="true"` and that pressing Escape returns it to `"false"`.
    - A test asserts the Captain picks table renders 5 rows from a 7-row fixture and that its `Team` cell shows the full club name while the main table's shows the short code.
    - A test asserts the captains row with null `ownership` does NOT render the en-dash that the main table's Own % column renders for the same null.
    - `bash scripts/verify_frontend_build.sh` prints `BUILD PURITY OK` — no pipeline JSON was pulled into the bundle.
  </acceptance_criteria>
  <done>The flag is keyboard- and touch-reachable with vanilla's exact glyph/aria mapping, the Captain picks sub-table renders with its own divergent fallback rules, the full frontend suite is green, and the production build stays free of pipeline data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pipeline → browser | `web/data/*.json` is generated by the Python export and rendered in the browser; its string fields (`name`, `team`, `news`, `status`) are the only externally-shaped values this plan renders |
| user → client filter | The search input and position chips are client-only predicates over already-fetched public data; no value crosses to a server |
| network → client | A failed or hostile `/data/*.json` response reaches `ErrorState` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | `XpTable.tsx`, `BandCell.tsx`, `statusFlag.tsx` — porting vanilla template-string markup | high | mitigate | Every ported vanilla template string becomes JSX text children (auto-escaped); React's raw-HTML escape-hatch prop is not used anywhere in `frontend/src`, enforced by the Task 1 and Task 3 test runs plus the repo-wide gate in plan 02-06 |
| T-02-02 | Information Disclosure | `ErrorState` on a failed `/data/xp_table.json` fetch | medium | mitigate | Reuse the Phase 1 `ErrorState`, which accepts only a `resource` label; the thrown Error's message, response body and request URL go to `console.error` and never into the DOM (Phase 1 control T-05-02, preserved) |
| T-02-03 | Denial of Service | Client render cost if the pipeline emits an oversized `xp_table.json` | low | accept | The `slice(0, 50)` cap bounds rendered rows by construction (R10); the fetch itself is same-origin static JSON already served publicly by the vanilla site |
</threat_model>

<verification>
1. `npm --prefix frontend run test` — full Vitest suite green, including the Phase 1 tests this plan must not regress (`PageShell.test.tsx`, `ErrorState.test.tsx`, `Spinner.test.tsx`, `routeIsolation.test.tsx`, `harness.test.tsx`).
2. `npm --prefix frontend run typecheck` — exits 0.
3. `bash scripts/verify_frontend_build.sh` — prints `BUILD PURITY OK`.
4. Manual dev-server spot check (optional, not gating): `npm --prefix frontend run dev` with uvicorn behind the proxy, visit `/`, confirm 50 rows, working chips/search, band tooltips, and a flag on at least one flagged player.
</verification>

<success_criteria>
- `/` renders the top-50 xP table from `/data/xp_table.json` with all seven columns at the vanilla number formats.
- Sort direction and glyph match the Node-verified vanilla polarity, asserted in `sortable.test.ts`.
- Position chips + search filter with vanilla's exact predicate, on top of the slice only.
- Captain picks renders 5 rows with its own divergent fallback rules.
- Flagged players carry a keyboard- and touch-accessible tooltip; available players carry no flag element.
- The complete `web/data/*.json` TypeScript interface set exists in `frontend/src/lib/api.ts` and no later plan needs to edit that file.
- Full frontend suite, typecheck and build-purity gate all green.
</success_criteria>

<output>
Create `.planning/phases/02-data-layer-non-pitch-pages/02-01-SUMMARY.md` when done.
</output>
