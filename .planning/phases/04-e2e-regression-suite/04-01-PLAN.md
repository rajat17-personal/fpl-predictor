---
phase: 04-e2e-regression-suite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - e2e/scripts/capture_fixtures.py
  - e2e/fixtures/v1/MANIFEST.md
  - e2e/fixtures/v1/normal/api/bootstrap-static.json
  - e2e/fixtures/v1/normal/api/fixtures.json
  - e2e/fixtures/v1/normal/api/intervals.json
  - e2e/fixtures/v1/normal/api/capture.json
  - e2e/fixtures/v1/normal/api/pools/
  - e2e/fixtures/v1/normal/api/entries/
  - e2e/fixtures/v1/normal/web-data/
  - api/main.py
  - frontend/package.json
  - tests/test_fixture_mode.py
  - .gitignore
autonomous: false
requirements: [E2E-01]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "With FPL_FIXTURE_DIR set, every API endpoint (/api/health, /api/meta, /api/team/{entry}, /api/solve, /api/plan, /api/rate/{entry}) answers from committed on-disk fixtures and makes zero outbound HTTP requests."
    - "With FPL_FIXTURE_DIR set, no model artifact is loaded — /api/solve still runs the real PuLP/CBC ILP, over a frozen prediction pool."
    - "With FPL_FIXTURE_DIR set, uvicorn serves frontend/dist at / (SPA deep links resolve via dist/404.html) and the frozen web-data set at /data."
    - "With FPL_FIXTURE_DIR unset, api/main.py behaves exactly as it does today: a single StaticFiles mount over web/, live FPL API calls, real joblib artifact load."
    - "Re-running the fixture verifier on a clean checkout confirms the committed fixture set is internally coherent: web-data/meta.json's gw equals the gw derived from the captured bootstrap-static.json, and a frozen pool exists for that gw through gw+5."
    - "npm --prefix frontend run build emits dist/404.html byte-identical to dist/index.html."
  artifacts:
    - e2e/scripts/capture_fixtures.py
    - e2e/fixtures/v1/MANIFEST.md
    - e2e/fixtures/v1/normal/api/capture.json
    - e2e/fixtures/v1/normal/api/bootstrap-static.json
    - e2e/fixtures/v1/normal/api/fixtures.json
    - e2e/fixtures/v1/normal/api/intervals.json
    - e2e/fixtures/v1/normal/api/pools/
    - e2e/fixtures/v1/normal/api/entries/6980093/
    - e2e/fixtures/v1/normal/web-data/
    - tests/test_fixture_mode.py
  key_links:
    - "predict.live._gw_pool is the single model-inference leaf; fixture mode rebinds it so build_pool/build_horizon_pool still run their real aggregation and decay math over frozen per-gameweek pools."
    - "_fetch_team and _free_transfers keep all their normalisation and error-shape logic; only their three requests.get call sites branch to disk."
    - "web-data/meta.json's gw is the join key that keeps /data and /api describing the same gameweek universe (D-07)."
    - "frontend/package.json's postbuild hook is what makes Starlette's existing StaticFiles(html=True) 404.html fallback reachable for SPA deep links."
  prohibitions:
    - "The API must make no outbound HTTP request to fantasy.premierleague.com while FPL_FIXTURE_DIR is set."
    - "models/artifacts/xp_model.joblib must never be loaded while FPL_FIXTURE_DIR is set."
    - "FPL_FIXTURE_DIR must never be set in a production or deploy configuration; the unset default must keep serving web/ from live data."
    - "e2e/fixtures/v1/** must never be edited in place once committed; a contract change cuts v2 alongside v1."
    - "web/data/*.json must never be copied or bundled into the frontend build output."
    - "Playwright route() mocking of /api/* is forbidden — the React-to-FastAPI wire must be exercised for real."
    - "Fixed-duration sleep waits are forbidden in every spec; use auto-retrying locator assertions and response waits."
    - "Screenshot or visual-regression baselines are forbidden; geometry is asserted through bounding boxes."
    - "Expected rendered values must never be re-derived from fixture JSON at test time; they are hand-derived hardcoded literals (D-15)."
    - "No dev-server variant and no E2E_DEV flag: local and CI topology are identical (D-03)."
    - "npm --prefix frontend run typecheck must never be used as a verification gate — it type-checks zero files."
    - "No spec may assert time-dependent copy without first pinning the browser clock and timezone."
---

<objective>
Build the two things every Playwright spec in this phase stands on: a frozen, versioned
fixture set captured from one real gameweek, and an env-var fixture seam in `api/main.py`
that makes the real FastAPI app read that set from disk instead of calling
`fantasy.premierleague.com` or loading a model artifact.

Purpose: E2E-01's north star — "pass or fail on code, never on the calendar". Every later
plan's assertions are hand-derived literals over the immutable `v1` fixture set produced here.
Output: `e2e/fixtures/v1/normal/**`, `e2e/fixtures/v1/MANIFEST.md`, the capture/verify script,
the fixture-mode branches in `api/main.py`, the SPA `404.html` build hook, and an in-process
pytest proof that fixture mode is network-free and artifact-free.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-e2e-regression-suite/04-CONTEXT.md
@.planning/phases/04-e2e-regression-suite/04-RESEARCH.md
@.planning/phases/04-e2e-regression-suite/04-PATTERNS.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Decide the capture policy for entry 6980093's manager fields</name>
  <reversibility rating="one-way">A committed fixture enters git history permanently; a later scrub does not remove it from prior commits.</reversibility>
  <read_first>
    - tests/test_api.py (lines 1-60 and the fixture-identity comment near the team/rate tests) — the existing synthetic-identity-only convention and its stated reason
    - .planning/phases/04-e2e-regression-suite/04-CONTEXT.md (D-07 and the Specific Ideas block)
    - .planning/phases/04-e2e-regression-suite/04-RESEARCH.md (Pitfall 5 and Open Question 2)
  </read_first>
  <decision>Which fields of your own FPL entry 6980093 may be committed verbatim into the permanent E2E fixture set</decision>
  <context>
    D-07 names entry 6980093 — your own FPL team — as the E2E capture target, and `api/main.py`'s
    `_fetch_team` returns a `manager` sub-object carrying `team_name`, a free-text `manager` name
    built from `player_first_name` and `player_last_name`, `overall_points`, `overall_rank` and
    `gw_points`. `tests/test_api.py`'s adjacent convention for every *other* fixture is
    synthetic-identity-only, on the grounds that a committed fixture is a permanent public record.
    The entry id, picks and rank are already publicly visible on the official FPL site for any
    entry id; the two free-text name fields are the only genuinely personal values.

    Whichever option you pick, `entries/6980093/picks_event{N}.json` and `history.json` are
    captured verbatim — they are the squad and transfer data every solver flow needs, and they
    carry no name fields. The choice only affects `entries/6980093/summary.json`.
  </context>
  <options>
    <option id="scrub-names">
      <name>Scrub the two manager name fields, keep the team name (recommended)</name>
      <pros>Keeps a recognisable team name in the rendered heading for debuggability; keeps the numeric season figures real; no personal name enters git history</pros>
      <cons>Rate-tab specs assert a placeholder manager name rather than a real one</cons>
    </option>
    <option id="verbatim">
      <name>Capture every field verbatim, including the real manager name</name>
      <pros>Maximum fidelity — the fixture is exactly what the live API returns</pros>
      <cons>A real personal name enters git history permanently and cannot be removed from prior commits; contradicts the convention stated in tests/test_api.py</cons>
    </option>
    <option id="scrub-all">
      <name>Scrub the team name and both manager name fields</name>
      <pros>No free-text identity of any kind in the fixture set</pros>
      <cons>A failing rate-tab test shows a synthetic team name, weakening the "a human debugging should see a recognisable page" goal</cons>
    </option>
  </options>
  <resume-signal>Reply with one of: scrub-names, verbatim, or scrub-all</resume-signal>
  <done>The developer's verbatim answer is recorded in the plan SUMMARY and in MANIFEST.md; Task 2 applies exactly that policy and no other.</done>
</task>

<task type="auto">
  <name>Task 2: Capture and commit the immutable v1 "normal" fixture set</name>
  <precondition>`models/artifacts/xp_model.joblib`, `models/artifacts/intervals.json`, `data/raw/live/bootstrap-static.json`, `data/raw/live/fixtures.json` and `data/raw/live/element_history.parquet` all exist locally, and `https://fantasy.premierleague.com/api/entry/6980093/` is reachable — the capture reads the model artifact and the cached upstream payloads from disk and fetches the three entry endpoints live exactly once. If any is absent or unreachable, halt and report; do not synthesise substitutes.</precondition>
  <files>e2e/scripts/capture_fixtures.py, e2e/fixtures/v1/MANIFEST.md, e2e/fixtures/v1/normal/api/**, e2e/fixtures/v1/normal/web-data/**, .gitignore</files>
  <read_first>
    - predict/live.py (`_load_live`, `_next_gw`, `_gw_pool`, `build_pool`, `build_horizon_pool`) — the exact pool column set and the gameweek derivation
    - predict/export.py (`export`, `build_meta`, `build_table`, `build_captains`, `build_squad`, `build_ticker`, `build_chips`, `build_standings`, `build_leaders`) — the export builders that must be reused rather than reimplemented
    - models/intervals.py (`ARTIFACT`, `load_artifact`, `apply_intervals`)
    - api/main.py (`_fetch_team`, `_free_transfers`, `ft_from_history`) — the exact upstream endpoints and the fields read from each
    - tests/test_api.py (`fake_boot`) — the field-by-field reference for what a trimmed bootstrap must keep
    - config.py (`ROOT`, `RAW_DIR`, `FPL_API`, `CURRENT_SEASON`)
    - web/data/watchlist.json — the price-watch payload that has no export-builder and is copied as-is
    - .gitignore
  </read_first>
  <action>
Freeze one real week as the "normal" set (D-06: a real capture, with real player names and realistic magnitudes, is what a human debugging a failed assertion needs to see; the blank and double variants are derived from this set by script in plan 04-03, never hand-authored).

Create `e2e/scripts/capture_fixtures.py`, a standalone script (module docstring, `from __future__ import annotations`, `argparse`, `sys.path.insert` of the repo root computed from `Path(__file__).resolve().parents[2]` so `import config` works without PYTHONPATH), run it once, and commit its output. Follow the repo's print-status convention (`[capture] …` tags) and its `def main(argv=None) -> int` / `sys.exit(main())` entry-point shape.

Two modes:

`--capture` (default) writes `e2e/fixtures/v1/normal/`:

- Read `data/raw/live/bootstrap-static.json` and `data/raw/live/fixtures.json` directly with `json.load` — never call `fetch_fpl_live`, which re-downloads. Derive `gw = _next_gw(boot)`.
- Write `api/bootstrap-static.json` trimmed to exactly the keys the API reads: top-level `total_players`; `events` entries keeping `id`, `is_next`, `finished`, `deadline_time`; `teams` entries keeping `id`, `name`, `short_name`; `elements` entries keeping `id`, `code`, `web_name`, `team`, `element_type`, `now_cost`, `status`, `chance_of_playing_next_round`, `selected_by_percent`, `ep_next`, `news`. Keep every element and every team — trim fields, never rows.
- Write `api/fixtures.json` trimmed to `event`, `team_h`, `team_a`, `kickoff_time`, `finished`, `team_h_difficulty`, `team_a_difficulty` per fixture, keeping every fixture row (later gameweeks are what `build_horizon_pool` and `build_ticker` walk).
- Load the model artifact with `joblib.load(config.ROOT / "models" / "artifacts" / "xp_model.joblib")`, then for each `g` in `range(gw, gw + 6)` call `predict.live._gw_pool(boot, fixtures, g, artifact)` and write it to `api/pools/gw{g}.json` with `DataFrame.to_json(orient="records")` round-tripped through `json.loads`/`json.dump(indent=1)` so the file is line-diffable. Six files; a gameweek that yields an empty pool still writes its file. JSON, not parquet, deliberately: the blank/DGW synthesis script is a zero-dependency Node script and must be able to read every fixture file.
- Copy `models/artifacts/intervals.json` to `api/intervals.json` verbatim.
- Fetch the three entry endpoints for entry 6980093 exactly once each with `requests.get` and the repo's `_HEADERS` user agent: `/entry/6980093/event/{gw-1}/picks/` to `api/entries/6980093/picks_event{gw-1}.json`, `/entry/6980093/` to `api/entries/6980093/summary.json`, `/entry/6980093/history/` to `api/entries/6980093/history.json`. Apply Task 1's recorded policy to `summary.json` and to nothing else. Trim `picks_event{N}.json` to `picks` (each pick keeping `element`) and `entry_history` (keeping `bank` and `value`); trim `summary.json` to `name`, `player_first_name`, `player_last_name`, `summary_overall_points`, `summary_overall_rank`, `summary_event_points`; trim `history.json` to `chips` (keeping `name`, `event`) and `current` (keeping `event`, `event_transfers`). A non-200 on the picks endpoint is fatal — print the status and return 1.
- Build `web-data/` by calling the real export builders against the captured `boot`/`fixtures` and `pool = build_pool(boot, fixtures, gw, artifact)`, so `/data` and `/api` describe one gameweek universe: `meta.json` from `build_meta(boot, gw, 1)`, `xp_table.json` from `build_table(pool, boot)`, `captains.json` from `build_captains(table)`, `squad.json` from `build_squad(pool)`, `fixtures.json` from `build_ticker(boot, fixtures, gw)`, `chips.json` from `build_chips(fixtures, gw)`, `standings.json` from `build_standings(boot, fixtures)`, `leaders.json` from `build_leaders(boot)`. Copy `web/data/watchlist.json` verbatim — it is produced by `models/price.py`, not by an export builder. Do not write a `scoreboard.json`: its absence is the real production state that `frontend/src/routes/Scoreboard.tsx` distinguishes from a server failure, and the fixture set must preserve it. Do not write a `history/` directory.
- Write `api/capture.json` — the machine-readable capture record the specs import: `fixture_version` ("v1"), `captured_utc` (the capture instant, ISO-8601 with a `Z` suffix), `frozen_now_utc` (identical to `captured_utc` — this is the instant every spec pins the browser clock to), `gw`, `picks_event` (`gw - 1`), `entry` (6980093), `season` from `config.CURRENT_SEASON`, and `pool_gws` (the list of gameweeks with a frozen pool).

`--verify` re-reads the committed set without the model artifact, without network and without pandas model inference, and returns non-zero on any breach: every file listed above exists; `web-data/meta.json`'s `gw` equals `_next_gw` of the committed `api/bootstrap-static.json`; `api/capture.json`'s `gw` matches both; a pool file exists for each of `gw` through `gw + 5` and each parses to a non-empty list whose first record has the keys `player_code`, `name`, `team`, `position`, `price_m`, `xp`, `xp_capt`; `web-data/xp_table.json` is a non-empty list; `api/entries/6980093/picks_event{gw-1}.json` carries exactly 15 picks; and no file anywhere under `e2e/fixtures/v1/` contains the substring `fantasy.premierleague.com`.

Write `e2e/fixtures/v1/MANIFEST.md` (D-08) with: the version and the immutability rule (v1 is never edited in place; a contract change cuts v2 alongside it, because the hardcoded goldens in every spec reference v1 values and mutating v1 invalidates them silently); capture date, gameweek, deadline and the `frozen_now_utc` instant; the provenance of each directory (`api/` from the cached upstream payloads plus three live entry endpoints; `web-data/` regenerated by `predict/export.py`'s own builders from that same capture; `watchlist.json` copied); the exact trimming keep-lists; the verbatim answer recorded in Task 1 and which files it applied to; the reason the pool is JSON rather than parquet; the note that `scoreboard.json` is intentionally absent; and a "Synthesis rules" heading left empty for plan 04-03 to fill.

Add `e2e/test-results/`, `e2e/playwright-report/` and `e2e/blob-report/` to `.gitignore` under a new `# --- Playwright E2E run output ---` heading, and add a line to the "Deliberately NOT ignored" comment block recording that `e2e/fixtures/` is committed on purpose.
  </action>
  <acceptance_criteria>
    - `e2e/scripts/capture_fixtures.py --verify` exits 0 against the committed set.
    - `git ls-files e2e/fixtures/v1 | wc -l` is at least 20, and `git check-ignore -q e2e/fixtures` exits non-zero (the fixture set is tracked).
    - `e2e/fixtures/v1/normal/api/bootstrap-static.json` is under 1,000,000 bytes (the untrimmed upstream payload is ~1.7 MB, so a byte-for-byte copy fails this).
    - `e2e/fixtures/v1/normal/api/pools/` holds exactly 6 files.
    - `e2e/fixtures/v1/normal/web-data/` contains meta.json, xp_table.json, captains.json, squad.json, fixtures.json, chips.json, standings.json, leaders.json and watchlist.json, and contains no scoreboard.json.
    - `e2e/fixtures/v1/MANIFEST.md` contains the strings `frozen_now_utc`, `never edited in place`, `Synthesis rules` and the recorded Task 1 answer.
    - No file under `e2e/fixtures/v1/` contains `fantasy.premierleague.com`.
  </acceptance_criteria>
  <verify>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python e2e/scripts/capture_fixtures.py --verify</automated>
    <fails_when>non-zero exit, or the output names a missing file, a gameweek mismatch between web-data/meta.json and api/bootstrap-static.json, or a pool file whose first record lacks one of the seven required keys</fails_when>
    <automated>test "$(ls e2e/fixtures/v1/normal/api/pools | wc -l)" = 6 &amp;&amp; test ! -e e2e/fixtures/v1/normal/web-data/scoreboard.json &amp;&amp; test "$(stat -c%s e2e/fixtures/v1/normal/api/bootstrap-static.json)" -lt 1000000 &amp;&amp; echo "FIXTURE SHAPE OK"</automated>
    <fails_when>non-zero exit, or `FIXTURE SHAPE OK` is absent from the output</fails_when>
    <automated>! grep -rql "fantasy.premierleague.com" e2e/fixtures/v1/ &amp;&amp; echo "NO UPSTREAM URLS IN FIXTURES"</automated>
    <fails_when>non-zero exit, or `NO UPSTREAM URLS IN FIXTURES` is absent from the output</fails_when>
  </verify>
  <done>The v1 normal fixture set is captured, verifiable offline, tracked in git, and documented by a MANIFEST that records its immutability rule and the Task 1 policy.</done>
</task>

<task type="auto">
  <name>Task 3: Add the FPL_FIXTURE_DIR seam and the SPA build hook to the running stack</name>
  <files>api/main.py, frontend/package.json, tests/test_fixture_mode.py</files>
  <read_first>
    - api/main.py (whole file) — `_initial_state`, `_refresh`, `_with_bands`, `_pool`, `_gw_pools`, `_free_transfers`, `_fetch_team`, and the single `app.mount` at the end
    - tests/conftest.py — the `_reset_api_state` artifact-sentinel precedent this seam generalises from a per-test monkeypatch into a runtime branch
    - tests/test_api.py (`fake_boot`, `fake_pool`, and the `monkeypatch.setattr(m, "_pool", ...)` call sites) — the assertion and mocking style `tests/test_fixture_mode.py` should feel native alongside
    - predict/live.py (`_gw_pool`, `build_pool`, `build_horizon_pool`) — confirm that both builders resolve `_gw_pool` from their own module globals at call time
    - models/intervals.py (`load_artifact`, `apply_intervals`)
    - frontend/package.json — the existing `build` script and the `check:tokens` precedent for a script that runs alongside another
    - scripts/verify_frontend_build.sh — PRE-EXISTING from Phase 3 and NOT modified by this task. Read it to confirm two facts the acceptance criteria rely on: it runs `npm --prefix frontend run build` itself (so the new `postbuild` hook fires through it without any change to the script), and its final line prints exactly `BUILD PURITY OK`. Its dist walk compares basenames against `web/data/*.json`, so a new `dist/404.html` cannot trip the purity check.
    - .planning/phases/04-e2e-regression-suite/04-RESEARCH.md (Pattern 1, Pattern 2, Pitfalls 1-3)
  </read_first>
  <action>
Extend `api/main.py` with a fixture-mode branch at every outbound-data call site (D-01: one real full-stack process, fixture-fed — the real FastAPI code paths, cache and ILP all still run, only the data source moves to disk). Add `import pandas as pd`, `from pathlib import Path`, and `import predict.live as live` alongside the existing imports.

Resolve the seam once, at module scope, just after the existing `_HEADERS` constant: `_FIXTURE_ROOT` from `FPL_FIXTURE_DIR` (an absolute `Path`, or `None` when unset), `_FIXTURE_API` as `_FIXTURE_ROOT / "api"`, and `_FIXTURE_DATA` from the optional `FPL_FIXTURE_DATA_DIR` override falling back to `_FIXTURE_ROOT / "web-data"`. The second variable exists so a blank/DGW server can reuse one API-side capture while serving a different `/data` set (plan 04-03). When `_FIXTURE_ROOT` is set, print a startup line tagged `[fixture-mode]` naming both resolved directories — a real deploy that ever picks this env var up must say so in its logs (threat T-04-01). Add a small `_fixture_json(*parts)` helper that loads a JSON file under `_FIXTURE_API`.

Branch these, and only these:

- `_load_live`: define `_load_live_fixture(force: bool = True)` returning the tuple parsed from `bootstrap-static.json` and `fixtures.json`, and rebind the module-level name `_load_live` to it when `_FIXTURE_ROOT` is set. This is what stops `predict.live._load_live`'s unconditional `fetch_fpl_live` download (Pitfall 1).
- `_gw_pool`: define `_gw_pool_fixture(boot, fixtures, gw, artifact)` with the same four-argument signature, reading `pools/gw{gw}.json` into a DataFrame and raising `HTTPException(503, ...)` naming the gameweek when the file is absent. When `_FIXTURE_ROOT` is set, rebind BOTH `api.main`'s own `_gw_pool` name and `live._gw_pool`, because `build_pool` and `build_horizon_pool` resolve that name from `predict.live`'s module globals. Rebinding the single model-inference leaf — rather than short-circuiting `_pool`/`_gw_pools` — is deliberate: the real aggregation, the real `xp_next` handling and the real `decay` horizon math keep running over frozen inputs, so E2E covers them (D-10 freezes the inputs, not the solver).
- `_with_bands`: replace its direct `intervals.load_artifact()` call with a new `_intervals_artifact()` that returns `_fixture_json("intervals.json")` in fixture mode and `intervals.load_artifact()` otherwise. Without this the p10/p90 bands would appear on a developer machine (where `models/artifacts/intervals.json` exists) and vanish in CI (where it does not) — a machine-dependent response body, which is the same class of defect as a calendar-dependent one.
- `_refresh`: the cold-start guard assigns a non-`None` sentinel string in fixture mode instead of calling `joblib.load` (Pitfall 3 — that artifact is gitignored and absent on a clean checkout).
- `_free_transfers` and `_fetch_team`: do not branch the functions themselves. Extract their three `requests.get` calls into three thin helpers — one for `/entry/{entry}/history/`, one for `/entry/{entry}/event/{event}/picks/`, one for `/entry/{entry}/` — each of which returns the parsed body (or `None`/a 404 signal) exactly as the inline call did today, and each of which reads `entries/{entry}/history.json`, `entries/{entry}/picks_event{event}.json` or `entries/{entry}/summary.json` in fixture mode. A missing picks file must produce the identical `HTTPException(404, ...)` message the live 404 path produces, and a missing history or summary file must degrade to the same `None`/best-effort behaviour as a `requests.RequestException`, so an unknown entry id exercises the same error path in both modes. All pick-to-squad normalisation, the manager sub-object assembly and `ft_from_history` stay untouched.
- The static mount (D-02: the same seam switches the site root from the vanilla `web/` directory to the built React app, and D-04: no `FPL_API_KEYS` is ever set in fixture mode, so `require_key` stays in its open pass-through — the keyed modes are already covered by Phase 1's pytest suite): leave the production path exactly as it is — one `app.mount("/", StaticFiles(directory=config.ROOT / "web", html=True), name="site")`. Add an `if _FIXTURE_ROOT:` branch before it that instead mounts `_FIXTURE_DATA` at `/data` and `config.ROOT / "frontend" / "dist"` at `/` with `html=True`, both with `check_dir=False`. `/data` must be registered first — Starlette matches mounts in registration order and the catch-all would otherwise swallow JSON requests and answer them with `404.html`. `check_dir=False` lets a fixture-mode server boot before the frontend build has finished, which is what allows plan 04-03's extra servers to start in parallel with the one that owns the build. No custom middleware and no catch-all route: `StaticFiles(html=True)` already serves `404.html` for any unmatched path, which is the whole SPA fallback.

In `frontend/package.json`, add a `postbuild` script that copies `dist/index.html` to `dist/404.html`. npm runs `post<script>` automatically after `<script>`, so `npm --prefix frontend run build` and `bash scripts/verify_frontend_build.sh` both produce it with no call-site change.

`scripts/verify_frontend_build.sh` already exists from Phase 3 and is deliberately NOT modified by this task — it is not in `files_modified` for that reason. It is reused unchanged because it already shells out to `npm --prefix frontend run build` itself, which is exactly what makes the new `postbuild` hook fire through it; and because its purity walk matches dist basenames against `web/data/*.json`, a newly emitted `dist/404.html` cannot trip it. Do not add the 404 check into that script: the SPA fallback and the build-purity gate are separate concerns, and the `cmp` in this task's verify block asserts the 404 invariant directly. If the build ever stops producing `dist/404.html`, that `cmp` is what fails, not the purity gate.

Create `tests/test_fixture_mode.py` — a pytest module that proves the seam in-process, in the style of `tests/test_api.py`. Point `FPL_FIXTURE_DIR` at `e2e/fixtures/v1/normal` with `monkeypatch.setenv`, `importlib.reload` `api.main`, and drive a `TestClient` while `requests.get` is monkeypatched to raise and `joblib.load` is monkeypatched to raise. Cover: `/api/health` and `/api/meta` answer with the fixture's gameweek and season; `/api/team/6980093` returns 15 picks plus a `manager` object; `/api/team/1` returns 404; `/api/solve` with that entry returns a `transfers` response whose `squad` has 15 rows, 11 starters, exactly 1 captain and a 2/5/5/3 position split; `/api/rate/6980093` returns a `score` no greater than 100 and a 15-row `xi`; `/api/plan` at `horizon=2` returns 2 weeks; `/data/xp_table.json` serves the frozen table; and a request for the client-side route `/team` returns HTML (the `404.html` fallback) rather than a JSON error. Add one test that reloads `api.main` with the env var *absent* and asserts the app has exactly one mount whose path is `/` — the production default is unchanged. Reload `api.main` once more at teardown so the rest of the pytest suite sees the unset-env module.
  </action>
  <acceptance_criteria>
    - `pytest tests/test_fixture_mode.py` passes, with `requests.get` and `joblib.load` both patched to raise for the duration of every fixture-mode test.
    - `pytest -q` (the whole existing suite) still passes, proving the seam did not disturb `tests/test_api.py`'s monkeypatch-based tests.
    - `grep -c "FPL_FIXTURE_DIR" api/main.py` is at least 1 and `grep -c "FPL_FIXTURE_DATA_DIR" api/main.py` is at least 1.
    - `api/main.py` contains exactly three `requests.get(` call sites, all inside the three extracted entry helpers.
    - `bash scripts/verify_frontend_build.sh` — the pre-existing Phase 3 script, run unmodified — still prints its `BUILD PURITY OK` final line, and because it invokes `npm --prefix frontend run build` internally the new `postbuild` hook leaves `frontend/dist/404.html` byte-identical to `frontend/dist/index.html`.
    - `git diff --exit-code -- scripts/verify_frontend_build.sh` is clean: this task changes `frontend/package.json`, not the verify script.
    - With the env var unset, `api/main.py`'s mount list has one entry at `/`.
  </acceptance_criteria>
  <verify>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m pytest tests/test_fixture_mode.py -x -q</automated>
    <fails_when>non-zero exit, or the summary line reports 0 tests collected</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m pytest -q</automated>
    <fails_when>non-zero exit, or the summary reports any failure or error</fails_when>
    <automated>bash scripts/verify_frontend_build.sh &amp;&amp; cmp frontend/dist/index.html frontend/dist/404.html &amp;&amp; echo "SPA FALLBACK OK"</automated>
    <fails_when>non-zero exit, `BUILD PURITY OK` missing from the output, or `SPA FALLBACK OK` absent — the latter means the postbuild hook did not emit a 404.html matching index.html, so SPA deep links would 404 in fixture mode</fails_when>
    <automated>git diff --exit-code -- scripts/verify_frontend_build.sh &amp;&amp; echo "VERIFY SCRIPT UNTOUCHED"</automated>
    <fails_when>non-zero exit, or `VERIFY SCRIPT UNTOUCHED` absent — the pre-existing Phase 3 build-purity gate was edited, which this task must not do</fails_when>
    <automated>test "$(grep -c 'requests.get(' api/main.py)" = 3 &amp;&amp; echo "NETWORK CALL SITES ENUMERATED"</automated>
    <fails_when>non-zero exit, or `NETWORK CALL SITES ENUMERATED` absent — a fourth call site means an unbranched path to the live FPL API survives</fails_when>
  </verify>
  <done>A uvicorn process started with FPL_FIXTURE_DIR set answers every endpoint from disk, runs the real ILP, serves the built SPA with working deep links, and makes no network call; with the variable unset nothing about production behaviour has changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process env → API data source | `FPL_FIXTURE_DIR` decides whether real users are served live or frozen data |
| filesystem → API responses | committed JSON becomes API response bodies verbatim |
| upstream FPL API → committed git history | third-party personal data crosses into a permanent public record |
| browser → static mount | unmatched paths fall through to the SPA `404.html` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | `api/main.py` fixture-mode branch | high | mitigate | Production default is unchanged (single `web/` mount, live calls); fixture mode prints a `[fixture-mode]` startup banner naming both directories; `tests/test_fixture_mode.py` asserts the unset-env app still has exactly one `/` mount; MANIFEST and the phase prohibition record that no deploy config may set the variable |
| T-04-02 | Information Disclosure | `e2e/fixtures/v1/normal/api/entries/6980093/summary.json` | high | mitigate | Blocking `checkpoint:decision` (Task 1) decides the manager-field policy before any capture runs; the recorded answer is written into MANIFEST.md; default recommendation scrubs the two free-text name fields |
| T-04-03 | Tampering | entry-scoped fixture file paths | low | accept | The entry id reaching the file path is a FastAPI-coerced `int` path parameter, so no traversal sequence can survive; gameweek is derived server-side from the captured bootstrap |
| T-04-04 | Spoofing | `dist/404.html` SPA fallback | low | accept | The fallback serves the app shell with a 404 status for unmatched paths; it exposes no new data and Starlette's own `html=True` branch is the mechanism, not custom routing. Recorded as a Phase 7 follow-up: the cutover may want a 200 for real client routes |
| T-04-05 | Information Disclosure | committed frozen pool and export JSON | low | accept | All values are already published on the public site's own `web/data/*.json`; the capture adds no non-public field |
</threat_model>

<flagged_assumptions>
- **Edge probe E2E-01 / concurrency (`unresolved` at planning time, resolved here):** "If interrupted or run in parallel, what is guaranteed?" — resolved explicitly in this plan's key_links and in plan 04-02's must_haves: one uvicorn process serves the whole Playwright run, so `_state["pools"]` and `_solve_cache` persist across specs. That is safe because the frozen pool never changes mid-run and the solve cache key is a sha1 over the full request body plus gameweek, so two specs collide only if they send byte-identical bodies.
- The capture reads `data/raw/live/*.json` as cached on the executing machine. If those files are older or newer than `web/data/*.json`, the `--verify` gameweek equality check is what catches the incoherence — it is a hard failure, not a warning.
</flagged_assumptions>

<artifacts_this_phase_produces>
New symbols and paths created by this plan (none of these exist yet; do not treat them as drift):

- Files: `e2e/scripts/capture_fixtures.py`, `e2e/fixtures/v1/MANIFEST.md`, `e2e/fixtures/v1/normal/api/{bootstrap-static.json,fixtures.json,intervals.json,capture.json}`, `e2e/fixtures/v1/normal/api/pools/gw{N}.json`, `e2e/fixtures/v1/normal/api/entries/6980093/{picks_event{N}.json,summary.json,history.json}`, `e2e/fixtures/v1/normal/web-data/*.json`, `tests/test_fixture_mode.py`
- Environment variables: `FPL_FIXTURE_DIR`, `FPL_FIXTURE_DATA_DIR`
- New `api/main.py` module-level names: `_FIXTURE_ROOT`, `_FIXTURE_API`, `_FIXTURE_DATA`, `_fixture_json`, `_load_live_fixture`, `_gw_pool_fixture`, `_intervals_artifact`, and the three extracted entry-endpoint helpers
- New `frontend/package.json` script: `postbuild`
- New build artifact: `frontend/dist/404.html`
- New JSON keys in `capture.json`: `fixture_version`, `captured_utc`, `frozen_now_utc`, `gw`, `picks_event`, `entry`, `season`, `pool_gws`
</artifacts_this_phase_produces>

<verification>
- `python e2e/scripts/capture_fixtures.py --verify` exits 0 from a clean checkout with no network and no model artifact.
- `pytest -q` is green, including the new `tests/test_fixture_mode.py`.
- `bash scripts/verify_frontend_build.sh` is green and `frontend/dist/404.html` matches `frontend/dist/index.html`.
- `git diff` on `api/main.py` shows the production mount statement unchanged.
</verification>

<success_criteria>
A uvicorn process launched with `FPL_FIXTURE_DIR=e2e/fixtures/v1/normal` serves the whole product — API and site — from committed data, runs the real ILP, and touches neither the network nor a model artifact; with the variable unset, `api/main.py` behaves byte-for-byte as before.
</success_criteria>

<output>
Create `.planning/phases/04-e2e-regression-suite/04-01-SUMMARY.md` when done, recording the verbatim Task 1 answer, the captured gameweek and `frozen_now_utc`, and any deviation from the trimming keep-lists.
</output>
