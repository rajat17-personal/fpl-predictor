---
phase: 04-e2e-regression-suite
plan: 04
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - e2e/specs/xp-table.spec.ts
autonomous: true
requirements: [E2E-03]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The xP table renders the exact cell strings of the frozen capture's top rows — position, player, team code, price, ownership, the band cell's numbers and captain xP — not merely 'a table appeared'."
    - "Exactly 50 body rows render for the frozen capture, in the pipeline's own descending-xP order, with no client-side re-sort applied before the slice."
    - "Clicking a column header once sorts ascending with nulls at the top and shows the down glyph; clicking the same header again sorts descending with nulls at the bottom and shows the up glyph — the vanilla polarity, preserved."
    - "Rows whose sort key compares equal keep their pre-sort relative order (stable sort), and a null numeric key sorts as the lowest value rather than being dropped."
    - "The captain picks sub-table renders exactly five rows using the full club name, and reproduces the deliberate literal 'undefined' ownership string for a null value rather than the main table's en-dash."
    - "Position chips and the search box filter strictly within the already-sliced top 50, and search matches the full club name as well as the short code."
    - "A filter combination matching nothing renders the 'No players match' empty block instead of an empty table."
    - "A flagged player's status flag is reachable by its accessible name and reveals its news text on activation."
  artifacts:
    - e2e/specs/xp-table.spec.ts
  key_links:
    - "frontend/src/lib/sortable.ts's sortRows comparator is the single source of the counter-intuitive dir polarity; the spec asserts the rendered consequence, never re-implements the comparator."
    - "The top-50 slice happens before filtering, so every filter assertion is scoped to those 50 rows and not to the full frozen table."
    - "The captain table's ownership cell and the main table's Own % cell are deliberately different code paths; asserting both in one spec is what keeps the parity deviation from being quietly unified."
  prohibitions:
    - "Expected rendered values must never be re-derived from fixture JSON at test time; they are hand-derived hardcoded literals (D-15)."
    - "The spec must not import frontend/src/lib/format.ts or sortable.ts — re-using the code under test to compute expectations lets a shared bug pass silently."
    - "Fixed-duration sleep waits are forbidden."
    - "Screenshot or visual-regression baselines are forbidden."
    - "No spec may assert time-dependent copy without first pinning the browser clock and timezone."
    - "Playwright route() mocking of /api/* is forbidden."
---

<objective>
Cover the flagship page — the xP table and its captain picks sub-table — with assertions on
exact cell values, exact row order, and the sort/filter semantics ported verbatim from the
vanilla site.

Purpose: E2E-03. Phase 2 enumerated 41 parity rules for this page and locked them in jsdom;
this plan proves them in a real browser against frozen data, so a formatting or ordering
regression fails on code rather than on someone noticing.
Output: `e2e/specs/xp-table.spec.ts`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-e2e-regression-suite/04-CONTEXT.md
@.planning/phases/04-e2e-regression-suite/04-PATTERNS.md
@.planning/phases/04-e2e-regression-suite/04-02-SUMMARY.md
@.planning/phases/02-data-layer-non-pitch-pages/02-CONTEXT.md
@.planning/phases/02-data-layer-non-pitch-pages/PARITY-DEVIATIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Exact cell values and row order for both tables</name>
  <files>e2e/specs/xp-table.spec.ts</files>
  <read_first>
    - frontend/src/routes/XpTable.tsx — the COLUMNS order, the top-50 slice with no re-sort (R10), the maxHi derivation (R11), the two table aria-labels, and the captains sub-table's R17/R18/R19 comments
    - frontend/src/components/BandCell.tsx and frontend/src/lib/bandCell.ts — what the xP band cell renders and what its accessible description says
    - frontend/src/lib/statusFlag.tsx — the status flag's trigger name and revealed news text
    - frontend/src/lib/format.ts — fixed1, fixed2, orDash semantics (read to hand-derive expectations once; do not import into the spec)
    - e2e/fixtures/v1/normal/web-data/xp_table.json and captains.json — the source of every literal
    - e2e/helpers/page.ts — gotoReady
  </read_first>
  <action>
Create `e2e/specs/xp-table.spec.ts` and cover the rendered content of `/` against the normal server. Navigate with `gotoReady` so the clock and fonts are settled. Every expectation is a hardcoded literal hand-derived once from the committed fixture; record the derivation in the plan SUMMARY so later plans and Phase 7 reuse it.

- Assert the page heading and the intro paragraph's linked accuracy-scoreboard text.
- Scope every row assertion to the table with aria-label `xP table`, so the captain sub-table's overlapping player names can never satisfy them by accident.
- Assert the seven column header labels in order: Pos, Player, Team, £m, Own %, the xP band header, Captain xP.
- Assert the body row count is exactly 50 for the frozen capture.
- Assert the first three body rows cell by cell: position, player name, team short code, price to one decimal, ownership to one decimal (or the en-dash when the frozen value is null), the band cell's low and high numbers, and captain xP to two decimals (or the en-dash when null).
- Assert the last body row's player name, which pins the top-50 slice boundary: the pipeline pre-sorts descending by xP and the page applies no re-sort before slicing, so this row is a direct consequence of that rule.
- Assert the "Showing the top 50 by xP" paragraph and the "Reading the numbers" paragraph are present.
- Scope the captain assertions to the table with aria-label `Captain picks`: assert exactly five body rows; assert row one's player name, the FULL club name (this column deliberately differs from the main table's short code), price, ownership and captain xP. If any of the five frozen captain rows has a null ownership, assert the literal string `undefined` in that cell — that is vanilla's un-guarded formatting, reproduced on purpose and recorded in the parity deviations ledger; an en-dash there would be the regression.
- If the frozen capture contains a player with a non-available status, assert its status flag is reachable by accessible name inside the player cell and that activating it reveals the frozen news text. If no such player exists in the capture, record that in the SUMMARY and skip only that assertion.
  </action>
  <acceptance_criteria>
    - `npm --prefix e2e run test -- specs/xp-table.spec.ts --project=chromium` passes.
    - The spec asserts a body row count of exactly 50 in the `xP table`.
    - The spec asserts exactly five rows in the `Captain picks` table.
    - Both table locators are scoped by aria-label, never by a bare `table` selector.
    - The spec contains no import from `frontend/src`.
    - The spec contains no screenshot comparison and no fixed-duration sleep.
  </acceptance_criteria>
  <verify>
    <automated>E2E_PYTHON=/home/sraja/miniconda3/envs/python314/bin/python E2E_VARIANTS=0 E2E_PORT=8120 npm --prefix e2e run test -- specs/xp-table.spec.ts --project=chromium</automated>
    <fails_when>non-zero exit, or the reporter summary reports 0 tests run</fails_when>
    <automated>node -e "const s=require('fs').readFileSync('e2e/specs/xp-table.spec.ts','utf8');if(/from\s+[\x27\x22].*frontend\/src/.test(s))throw new Error('spec imports the code under test');for(const t of ['xP table','Captain picks'])if(!s.includes(t))throw new Error('missing table scope: '+t);console.log('TABLE SCOPING OK')"</automated>
    <fails_when>non-zero exit, or `TABLE SCOPING OK` absent from the output</fails_when>
  </verify>
  <done>Both tables assert exact frozen cell strings and exact row counts, including the deliberate ownership-formatting divergence between them.</done>
</task>

<task type="auto">
  <name>Task 2: Sort semantics, ties and null keys</name>
  <files>e2e/specs/xp-table.spec.ts</files>
  <read_first>
    - frontend/src/lib/sortable.ts — sortRows' comparator, its documented polarity inversion, sortDirGlyph, and useSortable's fresh-click/repeat-click direction rule
    - frontend/src/routes/XpTable.tsx — NUMERIC_KEYS, the header buttons, and where the glyph renders
    - e2e/fixtures/v1/normal/web-data/xp_table.json — to hand-derive the expected orders, including at least one pair of rows whose price or ownership compares equal, and at least one row with a null ownership or null captain xP
  </read_first>
  <action>
Extend `e2e/specs/xp-table.spec.ts` with the ordering contract. Assert rendered consequences only — never re-implement the comparator in the spec.

- First click on the `£m` header: assert the header shows the down glyph, and assert the exact ordered list of the first five player names. The polarity is counter-intuitive and deliberate — a fresh click sorts ASCENDING with nulls at the TOP. Hand-derive the expected order from the frozen top 50 once.
- Second click on the same header: assert the up glyph and the exact ordered first five names, now DESCENDING with nulls at the BOTTOM.
- Clicking a different header resets to the fresh-click direction: assert the down glyph appears on the newly clicked header and no glyph remains on the previous one.
- **Adjacency and ordering (edge coverage):** pick two rows from the frozen capture whose sort key value is exactly equal — the same price, or the same ownership — and note their relative order in the unsorted (pipeline) rendering. After sorting on that key, assert those two names still appear in that same relative order. Equal keys must neither merge nor swap: the comparator returns 0 and the sort is stable, so ties preserve input order. Assert this in both directions.
- **Null keys (edge coverage):** sort on a numeric column that has at least one null in the frozen top 50. Assert the null-valued row is present in both directions — first at the top on the fresh click, then at the bottom on the repeat click. A null must sort as the lowest value, never be filtered out of the rendering.
- Sort on a text column (`Player` or `Team`) and assert the first and last names of the rendered order in both directions, so the locale-compare branch of the comparator is covered too.
  </action>
  <acceptance_criteria>
    - `npm --prefix e2e run test -- specs/xp-table.spec.ts --project=chromium` passes.
    - The spec asserts both sort glyphs and both directions for at least one numeric and one text column.
    - The spec asserts a stable tie order for two rows with an equal sort key, in both directions.
    - The spec asserts a null-keyed row appears at the top on the fresh click and at the bottom on the repeat click, and that the total row count is unchanged by sorting.
    - The spec derives no expected order by calling a comparator; every expected order is a literal list.
  </acceptance_criteria>
  <verify>
    <automated>E2E_PYTHON=/home/sraja/miniconda3/envs/python314/bin/python E2E_VARIANTS=0 E2E_PORT=8120 npm --prefix e2e run test -- specs/xp-table.spec.ts --project=chromium</automated>
    <fails_when>non-zero exit, or the reporter summary reports fewer tests than the file declares</fails_when>
    <automated>node -e "const s=require('fs').readFileSync('e2e/specs/xp-table.spec.ts','utf8');for(const t of ['▼','▲'])if(!s.includes(t))throw new Error('sort glyph not asserted: '+t);if(/\.sort\(/.test(s))throw new Error('spec computes its own ordering instead of asserting a literal list');console.log('SORT CONTRACT ASSERTED')"</automated>
    <fails_when>non-zero exit, or `SORT CONTRACT ASSERTED` absent from the output</fails_when>
  </verify>
  <done>The vanilla sort polarity, tie stability and null-key placement are locked by rendered-order assertions in both directions.</done>
</task>

<task type="auto">
  <name>Task 3: Filters, the empty state, and filter/sort interaction</name>
  <files>e2e/specs/xp-table.spec.ts</files>
  <read_first>
    - frontend/src/routes/XpTable.tsx — POSITIONS, the aria-pressed chips, the search input's accessible name, the R12/R13 filter-on-top-of-the-slice rule, the search matching `team` as well as `team_short`, and the "No players match" block
    - frontend/src/components/EmptyState.tsx — the empty-data block that is NOT the same as the no-match block
    - e2e/fixtures/v1/normal/web-data/xp_table.json — to hand-derive counts per position within the top 50 and a club whose full name differs from its short code
  </read_first>
  <action>
Complete `e2e/specs/xp-table.spec.ts` with the filter contract and the empty-result edge.

- Assert the five position chips render with the labels All, GK, DEF, MID, FWD and that All starts pressed.
- Click each of GK, DEF, MID and FWD in turn and assert the exact rendered row count for each — hand-derived from the frozen top 50, not from the full table. This is the R12 rule: filters run on top of the already-sliced 50, never against the full fetched array, so the four counts must sum to 50.
- Type a club's FULL name into the search box (choose a club whose full name is not a substring of its short code) and assert the resulting row count and that every rendered team cell shows that club's short code. The full club name is never rendered in any cell on this page, so a search that works on it can only be passing through the R13 match on the `team` field.
- **Empty result (edge coverage):** type a search term present in no row and assert the `No players match` heading and its follow-up copy render, that the `xP table` table is not in the document, and that the position chips and search box are still present so the user can recover. Then clear the search and assert the 50 rows return. Also assert this no-match block is distinct from the page-level empty-data block: the latter belongs to an empty fetch, not an empty filter.
- Assert filter and sort compose: apply a position filter, then sort on a numeric column, and assert the exact ordered names — proving the sort runs over the filtered subset, not the unfiltered 50.
  </action>
  <acceptance_criteria>
    - `npm --prefix e2e run test -- specs/xp-table.spec.ts --project=chromium` passes with every test in the file green.
    - The four position-filter row counts asserted in the spec sum to exactly 50.
    - The spec asserts a search on a full club name returns rows whose team cells show that club's short code.
    - The spec asserts the `No players match` block appears for an unmatched search and that the table element is absent at that moment.
    - The spec asserts the row count returns to 50 after clearing the search.
    - The spec asserts one filter-then-sort composition with a literal expected order.
  </acceptance_criteria>
  <verify>
    <automated>E2E_PYTHON=/home/sraja/miniconda3/envs/python314/bin/python E2E_VARIANTS=0 E2E_PORT=8120 npm --prefix e2e run test -- specs/xp-table.spec.ts --project=chromium</automated>
    <fails_when>non-zero exit, or the reporter summary reports any failed or flaky test</fails_when>
    <automated>node -e "const s=require('fs').readFileSync('e2e/specs/xp-table.spec.ts','utf8');for(const t of ['No players match','GK','DEF','MID','FWD'])if(!s.includes(t))throw new Error('missing filter coverage: '+t);console.log('FILTER COVERAGE OK')"</automated>
    <fails_when>non-zero exit, or `FILTER COVERAGE OK` absent from the output</fails_when>
    <automated>E2E_PYTHON=/home/sraja/miniconda3/envs/python314/bin/python E2E_VARIANTS=0 E2E_PORT=8120 npm --prefix e2e run test -- --project=chromium</automated>
    <fails_when>non-zero exit, or the reporter reports any failed or flaky test across the non-variant suite</fails_when>
  </verify>
  <done>Position and search filtering, their interaction with sorting, and the empty-result state are all covered against the frozen top 50.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frozen `/data/xp_table.json` → rendered DOM | untrusted-shaped JSON becomes page content, including a free-text `news` field |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-13 | Tampering | player `news` and `name` fields rendered into the DOM | low | accept | React escapes text children by default and this page uses no HTML-injecting API; the frozen capture contains no markup, and the status-flag assertion reads the rendered text rather than any raw HTML |
| T-04-14 | Tampering | a formatting or ordering regression shipping unnoticed | medium | mitigate | Exact-literal cell assertions plus both sort directions, tie stability and null placement; a silent change to `sortRows` or `format.ts` fails this spec |
| T-04-15 | Information Disclosure | none introduced | low | accept | The page renders only already-public export data |
</threat_model>

<flagged_assumptions>
- **Edge probe E2E-03 / adjacency, empty, ordering — resolved explicitly.** Adjacency and ordering are covered by the tie-stability assertions in Task 2 (equal keys neither merge nor swap; the comparator returns 0 and the sort is stable). Empty is covered by Task 3's unmatched-search assertions plus the explicit distinction between the no-match block and the page-level empty-data block. The single-element case is not reachable from this frozen capture and is not asserted.
- The frozen capture is assumed to contain at least one price or ownership tie and at least one null numeric value within the top 50. If it does not, record that in the SUMMARY and cover the tie/null cases through a position filter that narrows to rows that do — do not mutate the fixture.
</flagged_assumptions>

<artifacts_this_phase_produces>
New paths created by this plan (none exist yet; not drift):

- File: `e2e/specs/xp-table.spec.ts`
</artifacts_this_phase_produces>

<verification>
- `npm --prefix e2e run test -- specs/xp-table.spec.ts --project=chromium` is green.
- The non-variant suite as a whole is still green after this plan.
- The spec imports nothing from `frontend/src`.
</verification>

<success_criteria>
The xP table and captain picks are covered by exact cell values, exact row counts, both sort
directions with tie and null behaviour, and every filter path including the empty result —
all against the immutable v1 capture.
</success_criteria>

<output>
Create `.planning/phases/04-e2e-regression-suite/04-04-SUMMARY.md` when done, recording the hand-derived literals (first three rows, last row, captain rows, per-position counts) so Phase 7's parity pass can reuse them.
</output>
