---
phase: 04-e2e-regression-suite
plan: 08
type: execute
wave: 2
# Sequenced behind 04-07 despite zero files_modified overlap: this plan's gate is a real
# Playwright run whose webServer boots `api.main` — the very module 04-07 edits. Running both
# in one wave risks the E2E harness importing a half-written api/main.py and reporting a
# failure misattributed to the ghost-row change. The ordering also buys a free integration
# check of 04-07's restore under the real fixture-mode server.
depends_on: ["04-07"]
files_modified:
  - frontend/src/components/pitch/Pitch.tsx
  - frontend/src/components/RateDiff.tsx
  - frontend/src/components/pitch/Pitch.test.tsx
  - frontend/src/components/RateDiff.test.tsx
  - e2e/specs/rate-my-team.spec.ts
  - .planning/WINDOWS.md
  - .planning/phases/04-e2e-regression-suite/deferred-items.md
autonomous: true
gap_closure: true
requirements: [E2E-04]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "For the real, immutable v1 capture's own best_move (sell Mateta — starting:false — buy Wissa), the suggested-incoming ghost card renders inside the same role=\"group\" container as the outgoing player's dimmed card: the container labelled Bench."
    - "Within that Bench row group, the ghost cell is the immediate next sibling of the outgoing player's cell (ghost index === outgoing index + 1)."
    - "When the sell target resolves to exactly one xi row and that row is a starter, the ghost still renders in that starter's own formation row — the starting-sell behavior that already worked is preserved, not traded for the bench fix."
    - "When the sell name resolves to zero or to multiple xi rows, the ghost falls back to the buy's position row with afterCode null — no ghost that renders today stops rendering."
    - "E2E-04 boundary edge (probe-resolved, explicit): after the ghost-row change the Team score tile still reads as an integer over /100 — literally 83/100 for this capture — and the API's min(score, 100) clamp is still structurally asserted."
    - "E2E-04 precision edge (probe-resolved, explicit): after the ghost-row change the Team score tile detail still reads exactly `best XI xP 26.67 (12.6–40.7 in 8/10 GWs) vs ideal 32.21`."
    - "WINDOWS.md id=2 is status fixed with a resolved_at timestamp, and open_count drops to 1 (id=1, the typecheck no-op, is untouched by this plan)."
    - statement: "E2E-03 adjacency edge — e2e/specs/xp-table.spec.ts's exact cell literals for adjacent rows 1-3 plus row 50 remain the standing acceptance criterion (04-VERIFICATION.md truth 3, VERIFIED); this plan changes nothing it covers."
      verification: backstop
    - statement: "E2E-03 empty edge — xp-table.spec.ts's empty-result state assertion remains the standing acceptance criterion (04-VERIFICATION.md truth 3, VERIFIED)."
      verification: backstop
    - statement: "E2E-03 ordering edge — xp-table.spec.ts's both-sort-directions coverage with 2 independent tie-stability proofs remains the standing acceptance criterion (04-VERIFICATION.md truth 3, VERIFIED)."
      verification: backstop
  prohibitions:
    - "The ghost card must never render in a row group that does not contain the outgoing player's card, when the sell target resolves to exactly one xi row."
    - "The ghost row must never be derived from the buy's position when the sell target resolves to exactly one xi row."
    - "e2e/fixtures/v1 must never be edited to force a starting-player sell."
    - "e2e/specs/rate-my-team.spec.ts must never intercept /api/* with page.route()."
    - "Expected literals must never be re-derived from fixture JSON at test time inside the formatting logic under test."
    - "No spec may be weakened to pass — an assertion may only change because the rendered behavior it describes deliberately changed in this plan."
  artifacts:
    - frontend/src/components/pitch/Pitch.tsx
    - frontend/src/components/RateDiff.tsx
    - frontend/src/components/pitch/Pitch.test.tsx
    - frontend/src/components/RateDiff.test.tsx
    - e2e/specs/rate-my-team.spec.ts
    - .planning/WINDOWS.md
    - .planning/phases/04-e2e-regression-suite/deferred-items.md
  key_links:
    - "RateDiff.resolveRateOverlay's sell-target row derivation -> PitchGhost.row -> Pitch's rowGhost(\"BENCH\") -> the Bench PitchRow's buildRowSlots insertion at afterCode"
    - "e2e/specs/rate-my-team.spec.ts Bench-row assertions -> the real GET /api/rate/6980093 response over the frozen GW3 pool (no route mocking)"
    - "WINDOWS.md id=2 + deferred-items.md entry -> this plan's Pitch.tsx/RateDiff.tsx fix (ledger closure)"

flagged_assumptions:
  - "E2E-04 boundary, unexercisable half: a rating scoring exactly 100 cannot be produced by the immutable v1 capture. Documented, not forced — mutating v1 is prohibited (D-08)."
  - "E2E-04 precision, unexercisable half: the xi_p10-null branch (interval clause wholly absent) is not reachable on this capture. Documented, not forced."
  - "Derived-not-measured: after the fix the Forwards row should hold 2 cells and the Bench row 5 (formation 4-4-2 + 4 bench + 1 ghost). This is arithmetic from the spec's own asserted 4-4-2, NOT an empirical reading — Task 1 must confirm the real numbers from the live run before pinning them (D-15 hand-derivation rule)."
---

<objective>
Close verification Gap 2: `frontend/src/components/pitch/Pitch.tsx` inserts the suggested-transfer
ghost card into the row matching the BUY's position, never the row the SELL target actually
occupies. For the real v1 capture's own best_move — which sells Mateta, a benched player — the
ghost lands in the Forwards row while the dimmed outgoing card and its "Suggested transfer out"
label land in the Bench row, falsifying plan 04-06's must-have that the ghost renders in the same
formation row as the outgoing player.

The user chose to FIX this rather than waive WINDOWS.md id=2. Selling a worthless bench player is
an ordinary "best move", so the diff's core promise ("the incoming ghost sits where the outgoing
player was") must hold for bench sells too.

Purpose: make D-18's single-pitch visual diff truthful for the most common real suggestion shape,
and clear the last open item blocking this phase (WINDOWS.md id=2 blocks `/gsd-ship` under
`windows_enforce`).

Output: a ghost mechanism keyed off the sell target's actual row (including Bench), unit tests
covering both the bench and starter cases, an updated E2E spec asserting the corrected same-row
behavior against the unchanged frozen capture (D-08), and both ledger entries closed.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@frontend/src/components/pitch/Pitch.tsx
@frontend/src/components/RateDiff.tsx
@frontend/src/lib/formation.ts
@e2e/specs/rate-my-team.spec.ts
@.planning/phases/04-e2e-regression-suite/04-VERIFICATION.md
@.planning/phases/04-e2e-regression-suite/deferred-items.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Key the ghost row off the sell target's actual row, proven end to end against the real capture</name>
  <files>frontend/src/components/pitch/Pitch.tsx, frontend/src/components/RateDiff.tsx, e2e/specs/rate-my-team.spec.ts</files>

  <precondition>
    Plan 04-07 is complete and committed — this task's gate boots `api.main` through Playwright's
    webServer, so a half-applied 04-07 edit would surface here as an unrelated failure. Confirm with
    `/home/sraja/miniconda3/envs/python314/bin/python -m pytest tests/test_fixture_mode.py -q`
    reporting `10 passed`. Also required (installed by 04-02, not by this plan): `e2e/node_modules`
    with `@playwright/test@1.62.1` and the Chromium browser binary. Local runs must pass
    `E2E_PYTHON=/home/sraja/miniconda3/envs/python314/bin/python` — the config defaults to bare
    `python`, which is not this project's interpreter.
  </precondition>

  <read_first>
    - `frontend/src/components/pitch/Pitch.tsx` in full (278 lines) — the `PitchGhost` interface
      (lines 6-10), `buildRowSlots`'s `afterCode` insertion (lines 66-83), `PitchRow` (lines 97-138),
      the `rowGhost` helper (lines 162-165), the four formation rows that consume it (lines 218-257),
      and the Bench `PitchRow` (lines 263-271) that currently receives no ghost props at all.
    - `frontend/src/components/RateDiff.tsx` lines 13-81 — `VALID_PITCH_ROWS`, and
      `resolveRateOverlay`'s current row derivation: it filters `xi` for the sell name to set
      `outCode`, then independently sets `row` from the BUY row's `position`.
    - `frontend/src/lib/formation.ts` lines 39-50 — `splitPitchRows`: bench membership is exactly
      `!r.starting`, and formation-row membership is `starting && position === X`. The ghost row
      derivation must agree with this function or the ghost lands in a row the outgoing card is not in.
    - `e2e/specs/rate-my-team.spec.ts` in full (240 lines) — the header comment block (lines 4-35),
      the `rowCount` helper (lines 42-47), and the visual-diff test's per-row counts and row-group
      assertions (lines 176-239).
    - `.planning/phases/04-e2e-regression-suite/04-VERIFICATION.md` lines 35-54 — Gap 2's `reason`,
      `artifacts`, and `missing` item.
    - `.planning/phases/04-e2e-regression-suite/deferred-items.md` lines 19-49 — the root-cause
      analysis and the suggested follow-up this task implements.
  </read_first>

  <behavior>
    - Bench sell (the real capture): sell Mateta (`starting: false`), buy Wissa (FWD) — the ghost
      renders inside the `role="group"` labelled `Bench`, immediately after Mateta's cell; the
      Forwards group contains no ghost.
    - Starter sell (regression guard): sell a starting FWD, buy a FWD — the ghost still renders in
      the Forwards group immediately after the outgoing card, exactly as today.
    - Unresolvable sell: sell name matching zero or multiple xi rows — the ghost still renders, in
      the buy's position row, with `afterCode` null (appended at the row's end).
    - Unresolvable buy: buy name matching zero or multiple xp_table rows — no ghost at all, and the
      out treatment on the sold card is still applied (unchanged).
  </behavior>

  <reversibility rating="reversible">
    Two-way door: a widened string-union member, one extra prop spread on an existing row component,
    and a local change to one pure function's row selection. No stored data, no API contract, no
    build output shape is affected; reverting restores the previous rendering exactly.
  </reversibility>

  <action>
    Three files, in this order.

    **1. `frontend/src/components/pitch/Pitch.tsx`** — widen `PitchGhost["row"]` from
    `"GK" | "DEF" | "MID" | "FWD"` to also allow `"BENCH"` (uppercase, matching the existing
    position-code style; the row's visible `aria-label` stays the literal `Bench` and must not
    change — the E2E and Vitest suites both locate that group by name). Then spread
    `{...rowGhost("BENCH")}` onto the Bench `PitchRow` at the bottom of the component, exactly as
    the four formation rows already spread `rowGhost("GK")` … `rowGhost("FWD")`. `rowGhost`,
    `buildRowSlots`, `rowParts`, and `cardMeasure` need no change: `rowParts` already returns
    `Math.max(5, count)`, so a 4-player bench holding a ghost stays a 5-part measure and the row
    keeps its exact centering (G-03-1). Update the `PitchGhost`/ghost-card comment block to state
    that the row is the OUTGOING player's row, not the incoming player's position.

    **2. `frontend/src/components/RateDiff.tsx`** — in `resolveRateOverlay`, hold onto the matched
    sell row (the single-match filter that already produces `outCode`) as a local, then derive the
    ghost's row from it with this ordered rule, replacing the current unconditional use of the buy
    row's `position`:
      - sell row exists and its `starting` is false -> `"BENCH"`;
      - else sell row exists, `starting` is true, and its `position` is in `VALID_PITCH_ROWS` ->
        that `position`;
      - else the buy row's `position` when it is in `VALID_PITCH_ROWS` -> that `position`;
      - else no ghost, keeping the existing `import.meta.env.DEV` `console.warn` for the
        unexpected-position case.
    Add `"BENCH"` handling only through this rule — do not add `"BENCH"` to `VALID_PITCH_ROWS`,
    which exists to validate API-supplied `position` strings and must keep rejecting anything
    outside the four real positions. `afterCode` stays `outCode` unchanged, and the `diffs` map
    is untouched. Update the function's doc comment to record that the row now follows the sell
    target (including onto the bench) and to cite this plan and WINDOWS.md id=2.

    **3. `e2e/specs/rate-my-team.spec.ts`** — update the visual-diff test in the
    `Rate tab: visual diff` describe block to assert the corrected behavior. Do not touch the other
    four tests, do not change any tile literal, and do not add `page.route(` anywhere:
      - Per-row counts: the Goalkeeper/Defenders/Midfielders expectations stay as they are; the
        Forwards and Bench counts move — the ghost now grows the Bench row instead of the Forwards
        row. Read the real numbers off the run rather than trusting arithmetic (D-15): this plan's
        frontmatter records the derived expectation (Forwards 2, Bench 5) as an assumption to
        confirm, not as a measured fact.
      - Ghost placement: assert exactly one `Suggested incoming player` on the page; assert it is
        inside `getByRole("group", { name: "Bench" })`; assert `getByRole("group", { name: "Forwards" })`
        contains zero of them. Keep the existing `Wissa` / `£6.1` / `2.3` content assertions, scoped
        to the ghost locator.
      - Outgoing placement: keep the existing single `Suggested transfer out` assertions in the
        Bench group and the zero-in-Forwards assertion.
      - Add the adjacency proof that makes "same row" mean something: take
        `getByRole("group", { name: "Bench" }).locator("> div")` as the cell list, walk it to find
        the index of the cell containing `Suggested transfer out` and the index of the cell
        containing the `Suggested incoming player` label, and assert the ghost index equals the
        outgoing index plus one — mirroring `Pitch.test.tsx`'s own cell-index check for the
        Midfielders row.
      - Rewrite the file's header comment block and the inline comment above these assertions: they
        currently explain, at length, why the ghost and the outgoing card end up in separate row
        groups. Replace that explanation with the corrected contract (the ghost follows the sell
        target's own row, bench included), cite plan 04-08 and the closed WINDOWS.md id=2, and keep
        the surrounding hand-derived-literals note (score=83, xi_xp=26.67, best_move Mateta→Wissa,
        free_transfers=2) verbatim — those values did not change.
  </action>

  <verify>
    <automated>E2E_PYTHON=/home/sraja/miniconda3/envs/python314/bin/python E2E_VARIANTS=0 E2E_PORT=8140 npm --prefix e2e run test -- specs/rate-my-team.spec.ts --project=chromium</automated>
    <fails_when>Non-zero exit, or the reporter's summary line does not read `5 passed`, or stdout contains `failed` / `timed out` / `Error: expect(`. A failure inside the visual-diff test naming the Bench group means the ghost did not move; a failure naming a row count means the derived Forwards/Bench numbers were wrong and must be re-pinned from the real run.</fails_when>

    <automated>npm --prefix frontend run test</automated>
    <fails_when>Non-zero exit, a summary reporting fewer than `363 passed`, or any `failed` count. (363 is the pre-existing green Vitest baseline; this task adds no test — the existing starter-sell tests in RateDiff.test.tsx are the regression guard proving the starter path did not break.)</fails_when>

    <automated>bash scripts/verify_frontend_build.sh</automated>
    <fails_when>Non-zero exit, or stdout without the line `BUILD PURITY OK`. Note `npm --prefix frontend run typecheck` is a known no-op (WINDOWS.md id=1) — the `tsc -b` inside the build is the real type gate, so a `"BENCH"` union widening that missed a consumer surfaces here, not in typecheck.</fails_when>

    <automated>grep -rn "page.route(" e2e/specs/rate-my-team.spec.ts | wc -l</automated>
    <fails_when>Any output other than `0` — the rating must keep coming from the real fixture-fed API (E2E-01, D-01), never from a browser-layer mock.</fails_when>
  </verify>

  <acceptance_criteria>
    - `E2E_PYTHON=... E2E_VARIANTS=0 E2E_PORT=8140 npm --prefix e2e run test -- specs/rate-my-team.spec.ts --project=chromium` reports `5 passed`.
    - `npm --prefix frontend run test` reports `363 passed`.
    - `bash scripts/verify_frontend_build.sh` prints `BUILD PURITY OK`.
    - `grep -c 'BENCH' frontend/src/components/pitch/Pitch.tsx` prints a number `>= 2` (the union
      member and the bench row's `rowGhost` call).
    - `grep -c 'BENCH' frontend/src/components/RateDiff.tsx` prints a number `>= 1`.
    - `grep -c 'label="Bench"' frontend/src/components/pitch/Pitch.tsx` prints `1` — the row's
      visible accessible name is still the single literal `Bench` that every Vitest and Playwright
      locator in the repo resolves the group by; the `"BENCH"` union member is a separate internal
      key and must not have been substituted for it.
    - `grep -c 'two different role="group" containers' e2e/specs/rate-my-team.spec.ts || true` prints
      `0`. This gate is deliberately NOT comment-filtered: the stale claim lives in the file's own
      header prose, and removing that prose is exactly what is being verified.
    - `git status --porcelain e2e/fixtures/v1` produces no output — the immutable capture is untouched.
    - `git diff --name-only` lists exactly `frontend/src/components/pitch/Pitch.tsx`,
      `frontend/src/components/RateDiff.tsx`, `e2e/specs/rate-my-team.spec.ts`.
  </acceptance_criteria>

  <done>
    Against the real fixture-fed server and the unmodified v1 capture, the ghost card and the
    outgoing player's card render in the same `role="group"` container (Bench), adjacent, and the
    whole rate-my-team spec passes with every tile literal unchanged.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock the bench-row ghost at component level in the Vitest suites</name>
  <files>frontend/src/components/pitch/Pitch.test.tsx, frontend/src/components/RateDiff.test.tsx</files>

  <read_first>
    - `frontend/src/components/pitch/Pitch.test.tsx` in full (203 lines) — `GHOST_PLAYER`,
      the ghost describe block's cell-index assertion (lines 73-100), and the row-centering block's
      `assertRowIsExactlyCentered` helper plus its 6-part ghost case (lines 112-203).
    - `frontend/src/components/RateDiff.test.tsx` in full (139 lines) — the `resolveRateOverlay`
      describe block (lines 13-61) and the `RateDiff — one pitch (D-18)` render block (lines 63-139),
      including the existing starter-sell same-row test at lines 79-86 that must keep passing.
    - `frontend/src/test/fixtures/rate_response.json` — `best_move` is sell `Havertz` (a starting
      FWD) / buy `Haaland`; the `xi` also contains benched players `Egan` (DEF), `McBurnie` (FWD),
      `Ajer` (DEF) and `Tzolakis` (GK), all of which also exist in
      `frontend/src/test/fixtures/xp_table_squad.json`. `McBurnie` is the benched FWD this task
      uses to mirror the real capture's Mateta shape without touching any fixture file.
    - `frontend/src/components/pitch/Pitch.tsx` and `frontend/src/components/RateDiff.tsx` as they
      stand after Task 1.
  </read_first>

  <behavior>
    - `Pitch.test.tsx` test A: with `ghost={{ row: "BENCH", afterCode: <a bench player's code>, player: GHOST_PLAYER }}`,
      exactly one `Suggested incoming player` renders, it is inside the group labelled `Bench`, and
      its cell index is one greater than the named bench player's cell index.
    - `Pitch.test.tsx` test B: that same ghost-holding Bench row (4 real bench cards + 1 ghost = 5
      cells) still satisfies `assertRowIsExactlyCentered` at a 5-part measure — the ghost must not
      reintroduce the G-03-1 centering drift on the bench row.
    - `RateDiff.test.tsx` test C (`resolveRateOverlay`, bench sell): for a move selling `McBurnie`
      and buying `Haaland`, `ghost.row` is `"BENCH"`, `ghost.afterCode` is McBurnie's
      `player_code`, and `diffs` marks exactly that code `out`.
    - `RateDiff.test.tsx` test D (`resolveRateOverlay`, unresolvable sell): for a move selling
      `Nobody` and buying `Haaland`, `ghost.row` is `"FWD"` (the buy's position fallback) and
      `ghost.afterCode` is null.
    - `RateDiff.test.tsx` test E (render, bench sell): rendering `RateDiff` with the McBurnie→Haaland
      move puts both the `Suggested incoming player` ghost and the `Suggested transfer out` label
      inside the group labelled `Bench`, and puts zero ghosts in the group labelled `Forwards`.
  </behavior>

  <action>
    Add exactly five tests across the two existing test files. Add no new fixture file and modify no
    existing fixture — build the bench-sell move inline as an object literal of the same
    `RateBestMove` shape the file already constructs for its `Nobody` cases (sell `["McBurnie"]`,
    buy `["Haaland"]`, an `xp_gain` number).

    In `frontend/src/components/pitch/Pitch.test.tsx`: add tests A and B from `<behavior>` to the
    existing `Pitch — ghost card (D-16, D-18)` and `Pitch — row centering (G-03-1)` describe blocks
    respectively. Derive the bench player's `player_code` from the imported `squad.json` fixture by
    finding a row whose `starting` is false — do not hardcode a numeric literal that duplicates the
    fixture, matching how `captainCode` is already derived at the top of the file. Reuse the
    existing `assertRowIsExactlyCentered` helper for test B with a cell count of 5; do not modify
    the helper.

    In `frontend/src/components/RateDiff.test.tsx`: add tests C and D to the
    `resolveRateOverlay (backstop, PITCH-04)` describe block and test E to the
    `RateDiff — one pitch (D-18)` block, using `within(...)` scoping and
    `screen.getByRole("group", { name: "Bench" })` the same way the existing same-row test scopes to
    the Forwards group.

    Do not delete, rename, or weaken any of the 16 existing tests across these two files — in
    particular the starter-sell same-row test and the two `ghost={{ row: "MID", ... }}` tests are
    the guard that Task 1 preserved the starter path, and they must keep passing unmodified.
  </action>

  <verify>
    <automated>npm --prefix frontend run test -- src/components/RateDiff.test.tsx src/components/pitch/Pitch.test.tsx</automated>
    <fails_when>Non-zero exit, any `failed` count, or a passing-test total that does not increase by exactly 5 over these two files' current 16 (expected `21 passed` across the two files). A failure inside `resolveRateOverlay` naming `BENCH` means Task 1's ordered row rule is not implemented as specified.</fails_when>

    <automated>npm --prefix frontend run test</automated>
    <fails_when>Non-zero exit, any `failed` count, or a summary reporting fewer than `368 passed` (363 baseline + 5 new tests). Fewer than 368 with zero failures means a new test was silently not collected.</fails_when>

    <automated>bash scripts/verify_frontend_build.sh</automated>
    <fails_when>Non-zero exit, or stdout without the line `BUILD PURITY OK` (a `"BENCH"` literal typed where the union does not accept it fails the build's `tsc -b`, which `npm run typecheck` would not catch — WINDOWS.md id=1).</fails_when>
  </verify>

  <acceptance_criteria>
    - `npm --prefix frontend run test` reports `368 passed`, zero failures.
    - `npm --prefix frontend run test -- src/components/RateDiff.test.tsx src/components/pitch/Pitch.test.tsx` reports `21 passed`.
    - `grep -c 'BENCH' frontend/src/components/pitch/Pitch.test.tsx` prints a number `>= 2`.
    - `grep -c 'McBurnie' frontend/src/components/RateDiff.test.tsx` prints a number `>= 2`.
    - `git status --porcelain frontend/src/test/fixtures` produces no output — no fixture was edited.
    - `bash scripts/verify_frontend_build.sh` prints `BUILD PURITY OK`.
    - `git diff --name-only` lists exactly `frontend/src/components/pitch/Pitch.test.tsx` and
      `frontend/src/components/RateDiff.test.tsx`.
  </acceptance_criteria>

  <done>
    Both the bench-sell and the starter-sell ghost paths are pinned by component-level tests, the
    ghost-holding bench row is proven still exactly centered, and the full Vitest suite is green at
    368 tests.
  </done>
</task>

<task type="auto">
  <name>Task 3: Close WINDOWS.md id=2 and the deferred-items entry</name>
  <files>.planning/WINDOWS.md, .planning/phases/04-e2e-regression-suite/deferred-items.md</files>

  <read_first>
    - `.planning/WINDOWS.md` — the frontmatter counters (`open_count: 2`, `fixed_count: 0`,
      `total_count: 2`), the markdown table row for id=2, and the mirrored JSON block at the bottom.
      Both representations must stay consistent, which is why this is a tool call and not a hand edit.
    - `.planning/phases/04-e2e-regression-suite/deferred-items.md` — the single entry
      "Ghost card and outgoing label land in different pitch rows when the sold player is benched",
      whose `Suggested follow-up` (lines 44-49) is what Tasks 1-2 implemented.
    - `.planning/phases/04-e2e-regression-suite/04-VERIFICATION.md` lines 35-54 — Gap 2, to quote its
      `missing` item accurately in the resolution note.
  </read_first>

  <action>
    Ledger and documentation only — no source file changes in this task.

    1. Mark the broken-windows entry resolved with the GSD tool rather than by editing the file, so
       the frontmatter counters, the markdown table and the JSON block are all updated coherently:
       invoke the `windows fixed` subcommand for id `2` (the launcher resolves to
       `gsd-core/bin/gsd-tools.cjs`, e.g. `node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" windows fixed 2`).
       Confirm afterwards that `open_count` reads 1 and `fixed_count` reads 1, and that id=1 (the
       `npm run typecheck` no-op, Phase 1) is still `open` and untouched — it is a different defect
       and is not in this plan's scope.

    2. Append a short `## Resolution` section to the bottom of the phase's `deferred-items.md` entry
       (do not delete or reword the original analysis — it is the record of how the gap was found).
       State: fixed in plan 04-08; the ghost row is now derived from the sell target's actual row
       including the bench; the files changed; that `e2e/specs/rate-my-team.spec.ts` now asserts the
       same-row-plus-adjacency behavior against the same unmodified v1 capture; and that WINDOWS.md
       id=2 is marked fixed. Reference 04-VERIFICATION.md Gap 2 as the source of truth that required
       the fix.
  </action>

  <verify>
    <automated>node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" windows status</automated>
    <fails_when>Non-zero exit, or output showing `open_count` other than 1, `fixed_count` other than 1, or id=2 with a status other than `fixed`.</fails_when>

    <automated>grep -c '"status": "fixed"' .planning/WINDOWS.md</automated>
    <fails_when>Output other than `1` — the mirrored JSON block must record exactly one fixed entry (id=2). `0` means the tool call never ran or wrote only the table.</fails_when>

    <automated>grep -c '"status": "open"' .planning/WINDOWS.md</automated>
    <fails_when>Output other than `1` — id=1 (the Phase 1 typecheck no-op) must remain open. `0` means this task wrongly closed a defect outside its scope; `2` means id=2 was never closed.</fails_when>

    <automated>grep -c 'Resolution' .planning/phases/04-e2e-regression-suite/deferred-items.md</automated>
    <fails_when>Output of `0`, or a non-zero exit from grep (no match) — the deferred entry carries no resolution note.</fails_when>
  </verify>

  <acceptance_criteria>
    - `node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" windows status` reports `open_count` 1 and
      `fixed_count` 1.
    - `.planning/WINDOWS.md`'s id=2 row has status `fixed` and a non-empty `resolved_at` timestamp.
    - `.planning/WINDOWS.md`'s id=1 row is still `open` with an empty `resolved_at`.
    - `grep -c '04-08' .planning/phases/04-e2e-regression-suite/deferred-items.md` prints a number
      `>= 1`.
    - The original analysis text in `deferred-items.md` is unchanged:
      `grep -c 'Root cause' .planning/phases/04-e2e-regression-suite/deferred-items.md` still prints
      a number `>= 1`.
    - `git diff --name-only` lists exactly `.planning/WINDOWS.md` and
      `.planning/phases/04-e2e-regression-suite/deferred-items.md`.
  </acceptance_criteria>

  <done>
    WINDOWS.md id=2 is fixed with a timestamp, `open_count` is 1, and the deferred-items entry
    records how and where it was resolved — `/gsd-ship`'s `windows_enforce` gate no longer blocks on
    this phase's own defect.
  </done>
</task>

</tasks>

<flagged_assumptions>
Probe items carried by this plan (6 of the 8 phase-wide; the other 2 are carried by 04-07):

- **E2E-04 / boundary — resolved explicit**, authored as a `must_haves.truths` entry (the score tile
  still reads `83/100` and the `<= 100` structural clamp assertion survives the change). The
  unexercisable half — a rating scoring exactly 100 — is flagged below, not silently dropped.
- **E2E-04 / precision — resolved explicit**, authored as a `must_haves.truths` entry (the interval
  clause literal is unchanged by this plan). The unexercisable half — `xi_p10` null, interval clause
  wholly absent — is flagged below.
- **E2E-03 / adjacency, empty, ordering — resolved backstop** (three items), carried as structured
  backstop truths pointing at the executed, VERIFIED `e2e/specs/xp-table.spec.ts`. This plan changes
  nothing those assertions cover; no new criterion is authorable from Gap 2.

Unresolved assumptions surfaced rather than dropped:

- **Score exactly 100 is unreachable** on the immutable v1 capture. Forcing it would require editing
  `e2e/fixtures/v1`, which is prohibited (D-08). Documented, matching 04-04/04-05/04-06's precedent.
- **The `xi_p10`-null interval branch is unreachable** on this capture, same reason.
- **Row-count arithmetic is derived, not measured.** Forwards 2 / Bench 5 follows from the spec's own
  asserted 4-4-2 formation plus 4 bench cards plus 1 ghost, but D-15 requires literals to be
  hand-derived from a real observation. Task 1 must confirm the counts against the live run before
  pinning them; if they differ, the run's numbers win and this assumption is recorded as corrected in
  the SUMMARY.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `GET /api/rate/{entry}` response -> `resolveRateOverlay` | Server-supplied `best_move.sell` / `best_move.buy` names and `xi` rows (`position`, `starting`) drive which DOM row receives the ghost |
| `xp_table.json` -> `GhostCard` | The ghost card's rendered fields (name, price, xP) come from the fetched public pool file, not from user input |
| browser -> fixture-mode FastAPI (E2E only) | The suite drives the real API over localhost; no route interception exists to sanitize responses |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-08-01 | Tampering | `resolveRateOverlay` row derivation (`frontend/src/components/RateDiff.tsx`) | medium | mitigate | A malformed or hostile `/api/rate` payload carrying a `position` outside GK/DEF/MID/FWD must not produce an undefined row key or a ghost in no row at all. Task 1's ordered rule validates every non-`"BENCH"` row against the existing `VALID_PITCH_ROWS` set and falls through to "no ghost" with the existing DEV warn; `"BENCH"` is reachable only from the boolean `starting` field, never from an attacker-chosen string, and is deliberately kept out of `VALID_PITCH_ROWS`. |
| T-04-08-02 | Spoofing | `best_move.sell` / `best_move.buy` name matching | low | accept | Matching stays exact-string-equal on names with a strict single-match rule (unchanged by this plan); zero or multiple matches degrade to no out-treatment / buy-position fallback rather than guessing. A spoofed name therefore removes an overlay, never redirects it onto an unrelated player's card. |
| T-04-08-03 | Information disclosure | `GhostCard` rendering a not-owned player's row | low | accept | The ghost renders only fields already present in the publicly fetched `xp_table.json`, and moving the card between two rows of the same page discloses nothing new. |
| T-04-08-04 | Denial of service | Bench row holding 5 cells | low | accept | `rowParts` already returns `Math.max(5, count)`, so the bench row's shared flex basis is unchanged at 5 parts; Task 2 test B asserts the row stays exactly centered and never overflows, so there is no layout-thrash path. |
| T-04-08-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no packages — all three tasks edit existing tracked files using the toolchain 04-02 already installed and human-approved (`@playwright/test@1.62.1`, `@types/node@26.4.1`, `typescript@6.0.3`). The package-legitimacy gate has no install site to guard here. |

ASVS level 1; no high-severity threat identified for this change, and the single medium is mitigated
in Task 1 with a component-level regression test in Task 2.
</threat_model>

## Artifacts this phase produces

| Symbol / artifact | File | Kind |
|-------------------|------|------|
| `PitchGhost["row"]` union member `"BENCH"` | `frontend/src/components/pitch/Pitch.tsx` | widened type |
| `{...rowGhost("BENCH")}` on the Bench `PitchRow` | `frontend/src/components/pitch/Pitch.tsx` | new wiring (the bench row previously received no ghost props) |
| sell-target-derived ghost row (ordered rule) in `resolveRateOverlay` | `frontend/src/components/RateDiff.tsx` | changed logic |
| bench-row ghost placement + centering tests | `frontend/src/components/pitch/Pitch.test.tsx` | 2 new tests |
| bench-sell overlay tests + buy-position fallback test + bench render test | `frontend/src/components/RateDiff.test.tsx` | 3 new tests |
| Bench-group ghost assertions + cell-adjacency proof + rewritten header comment | `e2e/specs/rate-my-team.spec.ts` | changed assertions |
| id=2 status `fixed` + `resolved_at`, `open_count` 1 | `.planning/WINDOWS.md` | ledger closure |
| `## Resolution` note | `.planning/phases/04-e2e-regression-suite/deferred-items.md` | documentation |

## Source coverage audit

| Source | Item | Status | Where |
|--------|------|--------|-------|
| GOAL | ROADMAP Phase 4 SC 4 — rate-my-team flow covered end to end | COVERED | Task 1 restores the diff's declared contract; the spec still covers the flow end to end |
| REQ | E2E-04 — rate-my-team flow regression test | COVERED | Tasks 1-2 (`requirements: [E2E-04]`) |
| RESEARCH | No new research item — Gap 2 is pre-existing Phase 3 rendering code, outside 04-RESEARCH.md's fixture/harness scope | N/A | Recorded here so the absence is deliberate, not an omission |
| CONTEXT | D-18 single-pitch rate diff | COVERED | Task 1 keeps one pitch (row-group count assertion unchanged) while fixing where the ghost lands |
| CONTEXT | D-16 in-place pitch update / geometry via boundingBox, no screenshots | COVERED | Task 2 test B re-proves centering by declared layout model, no screenshot baseline added |
| CONTEXT | D-08 v1 fixtures never edited in place | COVERED | Enforced as a prohibition and by Task 1's `git status --porcelain e2e/fixtures/v1` criterion |
| CONTEXT | D-15 hardcoded hand-derived literals | COVERED | Task 1 re-pins only the two row counts that actually changed, from the real run; every tile literal is untouched |
| CONTEXT | D-13 Chromium only | COVERED | Task 1's run pins `--project=chromium`; no new project added |
| CONTEXT | D-01 real full-stack process, no route mocks | COVERED | Enforced by Task 1's `page.route(` count gate |
| VERIFICATION | Gap 2 `missing` — key the ghost off the sell target's actual row including Bench | COVERED | Task 1 |
| VERIFICATION | Gap 2 — WINDOWS.md id=2 disposition (fix chosen over waive) | COVERED | Task 3 |

No item is MISSING. Explicitly out of scope, not omitted: WINDOWS.md id=1 (Phase 1's `npm run
typecheck` no-op — a different defect, and the reason this plan gates on `verify_frontend_build.sh`
instead), and 04-REVIEW.md IN-01 (duplicated `fetchWithDetail` logic — an info-level finding, not a
verification gap).

<verification>
Run after all three tasks:

1. `npm --prefix frontend run test` — `368 passed`, zero failures.
2. `E2E_PYTHON=/home/sraja/miniconda3/envs/python314/bin/python npm --prefix e2e run test` — the
   full three-server suite (normal + blank + dgw projects), `42 passed`, confirming the ghost change
   broke none of the other ten spec files.
3. `bash scripts/verify_frontend_build.sh` — `BUILD PURITY OK`.
4. `/home/sraja/miniconda3/envs/python314/bin/python -m pytest -q` — `77 passed`, confirming this
   plan did not disturb 04-07's backend fix.
5. `node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" windows status` — `open_count` 1.
</verification>

<success_criteria>
- The ghost card and the outgoing player's card render in the same `role="group"` container, adjacent,
  for the real v1 capture's benched sell target.
- The starter-sell path is unchanged and still proven by the pre-existing tests.
- `e2e/fixtures/v1` is byte-identical (D-08) and no `page.route(` exists in the spec.
- Vitest `368 passed`; full Playwright suite `42 passed`; `BUILD PURITY OK`; pytest `77 passed`.
- WINDOWS.md id=2 is `fixed`, `open_count` is 1, and `deferred-items.md` records the resolution.
</success_criteria>

<output>
Create `.planning/phases/04-e2e-regression-suite/04-08-SUMMARY.md` when done.
</output>