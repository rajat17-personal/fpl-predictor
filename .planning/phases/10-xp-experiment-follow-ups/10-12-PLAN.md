---
phase: 10-xp-experiment-follow-ups
plan: 12
type: execute
wave: 6
depends_on: ["10-08", "10-10"]
files_modified:
  - data/news.py
  - config.py
  - features/engineer.py
  - backtest/walk_forward.py
  - tests/test_news.py
autonomous: false
requirements:
  - TODO-NEWS
user_setup:
  - service: "Guardian Open Platform"
    why: "The second sentiment source in D-02's method. Free developer key; only needed if the D-02 trigger fired. GDELT is keyless and carries the experiment alone if this signup is skipped."
    env_vars:
      - name: GUARDIAN_API_KEY
        source: "https://open-platform.theguardian.com/access/ — register for a free developer key, then add GUARDIAN_API_KEY=<key> to the repo-root .env file (mode 600), alongside the existing ODDS_API_KEY"

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "This plan is routed by the pre-declared D-02 trigger read from benchmark_tier1.json, not by appetite -- below the locked 0.500 means build, at or above means recorded not-triggered."
    - "The not-triggered and declined-on-cost outcomes are distinguished in the permanent record, because a future revisit needs to know whether the experiment was measured or never run."
    - "Every article contributing to a gameweek's features is published strictly before that gameweek's deadline, enforced by the request window itself and asserted by a named test."
    - "GUARDIAN_API_KEY never appears in a print, a cache filename or an exception message, and its absence degrades to GDELT-only mode rather than failing."
    - "No sentiment model is built: the tone figure is GDELT's own pre-computed field, per the todo's own scoping."
    - "If built, the experiment is measured on the standard 6-season protocol against a fresh contemporaneous control and the >=2,280 bar."
  artifacts:
    - "data/news.py (only if the trigger fired)"
    - config.NEWS_COLS
    - tests/test_news.py
    - "IMPROVEMENTS.md — the D-02 routing outcome"
  key_links:
    - "benchmark_tier1.json pooled spearman_xp_med -> the D-02 trigger -> this plan's route"
    - "data.availability.gw_deadlines -> the 7-day window (one shared deadline definition, never a second)"
    - "data.id_crosswalk.resolve_by_name -> news-entity resolution (one shared name matcher, never a second)"
---

<objective>
Act on the D-02 trigger plan 10-08 evaluated: if the post-Tier-1 pooled played-only Spearman
came in **below the locked 0.500**, build the GDELT + Guardian news-sentiment experiment; if it
came in **at or above 0.500**, record it as not-triggered following the `capt_mc` precedent and
stop.

Purpose: D-02 is a pre-declared conditional, which means the honest outcome is decided by a
number measured before this plan started rather than by appetite once it starts. Task 1 is a
decision checkpoint that reads that number and routes the plan. 10-RESEARCH.md Pitfall 3 also
records the directly relevant negative prior: `danielfrees/mlpremier` (arXiv 2405.02412) — the
very repo the todo cites to mine — published a Guardian-sentiment result that found no strong
predictive signal, underperforming both its own CNN and its Ridge/LightGBM baselines. That does
not override the trigger, but it does govern how much budget this plan spends if it fires.

Output: either a recorded not-triggered ledger entry and no new code, or `data/news.py`,
`config.NEWS_COLS`, a gating branch, a timestamp-leakage test, and a measured verdict.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-xp-experiment-follow-ups/10-CONTEXT.md
@.planning/phases/10-xp-experiment-follow-ups/10-RESEARCH.md
@.planning/phases/10-xp-experiment-follow-ups/10-PATTERNS.md
@.planning/phases/10-xp-experiment-follow-ups/10-08-SUMMARY.md
@.planning/todos/pending/2026-09-10-news-sentiment-conditional.md
</context>

## Artifacts this phase produces

New symbols this plan creates (exclude from drift verification, and only if the trigger fired):
module `data/news.py` with `NEWS_ENABLED`, `_GDELT_MIN_INTERVAL_S`, `_GUARDIAN_MIN_INTERVAL_S`,
`_GDELT_DOC_URL`, `_GUARDIAN_SEARCH_URL`, `_RAW_DIR`, `_OUT`, `_throttle`, `_require`,
`fetch_gdelt_window`, `fetch_guardian_window`, `build`, `load_news`, `attach`, `main`;
`config.NEWS_COLS` members `nw_doc_count_7d`, `nw_mean_tone_7d`, `nw_guardian_count_7d`;
`config.EXPERIMENTS["news_sentiment"]` (registered by plan 10-10); `tests/test_news.py`.

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Read the D-02 trigger and route this plan</name>
  <decision>Whether the Guardian/GDELT news-sentiment experiment gets built, per D-02's pre-declared Spearman trigger</decision>
  <context>
D-02 is a conditional locked before any Tier-1 run: the threshold is **0.500** pooled played-only
`spearman_xp_med` on `backtest.benchmark_external`'s EXP-1 basis, declared in plan 10-01 Task 3
and recorded in IMPROVEMENTS.md. Plan 10-08 measured the post-Tier-1 value into
`data/processed/experiments/benchmark_tier1.json`. I read that number from the artifact rather
than restating it, and print it alongside the threshold above.

Two things bear on the answer beyond the number itself. First, `danielfrees/mlpremier`
(arXiv 2405.02412) — the very repo the todo cites to mine — tested exactly this feature on
Guardian news text and found no strong predictive signal, underperforming both its own CNN and
its Ridge/LightGBM baselines. Second, the benchmark's only two scoreable seasons are 2021-22 and
2022-23, in which the availability family has no coverage at all, so this number measures
whether the ranking gap is still open, not what did or did not close it. I also print the
projected build wall clock in hours, computed from the distinct player-gameweek windows involved
at `_GDELT_MIN_INTERVAL_S = 5.0`.
  </context>
  <options>
    <option id="BUILD">
      <name>Build — the trigger fired (measured value below 0.500)</name>
      <pros>Honours the pre-declared conditional; the last availability increment after structured flags and injury records, and both sources are fully backfillable and timestamp-leakage-safe</pros>
      <cons>The projected fetch wall clock, against a prior that published a negative result for this exact feature</cons>
    </option>
    <option id="NOT-TRIGGERED">
      <name>Not triggered — the measured value is at or above 0.500</name>
      <pros>Mechanical application of a criterion fixed before the measurement; recorded as a numbered non-decision in the `capt_mc` shape, so a future reader knows it was ruled out by measurement</pros>
      <cons>None — this is the criterion doing its job</cons>
    </option>
    <option id="DECLINE">
      <name>Decline despite the trigger, on cost grounds</name>
      <pros>A legitimate developer override when the projected hours are not worth spending against the mlpremier prior</pros>
      <cons>Recorded as "declined on cost", NOT as "not triggered" — the distinction matters because a future revisit needs to know the experiment was never run rather than ruled out by measurement</cons>
    </option>
  </options>
  <resume-signal>Select BUILD, NOT-TRIGGERED, or DECLINE (confirming the mechanical route, or overriding it on cost)</resume-signal>
  <action>
Plan 10-08 measured the post-Tier-1 pooled played-only `spearman_xp_med` and recorded the D-02
outcome. Read it back from the artifact rather than from memory:

    python -c "import json,pathlib; print(json.loads(pathlib.Path('data/processed/experiments/benchmark_tier1.json').read_text())['pooled']['spearman_xp_med'])"

Present, from that number and from IMPROVEMENTS.md's `### D-02 news-sentiment trigger
evaluation` section:

1. The locked threshold: **0.500** (declared in plan 10-01 Task 3, before any Tier-1 run).
2. The measured post-Tier-1 pooled `spearman_xp_med`.
3. The mechanical outcome: below 0.500 means BUILD; at or above means NOT TRIGGERED.
4. The negative prior that governs budget if it fires: `danielfrees/mlpremier`
   (arXiv 2405.02412) tested exactly this feature on Guardian news text and found no strong
   predictive signal, underperforming both its own CNN and its Ridge/LightGBM baselines.
5. The structural caveat plan 10-08 recorded: the benchmark's two scoreable seasons are 2021-22
   and 2022-23, in which the availability family has no coverage — so this number measures
   whether the ranking gap is still open, not what did or did not close it.
6. What building costs: a rate-limited backfill across the training seasons against a keyless
   GDELT endpoint plus (optionally) a free Guardian key, in the shape of
   `data/transfermarkt.py`'s own fetcher. Estimate the wall clock explicitly from the distinct
   player-gameweek windows involved and `_GDELT_MIN_INTERVAL_S = 5.0`, and state it in hours.

Ask for confirmation of the mechanical route, with one legitimate override available:

- **BUILD** — the trigger fired; proceed to Tasks 2 and 3.
- **NOT TRIGGERED** — the trigger did not fire; skip Tasks 2 and 3, and instead record the
  outcome in IMPROVEMENTS.md exactly as Phase 9 recorded `capt_mc`: a numbered non-decision
  with the threshold, the measured value and the reason, never a silent skip. Set the
  `news_sentiment` row in the Phase 10 results table to `not triggered` / `off`.
- **DECLINE despite the trigger** — available only as an explicit developer override on cost
  grounds (the projected wall clock, given the mlpremier prior). If chosen, it is recorded as a
  declined-on-cost decision with the projected hours quoted, not as a not-triggered result —
  the distinction matters, because a future revisit needs to know the experiment was never run
  rather than ruled out by measurement.

Record the verbatim answer and write the corresponding entry to IMPROVEMENTS.md's `## Phase G`
before proceeding or stopping.
  </action>
  <verify>
    <human-check>The measured `spearman_xp_med`, the locked 0.500 threshold, the projected build wall clock in hours, and a verbatim BUILD / NOT TRIGGERED / DECLINE answer are all recorded in the checkpoint response</human-check>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import pathlib,sys; t=pathlib.Path('IMPROVEMENTS.md').read_text(); s=t.split('### D-02 news-sentiment trigger evaluation')[1][:3000]; ok=any(k in s for k in ('BUILD','not triggered','NOT TRIGGERED','declined on cost')); sys.exit('the D-02 section records no routing outcome') if not ok else print('routing outcome recorded')"</automated>
    <fails_when>non-zero exit, or the message `the D-02 section records no routing outcome`</fails_when>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records the measured pooled `spearman_xp_med` read from `benchmark_tier1.json`, not restated from memory
    - The SUMMARY records the locked 0.500 threshold and the mechanical outcome
    - The SUMMARY records the projected build wall clock in hours and the window count it came from
    - The SUMMARY records the verbatim routing answer
    - IMPROVEMENTS.md's D-02 section carries the routing outcome; on NOT TRIGGERED it follows the `capt_mc` numbered-non-decision shape, and on DECLINE it says "declined on cost" with the projected hours rather than "not triggered"
  </acceptance_criteria>
  <done>The pre-declared trigger has been read from the measured artifact and the plan is routed by it, with the distinction between not-triggered and declined-on-cost preserved in the permanent record.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GDELT + Guardian fetcher with a timestamp-leakage guarantee</name>
  <files>data/news.py, tests/test_news.py</files>
  <read_first>
    - data/fotmob.py — the whole file: the five-function optional-enrichment skeleton (`build`, `load_*`, `attach`, `main`, plus `_throttle`/`_require`), the kill switch, the on-disk cache via `ops.jsonio`, and the never-break-the-pipeline exception handling
    - data/transfermarkt.py — as written by plans 10-05/10-07: the resumable killable backfill shape, the incremental-append pattern, and the per-item failure counter
    - config.py — `load_dotenv()` and how `ODDS_API_KEY` is read, the established pattern the Guardian key must follow (mode-600 `.env`, never hardcoded, never printed)
    - data/id_crosswalk.py — `resolve_by_name` and `_NAME_FIXUPS`, the one shared name matcher every news-entity join must go through
    - data/availability.py — `gw_deadlines()`, the single shared deadline definition this feature's 7-day window must be anchored to
    - .planning/phases/10-xp-experiment-follow-ups/10-RESEARCH.md — the GDELT DOC 2.0 endpoint details, the Guardian `/search` shape, the Don't Hand-Roll entry mandating GDELT's own pre-computed `Tone` rather than a custom sentiment model, and Pitfall 3's negative prior
    - .planning/phases/10-xp-experiment-follow-ups/COVERAGE.md — the decided capability matrix for both APIs, including the opted-out surfaces (`show-fields=body`, GKG/BigQuery, `mode=tonechart`)
  </read_first>
  <precondition>Task 1's checkpoint recorded BUILD. On NOT TRIGGERED or DECLINE, skip this task and Task 3 entirely — the ledger entry Task 1 wrote is this plan's whole output.</precondition>
  <behavior>
    - `fetch_gdelt_window(query, start, end)` requests only articles published within `[start, end)` and `_require`-validates the response shape, raising `ValueError` naming the absent key on drift
    - Every article contributing to a gameweek's features has a publication timestamp strictly before that gameweek's deadline, enforced by the request window itself and asserted by a test
    - `fetch_guardian_window` reads `GUARDIAN_API_KEY` from the environment via `config.load_dotenv()`, never hardcodes it, never prints it, and skips the Guardian half with a printed note when the key is absent
    - No sentiment model is constructed anywhere: the tone figure comes from GDELT's own pre-computed field
    - Player-name entity resolution goes through `data.id_crosswalk.resolve_by_name` only
    - `build` is resumable and killable like the Transfermarkt fetcher; `load_news()` returns `None` when the artifact is absent
    - `attach` preserves the row count and prints coverage
  </behavior>
  <action>
Create `data/news.py` on `data/fotmob.py`'s five-function skeleton with `NEWS_ENABLED = True`,
`_GDELT_MIN_INTERVAL_S = 5.0` and `_GUARDIAN_MIN_INTERVAL_S = 1.0` (the locked values from
10-01-PLAN.md's decision table), `_RAW_DIR = config.RAW_DIR / "news"`,
`_OUT = config.PROCESSED_DIR / "news.parquet"`, and two per-source throttles rather than one
shared clock — the two endpoints have unrelated rate limits and a shared throttle would pace
both at the slower one.

Use plain `requests` against both endpoints. Do not add `gdeltdoc`: COVERAGE.md opts it out and
this project's convention is a thin `requests` wrapper, not a client package (which would also
need its own package-legitimacy gate).

`fetch_gdelt_window(query, start, end)` — GET `_GDELT_DOC_URL` with `mode=artlist` for the
document count and `mode=timelinetone` for the pre-computed tone, both bounded by
`startdatetime`/`enddatetime` derived from `start`/`end`. The window bound in the request is the
primary leakage control: an article that does not exist in the response cannot leak. Validate
with `_require` and cache each response under `_RAW_DIR` keyed by a slug of
`(query, start, end)`.

`fetch_guardian_window(query, start, end)` — GET `_GUARDIAN_SEARCH_URL` with
`from-date`/`to-date` and `api-key` read as `os.environ.get("GUARDIAN_API_KEY")` after
`config.load_dotenv()`. When the key is absent, print
`[news] Guardian key absent -- GDELT-only mode` and return an empty result: 10-RESEARCH.md's
Environment Availability table gives GDELT-alone as the documented fallback, so a missing
signup must degrade the experiment rather than block it. Never include the key in a printed
URL, a cache filename or an exception message — build the cache slug from the query and dates
only.

`build(*, resume=True, seasons=None, force=False)` — for each `(season, gw, player_code)` in
the authorised seasons, derive the window as the 7 days ending at
`data.availability.gw_deadlines()`'s `deadline_ts` for that gameweek (reuse that function; do
not re-derive deadlines), query on the player's display name, and emit three columns:
`nw_doc_count_7d`, `nw_mean_tone_7d`, `nw_guardian_count_7d`. Follow
`data/transfermarkt.py`'s resumability exactly: cache-keyed skip, incremental append, a
per-item failure counter, and progress prints with an ETA. Launch it detached through
`scripts/experiment_run.sh`'s convention and record the `tail -f` command in the SUMMARY.

Resolve each player's query name through `data.id_crosswalk.resolve_by_name` so the entity
column is a `player_code` and never a raw display string. A short-name or mononym query would
match unrelated news — the crosswalk's own whole-token verification and mononym exclusion are
exactly the discipline needed, and growing a second matcher here is what corrupted the FBref
join in Phase 9.

`load_news()` returns the parquet or `None`. `attach(full)` left-merges on
`["season", "gw", "player_code"]` with a row-count assert and a coverage print.

Create `tests/test_news.py`, entirely offline with `responses`, covering the seven behaviors.
The important one is `test_no_article_postdates_its_gameweek_deadline`: build a synthetic
response whose articles straddle a deadline, run the window derivation, and assert every
retained article's timestamp is strictly earlier than the deadline — the todo's own named
leakage test. Add `test_guardian_key_never_appears_in_cache_path_or_logs`: set a sentinel key
value, run a stubbed fetch with captured stdout, and assert the sentinel appears in neither the
captured output nor any created cache filename.
  </action>
  <verify>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m pytest tests/test_news.py -q</automated>
    <fails_when>non-zero exit, or the summary line reports any failed or errored test, or `no tests ran`</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m pytest tests/test_news.py::test_no_article_postdates_its_gameweek_deadline tests/test_news.py::test_guardian_key_never_appears_in_cache_path_or_logs -q</automated>
    <fails_when>non-zero exit, or `no tests ran` (a rename would silently disable the leakage and key-disclosure gates)</fails_when>
    <automated>grep -cE '^[[:space:]]*(import|from)[[:space:]]+gdeltdoc' data/news.py</automated>
    <fails_when>output is anything other than `0` — COVERAGE.md opts out of the client package in favour of plain requests. The pattern matches import statements only, so a comment recording the opt-out cannot invalidate its own gate</fails_when>
    <automated>grep -c "gw_deadlines" data/news.py</automated>
    <fails_when>output is `0` — the 7-day window must anchor to the one shared deadline definition, not a second derivation</fails_when>
    <automated>grep -c "resolve_by_name" data/news.py</automated>
    <fails_when>output is `0` — entity resolution must go through the shared crosswalk, never a second name matcher</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import pandas as pd, config, data.news as n; d=n.load_news(); assert d is not None, 'no news.parquet -- did the detached build finish?'; miss=[c for c in config.NEWS_COLS if c not in d.columns] if hasattr(config,'NEWS_COLS') else []; print('rows', len(d), 'cols', sorted(d.columns))"</automated>
    <fails_when>non-zero exit, or an AssertionError stating `news.parquet` is absent (the detached build has not finished)</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/ruff check .</automated>
    <fails_when>non-zero exit, or any diagnostic naming data/news.py</fails_when>
  </verify>
  <acceptance_criteria>
    - `data/news.py` defines all the named functions and both per-source throttle constants at 5.0 and 1.0
    - No `gdeltdoc` import statement, and no custom sentiment model: `grep -vE "^[[:space:]]*#" data/news.py | grep -cE "AutoModel|TextClassif|SentimentIntensity|transformers\."` returns `0`
    - The 7-day window derives from `data.availability.gw_deadlines()` and entity resolution from `data.id_crosswalk.resolve_by_name`
    - `GUARDIAN_API_KEY` is read via the environment after `config.load_dotenv()`, appears in no print and in no cache filename, and its absence produces GDELT-only mode rather than a failure
    - `test_no_article_postdates_its_gameweek_deadline` and `test_guardian_key_never_appears_in_cache_path_or_logs` exist and pass
    - `build` is resumable: a second invocation prints a non-zero skip count
    - `data/processed/news.parquet` exists with the three feature columns
  </acceptance_criteria>
  <done>A polite, resumable, killable two-source fetcher produces doc-count and pre-computed-tone features anchored to the shared deadline definition, with the article-timestamp leakage bound enforced by the request window and the Guardian key provably absent from every log and path.</done>
</task>

<task type="auto">
  <name>Task 3: Register, gate, and measure the sentiment experiment</name>
  <files>config.py, features/engineer.py, backtest/walk_forward.py, tests/test_news.py</files>
  <read_first>
    - config.py — `AVAILABILITY_COLS` and `INJURY_COLS` with their comment blocks (the shape `NEWS_COLS` mirrors), and the `EXPERIMENTS` dict where `news_sentiment` was registered by plan 10-10
    - features/engineer.py lines 38-60 — the `CONTEXT_COLS` expression as extended by plans 10-01 and 10-07
    - backtest/walk_forward.py lines 158-215 — `apply_experiment_feature_gating` and its docstring's family list, as extended by 10-01, 10-04 and 10-07
    - IMPROVEMENTS.md `### Phase G` declared criteria — the >= 2,280 bar this experiment is judged against
    - .planning/phases/10-xp-experiment-follow-ups/10-08-SUMMARY.md — the fresh 6-season control figure (`wf_tm_base6`) and the `features.parquet` sha256 discipline
    - data/news.py — as written by Task 2
  </read_first>
  <action>
Add `NEWS_COLS = ["nw_doc_count_7d", "nw_mean_tone_7d", "nw_guardian_count_7d"]` to `config.py`
after `INJURY_COLS`, with a comment in the same shape: a pre-match CONTEXT family computed
unconditionally, gate in `backtest/walk_forward.py`, plus two family-specific notes — that the
tone value is GDELT's own pre-computed figure and not a model output, and that a zero doc count
is a genuine observation (nobody wrote about this player) while an unresolvable entity is NaN,
the same resolved-versus-unresolved rule `INJURY_COLS` records.

Append `config.NEWS_COLS` to `features/engineer.py`'s `CONTEXT_COLS` expression beside the
other two Phase 10 families. Add one drop branch to
`backtest/walk_forward.py::apply_experiment_feature_gating` keyed on
`exp.get("news_sentiment", False)` dropping every `nw_`-prefixed column, and extend the
docstring's family list. One gate, one place.

Wire `data/news.py::attach` into the pipeline. `data/build_table.py` is not in this plan's file
set, so follow the precedent plan 10-07 Task 2 set and state it in `attach`'s docstring: the
optional-enrichment block calling `news_mod.attach(full)` lands with whichever later plan owns
that file, and this task's verify exercises `attach` directly against a real
`player_gw.parquet` frame in-process. Because no later plan in this phase owns
`data/build_table.py`, add the block here as a documented follow-up item in the SUMMARY's
deferred list rather than leaving it implicit — plan 10-16's close-out picks it up if the
experiment adopts, and it is unnecessary if it does not (a rejected flag's columns need not be
in the daily pipeline at all, exactly as Phase 9 left `understat`/`fotmob` wired but off).

Then measure, against the same >= 2,280 bar and the same fresh-control discipline plan 10-08
used. Rebuild the feature matrix once so `nw_` columns exist, record its new sha256, then:

    scripts/experiment_run.sh news_base6 --replicas 5
    scripts/experiment_run.sh news_on6 --replicas 5 --experiments news_sentiment

A fresh control is required rather than reusing `wf_tm_base6`, because adding the `nw_` columns
to the matrix changes `colsample_bytree` sampling even when they are gated off — the same
reasoning plan 10-08 Task 2 applied.

**Ledger handoff.** `IMPROVEMENTS.md` is not in this plan's file set, so record the measured
figures in the SUMMARY and hand the ledger write to plan 10-14, which owns that file and already
writes the bracket verdicts. State the handoff explicitly in the SUMMARY with the exact numbers
plan 10-14 must record, so nothing depends on being re-derived later.

Add `test_news_features_are_raw_context_not_rolled` to `tests/test_news.py`, mirroring the
availability and injury family-classification gates. Do NOT edit `tests/test_experiments.py`:
`news_sentiment` was already added to `_EXPERIMENT_KEYS` by plan 10-10, and that file belongs to
its file set. Verify the key is still present rather than editing it.
  </action>
  <verify>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import config; assert config.NEWS_COLS == ['nw_doc_count_7d','nw_mean_tone_7d','nw_guardian_count_7d'], config.NEWS_COLS; assert config.EXPERIMENTS['news_sentiment'] is False; import re,pathlib; t=pathlib.Path('tests/test_experiments.py').read_text(); assert 'news_sentiment' in t.split('_EXPERIMENT_KEYS')[1].split('}')[0]; print('registered and in the key set')"</automated>
    <fails_when>non-zero exit, or an AssertionError on the column list, the default flag state, or a `_EXPERIMENT_KEYS` block missing `news_sentiment`</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m data.build_table</automated>
    <fails_when>non-zero exit</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m features.engineer</automated>
    <fails_when>non-zero exit</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import config; from models.train import load_features; from backtest.walk_forward import apply_experiment_feature_gating as g; d=load_features(); off=g(d, config.resolve_experiments('none')); on=g(d, config.resolve_experiments('news_sentiment')); left=[c for c in off.columns if c.startswith('nw_')]; assert not left, left; kept=[c for c in on.columns if c.startswith('nw_')]; assert len(kept)==len(config.NEWS_COLS), kept; rolled=[c for c in d.columns if c.startswith('nw_') and c.endswith(('_r3','_r5','_r10','_rall'))]; assert not rolled, rolled; print('gate ok', kept)"</automated>
    <fails_when>non-zero exit, or an AssertionError listing `nw_` columns that survived the flag-off gate, a kept-column count mismatch, or a rolled `nw_` variant</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m pytest tests/test_news.py tests/test_leakage.py tests/test_experiments.py -q</automated>
    <fails_when>non-zero exit, or the summary line reports any failed or errored test</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import json,pathlib; b=json.loads(pathlib.Path('data/processed/experiments/wf_news_base6.json').read_text()); e=json.loads(pathlib.Path('data/processed/experiments/wf_news_on6.json').read_text()); assert len(b['seasons'])==6 and len(e['seasons'])==6, (b['seasons'], e['seasons']); assert b['replicas']==5 and e['replicas']==5; assert e['experiments']['news_sentiment'] is True and b['experiments']['news_sentiment'] is False; print('news', e['model+chips'], 'vs fresh base', b['model+chips'], 'delta', e['model+chips']-b['model+chips'], 'bar 2280')"</automated>
    <fails_when>non-zero exit, a FileNotFoundError on either tagged artifact, or an AssertionError on the season count, replica count, or flag states</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m pytest -q</automated>
    <fails_when>non-zero exit, or the summary line reports any failed or errored test</fails_when>
  </verify>
  <acceptance_criteria>
    - `config.NEWS_COLS` equals the three names in the exact order given; `EXPERIMENTS["news_sentiment"] is False`
    - `config.NEWS_COLS` appears in `features/engineer.py`'s `CONTEXT_COLS` and never in `ROLL_STATS`
    - The gate drops all three `nw_` columns when off and keeps all three when on
    - Both tagged 6-season runs exist at 5 replicas with the correct flag states
    - `tests/test_news.py::test_news_features_are_raw_context_not_rolled` passes
    - `tests/test_experiments.py` is unmodified by this plan (its `_EXPERIMENT_KEYS` already carries the key from plan 10-10)
    - The SUMMARY records the measured `model+chips` pair, the delta, the >= 2,280 comparison, the mechanical verdict, and the exact figures handed to plan 10-14 for the ledger write
  </acceptance_criteria>
  <done>The sentiment family is registered as pre-match context behind one default-off flag in the single gating seam, measured on the standard 6-season protocol against a fresh control, and its numbers are handed to plan 10-14 for the ledger with nothing left to re-derive.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GDELT / Guardian API responses -> feature columns | untrusted third-party content and schema cross here |
| `GUARDIAN_API_KEY` -> logs, cache filenames, exception messages | a secret can leak through any of the three |
| article publication timestamps -> pre-match features | an article published after the deadline is hindsight |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-12-01 | Information Disclosure | `GUARDIAN_API_KEY` in a print, a cache path or a traceback | high | mitigate | read via `os.environ` after `config.load_dotenv()` (the `ODDS_API_KEY` precedent, mode-600 `.env`, never committed); cache slugs are built from query and dates only; `test_guardian_key_never_appears_in_cache_path_or_logs` asserts a sentinel value appears in neither stdout nor any created filename |
| T-10-12-02 | Tampering | an article published after the deadline entering a pre-match feature | critical | mitigate | the request window itself is the primary bound (`startdatetime`/`enddatetime`, `from-date`/`to-date` anchored to `data.availability.gw_deadlines()`), and `test_no_article_postdates_its_gameweek_deadline` asserts it on a straddling synthetic response |
| T-10-12-03 | Tampering | GDELT or Guardian schema drift silently producing NaN features | high | mitigate | `_require` explicit-shape validation raising `ValueError` naming the absent key, the same T-09-09-01 discipline `data/fotmob.py` established |
| T-10-12-04 | Tampering | a mononym or short-name query matching unrelated news | high | mitigate | entity resolution goes through `data.id_crosswalk.resolve_by_name` only, inheriting its whole-token verification and mononym exclusion; no second matcher (the FBref join-corruption precedent) |
| T-10-12-05 | Denial of Service | a news outage breaking the daily pipeline | medium | mitigate | `attach` is an optional-enrichment path guarded the same way the four existing ones are; `load_news()` returns `None` when absent; a missing Guardian key degrades to GDELT-only rather than failing |
| T-10-12-06 | Repudiation | building an experiment the pre-declared trigger did not authorise, or recording a declined experiment as a measured rejection | high | mitigate | Task 1's blocking decision checkpoint reads the number from `benchmark_tier1.json` and preserves the not-triggered / declined-on-cost distinction in the ledger |
</threat_model>

<verification>
- `python -m pytest` green repo-wide, with both named gates in `tests/test_news.py` actually running
- `ruff check .` clean
- No `gdeltdoc` reference, no custom sentiment model, no second name matcher, no second deadline derivation
- Both tagged 6-season runs present at 5 replicas with correct flag states
- The gate drops all three `nw_` columns when off
- `tests/test_experiments.py` unmodified by this plan
</verification>

<success_criteria>
- The plan is routed by the pre-declared D-02 trigger read from a measured artifact, not by appetite
- On NOT TRIGGERED or DECLINE, the outcome is recorded in the `capt_mc` shape with the two cases distinguished, and no code is written
- On BUILD, a polite resumable two-source fetcher produces deadline-anchored features with the timestamp bound enforced by the request window and the API key provably contained
- The experiment is measured on the standard 6-season protocol against a fresh control, with its numbers handed to plan 10-14 for the ledger
</success_criteria>

<output>
Create `.planning/phases/10-xp-experiment-follow-ups/10-12-SUMMARY.md` when done. It MUST
record: the measured pooled `spearman_xp_med` read from `benchmark_tier1.json`, the locked
0.500 threshold, the verbatim routing decision, and — on BUILD — the projected versus actual
fetch wall clock, the `tail -f` command, whether Guardian ran or GDELT-only mode was used, the
measured `model+chips` pair with its delta and >= 2,280 comparison, and the exact figures handed
to plan 10-14 for the ledger write plus the `data/build_table.py` wiring follow-up.
</output>
