---
phase: 10-xp-experiment-follow-ups
plan: 15
type: execute
wave: 9
depends_on: ["10-14"]
files_modified:
  - IMPROVEMENTS.md
  - data/external/fbref/README.md
  - data/fbref.py
  - tests/test_leakage.py
autonomous: false
requirements:
  - TODO-FBREF-MANUAL
  - TODO-RL-SHAPING
user_setup:
  - service: "FBref (manual browser download)"
    why: "Automated FBref access is confirmed dead — Phase 9 logged three real Chrome UC-mode spike attempts that all hung against Cloudflare's challenge. D-04 fixes acquisition as manual and forbids new scraping infrastructure. Only needed if the Task 1 decision says go."
    dashboard_config:
      - task: "For each season 2016-17 onward, open the FBref Premier League player defensive-actions table in a normal browser (passing the Cloudflare challenge interactively), use Share & Export -> Get table as CSV, and save each file as data/external/fbref/fbref_<season>_defense.csv"
        location: "https://fbref.com/en/comps/9/ — Premier League seasons, Squad & Player Defensive Actions tables"

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The FBref item is decided by a recorded human decision against the confirmed-dead access reality and the measured FotMob +1/season prior -- a Drop is a decision with reasoning, never an unexplained absence."
    - "If acquired, the season-aggregate leakage limitation is documented in code, in the README and in the ledger, and NO adoption number is measured against a knowingly leaky join."
    - "The RL item is notes-only: no training runs and neither optimize/rl_env.py nor optimize/rl_train.py changes."
    - "The potential-based-shaping correction is on the record with the Ng et al. 1999 guarantee, the price-chasing failure mode named, and the log-linear compute curve any future revisit must project against."
    - "config.EXPERIMENTS['fbref_v2'] and ['rl_strategy'] both stay False; no default is flipped in this plan."
  artifacts:
    - "data/external/fbref/README.md plus the manually-acquired CSVs (only on a Go or Partial decision)"
    - "data/fbref.py — load_manual_snapshot"
    - "IMPROVEMENTS.md — the two tail subsections"
  key_links:
    - "data/id_crosswalk.resolve_by_name's fbref_key tier -> the manual snapshot's identity resolution"
    - "the existing config.FBREF_COLS / fbref_v2 flag / CONTEXT_COLS registration -> no new infrastructure needed (D-04)"
---

<objective>
Close out D-04's two dead-tail items: the manual FBref CSV snapshot, which happens only if
there is time to spare and the developer is willing to do the downloads, and the RL
reward-shaping notes, which are notes-only with no training.

Purpose: these two are last for a reason. FBref's automated access is confirmed dead (three
logged spike failures in Phase 9), so its acquisition cost is entirely human labour — which
makes a decision checkpoint, not an assumption, the right way to start. The RL item's whole
content is a correction of the developer's own stated instinct about shaping terms, and
recording that correction now is what prevents a future revisit from repeating a rejected
design. Both belong in the phase; neither belongs ahead of anything else in it.

Output: a go/no-go on FBref, the join behind the already-existing `fbref_v2` flag if it goes,
and the potential-based-shaping note in IMPROVEMENTS.md either way.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-xp-experiment-follow-ups/10-CONTEXT.md
@.planning/phases/10-xp-experiment-follow-ups/10-RESEARCH.md
@.planning/phases/10-xp-experiment-follow-ups/10-PATTERNS.md
@.planning/todos/pending/2026-09-09-manual-fbref-snapshot.md
@.planning/todos/pending/2026-09-09-rl-reward-shaping-revisit.md
</context>

## Artifacts this phase produces

New symbols this plan creates (exclude from drift verification, the FBref items only if the
decision says go): directory `data/external/fbref/` with `README.md` and
`fbref_<season>_defense.csv`; `data/fbref.py` additions `load_manual_snapshot`,
`_MANUAL_DIR`, `_MANUAL_KEEP_COLS`; `tests/test_leakage.py` test
`test_fbref_manual_snapshot_is_rolled_not_raw`; `IMPROVEMENTS.md` subsections
`### fbref_v2: manual snapshot — acquisition decision and outcome`,
`### RL reward shaping: potential-based only, recorded for any future revisit`.

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Go / no-go on the manual FBref snapshot</name>
  <decision>Whether to do the manual FBref CSV acquisition, and at what season scope</decision>
  <context>
D-04 puts this item dead last and conditions it on two things at once: everything else landing
with time to spare, AND you being willing to do the manual downloads. Neither is something this
plan can assume, and the acquisition cost here is entirely human labour.

**Access reality.** Phase 9 recorded `fbref_v2` as `not acquirable`: three real Chrome UC-mode
navigation attempts, 5-6 minutes each, all hung inside the reconnect call while a plain driver
confirmed the site still serves Cloudflare's `Just a moment...` challenge. A control site worked
fine on the same Chrome and network. The hosted proxy `fbrapi.com` was probed 2026-09-09 and
found half-down (TLS chain fails verification, no HTTP response even ignoring TLS). D-04 forbids
building new scraping infrastructure, and COVERAGE.md opts out of re-probing `fbrapi.com` on the
grounds that manual acquisition makes it moot.

**The human work.** One browser download per season from 2016-17 onward — ten seasons — passing
the Cloudflare challenge interactively each time, via Share & Export -> Get table as CSV, saved
under `data/external/fbref/` with the filename pattern in this plan's `user_setup`. A few
minutes per season.

**Expected value.** The todo's own deprioritization note is blunt: season-aggregate tables rank
below every research-derived todo, and FotMob's per-match defensive data — the same signal
family at finer granularity — already measured **+1 point/season**, well inside the harness's
~50-point noise band, and was rejected in Phase 9. `FBREF_COLS` and the `fbref_v2` flag already
exist, so the code side is a reader plus a join, not new infrastructure.

**Time actually remaining.** I print what plans 10-01 through 10-14 consumed against the
original expectation above, so "with time to spare" is answered with a fact rather than a feeling.
  </context>
  <options>
    <option id="A-go">
      <name>Go — all ten seasons</name>
      <pros>Completes D-12-era enrichment coverage on the one source that is still unmeasured; code side is trivial because FBREF_COLS and the flag already exist</pros>
      <cons>Ten interactive Cloudflare challenges of your time, against a FotMob prior of +1/season; and the join is season-aggregate, so it is not leakage-safe for within-season backtesting</cons>
    </option>
    <option id="B-partial">
      <name>Partial — a named subset of seasons</name>
      <pros>Most of the signal for a fraction of the labour; the recent seasons are where the defensive-contribution scoring rules that make these stats valuable actually applied</pros>
      <cons>Fewer seasons is a noisier estimate; same season-aggregate leakage limitation</cons>
    </option>
    <option id="C-drop">
      <name>Drop — record as declined on cost</name>
      <pros>Explicitly permitted by D-04 and by the todo itself; costs nothing and is recorded as a decision with the FotMob +1 prior quoted, not an unexplained absence</pros>
      <cons>The FBref defensive columns stay unmeasured — though the FotMob result suggests little is being given up</cons>
    </option>
  </options>
  <resume-signal>Select A, B (naming the seasons), or C</resume-signal>
  <action>
D-04 puts this item dead last and conditions it on two things at once: everything else landing
with time to spare, AND the developer being willing to do the manual CSV downloads. Neither is
something this plan can assume.

Present the state of play:

1. **Access reality.** Phase 9 recorded `fbref_v2` as `not acquirable`: three real Chrome
   UC-mode navigation attempts, 5-6 minutes each, all hung inside the reconnect call while a
   plain driver confirmed the site still serves Cloudflare's `Just a moment...` challenge. A
   control site worked fine on the same Chrome and network. The hosted proxy `fbrapi.com` was
   probed 2026-09-09 and found half-down (TLS chain fails verification, and no HTTP response
   even ignoring TLS). D-04 forbids building new scraping infrastructure, and COVERAGE.md opts
   out of re-probing `fbrapi.com` on the grounds that manual acquisition makes it moot.
2. **What the human work actually is.** One browser download per season from 2016-17 onward —
   ten seasons — passing the Cloudflare challenge interactively each time, via
   Share & Export -> Get table as CSV, saved under `data/external/fbref/` with the filename
   pattern in this plan's `user_setup`. Estimate it at a few minutes per season.
3. **What it is expected to buy.** The todo's own deprioritization note is blunt:
   season-aggregate tables rank below every research-derived todo, and FotMob's per-match
   defensive data — the same signal family, at finer granularity — already measured **+1
   point/season** (well inside the harness's ~50-point noise band) and was rejected in Phase 9.
   `FBREF_COLS` and the `fbref_v2` flag already exist in `config.py`, so if the CSVs arrive the
   code side is a reader plus a join, not new infrastructure.
4. **Time actually remaining.** State what plans 10-01 through 10-14 consumed against the
   original expectation, so "with time to spare" is answered with a fact rather than a feeling.

Ask for one of:
- **A) Go** — the developer will do the ten downloads; proceed to Task 2.
- **B) Partial** — a named subset of seasons only (e.g. the three most recent, where the
   defensive-contribution scoring rules that make these stats valuable actually applied).
   Name the seasons.
- **C) Drop** — record it as declined-on-cost with the FotMob +1 prior quoted, and skip Task 2.
   Per D-04 this is an expected and acceptable outcome, not a failure; the item is explicitly
   droppable.

Record the verbatim answer. Whichever it is, the outcome goes into IMPROVEMENTS.md at Task 3 —
including a Drop, which must be recorded as a decision with its reasoning rather than left as an
unexplained absence.
  </action>
  <verify>
    <human-check>A verbatim A / B / C answer is recorded in the checkpoint response, along with the time-remaining fact from point 4 and, on B, the named season subset</human-check>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records the verbatim decision letter and the developer's own wording
    - The SUMMARY records the time-consumed-versus-expected fact that answered "with time to spare"
    - On B: the SUMMARY names the exact seasons Task 2 is authorised to join
    - On C: the SUMMARY records it as declined-on-cost with the FotMob +1 prior quoted, and states that Task 2 is skipped
  </acceptance_criteria>
  <done>A recorded human decision, made against the confirmed access reality and the measured FotMob prior, either authorises the manual acquisition at a stated scope or declines it on cost.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Read and join the manual FBref snapshot behind the existing fbref_v2 flag</name>
  <files>data/fbref.py, data/external/fbref/README.md, tests/test_leakage.py</files>
  <read_first>
    - data/fbref.py — the whole file: the existing scrape-side code, `attach(full, id_map)`'s signature and join behaviour, and whatever column naming it already produces for `config.FBREF_COLS`
    - config.py lines 118-130 — `FBREF_COLS` (`fb_tkl_int_90`, `fb_blocks_90`, `fb_clr_90`, `fb_sca_90`, `fb_gca_90`, `fb_prog_90`) and its optional-enrichment comment; and `EXPERIMENTS["fbref_v2"]`, which already exists and stays False
    - features/engineer.py lines 28-56 — `FBREF_COLS`' membership of `CONTEXT_COLS` (not `ROLL_STATS`), which is where the manual snapshot's columns will land
    - data/external/README.md — the five-section committed-snapshot README shape
    - data/id_crosswalk.py — `resolve_by_name` and `_NAME_FIXUPS`, the one shared name matcher; FBref names are the exact case the crosswalk's `fbref_key` tier exists for
    - backtest/walk_forward.py — `apply_experiment_feature_gating`'s existing handling of `fbref_v2` (if any), so no second gate is added
    - .planning/todos/pending/2026-09-09-manual-fbref-snapshot.md — the kiwi snapshot pattern it names
  </read_first>
  <precondition>Task 1's checkpoint recorded decision A or B, and the CSV files for the authorised seasons exist under `data/external/fbref/`. On decision C, skip this task entirely.</precondition>
  <behavior>
    - `load_manual_snapshot()` reads every `fbref_<season>_defense.csv` present and returns one tidy frame, or `None` when the directory is empty
    - A CSV missing a required column raises `ValueError` naming every absent column at once
    - Names resolve to `player_code` through `data.id_crosswalk.resolve_by_name` only, unresolved rows are dropped with a printed resolution rate, and a duplicate `(season, player_code)` raises rather than multiplying join rows
    - These are SEASON-AGGREGATE per-90 figures, not per-match outcomes, so they are joined on `(season, player_code)` and reach the model through `CONTEXT_COLS` exactly as `FBREF_COLS` already does
    - `config.EXPERIMENTS["fbref_v2"]` stays False and the existing gate continues to drop `fb_` columns when it is off
  </behavior>
  <action>
Extend `data/fbref.py` rather than creating a new module — `FBREF_COLS`, the `fbref_v2` flag and
an `attach` path already exist, and D-04 forbids new scraping infrastructure. Add:

`_MANUAL_DIR = config.DATA_DIR / "external" / "fbref"` and `_MANUAL_KEEP_COLS` mapping FBref's
own exported header names onto `config.FBREF_COLS`. Derive that mapping from a real downloaded
file's verbatim header row and record the mapping table in the SUMMARY — FBref's CSV export
uses its own multi-level header text, and guessing it is the failure mode
10-RESEARCH.md's Pitfall 2 describes for a different vendor.

`load_manual_snapshot() -> pd.DataFrame | None` — glob the directory, read each CSV, validate
`_MANUAL_KEEP_COLS`' source columns raising `ValueError` naming all absent ones at once, extract
the season from the filename, resolve names through `data.id_crosswalk.resolve_by_name`, drop
unresolved rows with a `[fbref] {season}: {n} rows, {pct} names resolved` print, and raise
`AssertionError` on a duplicate `(season, player_code)`. Return `None` when the directory holds
no matching file.

Wire it into the existing `attach` path so a manual snapshot is used when present and the
scrape-side path is untouched. Because these are season-aggregate per-90 figures rather than
per-match outcomes, join on `(season, player_code)` — one value per player per season, broadcast
across that season's fixtures. That is a genuine limitation worth stating in a comment: a
season-aggregate figure joined to an early-season fixture carries information from later in the
same season, which is a leakage surface the per-match FotMob data did not have. Handle it the
only honest way available without per-match data: state in the comment and in the README that
these columns are valid only for a *cross-season* comparison and are NOT leakage-safe for
within-season backtesting, and register the concern in the SUMMARY so plan 10-16's audit can
record it. Do not silently join a season aggregate as if it were pre-match context.

Given that, do NOT run an adoption measurement in this task. A number measured on a knowingly
leaky join would be worse than no number — it is exactly the `+337-optimistic` shape this
project has already been burned by twice. `fbref_v2` stays False, the code stays merged (D-08),
and the ledger entry records the acquisition outcome plus the leakage limitation rather than a
fabricated verdict.

Write `data/external/fbref/README.md` in `data/external/README.md`'s five-section shape:
`## Source` (fbref.com, the exact table, the manual Share & Export method, per-season retrieval
dates, and the confirmed-dead automated access with the Phase 9 evidence), `## Attribution`
(FBref / Sports Reference, and their terms on manual export), `## Regeneration` (the human
download steps verbatim from this plan's `user_setup`, and the never-in-cron-or-CI sentence),
`## Reduction applied` (the verbatim exported header row, which columns `_MANUAL_KEEP_COLS`
retains, footprint), `## PII spot-check` (public player names and public match statistics only).
Add a sixth section `## Leakage limitation` stating the season-aggregate constraint above in
plain terms, so a future reader cannot mistake these columns for pre-match context.

Add `test_fbref_manual_snapshot_is_rolled_not_raw` to `tests/test_leakage.py` — guarded on the
snapshot directory's existence, mirroring the existing `UNDERSTAT`/`FOTMOB` path-existence
markers — asserting the `fb_` columns are present as raw `CONTEXT_COLS` members and that the
season-aggregate join produces exactly one distinct value per `(season, player_code)`. The
second assertion is what would catch a future edit that accidentally started varying these
figures per fixture, which would make the limitation harder to reason about, not easier.
  </action>
  <verify>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import data.fbref as f; assert callable(f.load_manual_snapshot); d=f.load_manual_snapshot(); print('None' if d is None else (len(d), sorted(d.columns)))"</automated>
    <fails_when>non-zero exit, an AttributeError on `load_manual_snapshot`, or a printed `None` when CSVs for the authorised seasons are present in `data/external/fbref/`</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import data.fbref as f, config; d=f.load_manual_snapshot(); assert d is not None; assert not d.duplicated(['season','player_code']).any(), int(d.duplicated(['season','player_code']).sum()); miss=[c for c in config.FBREF_COLS if c not in d.columns]; assert not miss, miss; print('seasons', sorted(d.season.unique()), 'players', d.player_code.nunique())"</automated>
    <fails_when>non-zero exit, or an AssertionError naming a duplicate `(season, player_code)` count or a missing `config.FBREF_COLS` member</fails_when>
    <automated>grep -c "resolve_by_name" data/fbref.py</automated>
    <fails_when>output is `0` — identity resolution must go through the shared crosswalk, never a second name matcher</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import config; assert config.EXPERIMENTS['fbref_v2'] is False, config.EXPERIMENTS['fbref_v2']; from backtest.walk_forward import apply_experiment_feature_gating as g; from models.train import load_features; d=load_features(); off=g(d, config.resolve_experiments('none')); left=[c for c in off.columns if c.startswith('fb_')]; assert not left, left; print('fbref_v2 still default-off and gated')"</automated>
    <fails_when>non-zero exit, or an AssertionError showing the flag was flipped or `fb_` columns survived the flag-off gate</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import pathlib,sys; t=pathlib.Path('data/external/fbref/README.md').read_text(); need=('## Source','## Attribution','## Regeneration','## Reduction applied','## PII spot-check','## Leakage limitation','Share & Export','never wired into cron or CI','not leakage-safe'); miss=[k for k in need if k not in t]; sys.exit('README missing: '+str(miss)) if miss else print('README complete')"</automated>
    <fails_when>non-zero exit, or the message `README missing:` listing an absent section or required phrase — in particular the `## Leakage limitation` section and the `not leakage-safe` wording</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m pytest tests/test_leakage.py -q</automated>
    <fails_when>non-zero exit, or the summary line reports any failed or errored test</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m pytest -q</automated>
    <fails_when>non-zero exit, or the summary line reports any failed or errored test</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/ruff check .</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `data/fbref.py` gains `load_manual_snapshot`, `_MANUAL_DIR` and `_MANUAL_KEEP_COLS`; no new module was created
    - `load_manual_snapshot` returns `None` on an empty directory and raises `ValueError` naming all absent source columns otherwise
    - Names resolve only through `data.id_crosswalk.resolve_by_name`; a duplicate `(season, player_code)` raises
    - `data/external/fbref/README.md` has all six sections including `## Leakage limitation` with the `not leakage-safe` wording
    - `config.EXPERIMENTS["fbref_v2"]` is still False and the gate still drops every `fb_` column when off
    - No adoption measurement was run and no verdict number was produced for `fbref_v2`
    - The SUMMARY records the verbatim FBref exported header row and the `_MANUAL_KEEP_COLS` mapping table
  </acceptance_criteria>
  <done>The manually-acquired seasons load through the existing FBref path with shared-crosswalk identity resolution, the season-aggregate leakage limitation is documented rather than measured around, and the flag stays off with no fabricated verdict.</done>
</task>

<task type="auto">
  <name>Task 3: Record the FBref outcome and the RL potential-based-shaping note</name>
  <files>IMPROVEMENTS.md</files>
  <read_first>
    - IMPROVEMENTS.md lines 653-688 — `### fbref_v2: real Chrome spike — access still blocked, no new infrastructure built (plan 09-09)`, the entry this plan's FBref subsection extends rather than replaces
    - IMPROVEMENTS.md lines 393-513 — `### rl_strategy: time-boxed MaskablePPO policy vs the solver-scored chip scheduler (plan 09-07)` and its diagnosis that the binding constraint was training budget, not reward design
    - IMPROVEMENTS.md lines 1198-1237 and 1400-1449 — the rl v2 dose-response addendum, the declared v3 time-box written before training, and the v3 outcome (beats chips_v2 on 2025-26, still -97 versus the shipped heuristic, rejected, log-linear compute curve recorded)
    - IMPROVEMENTS.md lines 254-269 — `### capt_mc: Monte-Carlo variant not triggered`, the shape a recorded non-decision takes
    - .planning/todos/pending/2026-09-09-rl-reward-shaping-revisit.md — the three-point analysis this task records, and the Ng et al. 1999 citation
    - optimize/rl_env.py lines 247-259 — the reward function the note refers to, so the note names the real code location
  </read_first>
  <action>
Write two subsections under `## Phase G`.

`### fbref_v2: manual snapshot — acquisition decision and outcome (plan 10-15)` — extend, do not
replace, Phase 9's own `### fbref_v2:` entry. Record: the verbatim Task 1 decision and its
reasoning; the confirmed-dead automated access carried forward from Phase 9 (three UC-mode
attempts, the Cloudflare challenge, `fbrapi.com` half-down 2026-09-09, and that COVERAGE.md
opted out of a re-probe); on a Go or Partial, the seasons actually acquired, the resolution rate,
and the `## Leakage limitation` finding — that season-aggregate per-90 figures joined to an
early-season fixture carry within-season information, so no adoption number was measured and the
flag stays off with the code merged (D-08); on a Drop, the declined-on-cost record with the
FotMob +1/season prior quoted.

Do NOT edit Phase F's own results table. Its `fbref_v2` / `not acquirable` cell is a true record
of what Phase 9 measured, and rewriting a prior phase's finding to reflect a later phase's work
destroys the historical record. Add the Phase 10 outcome as this new subsection and
cross-reference it from the Phase G narrative instead.

`### RL reward shaping: potential-based only, recorded for any future revisit (plan 10-15)` —
notes-only, no training, per D-04. Record all three points the todo sets out, as a correction
of a stated instinct rather than as a neutral summary, because that is what makes it useful to a
future revisit:

1. **Week-average baseline: skip.** It is action-independent, so by construction it cannot
   change the optimal policy, and PPO's own learned value function already performs exactly this
   variance-reduction role. Adding it would be redundant machinery, not a fix.
2. **Team value: potential-based only.** A raw additive bonus on delta-value (the referenced
   project's `+0.05 x delta_value`) biases the objective toward price-chasing, and is part of
   why that project's reported 2,918 was optimistic. If shaping is wanted, use potential-based
   shaping — `reward += gamma * Phi(s') - Phi(s)` with `Phi` a small weight times team value —
   which provably preserves the optimal policy (Ng, Harada and Russell, 1999) while still giving
   intermediate signal. Net points keep weight 1. Name the code location this would touch:
   `optimize/rl_env.py`'s reward function around lines 247-259, whose current pure form was
   proven equal to `run_season`'s own total to within 1e-6.
3. **The binding constraint was budget, not reward design.** Record the measured curve: v1
   rejected at 5-14k timesteps in a 30-minute box; v2 at 4x budget still rejected with a
   dose-response reading; v3's 9-hour single-season run beat `chips_v2` but still lost to the
   shipped heuristic by 97 points, and the compute relationship was log-linear. Any future
   re-run needs a much larger time-box declared BEFORE training per D-16, and the log-linear
   curve is the number to project against when declaring it.

Close the subsection with the adoption rule that does not change: judgment stays on pure
realised points through the honest harness, never on the shaped reward. A shaped reward is a
training aid; scoring it would be scoring the aid rather than the result.

Edit `IMPROVEMENTS.md` by scoped insertion only. Do not reflow or rewrite Phase A-F content, and
do not alter Phase F's own `fbref_v2` or `rl_strategy` entries.
  </action>
  <verify>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import pathlib,sys; t=pathlib.Path('IMPROVEMENTS.md').read_text(); need=('### fbref_v2: manual snapshot','### RL reward shaping: potential-based only','Ng, Harada and Russell','optimize/rl_env.py','log-linear','potential-based','fbrapi.com'); miss=[k for k in need if k not in t]; sys.exit('ledger missing: '+str(miss)) if miss else print('both tail subsections present')"</automated>
    <fails_when>non-zero exit, or the message `ledger missing:` listing an absent subsection heading or required citation</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import pathlib,sys; t=pathlib.Path('IMPROVEMENTS.md').read_text(); s=t.split('### RL reward shaping: potential-based only')[1].split('###')[0]; need=('skip','1e-6','weight 1','D-16'); miss=[k for k in need if k not in s]; sys.exit('RL note missing: '+str(miss)) if miss else print('all three RL points recorded')"</automated>
    <fails_when>non-zero exit, or the message `RL note missing:` listing an absent element of the three-point analysis</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import pathlib,sys; t=pathlib.Path('IMPROVEMENTS.md').read_text(); lost=[h for h in ('### fbref_v2: real Chrome spike','### rl_strategy: time-boxed MaskablePPO policy','### Addendum (2026-09-10): rl_strategy v3','## Phase F','## Reference findings') if h not in t]; sys.exit('lost prior anchors: '+str(lost)) if lost else print('all prior Phase F entries intact')"</automated>
    <fails_when>non-zero exit, or the message `lost prior anchors:` naming a Phase F entry this edit destroyed</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import config; assert config.EXPERIMENTS['rl_strategy'] is False and config.EXPERIMENTS['fbref_v2'] is False; assert not any(config.EXPERIMENTS.values()), [k for k,v in config.EXPERIMENTS.items() if v]; print('no training run, no default flipped')"</automated>
    <fails_when>non-zero exit, or an AssertionError showing a flipped flag — this plan is notes-only for RL and measurement-free for FBref</fails_when>
    <automated>git diff --stat optimize/rl_env.py optimize/rl_train.py</automated>
    <fails_when>the output is non-empty — D-04 makes the RL item notes-only, so neither RL module may change</fails_when>
  </verify>
  <acceptance_criteria>
    - Both named subsections exist under `## Phase G`
    - The FBref subsection records the verbatim decision, the carried-forward access evidence including `fbrapi.com`, and either the acquired seasons plus the leakage limitation or the declined-on-cost record with the FotMob +1 prior
    - The RL subsection records all three points with the Ng/Harada/Russell 1999 citation, the `optimize/rl_env.py` code location, the 1e-6 equality fact, the log-linear compute curve, and D-16's declare-before-training rule
    - The RL subsection closes with the pure-realised-points adoption rule
    - Phase F's own `fbref_v2`, `rl_strategy` and v3-addendum entries are unchanged
    - `optimize/rl_env.py` and `optimize/rl_train.py` are byte-identical to before this plan
    - No `config.EXPERIMENTS` default flipped
  </acceptance_criteria>
  <done>Both dead-tail items are closed in the permanent record: the FBref acquisition decision with its outcome and honest leakage limitation, and the potential-based-shaping correction with the compute curve any future RL revisit must project against.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a human browser download -> `data/external/fbref/` -> a committed artifact | hand-acquired third-party content crosses into a permanent tracked artifact |
| a season-aggregate figure -> a per-fixture feature | broadcasting one season value across that season's fixtures is a real leakage surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-15-01 | Tampering | a season-aggregate per-90 figure used as if it were pre-match context | high | mitigate | the limitation is stated in a code comment, in a dedicated `## Leakage limitation` README section (verify-enforced, including the literal `not leakage-safe` wording), and in the ledger entry; no adoption number is measured against a knowingly leaky join and `fbref_v2` stays off |
| T-10-15-02 | Tampering | a guessed FBref export header mapping producing wrong-column values | high | mitigate | `_MANUAL_KEEP_COLS` is derived from a real downloaded file's verbatim header row and recorded in the SUMMARY; a missing source column raises `ValueError` naming all absent ones |
| T-10-15-03 | Tampering | name-match false positives attributing another player's defensive stats | high | mitigate | resolution goes through `data.id_crosswalk.resolve_by_name` only (its `fbref_key` tier exists for exactly this source), a duplicate `(season, player_code)` raises, and the resolution rate is printed |
| T-10-15-04 | Legal/compliance | committing FBref-derived content | medium | mitigate | the README records the source, the manual export method and Sports Reference's terms on manual export; only the reduced per-90 columns are retained |
| T-10-15-05 | Repudiation | a dropped item left as an unexplained absence | medium | mitigate | a Drop is recorded as a declined-on-cost decision with the FotMob +1/season prior quoted, following the `capt_mc` recorded-non-decision precedent |
| T-10-15-06 | Repudiation | an RL revisit repeating the rejected raw-additive shaping design | medium | mitigate | the note records the correction explicitly (potential-based only, net points keep weight 1) with the Ng et al. 1999 guarantee and the price-chasing failure mode named |
</threat_model>

<verification>
- `python -m pytest` green repo-wide
- `ruff check .` clean
- On Go/Partial: `load_manual_snapshot` returns the authorised seasons with no duplicate `(season, player_code)`, and the README has all six sections
- `config.EXPERIMENTS["fbref_v2"]` and `["rl_strategy"]` still False; no default flipped anywhere
- `optimize/rl_env.py` and `optimize/rl_train.py` byte-identical
- Both tail subsections in IMPROVEMENTS.md; all Phase F entries intact
</verification>

<success_criteria>
- The FBref item is decided by a human against the confirmed access reality and the measured FotMob prior, then executed or declined with the outcome recorded either way
- Where it was executed, the season-aggregate leakage limitation is documented and no number was fabricated against it
- The RL item is closed as notes-only with no training and no RL module change
- The potential-based-shaping correction and the log-linear compute curve are on the record for any future revisit
</success_criteria>

<output>
Create `.planning/phases/10-xp-experiment-follow-ups/10-15-SUMMARY.md` when done. It MUST
record: the verbatim FBref decision and the time-remaining fact behind it; on Go/Partial the
verbatim FBref exported header row, the `_MANUAL_KEEP_COLS` mapping table, the seasons acquired
and the per-season name-resolution rate; and confirmation that no RL training ran and neither RL
module changed.
</output>
