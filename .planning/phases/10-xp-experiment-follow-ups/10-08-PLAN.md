---
phase: 10-xp-experiment-follow-ups
plan: 08
type: execute
wave: 4
depends_on: ["10-06", "10-07"]
files_modified:
  - data/build_table.py
  - IMPROVEMENTS.md
autonomous: true
requirements:
  - TODO-AVAIL-FLAGS
  - TODO-TM-INJURY
user_setup: []

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The availability experiment is judged on D-09's dual criterion with BOTH numbers measured: 2025-26 walk-forward model+chips delta, and pooled played-only Spearman delta against the 0.383 baseline."
    - "The Transfermarkt injury experiment is judged on the standard 6-season protocol against the 2,262 baseline and the >=2,280 bar."
    - "Both verdicts are recorded in IMPROVEMENTS.md's Phase 10 results table with numbers, and each flag stays default-off unless its own pre-declared criterion is met (D-07/D-08)."
    - "The D-02 news-sentiment trigger is evaluated against the locked 0.500 threshold using the measured post-Tier-1 Spearman, and its outcome (build / not-triggered) is recorded before plan 10-12 acts on it."
  artifacts:
    - "data/processed/experiments/wf_avail_*.json and wf_tm_*.json tagged run artifacts"
    - "data/processed/experiments/benchmark_tier1.json"
    - "IMPROVEMENTS.md — availability_flags and transfermarkt_injury verdict subsections plus the D-02 trigger record"
  key_links:
    - "data/transfermarkt.py::attach -> data/build_table.py's optional-enrichment block (the injury columns finally reach player_gw.parquet)"
    - "the measured pooled Spearman -> the D-02 trigger -> plan 10-12's build-or-record decision"
---

<objective>
Measure and rule on both Tier-1 experiments, then evaluate the D-02 trigger that decides
whether Tier 2's conditional sentiment work happens at all.

Purpose: everything in plans 10-01 through 10-07 built capability. This plan is where the
honest-harness discipline actually bites: the pre-declared criteria written in 10-01 Task 3 are
applied mechanically to measured numbers, and each flag's default is set by the rule rather
than by hope. Phase 9 ended with all eight of its flags default-off and its final combined run
identical to its opening baseline — that outcome was acceptable precisely because the criteria
were fixed in advance.

Output: the injury attach finally wired into `data/build_table.py`, four tagged walk-forward
runs, one tagged benchmark run, two ledger verdicts with numbers, and the recorded D-02
trigger outcome.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-xp-experiment-follow-ups/10-CONTEXT.md
@.planning/phases/10-xp-experiment-follow-ups/10-RESEARCH.md
@.planning/phases/10-xp-experiment-follow-ups/10-04-SUMMARY.md
@.planning/phases/10-xp-experiment-follow-ups/10-06-SUMMARY.md
@.planning/phases/10-xp-experiment-follow-ups/10-07-SUMMARY.md
</context>

## Artifacts this phase produces

New symbols this plan creates (exclude from drift verification): `data/build_table.py`'s fifth
optional-enrichment block importing `transfermarkt as tm_mod`; tagged artifacts
`wf_avail_base_2526`, `wf_avail_on_2526`, `wf_tm_base6`, `wf_tm_on6`, `benchmark_tier1`;
`IMPROVEMENTS.md` subsections `### availability_flags: dual-criterion adoption verdict`,
`### transfermarkt_injury: 6-season adoption verdict`, `### D-02 news-sentiment trigger evaluation`.

<tasks>

<task type="auto">
  <name>Task 1: Wire the injury attach into the pipeline and rebuild</name>
  <files>data/build_table.py</files>
  <read_first>
    - data/build_table.py lines 140-190 — the four existing optional-enrichment try/except attach blocks (`fbref`, `team_strength`, `understat`, `fotmob`) plus the `availability` block plan 10-01 added, and each block's `[source] skipped ({exc})` print form
    - data/transfermarkt.py — `attach(full)` as written by plan 10-07 Task 2, and its docstring naming this plan's task as its wiring point
    - config.py — `INJURY_COLS` and its comment on unconditional computation
  </read_first>
  <precondition>`data/external/transfermarkt/injury_spells.csv` exists (plan 10-07 Task 1 committed it). If plan 10-05's checkpoint returned decision D (not acquirable), skip this task entirely and record that in the SUMMARY — Task 2 then measures only the availability arm.</precondition>
  <action>
Add a fifth optional-enrichment block to `data/build_table.py`, mirroring the four existing
ones exactly in shape and placement: import `transfermarkt as tm_mod` in the same grouped
import at the top of the file (alongside `fotmob as fm_mod`, `understat as us_mod`,
`team_strength as ts_mod`), then a `try` calling `full = tm_mod.attach(full)` with an
`except Exception as exc` printing `  [transfermarkt] skipped ({exc})`, placed after the
`availability` block.

Include the same comment the `understat` and `fotmob` blocks carry, adapted: the injury columns
are computed UNCONDITIONALLY whenever `data/external/transfermarkt/injury_spells.csv` exists,
independent of `EXPERIMENTS["transfermarkt_injury"]`, so `player_gw.parquet` and
`features.parquet` stay stable across an A/B run and the feature-selection gate stays the only
switch (`backtest/walk_forward.py`). Reference `config.INJURY_COLS`'s own comment for the full
reasoning rather than duplicating it.

Then rebuild the pipeline end to end so both Tier-1 families are present in
`features.parquet` before any measurement run starts. Nothing after this point in the phase may
rebuild the feature matrix — a rebuild mid-measurement would make two tagged runs
non-comparable, which is the whole reason the flags gate at train time rather than at build
time.
  </action>
  <verify>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m data.build_table</automated>
    <fails_when>non-zero exit, or `[transfermarkt] skipped` appears in the output, or `[availability] skipped` appears in the output</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m features.engineer</automated>
    <fails_when>non-zero exit</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import pandas as pd, config; f=pd.read_parquet(config.PROCESSED_DIR/'features.parquet'); miss=[c for c in config.AVAILABILITY_COLS+config.INJURY_COLS if c not in f.columns]; assert not miss, miss; rolled=[c for c in f.columns if (c.startswith('av_') or c.startswith('tm_')) and c.endswith(('_r3','_r5','_r10','_rall'))]; assert not rolled, rolled; print('both families present:', len(config.AVAILABILITY_COLS), 'availability +', len(config.INJURY_COLS), 'injury')"</automated>
    <fails_when>non-zero exit, or an AssertionError naming a missing family column or a rolled `av_`/`tm_` variant</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m pytest -q</automated>
    <fails_when>non-zero exit, or the summary line reports any failed or errored test</fails_when>
    <automated>grep -v '^#' data/build_table.py | grep -c "tm_mod.attach"</automated>
    <fails_when>output is `0` — the attach call never landed in code (comment-only changes do not count)</fails_when>
  </verify>
  <acceptance_criteria>
    - `data/build_table.py` has exactly five optional-enrichment blocks after the id-map attach, the fifth calling `tm_mod.attach(full)`
    - A fresh `python -m data.build_table` prints neither `[transfermarkt] skipped` nor `[availability] skipped`
    - `features.parquet` carries all of `config.AVAILABILITY_COLS` and `config.INJURY_COLS`, unrolled
    - `python -m pytest` green repo-wide after the rebuild
    - The SUMMARY records the feature-matrix row and column counts, and the sha256 of `features.parquet`, as the frozen basis every subsequent measurement in this plan shares
  </acceptance_criteria>
  <done>Both Tier-1 feature families reach `features.parquet` through the standard optional-enrichment path, and the frozen matrix that every measurement run in this plan shares is recorded by hash.</done>
</task>

<task type="auto">
  <name>Task 2: Run the Tier-1 A/B measurements</name>
  <files>data/build_table.py</files>
  <read_first>
    - backtest/walk_forward.py lines 292-420 — `main`'s `--replicas`/`--experiments`/`--seasons`/`--tag` flags and the exact keys written into the tagged JSON (`model_mean`, `model+chips`, `capt_mean`, `capt_capture`, `multi_safe`, `form`, `hold`, `experiments`)
    - backtest/benchmark_external.py lines 268-356 — `score`'s `--experiments` gating path (added by quick task 260909-elx) and `_stats_block`'s `spearman_xp_med` key, which is the D-09(b) and D-02 measurement basis
    - scripts/experiment_run.sh — the detached-launch convention and its per-run log path, for the multi-hour 6-season runs
    - IMPROVEMENTS.md `### Phase G` declared criteria — the exact thresholds this task measures against, and the Phase F baseline table (6-season `model+chips` = 2262, 2025-26 `model+chips` = 2172, pooled `spearman_xp_med` = 0.383)
    - .planning/phases/10-xp-experiment-follow-ups/10-04-SUMMARY.md and 10-06-SUMMARY.md — the measured 2025-26 availability coverage, which the ledger entry must cite alongside the verdict
  </read_first>
  <precondition>`features.parquet`'s sha256 matches the value Task 1 recorded in the SUMMARY. If it does not, a rebuild happened between tasks and every run below must be redone against one frozen matrix — stop and report rather than mixing vintages.</precondition>
  <action>
Run five measurements, all through `scripts/experiment_run.sh` so each survives a shell
disconnect and gets its own log, and give the developer the `tail -f` command for each in the
SUMMARY.

**D-09(a) — availability, covered season.** The criterion is stated on 2025-26 specifically, so
run that season with the phase's standard replica count (5, matching the Phase F baseline
protocol — a 1-replica run's jitter spread would swamp a +25 point signal):

    scripts/experiment_run.sh avail_base_2526 --seasons 2025-26 --replicas 5
    scripts/experiment_run.sh avail_on_2526 --seasons 2025-26 --replicas 5 --experiments availability_flags

Compute the delta as `wf_avail_on_2526['model+chips'] - wf_avail_base_2526['model+chips']`. Use
the freshly measured base rather than Phase F's recorded 2172, because Task 1's rebuild changed
the feature matrix (the availability and injury columns now exist even when gated off, which
alters `colsample_bytree` sampling) — an A/B is only honest against its own contemporaneous
control. Record both the fresh base and Phase F's 2172 in the ledger so the difference is
visible rather than hidden.

**D-09(b) — availability, ranking.** 

    python -m backtest.benchmark_external --experiments none --tag tier1_base
    python -m backtest.benchmark_external --experiments availability_flags --tag tier1_avail

Take `pooled.spearman_xp_med` from each. The criterion is an improvement of >= +0.03 over the
0.383 baseline; measure the delta against the freshly measured `tier1_base` figure and record
both it and the 0.383 historical baseline.

**Transfermarkt — standard 6-season protocol.** Full coverage means the standard bar applies:

    scripts/experiment_run.sh tm_base6 --replicas 5
    scripts/experiment_run.sh tm_on6 --replicas 5 --experiments transfermarkt_injury

Judge `wf_tm_on6['model+chips']` against the >= 2,280 bar and against the fresh
`wf_tm_base6['model+chips']` control.

**Post-Tier-1 combined ranking measurement** (the D-02 trigger input):

    python -m backtest.benchmark_external --experiments availability_flags,transfermarkt_injury --tag tier1

`benchmark_tier1.json`'s `pooled.spearman_xp_med` is the number D-02's locked 0.500 threshold is
compared against. Note in the SUMMARY that this measurement covers 2021-22 and 2022-23 (the only
seasons theFPLkiwi's committed snapshot scores) and that the availability family has no coverage
in either — so the availability arm's contribution to this specific number is structurally zero,
and that fact must be stated in the trigger record rather than left implicit. It does not change
the trigger: D-02 is a measurement of whether the ranking gap is still open, whatever closed or
failed to close it.

Do not adjust any flag, lambda, hysteresis or threshold to chase a result. If a number lands
just short of a bar, that is the finding — Phase 9 recorded `capt_ceiling` at +1.5 against a
+2 bar and `understat` at +16 against a +18-needed bar without moving either bar.

This task modifies no files; its output is the five tagged artifacts. Task 3 writes them up.
  </action>
  <verify>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import json,pathlib; ps=[f'data/processed/experiments/wf_{t}.json' for t in ('avail_base_2526','avail_on_2526','tm_base6','tm_on6')]; miss=[p for p in ps if not pathlib.Path(p).exists()]; assert not miss, miss; d={p:json.loads(pathlib.Path(p).read_text()) for p in ps}; [print(p.split('wf_')[1], v['model+chips'], 'replicas', v['replicas'], 'seasons', len(v['seasons'])) for p,v in d.items()]"</automated>
    <fails_when>non-zero exit, or an AssertionError listing a missing tagged walk-forward artifact</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import json,pathlib; b=json.loads(pathlib.Path('data/processed/experiments/wf_avail_base_2526.json').read_text()); e=json.loads(pathlib.Path('data/processed/experiments/wf_avail_on_2526.json').read_text()); assert b['experiments']['availability_flags'] is False and e['experiments']['availability_flags'] is True; assert b['replicas']==5 and e['replicas']==5, (b['replicas'], e['replicas']); assert b['seasons']==['2025-26'] and e['seasons']==['2025-26']; print('D-09(a) delta', e['model+chips']-b['model+chips'], 'bar +25')"</automated>
    <fails_when>non-zero exit, or an AssertionError on either flag state, on a replica count other than 5, or on a season list other than 2025-26 alone</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import json,pathlib; b=json.loads(pathlib.Path('data/processed/experiments/wf_tm_base6.json').read_text()); e=json.loads(pathlib.Path('data/processed/experiments/wf_tm_on6.json').read_text()); assert len(b['seasons'])==6 and len(e['seasons'])==6, (b['seasons'], e['seasons']); assert e['experiments']['transfermarkt_injury'] is True; print('injury', e['model+chips'], 'vs fresh base', b['model+chips'], 'bar 2280')"</automated>
    <fails_when>non-zero exit, or an AssertionError on a season count other than 6 or on the flag state</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import json,pathlib; g=lambda t: json.loads(pathlib.Path(f'data/processed/experiments/benchmark_{t}.json').read_text())['pooled']['spearman_xp_med']; base=g('tier1_base'); av=g('tier1_avail'); comb=g('tier1'); print('base',base,'avail',av,'combined',comb,'| D-09(b) delta',round(av-base,4),'bar +0.03 | D-02 threshold 0.500 ->', 'BUILD' if comb < 0.500 else 'NOT TRIGGERED')"</automated>
    <fails_when>non-zero exit, a FileNotFoundError on any of the three benchmark artifacts, or a KeyError on `pooled.spearman_xp_med`</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import config; assert not any(config.EXPERIMENTS.values()), [k for k,v in config.EXPERIMENTS.items() if v]; print('every flag still default-off after measurement')"</automated>
    <fails_when>non-zero exit, or an AssertionError listing any flag whose default was flipped during measurement (D-08 forbids it before a verdict)</fails_when>
  </verify>
  <acceptance_criteria>
    - All four tagged walk-forward JSONs exist with the correct flag states, `replicas == 5`, and the correct season scope (1 season for the availability arm, 6 for the injury arm)
    - All three tagged benchmark JSONs exist and carry `pooled.spearman_xp_med`
    - No `config.EXPERIMENTS` default was changed during measurement
    - `features.parquet`'s sha256 is unchanged from Task 1's recorded value across all five runs
    - The SUMMARY records every measured figure: both `model+chips` pairs, all three pooled Spearman values, the two computed deltas, and each against its pre-declared bar
    - The SUMMARY records the `tail -f` command and wall clock for each detached run
  </acceptance_criteria>
  <done>Five measurements are on disk against one frozen feature matrix, both D-09 numbers and the D-02 trigger input are computed, and no flag or threshold was moved to chase a result.</done>
</task>

<task type="auto">
  <name>Task 3: Write both Tier-1 verdicts and the D-02 trigger record to the ledger</name>
  <files>IMPROVEMENTS.md</files>
  <read_first>
    - IMPROVEMENTS.md `### Phase G` — the declared criteria block and the nine-row pending results table written by plan 10-01 Task 3
    - IMPROVEMENTS.md lines 513-660 — the `### understat:` and `### fotmob:` verdict subsections from Phase 9: the shape a verdict entry takes (command, artifact path, the measured table, the criterion restated, the mechanical verdict, and the resulting default)
    - IMPROVEMENTS.md lines 989-1102 — the per-position/covered-row addendum, the precedent for reporting a partial-coverage measurement honestly
    - .planning/phases/10-xp-experiment-follow-ups/10-08-SUMMARY.md as it stands after Task 2 — every measured figure
    - .planning/phases/10-xp-experiment-follow-ups/10-06-SUMMARY.md — the 2025-26 availability coverage figure the availability verdict must cite
  </read_first>
  <action>
Write three subsections under `## Phase G`, each in the shape Phase 9's `### understat:` entry
established, and fill the two corresponding rows of the Phase 10 results table.

`### availability_flags: dual-criterion adoption verdict (plan 10-08)` — the two exact commands
run, the artifact paths, and a table giving: fresh 2025-26 base `model+chips`, flag-on
`model+chips`, the delta against the +25 bar; fresh pooled `spearman_xp_med`, flag-on value,
the delta against the +0.03 bar. Restate D-09's rule that BOTH must hold, then state the
mechanical verdict. Cite the 2025-26 availability coverage from 10-06-SUMMARY.md — a verdict on
a thin join means something different from a verdict on a full one, and a reader a year from now
needs that number in the same paragraph. Also record Phase F's own 2025-26 `model+chips` of 2172
beside the fresh base, and note that Task 1's rebuild is why they differ.

`### transfermarkt_injury: 6-season adoption verdict (plan 10-08)` — the same shape against the
>= 2,280 bar and the fresh 6-season control. Cite the id-resolved coverage and active-spell rate
from 10-07-SUMMARY.md. Add one sentence recording that the IJCSS 2025 paper the todo cites for
this source's expected value could not be verified (Assumptions Log A1), so the measured number
here stands on its own and is not corroborating a confirmed prior. If plan 10-05's checkpoint
returned decision D, this subsection instead records `not acquirable` with the probe evidence
and no measurement.

`### D-02 news-sentiment trigger evaluation (plan 10-08)` — the locked threshold (0.500), the
measured post-Tier-1 pooled `spearman_xp_med` from `benchmark_tier1.json`, and the mechanical
outcome: below 0.500 means plan 10-12 builds the sentiment experiment; at or above means it is
recorded not-triggered following the `capt_mc` precedent. Record the structural caveat Task 2
names: the benchmark's two scoreable seasons are 2021-22 and 2022-23, in which the availability
family has no coverage, so the availability arm cannot have moved this particular number — the
trigger is a measurement of whether the ranking gap is still open, not an attribution of why.
Also cite `danielfrees/mlpremier`'s published negative finding here, since it is the prior that
should govern how much tuning budget plan 10-12 spends if the trigger does fire.

Then update the Phase 10 results table: replace `pending` in the `measured` and `verdict` cells
for `availability_flags` and `transfermarkt_injury` with the numbers and the mechanical verdicts.
Leave `default` as `off` for any rejected flag (D-08: code stays merged, flag stays off). If a
criterion IS met, D-07's auto-adopt applies — but per D-10 an availability flag may not be
flipped default-on until `tests/test_availability.py::test_missing_snapshot_degrades_to_nan_not_raise`
is passing; state in the entry that it is (it has been since plan 10-01) and record the flip as
a deliberate `config.py` edit deferred to plan 10-16's close-out so that every default change in
this phase happens in one reviewable commit.

Edit `IMPROVEMENTS.md` by scoped replacement only. Do not reflow or rewrite Phase A-F content.
  </action>
  <verify>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import pathlib,sys; t=pathlib.Path('IMPROVEMENTS.md').read_text(); need=('### availability_flags: dual-criterion adoption verdict','### transfermarkt_injury:','### D-02 news-sentiment trigger evaluation','0.500','+25','+0.03','2,280','mlpremier','10.2478/ijcss-2025-0008'); miss=[k for k in need if k not in t]; sys.exit('ledger missing: '+str(miss)) if miss else print('all three verdict sections present')"</automated>
    <fails_when>non-zero exit, or the message `ledger missing:` listing an absent subsection heading or required figure</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import pathlib,re,sys; t=pathlib.Path('IMPROVEMENTS.md').read_text(); s=t.split('### Experiment results (Phase 10)')[1].split('###')[0]; rows=[l for l in s.splitlines() if l.strip().startswith('|') and ('availability_flags' in l or 'transfermarkt_injury' in l)]; sys.exit('expected 2 Tier-1 rows, got '+str(len(rows))) if len(rows)!=2 else None; bad=[l for l in rows if 'pending' in l]; sys.exit('a Tier-1 row is still pending: '+str(bad)) if bad else print('both Tier-1 rows filled')"</automated>
    <fails_when>non-zero exit, or a message naming a Tier-1 results row that is missing or still says `pending`</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import pathlib,sys; t=pathlib.Path('IMPROVEMENTS.md').read_text(); s=t.split('### Experiment results (Phase 10)')[1].split('###')[0]; sys.exit('a bracket row lost its pending state before the bracket ran') if s.count('pending') < 14 else print('seven bracket rows still pending as expected')"</automated>
    <fails_when>non-zero exit, or the message about a bracket row losing its pending state (fourteen = seven remaining flags x measured+verdict)</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import pathlib,sys; t=pathlib.Path('IMPROVEMENTS.md').read_text(); lost=[h for h in ('## Phase F','### Adoption criteria (D-05/D-06, pre-declared)','### understat:','### fotmob:','## Reference findings') if h not in t]; sys.exit('lost prior anchors: '+str(lost)) if lost else print('all prior anchors intact')"</automated>
    <fails_when>non-zero exit, or the message `lost prior anchors:` naming a Phase A-F heading the edit destroyed</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import config; assert not any(config.EXPERIMENTS.values()), [k for k,v in config.EXPERIMENTS.items() if v]; print('defaults unchanged; any flip is deferred to plan 10-16')"</automated>
    <fails_when>non-zero exit, or an AssertionError listing a flag flipped in this plan rather than in the single close-out commit</fails_when>
  </verify>
  <acceptance_criteria>
    - Three named subsections exist under `## Phase G`, each carrying its command, artifact path, measured table and mechanical verdict
    - The availability verdict cites the 2025-26 availability coverage figure and both the fresh base and Phase F's 2172
    - The injury verdict cites id-resolved coverage and the active-spell rate, and states the A1 unverified-prior caveat
    - The D-02 record states the locked 0.500, the measured value, the outcome, the two-scoreable-seasons caveat, and the mlpremier negative prior
    - Both Tier-1 rows in the Phase 10 results table carry numbers, not `pending`; the seven bracket rows still say `pending`
    - No `config.EXPERIMENTS` default was flipped in this plan
    - All Phase A-F anchor headings intact
  </acceptance_criteria>
  <done>Both Tier-1 experiments have numbered, mechanically-derived verdicts in the permanent ledger with their coverage caveats and unverified priors named, and the D-02 trigger outcome is recorded before plan 10-12 acts on it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tagged run artifacts -> ledger verdicts | a mis-read or cherry-picked number becomes the permanent record of what this phase found |
| `features.parquet` -> five measurement runs | a rebuild mid-plan makes two tagged runs non-comparable without any error signal |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-08-01 | Repudiation | a verdict back-fitted to the number that came out | high | mitigate | the criteria were written in plan 10-01 Task 3 in wave 1; this plan's verify prints each delta beside its pre-declared bar, and the action forbids adjusting any flag, lambda or threshold to chase a result |
| T-10-08-02 | Tampering | comparing two runs trained on different feature matrices | high | mitigate | Task 1 records `features.parquet`'s sha256; Task 2 carries a `<precondition>` asserting it is unchanged, and every A/B uses a freshly measured contemporaneous control rather than a historical figure |
| T-10-08-03 | Repudiation | reporting a partial-coverage result as if it were full-coverage | high | mitigate | the availability verdict must cite the measured 2025-26 coverage in the same paragraph as the verdict, and the D-02 record must state that the benchmark's two scoreable seasons carry no availability coverage at all |
| T-10-08-04 | Repudiation | citing an unverifiable paper as corroborating evidence | medium | mitigate | the injury verdict states the A1 caveat explicitly; the D-02 record cites mlpremier's negative finding rather than only the todo's optimistic framing |
| T-10-08-05 | Denial of Service | availability or injury attach failure breaking the daily pipeline | medium | mitigate | the fifth `data/build_table.py` block uses the same `try/except Exception` + skip print as the four existing ones |
</threat_model>

<verification>
- `python -m pytest` green repo-wide after the rebuild
- Four tagged walk-forward JSONs and three tagged benchmark JSONs present with correct flag states and scope
- `features.parquet` sha256 identical across all five runs
- Every `config.EXPERIMENTS` default still False at the end of this plan
- Both Tier-1 results rows filled; seven bracket rows still `pending`
- All Phase A-F anchors intact
</verification>

<success_criteria>
- Both Tier-1 experiments measured against their own pre-declared criteria on one frozen feature matrix
- Both verdicts in the ledger with numbers, coverage caveats and unverified priors named
- The D-02 trigger evaluated against the locked 0.500 and its outcome recorded
- No flag, threshold or default moved to chase a result
</success_criteria>

<output>
Create `.planning/phases/10-xp-experiment-follow-ups/10-08-SUMMARY.md` when done. It MUST
record: `features.parquet`'s sha256 and row/column counts, every measured figure with its bar
(both `model+chips` pairs, all three pooled Spearman values, both computed deltas), the
`tail -f` command and wall clock per detached run, the D-02 outcome, and — if any criterion was
met — the exact default flip deferred to plan 10-16.
</output>
