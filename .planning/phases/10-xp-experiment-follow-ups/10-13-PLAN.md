---
phase: 10-xp-experiment-follow-ups
plan: 13
type: execute
wave: 7
depends_on: ["10-11"]
files_modified:
  - models/bracket/registry.py
  - models/bracket/recurrent.py
  - models/bracket/transformer.py
  - colab/README.md
  - colab/bracket_deep.ipynb
  - tests/test_bracket.py
autonomous: false
requirements:
  - TODO-BRACKET
user_setup:
  - service: "Google Colab Pro"
    why: "D-13 puts the two GPU-bound sequence candidates on Colab and budgets all 200 owned compute units. The notebook has to be opened, run and its artifact downloaded by a human; nothing here can drive a Colab session."
    dashboard_config:
      - task: "Upload colab/bracket_deep.ipynb plus the exported sequence bundle to Colab, select a GPU runtime, run all cells, then download the produced per-season prediction parquet(s) into data/processed/experiments/colab/ and record the compute units consumed (shown in Colab's own usage panel)"
        location: "https://colab.research.google.com/"

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Both sequence candidates provably ignore padded timesteps: overwriting a masked position leaves the prediction bit-identical."
    - "The static side-vector is concatenated AFTER the encoder, never as an extra timestep (D-20)."
    - "Exactly one recurrent candidate exists (GRU, not LSTM) with the choice justified on the record (D-12)."
    - "manifest.json is the notebook's split authority: its per-test-season (train, val, test) triples are computed by the harness's own expanding-window rule, so a notebook cannot silently choose its own."
    - "Both candidates' frozen artifacts are scored through plan 10-09's validating local seam -- the local harness stays the sole judge (D-14)."
    - "Colab compute consumption is recorded per candidate against D-13's 200-unit budget, and any candidate that could not be run has a gate file saying so rather than a missing file."
  artifacts:
    - models/bracket/recurrent.py
    - models/bracket/transformer.py
    - colab/bracket_deep.ipynb
    - colab/README.md
    - "data/processed/experiments/bracket_gate_rnn.json and bracket_gate_transformer.json"
  key_links:
    - "models/bracket/sequence.py's padding mask -> src_key_padding_mask on the transformer encoder and last-non-padded-state selection in the GRU"
    - "manifest.json splits -> the Colab notebook's training loop (the anti-Pitfall-6 device)"
    - "the downloaded prediction parquet -> backtest/walk_forward.py::load_external_predictions -> run_season"
---

<objective>
Complete D-12's candidate roster with the two sequence models, run them on the compute D-13
assigns them, and score their frozen artifacts through plan 10-09's ingestion seam so the local
harness stays the sole judge.

Purpose: the recurrent and transformer candidates are the only two whose wall clock is genuinely
GPU-bound, which is why D-13's hybrid split sends them to Colab while Ridge, XGBoost, CatBoost
and the MLP ran locally. The infrastructure they need — the sequence builder, the training loop,
the granularity bracket, the gate schema, and the validating external-prediction seam — all
already exists and is tested, so this plan is the candidates plus the handoff, not new plumbing.

Output: `recurrent.py` (one of LSTM or GRU, not both, per D-12), `transformer.py` at the locked
size budget, all three deep candidates registered, a Colab notebook with a pinned-seed
walk-forward loop and a documented artifact contract, and gate results for both candidates with
their compute-unit consumption recorded.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-xp-experiment-follow-ups/10-CONTEXT.md
@.planning/phases/10-xp-experiment-follow-ups/10-RESEARCH.md
@.planning/phases/10-xp-experiment-follow-ups/10-PATTERNS.md
@.planning/phases/10-xp-experiment-follow-ups/10-09-SUMMARY.md
@.planning/phases/10-xp-experiment-follow-ups/10-10-SUMMARY.md
@.planning/phases/10-xp-experiment-follow-ups/10-11-SUMMARY.md
</context>

## Artifacts this phase produces

New symbols this plan creates (exclude from drift verification):
`models/bracket/recurrent.py` (`GruSeqRegressor`, `build_gru`, `_GRU_SPACE`);
`models/bracket/transformer.py` (`TransformerSeqRegressor`, `build_transformer`,
`_TRANSFORMER_SPACE`, `D_MODEL`, `N_HEAD`, `N_LAYERS`, `DIM_FF`, `DROPOUT`);
`models/bracket/registry.py` additions for `mlp`, `rnn`, `transformer` plus
`export_sequence_bundle`; `colab/README.md`; `colab/bracket_deep.ipynb`;
`tests/test_bracket.py` tests `test_sequence_regressors_consume_the_padding_mask`,
`test_transformer_size_matches_locked_budget`,
`test_exported_bundle_roundtrips_to_the_same_tensors`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Recurrent and transformer sequence regressors</name>
  <files>models/bracket/recurrent.py, models/bracket/transformer.py, models/bracket/registry.py, tests/test_bracket.py</files>
  <read_first>
    - models/bracket/sequence.py — the whole file as written by plan 10-11: `SEQ_WINDOW = 10`, `SEQ_STATS`, `STATIC_COLS`, `SequenceBundle`'s fields, and the left-padding plus `src_key_padding_mask`-convention boolean mask (True = padded)
    - models/bracket/deep.py — `train_torch_regressor`, `TorchRegressorAdapter`, `SEARCH_BUDGET = 12`, `_search_space`, `run_search`, `run_granularity_bracket`, and the docstring note that registry registration lands in this plan
    - models/bracket/registry.py — `CANDIDATES`, `is_available`, `build_regressor` as written by plan 10-10
    - models/bracket/gate.py — `played_only_spearman`, `GATE_MARGIN`, the gate JSON schema
    - optimize/rl_train.py — the raw-torch conventions (device, seed pinning, print cadence) both new modules follow
    - .planning/phases/10-xp-experiment-follow-ups/10-CONTEXT.md — D-12 ("one recurrent, LSTM or GRU, not both"), D-18, D-20; and 10-01-PLAN.md's decision table for the locked transformer size
    - .planning/phases/10-xp-experiment-follow-ups/10-RESEARCH.md — the Don't Hand-Roll entry on `src_key_padding_mask`
  </read_first>
  <behavior>
    - Both regressors accept a `SequenceBundle` and consume the padding mask: a padded timestep contributes nothing to the prediction, verified by asserting that changing a masked timestep's values leaves the prediction bit-identical
    - The recurrent model reads the sequence and takes the last non-padded hidden state, not the last index blindly
    - The transformer's encoder receives the mask as `src_key_padding_mask` and its pooled output averages over non-padded positions only
    - Both concatenate the static side-vector AFTER the encoder, per D-20, never as an extra timestep
    - Both expose the same `fit`/`predict` contract `TorchRegressorAdapter` establishes, so `models/train.py`'s stage-2 slot and `run_granularity_bracket` work unchanged
    - `registry.CANDIDATES` covers all seven `config.BRACKET_CANDIDATES` entries, and `is_available` returns False (not a raise) for any whose package is absent
  </behavior>
  <action>
Create `models/bracket/recurrent.py` with a GRU rather than an LSTM. D-12 requires exactly one
recurrent candidate and leaves the choice open; GRU is the pick because it has fewer parameters
for the same sequence length, which matters directly against D-13's fixed compute budget, and
because the 10-gameweek window is short enough that LSTM's extra gating buys little. Record that
reasoning in the module docstring so the choice is a decision on the record rather than an
accident.

`GruSeqRegressor(nn.Module)` — `nn.GRU(input_size=len(SEQ_STATS), hidden_size, num_layers,
batch_first=True)`, then select each row's last NON-PADDED hidden state using the mask (compute
each row's true length as `(~mask).sum(dim=1)` and gather at `length - 1`; a zero-length row —
a first appearance — takes a zeros vector). Do NOT use torch's sequence-packing helper with the
left-padded layout plan 10-11 produces: packing expects right-padding and would silently misread
every sequence. Record that constraint in the module docstring in your own words — do not write
the helper's function name into a comment, because this task's `<verify>` greps the file for
that identifier and a comment naming it would defeat its own gate. Concatenate the static
side-vector to the selected hidden state, then a small head to a scalar.

Create `models/bracket/transformer.py` with the locked size budget as module constants:
`D_MODEL = 64`, `N_HEAD = 4`, `N_LAYERS = 2`, `DIM_FF = 128`, `DROPOUT = 0.1`, each with a
comment referencing 10-01-PLAN.md's decision table.
`TransformerSeqRegressor(nn.Module)` — a linear input projection from `len(SEQ_STATS)` to
`D_MODEL`, a learned positional embedding over `SEQ_WINDOW` positions, then
`nn.TransformerEncoder(nn.TransformerEncoderLayer(d_model=D_MODEL, nhead=N_HEAD,
dim_feedforward=DIM_FF, dropout=DROPOUT, batch_first=True), num_layers=N_LAYERS)` called with
`src_key_padding_mask=mask`. Pool by mean over non-padded positions only (divide by
`(~mask).sum(dim=1)`, guarding the all-padded row with a zeros output rather than a division by
zero — that guard is a real case, since a first appearance is all-padded by construction).
Concatenate the static vector after pooling, then a head to a scalar.

Give each module a `_*_SPACE` deterministic search space of at most `SEARCH_BUDGET` configs and
a `build_*(objective, params)` factory returning a `TorchRegressorAdapter`-wrapped model, so
both plug into `run_granularity_bracket` unchanged.

In `models/bracket/registry.py`, add the three deep entries (`mlp` -> `deep.build_mlp`,
`rnn` -> `recurrent.build_gru`, `transformer` -> `transformer.build_transformer`) — the
registration plan 10-11's `deep.py` docstring deferred here. Import them lazily inside the
factory lookup rather than at module import time, so `models/bracket/registry.py` stays
importable on a machine without torch and `is_available("rnn")` can answer False instead of
raising at import.

Add the two named mask tests to `tests/test_bracket.py`. The mask test is the important one and
must be a real invariance check, not a shape check: build a synthetic bundle with known padding,
predict, then overwrite the padded timesteps with large arbitrary values, predict again, and
assert the two prediction vectors are bit-identical. A model that quietly attends to padding
would pass every shape assertion and produce a plausible, wrong number.
  </action>
  <verify>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m pytest tests/test_bracket.py -q</automated>
    <fails_when>non-zero exit, or the summary line reports any failed or errored test</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m pytest tests/test_bracket.py::test_sequence_regressors_consume_the_padding_mask -q</automated>
    <fails_when>non-zero exit, or `no tests ran` — a model that attends to padding passes every other assertion in this file, so this one must actually run</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "from models.bracket import transformer as t; assert (t.D_MODEL, t.N_HEAD, t.N_LAYERS, t.DIM_FF, t.DROPOUT) == (64, 4, 2, 128, 0.1), (t.D_MODEL, t.N_HEAD, t.N_LAYERS, t.DIM_FF, t.DROPOUT); print('locked size budget ok')"</automated>
    <fails_when>non-zero exit, or an AssertionError printing a size tuple other than (64, 4, 2, 128, 0.1)</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import config; from models.bracket import registry as r; missing=[c for c in config.BRACKET_CANDIDATES if c not in r.CANDIDATES]; assert not missing, missing; [print(c, r.is_available(c)) for c in config.BRACKET_CANDIDATES]"</automated>
    <fails_when>non-zero exit, or an AssertionError listing a `config.BRACKET_CANDIDATES` member absent from the registry</fails_when>
    <automated>grep -vE '^[[:space:]]*#' models/bracket/recurrent.py | grep -c "pack_padded_sequence"</automated>
    <fails_when>output is anything other than `0` — packing expects right-padding and would silently misread the left-padded layout plan 10-11 produces. Full-line comments are filtered out, but the action still forbids naming this identifier in a comment at all, since a docstring line would slip past the filter</fails_when>
    <automated>grep -c "src_key_padding_mask" models/bracket/transformer.py</automated>
    <fails_when>output is `0` — the encoder is not receiving the padding mask</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/ruff check .</automated>
    <fails_when>non-zero exit, or any diagnostic naming a file under models/bracket/</fails_when>
  </verify>
  <acceptance_criteria>
    - `models/bracket/recurrent.py` implements a GRU (not an LSTM) with the choice justified in its module docstring, and contains no sequence-packing call or mention of that helper's identifier anywhere, including in comments and docstrings
    - The recurrent model selects the last non-padded hidden state via `(~mask).sum(dim=1)`, with a zeros fallback for an all-padded row
    - `models/bracket/transformer.py`'s five size constants equal the locked budget and each cites 10-01-PLAN.md's decision table
    - The transformer passes `src_key_padding_mask` and mean-pools over non-padded positions with an all-padded guard
    - Both concatenate the static vector after the encoder, never as an extra timestep
    - `registry.CANDIDATES` covers all seven `config.BRACKET_CANDIDATES` entries, with lazy torch imports so `is_available` answers False rather than raising
    - `test_sequence_regressors_consume_the_padding_mask` proves bit-identical predictions when padded values are overwritten
  </acceptance_criteria>
  <done>Both sequence candidates exist at the locked sizes, provably ignore padded timesteps, concatenate the static vector where D-20 says, and are registered alongside the MLP so all seven candidates resolve through one registry.</done>
</task>

<task type="auto">
  <name>Task 2: Colab handoff — exported bundle, notebook, and the artifact contract</name>
  <files>models/bracket/registry.py, colab/README.md, colab/bracket_deep.ipynb, tests/test_bracket.py</files>
  <read_first>
    - backtest/walk_forward.py — `load_external_predictions`, `_EXTERNAL_PRED_COLS`, `_MAX_PLAUSIBLE_SPEARMAN_JUMP`, `_lgbm_played_spearman` and the `--external-preds` flag as written by plan 10-09; `DATA_SEASONS`, `TEST_SEASONS` and `_preds_for`'s expanding-window split logic (`train = DATA_SEASONS[:i-1]`, `val = DATA_SEASONS[i-1]`) that the notebook must reproduce exactly
    - .planning/phases/10-xp-experiment-follow-ups/10-09-SUMMARY.md — the exact artifact column contract a notebook must produce, and the recorded in-process LightGBM played-only Spearman every candidate is compared against
    - models/bracket/sequence.py — `SequenceBundle`'s fields and dtypes, which the export has to serialize losslessly
    - backtest/benchmark_external.py lines 25-27 and 143-182 — the two-step `--fetch`/default CLI shape, the loose model for a deliberate human-run step that produces a committed artifact
    - data/external/README.md — the five-section README shape `colab/README.md` adapts
    - .planning/phases/10-xp-experiment-follow-ups/10-RESEARCH.md — Pitfall 6 (a notebook re-implementing the split logic is the leakage risk) and Open Question 4 (the artifact carries predictions only; ground truth is re-attached locally)
  </read_first>
  <action>
Add `export_sequence_bundle(seasons, out_dir)` to `models/bracket/registry.py`: serialize the
`SequenceBundle` tensors plus the identity frame for the requested seasons into a small set of
`.npz` and `.parquet` files under `data/processed/experiments/colab_input/`, and write a
`manifest.json` recording `SEQ_WINDOW`, the ordered `SEQ_STATS` and `STATIC_COLS` lists, the
tensor shapes and dtypes, `features.parquet`'s sha256, and the exact
`(train_seasons, val_season, test_season)` triple per test season as computed by the same
expanding-window rule `_preds_for` uses. That manifest is the anti-Pitfall-6 device: the
notebook does not decide the splits, it reads them.

Write `colab/README.md` in `data/external/README.md`'s five-section shape, adapted:
`## Source` (which local command produced the input bundle), `## Attribution` (Google Colab Pro,
200 owned units, no new spend per D-13), `## Regeneration` (the exact local export command, the
upload/run/download steps, and the never-in-cron-or-CI sentence),
`## Reduction applied` (what the bundle contains and what it deliberately omits — no
`y_points`, no `y_minutes`, no identity beyond the join keys), and
`## PII spot-check` (the bundle carries no manager identity and no free text). Then add three
sections this handoff specifically needs:

`## Artifact contract` — the exact six columns `_EXTERNAL_PRED_COLS` requires
(`season`, `gw`, `player_code`, `fixture_id`, `xp_med`, `xp_mean`), the one-file-per-test-season
naming, and the two prohibitions stated as prohibitions: the artifact must NOT contain any
`y_*` ground-truth column (the local seam drops and warns about them), and it must NOT contain
more than one season per file (the seam raises on a multi-season dump).

`## Split contract` — the notebook must read `manifest.json`'s per-test-season
`(train_seasons, val_season, test_season)` triple and must never derive its own. Quote the
consequence: a notebook that trained one global model, or that accidentally included the test
season, produces a file that is schema-valid and leakage-corrupt, which the local seam can only
flag by implausibility rather than detect directly.

`## Compute accounting` — where Colab reports units consumed, and the requirement to record the
figure per candidate. D-13's budget is all 200 units with a hard stop at exhaustion, and an
unrecorded spend cannot be hard-stopped.

Create `colab/bracket_deep.ipynb` with cells in this order: (1) install the pinned
`torch`/`numpy`/`pandas`/`pyarrow` versions and print them; (2) mount or upload and load the
bundle plus `manifest.json`, asserting the manifest's `features_sha256` matches what the
downloader recorded; (3) define the candidate models by copying `recurrent.py` and
`transformer.py`'s classes verbatim into a cell — the notebook cannot import from this repo, so
state in a comment that these are copies and that a divergence between the two would invalidate
the comparison; (4) pin the seed from the manifest; (5) the per-test-season loop reading the
manifest's triples, training, predicting, and writing one parquet per test season with EXACTLY
the six contract columns; (6) print the per-candidate wall clock and remind the operator to
record units consumed. Keep the notebook free of any credential and of any repo path that would
only exist locally.

Add `test_exported_bundle_roundtrips_to_the_same_tensors` to `tests/test_bracket.py`: export one
season, reload the `.npz`/`.parquet` from disk, and assert the tensors are element-wise equal to
a fresh `build_sequences` call and that the manifest's split triples match
`backtest.walk_forward`'s own expanding-window computation for the same seasons. That second
assertion is what makes the manifest trustworthy as the notebook's split authority.
  </action>
  <verify>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "from models.bracket.registry import export_sequence_bundle as e; import config; p=e(['2025-26'], config.EXPERIMENTS_DIR/'colab_input'); print('exported to', p)"</automated>
    <fails_when>non-zero exit, or the export produces no files under `data/processed/experiments/colab_input/`</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import json,config,hashlib; m=json.loads((config.EXPERIMENTS_DIR/'colab_input'/'manifest.json').read_text()); need={'seq_window','seq_stats','static_cols','shapes','dtypes','features_sha256','splits'}; miss=[k for k in need if k not in m]; assert not miss, miss; h=hashlib.sha256((config.PROCESSED_DIR/'features.parquet').read_bytes()).hexdigest(); assert m['features_sha256']==h, 'manifest hash does not match the live feature matrix'; assert m['seq_window']==10, m['seq_window']; print('splits', m['splits'])"</automated>
    <fails_when>non-zero exit, or an AssertionError naming missing manifest keys, a hash mismatch against the live feature matrix, or a window other than 10</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import json,config; from backtest.walk_forward import DATA_SEASONS; m=json.loads((config.EXPERIMENTS_DIR/'colab_input'/'manifest.json').read_text()); bad=[]; 
for T,sp in m['splits'].items():
    i=DATA_SEASONS.index(T); exp_train=DATA_SEASONS[:i-1]; exp_val=DATA_SEASONS[i-1]
    if list(sp['train_seasons'])!=list(exp_train) or sp['val_season']!=exp_val: bad.append((T,sp,exp_train,exp_val))
assert not bad, bad
print('every manifest split matches _preds_for expanding-window rule')"</automated>
    <fails_when>non-zero exit, or an AssertionError listing a test season whose manifest split triple disagrees with `_preds_for`'s own expanding-window rule</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m pytest tests/test_bracket.py::test_exported_bundle_roundtrips_to_the_same_tensors -q</automated>
    <fails_when>non-zero exit, or `no tests ran`</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import json,pathlib,sys; nb=json.loads(pathlib.Path('colab/bracket_deep.ipynb').read_text()); src=''.join(''.join(c.get('source',[])) for c in nb['cells']); need=('manifest','xp_med','xp_mean','fixture_id','player_code','torch.manual_seed'); miss=[k for k in need if k not in src]; sys.exit('notebook missing: '+str(miss)) if miss else None; bad=[k for k in ('y_points','y_minutes','api_key','API_KEY','/home/sraja') if k in src]; sys.exit('notebook must not reference: '+str(bad)) if bad else print('notebook contract ok')"</automated>
    <fails_when>non-zero exit, the message `notebook missing:` listing an absent required token, or the message `notebook must not reference:` listing a ground-truth column, a credential name, or a local absolute path</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import pathlib,sys; t=pathlib.Path('colab/README.md').read_text(); need=('## Source','## Attribution','## Regeneration','## Reduction applied','## PII spot-check','## Artifact contract','## Split contract','## Compute accounting','never wired into cron or CI','fixture_id'); miss=[k for k in need if k not in t]; sys.exit('README missing: '+str(miss)) if miss else print('README complete')"</automated>
    <fails_when>non-zero exit, or the message `README missing:` listing an absent section or phrase</fails_when>
    <automated>git status --porcelain data/processed/experiments/colab_input</automated>
    <fails_when>the output is non-empty — the exported bundle lives under gitignored `data/processed/` and must never be staged</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/ruff check .</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `export_sequence_bundle` writes tensors plus a `manifest.json` carrying `seq_window`, `seq_stats`, `static_cols`, `shapes`, `dtypes`, `features_sha256` and `splits`
    - Every manifest split triple matches `backtest.walk_forward`'s own expanding-window rule, asserted both in the verify and by the round-trip test
    - `colab/README.md` has all eight sections including `## Artifact contract`, `## Split contract` and `## Compute accounting`
    - `colab/bracket_deep.ipynb` references the manifest, pins a seed, and emits the six contract columns; it contains no `y_points`/`y_minutes` reference, no credential name and no local absolute path
    - The exported bundle is untracked (`git status --porcelain` empty for that path)
    - `test_exported_bundle_roundtrips_to_the_same_tensors` passes
  </acceptance_criteria>
  <done>A lossless sequence bundle, a manifest that is the notebook's split authority, a README stating the artifact and split contracts as prohibitions, and a credential-free notebook that emits exactly what plan 10-09's seam accepts.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: Run the two candidates on Colab and score their artifacts locally</name>
  <action>Run the GRU and transformer candidates in a Colab GPU session and download their prediction artifacts</action>
  <instructions>
Everything automatable is done: the sequence bundle is exported to
`data/processed/experiments/colab_input/` with a `manifest.json` whose split triples I verified
against the harness's own expanding-window rule, `colab/bracket_deep.ipynb` is written and
credential-free, and `colab/README.md` states the artifact and split contracts. Nothing in this
repository can drive a Colab session, so the run itself is human-only.

  1. Upload `colab/bracket_deep.ipynb` and the exported bundle to Colab. Select a GPU runtime.
  2. Run all cells for the **GRU** candidate first, then the **transformer**. Follow
     `colab/README.md`'s `## Split contract`: the notebook reads its splits from `manifest.json`
     and must not be edited to derive its own.
  3. Download the produced per-test-season prediction parquets into
     `data/processed/experiments/colab/` as `colab_<candidate>_<season>.parquet`.
  4. **Record the compute units consumed per candidate** from Colab's usage panel, before and
     after each run. D-13's budget is all 200 owned units with a hard stop at exhaustion. If the
     GRU run alone consumes enough that the transformer cannot complete, stop and report the
     remaining balance rather than starting a run that will be killed mid-way — a truncated run
     produces a partial artifact that still looks scoreable.
  5. Paste into your response: per-candidate wall clock, units consumed and remaining, the
     downloaded filenames, and confirmation that you did not edit the notebook's split handling.

Once the artifacts land I will do the rest myself — the local harness is the sole judge (D-14):

    python -m backtest.walk_forward --external-preds data/processed/experiments/colab/colab_rnn_2025-26.parquet --seasons 2025-26 --replicas 5 --tag bracket_rnn_2526
    python -m backtest.walk_forward --external-preds data/processed/experiments/colab/colab_transformer_2025-26.parquet --seasons 2025-26 --replicas 5 --tag bracket_transformer_2526

I will watch for the `[external] IMPLAUSIBLE` warning plan 10-09 added. If it fires I will NOT
discard the number — it gets recorded WITH the flag raised, exactly as Phase 9 recorded its
`+337-optimistic-vs-+40-honest` finding. An implausible jump is a finding about the notebook's
split handling, not a result to hide.

I will then write each candidate's gate result to
`data/processed/experiments/bracket_gate_rnn.json` and `bracket_gate_transformer.json` in the
same schema `models/bracket/gate.py::run_gate` produces, using
`models.bracket.gate.played_only_spearman` and comparing against plan 10-10's recorded LightGBM
val-split baseline at `GATE_MARGIN = 0.010`. A candidate that could not be run at all gets its
gate file with `"status": "not_run_compute_exhausted"` and the remaining balance — an unrun
candidate must be recorded as unrun, never left as a missing file that plan 10-14's comparison
table would read as an oversight.
  </instructions>
  <verification>I will check that both `colab_<candidate>_2025-26.parquet` files parse, that `load_external_predictions` accepts them (season-set, row-count, column and ground-truth validation), that all seven `bracket_gate_*.json` files exist, and that both external-scored runs record `multi_safe` as `None`</verification>
  <resume-signal>Paste the wall clock, units consumed/remaining and downloaded filenames — or type "exhausted" with the remaining balance if the budget ran out</resume-signal>
  <verify>
    <human-check>Per-candidate wall clock, Colab units consumed and remaining, the downloaded artifact filenames, and any `[external] IMPLAUSIBLE` warning text are all pasted into the checkpoint response</human-check>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import json,pathlib,glob; ps=sorted(glob.glob('data/processed/experiments/bracket_gate_*.json')); got={pathlib.Path(p).stem.replace('bracket_gate_','') for p in ps}; need={'lgbm','ridge','xgb','catboost','mlp','rnn','transformer'}; miss=need-got; assert not miss, miss; [print(c, json.loads(open(f'data/processed/experiments/bracket_gate_{c}.json').read()).get('status'), json.loads(open(f'data/processed/experiments/bracket_gate_{c}.json').read()).get('spearman_xp_med')) for c in sorted(need)]"</automated>
    <fails_when>non-zero exit, or an AssertionError listing a candidate with no gate JSON — every one of the seven must have a file, including an unrun one carrying `status: not_run_compute_exhausted`</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import json,glob; ok=[]; 
for p in sorted(glob.glob('data/processed/experiments/wf_bracket_*_2526.json')):
    s=json.loads(open(p).read()); ok.append((p.split('wf_')[1], s['model+chips'], s.get('multi_safe')))
print(ok); assert ok, 'no external-scored walk-forward artifact was produced'
assert all(m is None for _,_,m in ok), 'multi_safe must be None for a frozen-artifact run'"</automated>
    <fails_when>non-zero exit, an AssertionError stating no external-scored artifact exists, or an AssertionError stating `multi_safe` was populated (plan 10-09 requires it to be `None` for a frozen-artifact run rather than a fabricated figure)</fails_when>
  </verify>
  <acceptance_criteria>
    - All seven `bracket_gate_<candidate>.json` files exist; any unrun candidate carries `status: not_run_compute_exhausted` with the remaining unit balance
    - Both external-scored tagged walk-forward artifacts exist with `multi_safe` recorded as `None`
    - The SUMMARY records per-candidate Colab wall clock, units consumed and remaining, and the downloaded filenames
    - Any `[external] IMPLAUSIBLE` warning is recorded verbatim with its number retained, not discarded
    - Each candidate's gate result uses `models.bracket.gate.played_only_spearman` and is compared against plan 10-10's LightGBM val-split baseline at `GATE_MARGIN = 0.010`
    - The notebook was not edited to derive its own splits (confirmed in the checkpoint response)
  </acceptance_criteria>
  <done>Both sequence candidates have been trained on the assigned compute, their frozen artifacts scored through the validating local seam, their unit consumption recorded against D-13's budget, and every one of the seven candidates has a gate file — including any that could not be run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a Colab session -> a downloaded prediction parquet -> a ledger verdict | untrusted, externally-trained output crosses into this project's adoption decision |
| `manifest.json` -> the notebook's split choice | if the notebook derives its own splits, the whole comparison is unverifiable |
| the exported bundle -> Colab | whatever is in the bundle leaves this machine |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-13-01 | Tampering | a model attending to padded timesteps and producing a plausible wrong number | critical | mitigate | `src_key_padding_mask` on the encoder, last-non-padded-state selection in the GRU, no `pack_padded_sequence` against a left-padded layout, and a bit-identical-prediction invariance test when padded values are overwritten |
| T-10-13-02 | Tampering | a notebook deriving its own splits and leaking the test season | critical | mitigate | `manifest.json` carries the per-test-season split triples computed by `_preds_for`'s own rule, the README states the split contract as a prohibition, a verify asserts the manifest matches the harness, and plan 10-09's implausibility check flags the symptom on arrival |
| T-10-13-03 | Tampering | notebook model classes drifting from the repository's own | high | mitigate | the notebook's copied classes are labelled as copies in a comment stating that a divergence invalidates the comparison; the manifest's `features_sha256` binds the run to one input vintage |
| T-10-13-04 | Information Disclosure | credentials or local paths embedded in a notebook that gets uploaded | high | mitigate | a verify greps the notebook JSON for credential names and local absolute paths and fails on any hit; the bundle carries no `y_*` ground truth and no identity beyond the join keys |
| T-10-13-05 | Repudiation | an unrun candidate silently absent from the comparison | high | mitigate | an unrun candidate must still get a gate file with `status: not_run_compute_exhausted` and the remaining balance; the verify asserts all seven files exist |
| T-10-13-06 | Repudiation | an unrecorded compute spend making D-13's hard stop unenforceable | medium | mitigate | units consumed and remaining are recorded per candidate in the checkpoint response and the SUMMARY, and the action forbids starting a run that cannot complete within the remaining balance |
</threat_model>

<verification>
- `python -m pytest` green repo-wide, with the padding-mask invariance test actually running
- `ruff check .` clean
- The transformer's five size constants equal the locked budget
- All seven candidates present in `registry.CANDIDATES` and all seven gate JSONs on disk
- `manifest.json` split triples match the harness's own expanding-window rule
- The notebook contains no credential, no local path and no ground-truth column
- Both external-scored runs record `multi_safe` as `None`
- The exported bundle is untracked
</verification>

<success_criteria>
- D-12's roster is complete: one recurrent (GRU, with the choice justified) and one small transformer at the locked size
- Both provably ignore padded timesteps and concatenate the static vector after the encoder per D-20
- The Colab handoff makes the manifest the split authority, so a notebook cannot silently choose its own
- Both candidates scored through the local validating seam, with compute consumption recorded against D-13's budget and any unrun candidate recorded as unrun
</success_criteria>

<output>
Create `.planning/phases/10-xp-experiment-follow-ups/10-13-SUMMARY.md` when done. It MUST
record: per-candidate Colab wall clock, units consumed and remaining out of 200, the downloaded
artifact filenames, all seven candidates' gate Spearman figures with deltas and ADVANCE/HOLD
verdicts, any `[external] IMPLAUSIBLE` warning verbatim, and the GRU-over-LSTM rationale.
</output>
