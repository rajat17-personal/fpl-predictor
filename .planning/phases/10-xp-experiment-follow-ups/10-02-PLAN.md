---
phase: 10-xp-experiment-follow-ups
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - data/fpl_standings.py
  - data/fplreview.py
  - predict/scoreboard.py
  - data/external/fplreview/README.md
  - tests/test_scoreboard.py
autonomous: true
requirements:
  - TODO-TOP100
  - TODO-FPLREVIEW
user_setup:
  - service: fplreview
    why: "Weekly free-model projection capture for the Tier-3 diagnostic benchmark. Their site returns HTTP 403 to automated access and their ToS blocks ingestion, so acquisition is deliberately manual (10-RESEARCH.md Pitfall 5, confirmed 403 this research session)."
    dashboard_config:
      - task: "Before each gameweek deadline: open app.fplreview.com/free, export or copy the current gameweek's projection table, and save it as data/external/fplreview/fplreview_<season>_gw<NN>.csv with at minimum the columns the README declares (player name, team, position, projected points for the upcoming gameweek)"
        location: "https://app.fplreview.com/free"

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The deadline-gated fplreview capture slot exists and validates a fresh manual paste immediately, so accumulation can start this gameweek rather than after the rest of the phase."
    - "Top-100 consensus ownership is fetchable from the FPL API and aggregated to player counts, with no manager identity persisted anywhere."
    - "Both benchmarks score as independently-optional keys in the existing post-gameweek scoreboard entry; with neither source present the entry is byte-identical to today's."
    - "Nothing automated ever touches fplreview.com -- data/fplreview.py contains zero requests references, enforced by a verify."
    - "The weekly product surface is untouched: predict/live.py and predict/export.py are unchanged and tests/test_product.py still passes."
  artifacts:
    - data/fpl_standings.py
    - data/fplreview.py
    - data/external/fplreview/README.md
    - tests/test_scoreboard.py
  key_links:
    - "data/fpl_standings.load_consensus -> predict/scoreboard.py::score_gw's consensus block"
    - "data/fplreview.load_gw -> predict/scoreboard.py::score_gw's fplreview block, joined through data/id_crosswalk.py"
    - "predict/scoreboard.py -> web/data/scoreboard.json (a published trust artifact -- anything written here is public)"
---

<objective>
Land D-01's Tier-3 work first: two near-zero-cost diagnostic benchmark columns in
`predict/scoreboard.py` — the top-100-manager consensus ownership ranking (fetched from the
FPL API) and fplreview's free-model projections (captured manually, ToS-constrained).

Purpose: D-01 sequences Tier 3 first precisely because the fplreview capture is
deadline-gated — a gameweek that passes without a manual capture is data permanently lost.
This plan makes the capture slot exist now so it can start accumulating, and pays for the
top-100 fetch (which is genuinely near-zero cost) in the same pass.

Output: `data/fpl_standings.py`, `data/fplreview.py`, two new scoreboard entry keys, a
committed-snapshot README for `data/external/fplreview/`, and `tests/test_scoreboard.py`
(the first dedicated test file `predict/scoreboard.py` has ever had).

Both benchmarks are DIAGNOSTIC ONLY. Neither ever becomes a model feature, and the fplreview
data is never redistributed (ToS). Nothing in `predict/live.py` or `predict/export.py` changes.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-xp-experiment-follow-ups/10-CONTEXT.md
@.planning/phases/10-xp-experiment-follow-ups/10-RESEARCH.md
@.planning/phases/10-xp-experiment-follow-ups/10-PATTERNS.md
@.planning/todos/pending/2026-09-10-top100-consensus-benchmark.md
@.planning/todos/pending/2026-09-10-fplreview-scoreboard-benchmark.md
</context>

## Artifacts this phase produces

New symbols this plan creates (exclude from drift verification): modules
`data/fpl_standings.py`, `data/fplreview.py`; tests `tests/test_scoreboard.py`;
directory `data/external/fplreview/` with its `README.md`;
`data/fpl_standings.py` symbols `STANDINGS_ENABLED`, `OVERALL_LEAGUE_ID`, `_MIN_INTERVAL_S`,
`_throttle`, `_require`, `fetch_standings`, `fetch_entry_picks`, `consensus_ownership`,
`build`, `load_consensus`, `main`; `data/fplreview.py` symbols `FPLREVIEW_DIR`,
`_REQUIRED_COLS`, `load_gw`, `validate`, `main`; `predict/scoreboard.py` entry keys
`mae_consensus`, `spearman_consensus`, `spearman_consensus_vs_model`, `n_consensus`,
`mae_fplreview`, `spearman_fplreview`, `n_fplreview`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Top-100 consensus ownership fetcher</name>
  <files>data/fpl_standings.py, tests/test_scoreboard.py</files>
  <read_first>
    - predict/scoreboard.py lines 24-36 — the `_HEADERS = {"User-Agent": "fpl-ml-project/0.1"}` module constant and `fetch_actuals`'s `requests.get(...)` + `raise_for_status()` + list-comprehension-to-DataFrame shape (the exact pattern to mirror)
    - data/fotmob.py lines 59-170 — `FOTMOB_ENABLED` kill switch, `_throttle()`, `_require(payload, path, expected_type, what)`, and the cache-then-fetch helper that returns `None` on `requests.RequestException` rather than raising
    - data/snapshot.py lines 90-125 — `_fetch_bootstrap`'s retry/backoff loop, the established shape for a required FPL API call
    - tests/test_cron.py lines 1-60 — the `responses`-based HTTP stubbing convention (`@responses.activate`, `responses.add(responses.GET, URL, json=...)`) so no test hits the network
    - .planning/phases/10-xp-experiment-follow-ups/10-RESEARCH.md — Code Examples (the standings 3-page loop) and Assumptions Log A3 (league id 314 is community-documented, NOT independently verified)
  </read_first>
  <behavior>
    - `_require` raises `ValueError` naming the missing key when a standings payload lacks `standings` or `standings.results`
    - `consensus_ownership` given picks for N managers returns one row per `player_id` with `n_owners` and `consensus_pct = n_owners / N`, ranked descending, and never counts a manager twice
    - `fetch_standings` stops paging as soon as a page returns an empty `results` list
    - `fetch_entry_picks` returns an empty list (not a raise) when a single manager's picks endpoint 404s — one unavailable manager must not kill the whole consensus
    - `build` writes `data/processed/consensus_<season>_gw<NN>.parquet` and `load_consensus(gw)` returns `None` when the file is absent
  </behavior>
  <action>
Create `data/fpl_standings.py` following `data/fotmob.py`'s optional-enrichment skeleton, with
`STANDINGS_ENABLED = True` as the kill switch, `_MIN_INTERVAL_S = 1.0`, `_throttle()` copied in
shape from `data/fotmob.py` lines 114-121, and the same
`_HEADERS = {"User-Agent": "fpl-ml-project/0.1"}` string `predict/scoreboard.py` already uses.
Write the on-disk cache under `config.RAW_DIR / "standings"` via `ops.jsonio.read_json` /
`write_json`, exactly as `data/fotmob.py` caches its pages.

`OVERALL_LEAGUE_ID = 314` with a comment recording that this is community-documented and
NOT independently verified (10-RESEARCH.md Assumptions Log A3), plus the spot-check
instruction: on the first real run, print the top-3 entry names and total-points figures from
page 1 so a human can eyeball that this is the genuine global Overall league and not an
arbitrary one. Make the id an argument default, not a hardcoded literal inside the URL, so a
correction is a one-line change.

`fetch_standings(gw, *, league_id=OVERALL_LEAGUE_ID, pages=3, top_n=100) -> list[dict]` GETs
`{config.FPL_API}/leagues-classic/{league_id}/standings/` with `params={"page_standings": p}`
for `p` in `1..pages`, `raise_for_status()`, extracts `standings.results` through `_require`,
breaks on an empty page, and returns the first `top_n` rows.

`fetch_entry_picks(entry_id, gw) -> list[dict]` GETs
`{config.FPL_API}/entry/{entry_id}/event/{gw}/picks/` and returns `payload["picks"]` via
`_require`. Catch `requests.RequestException` and return `[]` with a
`[standings] picks unavailable for entry {entry_id}: {exc}` print — one missing manager
degrades the consensus, it does not fail it.

`consensus_ownership(picks_by_entry) -> pd.DataFrame` counts distinct owning entries per
`element` id, returning columns `player_id`, `n_owners`, `consensus_pct`,
`consensus_rank` (1 = most owned), sorted descending by `n_owners`. Deduplicate within a single
manager's picks first so a bench/captain duplicate cannot double-count.

`build(gw, *, force=False)` chains the three, writes
`config.PROCESSED_DIR / f"consensus_{season}_gw{gw:02d}.parquet"` (season derived from
`config.SEASONS[-1]`), and prints total managers fetched plus distinct players owned.
`load_consensus(gw)` returns the parquet or `None`. `main()` exposes `--gw` (required) and
`--force`, following `data/fotmob.py::main`'s argparse shape.

Create `tests/test_scoreboard.py` and write the five behaviors above as tests using
`responses` to stub every FPL URL — this module must have zero network access in the test
suite. Include a test that stubs a 404 on one of three managers' picks and asserts the
consensus still covers the other two.
  </action>
  <verify>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m pytest tests/test_scoreboard.py -q</automated>
    <fails_when>non-zero exit, or the summary line reports any failed or errored test, or `no tests ran`</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import data.fpl_standings as s; assert s.STANDINGS_ENABLED is True; assert s.OVERALL_LEAGUE_ID == 314; assert callable(s._throttle) and callable(s._require) and callable(s.consensus_ownership) and callable(s.load_consensus); print('surface ok')"</automated>
    <fails_when>non-zero exit, an ImportError, or an AttributeError naming a missing symbol</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m data.fpl_standings --gw 1</automated>
    <fails_when>non-zero exit, or the printed manager count is 0 (the standings fetch returned nothing real)</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/ruff check .</automated>
    <fails_when>non-zero exit, or any diagnostic line naming data/fpl_standings.py</fails_when>
  </verify>
  <acceptance_criteria>
    - `data/fpl_standings.py` defines `STANDINGS_ENABLED`, `OVERALL_LEAGUE_ID`, `_MIN_INTERVAL_S`, `_throttle`, `_require`, `fetch_standings`, `fetch_entry_picks`, `consensus_ownership`, `build`, `load_consensus`, `main`
    - The `314` literal appears exactly once, as `OVERALL_LEAGUE_ID`'s value, and never inside a URL f-string: `grep -c "leagues-classic/314" data/fpl_standings.py` returns `0`
    - `consensus_ownership`'s output columns are exactly `player_id`, `n_owners`, `consensus_pct`, `consensus_rank`
    - `tests/test_scoreboard.py` uses `responses` and contains no live URL fetch: `grep -c "responses" tests/test_scoreboard.py` is non-zero
    - The real `--gw 1` run prints the top-3 entry names for the A3 spot-check, and the SUMMARY records them verbatim
  </acceptance_criteria>
  <done>Top-100 consensus ownership is fetchable, cached, shape-validated, degrades on a single unavailable manager, and the league-id assumption has been eyeballed against a real response.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: fplreview manual-capture slot with a validating reader</name>
  <files>data/fplreview.py, data/external/fplreview/README.md, tests/test_scoreboard.py</files>
  <read_first>
    - data/external/README.md — the five-section committed-snapshot shape to replicate: `## Source` (URL, retrieval date, revision), `## Attribution`, `## Regeneration` (the exact command, and the "deliberate, human-run, network-using step — never wired into cron or CI" sentence), `## Reduction applied`, `## PII spot-check`
    - backtest/benchmark_external.py lines 25-27 and 98-142 — `_TIDY_COLS` plus `_reduce_projection_csv`'s tolerant-header handling, the precedent for reading a third party's CSV whose layout varies between vintages
    - .planning/phases/10-xp-experiment-follow-ups/10-RESEARCH.md — Pitfall 5 (confirmed HTTP 403 to automated access; do not spend any budget on a scraper)
    - .planning/todos/pending/2026-09-10-fplreview-scoreboard-benchmark.md — the ToS constraint: diagnostic only, never redistributed, never a model input
  </read_first>
  <behavior>
    - `validate` raises `ValueError` naming every missing required column when a captured CSV lacks one
    - `validate` raises `ValueError` when the projected-points column is entirely non-numeric (a copy-paste that captured headers only)
    - `load_gw(season, gw)` returns `None` when no capture file exists for that gameweek — an uncaptured gameweek is simply absent from the scoreboard, never an error
    - `load_gw` resolves each captured row to a `player_code` through `data.id_crosswalk.resolve_by_name` and drops unresolved names, printing the resolution rate
    - A capture file with duplicate player names for one gameweek raises rather than silently multiplying rows in the downstream join
  </behavior>
  <action>
Create `data/external/fplreview/README.md` following `data/external/README.md`'s five-section
shape exactly. `## Source` records `https://app.fplreview.com/free`, the manual acquisition
method, and that no automated retrieval is attempted because their site returns HTTP 403 to
automated access (observed 2026-09-10). `## Attribution` credits FPL Review. `## Regeneration`
documents the human weekly step verbatim as it appears in this plan's `user_setup` — including
the exact filename pattern `fplreview_<season>_gw<NN>.csv` and the required columns — and
states in one sentence that this is a deliberate, human-run step never wired into cron or CI.
`## Reduction applied` records that only name / team / position / projected-points are kept and
every other column of their table is dropped. Add a sixth section,
`## ToS and redistribution posture`, stating in plain terms: these captures are a local
diagnostic benchmark only; they are never redistributed beyond this repository, never published,
and never used as a model input — enforced by the fact that no `config.EXPERIMENTS` flag and no
`config.*_COLS` constant references this directory.

Create `data/fplreview.py` with `FPLREVIEW_DIR = config.DATA_DIR / "external" / "fplreview"`
and `_REQUIRED_COLS` naming the four kept columns as they must appear after normalization
(`name`, `team`, `position`, `proj_pts`). It has no fetcher and no `requests` import at all —
that absence is the ToS mitigation, and it is asserted by this task's verify. Implement
`validate(df, path)` raising `ValueError` that names every missing column at once (not the
first), plus the all-non-numeric check; `load_gw(season, gw)` reading
`FPLREVIEW_DIR / f"fplreview_{season}_gw{gw:02d}.csv"`, returning `None` when absent,
lower-casing and stripping header whitespace before matching so a hand-pasted header still
resolves, resolving names via `data.id_crosswalk.resolve_by_name` (never a second name
matcher — 10-PATTERNS.md's mandatory-reuse rule), dropping unresolved rows, printing
`[fplreview] gw{NN}: {n} rows, {pct} names resolved`, and raising `AssertionError` on a
duplicate `player_code` within one gameweek; and `main()` with `--season`/`--gw` that just runs
`load_gw` and prints the validation result, so a human can check a fresh capture immediately
after saving it.

Extend `tests/test_scoreboard.py` with the five behaviors above, writing tiny CSV fixtures into
`tmp_path` and monkeypatching `data.fplreview.FPLREVIEW_DIR` — no test reads a real capture file.
  </action>
  <verify>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m pytest tests/test_scoreboard.py -q</automated>
    <fails_when>non-zero exit, or the summary line reports any failed or errored test</fails_when>
    <automated>grep -cE '^[[:space:]]*(import requests|from requests|import httpx|from httpx|import urllib)' data/fplreview.py</automated>
    <fails_when>output is anything other than `0` — an HTTP-client import means an automated-fetch path was added against a 403-ing, ToS-restricted target. The pattern matches import statements only, so a prose comment explaining the prohibition cannot invalidate its own gate</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import data.fplreview as f; print(sorted(f._REQUIRED_COLS)); assert f.load_gw('2026-27', 38) is None, 'an absent capture must return None, not raise'; print('absent-capture path ok')"</automated>
    <fails_when>non-zero exit, or an AssertionError on the absent-capture path</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import pathlib,sys; t=pathlib.Path('data/external/fplreview/README.md').read_text(); need=('## Source','## Attribution','## Regeneration','## Reduction applied','## PII spot-check','## ToS and redistribution posture','app.fplreview.com/free','403','never wired into cron or CI','fplreview_'); miss=[k for k in need if k not in t]; sys.exit('README missing: '+str(miss)) if miss else print('README complete')"</automated>
    <fails_when>non-zero exit, or the message `README missing:` listing an absent section heading or required phrase</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/ruff check .</automated>
    <fails_when>non-zero exit, or any diagnostic naming data/fplreview.py</fails_when>
  </verify>
  <acceptance_criteria>
    - `data/fplreview.py` contains zero HTTP-client import statements and zero `http`-scheme URL string literals
    - `data/fplreview.py` resolves names only through `data.id_crosswalk.resolve_by_name`: `grep -c "id_crosswalk" data/fplreview.py` is non-zero and no second name-matching dict is defined in the file
    - `validate` names ALL missing required columns in one `ValueError`, verified by a test asserting two missing names both appear in the message
    - `load_gw` on an absent gameweek returns `None`
    - `data/external/fplreview/README.md` has all six sections plus the ToS posture paragraph
  </acceptance_criteria>
  <done>The weekly manual capture has a documented destination, a filename contract, a validating reader that fails loudly on a bad paste, and a code-level guarantee that nothing automated ever touches fplreview.com.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Score both benchmarks in the post-gameweek scoreboard</name>
  <files>predict/scoreboard.py, tests/test_scoreboard.py</files>
  <read_first>
    - predict/scoreboard.py — the whole file: `score_gw`'s `entry` dict construction, the existing `mae_model`/`spearman_model` and conditional `mae_fpl`/`spearman_fpl` blocks, `running_summary`'s `if "mae_fpl" in df` conditional-column pattern, and `update`'s frozen-file loop
    - backtest/benchmark_external.py lines 257-265 — `_stats_block`'s exact `round(float(spearmanr(a, b).statistic), 4)` form (reuse this call, never a new metrics helper — 10-RESEARCH.md's Don't Hand-Roll table)
    - data/fpl_standings.py and data/fplreview.py — as written by Tasks 1 and 2
    - web/data/history/gw4.json — a real frozen prediction file, to confirm the `players` row shape (`player_id`, `name`, `team`, `xp`, `xp_capt`, `ep_next`) the new columns must join against
  </read_first>
  <behavior>
    - When a consensus parquet exists for a gameweek, `score_gw` adds `n_consensus`, `mae_consensus` (consensus rank vs actual-points rank is not an MAE — record `spearman_consensus` against actual points and `spearman_consensus_vs_model` against our xP instead), and both Spearman figures
    - When no consensus parquet exists, none of those keys appear and the entry is otherwise byte-identical to today's output
    - When an fplreview capture exists, `score_gw` adds `n_fplreview`, `mae_fplreview` and `spearman_fplreview` on common played rows only
    - When no capture exists, none of those keys appear
    - `running_summary` averages each new key only over the entries that carry it, mirroring the existing `mae_fpl` conditional
    - A gameweek scored before either benchmark existed can be rescored with `--force` and gains the new keys without losing any existing key
  </behavior>
  <action>
Extend `predict/scoreboard.py::score_gw` with two additive, independently-optional blocks
placed after the existing `mae_fpl` block, so an entry produced without either benchmark is
byte-identical to today's. Do not create a parallel scoring path (10-PATTERNS.md: extend
`score_gw`/`running_summary`, never fork them).

Consensus block: call `data.fpl_standings.load_consensus(gw)`; on `None`, skip entirely. On a
frame, inner-join to `pred` on `player_id`, then set `n_consensus`,
`spearman_consensus` = Spearman of `consensus_pct` against `actual`, and
`spearman_consensus_vs_model` = Spearman of `consensus_pct` against our `xp`. Do NOT emit an
MAE for this column — a consensus ownership percentage is not in points units and an MAE
against points would be a meaningless number in a published trust artifact. Record that
reasoning in a one-line comment.

fplreview block: call `data.fplreview.load_gw(season, gw)`; on `None`, skip. On a frame, inner
join to `pred` — join on `player_code` where the frozen file carries it, otherwise resolve
`pred.player_id` through the same crosswalk rather than matching on display names — filter to
`minutes > 0` (played-only, matching `backtest/benchmark_external.py`'s own basis so the two
numbers stay comparable), then set `n_fplreview`, `mae_fplreview` and `spearman_fplreview`
using the identical `spearmanr(...).statistic` and `.abs().mean()` forms already in this file.

Extend `running_summary` with one conditional per new key, mirroring the existing
`if "mae_fpl" in df` shape, so an average is only reported over entries that actually carry the
column.

Add a module-docstring paragraph stating that both new columns are DIAGNOSTIC ONLY: they are
never model inputs, and the fplreview figures are never redistributed (ToS). Extend
`tests/test_scoreboard.py` with the six behaviors above, building synthetic frozen-file dicts
and monkeypatching both loaders — including the both-absent case that must reproduce today's
entry keys exactly.
  </action>
  <verify>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m pytest tests/test_scoreboard.py -q</automated>
    <fails_when>non-zero exit, or the summary line reports any failed or errored test</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import predict.scoreboard as sb, inspect; src=inspect.getsource(sb.score_gw); assert 'load_consensus' in src and 'load_gw' in src, 'a benchmark block is missing from score_gw'; assert 'mae_consensus' not in src, 'an ownership percentage must not be scored as a points MAE'; s2=inspect.getsource(sb.running_summary); assert s2.count('in df') >= 3, 'running_summary lacks a conditional per new key'; print('scoreboard wiring ok')"</automated>
    <fails_when>non-zero exit, or an AssertionError naming a missing benchmark block, a points-MAE on ownership, or a missing summary conditional</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m pytest tests/test_product.py -q</automated>
    <fails_when>non-zero exit — the export-contract regression test proves the product surface is unchanged</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m pytest -q</automated>
    <fails_when>non-zero exit, or the summary line reports any failed or errored test</fails_when>
    <automated>grep -v '^#' predict/scoreboard.py | grep -c "fplreview" </automated>
    <fails_when>output is `0` — the fplreview block never landed in code (comment-only changes do not count)</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/ruff check .</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `score_gw` emits `n_consensus`, `spearman_consensus`, `spearman_consensus_vs_model` when a consensus parquet exists and none of them otherwise
    - `score_gw` emits `n_fplreview`, `mae_fplreview`, `spearman_fplreview` when a capture exists and none of them otherwise
    - `score_gw` never emits an `mae_consensus` key (ownership is not in points units)
    - A test asserts the both-absent entry's key set equals today's key set exactly
    - Both new blocks use `scipy.stats.spearmanr(...).statistic`; no new metrics helper function is defined in `predict/scoreboard.py`
    - The fplreview join resolves identity through `data.id_crosswalk`, never a display-name match
    - `tests/test_product.py` still passes (product surface untouched)
  </acceptance_criteria>
  <done>Both Tier-3 diagnostics score alongside `ep_next` in the post-gameweek scoreboard, each independently optional, with today's output reproduced exactly when neither source is present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FPL API (`leagues-classic`, `entry/.../picks`) -> `data/raw/standings/` | untrusted upstream JSON, including other managers' team names, crosses here |
| Human-pasted CSV -> `data/external/fplreview/` -> `predict/scoreboard.py` | untrusted, hand-edited file content crosses into a published trust artifact (`web/data/scoreboard.json`) |
| `predict/scoreboard.py` -> `web/data/scoreboard.json` | this file is served to the public site; anything written here is published |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-02-01 | Information Disclosure | top-100 managers' `player_name`/`entry_name` from the standings payload | high | mitigate | `consensus_ownership` aggregates to `player_id` counts only; no manager identifier, team name or entry id is written to any parquet or to `web/data/scoreboard.json`. Cached raw pages stay under gitignored `data/raw/`. This follows Phase 4's own recorded scrub-names decision for third-party manager identity |
| T-10-02-02 | Tampering | a mis-pasted fplreview CSV silently entering the published scoreboard | high | mitigate | `data/fplreview.py::validate` raises on missing columns and on an all-non-numeric projection column; duplicate `player_code` within a gameweek raises rather than multiplying join rows |
| T-10-02-03 | Legal/compliance | fplreview ToS forbids ingestion and redistribution | high | mitigate | zero `requests` references in `data/fplreview.py` (verify-enforced), no `config.EXPERIMENTS` flag and no `*_COLS` constant references the directory, and the README carries an explicit ToS-posture section |
| T-10-02-04 | Denial of Service | scoreboard cron blocked by a slow or failing standings fetch | medium | mitigate | `fetch_entry_picks` returns `[]` on `requests.RequestException`; `load_consensus`/`load_gw` return `None` when absent, so both blocks skip cleanly and `predict.scoreboard` keeps completing inside `scripts/daily.sh`'s `run_step` |
| T-10-02-05 | Spoofing | wrong league id yields a "top 100" that is an arbitrary league | medium | mitigate | `OVERALL_LEAGUE_ID` is a single named constant, the first real run prints the top-3 entries for a human eyeball, and the SUMMARY records them (10-RESEARCH.md Assumptions Log A3) |
</threat_model>

<verification>
- `python -m pytest` green repo-wide, `tests/test_scoreboard.py` present and passing
- `ruff check .` clean
- `grep -c "requests" data/fplreview.py` returns `0`
- A real `python -m data.fpl_standings --gw 1` run completed and its top-3 entries are recorded in the SUMMARY
- `web/data/scoreboard.json` regenerates unchanged when neither benchmark source is present
</verification>

<success_criteria>
- The deadline-gated fplreview capture slot exists, is documented, and validates a fresh paste immediately
- Top-100 consensus ownership is fetchable and aggregated without persisting any manager identity
- Both diagnostics score in the existing scoreboard entry, each independently optional
- Nothing in `predict/live.py` or `predict/export.py` changed; `tests/test_product.py` still passes
</success_criteria>

<output>
Create `.planning/phases/10-xp-experiment-follow-ups/10-02-SUMMARY.md` when done. It MUST
record: the verbatim top-3 entry names/points from the real standings run (the A3 spot-check),
the real per-GW standings + picks request count and wall clock, and the exact required-column
list a human must produce in each weekly fplreview capture.
</output>
