---
phase: 10-xp-experiment-follow-ups
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/availability.py
  - config.py
  - features/engineer.py
  - data/build_table.py
  - backtest/walk_forward.py
  - data/snapshot.py
  - tests/test_availability.py
  - tests/test_leakage.py
  - tests/test_experiments.py
  - IMPROVEMENTS.md
autonomous: true
requirements:
  - TODO-AVAIL-FLAGS
  - PHASE10-CRITERIA
user_setup: []

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A single point-in-time availability column travels capture -> provider -> as-of-deadline join -> features.parquet -> the walk-forward harness, and the harness completes with the flag both off and on."
    - "The availability join is leakage-safe by construction: every source row feeding gameweek g is timestamped strictly before g's deadline AND before every kickoff in g, proven on real data."
    - "A gameweek with no qualifying prior snapshot yields NaN availability features and the pipeline still completes -- it never raises and never falls forward to a later snapshot (D-10)."
    - "config.EXPERIMENTS['availability_flags'] is registered and defaults False; an unflagged walk_forward run reproduces the pre-phase-10 baseline (D-08 Phase 9 discipline)."
    - "data/snapshot.py captures news, news_added and chance_of_playing_this_round from today onward, so those columns start accruing before any later plan needs them."
    - "Every Phase 10 adoption criterion -- including D-02's exact Spearman trigger number -- is written to IMPROVEMENTS.md BEFORE any adoption-deciding run in this phase."
  artifacts:
    - data/availability.py
    - tests/test_availability.py
    - "IMPROVEMENTS.md — ### Phase G declared criteria section"
  key_links:
    - "data/availability.py::attach -> data/build_table.py's optional-enrichment block (columns reach player_gw.parquet)"
    - "config.AVAILABILITY_COLS -> features/engineer.py::CONTEXT_COLS (columns reach features.parquet unrolled, as pre-match context)"
    - "config.EXPERIMENTS['availability_flags'] -> backtest/walk_forward.py::apply_experiment_feature_gating (the single feature-selection seam)"
---

<objective>
Prove the Phase 10 availability spine end-to-end on the thinnest possible slice: ONE
derived availability column, ONE season, ONE replica, three real gameweeks of source data
-- wired through every layer this phase later expands (daily capture, a point-in-time
snapshot-source provider, the new as-of-deadline join family, the `config.EXPERIMENTS`
registry, the single `walk_forward` gating seam, the harness). Then lock every Phase 10
adoption criterion in writing, before any adoption-deciding run exists.

Purpose: the genuinely new architecture here is a THIRD leakage-safe feature family
(point-in-time snapshot, as-of-deadline) with no analog in this codebase -- neither
`ROLL_STATS`'s shift-then-roll nor `CONTEXT_COLS`'s static-per-fixture join
(10-RESEARCH.md Pattern 4). Proving it on one column first means an architectural
dead-end costs one commit, not ten. Task 3 exists because Phase 9's own discipline is
pre-declared criteria: the number that decides D-02 must be on paper before anything
measures against it.

Output: `data/availability.py` (provider registry + as-of-deadline resolver + attach),
one registered `av_chance_pct` column, one `availability_flags` gating branch, the
extended daily capture, new leakage/fallback/registry tests, and the `### Phase G`
declared-criteria block in `IMPROVEMENTS.md`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-xp-experiment-follow-ups/10-CONTEXT.md
@.planning/phases/10-xp-experiment-follow-ups/10-RESEARCH.md
@.planning/phases/10-xp-experiment-follow-ups/10-PATTERNS.md
@.planning/todos/pending/2026-09-10-availability-flags-pplay.md
</context>

## Locked planner decisions (CONTEXT.md "Claude's Discretion" items, decided here)

Decided once, here; every later Phase 10 plan inherits them verbatim.

| Item | Locked value | Rationale |
|---|---|---|
| D-02 Spearman trigger | pooled played-only `spearman_xp_med` **< 0.500** on `backtest.benchmark_external`'s EXP-1 basis (2021-22 + 2022-23 pooled, played-only, GW level) after BOTH Tier-1 experiments -> news sentiment gets built; **>= 0.500** -> recorded not-triggered | D-02's own stated figure ("~0.50") made exact. The arithmetic midpoint of the 0.383->0.579 gap is 0.481; the user's stated 0.50 is the binding number. |
| Flag names | `availability_flags`, `transfermarkt_injury`, `news_sentiment`, `bracket_ridge`, `bracket_xgb`, `bracket_catboost`, `bracket_mlp`, `bracket_rnn`, `bracket_transformer` | 10-RESEARCH.md Pattern 2's suggested keys |
| Column prefixes | `av_` availability, `tm_` injury, `nw_` sentiment | one grep-able prefix per family, mirroring `us_`/`fm_`/`ts_` |
| Deadline definition | `gw_deadline(season, gw) = min(kickoff_time over that (season, gw)) - 90 minutes`, derived offline from `data/processed/player_gw.parquet` | FPL deadlines are exactly 90 min before a gameweek's first fixture; deriving it from data already held keeps the availability build network-free (COVERAGE.md opts out of `events[].deadline_time` for this reason) |
| Sequence window (D-20) | 10 gameweeks | top of D-20's stated ~8-10 band |
| Transformer budget | `d_model=64`, `nhead=4`, 2 encoder layers, `dim_feedforward=128`, `dropout=0.1` | "small transformer encoder" per D-12, sized to fit D-13's compute box |
| Deep search budget (D-19) | 12 configs per deep candidate per granularity variant, hard stop, logged like `models/tune.py` | asymmetric per D-19; GBDT candidates get defaults only |
| Colab/local split (D-13) | recurrent + transformer -> Colab; Ridge/XGBoost/CatBoost/MLP -> local WSL | the only two candidates whose wall clock is genuinely GPU-bound |
| Fetcher rate limits | Transfermarkt `_MIN_INTERVAL_S = 3.0`; GDELT `5.0`; Guardian `1.0` | more conservative than `data/fotmob.py`'s 1.5 for the two unofficial/scraped targets |
| GDELT/Guardian client | plain `requests`, no `gdeltdoc` package | matches the project's no-wrapper fetcher convention; avoids a new package gate |
| Module layout | `data/availability.py`, `data/fpl_core_insights.py`, `data/transfermarkt.py`, `data/news.py`, `data/fpl_standings.py`, `data/fplreview.py`, `models/bracket/`, `colab/` | 10-RESEARCH.md's Recommended Project Structure |

## Multi-source coverage audit (whole Phase 10 plan set)

```
SOURCE   | ID                | Item                                                            | Plan(s)              | Status
-------- | ----------------- | --------------------------------------------------------------- | -------------------- | -------
GOAL     | -                 | Pursue Phase 9's leads under honest-harness discipline          | 10-01..10-16         | COVERED
GOAL     | -                 | Adoption bar unchanged: >=2,280 model+chips vs 2,262 baseline    | 10-01, 10-16         | COVERED
REQ      | -                 | ROADMAP says `Requirements: TBD` -- no mapped REQ-IDs           | -                    | n/a
CONTEXT  | D-01              | Tier 3 first, then Tier 1 -> Tier 2                             | 10-02..10-14         | COVERED
CONTEXT  | D-02              | Pre-declared Spearman trigger for news sentiment                | 10-01, 10-08, 10-12  | COVERED
CONTEXT  | D-03              | Model-class bracket runs unconditionally                        | 10-10, 10-11, 10-13  | COVERED
CONTEXT  | D-04              | FBref + RL notes in scope, dead last                            | 10-15                | COVERED
CONTEXT  | D-05              | FPL-Core-Insights vendor + verify + commit, GW N-1 rule          | 10-01, 10-06         | COVERED
CONTEXT  | D-06              | Transfermarkt backfill ALL seasons 2016-17+, killable job        | 10-07                | COVERED
CONTEXT  | D-07              | Committed normalized injury-spell table + README                | 10-07                | COVERED
CONTEXT  | D-08              | Anacron-style snapshot catch-up                                 | 10-03                | COVERED
CONTEXT  | D-09              | Availability dual criterion (+25 pts 2025-26 AND +0.03 Spearman)| 10-01, 10-08         | COVERED
CONTEXT  | D-10              | Safe fallback: no fresh snapshot -> NaN, test-asserted           | 10-01, 10-04         | COVERED
CONTEXT  | D-11              | Split verdicts in the final combined run                        | 10-16                | COVERED
CONTEXT  | D-12              | Six-family roster, stage-2 regressor only                       | 10-10, 10-11, 10-13  | COVERED
CONTEXT  | D-13              | Hybrid Colab/local compute, 200 units, hard stop                | 10-01, 10-13         | COVERED
CONTEXT  | D-14              | Colab produces frozen preds; local harness is sole judge        | 10-09, 10-13         | COVERED
CONTEXT  | D-15              | Two-stage gate, val played-only Spearman >= LightGBM + 0.01     | 10-10                | COVERED
CONTEXT  | D-16              | New packages -> dev-only hash-locked lockfile + human gate       | 10-10, 10-14         | COVERED
CONTEXT  | D-17              | torch winner adopts via ONNX/numpy export; never in production  | 10-14                | COVERED
CONTEXT  | D-18              | Deep candidates pooled AND per-position; GBDT per-position      | 10-11, 10-13         | COVERED
CONTEXT  | D-19              | Asymmetric tuning budget, searches logged                       | 10-01, 10-11, 10-13  | COVERED
CONTEXT  | D-20              | Sequence inputs = raw per-GW stats + static side-vector         | 10-11                | COVERED
CONTEXT  | D-21              | Sequence builder leakage assertions on real data                | 10-11                | COVERED
RESEARCH | -                 | `enrichment_preds_*` does NOT exist -- build the ingestion seam  | 10-09                | COVERED
RESEARCH | -                 | Spike Transfermarkt access BEFORE building the fetcher          | 10-05                | COVERED
RESEARCH | -                 | Check the figshare pre-scraped dataset first (10 min)           | 10-05                | COVERED
RESEARCH | -                 | FPL-Core-Insights license + real CSV schema unconfirmed         | 10-01, 10-06         | COVERED
RESEARCH | -                 | mlpremier is a NEGATIVE prior for D-02's own feature            | 10-12                | COVERED
RESEARCH | -                 | fplreview 403s automated access -- manual capture only           | 10-02                | COVERED
RESEARCH | -                 | League id 314 unverified (A3) -- spot-check on first real run    | 10-02                | COVERED
RESEARCH | -                 | IJCSS 2025 paper unverifiable (A1) -- claims stay [ASSUMED]      | 10-07, 10-16         | COVERED
RESEARCH | -                 | Every new fetcher needs `_require()` shape validation (V5)      | 10-02, 10-05, 10-07, 10-12 | COVERED
RESEARCH | -                 | Availability/injury must NOT go through ROLL_STATS              | 10-01, 10-04, 10-07  | COVERED
RESEARCH | -                 | Colab artifacts carry predictions only; truth re-attached (OQ4) | 10-09                | COVERED
TODO     | TODO-AVAIL-FLAGS  | Availability flags into P(play)                                 | 10-01, 10-04, 10-06, 10-08 | COVERED
TODO     | TODO-TM-INJURY    | Transfermarkt injury history                                    | 10-05, 10-07, 10-08  | COVERED
TODO     | TODO-BRACKET      | Model-class bracket                                             | 10-09..10-14         | COVERED
TODO     | TODO-NEWS         | Guardian/GDELT news sentiment (conditional)                     | 10-12                | COVERED
TODO     | TODO-TOP100       | Top-100 consensus benchmark                                     | 10-02                | COVERED
TODO     | TODO-FPLREVIEW    | fplreview scoreboard benchmark                                  | 10-02                | COVERED
TODO     | TODO-FBREF-MANUAL | Manual FBref CSV snapshot                                       | 10-15                | COVERED
TODO     | TODO-RL-SHAPING   | RL reward-shaping notes                                         | 10-15                | COVERED
```

Exclusions (not gaps): CONTEXT.md `## Deferred Ideas` is empty. `onnxruntime` is
deferred-until-triggered by D-17 and covered conditionally in 10-14. `skorch` is excluded
outright by 10-RESEARCH.md's own recommendation.

**Tier order vs tracer-first.** D-01 puts Tier 3 first because the fplreview capture is
deadline-gated. TRACER_MODE requires the phase to lead with one end-to-end slice. Both hold:
Tier 3 (10-02) is in **wave 1**, so nothing is sequenced ahead of it and no gameweek of
fplreview data is lost; the tracer runs in the same wave on a disjoint file set. Tier 1's own
expansion work starts in wave 2, strictly after Tier 3 has landed.

**Correction to D-05's "optional".** `backtest/walk_forward.py::DATA_SEASONS` drops 2026-27,
so our own daily snapshots (first capture 2026-08-31, season 2026-27) have **zero**
walk-forward coverage. D-09's covered-season criterion is stated on 2025-26, which only the
FPL-Core-Insights vendored backfill reaches. The vendoring is therefore **not optional** if
D-09 is to be measurable at all: plan 10-06 is a hard prerequisite of 10-08.

<assumption_delta_decision>
**Noun now primary:** availability *snapshot source* (was: our own `data/snapshots/*.parquet`).
**Decision:** `promote`.
**Rationale:** the detector fired on "optional one-time vendored 2025-26 backfill" -- a second
provider of a signal that had exactly one source. Rather than special-casing the vendored CSVs
alongside our own parquets, `data/availability.py` makes *snapshot source* primary: every
provider yields the same `(player_code, snapshot_ts, <availability fields>, source)` long
frame, and the leakage rule (`snapshot_ts < gw_deadline`) is applied once, uniformly,
downstream of all of them. Our daily capture becomes one provider among several. Not accepted debt.
**Invariant test:** `tests/test_availability.py::test_every_source_row_predates_its_gw_deadline`
asserts the rule for **every** `source` value present, so a newly registered provider cannot
bypass it.
</assumption_delta_decision>

## Artifacts this phase produces

Symbols created by Phase 10 (new -- exclude from drift verification):

- Modules: `data/availability.py`, `data/fpl_core_insights.py`, `data/transfermarkt.py`,
  `data/news.py`, `data/fpl_standings.py`, `data/fplreview.py`, `models/bracket/__init__.py`,
  `models/bracket/registry.py`, `models/bracket/classical.py`, `models/bracket/gbdt.py`,
  `models/bracket/gate.py`, `models/bracket/sequence.py`, `models/bracket/deep.py`,
  `models/bracket/recurrent.py`, `models/bracket/transformer.py`, `models/bracket/export.py`
- Tests: `tests/test_availability.py`, `tests/test_scoreboard.py`,
  `tests/test_transfermarkt.py`, `tests/test_bracket.py`, `tests/test_news.py`
- Scripts / data dirs / notebooks: `scripts/snapshot_catchup.sh`,
  `data/external/fpl_core_insights/`, `data/external/transfermarkt/`,
  `data/external/fplreview/`, `data/external/fbref/`, `colab/`,
  `requirements-experiments.in`, `requirements-experiments.txt`
- `config.py`: `AVAILABILITY_COLS`, `INJURY_COLS`, `NEWS_COLS`, `BRACKET_CANDIDATES`,
  `SEQUENCE_WINDOW`, and `EXPERIMENTS` keys `availability_flags`, `transfermarkt_injury`,
  `news_sentiment`, `bracket_ridge`, `bracket_xgb`, `bracket_catboost`, `bracket_mlp`,
  `bracket_rnn`, `bracket_transformer`
- `data/availability.py`: `AVAILABILITY_ENABLED`, `SOURCES`, `gw_deadlines`, `load_sources`,
  `resolve_as_of`, `build`, `load_availability`, `attach`, `main`, `_require_columns`,
  `_DEADLINE_LEAD`
- `backtest/walk_forward.py`: `load_external_predictions`
- `models/train.py`: `train_position(..., stage2=...)` parameter
- `predict/scoreboard.py`: `consensus_entry`, `fplreview_entry` scoreboard keys
- Feature columns: `av_chance_pct`, `av_status_*`, `av_days_since_news`,
  `av_snapshot_age_days`, `tm_injured`, `tm_days_out_so_far`, `tm_spells_prior_365d`,
  `tm_days_out_prior_365d`, `nw_doc_count_7d`, `nw_mean_tone_7d`
- Threat IDs: `T-10-01-01` .. `T-10-16-xx`

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "one availability column reaches the harness" — one column, one season, three gameweeks</name>
  <files>data/availability.py, config.py, features/engineer.py, data/build_table.py, backtest/walk_forward.py</files>
  <read_first>
    - data/fotmob.py — the five-function optional-enrichment shape (`build(*, force=False)`, `load_*()` returning None when absent, `attach(full)` with a row-count assert plus coverage print, `main()`) and its `_require()` explicit-shape validator
    - data/team_strength.py — the closest existing "join a context table as-of a decision point" attach
    - data/snapshot.py — `SNAP_DIR`, `_ELEMENT_COLS`, `load_snapshots()`, and `snapshot_frame`'s output columns (`player_code`, `ts_utc`, `date`, `status`, `chance_of_playing_next_round`, `next_gw`)
    - config.py lines 118-200 — `TEAM_STRENGTH_COLS`'s comment shape (a pre-match CONTEXT family computed unconditionally with the gate elsewhere), `UNDERSTAT_COLS`/`FOTMOB_COLS`, and the `EXPERIMENTS` dict
    - features/engineer.py lines 28-60 — why `UNDERSTAT_COLS`/`FOTMOB_COLS` are in `ROLL_STATS` and why this family must not be
    - data/build_table.py lines 140-185 — the three existing optional-enrichment try/except attach blocks
    - backtest/walk_forward.py lines 158-215 — `apply_experiment_feature_gating`, the ONE feature-selection seam
    - .planning/phases/10-xp-experiment-follow-ups/10-RESEARCH.md — Pattern 4 (three feature families), Pitfall 2 (unconfirmed CSV schema), Anti-Patterns
  </read_first>
  <precondition>`data/processed/player_gw.parquet` and `data/processed/features.parquet` both exist (this task reads real kickoff times and runs the real harness). If either is missing, run `python -m data.build_table` then `python -m features.engineer` before starting.</precondition>
  <action>
Fetch a real source sample FIRST, then design against it — 10-RESEARCH.md Pitfall 2 records
that FPL-Core-Insights' per-GW CSV schema is unconfirmed. List the season directory via
`https://api.github.com/repos/olbauday/FPL-Core-Insights/contents/data/2025-2026` to learn the
real folder and file naming (do not guess it), then retrieve the GW1, GW2 and GW3 playerstats
CSVs from `https://raw.githubusercontent.com/olbauday/FPL-Core-Insights/main/...` and write
them unmodified under `data/raw/fpl_core_insights/2025-2026/`. `data/raw/` is gitignored, so
this probe commits nothing and needs no license decision — plan 10-06 does the verified,
license-gated, committed vendoring of all 38 gameweeks. Print one file's exact column list and
record it verbatim in the SUMMARY. If no column maps onto `chance_of_playing_next_round`, stop
and report rather than inventing a mapping.

Create `data/availability.py` on `data/fotmob.py`'s five-function skeleton, with
`AVAILABILITY_ENABLED = True` as the module-level kill switch and
`_OUT = config.PROCESSED_DIR / "availability.parquet"`. Four responsibilities:

1. `gw_deadlines() -> pd.DataFrame` — one row per `(season, gw)` with
   `deadline_ts = min(kickoff_time) - _DEADLINE_LEAD`, `_DEADLINE_LEAD = pd.Timedelta(minutes=90)`,
   read from `data/processed/player_gw.parquet`. No network. The docstring states the
   postponement caveat: if a gameweek's first fixture is postponed, `min(kickoff_time)` moves
   later and the derived deadline becomes permissive, which is why `resolve_as_of` ALSO
   requires the source row to predate every kickoff in that gameweek.
2. A provider registry. `SOURCES` maps a source name to a zero-argument callable returning a
   long frame carrying at least `player_code`, `snapshot_ts`, `status`,
   `chance_of_playing_next_round`, `source`, validated by a
   `_require_columns(df, cols, what)` helper that raises `ValueError` naming the missing
   column — never silently NaN (V5 / T-10-01-01). Register two providers:
   - `"daily_snapshot"` — `data.snapshot.load_snapshots()`, with
     `snapshot_ts = pd.to_datetime(ts_utc, utc=True)`.
   - `"fpl_core_insights"` — every per-GW CSV under
     `config.DATA_DIR / "external" / "fpl_core_insights"` (the committed home 10-06 fills),
     falling back to `config.RAW_DIR / "fpl_core_insights"` (this task's uncommitted probe)
     when the committed directory is absent. Per D-05 these folders freeze at gameweek END, so
     a folder labelled gameweek N gets `snapshot_ts = max(kickoff_time over (season, N))` from
     `player_gw.parquet` — the moment it froze. `resolve_as_of` then performs the N-1 offset
     arithmetic for free, with no special case anywhere.
   A provider whose directory or files are absent returns an empty frame, never raises.
3. `resolve_as_of(snaps, deadlines) -> pd.DataFrame` — for each `(season, gw, player_code)`,
   select the source row with the maximum `snapshot_ts` satisfying BOTH
   `snapshot_ts < deadline_ts` and `snapshot_ts < min(kickoff_time)` for that gameweek. No
   qualifying row means that player-gameweek is absent from the output; `attach`'s left merge
   turns absence into NaN. Never fall forward to a later snapshot.
4. `build(*, force=False)`, `load_availability() -> pd.DataFrame | None` (None when `_OUT` is
   absent, never raises), `attach(full)` (left merge on `["season", "gw", "player_code"]`,
   `raise AssertionError` if the row count changes, then a
   `[availability] joined; coverage {pct}` print), and `main()` with `--force`. The one derived
   feature column this task ships is
   `av_chance_pct = pd.to_numeric(chance_of_playing_next_round, errors="coerce") / 100.0`.

In `config.py`, add `AVAILABILITY_COLS = ["av_chance_pct"]` immediately after `FOTMOB_COLS`,
with a comment modelled on `TEAM_STRENGTH_COLS`'s (lines 126-141): a pre-match CONTEXT family,
computed UNCONDITIONALLY so a flag flip is never a pipeline rebuild, with the feature-selection
gate in `backtest/walk_forward.py`. The comment states explicitly that this is a THIRD family,
distinct from `ROLL_STATS` (match outcomes) and from `CONTEXT_COLS`'s static-per-fixture
members, because its join key is a point-in-time snapshot resolved against the gameweek
deadline. Add `"availability_flags": False` to `EXPERIMENTS` under a `# --- Phase 10 ---`
comment.

In `features/engineer.py`, append `config.AVAILABILITY_COLS` to the `CONTEXT_COLS` expression
(never to `ROLL_STATS`) and extend that block's comment with the reason: an availability figure
resolved as of the deadline is already leakage-safe, and `_roll`'s shift-then-roll would apply
the wrong lookback horizon across double gameweeks and postponements.

In `data/build_table.py`, add a fourth optional-enrichment block mirroring the
`team_strength`/`understat`/`fotmob` blocks exactly (import `availability as avail_mod`, a
`try` calling `avail_mod.attach(full)`, an `except Exception as exc` printing
`  [availability] skipped ({exc})`), placed after the fotmob block.

In `backtest/walk_forward.py::apply_experiment_feature_gating`, add one drop branch keyed on
`exp.get("availability_flags", False)` that drops every column whose name starts with `av_`,
and extend the docstring's list of gated families with this one. Do not add a second gate
anywhere.

Then rebuild the pipeline and run both harness arms with the tags in `<verify>`.
  </action>
  <verify>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m data.availability --force</automated>
    <fails_when>non-zero exit, or the printed `[availability] joined; coverage` line reports 0.0% (the probe CSVs never reached the join)</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m data.build_table</automated>
    <fails_when>non-zero exit, or `[availability] skipped` appears in the output</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m features.engineer</automated>
    <fails_when>non-zero exit, or the printed features summary omits `av_chance_pct` from its column count</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import config; from models.train import load_features; from backtest.walk_forward import apply_experiment_feature_gating as g; d=load_features(); off=g(d, config.resolve_experiments('none')); on=g(d, config.resolve_experiments('availability_flags')); assert 'av_chance_pct' in on.columns, 'flag-on frame lost the column'; assert 'av_chance_pct' not in off.columns, 'flag-off frame kept the column'; n=int(on.av_chance_pct.notna().sum()); assert n > 0, 'no real availability rows joined'; print('gate ok; non-null av_chance_pct rows', n)"</automated>
    <fails_when>non-zero exit, or an AssertionError naming a lost column, a kept column, or zero joined rows</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m backtest.walk_forward --seasons 2025-26 --replicas 1 --tag t1_av_off</automated>
    <fails_when>non-zero exit, or no `[2025-26] model=` line in the output</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m backtest.walk_forward --seasons 2025-26 --replicas 1 --experiments availability_flags --tag t1_av_on</automated>
    <fails_when>non-zero exit, or no `[2025-26] model=` line in the output</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import json,pathlib; b=json.loads(pathlib.Path('data/processed/experiments/wf_t1_av_off.json').read_text()); e=json.loads(pathlib.Path('data/processed/experiments/wf_t1_av_on.json').read_text()); assert b['experiments']['availability_flags'] is False; assert e['experiments']['availability_flags'] is True; assert isinstance(b['model+chips'], int) and isinstance(e['model+chips'], int); print('off', b['model+chips'], 'on', e['model+chips'])"</automated>
    <fails_when>non-zero exit, a KeyError on `availability_flags` (the flag never reached the run record), or an AssertionError on either flag state</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/ruff check .</automated>
    <fails_when>non-zero exit, or any diagnostic line naming data/availability.py</fails_when>
  </verify>
  <acceptance_criteria>
    - `data/availability.py` defines all of `AVAILABILITY_ENABLED`, `SOURCES`, `_DEADLINE_LEAD`, `_require_columns`, `gw_deadlines`, `load_sources`, `resolve_as_of`, `build`, `load_availability`, `attach`, `main`
    - `SOURCES` has exactly two registered keys, `"daily_snapshot"` and `"fpl_core_insights"`, each returning the five required columns
    - `_require_columns` raises `ValueError` naming the absent column: `python -c "import pandas as pd; from data import availability as a; a._require_columns(pd.DataFrame({'player_code':[1]}), ['player_code','snapshot_ts'], 'probe')"` exits non-zero with `snapshot_ts` in the message
    - `resolve_as_of`'s selection uses BOTH the deadline and the min-kickoff bound; `grep -c "min" data/availability.py` is non-zero and the two-condition filter is present in the function body
    - `config.AVAILABILITY_COLS == ["av_chance_pct"]` and `config.EXPERIMENTS["availability_flags"] is False`
    - `config.AVAILABILITY_COLS` appears in `features/engineer.py`'s `CONTEXT_COLS` expression and appears nowhere in its `ROLL_STATS` expression
    - `data/processed/features.parquet` carries `av_chance_pct` and carries no `av_chance_pct_r3`/`_r5`/`_r10`/`_rall` column (proof it never entered the rolling path)
    - Both tagged walk-forward JSONs exist under `data/processed/experiments/` and record the opposite `availability_flags` state
    - The SUMMARY records the probe CSV's verbatim column list and the measured `av_chance_pct` join coverage
  </acceptance_criteria>
  <done>One availability column travels capture -> provider -> as-of-deadline resolution -> player_gw -> features -> the gate -> a completed walk-forward run, with the flag genuinely controlling the column's presence and both arms producing a `model+chips` integer.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend the daily capture and lock the leakage, safe-fallback and flag-registry contracts</name>
  <files>data/snapshot.py, tests/test_availability.py, tests/test_leakage.py, tests/test_experiments.py</files>
  <read_first>
    - data/snapshot.py — `_ELEMENT_COLS` (lines 41-51), `snapshot_frame`'s numeric-coercion loop (lines 71-73), `take_snapshot`'s per-UTC-day idempotency (lines 127-146)
    - tests/test_cron.py lines 1-60 — the `responses`-based bootstrap stub, the `SNAP_DIR` monkeypatch fixture that keeps tests off the irreplaceable real archive, and the `fake_boot` import from `test_api`
    - tests/test_api.py — `fake_boot()`'s element shape, so the new columns can be exercised without touching the network
    - tests/test_leakage.py — the `@needs_data` skip guard, the module-scoped `feat` fixture, and `test_understat_features_are_rolled_not_raw` as the family-classification precedent
    - tests/test_experiments.py lines 20-32 — `_EXPERIMENT_KEYS` and `test_experiments_registry_default_off`
    - data/availability.py — as written by Task 1
  </read_first>
  <behavior>
    - `snapshot_frame` output carries `chance_of_playing_this_round`, `news` and `news_added` for every element, with `chance_of_playing_this_round` numerically coerced like its `next_round` sibling
    - Every row `data/availability.py` resolves has `snapshot_ts` strictly less than its gameweek's `deadline_ts` AND strictly less than every kickoff in that gameweek — asserted for every `source` value present, so a future provider cannot bypass the rule
    - A `(season, gw, player_code)` with no qualifying prior source row produces NaN availability columns and `attach` still returns the same row count — no raise, no forward fill
    - `resolve_as_of` given only a source row dated AFTER the deadline returns no row for that gameweek (the direct anti-leakage case)
    - `features.parquet` carries `av_chance_pct` raw and carries no rolled `av_*` variant
    - `config.EXPERIMENTS` contains exactly the ten Phase 9 keys plus `availability_flags`, all False
  </behavior>
  <action>
Add `"chance_of_playing_this_round"`, `"news"` and `"news_added"` to `data/snapshot.py`'s
`_ELEMENT_COLS`, and add `"chance_of_playing_this_round"` to `snapshot_frame`'s
`pd.to_numeric(..., errors="coerce")` loop alongside the existing entries (`news` and
`news_added` stay as captured strings — `news_added` is an ISO timestamp string, parsed
downstream, not at capture). Extend `_ELEMENT_COLS`'s existing comment block to record why:
these three are the availability signal the P(play) stage has never seen, and capture starts
accruing them from today — the FPL API exposes only current state, so a day not captured is a
day permanently lost (the same argument the price snapshot already rests on).

Create `tests/test_availability.py` following `tests/test_cron.py`'s isolation discipline: an
autouse fixture that monkeypatches `data.snapshot.SNAP_DIR` to `tmp_path` so no test can write
into or read from the real `data/snapshots/` archive, plus `@needs_data` guards (mirroring
`tests/test_leakage.py`'s) on the tests that read `player_gw.parquet`/`availability.parquet`.
Write the six behaviors above as tests, with these exact names for the three that later plans
and gates reference:
`test_snapshot_frame_carries_new_availability_columns` (uses `responses` + `fake_boot`, no
network), `test_every_source_row_predates_its_gw_deadline` (the invariant test named in this
plan's assumption-delta block — it must iterate the distinct `source` values and assert the
rule for each, so registering a provider without the rule fails here), and
`test_missing_snapshot_degrades_to_nan_not_raise` (D-10's mandatory safe-fallback assertion:
build a two-row synthetic source frame whose only row postdates the deadline, call
`resolve_as_of`, assert the gameweek is absent from the result, then call `attach` against a
one-row `full` frame and assert the row count is unchanged and `av_chance_pct` is NaN).

Add to `tests/test_leakage.py` a `test_availability_features_are_raw_context_not_rolled(feat)`
guarded by `@needs_data`, mirroring `test_understat_features_are_rolled_not_raw`'s shape but
asserting the opposite classification: `av_chance_pct` is present, and no column matching
`av_*_r3`/`_r5`/`_r10`/`_rall` exists. This is the regression gate against a future edit
"helpfully" moving the family into `ROLL_STATS`.

In `tests/test_experiments.py`, add `"availability_flags"` to `_EXPERIMENT_KEYS`. Do not
weaken `test_experiments_registry_default_off` — its whole value is that a default flip must be
a deliberate edit.
  </action>
  <verify>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m pytest tests/test_availability.py tests/test_leakage.py tests/test_experiments.py -q</automated>
    <fails_when>non-zero exit, or the summary line reports any failed or errored test</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m pytest tests/test_availability.py::test_missing_snapshot_degrades_to_nan_not_raise tests/test_availability.py::test_every_source_row_predates_its_gw_deadline -q</automated>
    <fails_when>non-zero exit, or `no tests ran` in the output (a renamed test silently disables the D-10 and provider-invariant gates)</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import data.snapshot as s; need={'chance_of_playing_this_round','news','news_added'}; missing=need - set(s._ELEMENT_COLS); assert not missing, missing; print('captured', sorted(need))"</automated>
    <fails_when>non-zero exit, or an AssertionError printing a set of still-missing column names</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m pytest -q</automated>
    <fails_when>non-zero exit, or the summary line reports any failed or errored test</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/ruff check .</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `data.snapshot._ELEMENT_COLS` contains `chance_of_playing_this_round`, `news`, `news_added`
    - `chance_of_playing_this_round` appears inside `snapshot_frame`'s `pd.to_numeric` coercion tuple; `news` and `news_added` do not
    - `tests/test_availability.py` defines an autouse fixture that monkeypatches `data.snapshot.SNAP_DIR`; `grep -c "SNAP_DIR" tests/test_availability.py` is non-zero
    - The three named tests exist and pass by those exact names
    - `tests/test_leakage.py::test_availability_features_are_raw_context_not_rolled` exists and passes
    - `tests/test_experiments.py::_EXPERIMENT_KEYS` has exactly 11 entries and `test_experiments_registry_default_off` passes unmodified in body
    - `python -m pytest` is green with zero failures repo-wide
  </acceptance_criteria>
  <done>The three new availability fields are captured from today onward; the as-of-deadline rule, the D-10 NaN fallback, the raw-not-rolled family classification, and the default-off flag registry are each locked by a named passing test.</done>
</task>

<task type="auto">
  <name>Task 3: Write the Phase G declared criteria to IMPROVEMENTS.md before any adoption run</name>
  <files>IMPROVEMENTS.md</files>
  <read_first>
    - IMPROVEMENTS.md lines 120-230 — `## Phase F`'s own shape: the `### Baseline (measured …, all experiment flags off)` block, `### Adoption criteria (D-05/D-06, pre-declared)`, the `### Benchmark: our xP vs theFPLkiwi` table with the 0.383 / 0.579 pooled Spearman figures, and the `### Experiment results` table whose every row starts `pending` and is filled by the plan that measures it
    - IMPROVEMENTS.md lines 1400-1412 — `### Declared time-box (2026-09-09, written before training)`, the precedent for a criteria block written ahead of the run it governs
    - .planning/phases/10-xp-experiment-follow-ups/10-CONTEXT.md — D-02, D-09, D-11, D-13, D-15, D-16, D-17
  </read_first>
  <action>
Append a `## Phase G — xP experiment follow-ups (Phase 10)` section to `IMPROVEMENTS.md`,
after the existing `### Addendum (2026-09-10): rl_strategy v3 …` block and before
`## Reference findings`. Model its shape on `## Phase F`'s opening. It contains four
subsections:

`### Declared criteria (2026-09-10, written before any Phase 10 adoption run)` — a bullet
list stating, in numbers:
- Primary bar (unchanged): 6-season mean `model+chips` >= **2,280** against the measured
  **2,262** Phase F baseline.
- D-09 availability dual criterion, BOTH required: (a) 2025-26 walk-forward `model+chips`
  improves by >= **+25** points; (b) pooled played-only `spearman_xp_med` on
  `backtest.benchmark_external`'s EXP-1 basis improves by >= **+0.03** over the **0.383**
  baseline. Note explicitly that the 2025-26 coverage comes from the D-05 vendored backfill,
  not from our own daily snapshots (which start in season 2026-27 and are outside
  `backtest/walk_forward.py::DATA_SEASONS`).
- D-02 news-sentiment trigger: after BOTH Tier-1 experiments, if pooled played-only
  `spearman_xp_med` is **< 0.500** the sentiment experiment gets built; if **>= 0.500** it is
  recorded not-triggered (the `capt_mc` precedent). Record that the 0.383 -> 0.579 arithmetic
  midpoint is 0.481 and that 0.500 — D-02's own stated figure — is the binding number.
- D-15 bracket cheap gate: a candidate advances only if val-split played-only Spearman
  >= LightGBM's own on the same split **+ 0.010**; MAE is recorded as a diagnostic and is never
  a gate.
- D-11 split verdicts: the final combined run includes only full-coverage winners and is judged
  on >= 2,280; the availability experiment carries its own dual-criterion verdict recorded
  separately. No apples-to-oranges combination.
- D-13 compute budget: 200 Colab Pro units total plus open-ended local overnights; recurrent
  and transformer candidates on Colab, Ridge/XGBoost/CatBoost/MLP local; hard stop at unit
  exhaustion, consumption recorded per candidate.
- D-19 tuning budget: 12 configs per deep candidate per granularity variant; XGBoost/CatBoost
  get sensible LightGBM-adjacent defaults only.
- D-10: no availability flag may be flipped default-on until the missing-snapshot NaN fallback
  is asserted by a passing test (`tests/test_availability.py::test_missing_snapshot_degrades_to_nan_not_raise`).

`### Experiment results (Phase 10)` — a markdown table with the same five columns as Phase F's
(`flag | criterion | measured | verdict | default`) and one row per Phase 10 flag
(`availability_flags`, `transfermarkt_injury`, `news_sentiment`, `bracket_ridge`,
`bracket_xgb`, `bracket_catboost`, `bracket_mlp`, `bracket_rnn`, `bracket_transformer`), every
`measured` and `verdict` cell literally `pending` and every `default` cell `off`. Later plans
fill their own row; 10-16 confirms none remain pending.

`### Data provenance and access risk (Phase 10)` — a table with columns
`source | access status | committed? | license/ToS posture | evidence`, seeded with one row per
external source and its status as known at plan time: FPL API (open, not committed),
FPL-Core-Insights (reachable, commit pending 10-06's license check), Transfermarkt
(**unverified**, spike gated in 10-05), figshare pre-scraped dataset (**403 on direct fetch**,
checked in 10-05), GDELT DOC 2.0 (keyless, conditional), Guardian (free key needed,
conditional), fplreview (**403 to automated access, manual capture only, never redistributed,
never a model input**), fbrapi.com (half-down 2026-09-09, not re-probed per D-04).

`### Unverified prior evidence (Phase 10)` — record that the IJCSS 2025 paper
(10.2478/ijcss-2025-0008) could not be located this session and every claim the Tier-1 todos
attribute to it is `[ASSUMED]`; and that `danielfrees/mlpremier` (arXiv 2405.02412) — the repo
D-02's todo cites to mine — published a **negative** result for Guardian-based news sentiment,
underperforming both its own CNN and its Ridge/LightGBM baselines. State that neither note
changes any pre-declared trigger; both are context the eventual ledger entries must cite rather
than omit.

Edit `IMPROVEMENTS.md` by scoped insertion only — do not rewrite or reflow any existing Phase
A-F content.
  </action>
  <verify>
    <automated>grep -c "Phase G" IMPROVEMENTS.md</automated>
    <fails_when>output is `0`</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import pathlib,re; t=pathlib.Path('IMPROVEMENTS.md').read_text(); s=t.split('## Phase G')[1].split('## Reference findings')[0]; [print('found', k) for k in ('2,280','+25','0.03','0.500','0.481','0.010','availability_flags','transfermarkt_injury','news_sentiment','bracket_transformer','mlpremier','10.2478/ijcss-2025-0008','fplreview') if k in s]; missing=[k for k in ('2,280','+25','0.03','0.500','0.481','0.010','availability_flags','transfermarkt_injury','news_sentiment','bracket_ridge','bracket_xgb','bracket_catboost','bracket_mlp','bracket_rnn','bracket_transformer','mlpremier','10.2478/ijcss-2025-0008','fplreview') if k not in s]; assert not missing, missing; assert s.count('pending') >= 18, s.count('pending')"</automated>
    <fails_when>non-zero exit, or an AssertionError printing a list of absent required tokens or a `pending` count below 18 (nine flag rows x measured+verdict)</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import pathlib,sys; t=pathlib.Path('IMPROVEMENTS.md').read_text(); lost=[h for h in ('## Phase F','### Adoption criteria (D-05/D-06, pre-declared)','### Experiment results','## Reference findings') if h not in t]; sys.exit('lost prior anchors: '+str(lost)) if lost else print('all prior anchors intact')"</automated>
    <fails_when>non-zero exit, or the message `lost prior anchors:` naming a Phase A-F heading the edit destroyed</fails_when>
  </verify>
  <acceptance_criteria>
    - `IMPROVEMENTS.md` contains a `## Phase G — xP experiment follow-ups (Phase 10)` heading with the four subsections named in the action
    - The declared-criteria subsection contains the literal figures `2,280`, `2,262`, `+25`, `0.03`, `0.383`, `0.500`, `0.481` and `0.010`
    - The Phase 10 results table has exactly nine flag rows, each with `pending` in both `measured` and `verdict` and `off` in `default`
    - The provenance table names all eight external sources with the access status recorded in the action
    - The unverified-prior-evidence subsection names both `10.2478/ijcss-2025-0008` and `mlpremier`
    - All four Phase A-F anchor headings still exist (the insertion was scoped, not a rewrite)
  </acceptance_criteria>
  <done>Every Phase 10 adoption criterion, the exact D-02 trigger number, the nine pending flag rows, the per-source access-risk record and the two unverified priors are on paper in IMPROVEMENTS.md — before any plan in this phase measures anything against them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub raw / api.github.com -> `data/raw/fpl_core_insights/` | untrusted third-party CSV content and directory listing cross here (Task 1 probe) |
| FPL bootstrap-static -> `data/snapshots/*.parquet` | untrusted upstream JSON crosses here; already an established boundary, widened by three new fields |
| `data/processed/availability.parquet` -> `features.parquet` -> model training | a wrong value or a wrong timestamp here silently corrupts every downstream prediction |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01-01 | Tampering | `data/availability.py` provider registry | high | mitigate | `_require_columns` raises `ValueError` naming the absent column, so an upstream schema change fails loudly instead of producing an all-NaN feature the model silently trusts (the `data/fotmob.py` `_require()` precedent, T-09-09-01) |
| T-10-01-02 | Tampering | `resolve_as_of` time boundary | critical | mitigate | selection requires BOTH `snapshot_ts < deadline_ts` AND `snapshot_ts < min(kickoff_time)`; asserted for every registered `source` by `tests/test_availability.py::test_every_source_row_predates_its_gw_deadline`. A leaked post-deadline value would inflate every measured verdict in this phase |
| T-10-01-03 | Denial of Service | `data/build_table.py` availability block | medium | mitigate | wrapped in the same `try/except Exception` + `[availability] skipped` print as the three existing optional-enrichment blocks — an availability failure can never break the daily pipeline (`data/odds.py`'s optional-enrichment contract) |
| T-10-01-04 | Information Disclosure | `news` free-text captured into `data/snapshots/*.parquet` (a tracked, permanent archive) | low | accept | FPL's `news` field is public editorial copy about publicly-known players ("Knock - 75% chance of playing"), not third-party personal data; it is already public on every FPL client. No redaction applied |
| T-10-01-05 | Repudiation | pre-declared criteria written after a measurement | high | mitigate | Task 3 writes the criteria block in wave 1; plan 10-08 (the first adoption run) carries a `<precondition>` asserting the block exists, so no verdict can be back-fitted to a number |
| T-10-01-06 | Tampering | test isolation vs the irreplaceable `data/snapshots/` archive | high | mitigate | `tests/test_availability.py`'s autouse fixture monkeypatches `data.snapshot.SNAP_DIR` to `tmp_path`, matching `tests/test_cron.py`'s existing rule that no test may write into the un-backfillable archive |
</threat_model>

<verification>
- `python -m pytest` green repo-wide (no Phase 9 regression from the new column family)
- `ruff check .` clean
- `data/processed/features.parquet` carries `av_chance_pct` and no rolled `av_*` variant
- Both `wf_t1_av_off.json` and `wf_t1_av_on.json` exist and record opposite `availability_flags` states
- `config.EXPERIMENTS` has 11 keys, all False
- `IMPROVEMENTS.md` `## Phase G` exists with nine `pending` flag rows and all Phase A-F anchors intact
</verification>

<success_criteria>
- One availability column is proven end-to-end from capture through the gate to a completed harness run, on real source data
- The as-of-deadline leakage rule, the D-10 NaN fallback, and the raw-not-rolled family classification are each locked by a named passing test
- Three new availability fields are captured from today onward
- Every Phase 10 adoption criterion, including the exact D-02 number, is on paper before any adoption run
</success_criteria>

<output>
Create `.planning/phases/10-xp-experiment-follow-ups/10-01-SUMMARY.md` when done. It MUST
record: the probe CSV's verbatim column list, the measured `av_chance_pct` join coverage, the
two tagged `model+chips` figures, and the locked D-02 number as written to IMPROVEMENTS.md.
</output>
