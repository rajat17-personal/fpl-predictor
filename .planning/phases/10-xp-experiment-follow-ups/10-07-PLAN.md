---
phase: 10-xp-experiment-follow-ups
plan: 07
type: execute
wave: 3
depends_on: ["10-05", "10-04"]
files_modified:
  - data/transfermarkt.py
  - data/id_crosswalk.py
  - data/external/transfermarkt/README.md
  - config.py
  - features/engineer.py
  - backtest/walk_forward.py
  - tests/test_leakage.py
  - tests/test_experiments.py
autonomous: true
requirements:
  - TODO-TM-INJURY
user_setup: []

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The injury backfill runs as a killable background job whose progress survives a kill and resumes from its on-disk cache -- weeks of fetching are never redone (D-06/D-07)."
    - "The normalized injury-spell table is committed with a README recording retrieval date, source and reduction, so re-fetching is never a prerequisite for reproducing a result (D-07)."
    - "Every injury feature for gameweek g derives only from spells whose recorded dates precede g's kickoff, asserted on real data (the todo's own explicit leakage-test requirement)."
    - "config.EXPERIMENTS['transfermarkt_injury'] is registered, defaults False, and drops every tm_ column when off."
    - "Each player's Transfermarkt id is resolved once through the shared crosswalk and cached, never re-searched per fetch, and never via a second name matcher."
  artifacts:
    - "data/external/transfermarkt/injury_spells.csv plus README.md"
    - "data/transfermarkt.py — build/load/attach/main"
    - "config.INJURY_COLS"
  key_links:
    - "data/id_crosswalk.resolve_by_name -> tm_player_id cache -> injury spells -> player-GW overlap join"
    - "config.EXPERIMENTS['transfermarkt_injury'] -> backtest/walk_forward.py::apply_experiment_feature_gating"
---

<objective>
Build the Transfermarkt injury-history backfill and its player-gameweek feature join at the
scope plan 10-05's go/no-go checkpoint authorised: a resumable killable background fetcher, a
committed normalized spell table, a date-range overlap join into the pre-match context family,
and a default-off `transfermarkt_injury` flag.

Purpose: FPL's own availability flags can only be backfilled about one season (and, per plan
10-01's correction, only via the vendored 2025-26 snapshot). Injury records are retrospective
and timestamped, so they reach every training season back to 2016-17 — the whole point of
D-06 is to get availability signal where FPL flags cannot go.

Output: `build`/`load_transfermarkt`/`attach`/`main` on the module plan 10-05 left as a
probe-only parser, a `tm_player_id` cache in `data/id_crosswalk.py`, the committed spell table
with its README, `config.INJURY_COLS`, a gating branch, and the leakage assertion the todo
explicitly requires.

Scope note: this plan implements decision **A** or **B** from plan 10-05's checkpoint. On
decision **C** (figshare substitute) Task 1 changes to vendoring that dataset instead of
fetching — the join, features, flag and tests in Tasks 2-3 are unchanged. On decision **D**
(not acquirable) this plan does not execute at all; 10-05 already recorded the verdict and
plan 10-08 measures only the availability arm.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-xp-experiment-follow-ups/10-CONTEXT.md
@.planning/phases/10-xp-experiment-follow-ups/10-RESEARCH.md
@.planning/phases/10-xp-experiment-follow-ups/10-PATTERNS.md
@.planning/phases/10-xp-experiment-follow-ups/10-05-SUMMARY.md
@.planning/todos/pending/2026-09-10-transfermarkt-injury-history.md
</context>

## Artifacts this phase produces

New symbols this plan creates (exclude from drift verification): `data/transfermarkt.py`
additions `_ID_CACHE`, `resolve_tm_id`, `build`, `load_transfermarkt`,
`injury_status_as_of`, `attach`, and the `--build`/`--resume`/`--seasons` flags;
`data/id_crosswalk.py` addition `tm_id_cache`; `config.INJURY_COLS` members `tm_injured`,
`tm_days_out_so_far`, `tm_spells_prior_365d`, `tm_days_out_prior_365d`;
`config.EXPERIMENTS["transfermarkt_injury"]`; directory `data/external/transfermarkt/` with
`injury_spells.csv` and `README.md`; `tests/test_leakage.py` test
`test_transfermarkt_injury_dates_precede_kickoff`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Resumable killable backfill and the committed normalized spell table</name>
  <files>data/transfermarkt.py, data/id_crosswalk.py, data/external/transfermarkt/README.md</files>
  <read_first>
    - data/transfermarkt.py — the whole file as written by plan 10-05: `HEADERS`, `TIMEOUT`, `_MIN_INTERVAL_S = 3.0`, `_RAW_DIR`, `_OUT`, `_throttle`, `_require`, `_fetch_page`'s HTML cache, `parse_injury_table`'s six-column contract and its challenge-page raise, and `probe`
    - .planning/phases/10-xp-experiment-follow-ups/10-05-SUMMARY.md — the authorised scope (decision letter and named seasons), the verbatim served column headers, the measured per-page wall clock, and the projected total
    - data/id_crosswalk.py — the whole file: `resolve_by_name`'s three tiers, `_NAME_FIXUPS`, `build()`'s output shape and the `player_code.is_unique` invariant plan 09-02 established
    - data/fotmob.py lines 222-337 — `build(*, force=False)`'s cache-then-fetch loop, `load_fotmob()`'s return-None-if-absent contract, and `attach`'s row-count assert plus coverage print
    - data/external/README.md — the five-section committed-snapshot README shape
    - scripts/experiment_run.sh — the `nohup ... & disown` detach pattern plus per-run logging under `data/processed/experiments/`, the project's own convention for a long unattended job
  </read_first>
  <precondition>Plan 10-05's checkpoint recorded decision A, B or C. On decision D this plan does not execute. The authorised season scope named in 10-05-SUMMARY.md is the scope this task fetches — do not widen it.</precondition>
  <behavior>
    - `resolve_tm_id(name)` resolves a display name to a Transfermarkt player id exactly once per `player_code` and thereafter reads the cache; a second call for the same player issues zero HTTP requests
    - `build(resume=True)` skips any player whose HTML page is already cached, so killing and restarting resumes rather than restarting
    - `build` never loses completed work: the spell table is appended to disk incrementally, and a kill mid-run leaves a valid, readable partial artifact
    - The normalized spell table has one row per `(player_code, from_date)` with no duplicates, and `until_date` may be null for an ongoing spell
    - `load_transfermarkt()` returns `None` when the committed table is absent, never raises
    - A page that raises the challenge-page `ValueError` is recorded as a failed player and skipped, not fatal to the run
  </behavior>
  <action>
Add `tm_id_cache()` to `data/id_crosswalk.py`: a small persistent `player_code -> tm_player_id`
map stored as `config.DATA_DIR / "external" / "transfermarkt" / "tm_id_map.csv"`, with
read-through/write-through helpers. It lives beside the crosswalk because id resolution is the
crosswalk's job, and 10-RESEARCH.md Pattern 3 is explicit that resolving an FPL player to a
Transfermarkt id is itself a name-matching problem the crosswalk should own. Do NOT add a second
name matcher: the name-to-candidate step goes through `resolve_by_name`, and only the
FPL-side `player_code` result is cached against the Transfermarkt id. Preserve the
`player_code.is_unique` invariant plan 09-02 established — assert it after any cache write.

Add `resolve_tm_id(name, *, force=False)` to `data/transfermarkt.py`: cache hit returns
immediately; a miss issues one throttled search request, takes the first player hit, writes the
cache, and records the resolved pair. Print a one-line resolution record per new id so the
SUMMARY can carry a sample for human eyeball.

Add `build(*, resume=True, seasons=None, force=False)`:
1. Enumerate the target players: distinct `(player_code, name)` from
   `data/processed/player_gw.parquet` restricted to the authorised seasons.
2. For each, `resolve_tm_id` then `_fetch_page` (which already caches HTML under `_RAW_DIR`)
   then `parse_injury_table`.
3. Normalize each parsed frame to the committed contract: `player_code`, `tm_player_id`,
   `season_label`, `injury`, `from_date` (a date), `until_date` (a date or null for ongoing),
   `days_out` (int or null), `games_missed` (int or null). Coerce dates with an explicit
   format check and drop — with a counted, printed warning — any row whose `from_date` will not
   parse, rather than admitting `NaT` into a date-range comparison.
4. Append incrementally to `data/external/transfermarkt/injury_spells.csv` after every N
   players (choose N so a kill loses at most a minute of work), and print progress as
   `[transfermarkt] {i}/{total} players, {spells} spells, {failed} failed, eta {mins}m`.
5. On `resume=True`, skip any player whose HTML is already cached AND whose rows are already in
   the CSV. Deduplicate on `(player_code, from_date)` before every write, so a resumed run can
   never double-count a spell.
6. Wrap the per-player work so a challenge-page `ValueError`, a `requests.RequestException` or
   a parse failure increments the failure counter and continues — one bad page must not end a
   multi-hour run.

Add `load_transfermarkt()` returning the committed CSV or `None`. Expose `--build`,
`--resume`/`--no-resume`, `--seasons` and `--force` on `main()`, with help text stating that
`--build` is a deliberate, human-run, network-using step never wired into cron or CI, and that
it is safe to kill.

Run the backfill detached in `scripts/experiment_run.sh`'s style
(`nohup ... > data/processed/experiments/transfermarkt_build-<ts>.log 2>&1 & disown`) and give
the developer the `tail -f` command for that log in the SUMMARY — long runs must always come
with a log-following command.

Write `data/external/transfermarkt/README.md` with `data/external/README.md`'s five sections:
`## Source` (transfermarkt.com, the injury-history URL pattern, retrieval date range, and the
access evidence from 10-05's probe), `## Attribution`, `## Regeneration` (the exact
`python -m data.transfermarkt --build` command, the never-in-cron-or-CI sentence, and the note
that it is resumable and killable), `## Reduction applied` (the eight retained columns; raw HTML
pages stay uncommitted under gitignored `data/raw/transfermarkt/` per D-07), and
`## PII spot-check` (only public player names/ids and public injury records; no manager or
account identity). Add a `## ToS and redistribution posture` section stating what is committed
is a derived normalized table, not scraped pages, and recording the posture decision from
10-05's checkpoint.
  </action>
  <verify>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import data.transfermarkt as t; assert callable(t.build) and callable(t.load_transfermarkt) and callable(t.resolve_tm_id); import data.id_crosswalk as x; assert callable(x.tm_id_cache); print('surface ok')"</automated>
    <fails_when>non-zero exit, or an AttributeError naming a missing function</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import pandas as pd, config, pathlib; p=config.DATA_DIR/'external'/'transfermarkt'/'injury_spells.csv'; d=pd.read_csv(p); need=['player_code','tm_player_id','season_label','injury','from_date','until_date','days_out','games_missed']; miss=[c for c in need if c not in d.columns]; assert not miss, miss; assert not d.duplicated(['player_code','from_date']).any(), int(d.duplicated(['player_code','from_date']).sum()); assert d.from_date.notna().all(), 'unparsed from_date admitted'; print('spells', len(d), 'players', d.player_code.nunique())"</automated>
    <fails_when>non-zero exit, a FileNotFoundError on the spell table, or an AssertionError naming missing columns, a duplicate `(player_code, from_date)` count, or an unparsed `from_date`</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import pandas as pd, config; m=pd.read_csv(config.DATA_DIR/'external'/'transfermarkt'/'tm_id_map.csv'); assert m.player_code.is_unique, 'duplicate player_code would multiply rows in every downstream merge'; print('id map rows', len(m))"</automated>
    <fails_when>non-zero exit, or an AssertionError naming duplicate `player_code` entries (the plan 09-02 invariant)</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import time,data.transfermarkt as t,data.id_crosswalk as x; m=x.tm_id_cache(); k=next(iter(m)); s=time.monotonic(); [t.resolve_tm_id.__wrapped__ if False else None]; v1=m[k]; e=time.monotonic()-s; assert e < 0.5, e; print('cache read', e, 'seconds')"</automated>
    <fails_when>non-zero exit, or an AssertionError showing a cache read taking 0.5 seconds or more (which would mean a network request on a cache hit)</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import pathlib,sys; t=pathlib.Path('data/external/transfermarkt/README.md').read_text(); need=('## Source','## Attribution','## Regeneration','## Reduction applied','## PII spot-check','## ToS and redistribution posture','python -m data.transfermarkt --build','never wired into cron or CI','resumable'); miss=[k for k in need if k not in t]; sys.exit('README missing: '+str(miss)) if miss else print('README complete')"</automated>
    <fails_when>non-zero exit, or the message `README missing:` listing an absent section or phrase</fails_when>
    <automated>git status --porcelain data/raw/transfermarkt</automated>
    <fails_when>the output is non-empty — raw scraped HTML pages must stay uncommitted under gitignored `data/raw/` per D-07</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/ruff check .</automated>
    <fails_when>non-zero exit, or any diagnostic naming data/transfermarkt.py or data/id_crosswalk.py</fails_when>
  </verify>
  <acceptance_criteria>
    - `data/external/transfermarkt/injury_spells.csv` exists with the eight named columns, no duplicate `(player_code, from_date)`, and no null `from_date`
    - `data/external/transfermarkt/tm_id_map.csv` exists and `player_code` is unique
    - `load_transfermarkt()` returns `None` when the CSV is renamed away, and does not raise
    - `build`'s progress line reports players, spells, failures and an ETA; a resumed run skips already-cached players (proven by a second run's own printed skip count)
    - `data/raw/transfermarkt/` is untracked (raw HTML never committed)
    - `data/external/transfermarkt/README.md` has all six sections
    - The SUMMARY records the `tail -f` command for the detached build log, the total wall clock, the spell/player/failure counts, and a sample of 10 resolved `(name, tm_player_id)` pairs for eyeball
  </acceptance_criteria>
  <done>The authorised season scope is backfilled by a resumable killable job, the normalized spell table is committed with a reproducible README, ids resolve once through the shared crosswalk, and raw pages stay out of git.</done>
  <reversibility rating="costly">Per D-07: the committed spell table lives in git history permanently. Mitigated by committing only the derived eight-column normalized table (never raw pages), and by the ToS-posture record in the README.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Injury-spell overlap join into the pre-match context family</name>
  <files>data/transfermarkt.py, config.py, features/engineer.py</files>
  <read_first>
    - data/transfermarkt.py — as extended by Task 1, especially the committed spell table's column contract
    - data/availability.py — as written by 10-01/10-04: `gw_deadlines()`, and the family's registration in `CONTEXT_COLS`. The injury family is a sibling of this one, not of `ROLL_STATS`
    - config.py — `AVAILABILITY_COLS` and its comment block as the shape to mirror for `INJURY_COLS`; the `EXPERIMENTS` dict with the `# --- Phase 10 ---` comment
    - features/engineer.py lines 38-60 — the `CONTEXT_COLS` expression and its no-rolling comment
    - data/fotmob.py lines 278-318 — `attach`'s row-count assert and coverage print
    - .planning/phases/10-xp-experiment-follow-ups/10-RESEARCH.md — the Code Examples section's `injury_status_as_of` overlap-join sketch, and Pattern 4's family table
  </read_first>
  <behavior>
    - `injury_status_as_of(spells, player_code, as_of)` returns injured=True with a positive `days_out_so_far` when `as_of` falls inside a spell, and injured=False with zero days when it does not
    - An ongoing spell (null `until_date`) counts as active for any `as_of` at or after its `from_date`
    - `as_of` is each gameweek's deadline, never the fixture kickoff, so the feature reflects what a manager knew at the decision point
    - `attach` preserves the row count exactly and prints a coverage percentage
    - A player with no spells at all yields `tm_injured = 0` and zero counts — NOT NaN — because absence of an injury record is a genuine negative observation for a player the source covers, unlike the availability family where absence means no snapshot existed
    - A player whose `player_code` never resolved to a Transfermarkt id yields NaN across the family, because that is genuinely unknown rather than a negative
  </behavior>
  <action>
Implement `injury_status_as_of` on the 10-RESEARCH.md sketch, then vectorize the whole join in
`attach(full)` rather than looping per row: build the `(season, gw) -> deadline_ts` map from
`data.availability.gw_deadlines()` (reuse it, do not re-derive deadlines — one definition,
one place), then for each `(player_code, season, gw)` compute the four features against
`deadline_ts`:

- `tm_injured` — 1.0 when a spell is active at the deadline, else 0.0
- `tm_days_out_so_far` — days between the active spell's `from_date` and the deadline, else 0.0
- `tm_spells_prior_365d` — count of spells whose `from_date` falls in the 365 days before the
  deadline
- `tm_days_out_prior_365d` — total days out across those spells, clipped to the window

The critical distinction, which the behavior block above states and this action makes explicit
in a comment: **absence of a record is not absence of information here**. A player the source
covers with no active spell is genuinely not injured, so the features are 0.0. A player whose
`player_code` has no `tm_player_id` in the cache is genuinely unknown, so all four are NaN. Draw
that line from the id map's membership, not from the spell table's — a covered player with zero
spells must not be confused with an unresolvable one. Add a `_covered_player_codes()` helper
reading the id map so the rule is stated once.

Row-count-preserving left merge with an `AssertionError` on change, then a coverage print
naming both numbers: the fraction of rows with a resolved id, and the fraction of those with an
active spell.

In `config.py`, add
`INJURY_COLS = ["tm_injured", "tm_days_out_so_far", "tm_spells_prior_365d", "tm_days_out_prior_365d"]`
after `AVAILABILITY_COLS`, with a comment in `AVAILABILITY_COLS`' shape: a pre-match CONTEXT
family computed unconditionally, gate in `backtest/walk_forward.py`, plus the resolved-versus-
unresolved NaN rule and the sentence that these are NOT match outcomes and must never be
registered in `ROLL_STATS`. Add `"transfermarkt_injury": False` to `EXPERIMENTS` under the
Phase 10 comment.

In `features/engineer.py`, append `config.INJURY_COLS` to the `CONTEXT_COLS` expression next to
`config.AVAILABILITY_COLS`.

**Wiring split, stated deliberately.** `data/build_table.py` is not in this plan's file set —
plan 10-01 owns it and this plan runs in a later wave, so the optional-enrichment block that
calls `transfermarkt.attach(full)` during a pipeline rebuild lands in **plan 10-08 Task 1**,
which owns that file. This task therefore defines `attach(full)` and exercises it directly
against a real `player_gw.parquet` frame in-process (see `<verify>`), which proves the join is
correct without needing the wiring commit. Say exactly this in `attach`'s docstring, naming
plan 10-08 Task 1, so the next reader does not mistake an unwired attach for an oversight.
  </action>
  <verify>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import config; assert config.INJURY_COLS == ['tm_injured','tm_days_out_so_far','tm_spells_prior_365d','tm_days_out_prior_365d'], config.INJURY_COLS; assert config.EXPERIMENTS['transfermarkt_injury'] is False; print('registered')"</automated>
    <fails_when>non-zero exit, or an AssertionError printing a column list that differs in content or order, or a flag that is not False</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import pandas as pd, config, data.transfermarkt as t; full=pd.read_parquet(config.PROCESSED_DIR/'player_gw.parquet'); n=len(full); out=t.attach(full); assert len(out)==n, (n, len(out)); miss=[c for c in config.INJURY_COLS if c not in out.columns]; assert not miss, miss; cov=out.tm_injured.notna().mean(); inj=out.tm_injured.fillna(0).mean(); print('id-resolved coverage', round(float(cov),4), 'active-spell rate', round(float(inj),4))"</automated>
    <fails_when>non-zero exit, or an AssertionError naming a row-count change or a missing injury column, or a printed id-resolved coverage of 0.0</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import pandas as pd, datetime as dt, data.transfermarkt as t; sp=pd.DataFrame({'player_code':[1,1],'from_date':pd.to_datetime(['2023-01-01','2024-05-01']),'until_date':pd.to_datetime(['2023-02-01',None])}); a=t.injury_status_as_of(sp,1,pd.Timestamp('2023-01-15')); b=t.injury_status_as_of(sp,1,pd.Timestamp('2023-03-01')); c=t.injury_status_as_of(sp,1,pd.Timestamp('2024-06-01')); assert a['injured'] and a['days_out_so_far']==14, a; assert not b['injured'] and b['days_out_so_far']==0, b; assert c['injured'], c; print('overlap semantics ok', a, b, c)"</automated>
    <fails_when>non-zero exit, or an AssertionError printing a status dict whose `injured` flag or `days_out_so_far` disagrees with the hand-computed expectation (14 days on 2023-01-15, not injured on 2023-03-01, ongoing spell active on 2024-06-01)</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import config; import features.engineer as e; assert all(c in e.CONTEXT_COLS for c in config.INJURY_COLS), [c for c in config.INJURY_COLS if c not in e.CONTEXT_COLS]; assert not any(c in e.ROLL_STATS for c in config.INJURY_COLS), 'injury columns must never enter the rolling path'; print('family classification ok')"</automated>
    <fails_when>non-zero exit, or an AssertionError listing injury columns absent from `CONTEXT_COLS` or present in `ROLL_STATS`</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/ruff check .</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `config.INJURY_COLS` equals the four names in the exact order given, and `config.EXPERIMENTS["transfermarkt_injury"] is False`
    - `injury_status_as_of` satisfies the three hand-computed cases in the verify (mid-spell, post-spell, ongoing spell)
    - `attach` preserves the row count and prints both coverage numbers
    - A covered player with zero spells yields 0.0 across the family; an id-unresolvable player yields NaN across the family — proven by a test on synthetic data plus the `_covered_player_codes()` helper existing
    - `config.INJURY_COLS` appears in `features/engineer.py`'s `CONTEXT_COLS` and never in `ROLL_STATS`
    - Deadlines come from `data.availability.gw_deadlines()`: `grep -c "gw_deadlines" data/transfermarkt.py` is non-zero and `data/transfermarkt.py` defines no second deadline derivation
    - `attach`'s docstring states that `data/build_table.py`'s wiring lands in plan 10-08 Task 1
  </acceptance_criteria>
  <done>Four injury features join by date-range overlap against each gameweek's deadline, with a stated and tested distinction between a genuine zero and a genuine unknown, reusing the one shared deadline definition.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Leakage assertion and flag gating for the injury family</name>
  <files>backtest/walk_forward.py, tests/test_leakage.py, tests/test_experiments.py</files>
  <read_first>
    - backtest/walk_forward.py lines 158-215 — `apply_experiment_feature_gating` as extended by plans 10-01 and 10-04, and its docstring's list of gated families
    - tests/test_leakage.py — the whole file: the `@needs_data` guard, the module-scoped `feat` fixture, `test_understat_features_are_rolled_not_raw` and `test_team_strength_ratings_reproducible_from_prior_matches` (the two family-discipline precedents), and `test_availability_features_are_raw_context_not_rolled` from 10-01
    - tests/test_experiments.py lines 20-32 — `_EXPERIMENT_KEYS` as extended by 10-01
    - data/transfermarkt.py — `attach` and `_covered_player_codes` as written by Task 2
    - .planning/todos/pending/2026-09-10-transfermarkt-injury-history.md — its explicit requirement: "extend tests/test_leakage.py to assert injury-spell dates precede fixture kickoff"
  </read_first>
  <behavior>
    - Every spell contributing to a gameweek's injury features has `from_date` strictly before that gameweek's kickoff, asserted on the REAL committed spell table joined to the real `player_gw.parquet` — not on synthetic data
    - No spell's `from_date` used for gameweek g falls at or after g's deadline
    - `apply_experiment_feature_gating` with `transfermarkt_injury` off drops every `tm_` column; with it on, all four survive
    - `config.EXPERIMENTS` contains exactly the Phase 9 keys plus `availability_flags` and `transfermarkt_injury`, all False
    - The injury family is raw pre-match context: no rolled `tm_*_r3`/`_r5`/`_r10`/`_rall` column exists in `features.parquet`
  </behavior>
  <action>
Add one drop branch to `backtest/walk_forward.py::apply_experiment_feature_gating` keyed on
`exp.get("transfermarkt_injury", False)` that drops every column whose name starts with `tm_`,
and extend the docstring's family list. Do not add a second gate.

Add `test_transfermarkt_injury_dates_precede_kickoff` to `tests/test_leakage.py` — the
assertion the todo explicitly requires, written against real data in
`test_team_strength_ratings_reproducible_from_prior_matches`'s style rather than a synthetic
frame. Guard it with a skip marker on the committed spell table's existence (mirroring the
existing `UNDERSTAT`/`FOTMOB` path-existence markers) so the suite stays green on a machine
without the backfill. The assertion: for every `(player_code, season, gw)` where
`tm_injured == 1`, the contributing spell's `from_date` is strictly less than that gameweek's
`deadline_ts` from `data.availability.gw_deadlines()`, and therefore strictly less than every
kickoff in that gameweek. Assert BOTH bounds explicitly rather than relying on the deadline
being earlier — the postponement caveat `gw_deadlines`'s own docstring records means the
kickoff bound is the harder one and is the one the todo names.

Add a second test asserting the raw-not-rolled family classification for `tm_` columns, in the
shape of 10-01's `test_availability_features_are_raw_context_not_rolled`.

In `tests/test_experiments.py`, add `"transfermarkt_injury"` to `_EXPERIMENT_KEYS`. Leave
`test_experiments_registry_default_off`'s body untouched.

Then rebuild features and confirm the gate both ways with the real frame.
  </action>
  <verify>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m pytest tests/test_leakage.py tests/test_experiments.py -q</automated>
    <fails_when>non-zero exit, or the summary line reports any failed or errored test</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m pytest tests/test_leakage.py::test_transfermarkt_injury_dates_precede_kickoff -q</automated>
    <fails_when>non-zero exit, or `no tests ran` (a rename would silently disable the leakage gate the todo requires), or the test reports as skipped when `data/external/transfermarkt/injury_spells.csv` exists</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import config; from models.train import load_features; from backtest.walk_forward import apply_experiment_feature_gating as g; d=load_features(); off=g(d, config.resolve_experiments('none')); on=g(d, config.resolve_experiments('transfermarkt_injury')); left=[c for c in off.columns if c.startswith('tm_')]; assert not left, left; kept=[c for c in on.columns if c.startswith('tm_')]; assert len(kept)==len(config.INJURY_COLS), kept; print('gate ok', kept)"</automated>
    <fails_when>non-zero exit, or an AssertionError listing `tm_` columns that survived the flag-off gate or a kept-column count that does not match `config.INJURY_COLS`</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import config; import tests.test_experiments as te" 2>/dev/null; /home/sraja/miniconda3/envs/python314/bin/python -c "import re,pathlib; t=pathlib.Path('tests/test_experiments.py').read_text(); blk=t.split('_EXPERIMENT_KEYS')[1].split('}')[0]; assert 'transfermarkt_injury' in blk and 'availability_flags' in blk, blk; print('registry keys updated')"</automated>
    <fails_when>non-zero exit, or an AssertionError printing an `_EXPERIMENT_KEYS` block missing either Phase 10 key</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m pytest -q</automated>
    <fails_when>non-zero exit, or the summary line reports any failed or errored test</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/ruff check .</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `tests/test_leakage.py::test_transfermarkt_injury_dates_precede_kickoff` exists, runs against the real committed spell table, and asserts both the deadline bound and the kickoff bound
    - A second test asserts no rolled `tm_*` column exists in `features.parquet`
    - `apply_experiment_feature_gating` drops all four `tm_` columns when the flag is off and keeps all four when on
    - `_EXPERIMENT_KEYS` has 12 entries and `test_experiments_registry_default_off` passes with an unmodified body
    - `python -m pytest` green repo-wide
  </acceptance_criteria>
  <done>The injury family is gated by one default-off flag in the single feature-selection seam, and the leakage assertion the todo names is passing against real data with both time bounds checked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| transfermarkt.com HTML -> normalized spell table -> committed CSV | untrusted third-party markup crosses into a permanent tracked artifact |
| `tm_player_id` resolution -> a player's injury history | a wrong id attributes another footballer's injuries to one of our players |
| spell dates -> player-gameweek features | a mis-parsed or future-dated spell becomes hindsight in a "pre-match" feature |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-07-01 | Tampering | wrong-footballer id resolution corrupting an unrelated player's P(play) | high | mitigate | ids resolve through `data.id_crosswalk.resolve_by_name` only (whole-token verification, mononym exclusion — the crosswalk's own discipline), cached once per `player_code` with a uniqueness assert, and 10 resolved pairs are recorded in the SUMMARY for human eyeball |
| T-10-07-02 | Tampering | a challenge page or parse failure silently becoming "no injuries" | critical | mitigate | `parse_injury_table` (plan 10-05) raises on the four challenge markers; `build` counts and reports failures per player, and an unresolvable id yields NaN — never 0.0 — so a failure can never read as a negative observation |
| T-10-07-03 | Tampering | an unparsed `from_date` entering a date-range comparison as NaT | high | mitigate | `build` coerces with an explicit format check and drops unparsable rows with a counted printed warning; the verify asserts `from_date.notna().all()` on the committed table |
| T-10-07-04 | Tampering | future-dated spell information leaking into gameweek g | critical | mitigate | features are computed against `deadline_ts` from the single shared `data.availability.gw_deadlines()`, and `tests/test_leakage.py::test_transfermarkt_injury_dates_precede_kickoff` asserts both the deadline and the kickoff bound on real data |
| T-10-07-05 | Tampering | a resumed run double-counting spells | medium | mitigate | deduplication on `(player_code, from_date)` before every incremental write, asserted on the committed table |
| T-10-07-06 | Legal/compliance | committing scraped page content | high | mitigate | only the derived eight-column normalized table is committed; raw HTML stays under gitignored `data/raw/transfermarkt/`, verify-enforced by `git status --porcelain` returning empty for that path; posture recorded in the README |
| T-10-07-07 | Denial of Service | a multi-hour run dying on one bad page | medium | mitigate | per-player try/except with a failure counter, incremental appends, and `--resume` skipping cached players, so a kill or a crash costs at most a minute of work |
</threat_model>

<verification>
- `python -m pytest` green repo-wide, including the new leakage assertion against real data
- `ruff check .` clean
- `injury_spells.csv` committed with the eight-column contract, no duplicates, no null `from_date`
- `tm_id_map.csv` committed with unique `player_code`
- `data/raw/transfermarkt/` untracked
- Gate drops all four `tm_` columns when off, keeps all four when on
- `_EXPERIMENT_KEYS` at 12 entries, all defaults False
</verification>

<success_criteria>
- The authorised season scope is backfilled by a resumable, killable job whose work is never lost
- The normalized spell table and id map are committed with a reproducible README
- Four injury features join by date-range overlap against the shared deadline definition
- Genuine zero and genuine unknown are distinguished and tested
- The leakage assertion the todo requires passes on real data with both time bounds checked
</success_criteria>

<output>
Create `.planning/phases/10-xp-experiment-follow-ups/10-07-SUMMARY.md` when done. It MUST
record: the `tail -f` command for the detached build log, total wall clock, spell/player/failure
counts, 10 resolved `(name, tm_player_id)` pairs for eyeball, the measured id-resolved coverage
and active-spell rate from `attach`, and any Transfermarkt column header that differed from
plan 10-05's probe finding.
</output>
