---
phase: 10-xp-experiment-follow-ups
plan: 14
type: execute
wave: 8
depends_on: ["10-13"]
files_modified:
  - models/bracket/export.py
  - requirements-experiments.in
  - requirements-experiments.txt
  - IMPROVEMENTS.md
  - tests/test_bracket.py
autonomous: false
requirements:
  - TODO-BRACKET
  - TODO-NEWS
user_setup: []

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "Only gate-winners get a full 6-season walk-forward; every candidate that held at the cheap gate is recorded with its number and never silently dropped (D-15)."
    - "The model-class question is closed with numbers for all six challenger families plus the LightGBM control, whatever the answer."
    - "If a torch candidate clears the >=2,280 bar, adoption is via a runtime-free export -- ONNX or a numpy forward pass -- and torch never enters the production image (D-17)."
    - "The news-sentiment verdict plan 10-12 measured is written to the ledger from the figures 10-12 handed over, with nothing re-derived."
  artifacts:
    - "models/bracket/export.py (only if a torch candidate adopts)"
    - "IMPROVEMENTS.md — the bracket verdict subsection and all nine Phase 10 results rows filled"
  key_links:
    - "bracket_gate_*.json -> the D-15 advance decision -> the 6-season run -> the ledger row"
    - "models/bracket/export.py -> onnxruntime as the only production-side dependency a torch winner would add (D-17)"
---

<objective>
Close the model-class question with numbers: promote the gate-winners to a full 6-season
walk-forward against the >= 2,280 bar, build the runtime-free export path if and only if a torch
candidate actually clears it, and write both the bracket verdict and plan 10-12's
news-sentiment verdict into the permanent ledger.

Purpose: D-15's two-stage gate exists so closing this question costs cheap validation fits for
the losers and a full retrain only for candidates that earned it. D-17 exists so a torch winner
cannot smuggle a 2 GB dependency into a production image built for a solo, pre-revenue project.
D-03's "run everything, results inform interpretation only" means the answer is recorded whether
it is interesting or not — Phase 9's own value came from eight numbered rejections.

Output: 6-season runs for the gate-winners, `models/bracket/export.py` conditionally, and
IMPROVEMENTS.md's Phase 10 results table fully filled with no `pending` cell remaining.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-xp-experiment-follow-ups/10-CONTEXT.md
@.planning/phases/10-xp-experiment-follow-ups/10-RESEARCH.md
@.planning/phases/10-xp-experiment-follow-ups/10-10-SUMMARY.md
@.planning/phases/10-xp-experiment-follow-ups/10-11-SUMMARY.md
@.planning/phases/10-xp-experiment-follow-ups/10-12-SUMMARY.md
@.planning/phases/10-xp-experiment-follow-ups/10-13-SUMMARY.md
</context>

## Artifacts this phase produces

New symbols this plan creates (exclude from drift verification, the export module only if a
torch candidate adopts): `models/bracket/export.py` (`export_to_onnx`, `OnnxRegressor`,
`verify_parity`, `PARITY_TOLERANCE`, `main`); `requirements-experiments.in` addition
`onnxruntime==1.29.0`; `IMPROVEMENTS.md` subsections
`### Model-class bracket: two-stage gate results and adoption verdict`,
`### news_sentiment: adoption verdict`; `tests/test_bracket.py` test
`test_onnx_export_matches_torch_predictions`.

<tasks>

<task type="auto">
  <name>Task 1: Promote gate-winners to the full 6-season walk-forward</name>
  <files>tests/test_bracket.py</files>
  <read_first>
    - models/bracket/gate.py — `GATE_MARGIN = 0.010`, the gate JSON schema, and `run_gate`'s `eval_split` labelling
    - .planning/phases/10-xp-experiment-follow-ups/10-10-SUMMARY.md, 10-11-SUMMARY.md, 10-13-SUMMARY.md — all seven candidates' gate figures, the LightGBM val-split baseline, and any `not_run_compute_exhausted` status
    - backtest/walk_forward.py — `main`'s `--experiments`/`--replicas`/`--tag` flags, `load_external_predictions` and `--external-preds` (for a Colab-trained winner), and the `multi_safe: None` limitation of frozen-artifact scoring
    - models/train.py — `train_predict(..., stage2=...)` as parameterized by plan 10-10, the path a locally-trained winner takes
    - IMPROVEMENTS.md `### Phase G` declared criteria — the >= 2,280 bar and D-15's gate rule
    - .planning/phases/10-xp-experiment-follow-ups/10-08-SUMMARY.md — the fresh-control discipline and the `features.parquet` sha256 convention
  </read_first>
  <precondition>All seven `data/processed/experiments/bracket_gate_<candidate>.json` files exist (plan 10-13 Task 3 required them, including a `not_run_compute_exhausted` file for any unrun candidate). If any is missing, stop and report rather than inferring a verdict from an absent file.</precondition>
  <action>
Read all seven gate JSONs and compute the D-15 advance decision mechanically: a candidate
advances only when its `spearman_xp_med` is at least
`bracket_gate_lgbm.json`'s `spearman_xp_med` plus `GATE_MARGIN` (0.010). Print the full
comparison table — candidate, granularity where applicable, Spearman, delta, MAE (diagnostic
only), status, ADVANCE/HOLD — and record it verbatim in the SUMMARY. Every candidate appears in
that table, including any that held and any that could not be run; a candidate absent from the
table is a candidate whose result was lost.

For each ADVANCE candidate, and only those, run the full 6-season walk-forward at 5 replicas
against a fresh contemporaneous control, following plan 10-08's discipline:

- A locally-trained winner (ridge / xgb / catboost / mlp) runs through the `stage2` seam. Since
  `backtest/walk_forward.py::main` has no `--stage2` flag and this plan's file set does not
  include that module, drive it by its experiment flag instead:
  `scripts/experiment_run.sh bracket_<name>_6 --replicas 5 --experiments bracket_<name>`.
  That requires the flag to actually select the candidate in `models/train.py` — verify that
  wiring exists before running, and if the flag is inert (the gating branch drops columns rather
  than selecting a regressor), report it as a blocking gap rather than running a no-op and
  recording its number as a result. A flag that changes nothing produces a run identical to the
  control, which is indistinguishable from a genuine zero-delta finding, and that ambiguity is
  precisely what would corrupt this ledger entry.
- A Colab-trained winner (rnn / transformer) has only a 2025-26 artifact from plan 10-13, not
  six seasons. Producing six would require six more Colab runs against a budget plan 10-13
  already spent. So: record the candidate's 2025-26 external-scored figure as its measurement,
  state explicitly in the ledger that it was NOT evaluated on the 6-season basis the >= 2,280
  bar is defined on, and judge it against the same-season control instead. Do NOT compare a
  single-season figure to the 6-season 2,280 bar — that is an apples-to-oranges comparison of
  exactly the kind D-11 forbids for the availability arm, and the same reasoning applies here.
  If a sequence candidate's single-season delta is large enough to be worth six more Colab runs,
  say so as a recommendation with the unit cost, and leave the decision to the developer rather
  than spending an unbudgeted balance.

If NO candidate advances, that is the finding and this task's output is the comparison table
plus a recorded zero-advance result. Do not lower `GATE_MARGIN`, do not extend a search budget,
and do not promote the best-of-a-bad-field. Phase 9's `capt_mc` non-decision is the precedent:
a gate that does not fire is recorded as a gate that did not fire.

Add `test_gate_advance_rule_is_mechanical` to `tests/test_bracket.py`: a pure-logic test over a
synthetic dict of gate results asserting the advance decision is exactly
`spearman >= lgbm_spearman + 0.010`, that a candidate with a better MAE and a worse Spearman does
NOT advance, and that a `not_run_compute_exhausted` candidate is neither advanced nor treated as
a hold. Keeping the rule in a tested function rather than in prose is what makes the ledger
entry auditable.
  </action>
  <verify>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m pytest tests/test_bracket.py -q</automated>
    <fails_when>non-zero exit, or the summary line reports any failed or errored test</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import json,glob,pathlib; g={pathlib.Path(p).stem.replace('bracket_gate_',''): json.loads(open(p).read()) for p in glob.glob('data/processed/experiments/bracket_gate_*.json')}; need={'lgbm','ridge','xgb','catboost','mlp','rnn','transformer'}; miss=need-set(g); assert not miss, miss; base=g['lgbm']['spearman_xp_med']; adv=[c for c,r in g.items() if c!='lgbm' and r.get('status')=='ok' and (r.get('spearman_xp_med') or -9) >= base+0.010]; [print(c, g[c].get('status'), g[c].get('spearman_xp_med'), 'delta', None if g[c].get('spearman_xp_med') is None else round(g[c]['spearman_xp_med']-base,4)) for c in sorted(need)]; print('ADVANCE:', sorted(adv) or 'none')"</automated>
    <fails_when>non-zero exit, or an AssertionError listing a candidate with no gate JSON</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import json,glob,sys; advs=sorted(glob.glob('data/processed/experiments/wf_bracket_*_6.json')); g={c: json.loads(open(f'data/processed/experiments/bracket_gate_{c}.json').read()) for c in ('lgbm','ridge','xgb','catboost','mlp')}; base=g['lgbm']['spearman_xp_med']; expected={c for c,r in g.items() if c!='lgbm' and r.get('status')=='ok' and (r.get('spearman_xp_med') or -9) >= base+0.010}; got={p.split('wf_bracket_')[1].rsplit('_6.json',1)[0] for p in advs}; sys.exit(f'6-season runs {sorted(got)} do not match the locally-trained ADVANCE set {sorted(expected)}') if got != expected else print('6-season runs match the advance set exactly:', sorted(got) or 'none')"</automated>
    <fails_when>non-zero exit, or the message naming a mismatch between the 6-season runs performed and the mechanically-computed locally-trained ADVANCE set (a run for a non-advancing candidate, or a missing run for an advancing one)</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "from models.bracket import gate; assert gate.GATE_MARGIN == 0.010, gate.GATE_MARGIN; print('margin unchanged at', gate.GATE_MARGIN)"</automated>
    <fails_when>non-zero exit, or an AssertionError showing `GATE_MARGIN` was altered to change the advance outcome</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import config; assert not any(config.EXPERIMENTS.values()), [k for k,v in config.EXPERIMENTS.items() if v]; print('every flag still default-off')"</automated>
    <fails_when>non-zero exit, or an AssertionError listing a flag flipped in this task rather than in plan 10-16's single close-out commit</fails_when>
  </verify>
  <acceptance_criteria>
    - All seven candidates appear in the printed comparison table with Spearman, delta, MAE, status and ADVANCE/HOLD
    - The set of `wf_bracket_*_6.json` runs equals exactly the locally-trained ADVANCE set — no run for a holder, no missing run for a winner
    - `GATE_MARGIN` is still 0.010 and no search budget was extended
    - A Colab-trained winner's single-season figure is recorded as single-season and is NOT compared to the 6-season 2,280 bar
    - `test_gate_advance_rule_is_mechanical` passes, including the better-MAE-worse-Spearman case and the `not_run_compute_exhausted` case
    - If the `bracket_<name>` flag is inert for a locally-trained winner, that is reported as a blocking gap rather than run as a no-op
    - No `config.EXPERIMENTS` default flipped
  </acceptance_criteria>
  <done>The advance rule has been applied mechanically to all seven candidates, exactly the winners got a full 6-season run against a fresh control, and a single-season Colab figure is labelled as such rather than measured against a six-season bar.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Conditional package-legitimacy gate for onnxruntime</name>
  <what-built>
Task 1's mechanical comparison over all seven gate results is complete and its ADVANCE set is
recorded. Nothing is installed. This checkpoint fires ONLY if a torch-based candidate (`mlp`,
`rnn` or `transformer`) cleared its bar; otherwise D-17 has not triggered and there is nothing
to approve.
  </what-built>
  <how-to-verify>
If no torch candidate cleared its bar: confirm that, and I will record the non-trigger in the
`capt_mc` shape (a non-triggered conditional is recorded as such, never silently omitted),
install nothing, and skip Task 3.

If one did, review this and answer:

| Package | Pin | Seam verdict | Reasons | Reading |
|---|---|---|---|---|
| onnxruntime | 1.29.0 | SUS | `too-new`, `unknown-downloads` | Microsoft-maintained since 2018, `onnxruntime.ai` surfaced by the seam. The signals come from the latest release's publish date and a download lookup that returned null in this environment |

Same framing as plan 10-10 Task 1: this is the known seam limitation for fast-releasing,
actively-maintained libraries, matching the Phase 05-01 `uv`/`ruff` and Phase 09-06
`torch`/`gymnasium`/`stable-baselines3`/`sb3-contrib` precedents.

What it is FOR: D-17 requires a torch winner to adopt via runtime-free inference so the
production image never carries torch. `onnxruntime` is the CPU runtime that makes that possible.

Where it lands and why: the dev-only `requirements-experiments.txt`, NOT `requirements.in`. No
`bracket_*` flag is flipped default-on inside any plan in this phase — plan 10-16 owns every
default change in one reviewable commit — so until that commit no production code path calls an
ONNX model and a production dependency would be dead weight in the image. Promotion into
`requirements.in` is the explicit follow-up if and when the flag flips, and I will record it as a
deferred item.

I will re-verify the pin against the live PyPI registry immediately before install and report
any drift from 1.29.0.
  </how-to-verify>
  <resume-signal>Type "approve", "refuse", or "not triggered" (if no torch candidate cleared its bar)</resume-signal>
  <action>
This checkpoint fires ONLY if Task 1's comparison shows a torch-based candidate (`mlp`, `rnn` or
`transformer`) clearing its bar. If none did, D-17 has not triggered: skip this checkpoint and
Task 3, install nothing, and record the non-trigger in the SUMMARY following the `capt_mc`
precedent — a non-triggered conditional is recorded as such, never silently omitted.

If it did fire, present for approval:

| Package | Pin | Seam verdict | Reasons | Reading |
|---|---|---|---|---|
| onnxruntime | 1.29.0 | SUS | `too-new`, `unknown-downloads` | Microsoft-maintained since 2018, `onnxruntime.ai` surfaced by the seam. The signals come from the latest release's publish date and a download lookup that returned null in this environment |

State the same framing plan 10-10 Task 1 used: this is the known seam limitation for
fast-releasing, actively-maintained libraries, matching the Phase 05-01 `uv`/`ruff` and Phase
09-06 `torch`/`gymnasium`/`stable-baselines3`/`sb3-contrib` precedents.

State what this install is FOR: D-17 requires a torch winner to adopt via runtime-free
inference so the production image never carries torch. `onnxruntime` is the CPU runtime that
makes that possible.

State where it lands and why: the dev-only `requirements-experiments.txt`, NOT `requirements.in`.
No `bracket_*` flag is flipped default-on inside any plan in this phase (plan 10-16 owns every
default change in one reviewable commit), so until that commit no production code path calls an
ONNX model and a production dependency would be dead weight in the image. Promotion into
`requirements.in` is the explicit follow-up if and when the flag flips.

**Re-verify the pin against the live PyPI registry immediately before install** and report any
drift from the audit's 1.29.0. Record the verbatim answer. Do not install before it.
  </action>
  <verify>
    <human-check>Either a verbatim approval (or refusal) for onnxruntime==1.29.0 with its live-registry re-verification result, or a recorded statement that no torch candidate cleared its bar so D-17 did not trigger and nothing was installed</human-check>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records either the verbatim approval/refusal plus the live re-verification result and any drift, or the explicit non-trigger statement
    - On non-trigger: no install happened and Task 3 is skipped, with the non-trigger recorded in the `capt_mc` shape
    - On refusal: the SUMMARY states that the torch winner has no adoption path this phase and that its flag consequently stays off regardless of its measured number
  </acceptance_criteria>
  <done>Either a recorded human decision authorises the one conditional package at a re-verified pin, or D-17's non-trigger is on the record with nothing installed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Runtime-free export path, only if a torch candidate cleared the bar</name>
  <files>models/bracket/export.py, requirements-experiments.in, requirements-experiments.txt, tests/test_bracket.py</files>
  <read_first>
    - models/bracket/deep.py, recurrent.py, transformer.py — the three torch candidates' forward signatures and the `TorchRegressorAdapter`'s `predict` contract
    - requirements-experiments.in and requirements-experiments.txt — the D-16 header and the `uv pip compile --generate-hashes -c requirements.txt -c requirements-rl.txt` invocation
    - Dockerfile — the runtime stage's dependency install, to confirm what a production dependency would actually mean here (and that `requirements-rl.txt` is referenced nowhere)
    - .planning/phases/10-xp-experiment-follow-ups/10-RESEARCH.md — the Package Legitimacy Audit's `onnxruntime` row (SUS: `too-new`, `unknown-downloads`; latest 1.29.0), the explicit instruction to defer this install until D-17 triggers, and the Don't Hand-Roll entry preferring `torch.onnx.export` over a hand-written numpy forward pass
    - .planning/phases/10-xp-experiment-follow-ups/10-CONTEXT.md — D-17's exact requirement: torch stays dev-only, the production image never carries it
  </read_first>
  <precondition>Task 2's checkpoint recorded an approval for `onnxruntime==1.29.0`. If Task 2 recorded a non-trigger (no torch candidate cleared its bar) or a refusal, skip this entire task and install nothing.</precondition>
  <behavior>
    - `export_to_onnx(model, sample_input, out_path)` writes an ONNX graph via `torch.onnx.export` with the sequence and mask inputs marked as dynamic on the batch axis
    - `OnnxRegressor` loads the graph with `onnxruntime.InferenceSession` on CPU providers only and exposes the same `predict(X)` contract the torch adapter does
    - `verify_parity` asserts the ONNX and torch predictions agree within `PARITY_TOLERANCE` on at least 1,000 real rows, and raises naming the maximum absolute deviation when they do not
    - The exported graph runs with torch uninstalled — verified by importing `OnnxRegressor` in a subprocess whose `sys.modules` blocks torch
    - `onnxruntime` is added to the experiments lockfile only, never to `requirements.in` / `requirements-dev.in`
  </behavior>
  <action>
Add `onnxruntime==1.29.0` to `requirements-experiments.in` with an inline comment naming
D-17 as the reason and recording that it entered the lockfile only after a torch candidate
cleared its bar. Recompile with the same invocation plan 10-10 used
(`--generate-hashes -c requirements.txt -c requirements-rl.txt`) and `uv pip sync
... --require-hashes`.

A note on where D-17 actually lands: `onnxruntime` is described in D-17 as "a small CPU dep"
for production, but this plan puts it in the experiments lockfile, not `requirements.in`.
That is deliberate and must be recorded: nothing in this phase flips a `bracket_*` flag
default-on inside a plan (plan 10-16 owns every default change in one reviewable commit), so
until that commit happens no production code path calls an ONNX model and a production
dependency would be dead weight in the image. State in `export.py`'s module docstring that
promoting `onnxruntime` from the experiments lockfile into `requirements.in` is the explicit
follow-up step required if and when the flag is flipped default-on, and record the same as a
deferred item in the SUMMARY. That keeps D-17's "production image never carries torch"
guarantee intact while not adding an unused runtime dependency now.

Create `models/bracket/export.py`:
- `PARITY_TOLERANCE = 1e-4` with a comment: float32 graph execution and torch's own eager
  kernels differ in the last bits, so exact equality is the wrong assertion, but a deviation
  above 1e-4 on a points-scale prediction indicates a genuine graph-tracing problem rather than
  numerical noise.
- `export_to_onnx(model, sample_input, out_path)` — `torch.onnx.export` with named inputs
  matching the candidate's forward signature (`x_seq`, `mask`, `x_static` for a sequence model;
  `x` for the MLP), `dynamic_axes` on the batch dimension of every input and of the output, and
  `model.eval()` before tracing. Use `torch.onnx.export` rather than a hand-written numpy
  forward pass, per the Don't Hand-Roll table: a hand-rolled pass duplicates the framework's
  graph tracing and risks silent numerical drift between training and inference.
- `OnnxRegressor` — `onnxruntime.InferenceSession(path, providers=["CPUExecutionProvider"])`,
  with `predict` returning a 1-D numpy array. CPU-only providers explicitly: the point of the
  export is a runtime with no CUDA and no torch.
- `verify_parity(torch_model, onnx_path, bundle, n=1000)` — compare both on at least `n` real
  rows drawn from a real season's bundle, print the maximum absolute deviation, and raise
  `AssertionError` naming it when it exceeds `PARITY_TOLERANCE`.
- `main()` with `--candidate` and `--season`, running export then parity in one command.

Add `test_onnx_export_matches_torch_predictions` to `tests/test_bracket.py` on a tiny synthetic
model, plus a subprocess test that imports `OnnxRegressor` and predicts with `torch` blocked in
`sys.modules` — that second test is the actual proof of D-17's claim, and asserting it in a
subprocess is the only way to prove it while torch is importable in the parent process.
  </action>
  <verify>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import onnxruntime; print('onnxruntime', onnxruntime.__version__)"</automated>
    <fails_when>non-zero exit, an ImportError, or a printed version other than 1.29.0</fails_when>
    <automated>grep -c "onnxruntime" requirements.in requirements-dev.in</automated>
    <fails_when>the output shows a non-zero count for either file — D-17 keeps the production and dev locks clean until plan 10-16 flips a default</fails_when>
    <automated>grep -rl "onnxruntime\|requirements-experiments" Dockerfile .github/workflows/ 2>/dev/null | wc -l</automated>
    <fails_when>output is anything other than `0` — the experiments stack and its packages must not reach the image or CI</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m pytest tests/test_bracket.py -q</automated>
    <fails_when>non-zero exit, or the summary line reports any failed or errored test</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "from models.bracket import export as e; assert e.PARITY_TOLERANCE == 1e-4, e.PARITY_TOLERANCE; import inspect; src=inspect.getsource(e); assert 'torch.onnx.export' in src, 'a hand-written forward pass was used instead of the framework export'; assert 'CPUExecutionProvider' in src, 'the session is not pinned to CPU providers'; assert 'dynamic_axes' in src, 'the graph has no dynamic batch axis'; print('export surface ok')"</automated>
    <fails_when>non-zero exit, or an AssertionError naming a wrong tolerance, a hand-written forward pass, a non-CPU provider list, or a missing dynamic batch axis</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m models.bracket.export --candidate rnn --season 2025-26</automated>
    <fails_when>non-zero exit, or the printed maximum absolute torch-versus-ONNX deviation exceeds 1e-4</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import subprocess,sys,textwrap; code=textwrap.dedent('''
import sys
class Block:
    def find_module(self, name, path=None):
        if name == 'torch' or name.startswith('torch.'):
            raise ImportError('torch is blocked to prove the runtime-free path')
sys.meta_path.insert(0, Block())
from models.bracket.export import OnnxRegressor
print('imported OnnxRegressor without torch')
'''); r=subprocess.run([sys.executable,'-c',code],capture_output=True,text=True); print(r.stdout,r.stderr); sys.exit(r.returncode)"</automated>
    <fails_when>non-zero exit, or `torch is blocked` appearing in the captured stderr as an unhandled ImportError — that means `models/bracket/export.py` imports torch at module scope and the production path is not actually runtime-free</fails_when>
  </verify>
  <acceptance_criteria>
    - The verbatim human approval and live-registry re-verification for `onnxruntime==1.29.0` are recorded in the SUMMARY
    - `onnxruntime` appears in `requirements-experiments.in`/`.txt` only, with hashes, and in neither `requirements.in` nor `requirements-dev.in`
    - Neither `onnxruntime` nor `requirements-experiments` appears in `Dockerfile` or any CI workflow
    - `PARITY_TOLERANCE == 1e-4`; `export_to_onnx` uses `torch.onnx.export` with `dynamic_axes`; `OnnxRegressor` pins CPU providers
    - A real export+parity run reports a maximum deviation at or below 1e-4
    - `OnnxRegressor` imports and predicts in a subprocess with torch blocked — no module-scope torch import in `export.py`
    - `export.py`'s docstring names the `requirements.in` promotion as the explicit follow-up for a default flip, and the SUMMARY records it as a deferred item
  </acceptance_criteria>
  <done>A torch winner has a runtime-free ONNX inference path proven to match its torch predictions within 1e-4 and to import without torch present, with `onnxruntime` confined to the dev-only experiments lockfile until a default flip actually requires it.</done>
  <reversibility rating="reversible">Per D-17: the export path is purely additive and the LightGBM default remains one flag away. Nothing in this task changes a shipped default.</reversibility>
</task>

<task type="auto">
  <name>Task 4: Write the bracket and news-sentiment verdicts and clear every pending row</name>
  <files>IMPROVEMENTS.md</files>
  <read_first>
    - IMPROVEMENTS.md `### Phase G` — the declared criteria, the nine-row results table, and the two Tier-1 verdict subsections plan 10-08 wrote
    - IMPROVEMENTS.md lines 225-320 — Phase 9's `### capt_ceiling:` and `### chips_v2:` verdict subsections, the shape a sweep-plus-verdict entry takes here
    - IMPROVEMENTS.md lines 254-269 — the `### capt_mc: Monte-Carlo variant not triggered` entry, the exact shape a non-triggered conditional takes
    - .planning/phases/10-xp-experiment-follow-ups/10-10-SUMMARY.md, 10-11-SUMMARY.md, 10-13-SUMMARY.md — every gate figure, granularity result, wall clock and Colab unit consumption
    - .planning/phases/10-xp-experiment-follow-ups/10-12-SUMMARY.md — the news-sentiment figures handed to this plan for the ledger write, and the routing decision
  </read_first>
  <action>
Write two subsections under `## Phase G` and clear every remaining `pending` cell.

`### Model-class bracket: two-stage gate results and adoption verdict (plans 10-10 to 10-14)` —
in Phase 9's `### capt_ceiling:` shape:
- The commands and artifact paths for the gate and for each 6-season run.
- The full seven-row gate table verbatim from Task 1: candidate, granularity, val-split
  Spearman, delta vs LightGBM, MAE (labelled diagnostic-only), status, ADVANCE/HOLD. Include the
  `eval_split` caveat: these are in-sample-early-stopping validation figures and are NOT
  comparable to the 0.383 pooled test-set number.
- For each ADVANCE candidate, its 6-season `model+chips` against the fresh control and the
  >= 2,280 bar; for a Colab-trained winner, its single-season figure explicitly labelled as
  single-season with the reason (the six-season basis would have cost six more Colab runs
  against an already-spent budget) and the recommendation-plus-unit-cost if it looked worth it.
- The mechanical verdict per candidate and the resulting default (`off` for everything rejected,
  per D-08: code stays merged, flag stays off).
- D-13's compute accounting: units consumed and remaining out of 200, per candidate, and any
  candidate recorded `not_run_compute_exhausted`.
- D-19's budget accounting: configs actually evaluated per deep candidate per granularity
  against the declared 12.
- The GRU-over-LSTM choice and its reasoning, so D-12's "one recurrent, not both" is a recorded
  decision rather than an unexplained absence.

`### news_sentiment: adoption verdict (plan 10-12)` — written from 10-12-SUMMARY.md's handed-over
figures with nothing re-derived. On BUILD: the measured 6-season `model+chips` pair, the delta,
the >= 2,280 comparison, the mechanical verdict, whether Guardian ran or GDELT-only mode was
used, and `mlpremier`'s negative prior cited as the relevant prior evidence. On NOT TRIGGERED or
DECLINE: the `capt_mc`-shaped non-decision entry, with the not-triggered versus
declined-on-cost distinction preserved exactly as 10-12 recorded it.

Then fill every remaining `pending` cell in the Phase 10 results table — all seven bracket rows
plus `news_sentiment` — so that the table's `pending` count reaches zero. Every cell must carry
a number or an evidenced reason, which is the standard Phase F's own table met.

Edit `IMPROVEMENTS.md` by scoped replacement only. Do not reflow or rewrite Phase A-F content,
and do not alter the two Tier-1 verdict subsections plan 10-08 wrote.
  </action>
  <verify>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import pathlib,sys; t=pathlib.Path('IMPROVEMENTS.md').read_text(); need=('### Model-class bracket: two-stage gate results and adoption verdict','### news_sentiment:','diagnostic','GRU','2,280','0.010'); miss=[k for k in need if k not in t]; sys.exit('ledger missing: '+str(miss)) if miss else print('both verdict sections present')"</automated>
    <fails_when>non-zero exit, or the message `ledger missing:` listing an absent subsection heading or required token</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import pathlib,sys; t=pathlib.Path('IMPROVEMENTS.md').read_text(); s=t.split('### Experiment results (Phase 10)')[1].split('###')[0]; n=s.count('pending'); sys.exit(f'{n} pending cells remain in the Phase 10 results table') if n else print('zero pending cells')"</automated>
    <fails_when>non-zero exit, or a message reporting a non-zero count of remaining `pending` cells</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import pathlib,sys; t=pathlib.Path('IMPROVEMENTS.md').read_text(); s=t.split('### Experiment results (Phase 10)')[1].split('###')[0]; rows=[l for l in s.splitlines() if l.strip().startswith('|') and '---' not in l and 'flag' not in l]; sys.exit(f'expected 9 flag rows, found {len(rows)}') if len(rows)!=9 else print('all nine flag rows present and filled')"</automated>
    <fails_when>non-zero exit, or a message reporting a flag-row count other than nine</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import pathlib,sys; t=pathlib.Path('IMPROVEMENTS.md').read_text(); lost=[h for h in ('## Phase F','### availability_flags: dual-criterion adoption verdict','### D-02 news-sentiment trigger evaluation','### capt_mc: Monte-Carlo variant not triggered (plan 09-03)','## Reference findings') if h not in t]; sys.exit('lost prior anchors: '+str(lost)) if lost else print('all prior anchors intact')"</automated>
    <fails_when>non-zero exit, or the message `lost prior anchors:` naming a heading this edit destroyed, including plan 10-08's own two subsections</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import config; assert not any(config.EXPERIMENTS.values()), [k for k,v in config.EXPERIMENTS.items() if v]; print('defaults unchanged; every flip is plan 10-16 close-out')"</automated>
    <fails_when>non-zero exit, or an AssertionError listing a flag flipped in this plan</fails_when>
  </verify>
  <acceptance_criteria>
    - Both named subsections exist under `## Phase G` with commands, artifact paths, measured tables and mechanical verdicts
    - The bracket entry carries the seven-row gate table, the `eval_split` non-comparability caveat, the D-13 unit accounting, the D-19 config accounting, and the GRU-over-LSTM rationale
    - A Colab-trained winner's figure is labelled single-season, never compared to the 6-season bar
    - The news-sentiment entry is written from 10-12's handed-over figures and preserves the not-triggered versus declined-on-cost distinction
    - The Phase 10 results table has nine flag rows and zero `pending` cells
    - All prior anchors intact, including plan 10-08's two Tier-1 subsections
    - No `config.EXPERIMENTS` default flipped
  </acceptance_criteria>
  <done>The model-class question is closed with numbers for all seven candidates, the news-sentiment verdict is recorded from measured figures, and every cell of the Phase 10 results table carries a number or an evidenced reason.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI -> the conda environment | one further third-party package crosses in, conditionally |
| an ONNX graph -> a future production inference path | a graph that silently disagrees with its torch original would serve wrong predictions |
| gate artifacts -> ledger verdicts | a lost or misread candidate result becomes a permanent gap in the record |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-14-SC | Tampering | the conditional `onnxruntime` install (SUS in the audit) | high | mitigate | blocking-human approval with live-registry re-verification and drift reporting, hash-locked into the experiments lockfile only, installed via `uv pip sync --require-hashes`; verify-enforced absence from `requirements.in`, `requirements-dev.in`, `Dockerfile` and CI |
| T-10-14-01 | Tampering | an ONNX graph diverging numerically from its torch original | high | mitigate | `verify_parity` on at least 1,000 real rows at a 1e-4 tolerance, raising with the maximum deviation named; `torch.onnx.export` used rather than a hand-written forward pass |
| T-10-14-02 | Tampering | torch reaching the production path through a module-scope import in `export.py` | high | mitigate | a subprocess test imports `OnnxRegressor` with torch blocked in `sys.meta_path` and must succeed — the actual proof of D-17's guarantee |
| T-10-14-03 | Repudiation | an inert experiment flag producing a no-op run recorded as a zero-delta finding | critical | mitigate | Task 1 requires verifying the flag actually selects the candidate before running, and reporting an inert flag as a blocking gap rather than recording its number |
| T-10-14-04 | Repudiation | a single-season figure judged against the six-season bar | high | mitigate | the action forbids the comparison explicitly and the ledger entry must label the figure single-season with the reason and the unit cost of extending it |
| T-10-14-05 | Repudiation | a candidate's result lost from the record | high | mitigate | the verify asserts all seven gate files exist and that the 6-season run set equals the mechanically-computed advance set exactly; the results table must reach zero `pending` cells |
</threat_model>

<verification>
- `python -m pytest` green repo-wide
- `ruff check .` clean
- All seven gate files present; the 6-season run set equals the advance set exactly
- `GATE_MARGIN` still 0.010; no search budget extended
- `onnxruntime` (if installed) hash-locked in the experiments lockfile only, absent from production/dev locks, Dockerfile and CI
- The torch-blocked subprocess import of `OnnxRegressor` succeeds (if the export path was built)
- Phase 10 results table: nine rows, zero `pending`
- Every `config.EXPERIMENTS` default still False
</verification>

<success_criteria>
- Exactly the gate-winners got a 6-season run; every candidate has a recorded number or an evidenced reason
- A torch winner's adoption path is runtime-free and proven to import without torch
- Both verdicts are in the permanent ledger with compute and budget accounting
- No cell of the Phase 10 results table is still `pending`
</success_criteria>

<output>
Create `.planning/phases/10-xp-experiment-follow-ups/10-14-SUMMARY.md` when done. It MUST
record: the full seven-row gate comparison table verbatim, the 6-season figures for every
advance candidate against the fresh control and the bar, the `onnxruntime` approval and
re-verification (or the note that D-17 did not trigger), the measured torch-versus-ONNX maximum
deviation, D-13's final unit balance, and the deferred `requirements.in` promotion item.
</output>
