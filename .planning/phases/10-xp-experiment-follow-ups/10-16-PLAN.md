---
phase: 10-xp-experiment-follow-ups
plan: 16
type: execute
wave: 10
depends_on: ["10-08", "10-14", "10-15"]
files_modified:
  - config.py
  - IMPROVEMENTS.md
  - tests/test_product.py
  - tests/test_experiments.py
autonomous: false
requirements:
  - TODO-AVAIL-FLAGS
  - TODO-TM-INJURY
  - TODO-BRACKET
  - TODO-NEWS
  - TODO-TOP100
  - TODO-FPLREVIEW
  - TODO-FBREF-MANUAL
  - TODO-RL-SHAPING
  - PHASE10-CRON
  - PHASE10-COLAB-SEAM
  - PHASE10-CRITERIA

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The final combined run uses SPLIT VERDICTS (D-11): only full-coverage winners enter the combined 6-season run judged on >=2,280; the availability arm carries its own dual-criterion verdict recorded separately. No apples-to-oranges combination."
    - "Every default change this phase makes happens in ONE reviewable config.py commit, and every flag that did not meet its own pre-declared criterion is still False."
    - "Every D-01 through D-21 decision has a recorded outcome, and every one of the eight pending todos is resolved with a verdict or an evidenced reason."
    - "The weekly product surface is provably unchanged unless a flag actually adopted: predict/live.py and predict/export.py behaviour is asserted by tests/test_product.py's export-contract regression."
  artifacts:
    - "IMPROVEMENTS.md — the final combined run, the D-01..D-21 decisions audit, and the what-this-phase-did-not-resolve section"
    - "config.py — the single default-change commit (or a recorded no-op)"
  key_links:
    - "the split-verdict combined run -> the >=2,280 bar for full-coverage winners only"
    - "any adopted flag -> predict/live.py + predict/export.py wiring, gated by tests/test_product.py"
---

<objective>
Close the phase the way Phase 9 closed: one split-verdict combined run, one reviewable default
change, a decisions audit covering every D-01 through D-21, and an honest record of what this
phase did not resolve.

Purpose: a phase of experiments is only trustworthy if its close-out is as mechanical as its
criteria. Phase 9's own final run measured 2262 — identical to its opening baseline, 18 short of
the bar — and that was reported as the result rather than chased. D-11 adds a refinement Phase 9
did not need: this phase has a partial-coverage arm, so a single combined number would be
dishonest, and the verdicts split.

Output: the combined run for full-coverage winners, the single `config.py` default commit, the
audit, and the product-surface proof.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-xp-experiment-follow-ups/10-CONTEXT.md
@.planning/phases/10-xp-experiment-follow-ups/10-08-SUMMARY.md
@.planning/phases/10-xp-experiment-follow-ups/10-12-SUMMARY.md
@.planning/phases/10-xp-experiment-follow-ups/10-14-SUMMARY.md
@.planning/phases/10-xp-experiment-follow-ups/10-15-SUMMARY.md
</context>

## Artifacts this phase produces

New symbols this plan creates (exclude from drift verification): tagged artifact
`wf_combined_phase10`; `IMPROVEMENTS.md` subsections
`### Final combined run (plan 10-16, D-11 split verdicts)`,
`### Decisions audit (plan 10-16, D-01 through D-21)`,
`### What this phase did not resolve`; `tests/test_product.py` test
`test_phase10_flags_default_off_leaves_export_contract_unchanged`.

<tasks>

<task type="auto">
  <name>Task 1: The split-verdict combined run and the single default change</name>
  <files>config.py, tests/test_experiments.py</files>
  <read_first>
    - IMPROVEMENTS.md `### Phase G` — the declared criteria, the fully-filled nine-row results table, and every verdict subsection plans 10-08, 10-14 and 10-15 wrote
    - .planning/phases/10-xp-experiment-follow-ups/10-08-SUMMARY.md — both Tier-1 verdicts, the D-09 dual-criterion numbers, the 2025-26 availability coverage, and any default flip that plan deferred here
    - .planning/phases/10-xp-experiment-follow-ups/10-12-SUMMARY.md and 10-14-SUMMARY.md — the news-sentiment and bracket verdicts and any deferred flip
    - IMPROVEMENTS.md lines 708-790 — Phase 9's own `### Final combined run (plan 09-10, D-13)`: the shape this run's write-up follows, and its own finding that the combined run equalled the opening baseline by construction when every flag stayed off
    - config.py lines 183-215 — the `EXPERIMENTS` dict with all 18 keys, and `resolve_experiments`
    - tests/test_experiments.py lines 20-32 — `_EXPERIMENT_KEYS` and `test_experiments_registry_default_off`, which must be deliberately edited if and only if a default genuinely flips
    - .planning/phases/10-xp-experiment-follow-ups/10-CONTEXT.md — D-07 (auto-adopt), D-08 (default-off, code stays merged), D-10 (availability flip gated on the fallback test), D-11 (split verdicts)
  </read_first>
  <action>
Determine the adopting set mechanically from the ledger, not from impression. For each of the
nine Phase 10 flags, read its recorded verdict and criterion and classify it:

- **Full-coverage winner** — met its own pre-declared criterion on the standard 6-season
  protocol (`transfermarkt_injury`, `news_sentiment`, any `bracket_*`).
- **Partial-coverage winner** — `availability_flags` meeting BOTH halves of D-09's dual
  criterion.
- **Rejected / not triggered / not acquirable / not run** — everything else.

Then run the combined run per D-11's split rule:

    scripts/experiment_run.sh combined_phase10 --replicas 5 --experiments <comma-separated full-coverage winners>

If the full-coverage winner set is empty, run it with no `--experiments` flag and record
explicitly that the combined run equals the baseline by construction — Phase 9's own close-out
records exactly that outcome and it is a legitimate, informative result, not a missing
measurement.

Do NOT add `availability_flags` to the combined run even if it adopted. Its coverage is one
season out of six, so including it would mix a one-season signal into a six-season average and
produce a number that is neither. That is precisely the apples-to-oranges combination D-11
forbids; its verdict stands separately on the dual criterion plan 10-08 measured.

Then make the single default change. Every plan in this phase deliberately deferred its flips
here so that all of them land in ONE reviewable `config.py` commit:

- For each adopting flag (full-coverage or partial), set its `EXPERIMENTS` value to `True`.
- Before flipping `availability_flags`, verify D-10's gate: run
  `pytest tests/test_availability.py::test_missing_snapshot_degrades_to_nan_not_raise` and
  confirm it passes. D-10 forbids the flip without it. If it does not pass, do not flip, and
  record the block.
- Update `tests/test_experiments.py::test_experiments_registry_default_off`. That test asserts
  every flag defaults off, so a genuine adoption requires editing it — which is the point: the
  test's whole value is that a default flip must be a deliberate edit rather than an accident.
  Change it to assert the exact expected set of `True` flags by name, so a future accidental
  flip of a *different* flag still fails. Do not weaken it to "some flags may be true".
- If NOTHING adopts, change nothing in `config.py`, leave that test untouched, and record the
  no-op explicitly. A phase where every experiment was rejected and the shipped config is
  byte-identical to the opening baseline is the honest outcome Phase 9 already recorded once.

Record the combined run's `model+chips` against the 2,262 opening baseline and the 2,280 bar,
and state whether the phase moved the shipped default at all.
  </action>
  <verify>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m pytest tests/test_availability.py::test_missing_snapshot_degrades_to_nan_not_raise -q</automated>
    <fails_when>non-zero exit, or `no tests ran` — D-10 forbids flipping `availability_flags` default-on without this gate passing</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import json,pathlib; s=json.loads(pathlib.Path('data/processed/experiments/wf_combined_phase10.json').read_text()); assert s['replicas']==5, s['replicas']; assert len(s['seasons'])==6, s['seasons']; on=[k for k,v in s['experiments'].items() if v]; assert 'availability_flags' not in on, 'D-11 forbids the partial-coverage arm in the combined run'; print('combined model+chips', s['model+chips'], 'vs baseline 2262, bar 2280 | flags on:', sorted(on) or 'none')"</automated>
    <fails_when>non-zero exit, a FileNotFoundError on the combined artifact, or an AssertionError on the replica count, the season count, or the presence of `availability_flags` in the combined run's flag set</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import config,json,pathlib; on=sorted(k for k,v in config.EXPERIMENTS.items() if v); t=pathlib.Path('tests/test_experiments.py').read_text(); print('shipped defaults ON:', on or 'none'); assert (len(on)==0) or all(f in t for f in on), [f for f in on if f not in t]; print('every flipped flag is named in test_experiments.py')"</automated>
    <fails_when>non-zero exit, or an AssertionError listing a flag flipped in `config.py` that `tests/test_experiments.py` does not name — a flip the registry test does not know about is exactly the accident that test exists to catch</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m pytest tests/test_experiments.py -q</automated>
    <fails_when>non-zero exit, or the summary line reports any failed or errored test — if a default flipped, the registry test must have been deliberately updated to match</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m pytest -q</automated>
    <fails_when>non-zero exit, or the summary line reports any failed or errored test</fails_when>
    <automated>git diff --stat config.py</automated>
    <fails_when>the output shows changes to `config.py` while `tests/test_experiments.py` shows none, or vice versa — a default flip and its registry-test update must land together in one commit</fails_when>
  </verify>
  <acceptance_criteria>
    - `data/processed/experiments/wf_combined_phase10.json` exists at 5 replicas over 6 seasons
    - `availability_flags` does not appear in the combined run's enabled flag set, whatever its own verdict was
    - Every flag whose `config.EXPERIMENTS` value is now `True` met its own pre-declared criterion as recorded in the ledger, and is named explicitly in the updated `test_experiments_registry_default_off`
    - `availability_flags` was flipped only if `test_missing_snapshot_degrades_to_nan_not_raise` passes
    - If nothing adopted, `config.py` and `tests/test_experiments.py` are byte-identical to before this plan and the no-op is recorded
    - `python -m pytest` green repo-wide
    - The SUMMARY records the combined `model+chips` against 2,262 and 2,280, and the exact set of flags flipped (or the empty set)
  </acceptance_criteria>
  <done>The combined run covers exactly the full-coverage winners with the partial-coverage arm held out per D-11, and every default change this phase makes is one reviewable commit whose registry test names each flipped flag.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the weekly product surface is unchanged unless a flag adopted</name>
  <files>tests/test_product.py</files>
  <read_first>
    - tests/test_product.py — the whole file: the export-contract regression test plan 09-10 added, its assertions about `web/data/*.json`'s shape, and how it invokes the export path
    - predict/export.py — `WEB_DATA`, the emitted files, and every place a feature column could reach the product payload
    - predict/live.py — the live pool builders and where model predictions enter, so the test covers the real path rather than a proxy
    - IMPROVEMENTS.md — plan 09-10's own recorded finding: zero product wiring was added for flags that stayed off, and the no-op was verified by a real end-to-end `python -m predict.export` run rather than by code reading
    - config.py — the `EXPERIMENTS` dict as it stands after Task 1
  </read_first>
  <action>
Phase 9's close-out recorded that zero product wiring was added because none of its flags
adopted, and it proved the no-op with a real end-to-end export run plus a new
`tests/test_product.py` regression. This phase inherits that standard and needs it to hold in
both directions.

Add `test_phase10_flags_default_off_leaves_export_contract_unchanged` to
`tests/test_product.py`. It asserts, for every Phase 10 flag whose shipped default is `False`,
that the exported payload's key structure is independent of that flag — concretely, that no
`av_`, `tm_`, `nw_` or `bracket_`-derived key appears anywhere in the emitted
`web/data/*.json` payloads. Those are training-time features and a bracket-internal model
choice; none of them belongs in the product contract regardless of any flag's state, and the
existing site plus the Playwright E2E suite consume that contract unchanged.

Then run the real end-to-end export and diff the contract, rather than asserting from code
reading:

    python -m predict.export

Compare the emitted `web/data/*.json` key structure against what it was before this phase.
Because `data/processed/` is gitignored but `web/data/*.json` is tracked, `git diff` on that
path is the honest check: report exactly which files changed and whether any change is a key
addition or removal versus a value refresh (values legitimately move with a fresh model run;
keys must not).

If a flag DID adopt in Task 1, the export's *values* will change — that is the whole point of
adopting — but the contract keys still must not. Record the value deltas for the top-10 xP rows
as evidence the adoption actually reached the product, and confirm the key set is identical.
That distinction, keys stable and values moved, is what separates a real adoption from a
silently inert flag.
  </action>
  <verify>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m pytest tests/test_product.py -q</automated>
    <fails_when>non-zero exit, or the summary line reports any failed or errored test</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m pytest tests/test_product.py::test_phase10_flags_default_off_leaves_export_contract_unchanged -q</automated>
    <fails_when>non-zero exit, or `no tests ran`</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m predict.export</automated>
    <fails_when>non-zero exit, or the run emits no `[export]` progress lines</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import json,glob,sys; bad=[]; 
for p in sorted(glob.glob('web/data/*.json')):
    t=open(p).read()
    for pre in ('\"av_','\"tm_','\"nw_','\"bracket_'):
        if pre in t: bad.append((p, pre))
sys.exit('training-time feature keys leaked into the product contract: '+str(bad)) if bad else print('no feature-family key in any web/data payload')"</automated>
    <fails_when>non-zero exit, or the message naming a `web/data/*.json` file that now carries an `av_`/`tm_`/`nw_`/`bracket_` key</fails_when>
    <automated>cd /home/sraja/fpl && git diff --stat web/data</automated>
    <fails_when>the output is empty AND a flag adopted in Task 1 — an adopted flag that changes nothing in the exported payload is an inert flag, not an adoption</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m pytest -q</automated>
    <fails_when>non-zero exit, or the summary line reports any failed or errored test</fails_when>
  </verify>
  <acceptance_criteria>
    - `test_phase10_flags_default_off_leaves_export_contract_unchanged` exists and passes
    - No `web/data/*.json` payload contains an `av_`, `tm_`, `nw_` or `bracket_` key
    - A real `python -m predict.export` run completed
    - The SUMMARY reports the `git diff --stat web/data` result and classifies every change as a key change or a value refresh
    - If a flag adopted: the SUMMARY records the top-10 xP value deltas as evidence the adoption reached the product, and confirms the key set is unchanged
    - If nothing adopted: the SUMMARY records that the exported contract is unchanged in both keys and structure
  </acceptance_criteria>
  <done>The weekly product contract is proven stable in its keys by a real end-to-end export run, and any adoption is shown to have actually moved the exported values rather than being an inert flag.</done>
</task>

<task type="auto">
  <name>Task 3: Decisions audit and the honest record of what this phase did not resolve</name>
  <files>IMPROVEMENTS.md</files>
  <read_first>
    - IMPROVEMENTS.md lines 787-900 — Phase 9's `### Decisions audit (plan 09-10, D-01 through D-16)`: the per-decision table shape, one row per decision with its outcome and the evidence
    - IMPROVEMENTS.md lines 899-990 — Phase 9's `### What this phase did not resolve` and `### This phase in the project's continuous history (Phases A-E)` sections, the shape this close-out mirrors
    - IMPROVEMENTS.md `### Phase G` — every subsection this phase wrote, and the fully-filled results table
    - .planning/phases/10-xp-experiment-follow-ups/10-CONTEXT.md — the full D-01 through D-21 list every audit row must cover
    - All plan SUMMARYs 10-01 through 10-15 — the evidence each audit row cites
    - .planning/todos/pending/ — the eight todo files, each of which needs a resolution recorded
  </read_first>
  <action>
Write three closing subsections under `## Phase G`, in Phase 9's own close-out shapes.

`### Final combined run (plan 10-16, D-11 split verdicts)` — the command, the artifact path, the
6-season table, and the combined `model+chips` against the 2,262 opening baseline and the 2,280
bar. State the split explicitly: which flags were in the combined run, that
`availability_flags` was deliberately held out per D-11 with its own dual-criterion verdict
standing separately, and what the shipped default config now is versus what it was at the start
of the phase. If nothing adopted, say so plainly and note — as Phase 9's own entry does — that
the combined run then equals the opening baseline by construction.

`### Decisions audit (plan 10-16, D-01 through D-21)` — a table with one row per decision:
`decision | what it required | outcome | evidence`. Every one of the twenty-one rows carries an
outcome, and the evidence column cites a plan SUMMARY, a tagged artifact path, or a named test.
Rows that need particular care:
- **D-04** — record both tail items' outcomes, including a Drop as a decision.
- **D-05** — record the correction plan 10-01 made to its "optional" wording: our own snapshots
  have zero walk-forward coverage, so the vendoring was a prerequisite for D-09's measurability,
  not an option.
- **D-13** — the final Colab unit balance out of 200, and any candidate recorded
  `not_run_compute_exhausted`.
- **D-14** — that the ingestion seam was built and round-trip-proven BEFORE any Colab artifact
  arrived, and whether the implausibility check fired.
- **D-17** — either the export path with its measured torch-versus-ONNX deviation, or the
  recorded non-trigger.
- **D-02** — the locked 0.500, the measured value, and the routing outcome, distinguishing
  not-triggered from declined-on-cost.

`### What this phase did not resolve` — the honest residue, each item with its reason:
- Any source recorded not-acquirable or declined, with its evidence.
- The FBref season-aggregate leakage limitation, if that snapshot was acquired: the columns
  exist but are not leakage-safe for within-season backtesting, so no adoption number was
  measured against them.
- Every deferred wiring item the SUMMARYs recorded — the `data/build_table.py` news-attach
  block, and the `requirements.in` promotion of `onnxruntime` if a torch candidate adopted.
- The two unverifiable priors: the IJCSS 2025 paper (10.2478/ijcss-2025-0008), still
  `[ASSUMED]`, and the reminder that `mlpremier`'s Guardian-sentiment finding was negative.
- Any Colab-trained candidate measured on one season rather than six, with the unit cost of
  extending it.
- The structural fact worth carrying forward: the EXP-1 benchmark's only two scoreable seasons
  are 2021-22 and 2022-23, in which the availability family has no coverage at all — so the
  D-02 trigger measured whether the ranking gap is still open, never what closed it. Any future
  attempt on that gap needs a benchmark that overlaps the covered seasons.

Then resolve the eight todos. For each file in `.planning/todos/pending/` tagged
`resolves_phase: 10`, record its resolution in the audit's evidence column by name, so the
phase's own scope closes against the same eight items it opened with and nothing is left
ambiguous.

Edit `IMPROVEMENTS.md` by scoped insertion only. Do not reflow or rewrite Phase A-F content.
  </action>
  <verify>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import pathlib,sys; t=pathlib.Path('IMPROVEMENTS.md').read_text(); need=('### Final combined run (plan 10-16','### Decisions audit (plan 10-16','### What this phase did not resolve','split verdict','2,262','2,280','10.2478/ijcss-2025-0008','mlpremier'); miss=[k for k in need if k not in t]; sys.exit('close-out missing: '+str(miss)) if miss else print('all three close-out sections present')"</automated>
    <fails_when>non-zero exit, or the message `close-out missing:` listing an absent subsection heading or required token</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import pathlib,re,sys; t=pathlib.Path('IMPROVEMENTS.md').read_text(); s=t.split('### Decisions audit (plan 10-16')[1].split('###')[0]; miss=[f'D-{i:02d}' for i in range(1,22) if f'D-{i:02d}' not in s]; sys.exit('decisions audit missing rows for: '+str(miss)) if miss else print('all 21 decisions audited')"</automated>
    <fails_when>non-zero exit, or the message `decisions audit missing rows for:` listing any D-NN between D-01 and D-21 absent from the audit table</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import pathlib,sys,glob; t=pathlib.Path('IMPROVEMENTS.md').read_text(); s=t.split('## Phase G')[1]; todos=[pathlib.Path(p).stem for p in glob.glob('.planning/todos/pending/*.md')]; miss=[k for k in ('availability-flags','transfermarkt-injury','model-class-bracket','news-sentiment','top100-consensus','fplreview-scoreboard','manual-fbref','rl-reward-shaping') if k not in s]; sys.exit('Phase G never names these todos: '+str(miss)) if miss else print('all eight todos named in the Phase G record')"</automated>
    <fails_when>non-zero exit, or the message `Phase G never names these todos:` listing a `resolves_phase: 10` todo with no recorded resolution</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import pathlib,sys; t=pathlib.Path('IMPROVEMENTS.md').read_text(); s=t.split('### Experiment results (Phase 10)')[1].split('###')[0]; sys.exit(f'{s.count(chr(112)+chr(101)+chr(110)+chr(100)+chr(105)+chr(110)+chr(103))} unfilled cells remain') if 'pend'+'ing' in s else print('results table fully filled')"</automated>
    <fails_when>non-zero exit, or a message reporting unfilled cells remaining in the Phase 10 results table</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import pathlib,sys; t=pathlib.Path('IMPROVEMENTS.md').read_text(); lost=[h for h in ('## Phase A','## Phase F','### Decisions audit (plan 09-10, D-01 through D-16)','### What this phase did not resolve','## Reference findings') if h not in t]; sys.exit('lost prior anchors: '+str(lost)) if lost else print('all prior anchors intact')"</automated>
    <fails_when>non-zero exit, or the message `lost prior anchors:` naming a Phase A-F heading this edit destroyed — note Phase 9's own `### What this phase did not resolve` must survive alongside this phase's</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m pytest -q</automated>
    <fails_when>non-zero exit, or the summary line reports any failed or errored test</fails_when>
  </verify>
  <acceptance_criteria>
    - All three close-out subsections exist under `## Phase G`
    - The decisions audit has a row for every one of D-01 through D-21, each with an outcome and an evidence citation
    - The combined-run entry states which flags were included, that `availability_flags` was held out per D-11, and the shipped-default before/after
    - `### What this phase did not resolve` names every not-acquirable or declined source, every deferred wiring item, both unverifiable priors, any single-season Colab measurement, and the benchmark-coverage structural fact
    - All eight `resolves_phase: 10` todos are named in the Phase G record with a resolution
    - The Phase 10 results table has no unfilled cell
    - All Phase A-F anchors intact, including Phase 9's own decisions audit and did-not-resolve sections
  </acceptance_criteria>
  <done>Every decision from D-01 to D-21 has a recorded outcome with evidence, all eight todos are resolved by name, the combined run is reported under D-11's split rule, and the phase's residue is written down rather than left implicit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ledger verdicts -> the shipped `config.py` defaults | this is where measurement becomes production behaviour |
| an adopted flag -> `web/data/*.json` -> the public site | anything that reaches this payload is served to users |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-16-01 | Repudiation | mixing a one-season signal into a six-season average and reporting one number | high | mitigate | D-11's split rule is enforced by a verify asserting `availability_flags` is absent from the combined run's flag set, whatever its own verdict was |
| T-10-16-02 | Tampering | a default flipped without its pre-declared criterion being met | critical | mitigate | the adopting set is derived from the recorded ledger verdicts; every flipped flag must be named in the updated `test_experiments_registry_default_off`, and the verify fails on any flag flipped in `config.py` that the test does not name |
| T-10-16-03 | Tampering | flipping `availability_flags` without D-10's safe fallback proven | critical | mitigate | the fallback test is run as this task's first verify and the action forbids the flip without it |
| T-10-16-04 | Tampering | a training-time feature key leaking into the public product contract | high | mitigate | a verify greps every `web/data/*.json` payload for `av_`/`tm_`/`nw_`/`bracket_` keys and fails on any hit; `tests/test_product.py` locks it as a regression |
| T-10-16-05 | Repudiation | an adopted flag that is silently inert | high | mitigate | a real `python -m predict.export` run plus a `git diff --stat web/data` check: an adoption whose exported values do not move is reported as an inert flag, not an adoption |
| T-10-16-06 | Repudiation | a decision or todo left with no recorded outcome | high | mitigate | verifies assert a row for every D-01..D-21 and a named resolution for every one of the eight `resolves_phase: 10` todos, plus a zero unfilled-cell count in the results table |
| T-10-16-07 | Tampering | a scoped ledger edit destroying a prior phase's record | high | mitigate | a verify asserts every Phase A-F anchor still exists, including Phase 9's own decisions-audit and did-not-resolve headings |
</threat_model>

<verification>
- `python -m pytest` green repo-wide
- `wf_combined_phase10.json` at 5 replicas over 6 seasons, without `availability_flags`
- Every `config.EXPERIMENTS` flag now `True` is named in `test_experiments_registry_default_off`
- D-10's fallback test passing before any availability flip
- No `av_`/`tm_`/`nw_`/`bracket_` key in any `web/data/*.json` payload
- A real `python -m predict.export` run completed and its `web/data` diff classified
- All 21 decisions audited; all 8 todos named with a resolution; zero unfilled results cells
- All Phase A-F anchors intact
</verification>

<success_criteria>
- The combined run follows D-11's split rule with the partial-coverage arm held out
- Every default change lands in one reviewable commit whose registry test names each flipped flag, and every unmet criterion leaves its flag False
- The product contract is proven key-stable by a real export run, and any adoption is proven to have moved the values
- Every D-01..D-21 decision and every one of the eight todos has a recorded outcome, and the phase's residue is written down
</success_criteria>

<output>
Create `.planning/phases/10-xp-experiment-follow-ups/10-16-SUMMARY.md` when done. It MUST
record: the combined run's `model+chips` against 2,262 and 2,280 with the exact flag set
included; the exact set of defaults flipped (or the empty set) and, for each, the criterion it
met; the D-10 fallback-test result; the `git diff --stat web/data` classification; the final
Colab unit balance out of 200; and the complete list of deferred items carried out of this phase.
</output>
