---
phase: 10-xp-experiment-follow-ups
plan: 05
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - data/transfermarkt.py
  - tests/test_transfermarkt.py
  - IMPROVEMENTS.md
autonomous: false
requirements:
  - TODO-TM-INJURY
user_setup: []

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Whether Transfermarkt injury pages are actually reachable from this machine is a MEASURED fact recorded in IMPROVEMENTS.md, not an assumption -- pass or fail."
    - "The figshare pre-scraped injury dataset was checked for EPL coverage, season range and license before any scraper code was written."
    - "No 6-season fetcher infrastructure is built until access is confirmed by a real probe -- the Phase 9 fbref_v2 discipline, applied before the spend rather than after."
    - "If access is blocked, the outcome is recorded as `not acquirable` with evidence, exactly like fbref_v2, and the phase moves on without an abandoned half-built fetcher."
  artifacts:
    - "data/transfermarkt.py — probe + parser, or the recorded not-acquirable stub"
    - tests/test_transfermarkt.py
    - "IMPROVEMENTS.md — Transfermarkt access-risk entry in the Phase 10 provenance table"
  key_links:
    - "IMPROVEMENTS.md's Phase 10 provenance table (created by plan 10-01 Task 3) -> this plan's measured Transfermarkt access row"
    - "the go/no-go checkpoint -> plan 10-07 (the full backfill), which must not start on an unconfirmed source"
    - "data/id_crosswalk.resolve_by_name -> tm_player_id resolution (one shared matcher, never a second)"
---

<objective>
Resolve the single biggest unknown in Tier 1 before spending any effort on it: is
Transfermarkt's injury-history page actually reachable from this machine with plain `requests`?
10-RESEARCH.md's evidence is genuinely contradictory (some 2026 guides say plain `requests` +
a User-Agent works; others describe DataDome JA3/HTTP-2 fingerprinting that blocks bare
`requests`), and `worldfootballR` — the reference implementation the todo cites — has been
archived read-only since 2025-09-18.

Purpose: Phase 9 spent three real Chrome spike attempts on FBref before recording it as
not-acquirable. That cost was paid *after* deciding the source was worth building for. D-06
asks for an all-seasons 2016-17+ backfill measured in weeks of rate-limited wall clock. Paying
a bounded probe first is the whole lesson of that Phase 9 entry.

Output: a figshare shortcut check, a real 8-page probe with per-page verdicts, a
`checkpoint:decision` that routes plan 10-07 to either the full backfill or a recorded
not-acquirable verdict, and either the working parser or the honest failure evidence in
IMPROVEMENTS.md.

This plan deliberately builds NO caching, NO throttled bulk loop, NO normalization and NO join.
Those are plan 10-07's, and only if this plan's checkpoint says go.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-xp-experiment-follow-ups/10-CONTEXT.md
@.planning/phases/10-xp-experiment-follow-ups/10-RESEARCH.md
@.planning/phases/10-xp-experiment-follow-ups/10-PATTERNS.md
@.planning/todos/pending/2026-09-10-transfermarkt-injury-history.md
</context>

## Artifacts this phase produces

New symbols this plan creates (exclude from drift verification): module
`data/transfermarkt.py` with `TRANSFERMARKT_ENABLED`, `HEADERS`, `TIMEOUT`,
`_MIN_INTERVAL_S`, `_RAW_DIR`, `_OUT`, `_throttle`, `_require`, `_fetch_page`,
`parse_injury_table`, `probe`, `main`, and the `--probe`/`--figshare-check` flags;
`tests/test_transfermarkt.py` with `test_parse_injury_table_on_saved_fixture`,
`test_parse_injury_table_raises_on_challenge_page`,
`test_probe_records_verdict_per_page_without_raising`.

<tasks>

<task type="auto">
  <name>Task 1: Check the figshare pre-scraped dataset before writing any scraper</name>
  <files>data/transfermarkt.py</files>
  <read_first>
    - .planning/phases/10-xp-experiment-follow-ups/10-RESEARCH.md — Alternatives Considered (the figshare "Injuries from Transfermarkt.com" dataset: ~107k injuries, ~18,500 players, multi-league per search summaries; 403'd on direct fetch this session), Open Question 2, and Assumptions Log A5
    - data/external/README.md — the license posture this project applies to a vendored third-party dataset ("no explicit licence, treated as a small research snapshot with attribution"), the standard any figshare dataset would have to clear
    - data/fotmob.py lines 59-72 and 114-169 — the module constants, `_throttle()`, and the `_fetch` helper that returns `None` on `requests.RequestException` with a module-tagged print
  </read_first>
  <action>
Create `data/transfermarkt.py` with the module constants first, following `data/fotmob.py`'s
header shape: `TRANSFERMARKT_ENABLED = True`, a realistic browser `HEADERS` dict (a current
Chrome `User-Agent`, `Accept`, `Accept-Language`), `TIMEOUT = 30`,
`_MIN_INTERVAL_S = 3.0` (deliberately more conservative than fotmob's 1.5 — this is an
unofficial, anti-bot-protected target and D-06 accepts rate-limit-bound wall clock),
`_RAW_DIR = config.RAW_DIR / "transfermarkt"`, `_OUT = config.PROCESSED_DIR / "transfermarkt.parquet"`.
Add `_throttle()` copied in shape from `data/fotmob.py` lines 114-121.

The module docstring states the spike-first contract explicitly: this module is built in two
plans, this one probes access and parses one page shape, and 10-07 adds the bulk backfill only
if this plan's checkpoint said go. That way a reader who finds a probe-only module knows it is
deliberate, not abandoned.

Then implement `--figshare-check` on `main()` — a bounded, ~10-minute check that could remove
the entire scraper need (10-RESEARCH.md Open Question 2). It:
1. Searches the figshare public API for the dataset by title
   (`https://api.figshare.com/v2/articles/search` with a POST body
   `{"search_for": "Injuries from Transfermarkt"}`), which is keyless.
2. For each hit, prints the article id, title, license name, publication date, and the file
   list with sizes.
3. If a downloadable file is reachable, fetches only its first ~64KB and prints the header row
   and the first two data rows, so the column set and the league/season coverage are visible
   without a full download.
4. Prints an explicit verdict line in one of three forms:
   `[transfermarkt] figshare: USABLE — covers EPL {seasons}, license {name}`,
   `[transfermarkt] figshare: INSUFFICIENT — {reason}`, or
   `[transfermarkt] figshare: UNREACHABLE — {status/exception}`.

Catch `requests.RequestException` and print the UNREACHABLE verdict rather than raising — a
403 here is an expected outcome (it is what this research session got), not a task failure. The
whole point is to record which of the three verdicts is true.
  </action>
  <verify>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m data.transfermarkt --figshare-check</automated>
    <fails_when>non-zero exit, or the output contains none of the three literal verdict tokens `USABLE`, `INSUFFICIENT`, `UNREACHABLE`</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import data.transfermarkt as t; assert t.TRANSFERMARKT_ENABLED is True; assert t._MIN_INTERVAL_S >= 3.0, t._MIN_INTERVAL_S; assert 'Chrome' in t.HEADERS['User-Agent'], t.HEADERS; print('constants ok', t._MIN_INTERVAL_S)"</automated>
    <fails_when>non-zero exit, or an AssertionError on the kill switch, on a throttle interval under 3.0 seconds, or on a User-Agent that does not identify as a browser</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/ruff check .</automated>
    <fails_when>non-zero exit, or any diagnostic naming data/transfermarkt.py</fails_when>
  </verify>
  <acceptance_criteria>
    - `data/transfermarkt.py` exists with all the named module constants and `_throttle`
    - The module docstring states the two-plan spike-first contract
    - `--figshare-check` runs to completion and prints exactly one of the three verdict tokens
    - The SUMMARY records the verdict verbatim, plus the license name and column header row if any file was reachable
    - No caching loop, no bulk fetch and no normalization exists in the file yet: `grep -c "def build" data/transfermarkt.py` returns `0`
  </acceptance_criteria>
  <done>The figshare shortcut is checked and its verdict recorded, so the decision to build a scraper is made against evidence rather than in ignorance of a possible substitute.</done>
</task>

<task type="auto">
  <name>Task 2: Bounded real-page access probe and one-page parser</name>
  <files>data/transfermarkt.py, tests/test_transfermarkt.py</files>
  <read_first>
    - data/transfermarkt.py — as written by Task 1
    - data/id_crosswalk.py — `resolve_by_name`'s three tiers and `_NAME_FIXUPS` (lines 69-158); this is the ONE shared name matcher, and Transfermarkt's name-to-`tm_player_id` step must go through it, never a second dict (the FBref many-to-many join corruption is the lesson)
    - data/fotmob.py lines 124-169 — `_require(payload, path, expected_type, what)` and the cache-then-fetch helper's exception handling
    - IMPROVEMENTS.md lines 653-688 — the `### fbref_v2: real Chrome spike — access still blocked` entry: what "not acquirable" evidence looks like when recorded honestly (attempt count, duration, control-site comparison, the observed challenge-page text)
    - .planning/phases/10-xp-experiment-follow-ups/10-RESEARCH.md — Pitfall 1 (spike 5-10 pages with plain requests; warning signs are a 403/429 on the first real page or a DataDome/Cloudflare challenge body) and Assumptions Log A4 (the `/verletzungen/spieler/{id}` URL pattern and its column set are unverified)
  </read_first>
  <action>
Add `_fetch_page(url, *, what) -> str | None` — `_throttle()`, then `requests.get` with
`HEADERS`/`TIMEOUT`, `raise_for_status()`, returning `resp.text`. Catch
`requests.RequestException` and return `None` after a
`[transfermarkt] fetch failed for {what}: {exc}` print. Cache each fetched page under
`_RAW_DIR` as a `.html` file keyed by a slug of the URL, so a probe is re-analysable offline
and re-running the probe does not re-hit the site.

Add `parse_injury_table(html, *, what) -> pd.DataFrame` returning columns
`season`, `injury`, `from_date`, `until_date`, `days_out`, `games_missed`. Use
`pandas.read_html` on the page's injury table (no BeautifulSoup dependency needed — pandas
already carries an HTML parser and this project has no soup dependency). Before parsing,
detect an anti-bot interstitial explicitly: if the body contains any of
`Just a moment`, `DataDome`, `captcha-delivery`, or `Attention Required`, raise a `ValueError`
whose message names the detected marker. That is the T-10-05-01 mitigation — a challenge page
must never be parsed into an empty-but-valid frame that then reads downstream as "this player
had no injuries", which is the exact failure mode that would silently poison the whole
experiment.

Add `probe(n=8) -> list[dict]`. It selects `n` real players deterministically — take the
highest-`selected_by_percent` players from the most recent `data/snapshots/*.parquet`, resolve
each name through `data.id_crosswalk.resolve_by_name` to get its `player_code`, and derive the
Transfermarkt profile URL for each. Because Transfermarkt is keyed by its own player id and we
have no crosswalk to it yet, resolve the id in this probe via Transfermarkt's own public search
URL (`/schnellsuche/ergebnis/schnellsuche?query={name}`) and take the first player hit,
recording the resolved `(name, tm_player_id)` pair per probe row so a human can eyeball whether
the match is the right footballer. Do NOT build a persistent id cache here — that is 10-07's,
and only if the checkpoint says go.

For each of the `n` players, `probe` records a dict with `name`, `tm_player_id`, `url`,
`http_status_or_exception`, `bytes`, `challenge_marker_detected`, `rows_parsed`, and
`verdict` in `{"ok", "challenge", "http_error", "parse_error", "id_unresolved"}`. It never
raises — a total block must produce eight `challenge` verdicts and a clean return, because the
whole value of the probe is the verdict table. Print a summary line
`[transfermarkt] probe: {ok}/{n} pages parsed, {challenge} challenged, {errors} errored` and
write the full table to `config.EXPERIMENTS_DIR / "transfermarkt_probe.json"` via
`ops.jsonio.write_json`.

Expose `--probe` (with an `--n` defaulting to 8) on `main()`.

Create `tests/test_transfermarkt.py` with the three named tests, all offline: save one probe
response as a small committed-free fixture under `tmp_path` in the test itself (a minimal HTML
table string, not a real captured page), assert `parse_injury_table` returns the six columns;
assert it raises `ValueError` naming the marker on a body containing `Just a moment`; and assert
`probe` returns one dict per requested page with a `verdict` key when `_fetch_page` is
monkeypatched to return `None` for every call (the total-block case must not raise).
  </action>
  <verify>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m pytest tests/test_transfermarkt.py -q</automated>
    <fails_when>non-zero exit, or the summary line reports any failed or errored test, or `no tests ran`</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m data.transfermarkt --probe --n 8</automated>
    <fails_when>non-zero exit — the probe must complete and report verdicts even when every page is blocked; a raise here means the never-raise contract was not implemented</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import json,config; s=json.loads((config.EXPERIMENTS_DIR/'transfermarkt_probe.json').read_text()); rows=s if isinstance(s,list) else s['rows']; assert len(rows)==8, len(rows); need={'name','tm_player_id','url','verdict','rows_parsed','challenge_marker_detected'}; miss=[k for k in need if k not in rows[0]]; assert not miss, miss; from collections import Counter; print(Counter(r['verdict'] for r in rows))"</automated>
    <fails_when>non-zero exit, a FileNotFoundError on the probe JSON, or an AssertionError naming a row count other than 8 or a missing verdict field</fails_when>
    <automated>grep -c "_NAME_FIXUPS\|resolve_by_name" data/transfermarkt.py</automated>
    <fails_when>output is `0` — identity resolution must go through `data.id_crosswalk`, and a zero here means a second name matcher was grown instead</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/ruff check .</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `parse_injury_table` returns exactly `season`, `injury`, `from_date`, `until_date`, `days_out`, `games_missed`
    - `parse_injury_table` raises `ValueError` naming the detected marker on a body containing any of `Just a moment`, `DataDome`, `captcha-delivery`, `Attention Required`
    - `probe` returns `n` dicts and never raises, proven by the monkeypatched total-block test
    - `data/processed/experiments/transfermarkt_probe.json` exists with 8 rows carrying the six named fields
    - Every fetched page is cached under `data/raw/transfermarkt/` so the probe is re-analysable offline
    - `data/transfermarkt.py` still has no `build()`, no bulk loop and no `attach()`
    - The SUMMARY records the verdict counter, the per-page HTTP statuses, and — if any page parsed — the verbatim column headers Transfermarkt actually served (A4 confirmation or correction)
  </acceptance_criteria>
  <done>A real 8-page probe has run against transfermarkt.com from this machine, every page has a recorded verdict, an anti-bot interstitial provably raises instead of parsing to an empty frame, and the evidence is on disk.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 3: Go / no-go on the Transfermarkt backfill</name>
  <decision>Whether plan 10-07 builds the Transfermarkt injury backfill, and at what season scope</decision>
  <context>
Two bounded checks have now run — the figshare pre-scraped-dataset probe and a real 8-page
access probe against transfermarkt.com. D-06 asks for an all-seasons 2016-17+ backfill measured
in weeks of rate-limit-bound wall clock. Phase 9's FBref item is the direct precedent for what
happens when a source's access is assumed rather than measured: three real Chrome spike attempts
before it was recorded as not acquirable. This decision spends that evidence before the effort
rather than after.
  </context>
  <options>
    <option id="A-full-backfill">
      <name>Full backfill (D-06 as written)</name>
      <pros>All training seasons 2016-17 onward — the whole point of this source is reaching where FPL flags cannot; a background killable job means the wall clock costs attention, not blocking time</pros>
      <cons>The projected wall clock computed in this checkpoint, paid up front; only available if the probe parsed cleanly</cons>
    </option>
    <option id="B-reduced-seasons">
      <name>Reduced-season backfill</name>
      <pros>Same fetcher, a fraction of the wall clock; the recent seasons are where the defensive-contribution scoring rules that make availability valuable actually applied</pros>
      <cons>Weaker A/B — fewer seasons means a noisier estimate against a harness whose season-to-season SE is already ~50 points</cons>
    </option>
    <option id="C-figshare-substitute">
      <name>figshare substitute</name>
      <pros>Zero scraping infrastructure, minutes instead of hours, no anti-bot risk at all</pros>
      <cons>Only available if the figshare verdict was USABLE; coverage and license are the vendor's, not ours to extend</cons>
    </option>
    <option id="D-not-acquirable">
      <name>Not acquirable — record it like fbref_v2</name>
      <pros>Honest, cheap, and precedented: evidence in IMPROVEMENTS.md, no fetcher built, no half-built module left behind, phase moves straight to Tier 2</pros>
      <cons>The one P(play) signal that reaches every training season stays unmeasured</cons>
    </option>
  </options>
  <resume-signal>Select A, B (naming the seasons), C, or D</resume-signal>
  <action>
Two bounded checks have now run. Their results decide whether plan 10-07 builds a 6-season
backfill measured in weeks of rate-limited wall clock, or whether this source is recorded as
not acquirable and the phase moves on.

Present, from the SUMMARY and the two artifacts
(`data/processed/experiments/transfermarkt_probe.json`, the `--figshare-check` output):

1. The figshare verdict (`USABLE` / `INSUFFICIENT` / `UNREACHABLE`) with license and coverage
   if any was readable.
2. The probe verdict counter — how many of 8 pages parsed, how many were challenged, how many
   errored, how many had an unresolvable Transfermarkt id.
3. If any page parsed: the verbatim column headers served, so the reality can be compared
   against 10-RESEARCH.md's assumed `(season, injury type, from, until, days, games missed)`
   shape (Assumptions Log A4).
4. The observed per-page wall clock, and the implied total for D-06's full scope. Compute it
   explicitly: distinct EPL players across seasons 2016-17 onward as counted from
   `data/processed/player_gw.parquet`, times one profile page each, at
   `_MIN_INTERVAL_S = 3.0` seconds — state that figure in hours.

Then ask for one of four decisions:

- **A) Full backfill (D-06 as written)** — all seasons 2016-17+, background killable job.
  Choose this if the probe parsed cleanly and the projected wall clock is acceptable.
- **B) Reduced-season backfill** — the same fetcher, but a stated subset of seasons, if the
  projected wall clock is the only objection. Name the seasons.
- **C) figshare substitute** — skip the scraper entirely and vendor the figshare dataset per
  D-07's committed-snapshot pattern. Only available if the figshare verdict was `USABLE`.
- **D) Not acquirable** — record it like `fbref_v2`: evidence in IMPROVEMENTS.md, no fetcher
  built, `transfermarkt_injury` never registered, phase continues to Tier 2.

Record the verbatim answer. Whatever it is, write the corresponding row into
`IMPROVEMENTS.md`'s `### Data provenance and access risk (Phase 10)` table (created by plan
10-01): the measured access status, the evidence, and the decision taken. On decision D, also
add a `### transfermarkt_injury: access probe — not acquirable` subsection under `## Phase G`
in the same shape as Phase F's `### fbref_v2` entry, and set the `transfermarkt_injury` row in
the Phase 10 results table to `not acquirable` / `off` with the probe evidence in the
`measured` cell.
  </action>
  <verify>
    <human-check>A verbatim decision (A, B, C or D) is recorded in the checkpoint response, and IMPROVEMENTS.md's Phase 10 provenance table now carries a Transfermarkt row stating the measured access status rather than "unverified"</human-check>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import pathlib,sys; t=pathlib.Path('IMPROVEMENTS.md').read_text(); s=t.split('### Data provenance and access risk (Phase 10)')[1][:4000]; sys.exit('Transfermarkt row still says unverified or is absent') if ('ransfermarkt' not in s or 'unverified' in s.lower()) else print('provenance row updated')"</automated>
    <fails_when>non-zero exit, or the message `Transfermarkt row still says unverified or is absent`</fails_when>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records the verbatim decision letter and the developer's own wording
    - IMPROVEMENTS.md's Phase 10 provenance table's Transfermarkt row states a measured access status (not "unverified") with the probe evidence
    - The SUMMARY states the computed projected backfill wall clock in hours and the distinct-player count it was derived from
    - On decision D only: a `### transfermarkt_injury: access probe — not acquirable` subsection exists under `## Phase G`, and the results table row reads `not acquirable` / `off`
    - On decisions A, B or C: the SUMMARY names the exact scope plan 10-07 is authorised to build (which seasons, or the figshare substitute)
  </acceptance_criteria>
  <done>The developer has decided, against measured evidence and a computed wall-clock projection, whether plan 10-07 builds the backfill and at what scope — and the decision plus its evidence are in the permanent ledger.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| transfermarkt.com HTML -> `pandas.read_html` -> an injury frame | untrusted, anti-bot-protected, undocumented third-party markup crosses here |
| Transfermarkt search results -> `tm_player_id` | a wrong id silently attributes another footballer's injury history to one of our players |
| this machine's IP -> transfermarkt.com | our own request pattern crosses out; being blocked is a real outcome |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-05-01 | Tampering | an anti-bot challenge page parsed as an empty injury table | critical | mitigate | `parse_injury_table` scans for `Just a moment`, `DataDome`, `captcha-delivery`, `Attention Required` and raises `ValueError` naming the marker before any parse. An empty-but-valid frame would read downstream as "never injured" and would poison every measured verdict in Tier 1 |
| T-10-05-02 | Tampering | wrong-footballer id from the search endpoint | high | mitigate | the probe records the resolved `(name, tm_player_id)` pair per row for human eyeball at the checkpoint, and name resolution goes through `data.id_crosswalk.resolve_by_name` only — never a second matcher (the FBref many-to-many corruption precedent) |
| T-10-05-03 | Tampering | schema drift in the injury table's columns | high | mitigate | `parse_injury_table` returns a fixed six-column contract; the checkpoint requires the verbatim served headers to be compared against 10-RESEARCH.md's assumed shape (A4), so a mismatch is caught before 10-07 builds on it |
| T-10-05-04 | Denial of Service | our request rate getting this IP blocked | medium | mitigate | `_MIN_INTERVAL_S = 3.0` (double `data/fotmob.py`'s), the probe is capped at 8 pages, and every page is cached under `data/raw/transfermarkt/` so re-analysis never re-requests |
| T-10-05-05 | Legal/compliance | committing scraped content whose ToS forbids redistribution | high | mitigate | this plan commits no scraped page (`data/raw/` is gitignored); the D-07 committed artifact is a derived, normalized spell table decided in 10-07, and the checkpoint records the license posture explicitly |
| T-10-05-06 | Repudiation | building a fetcher on an unconfirmed source and calling the result a measurement | high | mitigate | the go/no-go checkpoint is blocking-human and 10-07 depends on this plan; decision D produces a recorded `not acquirable` verdict rather than an abandoned half-built module |
</threat_model>

<verification>
- `python -m pytest tests/test_transfermarkt.py` green, all offline
- `ruff check .` clean
- `data/processed/experiments/transfermarkt_probe.json` exists with 8 verdict rows
- The `--figshare-check` verdict recorded verbatim in the SUMMARY
- `data/transfermarkt.py` has no `build()`, no bulk loop, no `attach()`
- IMPROVEMENTS.md's Phase 10 provenance table's Transfermarkt row states a measured status
</verification>

<success_criteria>
- Access is a measured fact with per-page evidence, not an assumption
- The figshare shortcut was checked before any scraper effort
- An anti-bot page provably raises rather than parsing to a misleading empty frame
- A blocking-human decision, made against the evidence and a computed wall-clock projection, authorises or cancels plan 10-07's scope
</success_criteria>

<output>
Create `.planning/phases/10-xp-experiment-follow-ups/10-05-SUMMARY.md` when done. It MUST
record: the figshare verdict verbatim, the 8-page probe verdict counter and per-page HTTP
statuses, the verbatim served column headers if any page parsed, the computed projected
backfill wall clock in hours with the player count it came from, and the developer's verbatim
go/no-go decision.
</output>
