---
phase: 08-self-hosted-gameweek-data-capture
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - .planning/phases/08-self-hosted-gameweek-data-capture/08-BACKFILL-EVIDENCE.md
autonomous: true
requirements: []

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every gameweek of the current season that the official API reports as finished and data-checked is captured on disk, including the two the previous source never published — the reason this phase exists"
    - "The self-built gameweek-1 rows agree with the same gameweek as published by the previous source, key for key and column for column, or every disagreement is enumerated with a stated cause"
    - "`python -m data.build_table` produces current-season rows with position, fixture-difficulty and set-piece coverage, with no change to `data/build_table.py` or `data/id_map.py`"
    - "The ten already-cached past seasons come through the rebuild with identical row counts — the capture is additive and nothing regressed"
    - "The measured expected-points null rate per captured gameweek is recorded as a fact, so a future retrain reads early-gameweek nulls as a known gap rather than a join bug"
  prohibitions:
    - category: integrity
      status: unresolved
      verification: judgment
      statement: "MUST NOT edit, patch, hand-correct or selectively drop rows from a captured CSV to make the cross-check agree — a disagreement is a finding to record and, if the fault is ours, a code fix in data/gw_capture.py, never a data edit"
    - category: integrity
      status: unresolved
      verification: behavior
      statement: "MUST NOT fill the expected-points column for gameweeks 1 and 2 from the previously-published file, from a later snapshot, or from any interpolation — the null rate is a measured fact this plan records"
    - category: safety
      status: unresolved
      verification: behavior
      statement: "MUST NOT run predict.export, predict.digest, predict.scoreboard, scripts/daily.sh or scripts/weekly.sh — nothing in this plan may write anything under web/, which is frozen until Phase 7 CUT-01 completes"
    - category: safety
      status: unresolved
      verification: behavior
      statement: "MUST NOT rebuild features.parquet or retrain any model — the current season is excluded from the configured training and test seasons, so the canonical table gaining rows changes no model input, and a retrain here would be an unrequested change to a validated artifact"
    - category: safety
      status: unresolved
      verification: judgment
      statement: "MUST NOT modify data/build_table.py or data/id_map.py — reading the captured rows unchanged is the claim this plan verifies, and an edit there invalidates the verification"
  artifacts:
    - path: ".planning/phases/08-self-hosted-gameweek-data-capture/08-BACKFILL-EVIDENCE.md"
      provides: "The recorded evidence: per-gameweek captured row counts, the gameweek-1 cross-check result, the measured expected-points null rate, and the before-and-after per-season row counts of the canonical table"
      contains: "cross-check"
  key_links:
    - from: "data/gw_capture.py"
      to: "data/processed/player_gw.parquet"
      via: "the captured merged_gw.csv read by build_table's unmodified loader, after a rebuilt id_map"
      pattern: "build_table"
    - from: ".planning/phases/08-self-hosted-gameweek-data-capture/08-BACKFILL-EVIDENCE.md"
      to: "data/gw_capture.py"
      via: "the cross-check verdict either confirms the schema mapping or names the column that has to change"
      pattern: "gw_capture"
---

<objective>
Run the capture for real against the live API, prove the self-built rows against the previously
published source, and prove the canonical table ingests them.

Purpose: this is the task the phase was created for. The previous source stalled at gameweek 1 and
the per-player history endpoint retains only the current season, so the two missing gameweeks are
retrievable now and not after the season rolls over. Everything before this plan was infrastructure;
this is the data.

Output: real captured files under `data/raw/{CURRENT_SEASON}/`, a rebuilt canonical table carrying
current-season rows, and a committed evidence document recording what was captured, how it compared
against the published gameweek 1, and what the measured gaps are.

This plan contains no tracer task. The phase's tracer is 08-01 Task 1; these tasks exercise the
already-proven path against real data.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-self-hosted-gameweek-data-capture/08-RESEARCH.md
@.planning/phases/08-self-hosted-gameweek-data-capture/08-01-SUMMARY.md
@.planning/phases/08-self-hosted-gameweek-data-capture/08-02-SUMMARY.md
</context>

## Artifacts this phase produces

See the table in `08-01-PLAN.md` under "Artifacts this phase produces" — the single authoritative
list for Phase 8. This plan creates one row from it:
`.planning/phases/08-self-hosted-gameweek-data-capture/08-BACKFILL-EVIDENCE.md`. It creates no new
code symbols.

## API Coverage Matrix

See the matrix in `08-01-PLAN.md`. This plan adds no new endpoint; it is the first execution of the
already-covered ones against the live API rather than a mocked one.

## Measured baseline this plan is judged against

Read on this machine on 2026-09-11, before any capture. An executor finding different numbers must
record the observed ones and say so, not silently re-baseline.

| Fact | Value |
|---|---|
| Current-season rows in `data/processed/player_gw.parquet` | zero — no current-season row exists yet |
| Per-season row counts in that table | 2016-17 23679, 2017-18 22467, 2018-19 21790, 2019-20 22501, 2020-21 24365, 2021-22 25447, 2022-23 26505, 2023-24 29725, 2024-25 27283, 2025-26 29747 |
| Total rows | 253509 across ten seasons |
| Current-season files on disk | `fixtures.csv` and `players_raw.csv`, both dated 2026-08-21 and pre-season stale; no `merged_gw.csv` at all |
| Finished and data-checked gameweeks | 1, 2 and 3 |
| Elements in the live bootstrap | 656 |
| Previously-published current-season files | the gameweek-1 file returns 200 with 610 rows; the merged file returns 200 and contains gameweek 1 only; the gameweek-2 file returns 404 |
| Daily snapshot archive | four files, earliest 2026-08-31 — after both the gameweek-1 and gameweek-2 deadlines |
| Element count in the earliest snapshot | 626, against 656 elements today |
| `data/processed/id_map.parquet` | dated 2026-08-21, built from the pre-season `players_raw.csv` — stale for any element added since |

<tasks>

<task type="auto">
  <name>Task 1: Capture every finished gameweek of the current season for real</name>
  <precondition>The official FPL API is reachable from this host and `element-summary/1/` still returns history rows for round 1 — that endpoint retains only the current season, so if round 1 has already aged out the backfill is no longer possible and this plan must stop and report rather than capture a partial season</precondition>
  <files>.planning/phases/08-self-hosted-gameweek-data-capture/08-BACKFILL-EVIDENCE.md</files>
  <read_first>
    - data/gw_capture.py — the complete module from plans 08-01 and 08-02, in its committed state; read the run summary it prints and its exit-code contract before running it, so the output can be judged rather than merely observed.
    - .planning/phases/08-self-hosted-gameweek-data-capture/08-02-SUMMARY.md — what the per-gameweek resolution line reports and any deviation the executor recorded there.
    - The Measured baseline table above — the numbers this run is compared against.
  </read_first>
  <action>
Run the capture against the live API and record what came back.

Re-establish the live facts first, because the baseline table above is a day old and the completion
state moves: read the bootstrap's events and note which gameweeks report both finished and
data-checked, the element count, and the next gameweek's deadline. If a fourth gameweek has since
settled it is in scope — the target set is whatever the API reports now, not the three the baseline
names.

Then run the module with no flags. A plain run captures every finished, data-checked gameweek that
has no ledger file, which on a first run is all of them. The sweep is one request per element with a
delay between them, so it takes a few minutes; let it finish rather than interrupting it, because a
partial sweep still writes a ledger file that a later run would treat as complete.

Read the run's own output, not only its exit code. Note the per-gameweek row counts, the per-gameweek
expected-points resolution line including which snapshot date was used and the resolved fraction, and
the count of players whose individual request failed. A small non-zero failure count is information
rather than a defect, but if more than a handful failed, re-run with the force flag for the affected
gameweeks and record both attempts.

Confirm the outputs by inspection: one ledger file per captured gameweek under the season directory's
`gws` child, a `merged_gw.csv` at the flat season path carrying the union of them, and both mutable
files rewritten with today's timestamp rather than the pre-season one.

Open `08-BACKFILL-EVIDENCE.md` and record the run's UTC timestamp and verbatim command line, the live
event state observed, the per-gameweek captured row counts, the expected-points resolution outcome per
gameweek including the null rate as a percentage, the per-player failure count, and the resulting file
sizes and modification times. Record the null rate for gameweeks 1 and 2 as the documented permanent
gap, naming the reason — their deadlines precede the earliest snapshot — rather than presenting it as
an anomaly.

Run no export, digest or cron script, rebuild no features, and retrain nothing; the canonical-table
rebuild is Task 3's job and nothing in this phase writes under `web/`.
  </action>
  <verify>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import glob, os, pandas as pd, config; d=str(config.RAW_DIR/config.CURRENT_SEASON); led=sorted(os.path.basename(p) for p in glob.glob(d+'/gws/gw*.csv')); m=pd.read_csv(d+'/merged_gw.csv', low_memory=False); print('ledger:', led); print('rows', len(m), 'cols', len(m.columns)); print('per-gw', m.groupby('round').size().to_dict()); print('missing_source_keys', sorted(set(config.MERGED_GW_COLUMNS)-set(m.columns))); print('elements', m['element'].nunique())"</automated>
    <fails_when>non-zero exit, or `ledger:` lists fewer files than the number of finished, data-checked gameweeks the bootstrap reports, or `missing_source_keys` prints a non-empty list, or `per-gw` shows any gameweek with fewer than 400 rows — a gameweek that thin means the sweep died partway and still wrote a ledger file</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import pandas as pd, config, os, datetime as dt; d=config.RAW_DIR/config.CURRENT_SEASON; [print(f.name, len(pd.read_csv(d/f.name, low_memory=False)), dt.datetime.utcfromtimestamp(os.path.getmtime(d/f.name)).date().isoformat()) for f in (d/'players_raw.csv', d/'fixtures.csv')]"</automated>
    <fails_when>non-zero exit, or either printed date is 2026-08-21 or earlier — the two mutable files were not rewritten and the pre-season copies are still authoritative</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m pytest -q</automated>
    <fails_when>non-zero exit, or the summary reports any failure or error</fails_when>
    <automated>git status --porcelain web/ scripts/</automated>
    <fails_when>any output at all — the frozen surfaces must have no modification, and silence is the pass condition</fails_when>
  </verify>
  <acceptance_criteria>
    - One ledger file exists under the season directory's `gws` child for every gameweek the bootstrap reports as both finished and data-checked, and no ledger file exists for any gameweek it does not.
    - `merged_gw.csv` exists at the flat season path, its column count is 46, and no source key of `config.MERGED_GW_COLUMNS` is absent from it.
    - Each captured gameweek's row count is recorded in the evidence document, and each is within 10 percent of the element count the bootstrap reports for the same run.
    - `players_raw.csv` and `fixtures.csv` both carry a modification date from this run, not the pre-season date in the baseline table.
    - The evidence document records the expected-points null rate per captured gameweek, and states for gameweeks 1 and 2 that their deadlines precede the earliest daily snapshot, naming that as the reason.
    - The evidence document records the per-player request failure count from the run, even when it is zero.
    - `git status --porcelain web/ scripts/` prints nothing.
    - `git status --porcelain data/snapshots/` reports no deleted or modified file.
  </acceptance_criteria>
  <done>Every finished, data-checked gameweek of the current season is on disk in the vaastav schema, both mutable files are freshly refreshed, and the run's outputs and gaps are recorded in the evidence document.</done>
</task>

<task type="auto">
  <name>Task 2: Cross-check the self-built gameweek 1 against the previously published one</name>
  <precondition>The previously published current-season gameweek-1 file is still served: a request for it returns 200 rather than 404, and its row count is in the hundreds — if that source has since removed the file, the cross-check oracle is gone and this task records that fact instead of a comparison</precondition>
  <files>.planning/phases/08-self-hosted-gameweek-data-capture/08-BACKFILL-EVIDENCE.md</files>
  <read_first>
    - data/gw_capture.py — specifically `MERGED_GW_HEADER` and `build_gw_frame`, since a disagreement in a stat column points at one specific line of the mapping.
    - data/ingest.py lines 51-73 — the URL shape for the published per-season files, so the cross-check fetch reuses the same base rather than inventing a URL.
    - .planning/phases/08-self-hosted-gameweek-data-capture/08-BACKFILL-EVIDENCE.md — Task 1's recorded run, whose gameweek-1 row count this comparison starts from.
    - .planning/phases/08-self-hosted-gameweek-data-capture/08-RESEARCH.md — the Schema Mapping Table, which is the claim under test here.
  </read_first>
  <action>
This is the only external oracle the phase has: the same gameweek, built by someone else from the
same endpoint. Use it.

Fetch the published gameweek-1 file for the current season into a scratch location outside the
repository — not into `data/raw/`, where it could later be mistaken for captured output. Load it
alongthe captured gameweek-1 ledger file.

Compare on the natural key of an element and a fixture, so a double gameweek is compared row for row
rather than collapsed. Report four things separately: the keys present in both, the keys only in the
published file, the keys only in ours, and for the shared keys, per-column equality across every
column the two files share.

Treat the differences by category, and record each in the evidence document.

Keys only in the published file are the serious case: a player the other source has for gameweek 1
and we do not means the sweep missed an element or the round filter dropped a row. Investigate before
recording, and if the cause is ours, fix `data/gw_capture.py` and re-capture that gameweek with the
force flag rather than annotating the difference away.

Keys only in ours are expected in the other direction and benign — the published file is a snapshot
taken at one moment and the endpoint keeps serving rows for elements registered later. Record the
count and a couple of examples.

Column disagreements: the expected-points column is expected to disagree, because ours is missing for
gameweek 1 by design while the published file carries a real value captured at the time. Record that
explicitly as the known, deliberate difference and exclude it from the equality verdict. The modified
flag is a transient publisher-side field and may differ; record it the same way. Every other shared
column is a real claim about the schema mapping and must match exactly. Any column that does not is a
finding: name the column, the number of disagreeing rows, two concrete examples with both values, and
the diagnosis.

Write the verdict into `08-BACKFILL-EVIDENCE.md` under a clearly labelled cross-check section: the
fetch URL and its response code, both row counts, the four key-set counts, a per-column agreement
table over the shared columns, the two deliberate exclusions with their reasons, and a one-line
conclusion stating whether the schema mapping is confirmed. If any correction to
`data/gw_capture.py` was needed, name the commit and re-state the post-fix numbers.
  </action>
  <verify>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import requests, config; r=requests.get(f'{config.VAASTAV_RAW}/{config.CURRENT_SEASON}/gws/gw1.csv', timeout=30); print('status', r.status_code, 'bytes', len(r.content), 'lines', r.text.count(chr(10)))"</automated>
    <fails_when>non-zero exit, or `status` prints anything other than 200, or `lines` prints fewer than 100 — the oracle file is gone or truncated and the comparison cannot be made</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import pandas as pd, requests, io, config; d=config.RAW_DIR/config.CURRENT_SEASON; ours=pd.read_csv(d/'gws'/'gw1.csv', low_memory=False); theirs=pd.read_csv(io.StringIO(requests.get(f'{config.VAASTAV_RAW}/{config.CURRENT_SEASON}/gws/gw1.csv', timeout=30).text), low_memory=False); k=['element','fixture']; a=set(map(tuple, ours[k].values)); b=set(map(tuple, theirs[k].values)); print('ours_rows', len(ours), 'theirs_rows', len(theirs)); print('both', len(a&b), 'only_theirs', len(b-a), 'only_ours', len(a-b)); shared=sorted((set(ours.columns)&set(theirs.columns))-{'xP','modified'}); mo=ours.set_index(k).sort_index(); mt=theirs.set_index(k).sort_index(); idx=sorted(a&b); bad=[c for c in shared if not mo.loc[idx,c].astype(str).equals(mt.loc[idx,c].astype(str))]; print('shared_cols', len(shared)); print('disagreeing_cols', bad)"</automated>
    <fails_when>non-zero exit, or `only_theirs` prints anything other than 0 — a key the other source has and we do not means the sweep or the round filter lost a row. `disagreeing_cols` printing a non-empty list is a finding the action must diagnose and record, and is only acceptable once the evidence document names each listed column with its cause</fails_when>
  </verify>
  <acceptance_criteria>
    - The evidence document has a section labelled as the gameweek-1 cross-check, carrying the fetch URL, its response code, both row counts, and the three key-set counts.
    - The count of keys present only in the published file is zero, or the evidence document names each such key with a diagnosis and a commit reference for the fix.
    - The per-column agreement table covers every column the two files share, with the expected-points column and the modified flag listed as deliberate exclusions and each carrying its stated reason.
    - Every column outside those two exclusions agrees on all shared keys, or the evidence document names the column, the disagreeing row count, two concrete examples with both values, and the diagnosis.
    - The section ends with a one-line conclusion stating whether the schema mapping is confirmed.
    - The published file was written outside the repository working tree; `git status --porcelain` shows no untracked CSV anywhere under `data/`.
    - No row of any captured CSV was hand-edited: if a correction was needed, `git log --oneline -- data/gw_capture.py` shows the fixing commit and the affected gameweek was re-captured with the force flag.
  </acceptance_criteria>
  <done>The self-built gameweek 1 has been compared key for key and column for column against the independently published one, every difference is categorised and recorded, and the schema-mapping verdict is written down.</done>
</task>

<task type="auto">
  <name>Task 3: Rebuild the canonical table and prove the captured rows arrive with full enrichment</name>
  <precondition>A `merged_gw.csv` exists at the flat current-season path from Task 1, and Task 2's cross-check verdict is recorded — rebuilding the canonical table against rows whose mapping has not been validated would bake an unverified schema into the pipeline's primary artifact</precondition>
  <files>.planning/phases/08-self-hosted-gameweek-data-capture/08-BACKFILL-EVIDENCE.md</files>
  <read_first>
    - data/build_table.py — read in full. `build()`'s order of operations matters here: the season-local key, the stable-code attach through `id_map`, the position backfill from the id map, the set-piece merge, and the optional odds and enrichment joins that each print their own coverage line.
    - data/id_map.py lines 61-87 — `build()` and `load_id_map`'s caching: the cached parquet is returned unless a rebuild is requested, so a stale id map silently leaves every newly-registered element without a stable code.
    - backtest/walk_forward.py around line 46 — the line that drops the current season from the data seasons, which is why this rebuild changes no model input.
    - config.py lines 83-95 — `SEASONS` and `CURRENT_SEASON`, so the expected season list in the rebuilt table is eleven entries rather than ten.
    - The Measured baseline table above — the ten per-season row counts this rebuild must reproduce unchanged.
  </read_first>
  <action>
Prove the claim the whole phase rests on: that the captured rows flow into the canonical table with no
consumer change.

Rebuild the id map first. Its cached parquet is dated before the season started and
`load_id_map` returns the cache rather than rebuilding, so running the table build without this step
would leave every element registered since then without a stable player code — and the resulting
warning line is easy to read past. Run the id-map module directly so it rebuilds, and note the
current-season player count and source it reports.

Then run the table build. Read every line it prints rather than only its exit status: the per-season
row counts, the position coverage after backfill, the odds coverage percentage, and each optional
enrichment join's skip-or-attach line.

Judge four things against the baseline. The current season must now appear with rows, one per player
per played fixture across the captured gameweeks. The ten past seasons must come through with row
counts identical to the baseline table — this rebuild is additive and any movement there is a
regression to stop for. Position coverage for the current season's rows must be complete, since the
captured file carries the column directly rather than relying on the backfill path older seasons
need. Fixture-difficulty must be populated for the current season's rows, which is the check that the
refreshed fixtures file actually carried the difficulty columns through.

The odds coverage line deserves its own look, for the specific reason that it is the one join keyed on
the club name. A current-season coverage near zero while past seasons hold their usual level is the
signature of a club-name mismatch, which is the failure mode the club-name convention exists to
prevent. Record the current-season odds coverage as its own measured number, whatever it is — the odds
source may simply have no current-season data yet, which is a different and benign cause. Say which.

Record all of it in `08-BACKFILL-EVIDENCE.md`: the id-map rebuild result, the before-and-after
per-season row counts side by side, the current-season row count, position coverage, fixture-difficulty
coverage, expected-points coverage, and current-season odds coverage with its diagnosis. State
explicitly that features were not rebuilt and no model was retrained, and why that is correct — the
current season is excluded from the configured data seasons.

Close by confirming the negative: nothing under `web/` changed, no cron script changed, and the two
consumer modules are byte-identical to their committed state.
  </action>
  <verify>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m data.id_map</automated>
    <fails_when>non-zero exit, or the printed per-season lines omit the current season, or the current season's line reports it was sourced from the live bootstrap fallback rather than from the players_raw file this phase now writes</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m data.build_table</automated>
    <fails_when>non-zero exit, or the output contains a skip line naming the current season, or the printed season list in the summary block has fewer than 11 entries</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "import pandas as pd, config; df=pd.read_parquet(config.PROCESSED_DIR/'player_gw.parquet'); base={'2016-17':23679,'2017-18':22467,'2018-19':21790,'2019-20':22501,'2020-21':24365,'2021-22':25447,'2022-23':26505,'2023-24':29725,'2024-25':27283,'2025-26':29747}; got=df.groupby('season').size().to_dict(); print('regressions', {s:(base[s], got.get(s)) for s in base if got.get(s)!=base[s]}); cur=df[df.season==config.CURRENT_SEASON]; print('current_rows', len(cur)); print('position_cov', round(cur.position.notna().mean(),4)); print('fdr_cov', round(cur.fdr_self.notna().mean(),4)); print('code_cov', round(cur.player_code.notna().mean(),4)); print('xp_cov_by_gw', {int(k): round(v,3) for k,v in cur.groupby('gw').xp_fpl.apply(lambda s: s.notna().mean()).to_dict().items()})"</automated>
    <fails_when>non-zero exit, or `regressions` prints a non-empty dictionary, or `current_rows` prints 0, or `position_cov` prints below 1.0, or `fdr_cov` prints below 0.95, or `code_cov` prints below 0.98</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m pytest -q</automated>
    <fails_when>non-zero exit, or the summary reports any failure or error</fails_when>
    <automated>git status --porcelain web/ scripts/ data/build_table.py data/id_map.py</automated>
    <fails_when>any output at all — silence is the pass condition for the frozen surfaces and the two unchanged consumers</fails_when>
  </verify>
  <acceptance_criteria>
    - The rebuilt table contains current-season rows, and the evidence document records the count.
    - All ten past-season row counts equal the baseline table's values exactly; the regressions check prints an empty dictionary.
    - Current-season position coverage is 1.0 and fixture-difficulty coverage is at least 0.95, both recorded in the evidence document.
    - Current-season stable-player-code coverage is at least 0.98, proving the id-map rebuild picked up elements registered after the pre-season snapshot.
    - Expected-points coverage is recorded per current-season gameweek, and the zero values for gameweeks 1 and 2 are stated as the documented gap rather than as a defect.
    - Current-season odds coverage is recorded as a number with a one-line diagnosis distinguishing a club-name mismatch from an absent upstream source.
    - The evidence document states that features were not rebuilt and no model was retrained, and names the configured exclusion of the current season as the reason.
    - `git status --porcelain web/ scripts/ data/build_table.py data/id_map.py` prints nothing.
  </acceptance_criteria>
  <done>The canonical table carries current-season rows with full position, difficulty and identity coverage, the ten past seasons are unchanged, and every measured number including the honest gaps is recorded in the evidence document.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live public API → irreplaceable capture | the only window in which the two missing gameweeks are retrievable; a botched run that writes a ledger file closes that window silently |
| external published file → our verdict | a third-party file is used as the oracle that decides whether our schema mapping is correct |
| canonical table rebuild → live pipeline artifacts | the primary pipeline artifact is regenerated while the weekly recommendation cycle is running |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-03-01 | Tampering | a partial sweep writing a ledger file that later runs treat as complete | high | mitigate | The verify command fails a gameweek with an implausibly low row count, the per-player failure count is recorded, and a force re-capture is the prescribed remedy rather than acceptance |
| T-08-03-02 | Tampering | making the cross-check agree by editing captured data | high | mitigate | A prohibition forbids data edits outright; the acceptance criteria require a code commit and a force re-capture as the only permitted fix path, and require the diagnosis in writing |
| T-08-03-03 | Repudiation | a verdict recorded from reading rather than from a measured comparison | high | mitigate | The comparison is a runnable command whose four key-set counts and per-column disagreement list are printed; the evidence document must carry those printed numbers, not a narrative |
| T-08-03-04 | Denial of Service | the live weekly recommendation cycle during the rebuild | high | mitigate | Nothing in this plan writes under `web/` or runs a cron script or an export step, asserted by a silence-is-pass git status criterion in two tasks; the rebuilt table feeds only paths that exclude the current season |
| T-08-03-05 | Tampering | a stale id map silently leaving newly-registered elements without a stable code | high | mitigate | The id map is rebuilt explicitly before the table build, its source is asserted to be the refreshed players_raw file, and current-season code coverage is gated at 0.98 |
| T-08-03-06 | Information Disclosure | the oracle file fetched into the repository tree | low | mitigate | The published file is written to a scratch location outside the working tree, and a criterion asserts no untracked CSV appears under `data/` |
| T-08-03-SC | Tampering | package-manager installs | high | accept | No install task exists in this phase; `08-RESEARCH.md`'s Package Legitimacy Audit records zero new packages. Nothing to gate |
</threat_model>

<verification>
- Every finished, data-checked gameweek has a ledger file, and `merged_gw.csv` carries all 46 columns with no absent source key
- The gameweek-1 comparison reports zero keys present only in the published file, and every shared column outside the two declared exclusions agrees
- `python -m data.build_table` prints eleven seasons with no skip line for the current one, and the ten past-season row counts match the baseline exactly
- Current-season position coverage is complete, fixture-difficulty coverage is at least 0.95, stable-code coverage at least 0.98
- `/home/sraja/miniconda3/envs/python314/bin/python -m pytest -q` green
- `git status --porcelain web/ scripts/ data/build_table.py data/id_map.py` prints nothing

</verification>

<success_criteria>
- The two gameweeks the previous source never published are on disk, captured from the official API before the retention window closes
- The schema mapping is confirmed against an independent copy of gameweek 1, or every disagreement is diagnosed and recorded
- The canonical table ingests the captured rows with no change to either consumer module
- Nothing regressed: ten past seasons identical, frozen surfaces untouched, no model retrained
- Every measured number, including the honest expected-points gap, is written down in the evidence document

</success_criteria>

<output>
Create `.planning/phases/08-self-hosted-gameweek-data-capture/08-03-SUMMARY.md` when done
</output>