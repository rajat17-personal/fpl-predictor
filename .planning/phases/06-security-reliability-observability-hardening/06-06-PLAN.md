---
phase: 06-security-reliability-observability-hardening
plan: 06
type: execute
wave: 4
depends_on: ["06-02", "06-05"]
gap_closure: true
files_modified:
  - e2e/scripts/capture_fixtures.py
  - ruff.toml
  - data/ingest.py
  - tests/test_reliability.py
  - tests/test_capture_fixtures.py
autonomous: true
requirements: [REL-01, REL-04]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "e2e/scripts/capture_fixtures.py's capture path resolves every global name it loads — the script no longer raises NameError on its default, non---verify invocation."
    - "The project's existing lint command, `ruff check .`, inspects every tracked Python file in the repository — no tracked source sits in a lint blind spot, so the CI lint step and preflight Gate 2/8 would have caught CR-01 on the commit that introduced it."
    - "A future edit that re-hides a tracked Python file from the lint gate turns the test suite red and names the hidden file, rather than silently re-opening the blind spot."
    - "e2e/scripts/capture_fixtures.py imports as a module and its --verify path returns 0 from inside the pytest suite, so an import-time break or a CLI-wiring break is caught by pytest rather than by a developer running the tool months later."
    - "An undefined global on the capture path fails the test suite even if the lint configuration is later loosened — the runtime-object check does not depend on ruff."
    - "When the lint gate's inspected file set and the tracked Python file set are exactly equal, the coverage gate passes; a file present in either set but absent from the other fails it and is named in the assertion message."
    - "An empty inspected file set or an empty tracked file set fails the coverage gate rather than passing it vacuously."
    - "Both new gates report their findings sorted by path, so two runs over an unchanged tree produce byte-identical assertion output."
    - "A completed pytest run leaves the frozen fixture set byte-identical and adds no untracked file under it — `git status --porcelain -- e2e/fixtures` prints nothing."
    - statement: "A pytest run aborted part-way leaves the frozen fixture set byte-identical, because no gate added by this plan opens any path under e2e/fixtures for writing and none of them calls _capture()."
      verification: backstop
    - "Neither new gate leaks a file handle — the repository-wide bare-file-handle gate in tests/test_reliability.py stays at zero findings with both new modules tracked."
  artifacts:
    - e2e/scripts/capture_fixtures.py
    - ruff.toml
    - tests/test_reliability.py
    - tests/test_capture_fixtures.py
  key_links:
    - "ruff.toml `exclude` -> `ruff check .` -> .github/workflows/ci.yml's Lint step and scripts/preflight.sh Gate 2/8: narrowing the exclude is what makes the two lint gates that already exist actually see e2e/scripts/ and data/. Neither the workflow nor preflight is edited by this plan — the command string stays `ruff check .`."
    - "tests/test_reliability.py's coverage gate -> ruff.toml: the only thing standing between a future blanket exclude entry and a silent return of this whole bug class."
    - "tests/test_capture_fixtures.py -> e2e/scripts/capture_fixtures.py's module import, its main(['--verify']) path, and its _capture code object: the first automated exercise of this script from the test suite, and the ruff-independent backstop for the same defect."
  prohibitions:
    - "No finding may be silenced with a per-line or per-file suppression comment, or by narrowing ruff.toml's `select` list — every finding is fixed at the source."
    - "e2e/fixtures/v1 must never be rewritten, regenerated, or edited: v1 is immutable (D-08). No gate added here may call `_capture()`."
    - "No new runtime or dev dependency may be added — requirements.txt, requirements-dev.txt, requirements.in and requirements-dev.in stay byte-identical. pyflakes is present in the developer's conda environment but is absent from the hash-locked dev lock, so no gate may depend on it."
    - "No test added here may contact the live FPL API or load a model artifact."
    - "The narrowed lint exclude must not expose any untracked or gitignored path to the lint gate — the inspected set is exactly the tracked Python set, no more and no less."
---

<objective>
Close CR-01: restore the import that `e2e/scripts/capture_fixtures.py`'s capture path needs, and make
the lint gate that should have caught it actually cover the file.

Purpose: 06-02's Task 3 removed `import json` from `e2e/scripts/capture_fixtures.py` while converting
its bare file handles onto `ops.jsonio`. Line 133 still calls `json.loads(...)`, so the script's
default action — capturing or repairing the immutable v1 E2E fixture set the Playwright suite,
`tests/test_fixture_mode.py` and `tests/test_payloads.py` all depend on — raises
`NameError: name 'json' is not defined` on every invocation that is not `--verify`. The one-line
defect is not the interesting part. The interesting part is *why nothing caught it*: `ruff.toml`
excludes the whole of `e2e/` (and the whole of `data/`) from lint, hiding 10 of this repository's 54
tracked Python files from the CI lint step and from preflight Gate 2/8 — and ruff's already-selected
`F` rule set reports this exact defect as F821 the moment it is allowed to look.

Output: the restored import; a `ruff.toml` whose exclusions name regenerable subtrees rather than
whole source directories; and two complementary pytest gates — a self-tested lint-coverage gate that
makes a future blanket exclusion turn the suite red, and a runtime-object gate that catches an
undefined global on the capture path even if the lint configuration is later loosened.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/CLAUDE.md
@.planning/phases/06-security-reliability-observability-hardening/06-VERIFICATION.md
@.planning/phases/06-security-reliability-observability-hardening/06-REVIEW.md
@.planning/phases/06-security-reliability-observability-hardening/06-02-SUMMARY.md
</context>

<gap_coverage_audit>

| Gap (06-VERIFICATION.md `gaps:`) | Missing item | Closed by |
|---|---|---|
| CR-01 — `capture_fixtures.py`'s fixture-capture path crashes | "Add `import json` back to e2e/scripts/capture_fixtures.py's import block" | Task 1 |
| CR-01 | "Add a regression test (or a project-conventions lint/smoke check that isn't excluded for e2e/) that actually exercises the capture path" | Task 1 (the lint gate now covers the file — the *statically checks* half), Task 2 (self-tested coverage gate so the blind spot cannot silently return), Task 3 (module import + `--verify` run + runtime-object undefined-global check — the *actually exercises* half) |

**Not planned, deliberately.** The four ⚠ Warning anti-patterns in 06-VERIFICATION.md are recorded as
explicitly non-gating and pre-existing, and none of them is adjacent to this gap's file set, so this
plan does not touch them: **WR-01** (`predict/digest.py:41` falsy-`0.0` ownership),
**WR-02** (`ops/jsonio.py` write mode 0600), **WR-03** (`predict/scoreboard.py::score_gw` empty-merge
`IndexError`), **WR-04** (`api/main.py::require_key` non-constant-time comparison). They remain open
in `06-REVIEW.md` for a later hardening pass. The two `human_verification` items (live-browser CORS
check; cron install plus webhook confirmation) are end-of-phase UAT / user-setup items and get no plan.

**One adjacent item IS in scope, as a prerequisite rather than as cleanup.** Narrowing the lint
exclusion makes `ruff check .` see `data/*.py` for the first time, and it reports exactly one finding
there: `data/ingest.py:14: F401 'json' imported but unused`. That import has been dead since Phase 01
(commit `1ee176a`) — it is *not* a Phase 6 regression — but the new gate cannot be green while it
stands. Removing it is a precondition of the gate existing at all, not scope creep. Measured blast
radius of the exclusion change across the whole tree: **exactly two findings, both the dead/missing
`json` import above and CR-01 itself.**

</gap_coverage_audit>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end — restore the import and put every tracked Python file inside the lint gate</name>
  <files>e2e/scripts/capture_fixtures.py, ruff.toml, data/ingest.py</files>
  <read_first>
    - `e2e/scripts/capture_fixtures.py` lines 16-32 (the import block: `from __future__ import annotations`, then `argparse`, `datetime as dt`, `shutil`, `sys`, `pathlib.Path`, then the three `# noqa: E402` post-`sys.path` imports) and line 133 (`records = json.loads(pool.to_json(orient="records"))` — the untouched call that needs the import). Note the existing `# noqa: E402` comments are load-bearing: they suppress the module-import-not-at-top rule for the imports that must follow the `sys.path.insert`, and they must survive unchanged.
    - `06-REVIEW.md` finding CR-01 (lines 81-109) — its **Fix** block shows the exact target import block.
    - `ruff.toml` (whole file, 21 lines) — `target-version`, `line-length`, the 8-entry `exclude` list, and `[lint] select = ["E4", "E7", "E9", "F"]`. The `F` rule set is pyflakes: F821 (undefined name) and F401 (unused import) are already selected — the rules were never the problem, the exclusion was.
    - `.github/workflows/ci.yml` lines 48-49 (`- name: Lint (ruff)` / `run: ruff check .`) and `scripts/preflight.sh` lines 63-69 (Gate 2/8, `"$VENV_DIR/bin/ruff" check .` with its fail message that forbids ignore-list suppressions). Both invoke the bare `ruff check .` string — this task changes what that command *sees*, and must not change either call site.
    - `data/ingest.py` lines 11-24 (the import block) and lines 84 and 89 — confirm for yourself that the only other occurrences of the four-letter string in this file are inside the `"bootstrap-static.json"` and `"fixtures.json"` filename literals, i.e. the module-level import on line 14 really is dead.
    - `.gitignore` — `data/raw/`, `data/processed/`, `node_modules/`, `e2e/test-results/`, `e2e/playwright-report/`, and the deliberate `!data/snapshots/**` re-include. The narrowed exclude list must stay consistent with this file.
  </read_first>
  <action>
Three edits, one concern: make the lint command the project already runs actually cover, and pass
over, every tracked Python file.

**1. `e2e/scripts/capture_fixtures.py` — restore the missing import.**
Insert a single `import json` line into the stdlib import block, in the existing alphabetical order:
after `import datetime as dt` and before `import shutil`. Change nothing else in the file — do not
touch line 133, do not reorder the `# noqa: E402` imports, and do not add any suppression comment.
The target block is exactly the one printed in 06-REVIEW.md's CR-01 **Fix**.

**2. `ruff.toml` — replace the two over-broad exclusions with subtree-scoped ones.**
The `exclude` list currently reads `data`, `models/artifacts`, `frontend`, `e2e`, `web`,
`downloaded_files`, `.planning`, `.gsd`. The entries `data` and `e2e` were meant to skip regenerable
pipeline output and node/report trees, but they skip the source directories wholesale. Replace those
two entries with the subtrees actually intended, leaving the other six entries byte-identical and in
place. The verified target list, in this order, is: `data/raw`, `data/processed`, `data/snapshots`,
`models/artifacts`, `frontend`, `web`, `downloaded_files`, `.planning`, `.gsd`, `e2e/node_modules`,
`e2e/fixtures`, `e2e/playwright-report`, `e2e/test-results`.

This exact list was measured against this tree before this plan was written: `ruff check .` then
inspects **exactly** the 54 paths that `git ls-files '*.py'` returns — no gitignored path, no
`node_modules` file, no untracked file leaks in, and no tracked Python file is left out. (Ruff also
prints `ruff.toml` itself under `--show-files`; that is a config artifact, not a linted Python file,
and any check must filter to paths ending in `.py`.)

Update the file's header comment to record *why* the entries are subtree-scoped rather than
directory-scoped: the two blanket entries hid 10 of the repository's 54 tracked Python files (all
nine `data/*.py` modules plus `e2e/scripts/capture_fixtures.py`) from the CI lint step and from
preflight Gate 2/8, which is how a `NameError` on the fixture-capture tool's primary action shipped
unnoticed. Keep the existing comment explaining why there is a standalone `ruff.toml` and no
`pyproject.toml`.

**3. `data/ingest.py` — delete the dead module-level `import json` on line 14.**
This is a prerequisite, not optional cleanup: the narrowed exclusion makes `ruff check .` see this
file for the first time, and F401 fires on that line, so edit 2 cannot be green without it. The
import has been dead since commit `1ee176a` (Phase 01) — it is not a Phase 6 regression, and this is
the only finding the exclusion change surfaces outside CR-01 itself. Delete the line; add no
suppression comment and no replacement.

Do not edit `.github/workflows/ci.yml` or `scripts/preflight.sh`. Both already run `ruff check .`;
the point of this task is that the command string does not have to change for the coverage to change.
  </action>
  <reversibility rating="reversible">Restoring a stdlib import and widening a lint gate's file scope; both revert with a one-line edit and neither changes any runtime code path in the API, the pipeline, or the site.</reversibility>
  <verify>
    <automated>test "$(grep -c '^import json$' e2e/scripts/capture_fixtures.py)" -eq 1 &amp;&amp; echo "JSON IMPORT RESTORED"</automated>
    <fails_when>non-zero exit, or the string JSON IMPORT RESTORED absent from stdout</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/ruff check --no-cache e2e/scripts/capture_fixtures.py</automated>
    <fails_when>non-zero exit, or the string F821 anywhere in the output</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/ruff check .</automated>
    <fails_when>non-zero exit, or the string "All checks passed!" absent from stdout</fails_when>
    <automated>test "$(/home/sraja/miniconda3/envs/python314/bin/python -m ruff check --show-files . | grep -c '\.py$')" -eq "$(git ls-files '*.py' | wc -l)" &amp;&amp; echo "LINT GATE FILE COUNT MATCHES TRACKED PYTHON COUNT"</automated>
    <fails_when>non-zero exit, or the string LINT GATE FILE COUNT MATCHES TRACKED PYTHON COUNT absent from stdout</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m ruff check --show-files . | grep -q 'e2e/scripts/capture_fixtures.py' &amp;&amp; /home/sraja/miniconda3/envs/python314/bin/python -m ruff check --show-files . | grep -q 'data/ingest.py' &amp;&amp; echo "BOTH FORMERLY HIDDEN FILES ARE INSIDE THE LINT GATE"</automated>
    <fails_when>non-zero exit, or the string BOTH FORMERLY HIDDEN FILES ARE INSIDE THE LINT GATE absent from stdout</fails_when>
    <automated>! git ls-files '*.py' | xargs grep -n -- 'noqa$' &amp;&amp; echo "NO BARE SUPPRESSIONS"</automated>
    <fails_when>non-zero exit, or the string NO BARE SUPPRESSIONS absent from stdout</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m pytest -q</automated>
    <fails_when>non-zero exit, or the string "failed" in the summary line</fails_when>
    <automated>git diff --quiet -- e2e/fixtures requirements.txt requirements-dev.txt requirements.in requirements-dev.in &amp;&amp; echo "FIXTURES AND LOCKS UNTOUCHED"</automated>
    <fails_when>non-zero exit, or the string FIXTURES AND LOCKS UNTOUCHED absent from stdout</fails_when>
  </verify>
  <acceptance_criteria>
    - `e2e/scripts/capture_fixtures.py` contains exactly one line matching `^import json$`, positioned between the `import datetime as dt` line and the `import shutil` line
    - Line 133 of `e2e/scripts/capture_fixtures.py` is unchanged and still reads a `json.loads(pool.to_json(orient="records"))` call
    - The five `# noqa: E402` comments in `e2e/scripts/capture_fixtures.py` are unchanged
    - `ruff.toml`'s `exclude` list contains 13 entries in the order given in the action, with `data` and `e2e` no longer present as standalone entries and the other six original entries byte-identical
    - `ruff.toml`'s header comment states that the previous blanket entries hid 10 tracked Python files from the CI lint step and preflight Gate 2/8
    - `data/ingest.py` no longer contains a module-level `import json`, and lines 84 and 89 still reference the `bootstrap-static.json` and `fixtures.json` filenames
    - `/home/sraja/miniconda3/envs/python314/bin/ruff check .` exits 0 and prints `All checks passed!`
    - `ruff check --show-files .` filtered to `.py` paths yields the same count as `git ls-files '*.py' | wc -l`
    - `e2e/scripts/capture_fixtures.py` and `data/ingest.py` both appear in `ruff check --show-files .` output
    - `.github/workflows/ci.yml` and `scripts/preflight.sh` are not in this task's diff
    - No new suppression comment was added anywhere; `git ls-files '*.py' | xargs grep -n -- 'noqa$'` finds nothing
    - `/home/sraja/miniconda3/envs/python314/bin/python -m pytest -q` exits 0
    - `git diff --quiet -- e2e/fixtures` exits 0
  </acceptance_criteria>
  <done>The capture path's missing name is defined again, and the lint command CI and preflight already run now inspects all 54 tracked Python files — meaning the gate that missed CR-01 would now catch it on the introducing commit.</done>
</task>

<task type="auto">
  <name>Task 2: Self-tested lint-coverage gate in tests/test_reliability.py</name>
  <files>tests/test_reliability.py</files>
  <read_first>
    - `tests/test_reliability.py` (whole file, 168 lines). Three things matter. (a) Its module docstring warns that the scanner works on raw source TEXT, so any literal spelling of the builtin file-opening call anywhere in this module — code, comment, or docstring — would flag the file against itself; the module builds such literals via concatenation (`_OPEN_CALL`, `_READ_JSON_CALL`, `_WHAT_KWARG`) for exactly this reason. (b) `_tracked_python_files()` shows this repo's convention for shelling out to `git ls-files` with `cwd=_REPO_ROOT`, `capture_output=True`, `text=True`, `check=True`. (c) Every gate in this module is paired with positive and negative control tests so the gate cannot silently stop working — follow that convention for the new gate too.
    - `ruff.toml` as edited by Task 1 — the config the new helper points `--config` at, so both the real gate and its controls evaluate under the project's own rule selection rather than ruff's ambient defaults.
    - `06-VERIFICATION.md` gaps block, second `missing` item — this task is the "statically checks / isn't excluded for e2e/" half of it.
  </read_first>
  <action>
Extend `tests/test_reliability.py` — do not create a new module, and do not modify any existing test
in it. Add `import sys` to the existing import block (`re`, `subprocess`, `pathlib.Path`).

Add two module-level helpers, placed after `_tracked_python_files()` and before the control tests:

- `_ruff(*args)` — runs ruff as a module of the currently-running interpreter:
  `[sys.executable, "-m", "ruff", "check", "--no-cache", "--output-format", "concise", "--config",
  str(_REPO_ROOT / "ruff.toml"), *args]` with `cwd=_REPO_ROOT`, `capture_output=True`, `text=True`,
  `check=False`; returns the `CompletedProcess`. Invoke it through `sys.executable -m ruff` rather
  than a bare `ruff` on `PATH` so the gate always uses the hash-locked ruff installed alongside the
  test run and never depends on shell activation. Pass `--config` explicitly so a control file living
  outside the repository tree still evaluates under this project's `select` list.
- `_ruff_inspected_python_files(*extra)` — runs `_ruff("--show-files", *extra, ".")`, splits stdout on
  newlines, keeps only entries ending in `.py`, makes each one relative to `_REPO_ROOT`, and returns
  them sorted. The `.py` filter is required: `--show-files` also prints `ruff.toml` itself.

Then add four tests.

`test_ruff_detects_an_undefined_name(tmp_path)` — positive control for the *rule*. Write a
two-statement module into `tmp_path` via `Path.write_text` whose function body calls an attribute of
a name that the module never imports, run `_ruff` against that path, and assert the process exited
non-zero and its stdout names rule code F821. This proves the configured rule set really does detect
the CR-01 defect class, rather than the gate below passing because ruff never found anything to say.

`test_ruff_reports_nothing_for_a_clean_module(tmp_path)` — negative control. The same module with the
missing import present exits 0 and produces no finding line.

`test_ruff_check_covers_every_tracked_python_file()` — the real gate. Compare
`_ruff_inspected_python_files()` against the sorted keys of `_tracked_python_files()`. Assert first
that **both** collections are non-empty (an empty scan set is a vacuous pass, not a pass), then
assert the two sorted lists are equal. On failure, name the difference in both directions in the
assertion message — files that are tracked but invisible to the lint gate, and files the lint gate
inspects that git does not track — each list sorted, so two runs over the same tree produce
byte-identical output.

`test_coverage_gate_notices_a_reinstated_blanket_exclude()` — positive control for the *gate*. Call
`_ruff_inspected_python_files("--exclude", "e2e")` and assert `e2e/scripts/capture_fixtures.py` is
absent from the result while being present in the unmodified `_ruff_inspected_python_files()` call.
This proves the coverage gate reads a real exclusion rather than returning a constant set, so it will
actually fire if someone re-adds a blanket entry to `ruff.toml`.

Constraints while writing this module: use `Path.write_text` for the control files (never the builtin
file-opening call, whose literal text would trip this module's own scanner), never spell that builtin
call contiguously in any comment or docstring you add, keep every control file inside `tmp_path` so
nothing is written into the repository tree, and add no new dependency — ruff is already
`ruff==0.16.6` in the hash-locked `requirements-dev.txt`.
  </action>
  <reversibility rating="reversible">Four added tests and two helpers in an existing test module; deleting them changes no production behaviour.</reversibility>
  <verify>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m pytest -q tests/test_reliability.py</automated>
    <fails_when>non-zero exit, the string "failed" in the summary line, or the string "9 passed" absent from the summary line</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m pytest -q tests/test_reliability.py -k "undefined_name or reinstated_blanket_exclude"</automated>
    <fails_when>non-zero exit, or the string "2 passed" absent from the summary line</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m pytest -q tests/test_reliability.py -k "no_bare_file_handles"</automated>
    <fails_when>non-zero exit, or the string "1 passed" absent from the summary line</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/ruff check .</automated>
    <fails_when>non-zero exit, or the string "All checks passed!" absent from stdout</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m pytest -q</automated>
    <fails_when>non-zero exit, or the string "failed" in the summary line</fails_when>
    <automated>git diff --quiet -- e2e/fixtures requirements.txt requirements-dev.txt requirements.in requirements-dev.in &amp;&amp; echo "FIXTURES AND LOCKS UNTOUCHED"</automated>
    <fails_when>non-zero exit, or the string FIXTURES AND LOCKS UNTOUCHED absent from stdout</fails_when>
  </verify>
  <acceptance_criteria>
    - `tests/test_reliability.py` contains exactly nine test functions: the five that existed before this task, unmodified, plus `test_ruff_detects_an_undefined_name`, `test_ruff_reports_nothing_for_a_clean_module`, `test_ruff_check_covers_every_tracked_python_file`, and `test_coverage_gate_notices_a_reinstated_blanket_exclude`
    - `_ruff` invokes ruff as `[sys.executable, "-m", "ruff", ...]` with `--config` pointing at `_REPO_ROOT / "ruff.toml"` and `cwd=_REPO_ROOT`; the string `"ruff"` never appears as a bare `PATH` executable in the argv list
    - `_ruff_inspected_python_files` filters its results to paths ending in `.py` and returns them sorted and relative to `_REPO_ROOT`
    - `test_ruff_detects_an_undefined_name` asserts a non-zero return code and the literal rule code F821 in stdout
    - `test_ruff_check_covers_every_tracked_python_file` asserts both collections are non-empty before comparing them, and its failure message lists the two-directional difference sorted
    - `test_coverage_gate_notices_a_reinstated_blanket_exclude` asserts `e2e/scripts/capture_fixtures.py` is absent under `--exclude e2e` and present without it
    - Every control file is written under the `tmp_path` fixture via `Path.write_text`; `git status --porcelain` shows no new file outside `tests/test_reliability.py` after the run
    - `test_no_bare_file_handles_in_tracked_python` still passes with the edited module tracked
    - `/home/sraja/miniconda3/envs/python314/bin/python -m pytest -q tests/test_reliability.py` reports 9 passed
    - `git diff --quiet -- requirements.txt requirements-dev.txt requirements.in requirements-dev.in` exits 0
  </acceptance_criteria>
  <done>A blanket lint exclusion can no longer be re-added silently — re-hiding any tracked Python file turns the suite red and names the file, and two controls prove the gate detects both an undefined name and a real exclusion.</done>
</task>

<task type="auto">
  <name>Task 3: tests/test_capture_fixtures.py — exercise the capture script and its globals from pytest</name>
  <files>tests/test_capture_fixtures.py</files>
  <read_first>
    - `e2e/scripts/capture_fixtures.py` as edited by Task 1 (whole file). Note: `_capture` is the only function that writes — it issues live HTTP requests to the FPL API, loads `models/artifacts/xp_model.joblib`, and rewrites the immutable v1 fixture set, so no test may call it. `_verify` and `main(["--verify"])` are read-only against the committed set. The module runs `sys.path.insert(0, repo_root)` at import time and imports `config`, `ops.jsonio`, `predict.export` and `predict.live`; `joblib` and `requests` are imported inside `_capture`, not at module level.
    - `tests/test_reliability.py` as edited by Task 2 — the `_REPO_ROOT = Path(__file__).resolve().parents[1]` convention, the `subprocess.run(..., cwd=_REPO_ROOT, capture_output=True, text=True)` convention, and above all its docstring warning: the repository-wide bare-file-handle gate scans this new module too, so this file must never spell the builtin file-opening call contiguously in code, comment, or docstring.
    - `tests/test_fixture_mode.py` — the established convention in this repo for a test that depends on the committed `e2e/fixtures/v1` set.
    - `e2e/fixtures/v1/MANIFEST.md` — the v1 immutability contract (D-08).
    - `pytest.ini` — `testpaths = tests`, so the new module must live under `tests/`; and the `-p no:playwright -p no:seleniumbase` plugin disabling that applies to it.
  </read_first>
  <action>
**Naming hazard, read before writing a single line.** An acceptance check for this task negative-greps
this file for the parenthesised call form of the capture function. Refer to that function as `_capture`
— bare, no parentheses — everywhere in this module, including its docstring and every comment. A
docstring that spells the parenthesised form would fail the module against itself, which is the exact
self-flagging hazard `tests/test_reliability.py`'s own docstring documents for a different literal.

Create `tests/test_capture_fixtures.py`, the behavioural half of the CR-01 regression gate. Open with
a module docstring stating that the static half lives in `tests/test_reliability.py` (the
lint-coverage gate) and this half proves the script actually imports and that its read-only CLI path
runs; and that neither half may invoke `_capture`, because that function performs live HTTP requests,
loads a model artifact, and rewrites the immutable v1 fixture set.

Add a session-scoped fixture (or an `functools.lru_cache`-wrapped loader) that loads
`e2e/scripts/capture_fixtures.py` **by file path** — `importlib.util.spec_from_file_location` +
`module_from_spec` + `spec.loader.exec_module`, under a private module name such as
`_capture_fixtures_under_test`. By path, not by import statement, because `e2e/scripts/` is not a
package and has no `__init__.py`. Load it once per session; the module import pulls in `predict.live`
and is not free.

Then four tests.

`test_capture_fixtures_module_imports` — the loader completes and the module exposes `main`,
`_capture` and `_verify` as callables. This catches an import-time break — a removed stdlib import
used at module level, a renamed `predict.export` symbol — that no other test in this repository
covers today.

`test_capture_path_loads_no_undefined_global` — the ruff-independent backstop, and the direct
runtime-object form of CR-01. Walk the code object of each of `_capture`, `_verify` and `main` with
`dis.get_instructions`, collecting `instr.argval` for every instruction whose `opname` is
`LOAD_GLOBAL`, and recursing into any entry of `co_consts` that is itself a code object (so a nested
function such as `_verify`'s `require_file` is covered). Assert that every collected name resolves
either in the loaded module's `__dict__` or as an attribute of `builtins`. Report the unresolved
names sorted, per function, in the assertion message. `LOAD_GLOBAL` is the precise opcode here:
function-local imports (`joblib`, `requests` inside `_capture`) are `LOAD_FAST` and attribute names
(`loads`, `to_json`, `relative_to`) never appear as `LOAD_GLOBAL` arguments, so this check has no
false positives on the current source — it reported exactly `json` against the pre-fix file and
exactly nothing against the fixed one.

`test_verify_path_returns_zero` — call `main(["--verify"])` and assert it returns `0`, and that the
captured stdout contains the `[verify] OK:` marker. This exercises the argparse wiring and the whole
read path against the committed v1 set.

`test_the_gates_leave_the_frozen_fixture_set_untouched` — after the tests above, shell out to
`git status --porcelain -- e2e/fixtures` with `cwd=_REPO_ROOT` and assert its stdout is empty. Use
`status --porcelain` rather than `diff --quiet`: it catches an added untracked file under the fixture
tree as well as a modification, which is the failure mode that would matter if someone later
mistakenly wired the capture function into a test.

Constraints: no network call and no model-artifact load may occur in this module — `--verify` reaches
neither, and `_capture` is never invoked. Never spell the builtin file-opening call contiguously
anywhere in this file. Add no dependency.
  </action>
  <reversibility rating="reversible">A new, self-contained test module that nothing imports; deleting it changes no production behaviour.</reversibility>
  <verify>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m pytest -q tests/test_capture_fixtures.py</automated>
    <fails_when>non-zero exit, the string "failed" in the summary line, or the string "4 passed" absent from the summary line</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m pytest -q tests/test_capture_fixtures.py -k "undefined_global"</automated>
    <fails_when>non-zero exit, or the string "1 passed" absent from the summary line</fails_when>
    <automated>! grep -n '_capture()' tests/test_capture_fixtures.py &amp;&amp; echo "NO CAPTURE INVOCATION IN THE TEST MODULE"</automated>
    <fails_when>non-zero exit, or the string NO CAPTURE INVOCATION IN THE TEST MODULE absent from stdout</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m pytest -q tests/test_reliability.py</automated>
    <fails_when>non-zero exit, or the string "9 passed" absent from the summary line</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m pytest -q</automated>
    <fails_when>non-zero exit, the string "failed" in the summary line, or the string "166 passed" absent from the summary line — the measured pre-plan baseline is 158 passed, 1 skipped, plus Task 2's 4 tests and this task's 4</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/ruff check .</automated>
    <fails_when>non-zero exit, or the string "All checks passed!" absent from stdout</fails_when>
    <automated>test -z "$(git status --porcelain -- e2e/fixtures)" &amp;&amp; echo "FROZEN FIXTURE SET UNTOUCHED BY THE SUITE"</automated>
    <fails_when>non-zero exit, or the string FROZEN FIXTURE SET UNTOUCHED BY THE SUITE absent from stdout</fails_when>
    <automated>PREFLIGHT_PYTHON=/home/sraja/miniconda3/envs/python314/bin/python bash scripts/preflight.sh</automated>
    <fails_when>non-zero exit, any line beginning with "FAILED:", or the string PREFLIGHT PASSED absent from stdout</fails_when>
  </verify>
  <acceptance_criteria>
    - `tests/test_capture_fixtures.py` exists under `tests/` and contains exactly four test functions: `test_capture_fixtures_module_imports`, `test_capture_path_loads_no_undefined_global`, `test_verify_path_returns_zero`, `test_the_gates_leave_the_frozen_fixture_set_untouched`
    - The module loads `e2e/scripts/capture_fixtures.py` via `importlib.util.spec_from_file_location`, not via an `import` statement, and loads it once per session
    - `test_capture_path_loads_no_undefined_global` inspects `_capture`, `_verify` and `main`, collects only `LOAD_GLOBAL` argvals, recurses into nested code objects in `co_consts`, and resolves each name against the module `__dict__` or `builtins`
    - `test_verify_path_returns_zero` asserts a return value of `0` and the substring `[verify] OK:` in captured stdout
    - `test_the_gates_leave_the_frozen_fixture_set_untouched` uses `git status --porcelain -- e2e/fixtures` and asserts empty stdout
    - `grep -n '_capture()' tests/test_capture_fixtures.py` finds no match — the write path is never invoked
    - The module contains no `requests` call and no `joblib.load` call
    - `/home/sraja/miniconda3/envs/python314/bin/python -m pytest -q tests/test_capture_fixtures.py` reports 4 passed
    - `/home/sraja/miniconda3/envs/python314/bin/python -m pytest -q` reports 166 passed, 1 skipped — the measured pre-plan baseline of 158 passed / 1 skipped, plus Task 2's 4 tests and this task's 4. If the observed total differs, reconcile it against `pytest --collect-only -q` and record the real number in SUMMARY.md rather than deleting or weakening a test to reach the expected one
    - `git status --porcelain -- e2e/fixtures` prints nothing after the full suite
    - `bash scripts/preflight.sh` prints `PREFLIGHT PASSED`
  </acceptance_criteria>
  <done>The fixture-capture script is exercised by the test suite for the first time — its module imports, its read-only CLI path returns 0, and an undefined global anywhere on the capture path fails the suite independently of the lint configuration, all without writing a byte into the frozen fixture set.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tracked source → the CI lint gate | Ten of 54 tracked Python files sat outside this boundary; anything wrong in them reached `main` unexamined |
| lint configuration → the operator's confidence | A gate whose file scope silently shrinks is worse than no gate: it converts an unknown into a false assurance |
| test suite → `e2e/fixtures/v1` | The frozen fixture set is an immutable contract (D-08); a test with a write path into it would corrupt the Playwright suite's ground truth |
| test process → the live FPL API and the model artifact | `_capture()` crosses both; every gate here must stay on the read-only side |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-06-01 | Repudiation | `ruff.toml`'s `exclude` list silently shrinking the lint gate's file scope again | high | mitigate | Task 2's `test_ruff_check_covers_every_tracked_python_file` asserts set equality against `git ls-files '*.py'` and fails naming any hidden file; `test_coverage_gate_notices_a_reinstated_blanket_exclude` proves the gate actually reads exclusions |
| T-06-06-02 | Repudiation | a gate passing vacuously because its scan set is empty | high | mitigate | The coverage gate asserts both collections are non-empty before comparing; the F821 positive control proves the rule set detects the defect class rather than finding nothing to say |
| T-06-06-03 | Tampering | a test writing into the immutable `e2e/fixtures/v1` set | high | mitigate | No gate calls `_capture()` (asserted by a source grep); `git status --porcelain -- e2e/fixtures` is asserted empty after the suite; a `prohibitions` entry carries the rule |
| T-06-06-04 | Tampering | closing the finding with a suppression comment or a narrowed `select` list instead of a source fix | medium | mitigate | The bare-suppression gate `git ls-files '*.py' \| xargs grep -n -- 'noqa$'` is re-run; `ruff.toml`'s `select` list is unchanged by this plan and preflight Gate 2/8's own fail message already forbids ignore-list suppressions |
| T-06-06-05 | Spoofing | the gate resolving a different `ruff` than the hash-locked one, or none at all | medium | mitigate | `_ruff` invokes `[sys.executable, "-m", "ruff", ...]`, never a bare `PATH` executable, and passes `--config` at the repository's own `ruff.toml`; `pyflakes`, present ambiently in the developer's conda environment but absent from the hash-locked dev lock, is deliberately not used |
| T-06-06-06 | Information Disclosure | the narrowed exclusion exposing a gitignored or untracked path to the lint gate | low | mitigate | Set equality against `git ls-files '*.py'` fails in *both* directions, so an inspected-but-untracked path is a failure too; the exact list was measured to yield zero leakage before this plan was written |
| T-06-06-07 | Denial of Service | a test reaching the live FPL API or loading the model artifact and hanging CI | medium | mitigate | Only `main(["--verify"])` is invoked; `joblib` and `requests` are imported inside `_capture()`, which is never called, and a source grep asserts that |
</threat_model>

<verification>
- `/home/sraja/miniconda3/envs/python314/bin/ruff check .` exits 0 and prints `All checks passed!`
- `ruff check --show-files .` filtered to `.py` paths equals `git ls-files '*.py'` exactly, in both directions
- `/home/sraja/miniconda3/envs/python314/bin/ruff check --no-cache e2e/scripts/capture_fixtures.py` exits 0 with no F821
- `/home/sraja/miniconda3/envs/python314/bin/python -m pytest -q` exits 0 with zero failures
- `/home/sraja/miniconda3/envs/python314/bin/python -m pytest -q tests/test_reliability.py` reports 9 passed
- `/home/sraja/miniconda3/envs/python314/bin/python -m pytest -q tests/test_capture_fixtures.py` reports 4 passed
- `! git ls-files '*.py' | xargs grep -n -- 'noqa$' && echo "NO BARE SUPPRESSIONS"` exits 0
- `git status --porcelain -- e2e/fixtures` prints nothing
- `git diff --quiet -- requirements.txt requirements-dev.txt requirements.in requirements-dev.in` exits 0
- `bash scripts/preflight.sh` prints `PREFLIGHT PASSED`
- `.github/workflows/ci.yml` and `scripts/preflight.sh` appear in no task's diff
</verification>

<success_criteria>
- `e2e/scripts/capture_fixtures.py`'s capture path resolves every global name it loads — CR-01 is closed at the source, with no suppression
- The lint command CI and preflight already run inspects every tracked Python file, so the gate that missed CR-01 would now catch it on the introducing commit
- The blind spot cannot silently return: re-hiding any tracked Python file turns the suite red and names the file
- The defect class is caught twice by independent mechanisms — statically by ruff, and at the runtime-object level by a check that does not depend on the lint configuration
- The frozen v1 fixture set is byte-identical and gains no untracked file, and no gate contacts the live FPL API or loads a model artifact
- The hash-locked requirements pair is byte-identical; no dependency was added, and the ambiently-installed `pyflakes` is not depended upon
</success_criteria>

## Artifacts this phase produces

| Artifact | Kind | Location |
|----------|------|----------|
| `import json` (restored) | module import | `e2e/scripts/capture_fixtures.py` |
| subtree-scoped `exclude` list (13 entries) | lint configuration | `ruff.toml` |
| `_ruff(*args)` | test helper | `tests/test_reliability.py` |
| `_ruff_inspected_python_files(*extra)` | test helper | `tests/test_reliability.py` |
| `test_ruff_detects_an_undefined_name` | positive control (rule) | `tests/test_reliability.py` |
| `test_ruff_reports_nothing_for_a_clean_module` | negative control (rule) | `tests/test_reliability.py` |
| `test_ruff_check_covers_every_tracked_python_file` | the lint-coverage gate | `tests/test_reliability.py` |
| `test_coverage_gate_notices_a_reinstated_blanket_exclude` | positive control (gate) | `tests/test_reliability.py` |
| `tests/test_capture_fixtures.py` | test module | `tests/` |
| `test_capture_fixtures_module_imports` | import smoke gate | `tests/test_capture_fixtures.py` |
| `test_capture_path_loads_no_undefined_global` | ruff-independent defect gate | `tests/test_capture_fixtures.py` |
| `test_verify_path_returns_zero` | CLI read-path gate | `tests/test_capture_fixtures.py` |
| `test_the_gates_leave_the_frozen_fixture_set_untouched` | fixture-immutability gate | `tests/test_capture_fixtures.py` |

Removed: the dead module-level `import json` in `data/ingest.py` (dead since commit `1ee176a`).

## Flagged assumptions (unresolved edge-probe rows)

Probe accounting for this gap-closure scope: **14 probe items = 5 authored + 9 flagged.**

Authored (5, all REL-01, all in `must_haves.truths`): *adjacency* → the set-equality boundary when
the inspected and tracked sets are exactly equal; *empty* → an empty scan set fails rather than passes
vacuously; *ordering* → findings sorted by path for byte-identical output across runs; *concurrency* →
fixture-set immutability across a completed run (explicit) and across an aborted run (backstop);
*resource* → neither gate leaks a file handle, proven by the repository-wide gate in the same module.

Flagged (9):

- **REL-04 (unclassified, in this plan's `requirements`)** — the probe row carries no auto-derivable
  acceptance criterion. This plan's operational reading of REL-04 here is narrow and deliberate: the
  capture tool's read path must fail with an actionable message rather than a `NameError`, which is
  what Task 3's `--verify` gate and the undefined-global gate assert. The broader REL-04 surface
  (`predict/*` JSON-load failures) was satisfied by plans 06-01 and 06-02 and re-proven behaviourally
  in 06-VERIFICATION.md. Confirm at UAT.
- **SEC-01, SEC-03, REL-02, REL-03, REL-05, OBS-01, OBS-02, OBS-03 (8 unclassified rows)** — dispatched
  as one group: every one of these requirements is fully covered by the executed plans 06-01 through
  06-05 and was re-verified as SATISFIED in 06-VERIFICATION.md's Requirements Coverage table. This gap
  plan touches none of their surfaces and authors no predicate for them; they are recorded here rather
  than dropped.

Canon security and compliance prohibitions (input validation at trust boundaries, authentication on
privileged endpoints, secrets never reaching logs) are already carried by the `must_haves.prohibitions`
blocks of plans 06-01, 06-03 and 06-04 and were re-verified in 06-VERIFICATION.md; they are not
re-minted here.

## API coverage

`.planning/phases/06-security-reliability-observability-hardening/COVERAGE.md` already carries a
reasoned "No external API integration" declaration for this phase. The detector still reports
`detected: true`, but its two signals both come from the *existing* 06-01/06-02 plan bodies, not from
this plan. This gap plan adds no third-party API, SDK, or service client — its entire surface is a
stdlib import, a lint configuration, and two pytest modules — so the existing declaration continues to
cover the scope and COVERAGE.md is left unchanged.

<output>
Create `.planning/phases/06-security-reliability-observability-hardening/06-06-SUMMARY.md` when done
</output>
