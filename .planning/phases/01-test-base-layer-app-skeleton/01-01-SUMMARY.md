---
phase: 01-test-base-layer-app-skeleton
plan: 01
subsystem: infra
tags: [git, gitignore, repo-hygiene, package-legitimacy, security]

# Dependency graph
requires: []
provides:
  - Root .gitignore covering Python caches, Node output, regenerable pipeline data,
    model artifacts, installers, and secrets, with data/snapshots/ re-included
  - Full untracked source tree (api/, tests/, web/, config.py, pytest.ini,
    requirements.txt, scripts/, data/, features/, models/, optimize/, predict/,
    backtest/, .github/workflows/, top-level docs) committed and tracked in git
  - Human sign-off recorded for the 13 [SUS]-flagged packages plans 01-03/01-04 install
affects: [01-02, 01-03, 01-04]

# Actuals (#2632)
# Note: this plan's only authored artifact is .gitignore (1,371 bytes / ~343 tokens
# at chars/4); the other 76 files in the commit's 7,328-line diff are pre-existing
# source tracked for the first time (git add), not content generated by this plan.
# tokens below reflects the .gitignore authoring cost, not the raw commit diff size.
actuals:
  tokens: 350
  tasks: 2
  commits: 1

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Root .gitignore with directory-specific ignore rules (data/raw/, data/processed/)
       plus an explicit negation (!data/snapshots/, !data/snapshots/**) to re-include an
       irreplaceable subtree, rather than a single broad data/ rule that would shadow it"
    - "Explicit path-argument staging (git add .gitignore api config.py ... — never git add -A
       or git add .) so .gitignore acts as a second safety net, not the only one, against
       committing regenerable/oversized artifacts"

key-files:
  created:
    - .gitignore
  modified: []

key-decisions:
  - "Human approved the full 13-package [SUS] install set (react-router@7, @tanstack/react-query@5,
     vite@7.3.6, @vitejs/plugin-react@5.2.0, lucide-react, typescript-eslint, eslint-plugin-react-refresh,
     @types/node, vitest, @testing-library/react, @testing-library/jest-dom, and responses [PyPI]) plus
     the clean-verdict packages, including the two deliberate downgrade pins from npm `latest`
     (typescript@6.0.3, not 7.x; vite@7.3.6 + @vitejs/plugin-react@5.2.0, not Vite 8 + plugin-react 6)."
  - "Verbatim human answer to the package-legitimacy checkpoint: \"Approved\" — delivered via the
     orchestrator's blocking-human gate, never auto-approved. This sign-off unblocks the first
     pip/npm install commands in plans 01-03 and 01-04."

patterns-established:
  - "Package-legitimacy gate: any [SUS]-flagged install in this milestone requires a recorded,
     verbatim human sign-off before the install command runs — recorded here as the precedent."

requirements-completed: [APIT-01, UI-01]

coverage:
  - id: D1
    description: "Root .gitignore authored and the full untracked source tree committed as the repo's first tracked baseline"
    requirement: "APIT-01"
    verification:
      - kind: other
        ref: "git ls-files api/main.py config.py pytest.ini tests/test_product.py web/assets/app.js .github/workflows/daily.yml | wc -l | grep -qx 6 && git ls-files data/snapshots | grep -q . && git check-ignore -q data/raw && git check-ignore -q data/processed && git check-ignore -q models/artifacts && git check-ignore -q google-chrome-stable_current_amd64.deb"
        status: pass
      - kind: other
        ref: "git diff HEAD~1 --stat -- web/ (verified additions-only, no modifications to existing web/ content)"
        status: pass
    human_judgment: false
  - id: D2
    description: "Human sign-off recorded on the 13 [SUS]-flagged packages before any install in this phase runs"
    requirement: "UI-01"
    verification: []
    human_judgment: true
    rationale: "Package-legitimacy approval is an irreversible trust decision by protocol design (gate=\"blocking-human\") — it is never auto-passable by a test, and this coverage entry exists to record that the human, not the executor, made the call."

# Metrics
duration: 6min
completed: 2026-08-31
status: complete
---

# Phase 1 Plan 1: Test Base Layer & App Skeleton — Repo Baseline & Package Sign-off Summary

**Root .gitignore authored, the entire untracked source tree committed as the repo's first real baseline, and human sign-off recorded on all 13 package-legitimacy flags before Phase 1's first install runs.**

## Performance

- **Duration:** 6 min
- **Started:** 2026-08-31T16:18:52Z (approx, per STATE.md)
- **Completed:** 2026-08-31
- **Tasks:** 2
- **Files modified:** 77 (1 new: `.gitignore`; 76 pre-existing source files newly tracked)

## Accomplishments

- Authored `.gitignore` covering Python caches, Node output, regenerable pipeline data
  (`data/raw/`, `data/processed/`), regenerable model artifacts (`models/artifacts/`),
  installers/scratch (`downloaded_files/`, `*.deb`), and secrets (`.env`, `.env.*`,
  `!.env.example`) — with `!data/snapshots/` and `!data/snapshots/**` explicitly
  re-including the irreplaceable daily price-snapshot history.
- Made the repo's first real source commit: staged 76 pre-existing untracked files with
  explicit path arguments (never `git add -A`) — `api/`, `config.py`, `pytest.ini`,
  `requirements.txt`, `tests/`, `web/`, `scripts/`, `data/` (incl.
  `data/snapshots/2026-08-31.parquet`), `features/`, `models/`, `optimize/`, `predict/`,
  `backtest/`, `.github/workflows/`, and top-level docs.
- Verified every acceptance criterion and the plan's combined automated `<verify>`
  command (`REPO BASELINE OK`) before committing: 6 cited source paths tracked, 12
  `web/data/*.json` files tracked, all 5 regenerable/oversized paths correctly ignored,
  `web/` diff confirmed additions-only.
- Presented the Package Legitimacy Gate checkpoint for the 13 `[SUS]`-flagged packages
  (all "too-new" verdicts, zero `[SLOP]`, every one verified against a real source repo
  and tens-to-hundreds of millions of weekly downloads) and received explicit human
  approval before any install command runs.

## Task Commits

Each task was committed atomically:

1. **Task 1: Author root .gitignore and make the initial source commit** — `1ee176a` (chore)
2. **Task 2: Package legitimacy sign-off before the phase's first install** — checkpoint
   (`type="checkpoint:human-verify"`, `gate="blocking-human"`); no code change, resolved
   by recorded human answer below — no separate commit (nothing to stage for a sign-off).

**Plan metadata:** committed alongside this SUMMARY (see below).

## Package Legitimacy Sign-off (Task 2)

**Checkpoint gate:** `blocking-human` — never auto-approved, including under auto/yolo mode.

**Human's verbatim answer:** **"Approved"**

**Scope of approval:** The full install set researched in `01-RESEARCH.md`, including all
13 `[SUS]`-flagged packages (`responses` [PyPI], `react-router@7`,
`@tanstack/react-query@5`, `vite` pinned **7.3.6**, `@vitejs/plugin-react` pinned
**5.2.0**, `lucide-react`, `typescript-eslint`, `eslint-plugin-react-refresh`,
`@types/node`, `vitest`, `@testing-library/react`, `@testing-library/jest-dom`) plus the
clean-verdict packages (`react`, `react-dom`, `typescript` pinned **6.0.3**, `tailwindcss@4`,
`@tailwindcss/vite@4`, `jsdom`, `eslint`, `eslint-plugin-react-hooks`, `@types/react`,
`@types/react-dom`) — including the two deliberate downgrade pins from npm `latest`
(`typescript@6.0.3` not 7.x; `vite@7.3.6` + `@vitejs/plugin-react@5.2.0` not Vite 8 +
plugin-react 6).

**Delivery mechanism:** Delivered via the orchestrator's blocking-human gate to the
executor mid-plan — not auto-approved by any config flag (`workflow.auto_advance`,
`workflow._auto_chain_active`), consistent with `gate="blocking-human"` semantics.

**Downstream unblock:** Plans 01-03 (`pip install "responses>=0.25,<0.27"`) and 01-04
(`npm create vite@latest frontend`, then the pinned package installs) may now proceed
without re-requesting sign-off — this record is their authorization.

## Files Created/Modified

- `.gitignore` — root ignore rules (Python caches, Node output, regenerable data/model
  artifacts, installers, secrets) with an explicit negation re-including
  `data/snapshots/`
- 76 pre-existing files newly tracked in git (no content changes — first-time `git add`
  of the working tree's actual on-disk source; see the Task 1 commit for the full list)

## Decisions Made

- Directory-specific ignore rules (`data/raw/`, `data/processed/`) plus an explicit
  `!data/snapshots/`/`!data/snapshots/**` negation, rather than a single broad `data/`
  rule — verified via `git check-ignore`/`git status --ignored` that the negation
  correctly re-includes the subtree before committing.
- Human approved the full 13-package `[SUS]` install set and both deliberate downgrade
  pins (`typescript@6.0.3`, `vite@7.3.6` + `@vitejs/plugin-react@5.2.0`) — see Package
  Legitimacy Sign-off above for full scope and verbatim answer.

## Deviations from Plan

None — plan executed exactly as written. `.gitignore` content, staging command, and
commit message matched the plan's `<action>` block verbatim; the checkpoint was
presented exactly as authored in the plan and resolved by the human's own answer, with
no auto-approval attempted (gate is `blocking-human`).

## Issues Encountered

None. Pre-commit scan for credential-shaped entries (`.env`, `secrets*`, `*.pem`,
`*_key*`) in `git status --porcelain` found nothing before staging or committing.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- Repo baseline is in place: every source path this and later Phase 1 plans touch is
  tracked; every regenerable/oversized artifact is ignored; `data/snapshots/` and
  `web/data/*.json` are preserved.
- Package legitimacy sign-off is recorded and verbatim — plans 01-03 (`responses`
  install) and 01-04 (frontend scaffold + package installs) may now execute their
  install commands without re-blocking on this gate.
- No blockers. Ready for 01-02.

---
*Phase: 01-test-base-layer-app-skeleton*
*Completed: 2026-08-31*
