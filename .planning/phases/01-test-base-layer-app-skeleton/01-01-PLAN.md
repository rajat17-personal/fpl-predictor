---
phase: 01-test-base-layer-app-skeleton
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
autonomous: false
requirements: [APIT-01, UI-01]

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`git ls-files` lists api/main.py, config.py, pytest.ini, tests/test_product.py, web/assets/app.js, and .github/workflows/daily.yml — every path this phase and PATTERNS.md cite is a tracked source path"
    - "`git status --porcelain` reports no untracked entry for data/raw, data/processed, models/artifacts, __pycache__, or google-chrome-stable_current_amd64.deb — they are ignored, not merely absent from the index"
    - "data/snapshots/ is tracked: the daily price-snapshot history cannot be backfilled, so it must not fall into the data/ ignore rule"
    - "A human has reviewed the 13 registry-flagged [SUS] packages by name and version before any install command runs in plan 01-03 or 01-04"
  artifacts:
    - path: ".gitignore"
      provides: "Root ignore rules for regenerable data, model artifacts, Python/Node caches, build output, and secrets"
      contains: "node_modules"
  key_links:
    - from: ".gitignore"
      to: "frontend/node_modules/"
      via: "ignore rule authored before plan 01-04 runs npm install, so a 200MB+ dependency tree can never enter the index"
      pattern: "node_modules"
    - from: ".gitignore"
      to: "data/snapshots/"
      via: "negation rule re-including the irreplaceable price-snapshot history under the broad data/ ignore"
      pattern: "!data/snapshots"
  prohibitions:
    - requirement_id: APIT-01
      category: privacy
      status: resolved
      verification: judgment
      statement: "MUST NOT commit a real FPL_API_KEYS value, ODDS_API_KEY value, or any other live secret into a tracked file — the initial commit is the single largest opportunity in this milestone to leak one permanently into git history"
    - requirement_id: UI-01
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT modify, delete, or take offline anything under web/ — the vanilla site stays live and authoritative until CUT-01 (Phase 7); this plan only changes what git tracks, never what the site serves"
---

<objective>
Put the repository on a tracked, ignorable footing and gate every package install this phase will
perform behind a human review.

The repo has **no initial source commit and no `.gitignore`** (verified: `git ls-files` returns only
`.planning/**` and `.claude/CLAUDE.md`; `git status` shows `api/`, `tests/`, `web/`, `config.py`,
`pytest.ini` all as `??`). Every path PATTERNS.md and RESEARCH.md cite is a real on-disk working-tree
file that git does not know about. Until that is fixed, executor commits in later plans have no
baseline to diff against, and the very first `npm install` in plan 01-04 would drop a ~200MB
`frontend/node_modules/` tree next to a 134MB Chrome `.deb` and 77MB of regenerable parquet with
nothing stopping any of it from being staged.

Purpose: make later plans' commits meaningful and safe, and satisfy the Package Legitimacy Gate
before the first `pip install` / `npm install` of the phase.
Output: a root `.gitignore`, an initial source commit, and a recorded human sign-off on the
13 `[SUS]`-flagged packages.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-test-base-layer-app-skeleton/01-RESEARCH.md
@.planning/phases/01-test-base-layer-app-skeleton/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author root .gitignore and make the initial source commit</name>
  <reversibility rating="costly">Whether `web/data/*.json` is tracked shapes every later phase: Phase 4's frozen E2E fixtures, Phase 5's CI data story, and Phase 7's parity comparison all read from it. Untracking it later is a one-line change; un-committing a year of weekly churn is a history rewrite.</reversibility>
  <files>.gitignore</files>
  <read_first>
    - .planning/phases/01-test-base-layer-app-skeleton/01-RESEARCH.md — "Environment Availability" (minimum ignore set) and "Security Domain" (the node_modules / secret-leak threat)
    - .planning/phases/01-test-base-layer-app-skeleton/01-PATTERNS.md — the "IMPORTANT — repo tracking state" preamble, which establishes that these are canonical on-disk sources, not a gitignored mirror
    - .planning/codebase/CONCERNS.md — the production-readiness gap inventory, for the repo-hygiene items Phase 5 (SEC-04) will finish
    - config.py — the path constants (ROOT, DATA_DIR, RAW_DIR, PROCESSED_DIR) that name the directories being ignored
    - scripts/daily.sh and scripts/weekly.sh — confirm which generated paths the crons write, so no cron output is accidentally tracked or accidentally ignored
  </read_first>
  <action>
    Create `.gitignore` at the repo root, then stage and commit the untracked source tree.

    Ignore rules, grouped with a one-line comment per group:
    - Python caches: `__pycache__/`, `*.py[cod]`, `.pytest_cache/`
    - Node: `node_modules/`, `frontend/node_modules/`, `frontend/dist/`, `dist/`, `*.tsbuildinfo`
    - Regenerable pipeline data: `data/raw/`, `data/processed/`, `data/cron.log`
    - Regenerable model output: `models/artifacts/`
    - Scratch and installers: `downloaded_files/`, `google-chrome-stable_current_amd64.deb`, `*.deb`
    - Secrets: `.env`, `.env.*`, `!.env.example`

    Then a re-inclusion group with a comment stating that the daily price-snapshot history cannot be
    backfilled once lost: `!data/snapshots/` and `!data/snapshots/**`. Verify the negation actually
    works — a bare `data/raw/`-style rule does not shadow it, but if you write a broader `data/`
    rule the negation will not re-include children, so keep the ignore rules directory-specific as
    listed above.

    Deliberately NOT ignored, with a comment saying why: `web/data/*.json` (the pipeline-to-product
    contract; 304KB total; Phases 4, 5 and 7 all read from it), `.github/workflows/` (SEC-04 requires
    workflows tracked), `requirements.txt`, `pytest.ini`, `README.md`, `PLAN.md`, `ROADMAP.md`,
    `IMPROVEMENTS.md`, `PROJECT_INDEX.json`, `PROJECT_INDEX.md`.

    Before committing, run `git status --porcelain` and read the output. If any entry looks like a
    credential (a file named `.env`, `secrets*`, `*.pem`, `*_key*`, or a token-shaped string inside a
    tracked config), STOP and surface it rather than committing it.

    Stage with explicit path arguments (`git add .gitignore api config.py pytest.ini requirements.txt tests web scripts data features models optimize predict backtest .github README.md PLAN.md ROADMAP.md IMPROVEMENTS.md PROJECT_INDEX.json PROJECT_INDEX.md`) — never `git add -A` — so the ignore rules are a safety net and not the only thing standing between the index and 134MB of installer. Commit with message
    `chore(01-01): track project source and add root .gitignore`.
  </action>
  <acceptance_criteria>
    - `.gitignore` exists at the repo root and contains the literal `node_modules`
    - `git ls-files api/main.py config.py pytest.ini tests/test_product.py web/assets/app.js .github/workflows/daily.yml` prints all six paths
    - `git ls-files data/snapshots | head -1` prints at least one path (price history is tracked)
    - `git check-ignore -q data/raw`, `git check-ignore -q data/processed`, `git check-ignore -q models/artifacts`, `git check-ignore -q __pycache__`, and `git check-ignore -q google-chrome-stable_current_amd64.deb` each exit 0
    - `git ls-files web/data | wc -l` is greater than 0 (the export contract is tracked)
    - `git log --oneline -1` shows the chore commit
    - Nothing under `web/` was edited: `git diff HEAD~1 --stat -- web/ | grep -c '|'` counts only additions, no modifications to existing content
  </acceptance_criteria>
  <verify>
    <automated>git ls-files api/main.py config.py pytest.ini tests/test_product.py web/assets/app.js .github/workflows/daily.yml | wc -l | grep -qx 6 &amp;&amp; git ls-files data/snapshots | grep -q . &amp;&amp; git check-ignore -q data/raw &amp;&amp; git check-ignore -q data/processed &amp;&amp; git check-ignore -q models/artifacts &amp;&amp; git check-ignore -q google-chrome-stable_current_amd64.deb &amp;&amp; echo "REPO BASELINE OK"</automated>
    <fails_when>non-zero exit, or stdout does not contain "REPO BASELINE OK" — a wc mismatch means a source path is still untracked; a check-ignore failure means a large regenerable artifact is still stageable</fails_when>
  </verify>
  <done>Every source path this phase touches is tracked in git; every regenerable or oversized artifact is ignored; the irreplaceable `data/snapshots/` price history is tracked.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Package legitimacy sign-off before the phase's first install</name>
  <what-built>
    Nothing installed yet. `01-RESEARCH.md` ran the Package Legitimacy Gate against the live npm and
    PyPI registries on 2026-08-31 and returned **13 `[SUS]` verdicts, all on the "too-new" signal**
    (each package shipped a release within ~2 weeks of the audit). Every one was then manually
    checked and found to have a verified source repo and tens-to-hundreds of millions of weekly
    downloads. Zero `[SLOP]` verdicts. Per protocol the flag still requires your sign-off before the
    first install command runs.

    | Package | Registry | Weekly downloads | Source repo | Installed by |
    |---|---|---|---|---|
    | `responses` (pin `>=0.25,<0.27`) | PyPI | stats endpoint returned null | github.com/getsentry/responses | plan 01-03 |
    | `react-router@7` | npm | 43.5M | github.com/remix-run/react-router | plan 01-04 |
    | `@tanstack/react-query@5` | npm | 65.7M | github.com/TanStack/query | plan 01-04 |
    | `vite` (pin **7.3.6**, not `latest` 8.2.2) | npm | 176.3M | github.com/vitejs/vite | plan 01-04 |
    | `@vitejs/plugin-react` (pin **5.2.0**, not 6.x) | npm | 83.6M | github.com/vitejs/vite-plugin-react | plan 01-04 |
    | `lucide-react` | npm | 97.8M | github.com/lucide-icons/lucide | plan 01-04 |
    | `typescript-eslint` | npm | 87.3M | github.com/typescript-eslint/typescript-eslint | plan 01-04 |
    | `eslint-plugin-react-refresh` | npm | 39.1M | github.com/ArnaudBarre/eslint-plugin-react-refresh | plan 01-04 |
    | `@types/node` | npm | 429.8M | github.com/DefinitelyTyped/DefinitelyTyped | plan 01-04 |
    | `vitest` | npm | 99.9M | github.com/vitest-dev/vitest | plan 01-04 |
    | `@testing-library/react` | npm | 57.1M | github.com/testing-library/react-testing-library | plan 01-04 |
    | `@testing-library/jest-dom` | npm | 63.2M | github.com/testing-library/jest-dom | plan 01-04 |

    Not flagged (clean `OK` verdicts, listed so you see the whole install set): `react`, `react-dom`,
    `typescript` (pinned **6.0.3** exactly — 7.x breaks `typescript-eslint`'s peer range
    `>=4.8.4 <6.1.0`), `tailwindcss@4`, `@tailwindcss/vite@4`, `jsdom`, `eslint`,
    `eslint-plugin-react-hooks`, `@types/react`, `@types/react-dom`.
  </what-built>
  <how-to-verify>
    Spot-check the packages you do not already recognise. For each, open the registry page and
    confirm the "Repository" link matches the Source repo column above and that the weekly-download
    order of magnitude matches:

    1. https://www.npmjs.com/package/react-router
    2. https://www.npmjs.com/package/@tanstack/react-query
    3. https://www.npmjs.com/package/vite  — also confirm 7.3.6 exists under "Versions"
    4. https://www.npmjs.com/package/@vitejs/plugin-react — confirm 5.2.0 exists under "Versions"
    5. https://www.npmjs.com/package/lucide-react
    6. https://www.npmjs.com/package/vitest
    7. https://www.npmjs.com/package/@testing-library/react
    8. https://pypi.org/project/responses/ — confirm the project links point at getsentry/responses

    Then confirm the two pins you are agreeing to, because both are deliberate downgrades from
    `latest` and both are load-bearing for Phase 5's CI:
    - `typescript@6.0.3` (not 7.x) — 7.x cannot be linted by the current `typescript-eslint`
    - `vite@7.3.6` + `@vitejs/plugin-react@5.2.0` (not Vite 8 + plugin-react 6) — plugin-react 6
      requires `vite ^8.0.0` exclusively, stacking two majors published days before the research

    If any package looks wrong, say so and name it — nothing is installed until you approve.
  </how-to-verify>
  <resume-signal>Type "approved" to authorise the install set, or name the package(s) you want removed or re-pinned</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer workstation → git history | Once a blob is committed, removing it requires a history rewrite; this plan performs the first-ever mass staging operation in this repo |
| public package registries → local `node_modules` / site-packages | `npm install` and `pip install` execute registry-controlled code (install scripts, then imported code) with the developer's full privileges |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | npm + PyPI installs in plans 01-03 / 01-04 | high | mitigate | Package-legitimacy gate: 13 `[SUS]` packages enumerated with registry, download count, and source repo; `checkpoint:human-verify` with `gate="blocking-human"` blocks every install until a human signs off (never auto-approvable, `workflow.auto_advance` ignored) |
| T-01-01 | Information Disclosure | initial `git add` of a repo with no prior `.gitignore` | high | mitigate | `.gitignore` authored *before* staging; staging uses explicit path arguments, never `git add -A`; `git status --porcelain` is read and credential-shaped entries halt the task rather than being committed |
| T-01-02 | Tampering | `frontend/node_modules/` entering the index in plan 01-04 | medium | mitigate | `node_modules/` and `frontend/node_modules/` ignore rules land in this plan, one wave before the first `npm install` |
| T-01-03 | Denial of Service | 134MB `google-chrome-stable_current_amd64.deb` + 77MB regenerable parquet in git history | medium | mitigate | Ignored here so they cannot be staged; physical removal of the `.deb` is Phase 5 `SEC-04` scope |
| T-01-04 | Tampering | irreplaceable `data/snapshots/` price history swept into a broad `data/` ignore | medium | mitigate | Ignore rules are directory-specific (`data/raw/`, `data/processed/`) plus an explicit `!data/snapshots/` re-inclusion, and an acceptance criterion asserts the path is tracked |
</threat_model>

<verification>
- `.gitignore` exists and covers Python caches, Node output, regenerable data, model artifacts, installers, and secrets
- Every analog path cited by `01-PATTERNS.md` resolves under `git ls-files`
- `data/snapshots/` and `web/data/*.json` are tracked; `data/raw/`, `data/processed/`, `models/artifacts/`, `__pycache__/`, and the Chrome `.deb` are ignored
- The package-legitimacy checkpoint was presented and answered by a human (never auto-approved)
- `web/` content is unchanged — the vanilla site still serves the current gameweek
</verification>

<success_criteria>
- `git ls-files | wc -l` grows from 30-odd `.planning` files to the full source tree
- `git check-ignore -q` exits 0 for each of `data/raw`, `data/processed`, `models/artifacts`, `__pycache__`, `google-chrome-stable_current_amd64.deb`
- A human sign-off on the 13 `[SUS]` packages is recorded in the plan SUMMARY before plans 01-03 and 01-04 execute
</success_criteria>

## Artifacts this phase produces

Symbols and files created by **Phase 1 as a whole** (listed identically in every plan so the
source-grounding pass can exclude them from drift verification):

**New files — backend**
- `.gitignore` (root)
- `tests/conftest.py`
- `tests/test_api.py`

**New Python symbols**
- `api.main._initial_state()` — factory returning the pristine `_state` dict
- `tests.conftest._reset_api_state` — autouse pytest fixture
- `tests.test_api.fake_boot`, `tests.test_api.fake_pool`, `tests.test_api.TEAMS` — deterministic builders (copied from `tests/test_product.py`, intentionally duplicated to avoid cross-test-file coupling)
- `tests.test_api.test_health_and_meta_contract`
- `tests.test_api.test_solve_squad_contract`
- `tests.test_api.test_solve_request_bounds`
- `tests.test_api.test_solve_resolution_and_rounding`
- `tests.test_api.test_team_endpoint_contract`
- `tests.test_api.test_rate_endpoint_contract`
- `tests.test_api.test_require_key_three_modes`
- `tests.test_api.test_unauthenticated_endpoints_stay_open`
- `tests.test_api.test_concurrent_solve_and_refresh`
- `tests.test_api.test_state_isolation_and_no_artifact_load`

**New files — frontend (all under `frontend/`, greenfield)**
- `package.json`, `package-lock.json`, `index.html`, `vite.config.ts`, `vitest.config.ts`
- `tsconfig.json`, `tsconfig.app.json`, `tsconfig.node.json`, `eslint.config.js`
- `src/main.tsx`, `src/router.tsx`, `src/index.css`, `src/vite-env.d.ts`
- `src/lib/api.ts`
- `src/test/setup.ts`, `src/test/harness.test.tsx`
- `src/components/PageShell.tsx`, `Spinner.tsx`, `ErrorState.tsx`, `EmptyState.tsx`, `PlaceholderPage.tsx`, `NotFoundPage.tsx`
- `src/components/ErrorState.test.tsx`, `src/components/Spinner.test.tsx`, `src/routes/routeIsolation.test.tsx`
- `src/routes/XpTable.tsx`, `Team.tsx`, `Fixtures.tsx`, `Prices.tsx`, `League.tsx`, `Scoreboard.tsx`, `Differentials.tsx`, `Methodology.tsx`

**New TypeScript symbols**
- `fetchJson<T>(path)` and `fetchApi<T>(path)` in `src/lib/api.ts`
- `MetaResponse` type in `src/lib/api.ts`
- `router` (a `createBrowserRouter` instance) in `src/router.tsx`
- `PageShell`, `Spinner`, `ErrorState`, `EmptyState`, `PlaceholderPage`, `NotFoundPage` components
- `NAV_LINKS` constant in `src/components/PageShell.tsx`

**New scripts**
- `scripts/verify_dev_proxy.sh`
- `scripts/verify_frontend_build.sh`

**Modified**
- `api/main.py` — `_initial_state()` extracted; `_state` initialised from it (pure refactor, same keys, same defaults)
- `requirements.txt` — adds `responses>=0.25,<0.27`

<output>
Create `.planning/phases/01-test-base-layer-app-skeleton/01-01-SUMMARY.md` when done. Record the
human's verbatim answer to the package-legitimacy checkpoint — plans 01-03 and 01-04 depend on it.
</output>
