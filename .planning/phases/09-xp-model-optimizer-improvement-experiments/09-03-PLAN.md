---
phase: 09-xp-model-optimizer-improvement-experiments
plan: 03
type: execute
wave: 3
depends_on: ["09-01", "09-02"]
files_modified:
  - config.py
  - models/captaincy.py
  - models/simulate.py
  - backtest/walk_forward.py
  - tests/test_experiments.py
  - IMPROVEMENTS.md
autonomous: true
requirements: []
estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The ceiling-EV lambda is chosen by a measured sweep on the honest harness, not picked by hand"
    - "The captaincy experiment has a full-replica 6-season adoption-deciding number recorded against its pre-declared criterion (D-06: capture improves by at least 2 percentage points over captain-by-mean)"
    - "The flag flips default-on if and only if that number clears the criterion, and the default-off regression test is updated in the same commit when it does (D-07)"
    - "The Monte-Carlo variant is built only when the cheap quantile variant has already proven the direction works, and its non-construction is recorded with a number rather than forgotten (D-08)"
  artifacts:
    - config.py
    - models/captaincy.py
    - models/simulate.py
    - backtest/walk_forward.py
    - tests/test_experiments.py
    - IMPROVEMENTS.md
  key_links:
    - "config.CAPT_CEILING_LAMBDA -> models/captaincy.py add_ceiling_ev() -> xp_capt_ceiling -> backtest/season.py run_season(capt_col=) -> capt_capture column"
    - "config.EXPERIMENTS['capt_mc'] -> models/simulate.py -> xp_capt_mc -> the same capt_col seam"
---

<objective>
Finish D-01's experiment 2 — captaincy / Triple-Captain ceiling EV — by tuning it, measuring it honestly, and letting the pre-declared criterion decide its fate.

Purpose: the armband is this project's largest measured single-decision gap (IMPROVEMENTS.md Phase C: hit-rate 18%, capture 57%, ~4.8 pts per gameweek), and captain-by-mean recovered only +16/season of it. Plan 09-01 built the `xp_capt_ceiling` column and proved it changes the armband. This plan finds the right lambda, runs the adoption-deciding measurement, and applies D-07's auto-adopt rule.

Output: a measured lambda in `config.py`, a full-replica 6-season capture number in IMPROVEMENTS.md, an adoption verdict applied to the flag default, and either a built `models/simulate.py` Monte-Carlo variant or a recorded reason it was not triggered.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-xp-model-optimizer-improvement-experiments/09-CONTEXT.md
@.planning/phases/09-xp-model-optimizer-improvement-experiments/09-RESEARCH.md
@.planning/phases/09-xp-model-optimizer-improvement-experiments/09-PATTERNS.md
@.planning/phases/09-xp-model-optimizer-improvement-experiments/09-01-SUMMARY.md
</context>

<criterion_reading>
D-06's captaincy criterion is "captaincy capture ≥ +2 pts absolute over captain-by-mean". The harness reports `capt_capture` as a fraction (the 2026-09-08 baseline CSV shows 0.541, 0.583, 0.614 and so on; IMPROVEMENTS.md Phase C quotes the same quantity as "capture 57%"). "+2 pts absolute" therefore means **+2 percentage points**: a 6-season-mean `capt_capture` at least 0.02 above the captain-by-mean baseline measured in plan 09-01. Every task below uses that reading; restate it in the SUMMARY so it is never silently reinterpreted.
</criterion_reading>

<tasks>

<task type="auto">
  <name>Task 1: Sweep lambda on the honest harness and pin the measured winner</name>
  <files>config.py, models/captaincy.py, backtest/walk_forward.py</files>
  <precondition>Plan 09-01's `data/processed/experiments/wf_baseline_phase9.json` exists and records the captain-by-mean baseline `capt_capture` at 5 replicas over all six seasons — every delta in this plan is measured against that number.</precondition>
  <read_first>
    - models/captaincy.py — `CEILING_LAMBDA`, `fit_ceiling_artifact`, `add_ceiling_ev` as written in plan 09-01
    - config.py — `EXPERIMENTS`, `CAPT_CEILING_LAMBDA`, `resolve_experiments` from plan 09-01
    - backtest/walk_forward.py — the `--experiments` / `--seasons` / `--tag` handling and where `capt_col_active` is resolved
    - models/intervals.py — `QUANTILES` and the guarantee that `apply_intervals` already floors `p90` at 0
    - .planning/phases/09-xp-model-optimizer-improvement-experiments/09-01-SUMMARY.md — the recorded baseline numbers and the single-season provisional reading
    - IMPROVEMENTS.md Phase C — the captain-by-mean adoption entry so the sweep reports numbers in the same terms
  </read_first>
  <action>
Add a `--capt-lambda FLOAT` option to `backtest/walk_forward.py`'s argparse that overrides `config.CAPT_CEILING_LAMBDA` for the run, is threaded into the `models.captaincy.add_ceiling_ev(..., lam=...)` call site added in plan 09-01, and is echoed both in the `[wf]` summary line and in the JSON summary. Change nothing else about the harness.

Run the sweep at D-14's fast-iterate setting — all six seasons, `--replicas 1` (replica 0 is the unjittered true optimum, so one replica means jitter off, not zero teams) — for lambda values 0.0, 0.25, 0.5, 0.75 and 1.0, each with its own `--tag capt_lam_<value>`.

Lambda 0.0 must reproduce plain captain-by-mean, which is the sweep's built-in control: if `capt_lam_0.0` does not match the baseline `capt_capture` to within floating-point noise the wiring is wrong and the whole sweep is meaningless, so stop and fix rather than reporting numbers.

Pick the winning lambda by highest 6-season-mean `capt_capture`, breaking ties toward the smaller lambda (less deviation from the adopted default). Set `config.CAPT_CEILING_LAMBDA` to that value, with an inline comment naming the sweep tag it came from and the capture it achieved. Flip no flag default in this task.
  </action>
  <verify>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m backtest.walk_forward --experiments capt_ceiling --capt-lambda 0.0 --replicas 1 --tag capt_lam_0.0</automated>
    <fails_when>non-zero exit, or the `[wf]` line does not echo `capt_lambda=0.0`</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "
import json, pathlib
base = json.loads(pathlib.Path('data/processed/experiments/wf_baseline_phase9.json').read_text())
z = json.loads(pathlib.Path('data/processed/experiments/wf_capt_lam_0.0.json').read_text())
print('baseline', base['capt_capture'], 'lambda0', z['capt_capture'])
assert abs(base['capt_capture'] - z['capt_capture']) < 0.005, 'lambda 0 does not reproduce captain-by-mean; the ceiling wiring is wrong'
"</automated>
    <fails_when>non-zero exit, or the AssertionError text about lambda 0 not reproducing captain-by-mean</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "
import json, pathlib
tags = ['0.0','0.25','0.5','0.75','1.0']
rows = []
for t in tags:
    p = pathlib.Path(f'data/processed/experiments/wf_capt_lam_{t}.json')
    assert p.exists(), f'missing sweep run for lambda {t}'
    s = json.loads(p.read_text())
    assert len(s['seasons']) == 6, s['seasons']
    rows.append((t, s['capt_capture'], s['model+chips']))
for r in rows:
    print('lambda', r[0], 'capture', r[1], 'model+chips', r[2])
import config
assert str(config.CAPT_CEILING_LAMBDA) in tags, config.CAPT_CEILING_LAMBDA
"</automated>
    <fails_when>non-zero exit, an AssertionError naming a missing sweep run or a season count other than 6, or a pinned CAPT_CEILING_LAMBDA that is not one of the five swept values</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/ruff check .</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `backtest/walk_forward.py` contains a `--capt-lambda` argparse option and echoes `capt_lambda=` in its `[wf]` line
    - Five JSON summaries `wf_capt_lam_{0.0,0.25,0.5,0.75,1.0}.json` exist, each covering all six seasons
    - `wf_capt_lam_0.0.json`'s `capt_capture` is within 0.005 of the plan 09-01 baseline
    - `config.CAPT_CEILING_LAMBDA` equals one of the five swept values and carries an inline comment naming its sweep tag and measured capture
    - `config.EXPERIMENTS['capt_ceiling']` is still `False` after this task
  </acceptance_criteria>
  <done>The lambda in config is a number the harness chose, with the run that chose it named next to it.</done>
</task>

<task type="auto">
  <name>Task 2: Adoption-deciding run and the D-07 auto-adopt verdict</name>
  <files>config.py, tests/test_experiments.py, IMPROVEMENTS.md</files>
  <precondition>Task 1 is committed and `config.CAPT_CEILING_LAMBDA` holds the swept winner.</precondition>
  <read_first>
    - config.py — the pinned lambda and the `EXPERIMENTS` registry
    - tests/test_experiments.py — the all-flags-default-off assertion written in plan 09-01, which this task must update if and only if a default flips
    - IMPROVEMENTS.md Phase F — the baseline block, the pre-declared criteria block, and the experiment results table added by plan 09-01
    - scripts/experiment_run.sh — the unattended-run launcher (D-15) and how it reports the log path
    - data/processed/experiments/wf_baseline_phase9.json — the comparison numbers
  </read_first>
  <action>
Run the adoption-deciding measurement at D-14's "full adopt" setting: all six seasons at the harness's existing default replica count of 5, with `--experiments capt_ceiling`, launched through `scripts/experiment_run.sh capt_ceiling_adopt --experiments capt_ceiling`. Poll the log for the `[wf] tag=capt_ceiling_adopt` line, then read the JSON summary.

Apply D-07's auto-adopt rule mechanically, with no separate human sign-off:
  - If the 6-season-mean `capt_capture` is at least 0.02 above the plan 09-01 baseline **and** `model+chips` has not regressed below the baseline by more than the baseline's own season-to-season standard error, set `config.EXPERIMENTS['capt_ceiling'] = True` and, in the same commit, update `tests/test_experiments.py`'s default-state assertion so it asserts the new intended default set explicitly by name rather than "everything off". Keep that test strict — it must fail if any flag not deliberately adopted becomes True.
  - Otherwise leave the flag `False`. Per D-08 the code stays merged; nothing is deleted.

Record the outcome in IMPROVEMENTS.md's Phase F experiment results table: fill the `capt_ceiling` row's `criterion`, `measured` (the capture delta and the `model+chips` delta, both with the baseline values quoted), `verdict` (adopted or rejected) and `default` (on or off) cells, and add a short prose entry underneath in the established Phase A-E style quoting the sweep table from Task 1 and the adoption run's numbers.
  </action>
  <verify>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "
import json, pathlib
b = json.loads(pathlib.Path('data/processed/experiments/wf_baseline_phase9.json').read_text())
a = json.loads(pathlib.Path('data/processed/experiments/wf_capt_ceiling_adopt.json').read_text())
assert a['replicas'] == 5 and len(a['seasons']) == 6, (a['replicas'], a['seasons'])
assert a['experiments']['capt_ceiling'] is True
d_cap = a['capt_capture'] - b['capt_capture']
d_pts = a['model+chips'] - b['model+chips']
print('capture delta', round(d_cap, 4), 'model+chips delta', d_pts, 'clears +0.02:', d_cap >= 0.02)
"</automated>
    <fails_when>non-zero exit, a missing wf_capt_ceiling_adopt.json, or an AssertionError showing the run was not 6 seasons at 5 replicas with the flag on</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "
import json, pathlib, config
b = json.loads(pathlib.Path('data/processed/experiments/wf_baseline_phase9.json').read_text())
a = json.loads(pathlib.Path('data/processed/experiments/wf_capt_ceiling_adopt.json').read_text())
clears = (a['capt_capture'] - b['capt_capture']) >= 0.02
assert config.EXPERIMENTS['capt_ceiling'] == clears, (
    'default flag state contradicts the adoption criterion: measured clears=%s but default=%s'
    % (clears, config.EXPERIMENTS['capt_ceiling']))
print('verdict consistent; capt_ceiling default =', config.EXPERIMENTS['capt_ceiling'])
"</automated>
    <fails_when>non-zero exit, or the AssertionError text about the default flag state contradicting the adoption criterion</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m pytest tests/test_experiments.py -q</automated>
    <fails_when>non-zero exit, or a summary line reporting 0 tests collected</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "
import pathlib
t = pathlib.Path('IMPROVEMENTS.md').read_text()
i = t.index('capt_ceiling')
row = t[i:t.index(chr(10), i)]
assert 'pending' not in row, 'capt_ceiling row still says pending: ' + row
print(row)
"</automated>
    <fails_when>non-zero exit, a ValueError from `.index`, or the AssertionError text showing the results row still reads pending</fails_when>
  </verify>
  <acceptance_criteria>
    - `data/processed/experiments/wf_capt_ceiling_adopt.json` records 6 seasons, 5 replicas, `capt_ceiling: true`
    - `config.EXPERIMENTS['capt_ceiling']` is True exactly when the measured capture delta is at least 0.02
    - If the flag flipped, `tests/test_experiments.py` asserts the new default set by name and still fails on any undeclared True
    - IMPROVEMENTS.md's `capt_ceiling` results row has no `pending` cell and quotes both the capture delta and the `model+chips` delta against the named baseline
    - The lambda sweep table from Task 1 appears in the Phase F prose entry
  </acceptance_criteria>
  <reversibility rating="reversible">D-07's own rating: a flag flip is one config change and the weekly product can revert to the prior default instantly.</reversibility>
  <done>Whether the ceiling armband is on or off is a consequence of a number, and both the number and the consequence are written down.</done>
</task>

<task type="auto">
  <name>Task 3: Monte-Carlo captaincy variant — build it only if the quantile variant earned it</name>
  <files>models/simulate.py, backtest/walk_forward.py, tests/test_experiments.py, IMPROVEMENTS.md</files>
  <precondition>Task 2 is committed and its capture delta is recorded in IMPROVEMENTS.md — this task's whole branch condition reads that number.</precondition>
  <read_first>
    - models/intervals.py — the whole module; `apply_intervals` is the input this variant consumes and must not refit
    - models/captaincy.py — `add_ceiling_ev`, the column-naming convention to mirror
    - backtest/walk_forward.py — where `capt_col_active` is resolved, so a second captaincy variant slots into the same seam
    - .planning/phases/09-xp-model-optimizer-improvement-experiments/09-PATTERNS.md — the `models/simulate.py` section and its "no refit, no retrain" constraint
    - .planning/research/XP-IMPROVEMENT-OPTIONS.md lines 83-96 — option 1's description of the Monte-Carlo variant and the explicit warning against re-deriving a decomposed points model
    - IMPROVEMENTS.md Phase F — Task 2's recorded capture delta, which is this task's branch condition
  </read_first>
  <action>
Read Task 2's recorded capture delta and take exactly one of two branches. State which branch was taken, and the number that selected it, in both IMPROVEMENTS.md and the plan SUMMARY.

**Branch A — delta is at least +0.02 (the quantile variant proved the direction).** Build `models/simulate.py`, per the discretion note in CONTEXT.md that the Monte-Carlo layer follows a successful quantile variant:
  - Consume `models/intervals.py`'s existing artifact through `apply_intervals`; do not fit, refit or train anything. The clean-sheet sub-model rejection (−50/season) and the ranking-loss rejection (−55/season) are both instances of "re-derive the points model" and this variant must not become a third.
  - `sample_points(pool, artifact, *, n: int, seed: int) -> np.ndarray` — draw per-player point samples by inverting the per-position, per-xp-bucket empirical quantiles the artifact already stores (piecewise-linear interpolation between the p10/p25/p75/p90 knots, clipped at 0), with a fixed seed so runs are reproducible.
  - `add_mc_capt(pool, artifact, *, n=2000, seed=0) -> pd.DataFrame` — attach `xp_capt_mc` as the expected doubled-armband value under those samples.
  - Wire `config.EXPERIMENTS['capt_mc']` into `backtest/walk_forward.py`'s `capt_col_active` resolution so `capt_mc` selects `xp_capt_mc`; make the two captaincy flags mutually exclusive with a `SystemExit` naming both if the caller enables them together.
  - Add unit tests to `tests/test_experiments.py`: sampling is reproducible under a fixed seed; samples are never negative; with a degenerate artifact whose quantile spread is zero, `xp_capt_mc` collapses to the deterministic expectation.
  - Run a fast A/B (six seasons, `--replicas 1`, `--tag capt_mc_fast`), then the full-replica adoption run (`--tag capt_mc_adopt`) and apply the same D-07 rule as Task 2 against the *currently adopted* captaincy default, not the original baseline (D-13: each experiment A/Bs against the current default config).

**Branch B — delta is below +0.02.** Do not build the Monte-Carlo variant. The research doc's own sequencing rule is that Monte-Carlo is only worth its machinery once the cheap quantile variant has shown the ceiling direction pays. Instead: leave `models/simulate.py` uncreated, leave `config.EXPERIMENTS['capt_mc']` False, and record in IMPROVEMENTS.md's Phase F table a `capt_mc` row whose `measured` cell reads "not run — gated on capt_ceiling capture delta of <observed value>, below the +0.02 trigger" and whose `verdict` reads "not triggered". This is a recorded, numbered decision in the D-08 tradition, not a silent omission — and it does not violate D-01, because `capt_mc` is a variant-ordering rule inside experiment 2, not a seventh experiment.

In both branches, remove `models/simulate.py` from `files_modified` reality only by leaving it uncreated in Branch B; do not create an empty placeholder file.
  </action>
  <verify>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "
import json, pathlib, os
b = json.loads(pathlib.Path('data/processed/experiments/wf_baseline_phase9.json').read_text())
a = json.loads(pathlib.Path('data/processed/experiments/wf_capt_ceiling_adopt.json').read_text())
triggered = (a['capt_capture'] - b['capt_capture']) >= 0.02
built = os.path.exists('models/simulate.py')
assert built == triggered, 'branch mismatch: triggered=%s but models/simulate.py exists=%s' % (triggered, built)
print('branch', 'A (built)' if triggered else 'B (not triggered)', 'delta', round(a['capt_capture'] - b['capt_capture'], 4))
"</automated>
    <fails_when>non-zero exit, or the AssertionError text about a branch mismatch between the measured trigger and whether models/simulate.py exists</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "
import pathlib
t = pathlib.Path('IMPROVEMENTS.md').read_text()
i = t.index('capt_mc')
row = t[i:t.index(chr(10), i)]
assert 'pending' not in row, 'capt_mc row still says pending: ' + row
print(row)
"</automated>
    <fails_when>non-zero exit, a ValueError from `.index` meaning no capt_mc row exists, or the AssertionError text showing the row still reads pending</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m pytest -q</automated>
    <fails_when>non-zero exit, or any test that passed before this task now failing</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/ruff check .</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `models/simulate.py` exists if and only if the recorded `capt_ceiling` capture delta is at least +0.02
    - In Branch A, `models/simulate.py` contains `def sample_points(` and `def add_mc_capt(`, contains no LightGBM import, and `python -m backtest.walk_forward --experiments capt_ceiling,capt_mc` exits non-zero with a message naming both flags
    - In Branch A, two runs with the same seed produce identical `xp_capt_mc` values
    - In Branch B, `config.EXPERIMENTS['capt_mc']` is False and no file named `models/simulate.py` exists
    - IMPROVEMENTS.md's `capt_mc` row states the branch taken and the number that selected it
  </acceptance_criteria>
  <done>Experiment 2 is closed with a number for both of its variants, and neither variant was skipped without saying why in figures.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
| Symbol | Kind | Location |
|--------|------|----------|
| `--capt-lambda` | CLI flag | backtest/walk_forward.py |
| `models/simulate.py` | new file (Branch A only) | models/ |
| `sample_points()`, `add_mc_capt()` | functions (Branch A only) | models/simulate.py |
| `xp_capt_mc` | dataframe column (Branch A only) | models/simulate.py output |
| `capt_ceiling` / `capt_mc` results rows | document rows | IMPROVEMENTS.md Phase F |
| `data/processed/experiments/wf_capt_lam_*.json`, `wf_capt_ceiling_adopt.json`, `wf_capt_mc_*.json` | run artifacts (gitignored) | data/processed/experiments/ |
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config default -> weekly product | flipping `capt_ceiling` default-on changes which player the system recommends captaining |
| held-out residual artifact -> armband decision | the interval artifact feeding the ceiling column must never carry information from the season being decided |

No network calls, no new dependencies, no user input, no secrets in this plan.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-03-01 | Tampering | temporal leakage through the ceiling interval artifact | high | mitigate | `models/captaincy.py::fit_ceiling_artifact` fits on the validation season strictly before the test season (plan 09-01), never on `models/artifacts/intervals.json` which is fit on the 2025-26 test season; the lambda-0 control in Task 1 additionally proves the ceiling path reduces exactly to captain-by-mean, so a wiring error cannot hide behind a plausible number |
| T-09-03-02 | Elevation of Privilege | an experiment flag reaching default-on without clearing its criterion | high | mitigate | Task 2's second verify command asserts `config.EXPERIMENTS['capt_ceiling']` equals the measured clears-criterion boolean and fails loudly on any mismatch; the results row cannot be left `pending` |
| T-09-03-03 | Repudiation | an unreproducible Monte-Carlo number | medium | mitigate | Branch A pins a fixed seed and a test asserts two same-seed runs are identical, mirroring D-16's fixed-seed discipline for the RL work |
| T-09-03-04 | Tampering | scope drift into a decomposed points sub-model | medium | mitigate | `models/simulate.py` consumes `models/intervals.py`'s artifact only; the acceptance criteria forbid a LightGBM import in that module, encoding the twice-rejected sub-model anti-pattern as a check rather than an intention |

No package-manager installs occur in this plan.
</threat_model>

<verification>
- Lambda 0.0 reproduces captain-by-mean, proving the ceiling column is wired through the same seam.
- The adoption-deciding run is 6 seasons at 5 replicas with the flag on, and the flag's default state matches its own criterion.
- IMPROVEMENTS.md carries a non-pending row for both `capt_ceiling` and `capt_mc`.
- `python -m pytest -q` and `ruff check .` pass.
</verification>

<success_criteria>
- Experiment 2 is measured, decided, and recorded, with the deciding number and the command that produced it both written down.
- No weekly-product file changed: `predict/live.py` and `predict/export.py` stay untouched until plan 09-10 wires whatever ended up adopted.
</success_criteria>

<output>
Create `.planning/phases/09-xp-model-optimizer-improvement-experiments/09-03-SUMMARY.md` when done. Record the full lambda sweep table, the adoption run's capture and `model+chips` deltas against the named baseline, the adoption verdict, and which Monte-Carlo branch was taken with the number that selected it.
</output>
