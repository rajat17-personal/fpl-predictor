---
phase: 09-xp-model-optimizer-improvement-experiments
plan: 04
type: execute
wave: 4
depends_on: ["09-03"]
files_modified:
  - optimize/chips.py
  - backtest/season.py
  - backtest/walk_forward.py
  - config.py
  - tests/test_chips.py
  - tests/test_experiments.py
  - IMPROVEMENTS.md
autonomous: true
requirements: []
estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Chip timing is decided by comparing an xP score for playing now against the best xP score in the visible window, not by a fixture-structure heuristic or a fixed calendar slot"
    - "A chip decision at gameweek g still sees only gameweeks g..g+visibility, preserving the B3 causal fix"
    - "The scheduler's output is provably independent of realised points: shuffling y_points leaves the schedule identical"
    - "Wildcard's isolated marginal value is measured for the first time, alongside the existing FH/BB/TC isolated values (D-06)"
    - "No chip's isolated value regresses outside its confidence interval under the new scheduler (D-06)"
  artifacts:
    - optimize/chips.py
    - backtest/season.py
    - backtest/walk_forward.py
    - config.py
    - tests/test_chips.py
    - IMPROVEMENTS.md
  key_links:
    - "config.EXPERIMENTS['chips_v2'] -> backtest/season.py run_season() -> optimize/chips.py scored_schedule() -> the schedule dict run_season already consumes"
    - "backtest/season.py chip_deltas -> backtest/walk_forward.py isolated-chip aggregation -> the FH/BB/TC/WC table in IMPROVEMENTS.md"
---

<objective>
Run D-01's experiment 3: replace the heuristic chip scheduler's decision rule with an xP-scored, causal "use now versus best visible later" comparison, and measure Wildcard's isolated value for the first time.

Purpose: `PLAN.md` names WC fixed-slot timing as a known-weak spot, and the isolated-chip harness already reports FH +19.9±11.8, BB +12.4±7.6, TC +10.0±8.3 with WC unmeasured. Better timing can only harvest a fraction of those, plus whatever WC is worth — which nobody knows yet because it has never been measured.

Output: a `scored_schedule` in `optimize/chips.py` behind the `chips_v2` flag, WC isolated-value measurement in `backtest/season.py`, a leakage test proving the schedule ignores realised points, a hysteresis sweep, and an adoption verdict recorded in IMPROVEMENTS.md.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-xp-model-optimizer-improvement-experiments/09-CONTEXT.md
@.planning/phases/09-xp-model-optimizer-improvement-experiments/09-RESEARCH.md
@.planning/phases/09-xp-model-optimizer-improvement-experiments/09-PATTERNS.md
@.planning/phases/09-xp-model-optimizer-improvement-experiments/09-01-SUMMARY.md
@.planning/phases/09-xp-model-optimizer-improvement-experiments/09-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Solver-scored causal chip scheduler behind the chips_v2 flag</name>
  <files>optimize/chips.py, backtest/season.py, config.py, tests/test_chips.py</files>
  <read_first>
    - optimize/chips.py — the whole file: `HALVES`, `WC_SLOT`, `_fixture_structure`, `causal_schedule` (the visibility-window shell to preserve verbatim) and `default_schedule`
    - backtest/season.py lines 143-160 — where `chips.causal_schedule(preds, xp_col)` is called, and that the schedule is computed once before the season loop
    - backtest/season.py lines 169-220 — how the returned `schedule` dict is consumed per gameweek and how `chip_deltas` are recorded for fh/tc/bb
    - backtest/walk_forward.py — `FIXTURE_CTX` and `leakage_safe_plan`, the frozen-form horizon construction whose discipline this scheduler must not violate
    - config.py — `EXPERIMENTS`, `CHIPS_V2_HYSTERESIS` from plan 09-01
    - .planning/research/XP-IMPROVEMENT-OPTIONS.md lines 113-126 — option 3's description and the B3 causal-fix warning
    - .planning/phases/09-xp-model-optimizer-improvement-experiments/09-PATTERNS.md — the `optimize/chips.py` section: extend in place, keep the windowing shell, replace only the decision body
  </read_first>
  <action>
Add `scored_schedule(preds, xp_col="xp_med", visibility=4, hysteresis=None, capt_col=None) -> dict[int, str]` to `optimize/chips.py`, alongside (never replacing) `causal_schedule` and `default_schedule`. D-08 forbids deleting the existing scheduler: it stays as the default until the harness says otherwise.

Keep `causal_schedule`'s structure verbatim — the same `for half, gws in HALVES.items()`, the same `remaining = {"wc", "fh", "bb", "tc"}` per half, the same `for g in half_gws: window = [w for w in half_gws if g <= w <= g + visibility]`, the same one-chip-per-gameweek and priority order wc > fh > bb > tc. Only the decision body inside the loop changes. That windowing shell is the B3 causal fix and is the single most important thing to carry over.

Replace the fixture-structure `if/elif` body with a common scoring rule. Precompute, from `preds` alone, `gwp = preds.groupby(["gw", "player_code"])[xp_col].sum()` and per gameweek the descending-sorted xP vector `v_g`, plus `_fixture_structure`'s `dgw_clubs`/`bgw_clubs` and the club count. Score each chip as expected extra points from playing it at gameweek g:
  - `tc`: the best single-player gameweek value at g, taken from `capt_col` when given (so an adopted ceiling-EV armband also drives the Triple Captain trigger) and from `xp_col` otherwise. The extra captain multiple is exactly one more copy of the best captain.
  - `bb`: the sum of ranks 12 through 15 of `v_g` — the bench of a top-15 proxy squad.
  - `fh`: the sum of ranks 1 through 11 of `v_g`, scaled by the share of clubs blanking at g (`bgw_clubs / n_clubs`) — the fraction of a normal squad that would otherwise score nothing.
  - `wc`: the visibility-window-discounted forward sum of ranks 1 through 11 across `window`, using the same 0.84 decay `backtest/walk_forward.py::_plan_col` uses, so a wildcard fires into the best visible fixture run rather than at a fixed calendar slot.
Fire chip c at g when `score_c(g) >= max(score_c(w) for w in window) - hysteresis`, with `hysteresis` defaulting to `config.CHIPS_V2_HYSTERESIS`. Add a documented end-of-half forcing rule matching v1's spirit: an unused chip at the last gameweek of its half fires there rather than being wasted.

These four proxies are the implementer's declared starting point, not received truth — the harness judges them, and if the experiment loses it stays default-off with its number recorded (D-08). Document each proxy's one-line rationale in the function docstring.

Hard constraint the code must honour and the tests must prove: `scored_schedule` may read only `xp_col`/`capt_col`, `gw`, `player_code` and `team`. It must never touch `y_points`, `actual`, `y_minutes` or any other realised-outcome column, and it must never look beyond `g + visibility`.

Wire it into `backtest/season.py`: `run_season` gains a `scheduler: str = "v1"` keyword; when `"v2"` it calls `chips.scored_schedule(preds, xp_col, capt_col=capt_col)` instead of `chips.causal_schedule(preds, xp_col)`. Chip *application* stays byte-identical — only the choice of schedule changes. In `backtest/walk_forward.py`, pass `scheduler="v2" if exp["chips_v2"] else "v1"` to every `run_season` call that has `use_chips=True`.

Create `tests/test_chips.py` covering:
  - `scored_schedule` on a synthetic `preds` frame returns at most one chip per gameweek and at most one of each chip per half
  - the schedule is unchanged when a `y_points` column is added and randomly shuffled — the leakage proof, mirroring `tests/test_leakage.py`'s recompute-and-compare style
  - the schedule is unchanged when every gameweek beyond `max(half) ` is deleted from the frame for the first half, and unchanged when gameweeks beyond `g + visibility` have their xP multiplied by 100 for a `g` that already fired — the visibility proof
  - `hysteresis` larger than the whole score range makes every chip fire at the first gameweek of its half (the degenerate control)
  - `causal_schedule`'s output on the same synthetic frame is unchanged by this task's edits (regression guard on the untouched v1 path)
  </action>
  <verify>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m pytest tests/test_chips.py -q</automated>
    <fails_when>non-zero exit, or a summary line reporting 0 tests collected</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m backtest.walk_forward --seasons 2025-26 --replicas 1 --experiments chips_v2 --tag chips_v2_smoke</automated>
    <fails_when>non-zero exit, or the `[wf]` line reports `experiments=none`</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "
import pandas as pd, config
from optimize import chips
from models.train import load_features
from backtest.walk_forward import _preds_for
te, _, _ = _preds_for(load_features(), '2025-26')
v1 = chips.causal_schedule(te, 'xp_med')
v2 = chips.scored_schedule(te, 'xp_med')
print('v1', dict(sorted(v1.items())))
print('v2', dict(sorted(v2.items())))
assert v2, 'v2 scheduled no chips at all'
assert v1 != v2, 'v2 produced an identical schedule to v1; the decision rule did not change'
for half, gws in chips.HALVES.items():
    picked = [c for g, c in v2.items() if g in gws]
    assert len(picked) == len(set(picked)), (half, picked)
"</automated>
    <fails_when>non-zero exit, the AssertionError text about v2 scheduling no chips, the AssertionError about an identical schedule, or a duplicate-chip-within-a-half assertion</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/ruff check .</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `optimize/chips.py` contains `def scored_schedule(` and still contains `def causal_schedule(` and `def default_schedule(` unmodified in behaviour
    - `scored_schedule`'s body contains the literal window expression `g <= w <= g + visibility`
    - `backtest/season.py::run_season` accepts a `scheduler` keyword and its chip-application branches are otherwise unchanged
    - `tests/test_chips.py` collects at least 5 tests and all pass, including the shuffled-`y_points` invariance test
    - A live 2025-26 comparison prints two different schedules for v1 and v2
    - `config.EXPERIMENTS['chips_v2']` is still `False` after this task
  </acceptance_criteria>
  <done>Chip timing is an xP comparison inside the same visibility window the old scheduler used, and a test proves realised points play no part in it.</done>
</task>

<task type="auto">
  <name>Task 2: Measure Wildcard's isolated value for the first time</name>
  <files>backtest/season.py, backtest/walk_forward.py, tests/test_chips.py</files>
  <read_first>
    - backtest/season.py lines 169-220 — the per-gameweek loop; note that the `if chip == "wc": squad, meta = _squad_from_pick(r); bank = ...` block appears **twice** (once before and once after the Free Hit delta measurement) and that for a `wc` gameweek the second occurrence is a redundant repeat of the first
    - backtest/season.py lines 184-189 — the Free Hit isolated-delta measurement, which is the exact structural template for the Wildcard one: score the chip pick against `optimize_gw(pool, squad, bank, 0, mode="normal", max_transfers=0)`
    - backtest/walk_forward.py lines 143-146 and 181-187 — how `chips_df.attrs["chip_deltas"]` is harvested and aggregated into the isolated-chip table
    - optimize/transfers.py — `optimize_gw`'s signature, especially `max_transfers` and the returned `starters`/`squad`/`captain_code`/`hits` keys
    - tests/test_autosub.py — the existing convention for tests that step a synthetic season
  </read_first>
  <action>
Add Wildcard to the isolated-chip harness so D-06's "isolated WC value measured (currently unmeasured)" criterion can be evaluated at all.

Inside `run_season`'s `chip in ("wc", "fh")` branch, when `record_chips` is set and `chip == "wc"`, record a delta built exactly like the existing Free Hit one: the wildcard re-pick's score for that gameweek versus the score of simply holding the current squad with zero transfers, i.e. `optimize_gw(pool, squad, bank, 0, mode="normal", max_transfers=0)` evaluated through `_score`. Append `{"gw": gw, "chip": "wc", "delta": pts - base}` to `chip_deltas`. Measure the delta **before** the squad is permanently replaced, since the baseline needs the pre-wildcard squad.

State honestly in the code comment and in the SUMMARY what this measures and what it does not: it captures the wildcard's same-gameweek value only, whereas a wildcard's real value is largely the multi-gameweek squad it leaves behind. It is still the correct first measurement, because it is the same same-week isolation the FH/BB/TC numbers already use, so the four are directly comparable; the multi-week component shows up in the whole-season `model+chips` figure.

While in this region, collapse the duplicated `if chip == "wc"` block into a single occurrence placed after the delta measurement. This must be behaviour-preserving: before editing, record the `chips_df.points.sum()` for 2025-26 with the v1 scheduler; after editing, assert it is identical. If it is not identical, the duplication was load-bearing and the edit must be reverted and the finding recorded.

Add the `wc` row to whatever the isolated-chip aggregation in `backtest/walk_forward.py` prints, and include the isolated-chip table (mean, std, count per chip) in the tagged JSON summary under a `chip_deltas` key so later plans can compare confidence intervals mechanically rather than by eye.

Add tests to `tests/test_chips.py`: a synthetic season with a scheduled wildcard produces exactly one `wc` entry in `chip_deltas`; the `wc` delta is finite and is computed against a zero-transfer hold baseline; and `chip_deltas` still contains the fh/tc/bb entries it did before.
  </action>
  <verify>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "
from models.train import load_features
from backtest.walk_forward import _preds_for
from backtest.season import run_season
te, _, _ = _preds_for(load_features(), '2025-26')
d = run_season(te, 'xp_med', use_chips=True, record_chips=True)
deltas = d.attrs['chip_deltas']
chips_seen = sorted({r['chip'] for r in deltas})
print('total', int(d.points.sum()), 'chips measured', chips_seen)
assert 'wc' in chips_seen, 'wildcard still has no isolated measurement: ' + str(chips_seen)
assert int(d.points.sum()) == 2172, 'v1 season total changed from the 2172 measured on 2026-09-08; the wc edit was not behaviour-preserving'
"</automated>
    <fails_when>non-zero exit, the AssertionError text about the wildcard having no isolated measurement, or the AssertionError about the v1 season total changing from 2172</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m pytest tests/test_chips.py tests/test_autosub.py tests/test_legality.py -q</automated>
    <fails_when>non-zero exit, or a summary line reporting 0 tests collected</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m backtest.walk_forward --seasons 2024-25,2025-26 --replicas 1 --tag wc_measure</automated>
    <fails_when>non-zero exit, or the printed isolated-chip block has no `wc` row</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "
import json, pathlib
s = json.loads(pathlib.Path('data/processed/experiments/wf_wc_measure.json').read_text())
cd = s['chip_deltas']
print(cd)
assert 'wc' in cd, cd
for chip in ('wc', 'fh', 'bb', 'tc'):
    if chip in cd:
        assert set(cd[chip]) >= {'mean', 'std', 'count'}, (chip, cd[chip])
"</automated>
    <fails_when>non-zero exit, a KeyError on `chip_deltas`, or an AssertionError showing the wc row or the mean/std/count keys are missing</fails_when>
  </verify>
  <acceptance_criteria>
    - `backtest/season.py` contains exactly one `if chip == "wc"` squad-replacement block
    - Running the 2025-26 season with the v1 scheduler still totals 2172 points, the value measured on 2026-09-08
    - `chip_deltas` contains at least one `wc` record for a season whose schedule includes a wildcard
    - The tagged JSON summary carries a `chip_deltas` object with `mean`, `std` and `count` per chip
    - The code comment and the SUMMARY both state that the wc delta is a same-gameweek isolation and what it therefore omits
  </acceptance_criteria>
  <done>All four chips now have a comparable isolated number, so D-06's no-chip-regresses criterion is checkable rather than aspirational.</done>
</task>

<task type="auto">
  <name>Task 3: Hysteresis sweep, adoption-deciding run, and verdict</name>
  <files>config.py, backtest/walk_forward.py, tests/test_experiments.py, IMPROVEMENTS.md</files>
  <precondition>Tasks 1 and 2 are committed, and `data/processed/experiments/wf_baseline_phase9.json` plus the isolated-chip confidence intervals from Task 2's `wf_wc_measure` run are available as the comparison set.</precondition>
  <read_first>
    - config.py — `CHIPS_V2_HYSTERESIS` and the `EXPERIMENTS` registry, including whatever plan 09-03 flipped
    - backtest/walk_forward.py — the `--capt-lambda` precedent from plan 09-03 for adding a scalar override option
    - IMPROVEMENTS.md Phase F — the results table, the criteria block, and the rows already filled by plans 09-02 and 09-03
    - .planning/phases/09-xp-model-optimizer-improvement-experiments/09-RESEARCH.md Open Question 1 — the recommendation to start at hysteresis 0 and sweep 1-3 points only if the zero-margin variant proves noisy
    - tests/test_experiments.py — the default-flag assertion to update if and only if this flag flips
  </read_first>
  <action>
Add a `--chips-hysteresis FLOAT` option to `backtest/walk_forward.py` overriding `config.CHIPS_V2_HYSTERESIS`, echoed in the `[wf]` line and the JSON summary, mirroring plan 09-03's `--capt-lambda`.

Sweep hysteresis 0, 1, 2 and 3 points at D-14's fast-iterate setting (six seasons, `--replicas 1`, `--experiments chips_v2`), one tag each (`chips_hys_<value>`), starting at 0 per 09-RESEARCH.md's own recommendation. Pin the winner into `config.CHIPS_V2_HYSTERESIS` by highest 6-season-mean `model+chips`, breaking ties toward the smaller margin, with an inline comment naming the sweep tag and the number.

Run the adoption-deciding measurement through `scripts/experiment_run.sh chips_v2_adopt --experiments chips_v2` at the full default replica count over all six seasons, and apply D-07's auto-adopt rule with two conditions, both of which must hold:
  - `model+chips` improves over the current default configuration's number (that is the plan 09-01 baseline, updated by whatever plan 09-03 adopted — D-13's independent-A/B-against-current-default rule), and
  - no chip's isolated value regresses outside its own confidence interval: for each of fh, bb, tc and wc, the v2 mean must be at least `baseline_mean - baseline_std` from Task 2's measured table. This is D-06's chip criterion made arithmetic.
Set `config.EXPERIMENTS['chips_v2']` accordingly and update `tests/test_experiments.py`'s declared default set in the same commit if it flips. If the criterion fails, the flag stays off and the code stays merged (D-08).

Record in IMPROVEMENTS.md's Phase F: the hysteresis sweep table; the adoption run's `model+chips` delta against the named current-default number; the four-chip isolated table with v1 and v2 side by side including their standard deviations and counts; the first-ever measured Wildcard value called out explicitly as a new fact; and the verdict plus resulting default. Fill the `chips_v2` results row with no cell left `pending`.
  </action>
  <verify>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "
import json, pathlib
for h in ('0','1','2','3'):
    p = pathlib.Path(f'data/processed/experiments/wf_chips_hys_{h}.json')
    assert p.exists(), f'missing hysteresis sweep run {h}'
    s = json.loads(p.read_text())
    assert len(s['seasons']) == 6, s['seasons']
    print('hysteresis', h, 'model+chips', s['model+chips'])
import config
assert config.CHIPS_V2_HYSTERESIS in (0.0, 1.0, 2.0, 3.0), config.CHIPS_V2_HYSTERESIS
"</automated>
    <fails_when>non-zero exit, an AssertionError naming a missing sweep run or a season count other than 6, or a pinned hysteresis outside the four swept values</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "
import json, pathlib, config
a = json.loads(pathlib.Path('data/processed/experiments/wf_chips_v2_adopt.json').read_text())
b = json.loads(pathlib.Path('data/processed/experiments/wf_wc_measure.json').read_text())
assert a['replicas'] == 5 and len(a['seasons']) == 6, (a['replicas'], a['seasons'])
assert a['experiments']['chips_v2'] is True
regressions = []
for chip, base in b['chip_deltas'].items():
    new = a['chip_deltas'].get(chip)
    assert new is not None, 'chip missing from the v2 run: ' + chip
    if new['mean'] < base['mean'] - base['std']:
        regressions.append((chip, base['mean'], base['std'], new['mean']))
print('isolated-chip regressions outside CI:', regressions)
print('chips_v2 default =', config.EXPERIMENTS['chips_v2'])
assert not (config.EXPERIMENTS['chips_v2'] and regressions), 'flag adopted despite a chip regressing outside its CI: %s' % regressions
"</automated>
    <fails_when>non-zero exit, an AssertionError naming a chip missing from the v2 run, or the AssertionError text about the flag being adopted despite a chip regressing outside its CI</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "
import pathlib
t = pathlib.Path('IMPROVEMENTS.md').read_text()
i = t.index('chips_v2')
row = t[i:t.index(chr(10), i)]
assert 'pending' not in row, 'chips_v2 row still says pending: ' + row
assert 'Wildcard' in t or 'wildcard' in t, 'the first measured wildcard value is not called out'
print(row)
"</automated>
    <fails_when>non-zero exit, a ValueError from `.index`, the AssertionError about a pending row, or the AssertionError about the wildcard value not being called out</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m pytest -q</automated>
    <fails_when>non-zero exit, or any test that passed before this task now failing</fails_when>
  </verify>
  <acceptance_criteria>
    - `backtest/walk_forward.py` contains a `--chips-hysteresis` option echoed in its `[wf]` line
    - Four hysteresis sweep summaries exist, each over all six seasons
    - `config.CHIPS_V2_HYSTERESIS` is one of the four swept values with an inline comment naming its sweep tag
    - `data/processed/experiments/wf_chips_v2_adopt.json` records 6 seasons, 5 replicas, `chips_v2: true`, and a four-chip `chip_deltas` block
    - `config.EXPERIMENTS['chips_v2']` is never True while any chip's isolated mean sits below its v1 mean minus one v1 standard deviation
    - IMPROVEMENTS.md's `chips_v2` row has no `pending` cell and the measured Wildcard value is named as a new fact
  </acceptance_criteria>
  <reversibility rating="reversible">The v1 scheduler is untouched and still the fallback; flipping the flag back restores it in one config edit (D-07/D-08).</reversibility>
  <done>Experiment 3 is decided by its own pre-declared criteria, and the project knows what a wildcard is worth.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
| Symbol | Kind | Location |
|--------|------|----------|
| `scored_schedule()` | function | optimize/chips.py |
| `scheduler` keyword | parameter on `run_season` | backtest/season.py |
| `wc` chip_deltas record | new record kind | backtest/season.py |
| `chip_deltas` summary block | JSON summary key | backtest/walk_forward.py tagged output |
| `--chips-hysteresis` | CLI flag | backtest/walk_forward.py |
| `tests/test_chips.py` | new file | tests/ |
| `chips_v2` results row | document row | IMPROVEMENTS.md Phase F |
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| future gameweek data -> a present decision | the scheduler must see only g..g+visibility, and never a realised outcome |
| config default -> weekly product | flipping `chips_v2` default-on changes when the system recommends playing a chip |

No network calls, no new dependencies, no external input, no secrets in this plan.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-04-01 | Tampering | look-ahead leaking into `scored_schedule` | high | mitigate | The B3 visibility shell is preserved verbatim and asserted by a source check on the literal window expression; a test proves the schedule is invariant to a shuffled `y_points` column and to inflating xP beyond `g + visibility`; the function's allowed input columns are stated in its docstring |
| T-09-04-02 | Tampering | the wildcard-block collapse silently changing season scoring | medium | mitigate | The edit is gated on an exact-equality assertion against the 2172-point 2025-26 v1 total measured on 2026-09-08; a mismatch reverts the edit rather than accepting a new number |
| T-09-04-03 | Elevation of Privilege | `chips_v2` adopted while a chip quietly regresses | high | mitigate | The adoption verify computes the per-chip regression set arithmetically from both runs' `chip_deltas` and fails if the flag is on while that set is non-empty — D-06's chip criterion enforced by a command, not by reading a table |
| T-09-04-04 | Repudiation | a hysteresis value in config with no traceable provenance | low | mitigate | The pinned value must be one of the four swept values and carries an inline comment naming its sweep tag; the sweep table is recorded in IMPROVEMENTS.md |

No package-manager installs occur in this plan.
</threat_model>

<verification>
- v1 and v2 produce different schedules on real 2025-26 predictions, and v1's season total is unchanged at 2172.
- The scheduler is provably blind to realised points and to gameweeks beyond its window.
- All four chips have isolated values with mean, std and count in the tagged JSON.
- The adopted default cannot contradict either half of D-06's chip criterion.
</verification>

<success_criteria>
- Experiment 3 is measured and decided, Wildcard has a number for the first time, and the heuristic scheduler is still there behind the flag.
- Nothing in the weekly product path changed; `predict/live.py` chip notes are wired only in plan 09-10 and only for whatever ended up adopted.
</success_criteria>

<output>
Create `.planning/phases/09-xp-model-optimizer-improvement-experiments/09-04-SUMMARY.md` when done. Record the hysteresis sweep table, the v1-versus-v2 isolated-chip table for all four chips with standard deviations and counts, the first measured Wildcard value, the `model+chips` delta against the named current-default number, and the adoption verdict.
</output>
