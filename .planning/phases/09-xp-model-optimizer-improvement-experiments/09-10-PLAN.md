---
phase: 09-xp-model-optimizer-improvement-experiments
plan: 10
type: execute
wave: 10
depends_on: ["09-09"]
files_modified:
  - predict/live.py
  - predict/export.py
  - config.py
  - tests/test_product.py
  - tests/test_experiments.py
  - IMPROVEMENTS.md
autonomous: true
requirements: []
estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "One final combined walk-forward run over all adopted experiments produces the single number the D-05 2,280 bar is judged against (D-13)"
    - "Whatever the harness adopted is what the weekly product actually does — an adopted flag that never reaches predict/live.py would be an adoption in name only"
    - "The weekly recommendation cycle still works end to end after the wiring, with the same export contract the site consumes"
    - "Every one of the eight experiment flags ends the phase with a number or an evidenced reason in IMPROVEMENTS.md, and none is silently dropped (D-08)"
  artifacts:
    - predict/live.py
    - predict/export.py
    - IMPROVEMENTS.md
    - tests/test_product.py
  key_links:
    - "config.EXPERIMENTS adopted set -> predict/live.py xp_capt construction and chip note -> predict/export.py -> web/data/*.json export contract"
    - "the final combined walk_forward run -> the D-05 verdict recorded in IMPROVEMENTS.md Phase F"
---

<objective>
Close the phase: run D-13's single final combined measurement, judge it against D-05's 2,280 bar, make the weekly product actually do whatever was adopted, and leave a complete ledger.

Purpose: D-13 specifies independent A/Bs followed by one final combined run, and it is that combined number the phase bar judges. Separately, an adopted flag that changes only the backtest and never the weekly recommendation would be an adoption in name only — the milestone's core value is that the weekly recommendations keep flowing and get more trustworthy, not that a backtest improved.

Output: the final combined run and the D-05 verdict; adopted behaviour wired into `predict/live.py` and `predict/export.py` with the export contract unchanged; and a complete Phase F ledger with a multi-source coverage audit.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-xp-model-optimizer-improvement-experiments/09-CONTEXT.md
@.planning/phases/09-xp-model-optimizer-improvement-experiments/09-RESEARCH.md
@.planning/phases/09-xp-model-optimizer-improvement-experiments/09-01-SUMMARY.md
@.planning/phases/09-xp-model-optimizer-improvement-experiments/09-09-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The final combined run and the D-05 verdict</name>
  <files>IMPROVEMENTS.md</files>
  <precondition>Plans 09-03 through 09-09 have each recorded an adoption verdict, so `config.EXPERIMENTS` now holds the phase's adopted set and `data/processed/experiments/wf_baseline_phase9.json` holds the reference number.</precondition>
  <read_first>
    - config.py — the current `EXPERIMENTS` state, which is the adopted set this run combines
    - IMPROVEMENTS.md Phase F — the baseline block, the criteria block, and every experiment row filled by plans 09-02 through 09-09
    - .planning/phases/09-xp-model-optimizer-improvement-experiments/09-CONTEXT.md — D-05 and D-13 in full, so the bar and the protocol are applied exactly as written
    - scripts/experiment_run.sh — the unattended launcher
    - data/processed/experiments/wf_baseline_phase9.json — the measured phase baseline
  </read_first>
  <action>
Run the one final combined measurement D-13 calls for: all six seasons, the full default replica count, with the experiment flags left at whatever the phase adopted (do not pass `--experiments`; the point is to measure the shipped default configuration), launched through `scripts/experiment_run.sh final_combined`. If the adopted set is empty, run it anyway and record that the combined configuration equals the baseline — an empty adopted set is a legitimate and informative phase outcome, not a reason to skip the measurement.

Judge D-05 arithmetically and record the judgement whichever way it lands: the 6-season mean `model+chips` from the final combined run against the bar of 2,280, alongside the measured phase baseline from plan 09-01 and the ≈2,256 figure D-05 quotes. Report the delta per season and in aggregate, and state whether the improvement is outside the noise-band direction using the harness's own season-to-season standard error rather than an eyeballed impression.

Do not adjust any flag to chase the bar. D-07's auto-adopt rule already decided each flag on its own pre-declared criterion; retro-fitting the adopted set to clear a phase bar would invert the whole discipline. If the combined number falls short of 2,280, that is the phase's honest result and it gets recorded as such — the project's own history (odds improving MAE while season points stayed within noise; multi-gameweek +337 collapsing to +40) is exactly why the bar was set as a target rather than a promise.

Write the result into IMPROVEMENTS.md Phase F as a "Final combined run" block: the command, the adopted flag set, the per-season table, the 6-season means, the delta against both the measured baseline and the 2,280 bar, and a one-paragraph honest reading.
  </action>
  <verify>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "
import json, pathlib, config
f = json.loads(pathlib.Path('data/processed/experiments/wf_final_combined.json').read_text())
b = json.loads(pathlib.Path('data/processed/experiments/wf_baseline_phase9.json').read_text())
assert f['replicas'] == 5 and len(f['seasons']) == 6, (f['replicas'], f['seasons'])
assert f['experiments'] == config.EXPERIMENTS, 'the final run did not use the shipped default flag set: %s vs %s' % (f['experiments'], config.EXPERIMENTS)
print('baseline', b['model+chips'], 'final combined', f['model+chips'], 'delta', f['model+chips'] - b['model+chips'], 'bar 2280 cleared:', f['model+chips'] >= 2280)
"</automated>
    <fails_when>non-zero exit, a missing wf_final_combined.json, an AssertionError showing the run was not 6 seasons at 5 replicas, or the AssertionError showing the run did not use the shipped default flag set</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "
import pathlib
t = pathlib.Path('IMPROVEMENTS.md').read_text()
for k in ('Final combined', '2,280'):
    assert k in t, 'Phase F does not record %s' % k
i = t.index('Final combined')
print(t[i:i+400])
"</automated>
    <fails_when>non-zero exit, or the AssertionError naming a missing final-combined block or the missing 2,280 bar</fails_when>
  </verify>
  <acceptance_criteria>
    - `data/processed/experiments/wf_final_combined.json` records 6 seasons at 5 replicas with a flag set identical to `config.EXPERIMENTS`
    - IMPROVEMENTS.md Phase F contains a "Final combined run" block with the command, the adopted flag set, the per-season table and the 6-season means
    - The D-05 verdict is stated arithmetically against both the measured baseline and the 2,280 bar
    - No flag default was changed during this task
  </acceptance_criteria>
  <done>There is one number for the phase, produced by the configuration that actually ships, and the bar was judged against it without moving either.</done>
</task>

<task type="auto">
  <name>Task 2: Make the weekly product do what the harness adopted</name>
  <files>predict/live.py, predict/export.py, tests/test_product.py</files>
  <precondition>Task 1 is committed and `config.EXPERIMENTS` holds the final adopted set. If nothing was adopted, this task's only work is the no-op verification described below — run it anyway and record that the product path was confirmed unchanged.</precondition>
  <read_first>
    - predict/live.py lines 180-215 — `_gw_pool`'s construction of `xp` and `xp_capt` (line 187-194 computes `xp_capt` from the `mean` objective, the live captain-by-mean wiring), and `build_horizon_pool`'s frozen-form-plus-known-fixture construction
    - predict/live.py lines 270-335 — the CLI, `--horizon`, and the `optimize_gw` / `pick_squad` call sites
    - predict/export.py — `export()`, `build_chips` and `_chip_note`, and which files land in `web/data/`
    - models/captaincy.py — `add_ceiling_ev` and `fit_ceiling_artifact`, and note that the live path has no walk-forward validation season, so it must use `models/intervals.py`'s shipped artifact (fit on genuinely held-out residuals) rather than refitting
    - optimize/chips.py — `causal_schedule` and `scored_schedule`, and which of the two the export's chip note should use once v2 is adopted
    - tests/test_product.py — the existing export-contract tests and what they assert about `web/data/*.json`
    - .planning/PROJECT.md — the milestone invariant that the weekly recommendations must keep flowing and every change must leave the pipeline at least as correct
  </read_first>
  <action>
For each flag that ended the phase default-on, wire the same behaviour into the weekly product. Wire nothing for a flag that stayed off — that is the whole meaning of default-off.

  - `capt_ceiling` adopted: in `predict/live.py`, build `xp_capt` as the ceiling-EV column rather than the plain mean-objective prediction, reusing `models.captaincy.add_ceiling_ev` with `config.CAPT_CEILING_LAMBDA` and `models.intervals.load_artifact()`. The live path has no validation season to refit against, and the shipped intervals artifact is fit on genuinely held-out residuals, so using it here is correct rather than a compromise — say so in a comment. If the artifact is absent, fall back to today's mean-objective `xp_capt` with a printed `[captaincy]` warning rather than failing the weekly run.
  - `capt_mc` adopted: same seam, using `models/simulate.py`'s column instead.
  - `chips_v2` adopted: `predict/export.py`'s chip note should be produced by `optimize/chips.py::scored_schedule` rather than the heuristic path, keeping `chips.json`'s existing key shape untouched.
  - `team_strength`, `understat`, `fotmob`, `fbref_v2` adopted: these are training-time feature families. Confirm the live path already picks them up through the retrained model artifact and the same `data/build_table.py` joins, and add nothing to `predict/live.py` if it does. If the live pool builder constructs its feature frame independently of `features/engineer.py`, add the missing columns there so live inference sees the same features training saw — a model trained on columns the live path cannot supply is worse than not adopting at all.
  - `rl_strategy` adopted: the chip note and the recommended transfer count in the export should come from the trained policy. If it was not adopted, change nothing.

The export contract is the hard boundary. `web/data/*.json` keys, types and shapes must not change: the React site and the vanilla site both consume them, the milestone invariant keeps the vanilla site authoritative until CUT-01, and Phase 4's frozen E2E fixtures assert this schema. Add or extend tests in `tests/test_product.py` asserting that every exported file's key set is unchanged by this plan and that a full `python -m predict.export` run still produces every file the contract names.

Run the weekly path end to end after wiring — `python -m predict.export` — and confirm it completes and writes the same set of files with the same keys. Record in the SUMMARY which flags were wired and which needed nothing.
  </action>
  <verify>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m pytest tests/test_product.py -q</automated>
    <fails_when>non-zero exit, or a summary line reporting 0 tests collected</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "
import json, pathlib
files = sorted(p.name for p in pathlib.Path('web/data').glob('*.json'))
before = {f: sorted(json.loads((pathlib.Path('web/data')/f).read_text()).keys()) if isinstance(json.loads((pathlib.Path('web/data')/f).read_text()), dict) else 'list' for f in files}
print('export contract files:', files)
for f, k in before.items():
    print(' ', f, k if k == 'list' else k[:8])
assert files, 'no exported json files found; the export contract cannot be checked'
"</automated>
    <fails_when>non-zero exit, or the AssertionError about no exported json files being found</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "
import config, inspect
from predict import live
src = inspect.getsource(live)
if config.EXPERIMENTS['capt_ceiling']:
    assert 'captaincy' in src, 'capt_ceiling is adopted but predict/live.py never references the captaincy module, so the product ignores the adoption'
if config.EXPERIMENTS['capt_mc']:
    assert 'simulate' in src, 'capt_mc is adopted but predict/live.py never references the simulate module'
from predict import export as exp_mod
esrc = inspect.getsource(exp_mod)
if config.EXPERIMENTS['chips_v2']:
    assert 'scored_schedule' in esrc or 'scored_schedule' in src, 'chips_v2 is adopted but neither the export nor the live path uses scored_schedule'
print('adopted flags are reflected in the product path:', {k: v for k, v in config.EXPERIMENTS.items() if v})
"</automated>
    <fails_when>non-zero exit, or any AssertionError naming an adopted flag whose behaviour never reaches the product path</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m pytest -q</automated>
    <fails_when>non-zero exit, or any test that passed before this task now failing</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/ruff check .</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - Every flag that is True in `config.EXPERIMENTS` has a corresponding reference in `predict/live.py` or `predict/export.py`, checkable by the verify above
    - Every flag that is False has no product-path wiring
    - The set of files in `web/data/` and their top-level key sets are unchanged by this plan
    - `tests/test_product.py` asserts the export contract's file set and key sets
    - `predict/live.py` degrades to today's mean-objective `xp_capt` with a printed warning when the intervals artifact is absent, rather than failing
    - The full test suite passes
  </acceptance_criteria>
  <reversibility rating="reversible">Each wiring is guarded by the same flag that gated the experiment; flipping the flag back restores today's product behaviour in one config edit (D-07).</reversibility>
  <done>What the harness measured is what the weekly recommendation now does, and the export contract the site depends on is byte-compatible.</done>
</task>

<task type="auto">
  <name>Task 3: Close the ledger with a complete, auditable Phase F</name>
  <files>IMPROVEMENTS.md, config.py, tests/test_experiments.py</files>
  <read_first>
    - IMPROVEMENTS.md — the whole file; Phase A through E's style, and every Phase F block written by plans 09-01 through 09-10
    - config.py — the final `EXPERIMENTS` state
    - .planning/phases/09-xp-model-optimizer-improvement-experiments/09-CONTEXT.md — all sixteen decisions, so the closing audit can be checked against them one by one
    - .planning/research/XP-IMPROVEMENT-OPTIONS.md lines 187-195 — the success-criteria block D-05/D-06 adopted verbatim
    - all nine prior SUMMARY files in this phase directory — the numbers to consolidate
  </read_first>
  <action>
Finish IMPROVEMENTS.md's Phase F so the section stands on its own for a reader who was not here.

  - Complete the experiment results table: every one of the eight flags has `criterion`, `measured`, `verdict` and `default` filled, with no `pending` cell anywhere. Any experiment that produced no data carries an evidenced reason in its `measured` cell rather than a blank.
  - Add a "Decisions audit" sub-block walking D-01 through D-16 and stating, in one line each, how the phase honoured it and where the evidence sits. Where a decision was honoured in spirit rather than literally — for example the fixed experiment order in D-01, given that the shared experiment seam and the cheapest captaincy variant were built first as plan 09-01's tracer so that every later experiment had a flag to ride — say so plainly and point at the plan that did it. An audit that only records agreement is not an audit.
  - Add a "What this phase did not resolve" sub-block: the assumptions that stayed open, the sources that produced nothing, and any experiment whose number landed inside the noise band rather than clearly on one side of it. This is the honest counterpart to the results table and the thing a future phase actually needs.
  - Cross-reference the Phase A through E entries this phase's results extend or qualify — the captain-by-mean adoption, the clean-sheet sub-model and ranking-loss rejections, Phase E's FBref finding and its low-priority note on Understat — so the ledger reads as one continuous history.

Then confirm the phase left the codebase in a coherent state: `config.EXPERIMENTS` still holds exactly the eight keys plan 09-01 declared; `tests/test_experiments.py`'s declared default set matches `config.EXPERIMENTS` exactly, so the test would fail if a flag were flipped without a deliberate edit; and every module this phase added still imports cleanly.
  </action>
  <verify>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "
import pathlib, config
t = pathlib.Path('IMPROVEMENTS.md').read_text()
for flag in config.EXPERIMENTS:
    i = t.index(flag)
    row = t[i:t.index(chr(10), i)]
    assert 'pending' not in row, '%s row still says pending' % flag
for block in ('Phase F', 'Decisions audit', 'What this phase did not resolve', 'Final combined'):
    assert block in t, 'Phase F is missing the %s block' % block
for d in ('D-01','D-05','D-06','D-07','D-08','D-09','D-12','D-13','D-16'):
    assert d in t, 'the decisions audit does not mention %s' % d
print('ledger complete')
"</automated>
    <fails_when>non-zero exit, a ValueError from `.index` meaning a flag row is missing, or any AssertionError naming a pending row, a missing block, or an unmentioned decision</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -c "
import config
keys = set(config.EXPERIMENTS)
assert keys == {'capt_ceiling','capt_mc','chips_v2','team_strength','rl_strategy','understat','fotmob','fbref_v2'}, sorted(keys)
import importlib
for m in ('models.captaincy','data.id_crosswalk','backtest.benchmark_external','data.team_strength','optimize.chips','backtest.walk_forward','backtest.season'):
    importlib.import_module(m)
print('registry intact and every core module imports:', {k: v for k, v in config.EXPERIMENTS.items()})
"</automated>
    <fails_when>non-zero exit, an AssertionError showing the flag registry gained or lost a key, or an ImportError naming a module this phase created</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/python -m pytest -q</automated>
    <fails_when>non-zero exit, or a summary line reporting 0 tests collected</fails_when>
    <automated>/home/sraja/miniconda3/envs/python314/bin/ruff check .</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - Every one of the eight flags has a complete results row with no `pending` cell
    - Phase F contains a "Final combined run" block, a "Decisions audit" block naming D-01 through D-16, and a "What this phase did not resolve" block
    - The decisions audit states plainly where a decision was honoured in spirit rather than literally, naming the plan
    - Phase A through E content is unchanged
    - `config.EXPERIMENTS` holds exactly the eight keys plan 09-01 declared, and `tests/test_experiments.py`'s declared default set matches it
    - Every module this phase created imports cleanly
  </acceptance_criteria>
  <done>A reader who arrives cold can see what was tried, what it measured, what shipped, and what is still open — in one section.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
| Symbol | Kind | Location |
|--------|------|----------|
| ceiling-EV `xp_capt` construction | modified behaviour behind `capt_ceiling` | predict/live.py |
| `scored_schedule`-driven chip note | modified behaviour behind `chips_v2` | predict/export.py |
| export-contract key-set assertions | test functions | tests/test_product.py |
| "Final combined run", "Decisions audit", "What this phase did not resolve" | document blocks | IMPROVEMENTS.md Phase F |
| `data/processed/experiments/wf_final_combined.json` | run artifact (gitignored) | data/processed/experiments/ |
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adopted flag -> weekly recommendation | the boundary where a backtest result becomes advice a person acts on |
| pipeline -> web/data export contract | the schema both the vanilla site and the React rebuild consume, frozen by Phase 4's E2E fixtures |

No network calls, no new dependencies, no secrets in this plan.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-10-01 | Tampering | the export contract changing shape and breaking both sites | high | mitigate | `tests/test_product.py` asserts the exported file set and each file's top-level key set; a full `python -m predict.export` runs as part of verification; Phase 4's frozen E2E fixtures remain the independent check |
| T-09-10-02 | Repudiation | an adopted flag that never reaches the product, so the recorded adoption overstates what ships | medium | mitigate | A verify iterates `config.EXPERIMENTS` and asserts each True flag has a corresponding reference in `predict/live.py` or `predict/export.py`, failing by name otherwise |
| T-09-10-03 | Elevation of Privilege | retro-fitting the adopted set to clear the D-05 bar | high | mitigate | The plan forbids changing any flag during the final run, an acceptance criterion states it, and the verify asserts the final run's flag set equals the shipped `config.EXPERIMENTS` — so the number cannot be produced by a configuration that is not the one shipping |
| T-09-10-04 | Denial of Service | a wiring change breaking the weekly cron path | high | mitigate | The full suite plus a real `python -m predict.export` run gate the change; the captaincy wiring degrades to today's behaviour with a printed warning when the intervals artifact is absent rather than failing the weekly run, since the milestone's core value is that recommendations keep flowing |

No package-manager installs occur in this plan.
</threat_model>

<verification>
- The final combined run used the shipped default flag set at 6 seasons and 5 replicas.
- The D-05 verdict is recorded arithmetically against both the measured baseline and the bar.
- Every adopted flag has product-path wiring; every rejected flag has none.
- The export contract's file set and key sets are unchanged, and `python -m predict.export` completes.
- Phase F has no `pending` cell, and audits D-01 through D-16.
</verification>

<success_criteria>
- The phase has one honest headline number, recorded whether or not it cleared 2,280.
- The weekly recommendations still flow, through the configuration the harness actually endorsed.
- Every experiment ends with a number or an evidenced reason, and no code was deleted (D-08).
</success_criteria>

<output>
Create `.planning/phases/09-xp-model-optimizer-improvement-experiments/09-10-SUMMARY.md` when done. Record the final combined per-season table and 6-season means, the D-05 verdict with its delta against both the measured baseline and the 2,280 bar, which flags were wired into the product and which needed nothing, and the list of items in "What this phase did not resolve".
</output>
