---
phase: quick
plan: 260913-2ir
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_cron.py
  - tests/test_crosswalk.py
  - tests/test_leakage.py
autonomous: true
requirements: [CI-GREEN]

estimate:
  tokens: 45000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A clean-checkout run (no data/processed, no data/raw, RL stack absent) of the full pytest suite reports 0 failed."
    - "tests/test_cron.py's daily.sh failure-path test passes on any machine whose interpreter is sys.executable, with no path to a developer-specific interpreter left anywhere under tests/."
    - "tests/test_crosswalk.py's unknown-name test still RUNS (not skips) on a clean checkout and asserts pd.NA, without touching data/raw or the network."
    - "tests/test_leakage.py's Transfermarkt injury-date test skips (never errors) when data/processed/player_gw.parquet is absent, and still runs unchanged when it is present."
    - "The local full suite on the developer machine keeps its pre-edit pass count, and ruff check . stays clean."
  artifacts:
    - tests/test_cron.py
    - tests/test_crosswalk.py
    - tests/test_leakage.py
  key_links:
    - "tests/test_cron.py stub_python -> sys.executable (must exist on every runner)"
    - "data.id_crosswalk.resolve_by_name -> _fpl_name_index() -> id_map.load_id_map() -> build() (the data/raw dependency severed by monkeypatch, test-side only)"
    - "tests/test_leakage.py skip guard -> RAW (data/processed/player_gw.parquet) required by gw_deadlines()"
---

<objective>
Fix the 3 remaining CI-only pytest failures. All three are environment-coupling bugs in the tests themselves: they pass locally because this machine has pipeline artifacts and a conda interpreter that CI's clean checkout lacks. They were latent until the RL-stack collection errors stopped aborting pytest before these tests ran.

Purpose: get CI to green so the Phase 07 cutover work is not gated on a red pipeline, and prove it with a faithful clean-worktree simulation rather than by pushing and waiting.
Output: three test-only edits (one commit each) plus a recorded before/after CI-simulation result.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@tests/test_cron.py
@tests/test_crosswalk.py
@tests/test_leakage.py
@data/id_crosswalk.py

**Measured facts (verified before planning — do not re-derive):**
- `data/id_crosswalk.py:218` — `_fpl_name_index` is decorated `@functools.lru_cache(maxsize=1)`. `resolve_by_name` (line 272) calls it by module-global lookup at call time, so `monkeypatch.setattr(crosswalk, "_fpl_name_index", ...)` replaces the entire cached wrapper and takes effect immediately; monkeypatch restores the original (with its cache untouched) at teardown.
- `tests/test_cron.py` does NOT import `sys` at module level (its imports are: json, os, pytest, responses, config, data.snapshot, ops.notify, test_api).
- `tests/test_leakage.py:10` already defines `RAW = config.PROCESSED_DIR / "player_gw.parquet"`; line 15 defines the `needs_data` idiom combining FEATURES + RAW.
- `config.py:10` — `ROOT = Path(__file__).resolve().parent`, so a detached worktree checkout self-roots to that worktree.
- `data/external/transfermarkt/injury_spells.csv` IS git-tracked (so the existing TRANSFERMARKT guard passes in CI); `data/processed/` and `data/raw/` are entirely untracked.
- A blocker script for the RL packages exists at `/tmp/claude-1000/-home-sraja-fpl/66e20341-54a2-459c-9b04-51955799196e/scratchpad/block_rl.py`; it currently ends in `pytest.main(["--collect-only", "-q", "tests/"])` and must be adapted to run the real suite.

**Scope guard (hard):** only the three files in `files_modified` may change. No production modules (`data/id_crosswalk.py` and `data/id_map.py` explicitly included in that prohibition), no requirements files, no `.github/workflows/ci.yml`.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Prove the CI-sim harness reproduces all 3 failures, then land the three fixes</name>
  <files>tests/test_cron.py, tests/test_crosswalk.py, tests/test_leakage.py</files>
  <precondition>`git worktree list` shows only /home/sraja/fpl; the scratchpad path /tmp/claude-1000/-home-sraja-fpl/66e20341-54a2-459c-9b04-51955799196e/scratchpad exists and contains block_rl.py.</precondition>
  <action>
Work in four ordered steps. Steps A and B produce no commits; step C produces exactly three.

**A. Record the local pre-edit baseline.** Run the full suite on this machine from /home/sraja/fpl and write down the exact summary line (passed/failed/skipped counts). This is the number step C must not regress. Do not skip this — the recorded historical figure (427 passed, 1 skipped) predates today's RL-guard work and must be re-measured, not assumed.

**B. Build the clean-checkout CI simulation and prove it reproduces exactly the 3 known failures.** Create a detached worktree at the scratchpad path with `git worktree add --detach <SIM> HEAD` — it contains only committed files, which is exactly CI's data situation (no data/processed, no data/raw). Copy the existing blocker script INTO the worktree root (so Python puts the worktree root on sys.path[0] and `import config` resolves to the worktree's own config.py, not this repo's), change its final call to run the real suite instead of collection-only, and run it with the working directory set to the worktree root. Export PYTHONPATH pointing at the worktree root as belt-and-braces against the bare-pytest sys.path issue this project already hit once in CI. Confirm the run reports exactly 3 failed, and that the three names match the three named in this plan — if it reports a different count or different names, STOP and report: the harness is not faithful and the fixes would be unverifiable.

**C. Apply the three fixes, one commit per file**, so each is independently revertable.

*C1 — tests/test_cron.py (`test_daily_sh_failure_is_non_zero_notifies_once_and_lets_later_steps_run`).* The stub interpreter's `real_python` local is pinned to a developer-specific interpreter location that does not exist on the runner, so the stub's `exec` of `-m ops.notify` fails silently, no alert record is appended, and the `len(alerts) == 1` assertion sees 0. Rebind `real_python` to `sys.executable` and add a module-level `import sys` next to the existing `import os` (the module has none today). Also wrap the interpolated value in double quotes inside the generated bash `exec` line so an interpreter path containing spaces still execs correctly. Do not otherwise restructure the test.

*C2 — tests/test_crosswalk.py (`test_resolve_by_name_returns_na_for_unknown_name`).* An unresolvable name falls through both crosswalk tiers into the historical-registry tier, which loads the full id map and needs season files the clean checkout does not have, raising ValueError from an empty concat. Add `monkeypatch` to this ONE test's signature and, before calling `resolve_by_name`, replace the module's name-index attribute with a zero-argument lambda returning an empty `pd.Series(dtype="Int64")`. That satisfies the `.map()` contract the caller uses — mapping against an empty index yields NA for every key — while keeping the assertion meaningful and the test offline and deterministic. Give the test a short docstring recording why the stand-in exists and noting that setattr on the module attribute supersedes the lru_cache wrapper wholesale. Leave every other test in the file alone; the sibling known-name test resolves at the first tier and never reaches this code path. Do NOT edit data/id_crosswalk.py or data/id_map.py. If the stand-in proves awkward in practice, the fallback is a skipif on the raw-season inputs — but prefer the stand-in, because it keeps the test RUNNING in CI instead of silently vanishing.

*C3 — tests/test_leakage.py (`test_transfermarkt_injury_dates_precede_kickoff`).* Its skipif guards only on the committed spell table, which exists in CI, so the test runs and then dies inside `gw_deadlines()`, which reads the uncommitted player_gw build artifact. Widen the guard's condition to require BOTH the spell table AND the already-defined RAW path, and extend the reason string to name both prerequisites, matching this file's existing two-condition guard idiom at line 15. Change nothing in the test body.
  </action>
  <verify>
    <automated>cd /home/sraja/fpl && /home/sraja/miniconda3/envs/python314/bin/python -m pytest -q tests/test_cron.py tests/test_crosswalk.py tests/test_leakage.py 2>&1 | tail -5</automated>
    <automated>cd /home/sraja/fpl && grep -rn "envs/python314" tests/ | wc -l   # MUST print 0 — scope is the tests/ directory only, not this plan file</automated>
    <automated>cd /home/sraja/fpl && git log --oneline -3 -- tests/ | wc -l   # MUST print 3 — one commit per fixed file</automated>
    <automated>cd /home/sraja/fpl && git status --porcelain -- data/ api/ models/ optimize/ predict/ features/ ops/ requirements*.txt .github/ | wc -l   # MUST print 0 — scope guard</automated>
  </verify>
  <done>Step B's pre-fix CI-sim run reported exactly 3 failed and the three failing node ids matched this plan's three. All three files are fixed in three separate commits. The targeted local pytest run is green, no developer-specific interpreter path survives anywhere under tests/, and no file outside the three test files is modified.</done>
</task>

<task type="auto">
  <name>Task 2: Post-fix verification loop — clean-checkout run, local parity, lint, teardown</name>
  <files>(verification only — no file edits)</files>
  <action>
Re-run the same CI simulation from Task 1 step B against the post-fix commits. The worktree from step B is pinned to the pre-fix HEAD, so either refresh it to the new HEAD or remove and recreate it — do not run the stale checkout and mistake it for a post-fix result. Confirm the suite reports 0 failed; a nonzero skipped count is expected and correct there, because the pipeline-artifact-guarded tests (including the widened leakage guard from C3) have nothing to read on a clean checkout.

Then check the one case that must NOT skip in the simulation: the cron failure-path test. Inside the simulated run the interpreter it now points at exists by construction, and the script it drives is committed, so that test must appear as PASSED, not skipped and not failed. Re-run it in the worktree with verbose output to see its explicit outcome rather than inferring it from the summary line.

Then confirm no local regression: run the full suite on this machine and compare its passed count against Task 1 step A's recorded baseline — it must match, since all three edits only widen environment tolerance and none of them change behavior on a machine that has the artifacts. Run the project linter over the repository and confirm it stays clean (the new `import sys` must be genuinely used, not a dead import).

Finally tear the simulation down with a forced worktree removal and confirm the worktree list returns to the single real checkout. The simulated checkout is a throwaway: never run GSD worktree tooling, planning commands, or commits against it.
  </action>
  <verify>
    <automated>cd /home/sraja/fpl && /home/sraja/miniconda3/envs/python314/bin/python -m pytest -q 2>&1 | tail -3</automated>
    <automated>cd /home/sraja/fpl && /home/sraja/miniconda3/envs/python314/bin/ruff check . 2>&1 | tail -3</automated>
    <automated>cd /home/sraja/fpl && git worktree list | wc -l   # MUST print 1 after teardown</automated>
  </verify>
  <done>The post-fix CI simulation reports 0 failed; the cron failure-path test is explicitly PASSED (not skipped) inside it; the local full-suite passed count equals the Task 1 step A baseline; ruff reports no findings; the simulation worktree is removed and `git worktree list` shows only /home/sraja/fpl. Both the pre-fix (3 failed) and post-fix (0 failed) simulation summary lines are quoted verbatim in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process -> spawned shell (`scripts/daily.sh` via subprocess) | An interpreter path is interpolated into a generated bash script and exec'd |
| test process -> filesystem/network (crosswalk fallback tier) | The unpatched fallback would read on-disk season files or bootstrap live data |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-quick-01 | Tampering | tests/test_cron.py generated stub script | low | mitigate | Interpolate only `sys.executable` (process-owned, not attacker-supplied) and double-quote it in the generated `exec` line so a path with spaces cannot split into extra argv words |
| T-quick-02 | Information disclosure | tests/test_cron.py | low | mitigate | Removing the hardcoded developer home path stops leaking a local filesystem layout into a public CI log |
| T-quick-03 | Denial of service | tests/test_crosswalk.py fallback tier | low | mitigate | The test-only stand-in keeps the unknown-name assertion fully offline — no network bootstrap from a CI runner |
| T-quick-SC | Tampering | npm/pip/cargo installs | n/a | accept | No packages are installed by this plan; no install task exists, so the package-legitimacy gate does not apply |
</threat_model>

<verification>
- Clean-checkout simulation: 3 failed before the fixes, 0 failed after (skips expected).
- Local full suite: passed count unchanged from the pre-edit baseline measured in Task 1 step A.
- `ruff check .`: clean.
- `git worktree list`: one entry.
- Exactly three commits, each touching exactly one test file; zero changes outside `tests/`.
</verification>

<success_criteria>
All three named CI-only failures pass or correctly skip in a checkout containing only committed files, with no developer-machine coupling left under `tests/`, no production code touched, and no local regression.
</success_criteria>

<output>
Create `.planning/quick/260913-2ir-fix-3-ci-only-pytest-failures-from-envir/260913-2ir-SUMMARY.md` when done, quoting both simulation summary lines verbatim.
</output>
