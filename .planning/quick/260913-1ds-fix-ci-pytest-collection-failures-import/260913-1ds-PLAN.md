---
phase: quick
plan: 260913-1ds
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_rl_env.py
  - tests/test_bracket.py
  - tests/test_bracket_deep.py
autonomous: true
requirements: []
user_setup: []

estimate:
  tokens: 45000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A pytest run in an environment without gymnasium/torch/stable_baselines3/sb3_contrib collects every test file with ZERO collection errors."
    - "The 16 torch-free tests in tests/test_bracket.py still RUN (not skip) when torch is absent — they are the CI-visible half of that file."
    - "A local run with the full RL stack installed still executes all 35 tests across the affected files with the same pass count as before (9 in test_rl_env.py, 26 across test_bracket.py + test_bracket_deep.py)."
    - "No production module, requirements file, or CI workflow is modified — the fix is confined to test files."
  artifacts:
    - tests/test_rl_env.py
    - tests/test_bracket.py
    - tests/test_bracket_deep.py
  key_links:
    - "pytest.importorskip guards must run BEFORE the first import of the guarded package in each module, or collection still crashes."
    - "tests/test_bracket_deep.py must carry its own needs_data marker and df_full fixture — they are defined in tests/test_bracket.py and are not shared via conftest.py."
---

<objective>
Stop pytest COLLECTION from crashing in CI, where the dev-only RL/deep-learning stack (gymnasium, torch, stable_baselines3, sb3_contrib) is deliberately not installed per Phase 9 decision D-09.

Two test modules import those packages at module level, so pytest aborts the entire run before executing anything:
- `tests/test_rl_env.py:17` — `from gymnasium import spaces`
- `tests/test_bracket.py:342` — `import torch`

Purpose: CI currently cannot run the Python suite at all. Fixing collection restores the 393 tests that already collect plus the 16 torch-free bracket tests that are currently lost to the module-level crash.

Output: guarded `tests/test_rl_env.py`, a trimmed torch-free `tests/test_bracket.py`, and a new torch-guarded `tests/test_bracket_deep.py`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

@tests/test_rl_env.py
@tests/test_bracket.py
</context>

<measured_facts>
Every number below was measured against the working tree at plan time. Do not re-derive; verify against them.

**CORRECTION to the task brief — read this before touching tests/test_bracket.py.**
The brief stated the torch-dependent region begins at line 375 with `from models.bracket import deep as bracket_deep`. That is WRONG. The real first torch import is at **line 342 (`import torch  # noqa: E402`)**, and line 344 (`from models.bracket import sequence as bracket_sequence`) also pulls torch in transitively (`models/bracket/sequence.py:20` has a module-level `import torch`). Reproduced live: the collection traceback blames `tests/test_bracket.py:342`, not 375. Splitting at line 375 would leave the collection error unfixed.

**The real split boundary in tests/test_bracket.py is line 339** — the section banner `# --- Phase 10 plan 10-11: the leakage-safe padded/masked sequence builder (D-20/D-21) ---`. Lines 1–337 are torch-free; lines 339–559 (EOF) are the torch region.

| Fact | Measured value |
|---|---|
| CI-sim collection today | 393 tests collected, 2 errors (test_bracket.py, test_rl_env.py) |
| CI-sim collection target after fix | 409 tests collected (393 + the 16 torch-free bracket tests), 0 errors |
| Torch-free tests in tests/test_bracket.py lines 1–337 | 16 |
| Torch tests in tests/test_bracket.py lines 339–559 | 10 |
| Tests in tests/test_rl_env.py | 9 (all 9 pass locally) |
| Combined baseline for both files | 35 collected |

**Split cleanliness (already verified — the boundary is clean):**
- `numpy` is used ONLY in the moved region; there are zero `np.` references in lines 1–337.
- The moved region does NOT reference the `bracket_gate` module, `te_2025`, `_ARTIFACT_COLS`, `run_season`, `_preds_for`, or `load_external_predictions`. (A grep appears to hit `bracket_gate` in the moved block, but every hit is the literal filename string `bracket_gate_*.json`, never the module identifier.)
- The moved region DOES reference `needs_data`, `df_full`, `config`, `pd`, and `json` — all of which must be re-established in the new file.
- The head region imports `json` locally inside a test body (line 288), so removing the moved region's module-level `import json` (line 457) does not break it.
- `grep` confirms no workflow, script, or other test references `tests/test_bracket.py` or `tests/test_rl_env.py` by name, so adding a third file breaks no caller.

**Lint constraint:** `ruff.toml` selects `["E4", "E7", "E9", "F"]`. **E402 (module-import-not-at-top-of-file) is inside the E4 group.** Any import placed after a `pytest.importorskip(...)` call MUST carry a trailing `# noqa: E402`, matching the convention already used throughout `tests/test_bracket.py`. `ruff check` currently passes clean on both files and must still pass clean after the change.

**Why a whole-file skip is correct for tests/test_rl_env.py but wrong for tests/test_bracket.py:** all 9 rl_env tests need the RL stack (6 need `optimize.rl_env`, 3 already call `pytest.importorskip("sb3_contrib")`), so nothing of CI value is lost. By contrast, 16 of the 26 bracket tests are torch-free and MUST keep running in CI.
</measured_facts>

<tasks>

<task type="tracer">
  <name>Task 1: Guard tests/test_rl_env.py behind a module-level gymnasium importorskip</name>
  <files>tests/test_rl_env.py</files>
  <precondition>The CI simulation blocker script exists at /tmp/claude-1000/-home-sraja-fpl/66e20341-54a2-459c-9b04-51955799196e/scratchpad/block_rl.py. If it is missing, recreate it as a meta-path finder that raises ImportError for any module whose root package is one of gymnasium, torch, stable_baselines3, sb3_contrib, installs itself at sys.meta_path[0], then calls pytest.main(["--collect-only", "-q", "tests/"]) and exits with its return code.</precondition>
  <action>
In tests/test_rl_env.py, insert a module-level `pytest.importorskip("gymnasium")` call so it executes BEFORE the first gymnasium import.

Placement: keep `from __future__ import annotations` first and the third-party stdlib-safe imports (`numpy as np`, `pandas as pd`, `pytest`) where they are. Put the importorskip call on its own line immediately after the `import pytest` line, separated by a blank line, with a short comment stating that gymnasium ships only in the dev-only requirements-rl.txt (D-09) so CI must skip this module rather than crash collection.

Then move `from gymnasium import spaces` (currently line 17) and `from optimize.rl_env import CHIPS_ORDER, TRANSFER_CAPS, FplStrategyEnv` (currently line 20) so both sit AFTER the importorskip call, and append a trailing `# noqa: E402` to each of them plus to any other import that now follows the guard (`import config`, `from backtest import season as season_mod`). E402 is inside ruff's selected E4 group, so an unsuppressed post-guard import fails the CI lint gate.

The single gymnasium guard is sufficient for both moved imports: `optimize/rl_env.py` imports gymnasium at its own module level, so it cannot be reached when gymnasium is absent.

Leave every test function body untouched — in particular do not remove the three existing `pytest.importorskip("sb3_contrib", minversion=None)` calls, which guard a different package.
  </action>
  <verify>
    <automated>cd /home/sraja/fpl && /home/sraja/miniconda3/envs/python314/bin/python -m pytest tests/test_rl_env.py -q 2>&1 | tail -3 | command grep -q "9 passed"</automated>
    <automated>cd /home/sraja/fpl && /home/sraja/miniconda3/envs/python314/bin/python -m ruff check tests/test_rl_env.py</automated>
    <automated>cd /home/sraja/fpl && ! /home/sraja/miniconda3/envs/python314/bin/python /tmp/claude-1000/-home-sraja-fpl/66e20341-54a2-459c-9b04-51955799196e/scratchpad/block_rl.py 2>&1 | command grep -q "ERROR tests/test_rl_env.py"</automated>
  </verify>
  <done>
- `python -m pytest tests/test_rl_env.py -q` still reports 9 passed locally (the RL stack IS installed here, so the guard is a no-op).
- `ruff check tests/test_rl_env.py` passes clean.
- Under the CI simulation blocker, `tests/test_rl_env.py` no longer appears in the `short test summary info` ERROR list; exactly one collection error remains (`tests/test_bracket.py`), down from two.
  </done>
</task>

<task type="auto">
  <name>Task 2: Split the torch region of tests/test_bracket.py into a guarded tests/test_bracket_deep.py</name>
  <files>tests/test_bracket.py, tests/test_bracket_deep.py</files>
  <action>
Move lines 339–559 of tests/test_bracket.py (from the `# --- Phase 10 plan 10-11: the leakage-safe padded/masked sequence builder (D-20/D-21) ---` banner through end of file) into a NEW file tests/test_bracket_deep.py, guarded by a module-level torch importorskip. Do NOT add a module-level guard to tests/test_bracket.py itself — that would skip its 16 torch-free tests, which are exactly the ones CI needs.

In tests/test_bracket.py: delete lines 339–559 and the trailing blank lines that preceded the banner, leaving the file ending after the last torch-free test (`test_gate_records_the_in_sample_eval_split_label`). Change nothing above line 338. The head's existing imports all stay — `numpy` was never imported in the head, and the head's `json` usage is a function-local import, so no import cleanup is needed.

In the new tests/test_bracket_deep.py, write in this order:
1. A module docstring explaining this file holds the torch-dependent half of the bracket suite, split out of tests/test_bracket.py so that file's torch-free tests keep running in CI, where torch ships only in the dev-only requirements-rl.txt / requirements-experiments lockfile (D-09).
2. `from __future__ import annotations`
3. Torch-free imports: `json`, `pandas as pd`, `pytest`, `config`
4. `pytest.importorskip("torch")` on its own line, with a comment naming the CI-skip reason.
5. The torch-dependent imports, each carrying `# noqa: E402`: `numpy as np`, `torch`, and the four bracket submodules `sequence as bracket_sequence`, `deep as bracket_deep`, `recurrent as bracket_recurrent`, `transformer as bracket_transformer`, `registry as bracket_registry`. Also `from models.train import load_features  # noqa: E402` for the fixture.
6. Re-establish the two shared helpers the moved tests depend on, copied in behavior from tests/test_bracket.py lines 21–31: module constants `FEATURES = config.PROCESSED_DIR / "features.parquet"` and `RAW = config.PROCESSED_DIR / "player_gw.parquet"`, the `needs_data` skipif marker with the same "run the data pipeline first" reason, and the module-scoped `df_full` fixture returning `load_features()`.
7. All 10 moved test functions, bodies verbatim, keeping their `# ---` section banners and their `@needs_data` / `@pytest.fixture` decorators intact.

Delete the now-redundant module-level import lines that were interleaved in the moved region (the old lines 341–344, 457, and 459–461) since step 5 hoists them to the top of the new file; their `# noqa: E402` suppressions carry over to the hoisted forms. Keep function-local imports inside test bodies exactly as they are (for example the `import json` in `test_granularity_bracket_writes_gate_schema` and the `import numpy as np` / `from backtest.walk_forward import DATA_SEASONS` block inside `test_exported_bundle_roundtrips_to_the_same_tensors`) — they are harmless shadows and rewriting them is out of scope.

Do not modify models/bracket/deep.py, models/bracket/sequence.py, optimize/rl_env.py, optimize/rl_train.py, any requirements*.txt, or .github/workflows/ci.yml.
  </action>
  <verify>
    <automated>cd /home/sraja/fpl && /home/sraja/miniconda3/envs/python314/bin/python -m pytest tests/test_bracket.py --collect-only -q 2>&1 | tail -2 | command grep -q "16 tests collected"</automated>
    <automated>cd /home/sraja/fpl && /home/sraja/miniconda3/envs/python314/bin/python -m pytest tests/test_bracket_deep.py --collect-only -q 2>&1 | tail -2 | command grep -q "10 tests collected"</automated>
    <automated>cd /home/sraja/fpl && /home/sraja/miniconda3/envs/python314/bin/python -m ruff check tests/test_bracket.py tests/test_bracket_deep.py</automated>
    <automated>cd /home/sraja/fpl && /home/sraja/miniconda3/envs/python314/bin/python /tmp/claude-1000/-home-sraja-fpl/66e20341-54a2-459c-9b04-51955799196e/scratchpad/block_rl.py 2>&1 | tail -3 | command grep -q "409 tests collected"</automated>
    <automated>cd /home/sraja/fpl && ! /home/sraja/miniconda3/envs/python314/bin/python /tmp/claude-1000/-home-sraja-fpl/66e20341-54a2-459c-9b04-51955799196e/scratchpad/block_rl.py 2>&1 | command grep -q "error"</automated>
  </verify>
  <done>
- `tests/test_bracket.py` collects exactly 16 tests; `tests/test_bracket_deep.py` collects exactly 10; together 26, unchanged from the pre-split total for that file.
- `ruff check` passes clean on both files (no E402, no F401 unused-import, no F821 undefined-name).
- Under the CI simulation blocker: **0 collection errors** and **409 tests collected** (up from 393 collected + 2 errors). `tests/test_bracket.py`'s 16 tests are among the 409; `tests/test_bracket_deep.py` and `tests/test_rl_env.py` are skipped at module level, contributing 0 collected tests and 0 errors.
- `git status` shows exactly three changed/added paths, all under `tests/`.
  </done>
</task>

</tasks>

<verification>
Both directions must be proven — a guard that fixes CI but silently disables tests locally is a regression, not a fix.

**Direction A — CI simulation (packages ABSENT):**
```
cd /home/sraja/fpl && /home/sraja/miniconda3/envs/python314/bin/python \
  /tmp/claude-1000/-home-sraja-fpl/66e20341-54a2-459c-9b04-51955799196e/scratchpad/block_rl.py
```
Required: `409 tests collected` with NO `errors` in the summary line, and no `ERROR tests/...` lines. Before the fix this prints `393 tests collected, 2 errors`.

**Direction B — real local run (packages PRESENT):**
```
cd /home/sraja/fpl && /home/sraja/miniconda3/envs/python314/bin/python -m pytest -q
```
Required: the same pass/skip totals as the pre-change baseline, which was re-measured at plan time on this exact working tree: **`427 passed, 1 skipped`**. The guards must be pure no-ops locally — zero NEW skips, since gymnasium, torch, and sb3_contrib are all installed in the `python314` conda env. If the total moves at all, the split dropped or duplicated a test and must be corrected before commit.

This run takes roughly **5 minutes** (289s measured at plan time — it trains real LightGBM models). That is normal, not a hang; do not kill it early.

**Direction C — lint gate (the same gate CI runs):**
```
cd /home/sraja/fpl && /home/sraja/miniconda3/envs/python314/bin/python -m ruff check .
```
Required: `All checks passed!`

**Scope guard:**
```
cd /home/sraja/fpl && git status --porcelain
```
Required: only `tests/test_rl_env.py`, `tests/test_bracket.py`, and `tests/test_bracket_deep.py` appear as modified/added among tracked source. No production module, requirements file, or workflow file may appear.
</verification>

<success_criteria>
- CI simulation: 0 pytest collection errors, 409 tests collected (was 393 + 2 errors).
- The 16 torch-free tests in tests/test_bracket.py run — not skip — when torch is absent.
- Local full suite unchanged at 427 passed, 1 skipped.
- `ruff check .` clean.
- Exactly three files touched, all under `tests/`.
</success_criteria>

<output>
Create `.planning/quick/260913-1ds-fix-ci-pytest-collection-failures-import/260913-1ds-SUMMARY.md` when done.

Record in the summary: the corrected split boundary (line 339 / `import torch` at 342, NOT the briefed line 375), the measured before/after CI-sim collection numbers, and confirmation that the local suite total was unchanged.
</output>
