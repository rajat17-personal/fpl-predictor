---
phase: quick-260911-7fy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_bracket.py
  - .planning/WINDOWS.md
  - data/processed/experiments/bracket_gate_mlp.json          # gitignored runtime artifact
  - data/processed/experiments/bracket_search_mlp_pooled.json # gitignored runtime artifact
  - data/processed/experiments/bracket_search_mlp_per_position.json # gitignored runtime artifact
autonomous: true
requirements: [WINDOW-6]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "WINDOW-6: running the full pytest suite leaves data/processed/experiments/bracket_gate_mlp.json byte-identical — the measured plan 10-11 result survives every test run, with no manual restore."
    - "The granularity-bracket schema test still proves exactly what it was written to prove (the deep-candidate gate result's key set is a superset of bracket_gate_lgbm.json's, plus granularity + granularity_scores) while every artifact it writes lands under tmp_path."
    - "The live bracket_gate_mlp.json holds a REAL measured gate result (granularity pooled, played-only Spearman 0.3942), produced by an actual run_granularity_bracket('mlp') call — never hand-written to match a remembered number."
    - "The two real MLP search logs (bracket_search_mlp_pooled.json, bracket_search_mlp_per_position.json) hold their real 12-config searches again, not the single toy config the test left behind."
    - "No stray unit-test search artifact remains in data/processed/experiments/, and no test can recreate one."
    - "WINDOWS.md entry 6 reads as resolved."
  artifacts:
    - "tests/test_bracket.py — test_granularity_bracket_writes_gate_schema and test_mlp_search_respects_budget both redirect config.EXPERIMENTS_DIR to tmp_path"
    - "data/processed/experiments/bracket_gate_mlp.json — real measured pooled result"
    - ".planning/WINDOWS.md — entry 6 status fixed"
  key_links:
    - "models/bracket/deep.py resolves config.EXPERIMENTS_DIR at CALL time (deep.py:343-344 for search logs, deep.py:529-530 for the gate file), so monkeypatch.setattr(config, 'EXPERIMENTS_DIR', tmp_path) redirects both write sites — the same mechanism tests/test_bracket.py:226 already relies on."
    - "tests/test_bracket.py:352 reads the lgbm baseline OUT of config.EXPERIMENTS_DIR. That read must be captured from the REAL directory before the monkeypatch is applied, or the fix breaks the test it is meant to preserve."
---

<objective>
Stop `tests/test_bracket.py::test_granularity_bracket_writes_gate_schema` from overwriting the live, measured `data/processed/experiments/bracket_gate_mlp.json` with its monkeypatched toy result on every pytest run, then restore that file to the real plan 10-11 measurement — closing WINDOW-6 in `.planning/WINDOWS.md`.

Purpose: `bracket_gate_mlp.json` is the measured deliverable of plan 10-11 and the input plan 10-14's candidate ledger reads. It was manually restored three times during plan 10-13 because the suite clobbers it. Test runs must be side-effect-free on measured artifacts.
Output: An isolated test, a restored real gate result, and WINDOW-6 marked fixed.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/WINDOWS.md
@tests/test_bracket.py
@models/bracket/deep.py
</context>

<environment_facts>
Observed live at planning time — do not re-derive, but do re-check the Task 2 precondition:

- Python for ALL commands: `/home/sraja/miniconda3/envs/python314/bin/python` (conda env `python314`). Never `python`/`python3`.
- `pytest.ini` has no `rootdir`/`pythonpath` entry and `tests/conftest.py` does not touch `sys.path`. `import config` only resolves because `python -m pytest` prepends the CWD. **Every pytest command must run from `/home/sraja/fpl` and must use `python -m pytest`, never a bare `pytest`.**
- `data/processed/` is gitignored (`.gitignore:19`). `bracket_gate_mlp.json` is UNTRACKED — there is no `git restore` path for it. Only `tests/test_bracket.py` and `.planning/WINDOWS.md` are committable here.
- `models/bracket/deep.py` has no `__main__` block; the real gate is invoked in-process via `from models.bracket.deep import run_granularity_bracket`.
- torch 2.12.0+cu130 with `torch.cuda.is_available() == True`; `data/processed/features.parquet` present (33 MB, 2026-09-10).
- The authentic plan 10-11 measurement is preserved in `data/processed/experiments/bracket_mlp_gate_final-20260910T190818Z.log` (its final `RESULT {...}` line) and in `.planning/phases/10-xp-experiment-follow-ups/10-11-SUMMARY.md`. Treat that log as the read-only oracle: `granularity=pooled`, `spearman_xp_med=0.3942`, `mae_xp_med=1.7805`, `n_played_rows=11566`, `granularity_scores={per_position: 0.3924, pooled: 0.3942}`, `status=ok`, `val_season=2024-25`, winning pooled config `hidden=(64,32) dropout=0.1 lr=0.002 weight_decay=0.0` (val_loss 1.6524). The 10-11 SUMMARY records this run as deterministic (seed 0) and byte-identical across two executions.
</environment_facts>

<tasks>

<task type="tracer">
  <name>Task 1: Redirect the bracket tests' artifact writes to tmp_path, end-to-end</name>
  <files>tests/test_bracket.py</files>
  <action>
Two tests in this file write into the live `config.EXPERIMENTS_DIR`. Give both the isolation pattern already used at `tests/test_bracket.py:226` (`monkeypatch.setattr(config, "EXPERIMENTS_DIR", tmp_path)`).

`test_granularity_bracket_writes_gate_schema` (currently line 334, signature `(monkeypatch, df_full)`):
- Change the signature to `(monkeypatch, tmp_path, df_full)`.
- READ THE BASELINE FIRST. The existing body resolves `baseline_path = config.EXPERIMENTS_DIR / "bracket_gate_lgbm.json"` AFTER the run. That file lives only in the real experiments directory, so a naive monkeypatch makes the existing `assert baseline_path.exists()` fail. Capture the baseline path (or its parsed JSON) from the real `config.EXPERIMENTS_DIR` at the top of the test, BEFORE any monkeypatch of that attribute, then keep using the captured value for the key-superset assertions. Leave the assertion text and its "run plan 10-10's gate first" guidance intact.
- Apply `monkeypatch.setattr(config, "EXPERIMENTS_DIR", tmp_path)` after that capture and before the `run_granularity_bracket("mlp", df=df_full)` call, alongside the existing `_search_space` / `_SEARCH_MAX_EPOCHS` / `_FINAL_MAX_EPOCHS` monkeypatches.
- Add a positive isolation assertion after the run: the gate file and both per-granularity search logs the run produced now exist under `tmp_path` (`bracket_gate_mlp.json`, `bracket_search_mlp_per_position.json`, `bracket_search_mlp_pooled.json`), matching how line 233 asserts its own tmp_path write.
- Keep every existing assertion (missing-key superset check, `granularity` / `granularity_scores` presence, the `{per_position, pooled}` key-set check) unchanged.

`test_mlp_search_respects_budget` (currently line 316, signature `(monkeypatch)`): same defect class — its monkeypatched `"unit_test"` granularity makes `run_search` write `bracket_search_mlp_unit_test.json` into the live directory via `models/bracket/deep.py:344`. Add `tmp_path` to the signature and the same `monkeypatch.setattr(config, "EXPERIMENTS_DIR", tmp_path)` as the first statement. Its existing budget assertions stay as they are.

Do NOT touch the module-scoped `gate_lgbm_result` fixture (line ~172). It performs a genuine full-training `run_gate("lgbm")` and writes the real measured `bracket_gate_lgbm.json`, which the granularity test reads as its baseline — that write is correct, not pollution.

`config` is already imported in this module; add no new imports beyond what the edits need.
  </action>
  <verify>
    <automated>cd /home/sraja/fpl && B=$(sha256sum data/processed/experiments/bracket_gate_mlp.json data/processed/experiments/bracket_search_mlp_pooled.json data/processed/experiments/bracket_search_mlp_per_position.json) && /home/sraja/miniconda3/envs/python314/bin/python -m pytest -q tests/test_bracket.py && A=$(sha256sum data/processed/experiments/bracket_gate_mlp.json data/processed/experiments/bracket_search_mlp_pooled.json data/processed/experiments/bracket_search_mlp_per_position.json) && [ "$B" = "$A" ] && [ "$(grep -c 'monkeypatch.setattr(config, "EXPERIMENTS_DIR", tmp_path)' tests/test_bracket.py)" = "3" ] && echo ISOLATED</automated>
  </verify>
  <done>`tests/test_bracket.py` passes in full, all three live MLP experiment JSONs are byte-identical before and after that run (whatever they contained going in), and three tests now redirect `config.EXPERIMENTS_DIR` to `tmp_path`.</done>
</task>

<task type="auto">
  <name>Task 2: Restore bracket_gate_mlp.json by re-running the real gate</name>
  <precondition>`/home/sraja/miniconda3/envs/python314/bin/python -c "import torch; assert torch.cuda.is_available()"` succeeds and `data/processed/features.parquet` exists — the real gate run needs both.</precondition>
  <reversibility rating="reversible">Overwrites a gitignored runtime artifact whose authentic measurement is independently preserved in `bracket_mlp_gate_final-20260910T190818Z.log`.</reversibility>
  <files>data/processed/experiments/bracket_gate_mlp.json, data/processed/experiments/bracket_search_mlp_pooled.json, data/processed/experiments/bracket_search_mlp_per_position.json</files>
  <action>
Regenerate the real plan 10-11 measurement rather than hand-writing numbers — the file must hold a value that was measured, and the re-run also replaces the two toy search logs the broken test left behind.

Run `run_granularity_bracket("mlp")` in-process from `/home/sraja/fpl` with the conda python, with NO monkeypatching, teeing stdout+stderr to a timestamped log under `data/processed/experiments/` (follow the existing `bracket_mlp_gate_*.log` naming, e.g. `bracket_mlp_gate_restore-$(date -u +%Y%m%dT%H%M%SZ).log`). This is a long run — roughly 90-180 s of GPU training plus feature-load time — so start it with the log in place and give the user a `tail -f` command for that exact log path while it runs.

Then compare the freshly written `bracket_gate_mlp.json` against the plan 10-11 oracle: `granularity == "pooled"`, `spearman_xp_med == 0.3942`, `mae_xp_med == 1.7805`, `n_played_rows == 11566`, `granularity_scores == {"per_position": 0.3924, "pooled": 0.3942}`, `status == "ok"`, `val_season == "2024-25"`, `train_seasons == list(config.TRAIN_SEASONS)`, and `params` equal to the winning pooled config (`hidden` [64, 32], `dropout` 0.1, `lr` 0.002, `weight_decay` 0.0).

HALT CONDITION — if the re-run does not reproduce those numbers, STOP and report the delta to the user as a blocker. Do not hand-edit the JSON to the expected values and do not proceed to Task 3: a mismatch means the environment no longer reproduces plan 10-11 (plausible given WINDOWS entry 5's env damage), which is a finding the user must see, not something to paper over.

Once the numbers match, delete the stray `data/processed/experiments/bracket_search_mlp_unit_test.json` left by the pre-fix `test_mlp_search_respects_budget` (Task 1 stops it from ever being recreated).
  </action>
  <verify>
    <automated>cd /home/sraja/fpl && /home/sraja/miniconda3/envs/python314/bin/python -c "
import json, config
r = json.loads((config.EXPERIMENTS_DIR / 'bracket_gate_mlp.json').read_text())
assert r['status'] == 'ok', r
assert r['granularity'] == 'pooled', r['granularity']
assert r['spearman_xp_med'] == 0.3942, r['spearman_xp_med']
assert r['mae_xp_med'] == 1.7805, r['mae_xp_med']
assert r['n_played_rows'] == 11566, r['n_played_rows']
assert r['granularity_scores'] == {'per_position': 0.3924, 'pooled': 0.3942}, r['granularity_scores']
assert r['val_season'] == '2024-25', r['val_season']
assert r['train_seasons'] == list(config.TRAIN_SEASONS), r['train_seasons']
p = json.loads((config.EXPERIMENTS_DIR / 'bracket_search_mlp_pooled.json').read_text())
assert len(p['configs']) == 12, len(p['configs'])
q = json.loads((config.EXPERIMENTS_DIR / 'bracket_search_mlp_per_position.json').read_text())
assert len(q['configs']) == 12, len(q['configs'])
print('RESTORED', r['granularity'], r['spearman_xp_med'])
" && test ! -e data/processed/experiments/bracket_search_mlp_unit_test.json</automated>
  </verify>
  <done>`bracket_gate_mlp.json` holds the real pooled/0.3942 measurement with its winning config, both MLP search logs hold their real 12-config searches, the stray unit-test search artifact is gone, and the run's log file is on disk as evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Prove suite-wide stability and close WINDOW-6</name>
  <files>.planning/WINDOWS.md</files>
  <action>
Hash the restored `data/processed/experiments/bracket_gate_mlp.json`, run the FULL suite (`python -m pytest -q` from `/home/sraja/fpl`, no path filter, so `pytest.ini`'s `testpaths` picks up everything), then hash again — the restored measurement must survive the whole suite byte-for-byte. That is the actual WINDOW-6 claim, and it is only provable after Tasks 1 and 2 are both done. The suite is slow (it includes the real `run_gate("lgbm")` module fixture); give the user a progress note rather than letting it look hung.

If the full suite surfaces a failure unrelated to this change, report it — do not fix unrelated failures inside this quick task.

Then mark the ledger entry resolved with the tool rather than editing the table by hand:
`node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" windows fixed 6`
(the `windows` verb accepts `status`, `append`, `waive`, `fixed`; it rewrites both the table row and the trailing JSON block and bumps `open_count`/`fixed_count`).

Commit only the two tracked files — `tests/test_bracket.py` and `.planning/WINDOWS.md`. Everything under `data/processed/` is gitignored and must not be force-added.
  </action>
  <verify>
    <automated>cd /home/sraja/fpl && B=$(sha256sum data/processed/experiments/bracket_gate_mlp.json | cut -d' ' -f1) && /home/sraja/miniconda3/envs/python314/bin/python -m pytest -q && A=$(sha256sum data/processed/experiments/bracket_gate_mlp.json | cut -d' ' -f1) && [ "$B" = "$A" ] && node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" windows status --raw | /home/sraja/miniconda3/envs/python314/bin/python -c "
import json, sys
d = json.load(sys.stdin)
e = next(r for r in d['ledger']['entries'] if str(r['id']) == '6')
assert e['status'] == 'fixed', e
print('WINDOW-6', e['status'])
" && echo STABLE</automated>
  </verify>
  <done>Full pytest suite green, `bracket_gate_mlp.json` byte-identical across that entire run, WINDOWS entry 6 status `fixed`, and `tests/test_bracket.py` + `.planning/WINDOWS.md` committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test suite → measured artifacts | `data/processed/experiments/*.json` are measured deliverables read by later plans; test-run side effects cross into them |
| planning record → measured value | A number recalled from a summary could be typed into an artifact that is supposed to be measured |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-7fy-01 | Tampering | `tests/test_bracket.py` toy run writing `config.EXPERIMENTS_DIR` | high | mitigate | Task 1 redirects `config.EXPERIMENTS_DIR` to `tmp_path` in both offending tests; Task 3 proves suite-wide byte stability by sha256 |
| T-7fy-02 | Tampering | `bracket_gate_mlp.json` restored by hand to a remembered number | high | mitigate | Task 2 restores by re-running the real gate and HALTS on any mismatch instead of hand-writing values; the 10-11 log is the read-only oracle |
| T-7fy-03 | Repudiation | Restored value with no provenance | medium | mitigate | Task 2 tees the restore run to a timestamped `bracket_mlp_gate_restore-*.log` beside the original 10-11 log |
| T-7fy-04 | Tampering | Gitignored artifacts force-added into the repo | low | mitigate | Task 3 commits only `tests/test_bracket.py` and `.planning/WINDOWS.md`; no `git add -f` |
| T-7fy-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs in this plan — nothing to audit (and WINDOWS entry 5 bars `uv pip sync` against the shared env regardless) |
</threat_model>

<verification>
1. `tests/test_bracket.py` passes with all its original assertions intact (nothing weakened to make isolation easy).
2. Running the full suite twice in a row leaves `data/processed/experiments/bracket_gate_mlp.json` byte-identical.
3. The live gate file's values are reproduced by a real `run_granularity_bracket("mlp")` call, corroborated by `bracket_mlp_gate_final-20260910T190818Z.log`.
4. `git status --porcelain data/processed/` stays empty (gitignored artifacts never enter the commit).
</verification>

<success_criteria>
- The suite no longer writes any file under `data/processed/experiments/` except the two legitimate real gate runs (`bracket_gate_lgbm.json` via the module fixture).
- `bracket_gate_mlp.json`: `granularity` pooled, `spearman_xp_med` 0.3942, measured not typed.
- WINDOWS.md entry 6 status `fixed`; `open_count` decremented.
- `python -m pytest -q` green from `/home/sraja/fpl`.
</success_criteria>

<output>
Create `.planning/quick/260911-7fy-fix-test-granularity-bracket-writes-gate/260911-7fy-SUMMARY.md` when done.
</output>
