---
phase: quick-260911-8as
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.in
  - requirements.txt
  - requirements-dev.txt
  - tests/test_transfermarkt.py
autonomous: false          # Task 1 is a blocking-human package-legitimacy gate
requirements: [DEPLOCK-01]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low          # estimate-calibration: sample_count 0, factor 1.0

must_haves:
  truths:
    - "DEPLOCK-01: a from-lockfile-only rebuild of the environment yields a working `pandas.read_html` on BOTH dispatch legs — the `lxml` leg and the `bs4`+`html5lib` fallback leg — so data/transfermarkt.py's backfill and data/fbref.py's scraper cannot crash on a missing parser again."
    - "`requests` TLS verification resolves a real, non-empty CA bundle that comes from a certifi pin the project lockfile owns directly, not only transitively."
    - "Recompiling requirements.txt changes NO version of any package already pinned there — the diff is additive only, with zero collateral upgrades to pandas/fastapi/lightgbm/pulp or anything else in the production image."
    - "Every package pinned in two or more of the four lockfiles is pinned at the identical version, so the `-c requirements.txt` layering that requirements-dev/-rl/-experiments were compiled under still holds and those two untouched locks stay valid."
    - "CI fails if either parser leg goes missing again — the guard runs in the ci.yml test job, not only on the developer machine."
    - "Docker's compiler-free builder stage (Dockerfile:14-23, D-03) can still install every pin in requirements.txt as a prebuilt wheel."
    - "`python -m pytest` is green across the whole suite."
  artifacts:
    - "requirements.in — direct pins for lxml, beautifulsoup4, html5lib, certifi with a comment explaining why nothing imports them by name"
    - "requirements.txt — recompiled, hash-locked, additive diff only"
    - "requirements-dev.txt — recompiled (it is `-r requirements.in`, so it inherits the four pins)"
    - "tests/test_transfermarkt.py — a permanent both-flavors + CA-bundle regression gate"
  key_links:
    - "requirements.in -> requirements.txt (uv pip compile) -> Dockerfile:23, daily.yml:21, weekly.yml:25, ci.yml:47/79/119. Anything added to requirements.in lands in the production image and in every workflow — this is why the Docker wheel check in Task 2 is load-bearing, not ceremonial."
    - "requirements.in -> requirements-dev.in line 1 (`-r requirements.in`) -> requirements-dev.txt -> ci.yml's test job. This is the link that makes Task 3's new test a real CI gate rather than a local-only assertion."
    - "pandas.io.html._validate_flavor(None) -> ('lxml','bs4') -> _parser_dispatch: the 'lxml' branch imports lxml.etree; the 'bs4' branch imports html5lib AND bs4. Both legs are live at data/transfermarkt.py:189 and data/fbref.py:55 because both call read_html without a flavor argument."
    - "tests/test_transfermarkt.py:46 (test_parse_injury_table_on_saved_fixture) already exercises read_html — but its clean HTML always succeeds on the lxml leg, so the bs4 fallback leg is never reached. That is exactly how the missing html5lib/beautifulsoup4 survived CI; Task 3's test pins each leg explicitly."
---

<objective>
Give `data/transfermarkt.py`'s scraping stack a home in the project lockfiles so it survives an environment rebuild: add direct `lxml`, `beautifulsoup4`, `html5lib` and `certifi` pins to `requirements.in`, recompile `requirements.txt` and `requirements-dev.txt` with the project's recorded `uv pip compile` commands, and add a CI-visible regression gate that proves both `pandas.read_html` parser legs are installable.

Purpose: WINDOWS.md entries 4-5 record a `uv pip sync` against the shared conda env that stripped it to the lockfiles. The Transfermarkt backfill then crashed three separate times — on certifi, then html5lib, then beautifulsoup4 — plus a fourth ad-hoc `lxml` reinstall during recovery. All four were ad-hoc installs in no lockfile. Nothing in this repo imports them by name (they are reached only through `pandas.read_html`'s internal dispatch), so they are invisible to a reader, to a dependency scanner, and to the existing test suite. This closes that gap permanently.
Output: Four direct pins, two recompiled hash-locked files, one regression test, a green suite.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/WINDOWS.md
@requirements.in
@requirements-dev.in
@tests/test_transfermarkt.py
</context>

<environment_facts>
Observed live at planning time on this machine. Do not re-derive these — but DO re-verify the two marked RE-CHECK, since they are load-bearing and cheap.

**Interpreter / tooling (absolute paths — use these, the shell's `python` is not this env):**
- `PY=/home/sraja/miniconda3/envs/python314/bin/python` (Python 3.14, conda env `python314`)
- `UV=/home/sraja/miniconda3/envs/python314/bin/uv` (uv 0.12.9, already installed in this env)
- This shell has a `grep`-to-`ugrep` shim whose flag semantics differ from GNU grep (Phase 06-04 precedent). Every grep in this plan is written as `command grep` deliberately. Keep it that way.
- Run pytest as `$PY -m pytest`, never bare `pytest` — bare `pytest` omits CWD from `sys.path` (Phase 05-05 precedent, commit 11cd68e).

**Lockfile layering (read from the four `# This file was autogenerated by uv` headers):**
- `uv pip compile requirements.in --generate-hashes --python-version 3.14 -o requirements.txt`
- `uv pip compile requirements-dev.in --generate-hashes --python-version 3.14 -c requirements.txt -o requirements-dev.txt`
- `uv pip compile requirements-rl.in --generate-hashes --python-version 3.14 -c requirements.txt -o requirements-rl.txt`
- `uv pip compile requirements-experiments.in --generate-hashes --python-version 3.14 -c requirements.txt -c requirements-rl.txt -o requirements-experiments.txt`
- `requirements-dev.in` line 1 is `-r requirements.in`. `requirements-rl.in` and `requirements-experiments.in` have NO `-r` line — they pull nothing from requirements.in, so adding packages to requirements.in cannot pull those packages into their locks. Those two files are NOT recompiled by this plan, and the Task 3 pin-parity check is what proves that decision safe.

**Why `requirements.in` and not the other two `.in` files (the placement decision):**
- `requirements-dev.in` is labelled "Test-only" in its own comment and holds pytest/ruff/responses. `data/transfermarkt.py` and `data/fbref.py` are production data-pipeline modules in the `data/` package, module-level-imported by `data/build_table.py:17-19`. A production module's parser stack is not test-only.
- `requirements-rl.in` and `requirements-experiments.in` are both fenced by explicit recorded decisions (D-09 and D-16, quoted in their own headers): they must NEVER be merged into requirements.txt and must NEVER be installed by Docker or any workflow. Wrong home by construction.
- Cost check clears: `lxml` publishes a `cp314-cp314-manylinux_2_28_x86_64` wheel (confirmed from the installed dist-info WHEEL tag), and `python:3.14-slim` sits well above glibc 2.28, so Dockerfile's deliberately compiler-free builder stage (D-03) stays valid. `beautifulsoup4` and `html5lib` are pure-Python (`Root-Is-Purelib: true`). **RE-CHECK this in Task 2** — it is the fact that makes `requirements.in` the correct home.

**Currently installed, working versions (the versions the backfill actually ran on):**
`certifi==2025.11.12`, `beautifulsoup4==4.15.0`, `html5lib==1.1`, `lxml==6.1.3`, `soupsieve==2.9.2`, `webencodings==0.6.1`, `six==1.17.0`, `pandas==3.0.5`, `requests==2.32.5`.

**Current lockfile coverage (grep across all four `.txt` files):**
- `certifi==2025.11.12` IS already pinned in requirements.txt:17 and requirements-dev.txt:22 — transitively, `# via httpcore / httpx / requests / understatapi`. It is covered today; the direct pin added here documents the dependency and removes the transitive-only guarantee that failed last rebuild.
- `six==1.17.0` is already pinned (`# via python-dateutil`).
- `beautifulsoup4`, `html5lib`, `lxml`, `soupsieve`, `webencodings` are in **no lockfile at all**. These are the real gaps.
- Expected new top-level entries after recompile: beautifulsoup4, html5lib, lxml, soupsieve, webencodings. `certifi` and `six` should only gain a `# via` annotation line, not change version.

**Why both parser legs are genuinely required (read from the installed pandas 3.0.5 source, not assumed):**
- `pandas.io.html._validate_flavor(None)` returns `("lxml", "bs4")`.
- `pandas.io.html._parser_dispatch("lxml")` calls `import_optional_dependency("lxml.etree")`.
- `pandas.io.html._parser_dispatch("bs4")` calls `import_optional_dependency("html5lib")` AND `import_optional_dependency("bs4")` — html5lib is required *alongside* bs4, not instead of it.
- `_parse` walks the flavor tuple and falls back from lxml to bs4 when `parse_tables()` raises `ValueError`. Transfermarkt's real markup takes that fallback; the clean HTML in `tests/test_transfermarkt.py` does not. That asymmetry is the root cause of the gap surviving CI.
- Both call sites pass no flavor: `data/transfermarkt.py:189` and `data/fbref.py:55`.

**HARD PROHIBITION — the incident this task exists to close:**
Never run `uv pip sync` (or `pip-sync`) against this shared conda env. It treats the whole env as the sync target and removes every package outside the named lockfile. Use `uv pip install --require-hashes -r <file>` — one file per invocation, exactly the form that was proven during the WINDOWS-5 recovery.
</environment_facts>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package-legitimacy gate for the four new direct pins</name>
  <what-built>
Nothing is built yet — this gate runs BEFORE any file edit, because the four packages are about to enter `requirements.in`, and therefore the production Docker image (Dockerfile:23) and every GitHub Actions workflow (daily.yml:21, weekly.yml:25, ci.yml:47/79/119).

This project has run a blocking-human package-legitimacy gate for every new package since Phase 01 (Phase 01's 13-package set, Phase 02's four npm packages, Phase 05's uv/ruff, Phase 09's four RL packages, Phase 10's xgboost/catboost), with the verbatim approval recorded in STATE.md each time. A quick task has no RESEARCH.md audit table, so the fallback policy applies: all four are treated as [ASSUMED] until a human confirms them.

Before presenting, re-verify each against the live registry and report any drift from the installed version:
- https://pypi.org/project/lxml/ — installed 6.1.3
- https://pypi.org/project/beautifulsoup4/ — installed 4.15.0
- https://pypi.org/project/html5lib/ — installed 1.1
- https://pypi.org/project/certifi/ — installed 2025.11.12, already present in requirements.txt:17 transitively via requests/httpx/understatapi
  </what-built>
  <how-to-verify>
For each of the four packages, confirm from the pypi.org project page:
1. Release history and project age consistent with a long-established library (not a days-old project).
2. Maintainer/owner matches the known upstream for that library.
3. Distribution name vs import name. `beautifulsoup4` imports as `bs4` — a legitimate, decades-old mismatch, but call it out explicitly, because a name/import mismatch is also the classic typosquat shape and is the one entry here that deserves a deliberate look.
4. That all three non-certifi packages are already installed and in active use in this env (they are what the Transfermarkt backfill has been running on), so this pins existing reality rather than introducing new code.

Scope reminder for the reviewer: approving puts `lxml` (a C extension), `beautifulsoup4` and `html5lib` into the shipped API image, not just the developer machine. `certifi` is already in that image today.

This gate is never auto-approvable — it is not covered by `workflow.auto_advance`, and not covered by the project-wide `end-of-phase` human-verify mode.
  </how-to-verify>
  <resume-signal>Type "approve all four" (or name which to reject, and the plan halts with the rejection recorded). Capture the verbatim answer for the SUMMARY.</resume-signal>
</task>

<task type="tracer" tdd="false">
  <name>Task 2: Pin the four packages and recompile both affected lockfiles end-to-end</name>
  <precondition>Network access to pypi.org is available — `uv pip compile` must reach the index to resolve and hash the new packages, and the Docker wheel check downloads one wheel.</precondition>
  <reversibility rating="reversible">Placement in requirements.in rather than requirements-dev.in is undone by moving the four lines and re-running the same two compile commands.</reversibility>
  <files>requirements.in, requirements.txt, requirements-dev.txt</files>
  <action>
This is the end-to-end slice: it touches every layer this task will modify — the source `.in`, the compiled lock, the installed environment, and a live `read_html` through both dispatch legs — so an architectural problem (a missing wheel, a collateral upgrade) surfaces now rather than after three commits.

**Step 1 — edit `requirements.in`.** Append a new section after the existing "Product layer" block, following this file's established heavy-comment convention (see how `understatapi` and `pulp` document their pins). Use version ranges, not exact pins: the surrounding production dependencies use ranges (`pandas>=2.2`, `scipy>=1.12`) and the lockfile supplies exact reproducibility. Reserve `==` for the cases that have a specific documented reason, as `understatapi==0.7.1` does.

The section must pin `lxml>=6.1,<7`, `beautifulsoup4>=4.13`, `html5lib>=1.1` and `certifi>=2025.11.12`, with a comment block explaining: that these reach the code only through `pandas.read_html`'s internal flavor dispatch so nothing in the repo imports them by name; that both legs are live because both call sites pass no flavor argument; that html5lib is required alongside bs4 rather than instead of it; that certifi is already resolved transitively via requests/httpx/understatapi and is pinned directly because a transitive-only guarantee is what failed; and a do-not-remove-as-unused warning naming WINDOWS.md entries 4-5. The upper bound on lxml guards the C-extension major version, mirroring the reasoning already written for `pulp>=3.3,<4.0`.

**Step 2 — recompile, using the exact commands recorded in the two lock headers**, from the repo root:

    $UV pip compile requirements.in --generate-hashes --python-version 3.14 -o requirements.txt
    $UV pip compile requirements-dev.in --generate-hashes --python-version 3.14 -c requirements.txt -o requirements-dev.txt

uv prefers the versions already present in an existing output file, so this should produce an additive diff. Do NOT pass `--upgrade`. Do not recompile requirements-rl.txt or requirements-experiments.txt — neither `.in` reads requirements.in, so neither can gain these packages, and Task 3 proves their constraint surface is unchanged.

**Step 3 — if the additive-only gate below fails**, meaning uv re-resolved and moved unrelated production pins (pandas, fastapi, lightgbm, pulp, …): HALT and report. Do not commit a broad re-resolve under cover of a four-package change, and do not hand-edit the lock to force the diff back. That is a separate decision for the developer.

**Step 4 — install non-destructively into the shared conda env**, one file per invocation, exactly the form proven during the WINDOWS-5 recovery. The sync verb is prohibited here (see environment_facts).

Do not touch Dockerfile or any workflow file: they already install `requirements.txt`, so they pick these up with no edit.
  </action>
  <verify>
    <automated>
set -euo pipefail
cd /home/sraja/fpl
PY=/home/sraja/miniconda3/envs/python314/bin/python
UV=/home/sraja/miniconda3/envs/python314/bin/uv
SCRATCH=/tmp/claude-1000/-home-sraja-fpl/2826e980-67d8-4757-91f3-1b28c38f0b05/scratchpad

# --- Gate A: the recompile is ADDITIVE ONLY (no existing pin moved) ---------
# Removed lines that are not the diff's own "--- a/" header and not comments.
# A "# via" annotation block reflowing when certifi gains a direct requirement
# is an expected comment-only removal; a removed "name==version \" line is not.
#
# Non-vacuity guard FIRST: this gate is only meaningful against an uncommitted
# working tree. Run it BEFORE committing -- once committed, git diff is empty
# and every count below is trivially zero.
ADDED=$(git diff -U0 -- requirements.txt \
  | command grep -E '^\+' | command grep -v '^+++' | wc -l)
echo "requirements.txt added lines: $ADDED"
test "$ADDED" -gt 0 || { echo "GATE A VACUOUS: no uncommitted diff in requirements.txt -- run this gate before committing"; exit 1; }

REMOVED=$(git diff -U0 -- requirements.txt \
  | command grep -E '^-' \
  | command grep -v '^---' \
  | command grep -vE '^-[[:space:]]*#' \
  | wc -l)
echo "requirements.txt removed non-comment lines: $REMOVED"
test "$REMOVED" -eq 0

REMOVED_DEV=$(git diff -U0 -- requirements-dev.txt \
  | command grep -E '^-' \
  | command grep -v '^---' \
  | command grep -vE '^-[[:space:]]*#' \
  | wc -l)
echo "requirements-dev.txt removed non-comment lines: $REMOVED_DEV"
test "$REMOVED_DEV" -eq 0

# --- Gate B: the four packages are present, hash-locked, in BOTH locks ------
for f in requirements.txt requirements-dev.txt; do
  for pkg in lxml beautifulsoup4 html5lib certifi soupsieve webencodings; do
    command grep -qE "^${pkg}==" "$f" || { echo "MISSING $pkg in $f"; exit 1; }
  done
  echo "$f: all six entries present"
done
$PY - <<'EOF'
import re, pathlib, sys
for f in ("requirements.txt", "requirements-dev.txt"):
    text = pathlib.Path(f).read_text()
    for pkg in ("lxml", "beautifulsoup4", "html5lib", "certifi"):
        m = re.search(rf"^{pkg}==[^\s\\]+((?:\s*\\\n\s*--hash=sha256:[0-9a-f]+)+)", text, re.M)
        if not m:
            sys.exit(f"{f}: {pkg} has no --hash lines")
    print(f"{f}: hashes present for all four direct pins")
EOF

# --- Gate C: Docker's compiler-free builder can still use wheels only -------
# python:3.14-slim is the Dockerfile:14 builder base; D-03 keeps it compiler-free.
LXML_PIN=$(command grep -m1 -E '^lxml==' requirements.txt | sed 's/^lxml==//; s/[[:space:]]*\\$//')
echo "checking prebuilt wheel for lxml==$LXML_PIN on the Docker target"
rm -rf "$SCRATCH/wheelcheck" && mkdir -p "$SCRATCH/wheelcheck"
$PY -m pip download "lxml==$LXML_PIN" --no-deps --only-binary=:all: \
  --python-version 3.14 --implementation cp --abi cp314 \
  --platform manylinux_2_28_x86_64 -d "$SCRATCH/wheelcheck"
ls "$SCRATCH/wheelcheck"/lxml-*.whl

# --- Gate D: non-destructive install, one file per invocation ---------------
$UV pip install --require-hashes -r requirements.txt
$UV pip install --require-hashes -r requirements-dev.txt

# --- Gate E: END-TO-END — both read_html legs + the real project call site --
$PY - <<'EOF'
import io
import pandas as pd
import certifi, requests.certs
import data.transfermarkt as tm
import data.build_table  # noqa: F401 - module-level imports data.transfermarkt

html = "<table><tr><th>a</th><th>b</th></tr><tr><td>1</td><td>2</td></tr></table>"
for flavor in ("lxml", "bs4"):
    df = pd.read_html(io.StringIO(html), flavor=flavor)[0]
    assert df.shape == (1, 2), (flavor, df.shape)
    print(f"read_html flavor={flavor}: OK {df.shape}")

bundle = requests.certs.where()
assert bundle == certifi.where(), (bundle, certifi.where())
import os
assert os.path.getsize(bundle) > 100_000, os.path.getsize(bundle)
print(f"requests CA bundle -> certifi: OK ({bundle})")

rows = tm.parse_injury_table(
    "<table><thead><tr><th>Season</th><th>Injury</th><th>from</th>"
    "<th>until</th><th>Days</th><th>Games missed</th></tr></thead>"
    "<tbody><tr><td>24/25</td><td>Thigh strain</td><td>01/01/2025</td>"
    "<td>15/01/2025</td><td>14</td><td>3</td></tr></tbody></table>",
    what="plan 8as gate",
)
assert len(rows) == 1, rows
print("data.transfermarkt.parse_injury_table end-to-end: OK")
EOF
    </automated>
  </verify>
  <done>requirements.in carries the four documented direct pins. requirements.txt and requirements-dev.txt are recompiled with hashes, with zero non-comment line removals (no existing pin moved). A prebuilt cp314 manylinux wheel exists for the pinned lxml on the Docker builder's target platform. Both read_html legs, the requests-to-certifi CA link, and data.transfermarkt.parse_injury_table all work against a from-lockfile install. Committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Make both parser legs a permanent CI gate, then prove the suite and the lock layering</name>
  <files>tests/test_transfermarkt.py</files>
  <behavior>
    - Test 1 (`test_read_html_lxml_flavor_is_installed`): `pd.read_html(..., flavor="lxml")` parses a one-row table and returns the expected shape. Fails with ImportError if lxml is absent.
    - Test 2 (`test_read_html_bs4_flavor_is_installed`): `pd.read_html(..., flavor="bs4")` parses the same table. Fails with ImportError if EITHER html5lib or beautifulsoup4 is absent — pandas' bs4 branch requires both.
    - Test 3 (`test_requests_ca_bundle_resolves_to_certifi`): `requests.certs.where()` equals `certifi.where()`, the file exists, and is large enough to be a real CA bundle rather than a stub.
    - All three run offline, consistent with this file's existing zero-network posture.
  </behavior>
  <action>
Extend `tests/test_transfermarkt.py` with the three tests above. This file is the right home: it is already the offline test surface for `data/transfermarkt.py`'s scraping, and the CI test job (`ci.yml:47`, `ci.yml:79`) installs `requirements.txt` + `requirements-dev.txt` and runs the suite — which is what converts these from local assertions into a real gate.

Pin each flavor explicitly. The existing `test_parse_injury_table_on_saved_fixture` at line 46 does call `read_html`, but its clean HTML always succeeds on the lxml leg, so the bs4 fallback leg is never reached — that is precisely why a missing html5lib/beautifulsoup4 sailed through CI. An explicit `flavor=` argument on each test is the whole point; do not collapse them into one flavor-less call.

Update the module docstring: its first paragraph says "All three tests run with zero network access", which this change makes false. Reword it to cover the added dependency-availability tests without claiming a count that will go stale again.

Keep the shared one-row HTML string as a module-level constant next to the existing `_INJURY_TABLE_HTML`, matching this file's convention.

**On the TDD gate:** these tests characterize an environment invariant that Task 2 has already satisfied, so there is no production code to drive red-then-green — the same situation STATE.md records for Phase 01 Task 2 and Phase 06-01 Task 2. Instead, prove the gate genuinely detects the regression it claims to, using the RED-proof command in the verify block below: it hides `html5lib` from the import system and confirms the bs4 leg then raises. Record that observed ImportError message in the SUMMARY. A test that cannot be shown to fail is not a gate.
  </action>
  <verify>
    <automated>
set -euo pipefail
cd /home/sraja/fpl
PY=/home/sraja/miniconda3/envs/python314/bin/python

# --- Gate A: RED-proof — the bs4 leg really does need html5lib -------------
# Run OUTSIDE the suite (mutating sys.modules inside pytest would leak).
$PY - <<'EOF'
import sys, io
sys.modules["html5lib"] = None          # makes `import html5lib` raise ImportError
import pandas as pd
try:
    pd.read_html(io.StringIO("<table><tr><td>1</td></tr></table>"), flavor="bs4")
except ImportError as exc:
    print("RED-proof OK — bs4 leg requires html5lib:", exc)
    raise SystemExit(0)
raise SystemExit("RED-proof FAILED: bs4 flavor parsed without html5lib")
EOF

# --- Gate B: the three new tests pass ---------------------------------------
$PY -m pytest tests/test_transfermarkt.py -v

# --- Gate C: lockfile layering invariant across ALL FOUR locks --------------
# requirements-dev/-rl/-experiments were each compiled with -c requirements.txt.
# That contract holds only while every shared package pins to the same version.
# Passing this is what makes it correct to leave -rl/-experiments uncompiled.
$PY - <<'EOF'
import re, pathlib, sys

def pins(path):
    out = {}
    for name, ver in re.findall(r"^([A-Za-z0-9._-]+)==([^\s\\]+)",
                                pathlib.Path(path).read_text(), re.M):
        out[name.lower().replace("_", "-")] = ver
    return out

base = pins("requirements.txt")
bad = False
for other in ("requirements-dev.txt", "requirements-rl.txt", "requirements-experiments.txt"):
    o = pins(other)
    shared = set(base) & set(o)
    conflicts = {k: (base[k], o[k]) for k in shared if base[k] != o[k]}
    print(f"{other}: {len(shared)} shared pins, {len(conflicts)} conflicts")
    if conflicts:
        bad = True
        for k, (a, b) in sorted(conflicts.items()):
            print(f"  CONFLICT {k}: requirements.txt={a} {other}={b}")
if bad:
    sys.exit("lockfile layering broken: shared pins disagree with requirements.txt")
print("layering invariant holds across all four lockfiles")
EOF

# --- Gate D: whole suite green ----------------------------------------------
$PY -m pytest
    </automated>
  </verify>
  <done>tests/test_transfermarkt.py pins both read_html flavors and the requests-to-certifi CA link, its docstring no longer claims a stale test count, the RED-proof shows the bs4 gate detects a missing html5lib, every shared pin agrees across all four lockfiles, and the full `python -m pytest` suite is green. Committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI -> developer conda env | Untrusted third-party code enters the machine that holds the model artifacts and cron credentials |
| PyPI -> `requirements.txt` -> Docker image / GitHub Actions | The same code reaches the production image and every scheduled workflow run — requirements.txt is installed at Dockerfile:23, daily.yml:21, weekly.yml:25 and ci.yml:47/79/119 |
| transfermarkt.com -> `pd.read_html` (lxml / bs4) | Untrusted remote HTML is fed to a C-extension parser |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-8as-SC | Tampering | pip installs of lxml / beautifulsoup4 / html5lib / certifi | high | mitigate | No RESEARCH.md audit table exists for a quick task, so fallback policy treats all four as [ASSUMED]: Task 1 is a blocking-human package-legitimacy gate re-verifying each against pypi.org before any edit, explicitly flagging the bs4-vs-beautifulsoup4 import/name mismatch (the classic typosquat shape). Never auto-approvable. |
| T-8as-01 | Tampering | requirements.txt -> Docker image | high | mitigate | Every install path already uses `--require-hashes`; Task 2 Gate B asserts each of the four direct pins carries `--hash=sha256:` lines, so a swapped artifact fails the install rather than entering the image. |
| T-8as-02 | Tampering | `uv pip compile` re-resolve | medium | mitigate | A fresh full re-resolve could silently upgrade unrelated production pins (pandas, fastapi, lightgbm, pulp) under cover of a four-package change. Task 2 Gate A fails on any non-comment line removal in either lock, and the plan forbids committing a broad re-resolve — halt and report instead. |
| T-8as-03 | Spoofing / Information disclosure | `requests` TLS to transfermarkt.com | medium | mitigate | A missing certifi bundle is what makes a developer reach for `verify=False`, which would expose scraping traffic to MITM. Task 2 Gate E and Task 3 Test 3 both assert `requests.certs.where() == certifi.where()` and that the bundle is a real file, and certifi is now a direct pin rather than a transitive-only guarantee. |
| T-8as-04 | Denial of service | lxml / bs4 parsing untrusted remote HTML | low | accept | `pandas.read_html` uses lxml's HTML parser on already-fetched markup with no XML external-entity resolution and no DTD processing, so the XXE class does not apply. A hostile page could still be pathologically large; input arrives only through `data/transfermarkt.py`'s own throttled scraper on a developer machine, never from a user request, and `_CHALLENGE_PAGE_MARKERS` already rejects non-table responses before parsing. Accepted — this plan changes packaging only, not the parsing surface, which has been live since plan 10-05. |
| T-8as-05 | Elevation of privilege | `uv pip sync` against the shared conda env | high | mitigate | The WINDOWS-5 incident itself: sync treats the whole shared env as its target and removes everything outside the named lockfile. Prohibited in environment_facts and in Task 2's action; every install in this plan uses `uv pip install --require-hashes -r <file>`, one file per invocation. |

Note: the SUMMARY should record whether WINDOWS.md entry 5 warrants an update. This plan closes the *lockfile-gap* half of that incident (the ad-hoc installs now have a home) but not the *standing rule* half (never sync against the shared env), so entry 5 is not simply "fixed" — leave the ledger to a human decision rather than mutating it here.
</threat_model>

<verification>
1. `git diff requirements.in` shows only the new documented section — no existing pin touched.
2. `git diff --stat requirements.txt requirements-dev.txt` shows insertions dominating, with removals limited to reflowed `# via` comment blocks.
3. `lxml`, `beautifulsoup4`, `html5lib`, `certifi`, `soupsieve`, `webencodings` all appear hash-locked in both requirements.txt and requirements-dev.txt.
4. A prebuilt cp314 manylinux wheel exists for the pinned lxml — Dockerfile's compiler-free builder stage (D-03) stays valid.
5. `pd.read_html` works under `flavor="lxml"` and under `flavor="bs4"` after a from-lockfile install.
6. `requests.certs.where()` resolves to the pinned certifi bundle.
7. Shared pins agree across all four lockfiles, so requirements-rl.txt and requirements-experiments.txt remain valid uncompiled.
8. `$PY -m pytest` green.
9. Dockerfile and `.github/workflows/*.yml` are unmodified — `git status` must show no change to them.
</verification>

<success_criteria>
- The exact crash sequence from WINDOWS.md entries 4-5 (certifi, then html5lib, then beautifulsoup4, plus the ad-hoc lxml reinstall) cannot recur from a lockfile-only rebuild.
- The bs4 fallback leg — the one the existing suite never reached — is now covered by a test that has been demonstrated to fail when html5lib is missing.
- No production pin moved, no workflow or Dockerfile edit was needed, and the two fenced experiment lockfiles were neither recompiled nor contaminated.
- Full test suite green.
</success_criteria>

<output>
Create `.planning/quick/260911-8as-add-certifi-beautifulsoup4-html5lib-and-/260911-8as-SUMMARY.md` when done.

Record in the SUMMARY: the verbatim Task 1 human approval and any registry drift found; the observed RED-proof ImportError message; the exact removed/added line counts for both recompiled locks; and the WINDOWS-5 ledger note from the threat model.
</output>
