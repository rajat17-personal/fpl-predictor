/* Placeholder so Team.tsx's three-tab shell has a complete import surface
 * for Task 1 (03-02-PLAN.md). Task 3 of this plan replaces this file with
 * the full on-demand rating + four vanilla tiles. */
export interface RateTabProps {
  entry: number | null;
}

export function RateTab(_props: RateTabProps) {
  return null;
}

export default RateTab;
